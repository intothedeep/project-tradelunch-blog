# Tasks — tradelunch / dashboard_client_web

**Last updated:** 2026-05-06
**Active phase:** Phase 2 — Financial Dashboard MVP
**Branch:** feature/finance

---

## Phase 0 — Workflow Baseline

- [x] Create `00.plan.md` at repo root
- [x] Create `00.tasks.md` at repo root
- [ ] Create `01.status.md` at repo root (engineer agent responsibility)

---

## Phase 1 — Blog MVP

- [x] Blog post list server-side render (first 10 posts via server action)
- [x] Infinite scroll load-more via route handler `/api/posts/load-more`
- [x] Per-post Markdown rendering (GFM, KaTeX, syntax highlighting)
- [x] Loading skeleton for post list
- [x] Desktop sticky navigation bar
- [x] Mobile bottom drawer navigation
- [x] Dark/light theme toggle with system default
- [x] next-intl i18n setup and locale routing
- [x] Favicon set and OG/Twitter metadata
- [x] Fix hydration error from `new Date()` in server component
- [x] Apply React `cache()` to post fetching
- [x] Dashboard grid layout scaffold (62343d9)

---

## Phase 2 — Financial Dashboard MVP

### 2.1 Grid Layout Shell
- [x] `FinancialGridLayout.client.tsx` with responsive breakpoints (lg 12 / md 8 / sm 4 cols)
- [x] `useElementSize.hook` for container-aware layout width
- [x] Drag handle config (`.drag-handle` class, threshold 3px)
- [x] SE resize handle per widget
- [x] Widget barrel export (`components/dashboard/widgets/index.ts`)

### 2.2 Widget Set — Mock Data
- [x] `CurrencyWidget.client.tsx` — pair, rate, change, sparkline array
- [x] `CommodityWidget.client.tsx` — symbol, name, price, change, sparkline array
- [x] `StockWidget.client.tsx` — ticker, price, change, volume
- [x] `StockGroupWidget.client.tsx` — grouped tickers table
- [x] `Company13FWidget.client.tsx` — holdings table with CIK and reportDate
- [x] `InterestRatesWidget.client.tsx` — global central bank rates table
- [x] `TreasuryWidget.client.tsx` — US yield curve table
- [x] `GridCard.client.tsx` — shared card shell with drag handle

### 2.3 Sparkline Chart Rendering
- [ ] Implement sparkline `LineChart` (recharts) inside `CurrencyWidget`
  - Acceptance: chart renders 20-point line, no axes, respects theme CSS token colors, does not throw on empty array
- [ ] Implement sparkline `LineChart` inside `CommodityWidget`
  - Acceptance: same criteria as CurrencyWidget sparkline
- [ ] Confirm `StockWidget` does not need sparkline (volume text is sufficient) — decision required

### 2.4 Dashboard Navigation Entry
- [ ] Add "Dashboard" entry to `LINKS` array in `navigation-desktop.tsx`
  - Acceptance: link `{ title: 'Dashboard', href: '/dashboard' }` appears in desktop nav and mobile drawer; navigates to `/dashboard`; active route receives visual highlight matching existing link style

### 2.5 13F Staleness Indicator
- [ ] Format `reportDate` string to human-readable label in `Company13FWidget`
  - Acceptance: "2024-Q4" renders as "Q4 2024" or equivalent readable format
- [ ] Add staleness badge if filing is older than 90 days from today
  - Acceptance: badge appears with label "Stale" when `reportDate` is more than 90 days old relative to current date; no badge when within 90 days; threshold is a named constant, not a magic number

### 2.6 Real Data Integration
- [ ] Create `env.schema.ts` using Zod to validate all financial API keys
  - Acceptance: build fails with descriptive error if required env variable is missing; no secrets hardcoded in source
- [ ] Create `getFinancialData.api.ts` — real data fetcher replacing mock
  - Acceptance: function returns `IFinancialDashboardData`; types match existing mock interface exactly; network errors throw typed errors, not raw exceptions
- [ ] Integrate FX rates from a real source (e.g., open.er-api.com or exchangerate-api.com)
  - Acceptance: `currencies` array populated from live API; at minimum USD/KRW, USD/EUR, USD/JPY, USD/CNY
- [ ] Integrate commodities and stock prices from a real market data source
  - Acceptance: `stocks` and `commodities` arrays populated from live API; stale time in `useFinancialDataQuery` remains 10s, refetch interval 30s
- [ ] Integrate US Treasury yields from FRED or US Treasury API
  - Acceptance: `treasuries` array populated from live API; 2Y, 5Y, 10Y, 30Y maturities present
- [ ] Integrate global interest rates from FRED or central bank API
  - Acceptance: `interestRates` array populated from live API; at minimum US, Eurozone, UK, Japan, South Korea
- [ ] Integrate 13F holdings from SEC EDGAR API
  - Acceptance: `companies13F` array populated from SEC EDGAR full-text search API; at minimum 2 companies; CIK matches EDGAR record

### 2.7 Error and Loading State Hardening
- [ ] Replace plain-text loading strings in `InterestRatesWidget` and `TreasuryWidget` with skeleton rows
  - Acceptance: loading state shows 5 skeleton rows matching table column widths; no layout shift when real data loads
- [ ] Confirm all 7 widgets render a user-facing fallback (not raw error message) on API failure
  - Acceptance: each widget in error state shows a card with title preserved and a non-red-500 inline message (e.g., "Data unavailable"); no `console.error` leaks to production

---

## Phase 3 — Production Hardening (PLANNED)

- [ ] Audit `_scripts/` directory — determine purpose and move to correct location or delete
  - Acceptance: either contents are moved to a named `packages/` subpackage with a `package.json`, or the directory is removed and the deletion is committed
- [ ] Resolve `.agent/rules/nexjs.rule.md` duplication with `.claude/rules/nexjts.md`
  - Acceptance: exactly one canonical Next.js rule file exists; references updated in any agents that consume it
- [ ] Decide language policy for `docs/`
  - Acceptance: `docs/doc.nextjs.api.actions.md` is either translated to English and kept in `docs/`, or moved to `.claude/` if agent-facing only
- [ ] Verify `article_server` workspace status
  - Acceptance: either `apps/article_server/` exists with a valid `package.json`, or the workspace entry is removed from root `package.json` and `turbo.json` is updated accordingly
- [ ] Add `env.schema.ts` build-time validation gate to CI (if CI pipeline is added)
  - Acceptance: CI step runs `check-types` and fails if env schema is violated

---

## Phase 4 — Package Manager Migration (pnpm 9.5.0)

> USER RESPONSIBILITY: After all tasks below are complete, the user must manually delete `yarn.lock` and all `node_modules` directories, then run `pnpm install`. Engineer agents must NOT run install or delete node_modules.

### Group A — Config Edits (root)

- [ ] Set `"packageManager": "pnpm@9.5.0"` in root `package.json` and remove the `"workspaces"` field
  - File: `package.json` (root)
  - Acceptance: `package.json` contains `"packageManager": "pnpm@9.5.0"`, no `"workspaces"` key is present

- [ ] Fix broken `dev:web` script target in root `package.json` (currently targets non-existent `web` workspace)
  - File: `package.json` (root)
  - Acceptance: `dev:web` script targets the correct workspace name that exists under `apps/`; `pnpm run dev:web --filter` resolves without "workspace not found" error

- [ ] Fix broken `dev:server` script target in root `package.json` (currently targets non-existent `article_server` workspace)
  - File: `package.json` (root)
  - Acceptance: `dev:server` either targets a workspace that exists under `apps/` or is removed; no dangling script references remain

- [ ] Create `pnpm-workspace.yaml` at repo root listing all workspace globs
  - File: `pnpm-workspace.yaml` (new)
  - Acceptance: file exists at repo root; contains `packages:` block with glob patterns covering `apps/*` and `packages/*`; pnpm recognizes all existing workspaces when `pnpm ls -r` is run after install

- [ ] Create `.npmrc` at repo root enforcing strict isolation
  - File: `.npmrc` (new or edit)
  - Acceptance: file contains `node-linker=isolated` (or equivalent strict default); does NOT contain `shamefully-hoist=true` or `node-linker=hoisted`; `strict-peer-dependencies=false` included to prevent peer-dep noise from blocking install

### Group B — Workspace package.json Edits

- [ ] Remove `yarn` from the `build` script in `apps/dashboard_client_web/package.json`
  - File: `apps/dashboard_client_web/package.json`
  - Acceptance: `build` script value does not contain the string `yarn`; script is syntactically valid JSON; running `pnpm run build` in the workspace would invoke the intended command without a yarn subprocess

- [ ] Remove `yarn run clean &&` prefix from `build`, `deprecated:dev`, and `prepublishOnly` scripts in `packages/markdown-parsing/package.json`
  - File: `packages/markdown-parsing/package.json`
  - Acceptance: all three script values do not contain the string `yarn`; `clean` step is either replaced with `pnpm run clean` or dropped if no `clean` script exists; scripts are syntactically valid JSON

- [ ] Remove `yarn run clean &&` prefix from the `build` script in `packages/axios/package.json`
  - File: `packages/axios/package.json`
  - Acceptance: `build` script value does not contain the string `yarn`; `clean` step is either replaced with `pnpm run clean` or dropped if no `clean` script exists; script is syntactically valid JSON

### Group C — Documentation Edits

- [ ] Update yarn references in root `CLAUDE.md`
  - File: `CLAUDE.md` (root)
  - Acceptance: no occurrence of `yarn install`, `yarn build`, `yarn run`, or `yarn add` remains in the file; equivalent pnpm commands are substituted where needed

- [ ] Update yarn references in `apps/dashboard_client_web/README.md`
  - File: `apps/dashboard_client_web/README.md`
  - Acceptance: no occurrence of `yarn` as a command remains; pnpm equivalents are substituted

- [ ] Update yarn references in `apps/docs/README.md`
  - File: `apps/docs/README.md`
  - Acceptance: no occurrence of `yarn` as a command remains; pnpm equivalents are substituted

- [ ] Update yarn references in `packages/markdown-parsing/README.md`
  - File: `packages/markdown-parsing/README.md`
  - Acceptance: no occurrence of `yarn` as a command remains; pnpm equivalents are substituted

- [ ] Update yarn references in `apps/dashboard_server/docs/node.server.md`
  - File: `apps/dashboard_server/docs/node.server.md`
  - Acceptance: no occurrence of `yarn` as a command remains; pnpm equivalents are substituted

- [ ] Update yarn references in `docs/guide.spec.md`
  - File: `docs/guide.spec.md`
  - Acceptance: no occurrence of `yarn` as a command remains; pnpm equivalents are substituted

### Group D — Permissions / Settings

- [ ] Replace yarn permission entries in `.claude/settings.local.json`
  - File: `.claude/settings.local.json`
  - Acceptance: `Bash(yarn install:*)` and `Bash(yarn build)` entries are removed; `Bash(pnpm install:*)` and `Bash(pnpm build)` are added in their place; JSON remains valid

### Group E — User-Run Install (USER RESPONSIBILITY — do not automate)

- [ ] USER: Delete `yarn.lock` at repo root
  - Acceptance: `yarn.lock` does not exist in the repo after this step

- [ ] USER: Delete `node_modules` at repo root and in each workspace (`apps/*`, `packages/*`)
  - Acceptance: no `node_modules` directory exists anywhere in the repo tree after this step

- [ ] USER: Run `pnpm install` from repo root
  - Acceptance: command exits 0; `pnpm-lock.yaml` is generated at repo root; no install error is printed to stdout

### Group F — Verification (run after user completes Group E)

- [ ] Verify `pnpm-lock.yaml` exists and `yarn.lock` is absent
  - Acceptance: `pnpm-lock.yaml` is present at repo root; `yarn.lock` is absent from repo root; both conditions are true simultaneously

- [ ] Verify all workspaces are recognized by pnpm
  - Command: `pnpm ls -r --depth 0`
  - Acceptance: output lists every workspace defined in `pnpm-workspace.yaml` with no "workspace not found" or "ERR_PNPM" errors

- [ ] Verify Turborepo dev pipeline starts without module-not-found errors
  - Command: `turbo dev` (or `pnpm turbo dev`) from repo root
  - Acceptance: all workspace dev servers start; no `Cannot find module` or `ERR_MODULE_NOT_FOUND` error appears in the first 30 seconds of output

- [ ] Verify TypeScript type-check passes in `apps/dashboard_client_web`
  - Command: `pnpm --filter dashboard_client_web run check-types` (or equivalent script name)
  - Acceptance: command exits 0 with no TypeScript errors; phantom-dep-related type errors (if any) have been resolved by adding the missing dep to the correct `package.json`

- [ ] Verify build succeeds in `apps/dashboard_client_web`
  - Command: `pnpm --filter dashboard_client_web run build`
  - Acceptance: command exits 0; `.next/` output directory is produced; no missing module errors during build
