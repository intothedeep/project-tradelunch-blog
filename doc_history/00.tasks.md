# Tasks — tradelunch / dashboard_client_web

**Last updated:** 2026-05-07
**Active phase:** Phase 2-simple — Financial Dashboard MVP
**Branch:** feature/finance

---

## Phase 0 — Workflow Baseline

- [x] Create `00.plan.md` at repo root
- [x] Create `00.tasks.md` at repo root
- [ ] Create `01.status.md` at repo root (engineer agent responsibility)

---

## Phase 1 — Blog MVP

- [x] Blog post list server-side render (first 10 posts via server action)
- [x] Infinite scroll load-more via route handler `/api/posts/load-more`
- [x] Per-post Markdown rendering (GFM, KaTeX, syntax highlighting)
- [x] Loading skeleton for post list
- [x] Desktop sticky navigation bar
- [x] Mobile bottom drawer navigation
- [x] Dark/light theme toggle with system default
- [x] next-intl i18n setup and locale routing
- [x] Favicon set and OG/Twitter metadata
- [x] Fix hydration error from `new Date()` in server component
- [x] Apply React `cache()` to post fetching
- [x] Dashboard grid layout scaffold (62343d9)

---

## Phase 2-simple — Financial Dashboard MVP

### Cycle 2 — Display Variant Prototypes

**Goal:** Build two preview pages using static mock data so the user can choose a display style. No real API calls. Mock data must be shaped per the `IDashboardSnapshot` contract the architect defines.

**Target directory:** `apps/dashboard_client_web/app/dashboard/`

- [ ] Define `IDashboardSnapshot` type in `apps/dashboard_client_web/types/dashboard.ts`
  - Acceptance: type covers all 22 items (12 base + 10 stocks); each item has `label: string`, `value: number`, `changePercent: number`, `category: string`; no `any`; file exports the type as named export

- [ ] Create static mock file `apps/dashboard_client_web/apis/getDashboardSnapshot.mock.api.ts`
  - Acceptance: exports a single `IDashboardSnapshot` constant; all 22 items present by label; NCSOFT (`036570`) and SMR both appear; `changePercent` values are non-zero (positive and negative represented); no `any`

- [ ] Build Variant A preview page at `app/dashboard/preview/cards/page.tsx`
  - Acceptance: renders all 22 items as individual cards; each card shows label, value, signed change % with up/down arrow; green for positive change, red for negative; readable on 375px mobile and 1280px desktop without horizontal scroll; page uses `IDashboardSnapshot` mock data; no `any`

- [ ] Build Variant B preview page at `app/dashboard/preview/table/page.tsx`
  - Acceptance: renders one table per category (FX, Crypto, Indices, Interest Rates, Stocks); each row shows label, value, signed change % with up/down arrow; green for positive change, red for negative; readable on 375px mobile and 1280px desktop without horizontal scroll; page uses `IDashboardSnapshot` mock data; no `any`

- [ ] Verify both preview pages: NCSOFT and SMR both visible in each variant
  - Acceptance: `036570` / NCSOFT row visible in Stocks section of Variant B table; NCSOFT card visible in Variant A grid; SMR card/row visible in both variants

- [ ] Verify both preview pages: `pnpm --filter dashboard_client_web check-types` exits 0
  - Acceptance: command exits 0 with no TypeScript errors after adding preview pages

- [ ] Verify both preview pages: no hydration errors in browser console
  - Acceptance: browser console shows zero hydration warnings on hard reload of either preview page; all values are primitives (no `Date` objects passed as props)

### Cycle 2 — Variant C (Chart + Table Split)

> **v1 — recharts line chart (built and discarded 2026-05-07).** Initial build used `recharts` `<LineChart>` with 30-day close-only points. Worked, but user requested OHLC candlesticks + 90-day history + TradingView aesthetic, which pushes past recharts' sweet spot. Swapped to `lightweight-charts`. nof1.ai/models/31 was considered as a design reference but the editorial/parchment aesthetic conflicted with the trading-terminal direction the user picked.

- [x] Define history types in `apps/dashboard_client_web/types/history.ts`
- [x] Create mock history file `apps/dashboard_client_web/apis/getDashboardHistory.mock.api.ts` (v1 — line points, 30 days)
- [x] Create Jotai atom file `apps/dashboard_client_web/store/dashboard.atom.ts`
- [x] Build `ChartPanel.client.tsx` (v1 — recharts)
- [x] Build `HistoryTable.client.tsx`
- [x] Build `ChartVariantLayout.client.tsx`
- [x] Build Variant C preview page at `app/dashboard/preview/chart/page.tsx`
- [x] Verify v1: `check-types` exits 0
- [x] Verify v1: no hydration errors; click interaction works; default selection GOOGL/Alphabet
- [x] Verify v1: mobile responsive (`flex-col lg:flex-row`)

#### Cycle 2 Variant C — v2 swap (OHLC + lightweight-charts)

> **Decision (2026-05-07):** Swap chart library to `lightweight-charts` v5+ for OHLC candlestick rendering. Mock history expanded to 90 daily candles per item. ChartPanel re-implemented with imperative TradingView-style canvas API. `recharts` stays in deps because old widgets and `components/ui/chart.tsx` still reference it (slated for cleanup with the rest of the soft-delete in Cycle 3 Group D).

- [ ] Extend `types/history.ts` with `IOHLCPoint`, `IItemOHLCHistory`, `IDashboardOHLCHistory` (additive — keep existing line types in place)
  - Acceptance: new types exported alongside existing; `IOHLCPoint` has `time: string`, `open`, `high`, `low`, `close: number`; no `any`

- [ ] Replace mock with OHLC, 90 days
  - File: `apps/dashboard_client_web/apis/getDashboardHistory.mock.api.ts`
  - Acceptance: exports `MOCK_DASHBOARD_HISTORY: IDashboardOHLCHistory`; 22 labels × exactly 90 candles each; deterministic pseudo-random walk seeded by label index (no `Math.random()`); final candle's `close` ≈ snapshot's current value for that label; ISO `time` strings only

- [ ] Replace `ChartPanel.client.tsx` to use `lightweight-charts`
  - Acceptance: no `recharts` import; uses `createChart`, `CandlestickSeries`, `ColorType`; container with `ResizeObserver` (or window resize listener) to refit on viewport changes; cleans up via `chart.remove()` on unmount; up = green `#22c55e`, down = red `#ef4444`; dark background `#0d0e12`; ≤120 LOC; no `any`

- [ ] Add `lightweight-charts` to `dependencies` in `apps/dashboard_client_web/package.json`
  - Acceptance: `^5.0.0` added alphabetically; no other deps removed; user runs `pnpm install` to materialize

- [ ] USER: run `pnpm install` to materialize `lightweight-charts`
  - Acceptance: `node_modules/lightweight-charts` exists; dev server picks it up on next reload

- [ ] Verify v2: `pnpm --filter dashboard_client_web check-types` exits 0
- [ ] Verify v2: candles render for default selection (Alphabet); clicking each of 22 rows swaps the candle series within 100ms; NCSOFT and SMR rows render; chart re-fits on window resize
- [ ] Verify v2: mobile (375px) — chart stays at 400px height, no horizontal scroll on either pane

#### Cycle 2 Variant C — v3 (TradingView Essential Clone)

> **Decision (2026-05-07):** User provided a TradingView screenshot (`/tradingview.png`) as the reference target. Visual upgrade from "dark candlestick" (v2) to "TradingView terminal lookalike" (v3). nof1.ai/models/31 design path is permanently rejected — the project commits to the TradingView aesthetic. Skipped from full clone for KISS: drawing tools sidebar, indicators dropdown, alerts, bottom detail panel, real timeframe switching (buttons are display-only mocks).

- [ ] Add `volume: number` to `IOHLCPoint` in `apps/dashboard_client_web/types/history.ts`
  - Acceptance: type extended; downstream types still compile; no separate volume-only type introduced

- [ ] Add deterministic volume to mock candles in `getDashboardHistory.mock.api.ts`
  - Acceptance: each of the 22 × 90 candles has a `volume` value in roughly 100k–10M range; LCG-seeded; values vary plausibly (no flat lines)

- [ ] Create `apps/dashboard_client_web/utils/computeMA.ts` (pure helper)
  - Acceptance: signature `computeMA(closes: number[], period: number): (number|null)[]`; null-pads first `period-1` indices; ≤30 LOC; no `any`

- [ ] Create `apps/dashboard_client_web/components/dashboard/ChartHeader.client.tsx`
  - Acceptance: ticker + price + timeframe pill row (1m 5m 15m 30m 1h 4h D W M, D active); click handlers no-op; ≤80 LOC; no `any`

- [ ] Create `apps/dashboard_client_web/components/dashboard/ChartTimescale.client.tsx`
  - Acceptance: bottom row of buttons (1D 5D 1M 3M 6M YTD 1Y 5Y All, 6M active); UTC label + last-candle date on right; click handlers no-op; ≤50 LOC; no `any`

- [ ] Replace `ChartPanel.client.tsx` with TV-styled chart
  - Acceptance: candles + 3 MA lines (MA5 yellow #f5b942, MA20 blue #2962ff, MA50 purple #ab47bc) + volume histogram (separate scale, bottom 30%); current-price axis label visible; chart bg `#131722`, border `#2a2e39`, text `#d1d4dc`; ResizeObserver re-fit; ≤180 LOC; no `any`

- [ ] Replace `HistoryTable.client.tsx` with TV-style watchlist
  - Acceptance: WATCHING header + sticky column headers (Last, Chg, Chg%); category sections with ▼ caret; rows with 16px deterministic-hue circle icon + ticker + 3 numeric columns; selected row tint `rgba(41,98,255,0.10)`; ≤140 LOC; no `any`

- [ ] Update `ChartVariantLayout.client.tsx` to TV proportions
  - Acceptance: 70/30 split on `lg+`; outer bg `#131722`; 1px divider `#2a2e39` between chart and watchlist; ≤80 LOC; no `any`

- [ ] Verify v3: check-types exits 0; candles + MAs + volume render; clicking each row swaps everything; visual diff against `tradingview.png` is "close enough for essential clone"

#### Cycle 2 Variant C — v3 refactor for SRP / file size

> **Context (2026-05-07):** `ChartPanel.client.tsx` reached 489 LOC, violating both the v3 acceptance criterion (≤180 LOC) and the repo rule (≤300 LOC per file, `.claude/CLAUDE.md` §5.1). Three extraction modules have been written; `ChartPanel.client.tsx` itself still needs to be slimmed.

- [x] Extract chart formatters and `visibleStartIdx` to `utils/chart-format.ts`
  - Acceptance: file exists at `apps/dashboard_client_web/utils/chart-format.ts`; exports `formatPrice`, `formatVolume`, `formatOscillator`, `visibleStartIdx` as named exports; all functions are pure (no side effects); no `any`

- [x] Extract `Legend` to `components/dashboard/ChartLegend.client.tsx`
  - Acceptance: file exists at `apps/dashboard_client_web/components/dashboard/ChartLegend.client.tsx`; renders the hover-data legend previously inlined in `ChartPanel.client.tsx`; uses `"use client"` directive; no `any`

- [x] Extract chart setup into `hooks/useTradingViewChart.hook.ts`
  - Acceptance: file exists at `apps/dashboard_client_web/hooks/useTradingViewChart.hook.ts`; encapsulates `createChart` call, all series additions (candle, MA, volume, RSI, MACD), ResizeObserver, and crosshair subscribe; exposes `{ hoverIndex }`; no `any`

- [x] Slim `components/dashboard/ChartPanel.client.tsx` to orchestration only
  - Acceptance: file is ≤180 LOC; imports `useTradingViewChart` from `@/hooks/useTradingViewChart.hook`, `ChartLegend` from `@/components/dashboard/ChartLegend.client`, and helpers from `@/utils/chart-format`; reads `selectedLabelAtom` and `selectedRangeAtom` from Jotai; manages indicator toggle state for MA5/20/50/100/200, RSI, and MACD; manages gear menu open state with outside-click close; renders `ChartHeader`, `ChartTimescale`, `ChartConfigMenu`, and `ChartLegend`; no `recharts` import; no inline `Legend` definition; no inline `createChart` call; no `any`

- [ ] Verify refactor
  - Acceptance: `pnpm --filter dashboard_client_web check-types` exits 0; `wc -l apps/dashboard_client_web/components/dashboard/ChartPanel.client.tsx` reports ≤180; manual browser smoke test on `/dashboard/preview/chart` confirms candles + MAs + volume + RSI + MACD all render; clicking each watchlist row swaps the chart; gear menu toggles indicators; ResizeObserver re-fits on window resize

#### Cycle 2 Variant C — v3 refactor follow-up (architect Round 1)

> **Context (2026-05-07):** Architect reviewed the ChartPanel refactor (174 LOC, check-types passing) and identified 1 must-fix and 3 should-fix issues. Two nice-to-haves are explicitly deferred to a later cycle.

- [x] Remove duplicate `formatPrice` from `ChartHeader.client.tsx` (must-fix)
  - Acceptance: `apps/dashboard_client_web/components/dashboard/ChartHeader.client.tsx` imports `formatPrice` from `@/utils/chart-format`; the local `function formatPrice(...)` definition at the top of the file is removed; `pnpm --filter dashboard_client_web check-types` exits 0.

- [x] Hoist shared MA constants to a single location (should-fix)
  - Acceptance: `MA_PERIODS`, `MAPeriod`, `MAVisibility` declared once in `apps/dashboard_client_web/types/dashboard.ts`; the triplicate declarations in `ChartPanel.client.tsx`, `useTradingViewChart.hook.ts`, `ChartLegend.client.tsx`, and `ChartConfigMenu.client.tsx` (any of those that have them) replaced with type-only imports; check-types exits 0.

- [x] Replace `document.getElementById('chart-config-menu')` with a ref-based contract (should-fix)
  - Acceptance: `ChartPanel.client.tsx` no longer calls `document.getElementById`; the outside-click close handler uses a `menuRef: RefObject<HTMLDivElement | null>` either passed into `ChartConfigMenu` as a prop or forwarded; the wrapping `<div id="chart-config-menu" className="contents">` is removed; check-types exits 0; click-outside still closes the menu.

- [x] Group indicator state into `IndicatorState` (should-fix)
  - Acceptance: new exported interface `IndicatorState` in `apps/dashboard_client_web/types/dashboard.ts` with shape `{ maArrays: Record<MAPeriod, (number|null)[]>; rsiArr: (number|null)[]; macdResult: MACDResult; maVisible: MAVisibility; rsiVisible: boolean; macdVisible: boolean }`; `useTradingViewChart` parameter surface drops from 11 to 6 by accepting `indicators: IndicatorState` instead of the six individual indicator-related params; `ChartLegend` props update symmetrically; `ChartPanel` builds the object once and passes it to both; check-types exits 0.

**Deferred (not in scope for this cycle):**
- Split `selectedRange` out of the full-rebuild `useEffect` into its own effect that only calls `setVisibleLogicalRange` — performance win but no correctness impact, deferred to avoid scope creep.
- Extract `useCrosshairHover` from `useTradingViewChart` to separate lifecycle from interaction tracking — SRP polish with no user-visible change, deferred until hook grows further.

---

### Cycle 3 — Full Implementation

**Prerequisite:** User has reviewed Cycle 2 previews and selected the winning display variant (A, B, or C).

#### Group A — Type contract + validation

- [ ] Finalize `IDashboardSnapshot` type in `apps/dashboard_client_web/types/dashboard.ts` based on chosen variant needs
  - Acceptance: type is complete, exported, zero `any`; includes `fetchedAt: string` (ISO 8601) for the timestamp field

- [ ] Create `apps/dashboard_client_web/app/actions/getDashboardSnapshot.schema.ts` — Zod schema + inferred TS type (SRP: validation only, no fetching)
  - Acceptance: Zod schema covers all 22 items; inferred type is exported alongside the schema; invalid API response causes a typed error, not a runtime crash; schema shape matches `IDashboardSnapshot`; no `any`

#### Group B — Server fetcher

> **Data source rule (per Plan §Phase 2-simple → Resolved Decisions §5):** Server Actions read from the user's own backend (`dashboard_server` or future endpoint), NOT from external providers (Twelve Data, KRX OpenAPI, etc.) at request time. External ingestion is the backend's responsibility on a scheduled job. Until the backend is ready, the action returns the mock value used in Cycle 2 — flag-gated by env (e.g. `DASHBOARD_DATA_SOURCE=mock|backend`).

- [ ] Create `apps/dashboard_client_web/app/actions/getDashboardSnapshot.action.ts` — Server Action that imports schema from `getDashboardSnapshot.schema.ts` and calls `.parse()` on the response (SRP: fetching + parsing only, no schema definition)
  - Acceptance: function is a Next.js Server Action; reads from internal backend endpoint when `DASHBOARD_DATA_SOURCE=backend`, else returns the Cycle 2 mock; returns `IDashboardSnapshot`; backend URL read from `.env` via env schema; no external API keys ever touched in this file; network errors return typed error, not raw exception; `fetchedAt` is ISO string from the backend response (NOT `new Date()` on the Next.js side, which would break determinism); schema is imported, not redefined inline

- [ ] CONDITIONAL (only if Variant C wins): history fetcher for selected ticker — reads from internal backend
  - File: `apps/dashboard_client_web/app/actions/getDashboardHistory.action.ts`
  - Acceptance: server action accepts a ticker label, returns `IItemHistory` (or null on error); reads from internal backend endpoint when `DASHBOARD_DATA_SOURCE=backend`, else returns mock history from Cycle 2; TanStack Query `staleTime: 5 * 60 * 1000` on the client hook + `unstable_cache` server-side; lazy fetch on click (not pre-fetched for all 22 items); error fallback shows "history unavailable" without crashing the page; **no direct external API calls — all rate-limit / credit concerns are the backend's responsibility, not the Next.js app's**

- [ ] Verify backend exposes the snapshot and history endpoints with the agreed shape
  - Acceptance: `dashboard_server` (or designated backend) exposes `GET /api/dashboard/snapshot` returning shape matching `IDashboardSnapshot` schema, and (if Variant C wins) `GET /api/dashboard/history?label=…` returning `IItemHistory`; ingestion job populates both at the cadence specified in Plan §2; KRX ticker mapping for NCSOFT (`036570`) is handled by the backend, not the Next.js app

- [ ] Implement per-category polling intervals in the client hook
  - File: `apps/dashboard_client_web/hooks/useDashboardSnapshot.query.client.ts`
  - Acceptance: FX refetch interval 60s; Crypto 30s; Indices US 60s; Indices KRX daily (one fetch per session); Rates 24h; Stocks 60s; intervals are named constants, not magic numbers; intervals describe how often the Next.js app re-reads from the user's backend (the backend's own ingestion cadence is independent)

#### Group C — Page conversion

- [ ] Replace `/dashboard/page.tsx` content with the winning display variant component
  - Acceptance: page renders real data from `getDashboardSnapshot.action.ts`; loading skeleton visible during initial fetch; error state shows readable message (not raw error); page title is "Dashboard" in document head

- [ ] Add "Dashboard" link to desktop and mobile navigation
  - File: navigation component in `apps/dashboard_client_web/components/`
  - Acceptance: "Dashboard" link present in desktop sticky nav and mobile bottom drawer; navigates to `/dashboard`; active route receives visual highlight matching existing link style

#### Group D — Soft-delete old implementation

- [ ] Rename superseded files with `x_` prefix (do not delete — user deletes manually)
  - Files: `FinancialGridLayout.client.tsx`, `GridCard.client.tsx`, all original widget files under `components/dashboard/widgets/`, `getFinancialData.mock.api.ts`, `useElementSize.hook.ts`
  - Acceptance: all listed files renamed to `x_[original-name]`; no import anywhere in the active code references the old filenames; `check-types` still exits 0 after rename

#### Group E — MVP acceptance gate

- [ ] All 22 items display value + 1-day change % with color indicator
  - Acceptance: manual browser check confirms all 22 labels visible on `/dashboard`; no item shows "undefined" or "NaN"

- [ ] Page renders in <2s on dev server with mock data
  - Acceptance: Chrome DevTools Network tab shows `/dashboard` document response under 2000ms on localhost with mock data active

- [ ] Zero `any` types in new code paths
  - Acceptance: `pnpm --filter dashboard_client_web check-types` exits 0; `grep -r ": any" apps/dashboard_client_web/types apps/dashboard_client_web/app/actions apps/dashboard_client_web/hooks` returns no matches

- [ ] No hydration errors
  - Acceptance: browser console shows zero hydration warnings on hard reload of `/dashboard` in both dev and production build

---

### Cycle 4 — Verification + Iteration Trigger

- [ ] PM verifies all MVP-done acceptance criteria against the live `/dashboard` page
  - Acceptance: all items in Cycle 3 Group E are checked off; PM records pass/fail for each criterion

- [ ] PM decides scope of Cycle 5 (if any)
  - Options: display polish (sparklines, time-window selector), production hardening, Phase 4 pnpm migration
  - Acceptance: `00.tasks.md` updated with Cycle 5 task list before developer begins any Cycle 5 work

---

## Phase 3 — Production Hardening (PLANNED)

- [ ] Audit `_scripts/` directory — determine purpose and move to correct location or delete
  - Acceptance: contents moved to named `packages/` subpackage with `package.json`, or directory removed and deletion committed
- [ ] Resolve `.agent/rules/nexjs.rule.md` duplication with `.claude/rules/nexjts.md`
  - Acceptance: exactly one canonical Next.js rule file exists; references updated in any agents that consume it
- [ ] Decide language policy for `docs/`
  - Acceptance: `docs/doc.nextjs.api.actions.md` is either translated to English and kept in `docs/`, or moved to `.claude/` if agent-facing only
- [ ] Verify `article_server` workspace status
  - Acceptance: either `apps/article_server/` exists with valid `package.json`, or workspace entry removed from root `package.json` and `turbo.json` updated
- [ ] Add `env.schema.ts` build-time validation gate
  - Acceptance: build fails with descriptive error if required env variable is missing; no secrets hardcoded in source

---

## Phase 4 — Package Manager Migration (pnpm 9.5.0)

> USER RESPONSIBILITY: After all tasks below are complete, the user must manually delete `yarn.lock` and all `node_modules`, then run `pnpm install`. Engineer agents must NOT run install or delete node_modules.

### Group A — Config Edits (root)

- [ ] Set `"packageManager": "pnpm@9.5.0"` in root `package.json` and remove `"workspaces"` field
  - Acceptance: `package.json` contains `"packageManager": "pnpm@9.5.0"`, no `"workspaces"` key present

- [ ] Fix broken `dev:web` script target in root `package.json`
  - Acceptance: `dev:web` script targets correct workspace name; resolves without "workspace not found" error

- [ ] Fix broken `dev:server` script target in root `package.json`
  - Acceptance: `dev:server` targets an existing workspace or is removed; no dangling script references

- [ ] Create `pnpm-workspace.yaml` at repo root
  - Acceptance: file exists; contains `packages:` block with globs covering `apps/*` and `packages/*`

- [ ] Create `.npmrc` at repo root enforcing strict isolation
  - Acceptance: contains `node-linker=isolated`; does NOT contain `shamefully-hoist=true`; `strict-peer-dependencies=false` included

### Group B — Workspace package.json Edits

- [ ] Remove `yarn` from `build` script in `apps/dashboard_client_web/package.json`
  - Acceptance: `build` script value does not contain `yarn`; syntactically valid JSON

- [ ] Remove `yarn run clean &&` from scripts in `packages/markdown-parsing/package.json`
  - Acceptance: `build`, `deprecated:dev`, `prepublishOnly` do not contain `yarn`; scripts are valid JSON

- [ ] Remove `yarn run clean &&` from `build` script in `packages/axios/package.json`
  - Acceptance: `build` script does not contain `yarn`; script is valid JSON

### Group C — Documentation Edits

- [ ] Update yarn references in root `CLAUDE.md`
- [ ] Update yarn references in `apps/dashboard_client_web/README.md`
- [ ] Update yarn references in `apps/docs/README.md`
- [ ] Update yarn references in `packages/markdown-parsing/README.md`
- [ ] Update yarn references in `apps/dashboard_server/docs/node.server.md`
- [ ] Update yarn references in `docs/guide.spec.md`
  - Acceptance for all above: no occurrence of `yarn` as a command remains; pnpm equivalents substituted

### Group D — Permissions / Settings

- [ ] Replace yarn permission entries in `.claude/settings.local.json`
  - Acceptance: `Bash(yarn install:*)` and `Bash(yarn build)` removed; `Bash(pnpm install:*)` and `Bash(pnpm build)` added; JSON remains valid

### Group E — User-Run Install (USER RESPONSIBILITY)

- [ ] USER: Delete `yarn.lock` at repo root
- [ ] USER: Delete `node_modules` at repo root and in each workspace
- [ ] USER: Run `pnpm install` from repo root
  - Acceptance: exits 0; `pnpm-lock.yaml` generated; no install error

### Group F — Verification

- [ ] Verify `pnpm-lock.yaml` exists and `yarn.lock` absent
- [ ] Verify all workspaces recognized: `pnpm ls -r --depth 0`
- [ ] Verify `turbo dev` starts without module-not-found errors
- [ ] Verify `pnpm --filter dashboard_client_web run check-types` exits 0
- [ ] Verify `pnpm --filter dashboard_client_web run build` exits 0
