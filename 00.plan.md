# Product Plan — tradelunch

**Last updated:** 2026-09-22
**Active branch:** main (blog-only repo since 2026-07-09)
**Owner:** Taek Lim

> Single source of truth for **product intent + roadmap**. Atomic execution lives in
> `00.tasks.md`; running state lives in `01.status.md`. Superseded/verbose docs are archived
> under `_docs/` (gitignored). See CLAUDE.md "Workflow Docs" rule.
>
> **Completed phases live in `_docs/archive/` (cold storage — do not read unless investigating
> history). This doc holds active + planned work only. Split detail, if any, is linked from stubs
> below; a stub's state is the truth if it and the detail disagree. See `.claude/rules/docs.md`.**

---

## Product Identity

Personal portfolio + blog for Taek Lim (GitHub: tradelunch), one Next.js 16 monorepo,
domain `my.prettylog.com`. Surfaces:

1. **Blog / portfolio** — curated multi-user technical posts (Clerk auth). Posts are authored
   either by the Python publishing agent (`apps/blog_agent`, direct-write) OR via the in-app
   authoring UI (`/write`, Phase D2 + the authoring UX cycle — create/edit/autosave/thumbnail UX
   shipped).
2. **Log** — Threads-style personal microfeed (`/log/[username]`, Phase Y, shipped).

The **financial dashboard surface moved to the separate `project_tradelunch_finance` repo**
(2026-07-09); see the Finance-track MOVED stub below.

---

## Tech Stack (locked)

- Next.js 16 App Router, TypeScript strict; Tailwind v4, Radix UI, shadcn/ui, lucide-react
- Turborepo monorepo: `apps/dashboard_client_web`, `apps/dashboard_server`, `apps/blog_agent`, `packages/*`
- Jotai, TanStack Query v5, next-intl (recharts/lightweight-charts removed — `01.status.md` 2026-07-13 refactor(hygiene))
- Backend: Express 5 + `pg` Pool (raw SQL) against Supabase pooler; no Sequelize, no supabase-js
- Images: OCI Object Storage via the provider-swappable storage module (`STORAGE_PROVIDER`)
- Publishing: Python LangGraph agent (`apps/blog_agent`), `uv`-managed
- pnpm 9.15.9; prod = Vercel (two projects, one repo)

---

## Architecture Decisions (resolved) — split out (2026-09-22)

> Current state: no open item; standing constraints still live — `dashboard_server/tsconfig.json`
> MUST inline strict (no workspace `extends`), keyset cursors are opaque STRINGS, `blog_agent` is a
> DIRECT-WRITE publisher (`user_id=1`) and Express is read-only over its rows. <!-- see decisions.plan.md -->
> Detail: [decisions.plan.md](./decisions.plan.md)

---

## Deployment Roadmap — Vercel + Supabase + (eventual) Clerk

Reference pattern: kcdd-market_v2 (proven, same owner). Replaces any prior "deploy to VPS"
assumption. **Execution detail (ordered stages S1–S9, owner per step, commands, acceptance,
rollback) lives in `00.tasks.md` Phase A.**

**Critical path to live blog + mock dashboard on `my.prettylog.com`:**

> S1.2 create Supabase project → S1.3 link CLI → S1.6 db push → S1.7 verify CTE →
> S3 runtime verify (dev server vs Supabase) → S4.2 backend Vercel project → S4.3 backend env →
> S4.4 verify → S5.2 frontend Vercel project → S5.3 frontend env → S6.1 confirm GitHub → S6.2 DNS.
> S7.1 CI done; S7.2 lint step deferred.

**Changed conditions (since original plan):** AWS RDS **deleted** → S2 data migration SUPERSEDED
(Supabase starts empty; fresh data authored directly). AWS S3 **deleted** → image migration
SUPERSEDED (images uploaded fresh to Supabase Storage from Phase B). Server publish scripts that
used Sequelize+S3 are BROKEN and excluded from the Vercel build — reworked as the Python
`apps/blog_agent` (Phase A merge) + Phase B repoint.

---

### Phase A — Deploy Current State (Free, Read-Only) — DONE (2026-06-26)

> Read-only blog + mock dashboard live on Vercel; backend reads Supabase via `pg` Pool (no SSH/AWS); CI typecheck; acceptance gate MET. blog_agent merged (f51cc7e). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-A.md](./_docs/archive/plan.phase-A.md)

---

### Phase B — DB/Storage Repoint + Real Dashboard Data — DONE (backend, 2026-06-26)

> AWS Postgres/SSH/S3 retired; blog*agent repointed to Supabase pooler + Supabase Storage `blog.prettylog` + Cloudflare CDN (BA-09); first real publish landed end-to-end. Ingestion cron SUPERSEDED by Phase I. **B4 dashboard mock→real COMPLETE (2026-06-29): `DASHBOARD_DATA_SOURCE=backend` flipped + chart history wired to backend.** <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-B.md](./_docs/archive/plan.phase-B.md)
> **Open (non-blocking):** Cloudflare CNAME (branded-domain layer only); re-publish with Ollama up for LLM tags/categories. See `00.tasks.md` B5.6–B5.7. *(B5.5 client cursor `Number()` coercion FIXED.)\_

---

### Phase C — Production Hardening

No user-visible features; correctness + maintainability. Still open in THIS repo:

- `env.schema.ts` build-time Zod failure on missing required vars
- lint/CI debt (S7.2 `--max-warnings 0`; blog_agent ruff)
Done/moved: dead-dep reconcile — `recharts`/`lightweight-charts` REMOVED from `dashboard_client_web`
(`01.status.md` 2026-07-13 refactor(hygiene)); chart-hook split + `ChartPanel` re-slim (2026-06-30, finance repo
now); `posts.ts` service-layer split landed as HX.2 (`db00d42`); axios retirement shipped as F-fetch.2.

---

### Phase D — Multi-User Blog Platform (Clerk Auth) — ENG COMPLETE (2026-06-27)

> Clerk auth enforced at the Express layer (pg Pool bypasses RLS by design → app-layer owner-scoping); lazy provisioning in `resolveAuth` (no webhook) + `/onboarding` username claim; schema `0003`; `/write` authoring + autosave (D2, dc28f19) + create/edit UX cycle + editor i18n; D4 admin moderation = soft-delete any post by id (70375e6). Open-signup default; `/admin/invites` DROPPED. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-D.md](./_docs/archive/plan.phase-D.md)
> **Open USER gates:** admin bootstrap SQL (D4.0b, match by `username`/`clerk_user_id`, never email). Clerk env + public-sign-up = DONE (D1.1); image-upload env → see `00.tasks.md` "Go-live USER gates" (`STORAGE_PROVIDER=oci`), the single place that fact lives.
> **Open ENG:** lint/CI debt (S7.2 `--max-warnings 0`; blog*agent ruff + pyenv 3.12.3); non-blocking D3.4 `/settings/profile`. *(UX12b-persist thumbnail persistence SHIPPED `31eb14a`.)\_

---

### Phase E — Blog Engagement (Saved Posts · Likes · Comments) — ENG COMPLETE (`7b2ea0a`)

> Saved Posts (`/me/saved`, migration `0007`), Likes (`0008` `post_likes`, optimistic button), Comments (`0009` Option-C materialized path, unlimited depth, soft-delete tombstone) — all shipped + pushed. Issue #1 = PARALLEL (blog_agent's posts-based comment columns untouched). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-E.md](./_docs/archive/plan.phase-E.md)
> **SUPERSEDED (apply gate closed):** the `0007`+`0008`+`0009` apply gate is SUPERSEDED by **Phase M** — the clean-cut applied all 12 blog migrations to the new blog-only Supabase project (2026-07-09). No open USER item.

---

### Phase E2 — Comment Edit + Comment Pagination — DONE/SHIPPED (`85093b4`)

> Closed Phase E's two deferrals: comment EDIT (`PATCH /comments/:id`, auth==delete, tombstone→409, "(edited)" badge) + keyset pagination (50 roots/page each with full subtree, STRING cursor). Purely additive — no schema/index/migration change. `comments.ts` split into a barrel. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-E2.md](./_docs/archive/plan.phase-E2.md)

### Blog card actions — SHIPPED (`894fce3`)

> Post-card Share / Save / whole-card navigation; favorites backend (migrations `0006_post_favorites` + `0007_*_idx`, `controllers/favorites/` + `SaveButton`/`useSharePost`/`useFavorites`). Not a numbered phase. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/blog-card-actions.plan.md](./_docs/archive/blog-card-actions.plan.md)

---

### Phase F — Blog Image Upload Rework (Express proxy + 2-step resize) — DONE (`2a0457b`)

> `POST /v1/api/posts/images` (multer + sharp `rotate().resize(1600,inside).webp(q80)`, server-to-server upload, full error mapping); client step-1 native-Canvas resize (no library); old signed-URL path soft-deleted `x_*`; Decision #27 SUPERSEDES #14. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-F.md](./_docs/archive/plan.phase-F.md)
> **Open (USER):** F17 manual e2e + sharp libvips runtime confirm on Vercel. Image-upload env precondition: see `00.tasks.md` "Go-live USER gates" (`STORAGE_PROVIDER=oci`) — the fact lives there only.

---

### Phase F2 — blog_agent webp compression (local Python CLI) — DONE (`3e5b04c`)

> `apps/blog_agent/utils/image_transform.py` `to_webp` (Pillow LANCZOS + `exif_transpose`, 1600 fit-inside, q80, metadata stripped); thumbnails keep 1200×630 letterbox → webp q80 (Option A); path scheme unchanged; intentional duplicate of Phase F's Node transform. Decision #28. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-F2.md](./_docs/archive/plan.phase-F2.md)

---

### Phase G — Editor Category Selector + Tags (write/edit screen) — DONE (`bced84e`)

> Cascading 3-box category picker + flat tag-chip editor on `/write`; migration `0010` switches category uniqueness to `(user_id, parent_id, title)`; tri-state `category_id` PATCH (CASE not COALESCE); by-id read now returns `tags` + `stored_uri`; new `POST /v1/api/categories`; AC1–AC10 met. Decision #29. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-G.md](./_docs/archive/plan.phase-G.md)
> **SUPERSEDED (push gate closed):** migration `0010_category_unique_parent.sql` push is SUPERSEDED by **Phase M** — the clean-cut applied all 12 blog migrations to the new blog-only Supabase project (2026-07-09). No open USER item.

---

### Phase H — Multi-user Reddit-style Blog (global feed + context rails) — DONE P0–P5 (`2c0f897` / `f83a38e`)

> Reddit-style 3-col `BlogShell` route group (`app/(feed)`); `/tags/[tag]` global tag feed; global TopBar (SearchBar tag-nav + CreateButton); identical global LEFT rail (PrimaryNav + global TagCloud); per-user context RIGHT rail (category tree + scoped tags + profile card) with `<lg` sheet; localStorage recents/saved-tags; 4 viewer-agnostic tag-read APIs. Categories stay per-user; topics = tags. Decision #30. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-H.md](./_docs/archive/plan.phase-H.md)
> **Open (DEFERRED):** P6 — real search API (`GET /v1/api/posts/search`) + `/explore`; HX.1/HX.2 non-blocking follow-ups.

---

### Phase 2-mobile — Mobile context UX (chip header + in-article TOC) — SHIPPED (MOB.0–MOB.3)

> Two additive client-side-only mobile (`<lg`) UX changes — (D) top-of-article collapsible TOC, (B) top-of-feed context header (author chip + category/tag chip row) — replacing the heavier `<lg` `UserContextSheet`; Phase 2-filter later SUPERSEDED the NAV chip row (`MobileChipRow`/`getMobileChips` `x_`-retired). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-2-mobile.md](./_docs/archive/plan.phase-2-mobile.md)

---

### Phase 2-filter — Multi-category + multi-tag feed filter (desktop rails + mobile rows) — DONE (SHIPPED 2026-06-29)

> Reader narrows the author feed `/blog/[username]` by categories AND/OR tags at all breakpoints; URL state (`?categories=a,b&tags=a,b`), same-type OR / cross-type AND, no migration; SUPERSEDES Phase 2-tags. Decision #34. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-2-filter.md](./_docs/archive/plan.phase-2-filter.md)

---

### Phase 2-fetch — Feed data layer: axios→native fetch + tag revalidation — DONE (SHIPPED 2026-06-29)

> Blog feed read moved off client axios onto a server-only native fetch wrapper for Next 16 tag-based revalidation (instant read-your-writes on publish-class mutations); Express CDN backstop lowered. Builds on Phase 2-filter; delivers the instant-invalidation upgrade #34 F.11 deferred. Decision #35. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-2-fetch.md](./_docs/archive/plan.phase-2-fetch.md)

### Phase 2-fetch follow-up — F-fetch.2 client axios retirement — DONE/SHIPPED (2026-07-08)

> **Shipped:** all axios removed from BOTH frontends. `dashboard_client_web` (25 `.api.ts` + local `axios_instance`) and `finance_web` (`getMe`) migrated to native-fetch wrappers `apis/http.core.ts` (pure/isomorphic) + `apis/http.client.ts` (`clientRequest<T>`) + `apis/http.server.ts` (delegates to core, `serverRequest` unchanged); `axios_instance.ts` → `x_`-retired; npm `axios` dropped from both apps. `ApiError` gained `body?` (createCategory 409). **`@repo/axios` never existed** (confirmed). Envelope double-unwrap risk resolved: every fetcher verified against its real Express shape and unwraps exactly once — extra latent bugs fixed beyond plan (createPost/updatePost/getMyDrafts/uploadImage returned the envelope as the raw type; getPosts/getPostsByTag/getPopularTags/getUserPopularTags double `.data`; getCategories dead `.status` guard). Gate: 4 apps check-types 0 · 4 apps build green · dashboard_server jest 352/352 · zero axios imports in active code.
>
> **Bundled follow-on — backend `{success,data}` envelope convergence:** owner-chosen "envelope 통일" (code was already ~95% envelope). Added `sendOk`/`sendError` helpers to `dashboard_server` + `finance_api`; converted the 4 remaining RAW routes to the envelope (dashboard `GET /users/me`, `POST /users/me/username`, `GET /users/:username/categories`; finance `GET /users/me`) with the 4 consuming fetchers flipped to unwrap once; added a global error-handler middleware to `finance_api` (was missing). `TCategoryTreeResponse` now dead (kept per soft-delete). NOT converted: `GET /users/:username/category/:categoryId` (bespoke top-level-spread shape, no frontend consumer found — flagged for later).

---

### Finance track (Phases I–L, M/N retention, O, P, Q, R–W, X, X2) — MOVED to `project_tradelunch_finance` (2026-07-09)

> All finance product intent, phases, open gates, and decisions #9/#32/#38–#45 now live in that
> repo's docs; this repo is blog-only. Frozen copy at time of move: <!-- ARCHIVE: history-only -->
> [\_docs/archive/plan.finance-track.md](./_docs/archive/plan.finance-track.md)

---

### Phase SEO-2 — SEO coverage completion (sitemap detail URLs · author/home metadata · dynamic OG) — SHIPPED (2026-07-05, `63eb56b`)

> Sitemap now enumerates `/symbols/[ticker]` (getRankings top-1000) + `/politicians/[filerId]` (new Express `GET /v1/api/politicians` list route); per-author + home metadata; dynamic OG via `next/og` (`lib/og.tsx`) on blog/symbols/politicians/funds; hreflang DROPPED (content is single-language). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.seo-2.md](./_docs/archive/plan.seo-2.md)

### Blog owner-private visibility + StatusBadge — SHIPPED (2026-07-05, `53196a8`)

> A `status='private'` post was invisible even to its own author on the public blog because server-side fetches ran anonymously. Forward the Clerk token (`auth().getToken()`) on the detail / TOC / category-sidebar reads so the owner sees their own non-public posts; anonymous visitors unchanged (private still 404s / excluded — `auth()` forces dynamic render, no private cache leak; OG images stay anonymous). Backend detail + author-feed routes were already owner-aware (`status='public' OR user_id=viewer`); the category route (`/users/:username/categories` ×2 queries) gained `optionalAuth` + `OR user_id=$viewer`. `/me` widened (`listDrafts` → `status IN ('draft','private')`). 🔒 Private / Draft `StatusBadge` on feed card + detail header + `/me`. Also fixed React #418 date hydration on post cards (`utils/formatPostDate`, `16e2e18`).

---

### Log (Phase Y · Y-TD · Y M2) — split out (2026-09-22)

> Current state: **Y M1 SHIPPED** (2026-07-10, UX 반복 `fdbbea2`까지 main); **Y-TD + Y M2 ENG DONE**
> (2026-07-13, merged to main `83f401c`; Y-TD 공개범위 PRIVATE 확정). ⚑OPEN: 마이그레이션
> `0023`(todo) / `0024`(social) 적용 게이트 · Y-TD-T7 / Y-M2-T9 고의파손 검증. 원자 태스크 =
> `00.tasks.md` Phase Y / Y-TD / Y M2.
> Detail: [log.plan.md](./log.plan.md)

---

### Phase CF — Cloudflare 캐시 위임 (Next 서버측 캐시 제거) — ENG DONE (코드 T1–T5 shipped, `4e5c085`에 폴드; audited 2026-09-22) · CF-T6 → **Phase CF2**로 이관

> **문제:** 실트래픽은 낮은데 Supabase+Vercel 무료 티어 트래픽이 초과 + 오너가 콘텐츠 갱신
> 지연(stale) 체감. 근인 = 공개 페이지가 전부 `force-dynamic`인데 앞단 엣지 캐시가 없어
> 오리진 직격 + 익명 피드의 60s Next Data Cache(`revalidate`)+태그 무효화가 stale 윈도를 만듦.
>
> **결정:** Cloudflare를 엣지의 **단일 캐시 레이어**로 삼고 Next는 항상 신선(`no-store`)하게 둔다.
> Next의 서버측 캐시 프리미티브를 제거. 범위 = `apps/dashboard_client_web`만: 중앙 서버 fetch
> 래퍼 `apis/http.server.ts`에서 `cache`/`tags`/`revalidate` 제거(`no-store` 하드코딩) · 익명 피드
> `apis/getPosts.fetch.server.ts`의 익명/오너 분기를 단일 uncached fetch로 축약(`FEED_REVALIDATE_SECONDS`
> 제거) · `app/actions/postPublish.action.ts`의 `updateTag` 무효화 제거 · delete/status 훅 주석 정리.
>
> **명시적 유지(범위 외):** 모든 `force-dynamic` 라우트 설정 · `apis/getPost.api.ts`의 React
> `cache()` 요청 디듀프 · 클라이언트 React Query `staleTime`. 승인된 Next-캐시 예외는 **2개**
> (`sitemap.ts` 24h · OG 라우트 24h) — `http.server.ts`는 `no-store`가 기본값이고 opt-in
> `revalidate` 인자가 `d094859`에서 추가됐다(CF의 "blanket no-store" 문구는 그만큼 개정됨).
>
> **후속 전량 → Phase CF2** (엣지 설정은 CF2가 소유; 여기서 반복하지 않는다).

---

### Phase CF2 — Cloudflare 엣지 캐시 (Vercel 402 대응) — 앱측 누수 SHIPPED (`d094859`, `e4c6f48`) · 엣지 설정 대기

> **사건(2026-09-22):** `my.` / `blogapi.` 둘 다 HTTP 402 `x-vercel-error: DEPLOYMENT_DISABLED`
> (Vercel Hobby 쿼터 소진). **근인 = 앱측 캐시/쿼리 누수**이지 플랫폼이 작아서가 아니다(Decision #46).
> 결정적 사실: Cloudflare가 네임서버이긴 하나 `my.`/`blogapi.`는 **DNS-only(grey cloud)** —
> Cloudflare는 앱 요청 경로에 **한 번도 있던 적이 없다**.
>
> **결정:** Vercel+Supabase 유지, 마이그레이션 없음. 누수 5건을 먼저 막고(shipped: OG 24h ·
> 목록 SELECT에서 `content` 제거 · 이미지 옵티마이저 우회 · `?limit=` 클램프 · sitemap 24h),
> 그다음 Cloudflare를 **진짜로** 앞단에 세운다.
>
> **순서 불변식 CF2-0 → 1 → 2 → 3:** 앱이 `Set-Cookie: locale`을 뱉고 Clerk가
> `Cache-Control: private, no-store`를 붙이는 동안에는 프록시를 켜도 캐시가 0이다. `locale`
> 쿠키는 렌더에 영향 없는 write-only라 **삭제**(CF2-0a); Clerk matcher 축소는 공개 RSC `auth()`를
> 깨뜨려 **REJECTED** → 그 헤더는 엣지에서 **Edge TTL "Override origin"** 으로 덮는다.
>
> **오너 결정:** Edge TTL 24h · `blogapi.`는 grey 유지(Clerk 토큰이 CF를 통과하면 오설정 시
> 사용자 간 데이터 누출, CF2-4 보류). **HTML TTL도 24h로 확정**(2026-09-22): 레포에 purge
> 코드가 0줄이라 엣지 TTL = 발행 후 stale 윈도이지만, 오너가 수동 purge로 감당하기로 했다
> — `_scripts/cf-purge.sh`(CF2-5a). 발행 시 자동 purge는 CF2-5b로 보류.
> 원자 태스크·측정치·AC = `00.tasks.md` Phase CF2.

---

## Free-Tier Reality Check

| Concern                 | Limit                                   | Status                                                                                                         |
| ----------------------- | --------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Vercel Hobby            | Personal use; individual GitHub account | ⚑2026-09-22 402 `DEPLOYMENT_DISABLED` (쿼터 소진) → Phase CF2.                                                                    |
| Supabase Free (blog DB) | 500 MB DB, 7-day auto-pause             | Blog-only DB is small; `supabase-keepalive.yml` weekly ping prevents auto-pause.                               |
| OCI Object Storage      | Always-free tier                        | Blog images (`STORAGE_PROVIDER=oci`, bucket `blog-assets.prettylog.com`); Supabase Storage retired for images. |
| Clerk Free              | 10,000 MAU                              | Safe for open self-signup (Phase D). Clerk Production instance on `my.prettylog.com`.                          |
| Vercel Serverless Hobby | 100 GB-h/mo, 10s timeout                | Fine for blog reads. (Finance ingestion crons moved with the finance repo.)                                    |

---

## Decisions Log (resolved questions) — split out (2026-09-22)

> Current state: **0 open** — #46 (2026-09-22, 호스팅 유지 = Vercel+Supabase, 원인은 앱측 캐시 누수)
> resolved this cycle and kept inline until Phase CF2 ships; #1–#31, #33–#37 archived, finance #9/#32/#38–#45
> moved with the finance track. <!-- see decisions.plan.md -->
> Detail: [decisions.plan.md](./decisions.plan.md)
