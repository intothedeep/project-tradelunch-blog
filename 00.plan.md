# Product Plan — tradelunch

**Last updated:** 2026-06-26
**Active branch:** feature/finance → main (after Phase A deploy)
**Owner:** Taek Lim

> Single source of truth for **product intent + roadmap**. Atomic execution lives in
> `00.tasks.md`; running state lives in `01.status.md`. Superseded/verbose docs are archived
> under `_docs/` (gitignored). See CLAUDE.md "Workflow Docs" rule.

---

## Product Identity

Personal portfolio + fintech site for Taek Lim (GitHub: tradelunch), one Next.js 16 monorepo,
domain `my.prettylog.com`. Two surfaces:

1. **Blog / portfolio** — curated single-author technical posts, read-only. Posts are authored
   by the Python publishing agent (`apps/blog_agent`); no in-app authoring UI.
2. **Financial dashboard** — TradingView-style chart + 22-item watchlist with indicators,
   drawing tools, intraday intervals. Currently mock data; Cycle 3 wires the real backend.

---

## Tech Stack (locked)

- Next.js 16 App Router, TypeScript strict; Tailwind v4, Radix UI, shadcn/ui, lucide-react
- Turborepo monorepo: `apps/dashboard_client_web`, `apps/dashboard_server`, `apps/blog_agent`, `packages/*`
- Jotai, TanStack Query v5, next-intl, recharts (shadcn chart), lightweight-charts v5
- Backend: Express 5 + `pg` Pool (raw SQL) against Supabase pooler; no Sequelize, no supabase-js
- Publishing: Python LangGraph agent (`apps/blog_agent`), `uv`-managed
- pnpm 9.5.0; PM2 (current prod) → Vercel (post-deploy)

---

## Architecture Decisions (resolved)

**`@repo/types` package (2026-06-24).** Lean package (no build step, raw TS, `main`/`types` →
`src/index.ts`) for API-boundary types shared front+back: `ETreeNodeType`, `TCategoryTreeNode`,
`TPostTreeNode`, `TCategoryTreeResponse`, `TTreeNode`, `TTreeNodeWithChildren`. Client adds
`transpilePackages: ['@repo/types']`. `@repo/markdown-parsing` later retired from the client
(TOC inlined to `utils/markdown/`).

**Env var convention (kcdd2).** SCREAMING_SNAKE_CASE, no prefix namespacing (`DATABASE_URL`,
not `SUPABASE_DATABASE_URL`). Key vars: `DATABASE_URL` (Supabase pooler 6543), `NODE_ENV`,
`SITE_DOMAIN`, `API_SITE_DOMAIN`, `CDN_ASSETS`, `SUPABASE_STORAGE_BUCKET` (default `blog.prettylog`).
**Renamed (2026-06-26):** `CDN_ASSET_POSTS`→`CDN_ASSETS` (server) /
`NEXT_PUBLIC_CDN_ASSET_POSTS`→`NEXT_PUBLIC_CDN_ASSETS` (client), default domain
`posts.prettylog.com`→`assets.prettylog.com` (commit 3100a9f); the image bucket is env-driven via
`SUPABASE_STORAGE_BUCKET` (default `blog.prettylog`, replaced hardcoded `blog-images`, commit 20dbf26).

**Data access.** Raw SQL kept verbatim. Sequelize execution layer replaced by `pg` Pool
(`max: 1`) → Supabase transaction pooler (port 6543). No supabase-js. The 591-line `posts.ts`
SQL is unchanged.

**Backend topology.** `dashboard_server` stays in the monorepo at `apps/dashboard_server`.
Two Vercel projects, one repo. Vercel Root Directory = the app dir; install/build from repo root.

**Vercel tsconfig strict-narrowing constraint (2026-06-26).** `@vercel/node` type-checks the
serverless function import graph with a bundled `ts-node@10.9.1`, which **cannot resolve a tsconfig
`extends` that points at a workspace package** (`@repo/typescript-config`) under TypeScript ≥5.3 —
it silently drops inherited `strict`/`strictNullChecks`, collapsing discriminated-union narrowing
(`TS2339` on `result.reason` after `if(!result.ok)`). Fixed in ts-node 10.9.2 but Vercel pins
10.9.1 (ref: github.com/vercel/vercel/issues/12087). **Resolution:** `apps/dashboard_server/tsconfig.json`
is **self-contained** — it inlines the compiler options (target/module/strict/esModuleInterop/
skipLibCheck/composite/ignoreDeprecations) instead of extending `@repo/typescript-config`; and
`vercel.json` `buildCommand` is `pnpm --filter @repo/types build && pnpm --filter dashboard_server
check-types`, a deterministic strict gate via real `tsc`, independent of `@vercel/node` internals.
No application code changed; the `src/helpers/*` discriminated unions were correct. **Standing
constraint:** any tsconfig that `@vercel/node` reads MUST inline strict (no workspace `extends`);
only this one server tsconfig is self-contained, all other workspaces still
`extends @repo/typescript-config`.

**Keep `@repo/types` centralized (2026-06-26, architect-validated).** `@repo/types` stays the
single source of truth for the client↔server HTTP contract — do **NOT** duplicate types per-app
(duplication would cause silent drift). `@repo/types` was **not** the cause of the Vercel TS2339
deploy failure (the failing types were local to `dashboard_server/src/helpers/*`); the fix was the
self-contained server tsconfig above.

**blog_agent merge (2026-06-25).** The Python LangGraph publishing agent was merged into the
monorepo as `apps/blog_agent` (lift-and-shift, code logic unchanged). Turbo recognises it via a
thin `package.json` stub delegating to `uv run`; `start` is excluded from `dev`/`build`, only
`test`/`lint`/`check-types` are filterable. Schema SSOT is
`apps/dashboard_server/schema/tradelunch.schema.sql`; `apps/blog_agent/schema/…` is a byte-identical
copy guarded by `pnpm check:schema` (drift detection). See "blog_agent" under Phase A/B below.
Full ADR archived in `_docs/00.blog-agent-merge.arch.md`.

---

## Deployment Roadmap — Vercel + Supabase + (eventual) Clerk

Reference pattern: kcdd-market_v2 (proven, same owner). Replaces any prior "deploy to VPS"
assumption. **Execution detail (ordered stages S1–S9, owner per step, commands, acceptance,
rollback) lives in `00.tasks.md` Phase A.**

**Critical path to live blog + mock dashboard on `my.prettylog.com`:**
> S1.2 create Supabase project → S1.3 link CLI → S1.6 db push → S1.7 verify CTE →
> S3 runtime verify (dev server vs Supabase) → S4.2 backend Vercel project → S4.3 backend env →
> S4.4 verify → S5.2 frontend Vercel project → S5.3 frontend env → S6.1 confirm GitHub → S6.2 DNS.
> S7.1 CI done; S7.2 lint step deferred.

**Changed conditions (since original plan):** AWS RDS **deleted** → S2 data migration SUPERSEDED
(Supabase starts empty; fresh data authored directly). AWS S3 **deleted** → image migration
SUPERSEDED (images uploaded fresh to Supabase Storage from Phase B). Server publish scripts that
used Sequelize+S3 are BROKEN and excluded from the Vercel build — reworked as the Python
`apps/blog_agent` (Phase A merge) + Phase B repoint.

---

### Phase A — Deploy Current State (Free, Read-Only)

**Goal:** current version — read-only blog + mock dashboard — live on Vercel at zero cost, zero
new features.

**Done means:** frontend live on Vercel (later `my.prettylog.com`); backend live as a second
Vercel project reading Supabase via `pg` Pool (raw SQL, no SSH tunnel, no Sequelize); dashboard
still mock; CI runs typecheck on every push; auto-deploy on merge to main.

**blog_agent (Phase A merge — DONE, commit f51cc7e):** Python agent lifted into
`apps/blog_agent`, turbo-integrated, schema-drift guard added. Runtime infra untouched (still
`DB_PG_*`/`AWS_*` env contract) — repoint is Phase B.

**Acceptance gate (all must hold before Phase B):**
- `https://my.prettylog.com/blog/@taeklim` loads posts from Supabase; infinite scroll works; no SSH/AWS
- `https://my.prettylog.com/dashboard` loads chart with mock data; all 22 watchlist items visible
- CI green on main
- No hardcoded secrets in source
- Both apps `check-types` exit 0; no `sequelize` / `supabase-js` import under `dashboard_server`

**Non-goals:** real market data, auth, any new feature, storage migration, moving server out of monorepo.

---

### Phase B — DB/Storage Repoint + Real Dashboard Data

**Goal:** retire AWS Postgres + SSH tunnel + S3; serve real market data; repoint blog_agent to
Supabase/Cloudflare.

**Done means:** server reads all data from Supabase via `pg` Pool; Cycle 3 Server Actions switch
mock→backend on `DASHBOARD_DATA_SOURCE=backend`; all 22 items show real values + 1-day change;
blog images served from Supabase Storage `blog.prettylog`; AWS S3 SDK/env vars removed.

**blog_agent (Phase B repoint — BA-09, separate session, USER prereqs):** `DB_PG_*` →
`DATABASE_URL` (asyncpg + Supabase pooler); boto3/AWS S3 → Supabase Storage S3-compatible
endpoint + `blog.prettylog` bucket; `CDN_ASSETS` → Cloudflare CNAME; clean RDS CA / SSH /
boto3 remnants. Ties off `00.tasks.md` S8.3.
- **USER prereqs:** create `blog.prettylog` bucket (DONE — created PUBLIC 2026-06-26); set
  Cloudflare CNAME (OPEN); confirm pooler `DATABASE_URL` (DONE).

**Storage decision:** AWS S3 deleted → Supabase Storage is a fresh setup (permanent 1 GB free
tier), not a migration. The image bucket is env-driven via `SUPABASE_STORAGE_BUCKET` (default
`blog.prettylog`, commit 20dbf26). `CDN_ASSETS` (default `assets.prettylog.com`, commit 3100a9f)
is served via Cloudflare fronting the Supabase public bucket — the CDN MUST rewrite
`/<bucket>/<path>` → origin `/storage/v1/object/public/<bucket>/<path>`. Bucket `blog.prettylog`
is created PUBLIC (USER, DONE 2026-06-26).

**Ingestion cron:** DEFERRED (post-refactor). Preferred future host: GitHub Actions scheduled
workflow (avoids Vercel 10s timeout). Supabase 7-day auto-pause is an accepted known risk;
fallback = manual resume.

**Non-goals:** auth, user-facing writes, ingestion cron.

---

### Phase C — Production Hardening

No user-visible features; correctness + maintainability.

- `env.schema.ts` build-time Zod failure on missing required vars
- Split `useTradingViewChart.hook.ts` (369 LOC) and re-slim `ChartPanel.client.tsx` (218 LOC)
- Extract `dashboard_server` service layer (`controllers/posts/posts.ts` 591 LOC, SQL inline, cursor pagination ×3)
- Remove/reconcile dead deps; resolve `@repo/axios` vs local `apis/axios_instance.ts`
- Phase 4 pnpm-migration checkbox reconciliation; `.agent/rules/nexjs.rule.md` consolidation

---

### Phase D — Multi-User Blog Platform (Clerk Auth)

> **STATUS (2026-06-26): Phase D is ENG COMPLETE — remaining work = USER go-live gates only.**
> Shipped this effort: D1 auth foundation + lazy provisioning + onboarding (commit 8ca524e);
> D2 authoring (commit dc28f19); D4 admin moderation (commit 70375e6); D-lazy.4 dependency/env
> cleanup (commit 3a77847). D4.2 `/admin/invites` DEFERRED/DROPPED. Open USER gates: Clerk env on
> BOTH Vercel projects (frontend `pk_live`+`CLERK_SECRET_KEY`; backend `CLERK_SECRET_KEY`+
> `ALLOWED_ORIGINS`) + Clerk dashboard public-sign-up ON; backend `SUPABASE_URL`+`SUPABASE_SECRET_KEY`
> (else image upload → 503); admin bootstrap SQL (D4.0b). Sole residual ENG (non-blocking, deferred):
> D3.4 `/settings/profile`.

Largest phase; do not begin until Phase B is stable (schema `0003` + the security filter below
may land earlier). **Signup model CHANGED (2026-06-26):** now **open self-signup** via lazy
provisioning — invite-only is no longer the active model (see "Provisioning UPDATE" below). The
`ALLOW_OPEN_SIGNUP` env flag was **removed** in D-lazy.4 (commit 3a77847); open self-signup is now
the unconditional default. The `invites` table ships in `0003` but is dormant until/unless
invite-only is re-enabled.

**Central decision — auth enforcement (FINAL, two design agents reconciled):** Clerk verification
happens at the **Express layer**; the `pg` Pool + raw SQL stay. The Express server connects to
Postgres directly (service-equivalent credentials), so it **BYPASSES Supabase RLS by design**.
Therefore authorization is enforced in the **Express application/service layer, NOT in RLS**.
Every write derives `user_id` from the **verified Clerk token → mapped `users` row**, NEVER from
the request body. *(Rejected: a supabase-js + user-JWT rewrite of the 591-line raw SQL; rejected a
hybrid pg/supabase-js split.)*

**Integration method:** native Supabase **Third-Party Auth (TPA)** — register Clerk as a TPA
provider. For the Express path, even TPA is not required: Express verifies the Clerk JWT via
`@clerk/express` `verifyToken` against the Clerk JWKS using `CLERK_SECRET_KEY`. TPA/RLS is
**optional defense-in-depth for any FUTURE supabase-js read path only**. *(Deprecated and removed:
the "JWT template signed with the Supabase JWT secret" + `clerk_user_id()` SQL helper approach.)*

**Identity mapping:** Clerk owns identity (string `user_2ab…`). Supabase `users` keeps its integer
`id` PK; add a `clerk_user_id TEXT UNIQUE` bridge column. Request flow:
verify JWT → `sub` → `SELECT id,username,is_admin FROM users WHERE clerk_user_id=$1` → attach
`req.auth`. `DEFAULT_USER_ID=2` stays **ONLY** as the blog_agent's publisher identity — never a
fallback for HTTP writes.

**Provisioning (ORIGINAL — superseded):** invite-only. Clerk `user.created` webhook →
`POST /v1/api/webhooks/clerk` (Svix-verified via `CLERK_WEBHOOK_SECRET`) → validate invite →
INSERT into `users` (idempotent, `ON CONFLICT (clerk_user_id) DO NOTHING`).

**Provisioning UPDATE (FINAL, 2026-06-26) — Svix webhook → lazy provisioning:** the `user.created`
webhook is replaced by **lazy provisioning** with `resolveAuth` as the single chokepoint. A valid
Clerk token with no `users` row creates a minimal row (`clerk_user_id` only, `username` NULL) via
race-safe `INSERT ... ON CONFLICT DO NOTHING` + UNION SELECT; `username` is later claimed at
`/onboarding`. The webhook is **soft-deleted** (`clerk.ts`→`x_clerk.ts`, `index`→`x_index.ts`),
`/api/webhooks` is **unmounted**, and the `express.raw()` body block is removed from `server.ts`.
- **Recap (provision-time fields):** at first authenticated hit ONLY `clerk_user_id` is stored;
  `email` and `username` are **NULL** (the token exposes only `sub`). `username` is claimed at
  onboarding; `email` is never backfilled. → admin bootstrap + any user lookup must key on
  `username` or `clerk_user_id`, NEVER `email`.
- **Trade-off:** lazy provisioning **cannot invite-gate** and cannot backfill email from the token
  (only `sub` is available). Reverting to invite-only later requires re-enabling the webhook **or**
  adding a Clerk custom JWT claim.
- **No new migration:** `users.username` is already `UNIQUE` + nullable (migration `0001`) and
  `clerk_user_id` is already `UNIQUE` (migration `0003`). Runtime gate: lazy upsert requires the
  `0003` columns (`clerk_user_id`/`is_admin`/`display_name`) to be live in Supabase before relying
  on it in prod (DONE — `0003` applied, Local=Remote).
- **Onboarding:** new users land on `/onboarding` (`<SignUp forceRedirectUrl="/onboarding">`,
  middleware-protected) to claim a username (`^[a-z0-9_]{3,30}$` + reserved blocklist), then
  redirect to `/blog/@username`. Execution detail in `00.tasks.md` Phase D D-lazy/D-onb.

**Schema (additive, migration `0003_clerk_multiuser.sql`):**
- `users` += `clerk_user_id TEXT UNIQUE`, `display_name`, `avatar_url`, `is_admin BOOLEAN DEFAULT false`.
- New `invites` table: `code` unique, `created_by`, `used_by`, `used_at`, `expires_at`
  (dormant under open self-signup; used only if invite-only is re-enabled).
- `posts`: **NO structural change** — `user_id` is already the owner FK; `status` enum
  (`'public'`/`'private'`/`'follower'`) is already the publish/visibility axis (do **NOT** add a
  `published` bool); `slug` stays non-unique (versioned via `ROW_NUMBER() PARTITION BY slug` — do
  **NOT** add `UNIQUE(user_id,slug)`). Optional: add the currently-missing `posts.user_id→users.id` FK.
- Must update **BOTH** schema SSOT copies (`dashboard_server/schema` + `blog_agent/schema`) so
  `pnpm check:schema` stays green.

**SECURITY FIX (must call out):** `GET /posts/users/:username` list query currently lacks a
`status='public'` filter — harmless while single-author, but it **leaks other users' private/draft
posts** once multi-user. Add the filter.

**D2 Authoring — Cycle 2 design findings + decisions (2026-06-26):**

- **CRITICAL FINDING — global feed leak (highest severity):** the global list route
  `GET /v1/api/posts` (in `posts.ts`) has **NO status filter** (only `deleted_at IS NULL`), so every
  user's private/draft/follower posts are exposed to anonymous callers. D2.7 only closed the two
  single-fetch routes; the global feed is **still leaking**. Known security defect — closed this
  cycle in **D2.3a** (`AND p.status='public'`).
- **CRITICAL FINDING — no `draft` enum value:** `post_status_enum` is `('public','private','follower')`
  — it has **NO `'draft'`**. Draft authoring (D2.2/D2.5) requires migration `0005_post_status_draft.sql`
  (`ALTER TYPE post_status_enum ADD VALUE IF NOT EXISTS 'draft'`) mirrored to **BOTH** schema SSOT
  copies, then a **USER `supabase db push`** (DONE — `0005` applied, `'draft'` present, Local=Remote).
- **Image upload decision — Option B (signed-upload-URL):** the server issues a Supabase Storage
  signed URL via native `fetch` (no `@supabase/supabase-js` dep, no multer); the client PUTs the file
  directly to Supabase; the post body stores `${CDN_ASSETS}/${path}`. Avoids Vercel
  body-size/timeout limits and keeps the server pg-Pool-only. *Rejected: Option A (server proxies
  bytes via multer); Option C (client supabase-js + RLS).*
- **File-size rule:** `posts.ts` (591 LOC) stays **read-only**; writes go in a new `posts.write.ts`;
  the `posts/index.ts` barrel composes both routers.

**D2 Authoring — SHIPPED (commit dc28f19, 2026-06-26):** all ENG tasks landed (contract freeze,
`0005` migration authored, write router, drafts route, global-feed + profile status filters,
signed-URL image upload, `/write` editor + autosave). USER gates: `db push 0005` (D2.1b) DONE;
`blog.prettylog` bucket created PUBLIC (D2.6a) DONE, but backend `SUPABASE_URL`/`SUPABASE_SECRET_KEY`
still OPEN (else image upload → 503). **Two MUST-FIX integration fixes shipped:** (1) the editor seed
loads via an owner-scoped `getPostById` so opening an existing post no longer clobbers the draft on
first autosave; (2) `validatePostInput` relaxes the title requirement for `status='draft'` (drafts
may save with an empty/partial title; full validation still enforced on publish).

**D4 Admin — findings + decisions (Cycle 3, 2026-06-26):**

- **Admin delete = SOFT-delete (`deleted_at`), acting on ANY post by id — NOT hard-delete.**
  Rationale: `posts.parent_id` is `ON DELETE CASCADE`, so a true `DELETE` cascades to child posts;
  and `post_tags`/`files`/`post_categories` lack FK cascade, so a true `DELETE` orphans their rows.
  Soft-delete (`deleted_at` stamp) is therefore the correct moderation primitive. Admin routes are
  NON-owner-scoped (operate by id under `requireAdmin`), unlike the owner-scoped author write paths.
- **Admin bootstrap gap + decision (Option A — manual SQL).** Lazy provisioning sets
  `is_admin=false` and there is **no grant UI**, so the **first admin is created manually**: after
  the owner signs up + completes onboarding, run Supabase SQL
  `UPDATE users SET is_admin=true WHERE username='<owner>'` (D4.0b); verify via
  `GET /v1/api/users/me` → `isAdmin:true`. **Match key:** lazy-provisioned rows have `email` NULL
  (only `clerk_user_id` stored at provision; `username` claimed at onboarding), so the bootstrap SQL
  MUST match by `username` (or `clerk_user_id`), **NEVER** by email. Owner = `taek.lim.us@gmail.com`
  (Clerk identity). **Future option (noted, not built):** an env allowlist `ADMIN_CLERK_IDS` checked
  in `resolveAuth` for self-bootstrap without manual SQL.
- **D4.2 `/admin/invites` — DEFERRED/DROPPED.** Under open self-signup there is **no enforcement
  point** (webhook soft-deleted; lazy provisioning ignores invites), so an invite-management UI
  would create a **false-authorization illusion** (codes that gate nothing). Reviving invite-gating
  requires EITHER re-enabling the Svix webhook (restore `x_clerk.ts`/`x_index.ts`, re-mount
  `/api/webhooks`, restore `express.raw`, re-register the Clerk endpoint + `CLERK_WEBHOOK_SECRET`)
  OR adding a Clerk custom JWT claim checked in `resolveAuth`. Recorded as a known trade; not built.

**Top risks:**
1. **IDOR / privilege escalation** — the `pg` Pool bypasses RLS, so a missing `WHERE user_id=$caller`
   has **no DB safety net**. Mitigation: a single `requireAuth` middleware as the sole source of
   `userId` + owner-scoped queries + jest tests proving user A cannot mutate user B's post.
2. **Deprecated-method trap** — the old JWT-template/`clerk_user_id()` approach; this rewrite fixes it.
3. **Unprovisioned user** — a Clerk user with no local `users` row would 500. Mitigation (now
   shipped): lazy provisioning in `resolveAuth` creates the row on first authenticated hit
   (idempotent, race-safe). *(Replaces the original "idempotent webhook + fail-soft 403".)*

**Milestones (D1→D4 shape retained):**
- **D1 — Auth foundation (SHIPPED, commit 8ca524e):** Clerk app (open self-signup);
  `@clerk/nextjs` (client) + `@clerk/express` (server); `verifyToken` → `users` row → `req.auth`;
  `requireAuth`/`requireAdmin`; **lazy provisioning in `resolveAuth`** (replaces the `user.created`
  Svix webhook); username `/onboarding` claim; optional Supabase TPA registration.
- **D2 — Authoring UI (SHIPPED, commit dc28f19):** `/write` Markdown editor; draft auto-save
  (`status='draft'`); `POST`/`PATCH`/`DELETE /v1/api/posts` owner-scoped; signed-URL Option B image
  upload to Supabase Storage. Cycle 2 closed the global-feed status leak (D2.3a) and added the
  `'draft'` enum via migration `0005`. USER gates: `db push 0005` DONE; `blog.prettylog` bucket DONE;
  backend `SUPABASE_*` creds still OPEN. See "D2 Authoring — findings + decisions" + "D2 Authoring —
  SHIPPED" above.
- **D3 — Publish + profiles:** publish/unpublish (SHIPPED via owner-scoped `posts.write.ts` PATCH,
  dc28f19); de-hardcode `taeklim` → `params.username` (SHIPPED); author byline (SHIPPED, 01574aa);
  `/settings/profile` (D3.4) — **residual ENG, deferred (non-blocking)**.
- **D4 — Admin moderation (SHIPPED, commit 70375e6):** `is_admin`; `/admin/posts` (list +
  unpublish via `status='private'` + **soft-delete any post by id**, NON-owner-scoped under
  `requireAdmin`). First admin via manual USER SQL (no grant UI — D4.0b, still open USER gate).
  `/admin/invites` is **DEFERRED/DROPPED** — no enforcement point under open self-signup. See "D4
  Admin — findings + decisions" above.

Each sub-milestone's atomic acceptance criteria are in `00.tasks.md` Phase D.

---

## Free-Tier Reality Check

| Concern | Limit | Status |
|---|---|---|
| Vercel Hobby | Personal use; private repo must be under an **individual** GitHub account (org needs Pro) | Repo must be transferred org → individual; then `git remote set-url`. |
| Supabase Free | 500 MB DB, 1 GB storage, 7-day auto-pause | Estimate DB size before Phase B. Auto-pause accepted; manual resume fallback. |
| Supabase Storage | 1 GB permanent free | Blog images here from Phase B; `blog.prettylog` bucket created PUBLIC; optional Cloudflare CNAME. |
| Clerk Free | 10,000 MAU | Safe for open self-signup launch (Phase D). |
| Vercel Serverless Hobby | 100 GB-h/mo, 10s timeout | Fine for blog reads. Ingestion cron deferred → GitHub Actions. |

---

## Decisions Log (resolved questions)

1. **Commercial?** No — personal/non-profit; Vercel Hobby appropriate.
2. **Signup model (Phase D)?** Open self-signup via lazy provisioning — was invite-only; the
   `ALLOW_OPEN_SIGNUP` flag was later removed (3a77847), open self-signup is now the unconditional
   default; `invites` table ships dormant. *(Updated 2026-06-26.)*
3. **ORM?** Raw SQL kept; `pg` Pool replaces Sequelize execution; no supabase-js.
4. **Backend repo?** Two Vercel projects, one monorepo.
5. **Supabase pause?** Accepted known risk while cron deferred.
6. **Ingestion host?** Deferred; GitHub Actions scheduled workflow preferred.
7. **`article_server` ref?** Removed from root `package.json` + `ecosystem.config.js`.
8. **S3 migration?** AWS S3 deleted → fresh Supabase Storage setup (Phase B).
9. **NCSOFT KRX suffix** — `036570.KS` likely; confirm with Twelve Data before Phase B. **OPEN.**
10. **`@repo/types`** — created (see Architecture Decisions).
11. **Vercel + org repo** — needs individual account; transfer required.
12. **Clerk↔Supabase integration (Phase D)?** Express-layer `verifyToken` keeps `pg` Pool (RLS
    bypassed by design → app-layer owner-scoping is the gate); native Supabase TPA (optional,
    future supabase-js reads only). Deprecated JWT-template/`clerk_user_id()` method dropped.
13. **Provisioning (Phase D)?** Lazy provisioning in `resolveAuth` (single chokepoint), NOT a Svix
    `user.created` webhook (webhook soft-deleted, `/api/webhooks` unmounted). Trade: cannot
    invite-gate or backfill email; revert needs webhook re-enable or a Clerk custom JWT claim. *(2026-06-26.)*
14. **Blog image upload (Phase D D2)?** Option B — server issues a Supabase Storage signed-upload URL
    via native `fetch`; client PUTs directly; body stores `${CDN_ASSETS}/${path}`. No supabase-js,
    no multer; avoids Vercel body-size/timeout limits, keeps server pg-Pool-only. Rejected Option A
    (multer proxy) + Option C (client supabase-js + RLS). *(2026-06-26.)*
15. **Admin delete (Phase D D4)?** **Soft-delete** (`deleted_at`), acting on ANY post by id, NON-owner-scoped
    under `requireAdmin` — NOT hard-delete: `posts.parent_id` is `ON DELETE CASCADE` (true delete
    cascades to child posts) and `post_tags`/`files`/`post_categories` lack FK cascade (true delete
    orphans rows). *(2026-06-26.)*
16. **First admin bootstrap (Phase D D4)?** Manual USER SQL (Option A): `UPDATE users SET is_admin=true
    WHERE username='<owner>'` after onboarding — lazy provisioning sets `is_admin=false` and there is
    no grant UI. **Match by `username`/`clerk_user_id`, NEVER email** (`email` is NULL for
    lazy-provisioned users). Future option (noted, not built): env allowlist `ADMIN_CLERK_IDS`
    checked in `resolveAuth`. *(2026-06-26.)*
17. **`/admin/invites` (Phase D D4.2)?** **DEFERRED/DROPPED** — under open self-signup there is no
    enforcement point (webhook soft-deleted, lazy provisioning ignores invites), so an invite UI would
    be a false-authorization illusion. Revival needs re-enabling the Svix webhook OR a Clerk custom JWT
    claim in `resolveAuth`. *(2026-06-26.)*
18. **Image storage bucket env-driven?** Yes — `SUPABASE_STORAGE_BUCKET` (default `blog.prettylog`)
    replaced the hardcoded `blog-images`. *(commit 20dbf26, 2026-06-26.)*
19. **CDN asset var rename?** `CDN_ASSET_POSTS`→`CDN_ASSETS` (server) /
    `NEXT_PUBLIC_CDN_ASSET_POSTS`→`NEXT_PUBLIC_CDN_ASSETS` (client); default domain
    `posts.prettylog.com`→`assets.prettylog.com`; served via Cloudflare fronting the Supabase public
    bucket — CDN MUST rewrite `/<bucket>/<path>` → origin `/storage/v1/object/public/<bucket>/<path>`.
    *(commit 3100a9f, 2026-06-26.)*
20. **Runtime terminology?** Glossary added to CLAUDE.md (front/client, ssr server/next server,
    express/backend) to disambiguate the two Vercel projects + the Next SSR layer. *(2026-06-26.)*
21. **Admin-bootstrap match key?** Lazy provisioning stores only `clerk_user_id` at provision
    (`email`/`username` NULL until onboarding), so bootstrap SQL + user lookups match by `username`
    (or `clerk_user_id`), **never** email. *(2026-06-26.)*
22. **Vercel server build (TS2339 deploy failure)?** `apps/dashboard_server/tsconfig.json` is made
    **self-contained** (inlines strict compiler options, no workspace `extends`) because `@vercel/node`'s
    bundled `ts-node@10.9.1` cannot resolve a tsconfig `extends → @repo/typescript-config` under TS ≥5.3
    and silently drops `strictNullChecks` (collapsing discriminated-union narrowing). `vercel.json`
    `buildCommand` is a deterministic `tsc` strict gate (`pnpm --filter @repo/types build && pnpm
    --filter dashboard_server check-types`). `@repo/types` stays **centralized** (single client↔server
    contract source — not the cause of the bug; per-app duplication rejected to avoid drift). Standing
    constraint: any tsconfig `@vercel/node` reads MUST inline strict. *(verified green on Vercel,
    2026-06-26; ref github.com/vercel/vercel/issues/12087.)*

**Still open:** #9 (KRX ticker suffix). Everything else resolved.
