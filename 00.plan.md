# Product Plan — tradelunch

**Last updated:** 2026-06-30
**Active branch:** feature/finance → main (after Phase A deploy)
**Owner:** Taek Lim

> Single source of truth for **product intent + roadmap**. Atomic execution lives in
> `00.tasks.md`; running state lives in `01.status.md`. Superseded/verbose docs are archived
> under `_docs/` (gitignored). See CLAUDE.md "Workflow Docs" rule.
>
> **Completed phases live in `_docs/archive/` (cold storage — do not read unless investigating
> history). This doc holds active + planned work only. See `.claude/rules/docs.md`.**

---

## Product Identity

Personal portfolio + fintech site for Taek Lim (GitHub: tradelunch), one Next.js 16 monorepo,
domain `my.prettylog.com`. Two surfaces:

1. **Blog / portfolio** — curated multi-user technical posts (Clerk auth). Posts are authored
   either by the Python publishing agent (`apps/blog_agent`, direct-write) OR via the in-app
   authoring UI (`/write`, Phase D2 + the authoring UX cycle — create/edit/autosave/thumbnail UX
   shipped).
2. **Financial dashboard** — TradingView-style chart + 22-item watchlist with indicators,
   drawing tools, intraday intervals. Now backend-sourced: `DASHBOARD_DATA_SOURCE=backend` flip done + chart history wired to the real backend (snapshot + chart both live).

---

## Tech Stack (locked)

- Next.js 16 App Router, TypeScript strict; Tailwind v4, Radix UI, shadcn/ui, lucide-react
- Turborepo monorepo: `apps/dashboard_client_web`, `apps/dashboard_server`, `apps/blog_agent`, `packages/*`
- Jotai, TanStack Query v5, next-intl, recharts (shadcn chart), lightweight-charts v5
- Backend: Express 5 + `pg` Pool (raw SQL) against Supabase pooler; no Sequelize, no supabase-js
- Publishing: Python LangGraph agent (`apps/blog_agent`), `uv`-managed
- pnpm 9.5.0; PM2 (current prod) → Vercel (post-deploy)

---

## Architecture Decisions (resolved)

**`@repo/types` package (2026-06-24).** Lean package (no build step, raw TS, `main`/`types` →
`src/index.ts`) for API-boundary types shared front+back: `ETreeNodeType`, `TCategoryTreeNode`,
`TPostTreeNode`, `TCategoryTreeResponse`, `TTreeNode`, `TTreeNodeWithChildren`. Client adds
`transpilePackages: ['@repo/types']`. `@repo/markdown-parsing` later retired from the client
(TOC inlined to `utils/markdown/`).

**Env var convention (kcdd2).** SCREAMING_SNAKE_CASE, no prefix namespacing (`DATABASE_URL`,
not `SUPABASE_DATABASE_URL`). Key vars: `DATABASE_URL` (Supabase pooler 6543), `NODE_ENV`,
`SITE_DOMAIN`, `API_SITE_DOMAIN`, `CDN_ASSETS`, `SUPABASE_STORAGE_BUCKET` (default `blog.prettylog`).
**Renamed (2026-06-26):** `CDN_ASSET_POSTS`→`CDN_ASSETS` (server) /
`NEXT_PUBLIC_CDN_ASSET_POSTS`→`NEXT_PUBLIC_CDN_ASSETS` (client), default domain
`posts.prettylog.com`→`assets.prettylog.com` (commit 3100a9f); the image bucket is env-driven via
`SUPABASE_STORAGE_BUCKET` (default `blog.prettylog`, replaced hardcoded `blog-images`, commit 20dbf26).

**Data access.** Raw SQL kept verbatim. Sequelize execution layer replaced by `pg` Pool
(`max: 1`) → Supabase transaction pooler (port 6543). No supabase-js. The 591-line `posts.ts`
SQL is unchanged.

**Pagination cursor contract (2026-06-26).** Post ids are **Snowflake** (~3.29e17, generated by
blog_agent) and EXCEED JS `Number.MAX_SAFE_INTEGER` (~9.0e15). All keyset list endpoints
(`p.id < cursor`) therefore treat the cursor as an **opaque numeric string**, default the maximum
int8 `'9223372036854775807'`, bind it as a string, and return `nextCursor` as the full-precision id
string. `@repo/types` `TAdminPostListResponse.nextCursor` is `string | number | null`. See
Decisions Log #24.

**Backend topology.** `dashboard_server` stays in the monorepo at `apps/dashboard_server`.
Two Vercel projects, one repo. Vercel Root Directory = the app dir; install/build from repo root.

**Vercel tsconfig strict-narrowing constraint (2026-06-26).** `@vercel/node` type-checks the
serverless function import graph with a bundled `ts-node@10.9.1`, which **cannot resolve a tsconfig
`extends` that points at a workspace package** (`@repo/typescript-config`) under TypeScript ≥5.3 —
it silently drops inherited `strict`/`strictNullChecks`, collapsing discriminated-union narrowing
(`TS2339` on `result.reason` after `if(!result.ok)`). Fixed in ts-node 10.9.2 but Vercel pins
10.9.1 (ref: github.com/vercel/vercel/issues/12087). **Resolution:** `apps/dashboard_server/tsconfig.json`
is **self-contained** — it inlines the compiler options (target/module/strict/esModuleInterop/
skipLibCheck/composite/ignoreDeprecations) instead of extending `@repo/typescript-config`; and
`vercel.json` `buildCommand` is `pnpm --filter @repo/types build && pnpm --filter dashboard_server
check-types`, a deterministic strict gate via real `tsc`, independent of `@vercel/node` internals.
No application code changed; the `src/helpers/*` discriminated unions were correct. **Standing
constraint:** any tsconfig that `@vercel/node` reads MUST inline strict (no workspace `extends`);
only this one server tsconfig is self-contained, all other workspaces still
`extends @repo/typescript-config`.

**Keep `@repo/types` centralized (2026-06-26, architect-validated).** `@repo/types` stays the
single source of truth for the client↔server HTTP contract — do **NOT** duplicate types per-app
(duplication would cause silent drift). `@repo/types` was **not** the cause of the Vercel TS2339
deploy failure (the failing types were local to `dashboard_server/src/helpers/*`); the fix was the
self-contained server tsconfig above.

**blog_agent merge (2026-06-25).** The Python LangGraph publishing agent was merged into the
monorepo as `apps/blog_agent` (lift-and-shift, code logic unchanged). Turbo recognises it via a
thin `package.json` stub delegating to `uv run`; `start` is excluded from `dev`/`build`, only
`test`/`lint`/`check-types` are filterable. Schema SSOT is
`apps/dashboard_server/schema/tradelunch.schema.sql`; `apps/blog_agent/schema/…` is a byte-identical
copy guarded by `pnpm check:schema` (drift detection). See "blog_agent" under Phase A/B below.
Full ADR archived in `_docs/00.blog-agent-merge.arch.md`.

**blog_agent is a DIRECT-WRITE publisher (2026-06-26, architect-verified; first real publish landed
2026-06-26).** `apps/blog_agent` writes blog content **directly to the database and storage** — it
does **NOT** call the Express `/v1/api/posts` write API (that write path is the Clerk-authenticated
`/write` UI surface). The agent inserts/updates `posts`/`files`/`categories`/`tags`/join rows
directly into Postgres (SQLAlchemy/asyncpg over the Supabase pooler) and uploads images directly to
Supabase Storage bucket `blog.prettylog` (supabase-py). **Publisher identity is `user_id=1`** (the
single real `users` row — `taeklim`, Clerk-linked; the earlier `DEFAULT_USER_ID=2` never existed and
caused an FK violation — see Decisions Log #25); Clerk is not involved in the publish path. Express
only **READS** the agent's rows (the public blog feed + post detail). This is the intended division:
the agent is the single-author content producer; the Express server is the read API; the Clerk
`/write` UI is the separate multi-user authoring path (Phase D). Implication: publish→read
correctness depends on the agent writing the **same row shapes the server reads** (status default
`'public'`, `is_thumbnail`, `stored_uri` as an absolute CDN URL, tags/categories join shapes). The
first real publish succeeded end-to-end on 2026-06-26 (`java-spring-jdbc`, status='public', thumbnail
public URL, reads back via `GET /v1/api/posts`) — see Phase B and `00.tasks.md` B5.

---

## Deployment Roadmap — Vercel + Supabase + (eventual) Clerk

Reference pattern: kcdd-market_v2 (proven, same owner). Replaces any prior "deploy to VPS"
assumption. **Execution detail (ordered stages S1–S9, owner per step, commands, acceptance,
rollback) lives in `00.tasks.md` Phase A.**

**Critical path to live blog + mock dashboard on `my.prettylog.com`:**

> S1.2 create Supabase project → S1.3 link CLI → S1.6 db push → S1.7 verify CTE →
> S3 runtime verify (dev server vs Supabase) → S4.2 backend Vercel project → S4.3 backend env →
> S4.4 verify → S5.2 frontend Vercel project → S5.3 frontend env → S6.1 confirm GitHub → S6.2 DNS.
> S7.1 CI done; S7.2 lint step deferred.

**Changed conditions (since original plan):** AWS RDS **deleted** → S2 data migration SUPERSEDED
(Supabase starts empty; fresh data authored directly). AWS S3 **deleted** → image migration
SUPERSEDED (images uploaded fresh to Supabase Storage from Phase B). Server publish scripts that
used Sequelize+S3 are BROKEN and excluded from the Vercel build — reworked as the Python
`apps/blog_agent` (Phase A merge) + Phase B repoint.

---

### Phase A — Deploy Current State (Free, Read-Only) — DONE (2026-06-26)

> Read-only blog + mock dashboard live on Vercel; backend reads Supabase via `pg` Pool (no SSH/AWS); CI typecheck; acceptance gate MET. blog_agent merged (f51cc7e). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-A.md](./_docs/archive/plan.phase-A.md)

---

### Phase B — DB/Storage Repoint + Real Dashboard Data — DONE (backend, 2026-06-26)

> AWS Postgres/SSH/S3 retired; blog_agent repointed to Supabase pooler + Supabase Storage `blog.prettylog` + Cloudflare CDN (BA-09); first real publish landed end-to-end. Ingestion cron SUPERSEDED by Phase I. **B4 dashboard mock→real COMPLETE (2026-06-29): `DASHBOARD_DATA_SOURCE=backend` flipped + chart history wired to backend.** <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-B.md](./_docs/archive/plan.phase-B.md)
> **Open (non-blocking):** Cloudflare CNAME (branded-domain layer only); re-publish with Ollama up for LLM tags/categories. See `00.tasks.md` B5.6–B5.7. _(B5.5 client cursor `Number()` coercion FIXED.)_

---

### Phase C — Production Hardening

No user-visible features; correctness + maintainability.

- `env.schema.ts` build-time Zod failure on missing required vars
- Split `useTradingViewChart.hook.ts` (369 LOC) and re-slim `ChartPanel.client.tsx` (218 LOC)
- Extract `dashboard_server` service layer (`controllers/posts/posts.ts` 591 LOC, SQL inline, cursor pagination ×3)
- Remove/reconcile dead deps; resolve `@repo/axios` vs local `apis/axios_instance.ts`
- Phase 4 pnpm-migration checkbox reconciliation; `.agent/rules/nexjs.rule.md` consolidation

---

### Phase D — Multi-User Blog Platform (Clerk Auth) — ENG COMPLETE (2026-06-27)

> Clerk auth enforced at the Express layer (pg Pool bypasses RLS by design → app-layer owner-scoping); lazy provisioning in `resolveAuth` (no webhook) + `/onboarding` username claim; schema `0003`; `/write` authoring + autosave (D2, dc28f19) + create/edit UX cycle + editor i18n; D4 admin moderation = soft-delete any post by id (70375e6). Open-signup default; `/admin/invites` DROPPED. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-D.md](./_docs/archive/plan.phase-D.md)
> **Open USER gates:** Clerk env on BOTH Vercel projects + dashboard public-sign-up ON; backend `SUPABASE_URL`+`SUPABASE_SECRET_KEY` (else image upload → 503); admin bootstrap SQL (D4.0b, match by `username`/`clerk_user_id`, never email).
> **Open ENG:** lint/CI debt (S7.2 `--max-warnings 0`; blog_agent ruff + pyenv 3.12.3); non-blocking D3.4 `/settings/profile`. _(UX12b-persist thumbnail persistence SHIPPED `31eb14a`.)_

---

### Phase E — Blog Engagement (Saved Posts · Likes · Comments) — ENG COMPLETE (`7b2ea0a`)

> Saved Posts (`/me/saved`, migration `0007`), Likes (`0008` `post_likes`, optimistic button), Comments (`0009` Option-C materialized path, unlimited depth, soft-delete tombstone) — all shipped + pushed. Issue #1 = PARALLEL (blog_agent's posts-based comment columns untouched). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-E.md](./_docs/archive/plan.phase-E.md)
> **Open:** USER apply-migrations gate — `supabase db push` of `0007`+`0008`+`0009`, then re-run server jest for DB-gated assertions.

---

### Phase E2 — Comment Edit + Comment Pagination — DONE/SHIPPED (`85093b4`)

> Closed Phase E's two deferrals: comment EDIT (`PATCH /comments/:id`, auth==delete, tombstone→409, "(edited)" badge) + keyset pagination (50 roots/page each with full subtree, STRING cursor). Purely additive — no schema/index/migration change. `comments.ts` split into a barrel. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-E2.md](./_docs/archive/plan.phase-E2.md)

### Blog card actions — SHIPPED (`894fce3`)

> Post-card Share / Save / whole-card navigation; favorites backend (migrations `0006_post_favorites` + `0007_*_idx`, `controllers/favorites/` + `SaveButton`/`useSharePost`/`useFavorites`). Not a numbered phase. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/blog-card-actions.plan.md](./_docs/archive/blog-card-actions.plan.md)

---

### Phase F — Blog Image Upload Rework (Express proxy + 2-step resize) — DONE (`2a0457b`)

> `POST /v1/api/posts/images` (multer + sharp `rotate().resize(1600,inside).webp(q80)`, server-to-server upload, full error mapping); client step-1 native-Canvas resize (no library); old signed-URL path soft-deleted `x_*`; Decision #27 SUPERSEDES #14. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-F.md](./_docs/archive/plan.phase-F.md)
> **Open (USER):** backend `SUPABASE_URL`+`SUPABASE_SECRET_KEY` (else upload → 503) + F17 manual e2e + sharp libvips runtime confirm on Vercel.

---

### Phase F2 — blog_agent webp compression (local Python CLI) — DONE (`3e5b04c`)

> `apps/blog_agent/utils/image_transform.py` `to_webp` (Pillow LANCZOS + `exif_transpose`, 1600 fit-inside, q80, metadata stripped); thumbnails keep 1200×630 letterbox → webp q80 (Option A); path scheme unchanged; intentional duplicate of Phase F's Node transform. Decision #28. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-F2.md](./_docs/archive/plan.phase-F2.md)

---

### Phase G — Editor Category Selector + Tags (write/edit screen) — DONE (`bced84e`)

> Cascading 3-box category picker + flat tag-chip editor on `/write`; migration `0010` switches category uniqueness to `(user_id, parent_id, title)`; tri-state `category_id` PATCH (CASE not COALESCE); by-id read now returns `tags` + `stored_uri`; new `POST /v1/api/categories`; AC1–AC10 met. Decision #29. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-G.md](./_docs/archive/plan.phase-G.md)
> **Open (USER):** `supabase db push` migration `0010_category_unique_parent.sql`.

---

### Phase H — Multi-user Reddit-style Blog (global feed + context rails) — DONE P0–P5 (`2c0f897` / `f83a38e`)

> Reddit-style 3-col `BlogShell` route group (`app/(feed)`); `/tags/[tag]` global tag feed; global TopBar (SearchBar tag-nav + CreateButton); identical global LEFT rail (PrimaryNav + global TagCloud); per-user context RIGHT rail (category tree + scoped tags + profile card) with `<lg` sheet; localStorage recents/saved-tags; 4 viewer-agnostic tag-read APIs. Categories stay per-user; topics = tags. Decision #30. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-H.md](./_docs/archive/plan.phase-H.md)
> **Open (DEFERRED):** P6 — real search API (`GET /v1/api/posts/search`) + `/explore`; HX.1/HX.2 non-blocking follow-ups.

---

### Phase 2-mobile — Mobile context UX (chip header + in-article TOC) — SHIPPED (MOB.0–MOB.3)

> Two additive client-side-only mobile (`<lg`) UX changes — (D) top-of-article collapsible TOC, (B) top-of-feed context header (author chip + category/tag chip row) — replacing the heavier `<lg` `UserContextSheet`; Phase 2-filter later SUPERSEDED the NAV chip row (`MobileChipRow`/`getMobileChips` `x_`-retired). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-2-mobile.md](./_docs/archive/plan.phase-2-mobile.md)

---

### Phase 2-filter — Multi-category + multi-tag feed filter (desktop rails + mobile rows) — DONE (SHIPPED 2026-06-29)

> Reader narrows the author feed `/blog/[username]` by categories AND/OR tags at all breakpoints; URL state (`?categories=a,b&tags=a,b`), same-type OR / cross-type AND, no migration; SUPERSEDES Phase 2-tags. Decision #34. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-2-filter.md](./_docs/archive/plan.phase-2-filter.md)

---

### Phase 2-fetch — Feed data layer: axios→native fetch + tag revalidation — DONE (SHIPPED 2026-06-29)

> Blog feed read moved off client axios onto a server-only native fetch wrapper for Next 16 tag-based revalidation (instant read-your-writes on publish-class mutations); Express CDN backstop lowered. Builds on Phase 2-filter; delivers the instant-invalidation upgrade #34 F.11 deferred. Decision #35. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-2-fetch.md](./_docs/archive/plan.phase-2-fetch.md)

---

### Phase I — Stock Collector 편입 + Yahoo OHLC 적재 + 시총 랭킹

> **STATUS (2026-06-29): LIVE on prod + COMMITTED (main `f971661`) + architect-validated.** Daily run verified live (universe=42, 9870 bars + 42 snapshots, skipped=0); ENG follow-ups shipped (parquet archive wired + seeded 11636 rows/83 obj, I2.8 cache + migration 0013 applied, `SUPABASE_SECRET_KEY`+apikey, checkout v7). `supabase db push` 0011/0012/0013 APPLIED. Dashboard `DASHBOARD_DATA_SOURCE=backend` flip DONE + chart history wired → `/dashboard` now backend-sourced; remaining = KOSPI/KOSDAQ `.KS/.KQ` coverage (Decision #32) + I1.16 USER live-prod READ smoke. `apps/stock_collector/` built
> autonomously — Phase 1 (daily OHLC→snapshots) + Phase 2 (weekly market-cap ranking, sticky universe, migrations
> 0011/0012) DONE(ENG); Phase 1.5 parquet archive wired into run_daily + seed_archive (seeded 11636 rows → 83 objects in PRIVATE market-archive bucket). 70 pytest green,
> check:schema in sync, functional-core purity verified (psycopg sink-only), all files ≤300 LOC. Layout refinement:
> all Python under `src/` with import root `collector.*`. Atomic per-item status + USER gates + follow-ups in
> `00.tasks.md` Phase I banner. Live Yahoo fetch smoke 14/14 OK; live DB write verified; dashboard live-READ smoke = remaining USER step.

> **Intent:** 외부 Python 시장-데이터 수집기를 모노레포에 `apps/stock_collector/`로 편입하여
> **기존** 금융 대시보드(`market_snapshots`/`market_history` 테이블, reader
> `controllers/dashboard/dashboard.ts`)에 실데이터를 공급한다. Phase 1은 Yahoo(yfinance) OHLC
> 일간 적재 + 스냅샷 합성으로 대시보드를 mock→real로 전환하고, Phase 2는 시총 기반 글로벌/섹터
> 랭킹으로 watchlist를 자동 확장한다. 소유자 + system-architect + ml-engineer가 사전 결정했으며
> 설계는 FINAL이다. Atomic breakdown은 `00.tasks.md` "Phase I" (I1.x / I1.5.x / I2.x / I3.x). 자세한
> 결정 요약은 Decisions Log #31 참조.

**편입/런타임 결정 (locked):**

- **편입 방식 = SIMPLE COPY** (git subtree 사용 안 함). 원본은 소유자가 수동 제거한다. pnpm은
  이 앱을 무시한다(`package.json` 없음 → workspace 멤버 아님); polyglot sibling 앱으로 공존한다.
  `.gitignore`에 `apps/stock_collector/{data,.venv,queue,*.csv,htmlcov}`를 추가한다 — **원시 CSV는
  절대 커밋하지 않는다(107MB)**.
- **소스 = Yahoo(yfinance) ONLY (Phase 1).** SEC 13F(94MB)는 **제외**한다.
- **스케줄링 = GitHub Actions cron** (Supabase가 아님): DAILY OHLC + WEEKLY 시총 re-rank. 시크릿은
  GH Actions Secrets에. 러너는 ephemeral — CSV는 런 도중에만 존재하고 업로드/커밋되지 않는다.
- **스토리지 스택:** Supabase Postgres = serving(대시보드가 SQL로 읽음, 진짜 UPSERT
  `ON CONFLICT`). Supabase Storage + Parquet = archive/analytics(Phase 1.5). 단일 공유 Supabase
  프로젝트(blog + dashboard + market 공존).
- **Supabase 무료 티어 OK:** 500MB DB / 1GB storage / 5GB egress / unlimited API / 7-day pause(매일
  쓰기로 pause 방지). 추정 OHLC DB ~70–90MB, Parquet archive ~75MB — 모두 한도 이하.
- **카테고리(대시보드 계약):** `fx`, `crypto`, `indices`, `rates`, `stocks`. Seed watchlist =
  편집 가능한 `configs/watchlist.yaml`(category-keyed).

**Sticky universe (USER RULE — 어떤 에이전트 제안보다 우선):**

- **한 심볼이 랭킹에 한 번이라도 진입하면 영구히 수집한다. 자동 제거 없음, 히스테리시스 없음.**
  Soft-delete(`deleted_at`)만, 그리고 **수동 admin 제거에 한해서만** — 자동 제거는 절대 없다.
  프로젝트 규칙(어떤 행도 hard-DELETE 금지)을 그대로 따른다.

**랭킹/계약 결정:**

- **Sector ranking depth = top-10** (top-5 아님). 추가로 **global top-20**.
- **FX 라벨 = SOURCE-NATIVE direction (RESOLVED):** EUR=`EURUSD=X`→라벨 "EUR/USD";
  KRW=`KRW=X`→"USD/KRW"; JPY=`JPY=X`→"USD/JPY"; THB=`THB=X`→"USD/THB". 기존 `seed.market.sql`의
  stale 라벨 `'KRW/USD'`는 source-native `'USD/KRW'`로 교체한다.
- **rates는 Phase 1에서 Yahoo treasury YIELD 지수로 채운다 (RESOLVED, 연기 아님):** `^IRX`(3M),
  `^FVX`(5Y), `^TNX`(10Y), `^TYX`(30Y). (US 1M/1Y, KRX KOSPI200 선물, KOSDAQ150은 Yahoo에 없음 →
  Phase 3.)

**Seed watchlist (`configs/watchlist.yaml` — Phase 1에서 ALL 카테고리 채움; label은 전 카테고리 전역 유일):**

- **fx (source-native):** `EURUSD=X`→"EUR/USD", `KRW=X`→"USD/KRW", `JPY=X`→"USD/JPY", `THB=X`→"USD/THB"
- **crypto:** `BTC-USD`→"BTC/USD", `ETH-USD`→"ETH/USD", `SOL-USD`→"SOL/USD"
- **rates (yields):** `^IRX`→"US 3M", `^FVX`→"US 5Y", `^TNX`→"US 10Y", `^TYX`→"US 30Y"
- **indices:** `^GSPC`→"S&P 500", `^IXIC`→"NASDAQ Composite", `^NDX`→"NASDAQ 100", `NQ=F`→"NASDAQ 100 Futures", `^KS11`→"KOSPI", `^KS200`→"KOSPI 200", `^KQ11`→"KOSDAQ" — **KRX 스폿 지수 3개는 Yahoo가 제공하므로 Yahoo-coverage 규칙에 따라 Phase 1 포함**(USER 2026-06-28; Yahoo 미제공인 KOSPI200 선물/KOSDAQ150만 Phase 3)
- **stocks (label = ticker symbol, 자명 전역유일):** equity ETF `QQQ,QQQM,QLD,TQQQ,SPY,SCHD`; treasury ETF `SGOV,SHY,IEF,TLT`; theme/individual `TSLA,GOOG,AAPL,NVDA,SOXL,IBIT,MSTR,COIN,CRCL,SPCX,SMR,SOFI,XLE,DBA`; + 주간 자동 채움 global top-20 + sector top-10 (모두 label=ticker → 서술형 라벨과 충돌 0).
  총 seed = fx 4 + crypto 3 + rates 4 + indices 7 + stocks 24 = **42 라벨, 전부 전역 유일.** (KRX 스폿 지수 3개 포함 — Yahoo-available.)

**아키텍처 결정 (system-architect — 검증된 계약 사실):**

- `market_history` PK=`(label,interval,bar_time)`, **category 컬럼 없음** → `db_sink`는 **LABEL로
  기록**한다(라벨은 전역 유일해야 함).
- `market_snapshots` PK=`(category,label)`, `CHECK category in 5set`, **change 컬럼 2개**
  (`change_absolute`+`change_percent`, 둘 다 `NOT NULL` — 단일 change 아님; 리더가 `change:{absolute,
percent}`로 매핑), `value/change_absolute/change_percent/as_of/revalidate_seconds/fetched_at`에
  `NOT NULL`; `ticker`/`exchange`는 NULLABLE이나 **stocks 행은 둘 다 채워야 함**(리더가 `category='stocks'`
  에서 `ticker`/`exchange` 반환, client `IStockItem.exchange`는 non-null `'US'|'KRX'`).
- client `IStockItem.exchange`는 strict union `'US'|'KRX'` — **`exchange`는 `stocks` 카테고리에만 존재하고 Phase 1 stocks는 전부 US 티커라 항상 `'US'`; KRX 개별주(.KS/.KQ) 편입 시 `'KRX'` 활성(Phase 3). KRX 스폿 지수(`^KS11`/`^KS200`/`^KQ11`)는 `indices` 카테고리라 exchange 필드가 없음 — 포함돼도 exchange 무관.**
- snapshot meta는 collector가 **합성(SYNTHESIZE)** 한다: `as_of`=최신 bar 날짜, `fetched_at`=런
  시각, `revalidate_seconds`=카테고리별(fx=60/crypto=30/indices=60/rates=86400/stocks=60).
- **디렉터리 `apps/stock_collector/`:** copy 재사용 `src/lib/{job_queue,rate_limit,constants,
read_all_tickers}`, `src/yahoo_client/{download_publisher,download_consumer}`. (**Massive drop —
  `src/massive_client/download_tickers`는 copy하지 않는다**: import-time side-effect 때문에 lib로
  import 불가; universe는 Wikipedia + IWB로 조립.) NEW 패키지 `src/collector/`:
  `config/watchlist_loader.py`; **FUNCTIONAL CORE 순수 모듈** `transform/{ohlc,snapshot,mapping,
fx_direction,universe_resolve}.py` + `ranking/{universe,screen}.py`; **IO BOUNDARY**
  `sink/{db_sink,parquet_sink}.py` (psycopg는 여기에만); `entrypoints/{run_daily,run_weekly}.py`.
  또한 `configs/{watchlist.yaml, settings.py}`, `schema/rows.py`, `pyproject.toml`(name=stock-collector)+
  `uv.toml`+`.python-version`(3.12)+`.env.example`. 레이어 방향 entrypoints→(config|transform|ranking)
  →sink. 파일 ≤300 LOC, SRP, functional core(순수 transform, psycopg는 sink/에 격리). 테스트
  `__tests__/spec_*.py`(pytest).
- **DAILY universe 병합(레이어 주의):** yaml ∪ tracked 병합은 **순수** `transform/universe_resolve.py`
  `resolve_universe`(deterministic)에 둔다; tracked_symbols **DB read**는 `sink/db_sink.py`
  `read_tracked_symbols()`에 둔다(transform/ 아님 — 레이어 방향 보존). `read_tracked_symbols()`는
  `to_regclass`로 테이블 존재를 가드 → 테이블 부재(Phase 1) 시 `[]` 반환(run_daily 미파손).

**DB 마이그레이션 (`apps/dashboard_server/supabase/migrations/`):**

- **`tracked_symbols`:** `symbol`(PK), `category`(CHECK 5set), `label`(UNIQUE — 전역 history 라벨
  보호), `sector` NULL, `source` DEFAULT `'yahoo'`, `exchange` NULL, `first_ranked_at`,
  `last_ranked_at`, `created_at`, `updated_at`, `deleted_at`(soft-delete 전용). re-rank 시 UNION =
  `ON CONFLICT(symbol) DO UPDATE`(`deleted_at=NULL`로 revive). `category` index
  `WHERE deleted_at IS NULL`.
- **`market_rankings`:** append-only 주간 시계열. `as_of` DATE, `symbol`, `scope`('global'|'sector'
  CHECK), `sector` NULL, `rank` INT, `market_cap` NUMERIC NULL, `created_at`. PK`(as_of,symbol,
scope)`. **모든** tracked 심볼을 주간 기록(top-N만 아님). idempotent =
  `ON CONFLICT(as_of,symbol,scope) DO UPDATE`.
- `market_history`/`market_snapshots`는 변경 없이 재사용; UPSERT `ON CONFLICT`. 두 마이그레이션은
  **BOTH** schema SSOT copy에 mirror하여 `pnpm check:schema` green 유지.
- **마이그레이션 번호 주의(sequencing):** **확정: `0010`(`0010_category_unique_parent.sql`, Phase G)
  적용됨 → Phase I = `0011`(tracked_symbols)/`0012`(market_rankings); 적용 직전
  `ls apps/dashboard_server/supabase/migrations/`로 다음 빈 번호를 재확인한다(multi-session 동시
  점유 회피).**

**데이터 흐름:**

- **DAILY: collection target = `watchlist.yaml` ∪ `tracked_symbols`(active)** — pure
  `transform.universe_resolve.resolve_universe`가 yaml 엔트리와 `sink.db_sink.read_tracked_symbols()`
  를 symbol-dedupe(yaml이 label/category/exchange에서 우선)로 UNION(`read_tracked_symbols`는
  `to_regclass`로 테이블 가드 → 부재 시 `[]`, Phase 1 미파손) → label별 incremental
  `from_date`(`max(bar_time)+1d`) → yahoo publisher/consumer → CSV(ephemeral) → `db_sink.load_history`
  → `db_sink.refresh_snapshots`.
- **WEEKLY:** `universe.assemble`(~600–1000 large-cap pool: Wikipedia S&P500 + Wikipedia Nasdaq100 +
  iShares IWB CSV(Russell 1000); **S&P500 leg는 GitHub `datasets/s-and-p-500-companies` CSV를
  parser-stable primary/fallback로** — Massive 의존 없음) → marketCap+sector fetch → `screen.rank`
  (global + per-sector 순수) → `tracked_symbols` UPSERT(UNION, sticky) + `market_rankings`
  INSERT(append).

**GitHub Actions (세 워크플로):**

- `.github/workflows/collector-daily.yml` — cron `"30 21 * * 1-5"`(월–금 종가 직접 캡처; 토요일
  휴장 drop; incremental `from_date`가 catch-up), `astral-sh/setup-uv`(commit SHA pin `# v8.x`,
  `enable-cache: true`, `working-directory: apps/stock_collector`), `uv sync`, `run_daily`,
  `concurrency:` group(중복 런 취소); 시크릿 `DATABASE_URL`=Supabase Supavisor **session pooler**
  (`aws-0-<region>.pooler.supabase.com:5432`; direct `db.<ref>.supabase.co:5432`는 IPv6-only라 IPv4
  GH runner에서 실패 → **금지**). 워크플로 실패 시 알림(`if: failure()` step) 필수.
- `.github/workflows/collector-weekly.yml` — cron `"0 6 * * 0"`, `run_weekly`, 동일 setup-uv pin +
  `concurrency:` group; 시크릿 `DATABASE_URL` only(**MASSIVE_API_KEY 없음** — universe는 Wikipedia+IWB).
- `.github/workflows/collector-keepalive.yml` — cron `"0 12 1,15 * *"`, GH API touch/dummy-commit으로
  repo 활동 유지 → **60일 무커밋 cron auto-disable 방지**(Supabase 7일 pause와 별개 — GH 측 제약).
- Phase 1.5는 Storage용 `SUPABASE_URL`+`SUPABASE_SERVICE_ROLE` 추가. **CSV는 절대 업로드 안 함.**

**Analytics/Parquet 레이어 (ml-engineer — Phase 1.5 상세):**

- Parquet = pyarrow, **zstd** 압축, 파티션 `market/{source}/{ticker}/{ticker}_{YYYY}.parquet`.
  컬럼: `symbol`(dict string), `date`(**date32** — timestamp 아님, tz drift 회피),
  `open/high/low/close`(float64), `adj_close`(float64), `volume`(int64), `dividends`(float64
  nullable), `stock_splits`(float64 nullable). `dividends`+`stock_splits`는 지금 추가(yfinance
  응답에서 무료, total-return/배당 팩터 가치).
- Parquet은 row upsert 없음 → 현재 연도 파일 = **read-merge-rewrite**: 기존 read → 신규 concat →
  `date` dedupe(keep=last) → sort → 고정 스키마 cast → tmp write → atomic `os.replace` → Storage에
  object upsert. 과거 연도 파일은 immutable. 연도 파티셔닝으로 일간 rewrite 저렴(~252 rows).
- **Weekly screening 최적화:** `market_cap = 캐시된 shares_outstanding × local close`(OHLC archive
  에서) — API 비용 절감; shares는 월간 `fast_info`로 refresh; sector는 `.info` 1회 후 분기 캐시.
  Rate-limit: yahoo 30rpm token bucket + exp backoff(2/4/8s, 3 retries) 재사용; partial 허용; 실패
  시 직전 주 `market_cap` carry-forward(전체 런 abort 금지). 심볼 정규화: Wikipedia `BRK.B` vs
  yfinance `BRK-B` — 두 형태 모두 저장. 후보 풀 소스: Wikipedia S&P500/Nasdaq100 테이블 + iShares
  IWB holdings CSV(IWB는 fragile로 flag, snapshot 캐시).
- **ML 컬럼:** 지금 KEEP = `dividends`/`stock_splits`/`adj_close`/sector 캐시/월간 shares. DEFER
  (YAGNI) = `.info` fundamentals(P/E 등 — point-in-time leakage 위험), financials, options chains,
  13F, intraday.

**리스크 / open items:**

- **yfinance datacenter-IP ban (최상위 prod 리스크):** Phase 1은 yfinance + pinned `curl_cffi`
  impersonation 세션. GH Actions 스케줄 실패가 silent → **run-failure 알림 필수**. Fallback ladder:
  self-hosted runner → residential proxy → 유료 provider. **Provider 선택 OPEN(Decisions #32).**
- **GitHub 60일 무커밋 cron auto-disable:** collector 미커밋 → keepalive(I1.17)로 방지(Supabase 7일
  pause와 별개 — GH 측 제약).
- **prod `market_snapshots`는 collector 첫 런 전까지 EMPTY** (`seed.market.sql`은 auto-apply 안 됨 —
  `config.toml`은 `seed.sql`만 로드). stale-row cleanup 불필요.
- **copy-in 기술부채:** subtree 미사용 → 업스트림 collector 수정은 수동 re-copy 필요.
- **KRX yahoo 커버리지(`.KS/.KQ` suffix)** — 지연/공백 가능; 데이터 품질 리스크 (Decisions Log #9
  KRX ticker-suffix open item과 연결).
- **Dual Postgres writer**(collector UPSERT + Express read) — short transaction, Supabase session
  pooler.
- **adj_close가 계약 OHLC에서 빠짐**(raw close, `auto_adjust=False`) — split/배당 점프 가능; 기존
  fetcher와 일관.
- **weekly rate-limit ~20–33분**(600–1000 심볼) — shares×close 유도로 완화.
- **리더 meta = 카테고리 첫 행만**(기존 계약 유지, 재설계 안 함): `categoryRows[0]`(seq 최소)의 `as_of`/
  `revalidate_seconds`를 카테고리 전체 meta로 사용 → `revalidate_seconds`는 카테고리 상수라 무해하나,
  라벨별 `as_of`가 다르면(US 휴장 vs KRX) 첫 행 값만 노출(표시상 nuance, non-blocking).
- **스냅샷 표시 순서 = `ORDER BY category, seq`**(최초 insert 순; UPSERT가 seq 보존) → sticky 신규 편입
  심볼은 맨 뒤 append, `watchlist.yaml` 순서는 표시에 보장되지 않음(허용; 정렬 요구 시 별도 order 컬럼 필요).
- **일간 수집 vs sub-minute `revalidate_seconds`**: Phase 1은 일간 OHLC만 적재하는데 fx=60/crypto=30/
  stocks=60은 준실시간 캐시 힌트라 클라이언트가 폴링해도 전일 종가만 받음(무해한 추가 fetch). 인트라데이
  신선도는 Phase 3(인트라데이 수집)에서 해결.

**Phase 구분 (요약 — atomic은 `00.tasks.md`):**

- **Phase 1:** copy-in(**lib/yahoo_client only; massive·sec13f 제외**); `.gitignore`;
  pyproject+uv.toml+.python-version+.env.example; `configs/watchlist.yaml`(fx/crypto/rates-yields/
  indices/stocks ALL, 42 라벨 전역유일) + settings.py; `schema/rows.py`; 순수 `transform/{ohlc,snapshot,
mapping,fx_direction,universe_resolve}` + spec; `watchlist_loader` + 검증(label 전역유일, exchange
  union, category 5set); `db_sink` UPSERT history+snapshots + `read_tracked_symbols`(table-guarded);
  `run_daily`(universe = yaml ∪ tracked); `collector-daily.yml`(cron `30 21 * * 1-5`) + `DATABASE_URL`
  시크릿 + `collector-keepalive.yml`; 대시보드 `/api/dashboard/snapshot`+`/history` 실데이터 검증;
  _(선택)_ stale `seed.market.sql` FX 라벨 source-native 갱신(prod 영향 없음).
- **Phase 1.5:** `parquet_sink`(pyarrow/zstd/date32, read-merge-rewrite, dividends+splits) → Supabase
  Storage; daily 워크플로 archive step + `SUPABASE_SERVICE_ROLE` 시크릿.
- **Phase 2:** 마이그레이션 `tracked_symbols` + `market_rankings`; 순수 `ranking/universe`
  (Wikipedia+IWB, MASSIVE 의존 없음) + `ranking/screen` + spec; `db_sink` tracked_symbols UNION/sticky
    - market_rankings append; `run_weekly` + rate-limit/backoff/partial/carry-forward;
      `collector-weekly.yml`(cron `0 6 * * 0`, `DATABASE_URL` only) 시크릿.
- **Phase 3 (DEFERRED):** FRED 1M/1Y yields→rates(Yahoo 미제공); KRX KOSPI200 선물/KOSDAQ150(Yahoo
  미제공); 추가 fx/crypto 소스. _(KRX 스폿 지수 `^KS11`/`^KS200`/`^KQ11`는 Yahoo-available이라 Phase 1에 포함됨 — 더 이상 Phase 3 아님.)_

**Non-goals:** SEC 13F 편입(제외); git subtree(simple copy); 원시 CSV 커밋(절대); Supabase cron(GH
Actions로); 자동 universe 제거/히스테리시스(sticky — soft-delete + 수동 admin만); 대시보드 reader
재설계(기존 `market_snapshots`/`market_history` 계약 유지).

---

### Phase J — SEC 13F Holdings (monthly batch) — DONE (2026-06-30)

> SEC EDGAR 13F-HR monthly batch in `apps/stock_collector`: monthly cron over curated fund CIKs → submissions + Archives infotable XML → `sec_filings`/`sec_holdings` (migration 0017). 0017 applied + first live run done. Technical decision record kept in Decisions Log #36. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-J.md](./_docs/archive/plan.phase-J.md)

---

### Phase K — `/funds` 13F Holdings Viewer (read-only) — DONE (2026-06-30)

> Read-only `/funds` viewer over the Phase J store (separate page, not /dashboard): Express `GET /api/funds`[`/:cik`] (`to_regclass` guard, SQL `SUM() OVER()` weight, s-maxage 86400) + Next `/funds`+`/funds/[cik]` (force-dynamic, no mock) + `/funds/preview` fixture. Live data shows (0017 applied + first run done). Technical decision record kept in Decisions Log #37. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.phase-K.md](./_docs/archive/plan.phase-K.md)

---

### Phase L — `/funds/[cik]` 13F 분기별 Rank-Flow 시각화 (backfill 2년 + 색상 랭킹 + 클릭 추적) — ENG DONE (active scope) / L-next DEFERRED (2026-06-30)

> **Intent:** Phase J/K(13F store + `/funds` 뷰어) 위에, 펀드의 분기별 holdings를 **컬럼=분기 / 행=보유종목(비중 랭킹) / 종목별 고정 색** rank-flow 테이블로 시각화. 한 종목 클릭 → 전 분기 컬럼에서 같은 종목 셀이 inner-border ring으로 active → 시계열 추적. system-architect(데이터/백필/엔드포인트) + product-manager(rank-flow UX) 공동 설계, 오너가 결정 잠금. Atomic = `00.tasks.md` Phase L. 결정 요약 = Decisions Log #38.

**오너 결정 (locked 2026-06-30):**

- **rank-flow = `/funds/[cik]` 유일·기본 뷰** — 기존 Phase K 평면 단일-분기 테이블은 은퇴(soft-delete `x_`), period 셀렉터 불필요(분기를 컬럼 축으로 공간화).
- **표시 분기 = 최근 8개(2년), 가로 스크롤. 백필도 최근 2년만 수집**(`run_backfill --since <2yr>`) — 데이터 윈도우 = UI 윈도우 일치, Supabase 무료티어(500MB DB) 보호(2년 추정 ~30–90MB).
- **행 = "8분기 중 한 번이라도 top-K(=25) 진입한 종목의 합집합"** — union 멤버는 모든 컬럼에 행 유지(그 분기에 K 밖이어도 실제 rank/weight 표시, 미보유면 ghost 셀) → bump-chart 연속성. 나머지는 컬럼별 "+M more (Σweight%)" 1행 집계.
- **종목 색 = cusip 해시 고정 hue**(rank/period 무관, 재렌더·리로드 불변; light/dark 대비 ≥4.5:1; 색은 보조 큐 — 텍스트 라벨+rank 항상 병기, colorblind 대비 active=ring 형태 큐).
- **Δ 배지(▲▼/NEW/EXIT/Δweight) · 비선택 디밍 · 연결선 = v1 미포함 → L-next follow-up.**

**데이터/백필 (architect):** 신규 순수 `all_13f(refs, since=)`+`group_by_period`+`merge_submission_pages`(sec_parse.py); 신규 fetch `submission_page_names`+`fetch_submission_page`(sec_fetch.py — `filings.files` 페이지네이션 안전망, `recent`는 ~1000건 캡이라 장기 펀드 분기 누락 방지; 2년이면 보통 `recent`로 충분하나 안전망 구현). 신규 `entrypoints/run_backfill.py`(argparse `--cik/--since/--limit/--dry-run`; 기본 `--since`≈2년 전; run_monthly 미러; per-(fund,period) iterate; fetch/parse/aggregate/upsert 재사용; `supersede_prior_filings`는 period-scoped+filing_date guard라 여러 분기 백필 안전). **workflow_dispatch 일회성** CI(반복 아님; 최신 분기는 monthly가 유지).

**rank-flow 엔드포인트 (v1, 분석 뷰 대신):** 신규 `GET /api/funds/:cik/rankflow?quarters=8&k=25` → `{periods:[{periodOfReport,totalValueUsd,remainingCount,remainingWeightPct}], rows:[{cusip,label,cells:{[period]:{rank,weightPct,valueUsd}|null}}]}`; rows=union-of-ever-top-K; funds.ts 컨벤션(to_regclass guard, padStart(10), parameterized, cache s-maxage 86400) 미러.

**UI (PM):** `RankFlowTable`(컬럼=분기 newest-left, `YYYY Q#` 헤더; 종목별 고정색 셀; 클릭→active cusip→전 컬럼 inner ring; toggle/switch 단일선택; 키보드 `aria-pressed`; 가로 스크롤; 375px 반응형 tap). 순수 util 3종+vitest: `cusipColor`(고정성 회귀잠금)·`quarterLabel`(03/06/09/12→Q1-4)·`rankFlowRows`(union+tie-break weight DESC→valueUsd DESC→cusip ASC). 신규 server action `getFundRankFlow`+Zod+AssertEqual. 평면 테이블 은퇴, preview fixture(≥2 분기) 갱신.

**L-next (follow-up, DEFERRED):** 마이그레이션 `0018` 분석 뷰(`v_sec_position_delta`/`v_sec_exits`/`v_sec_fund_period_summary`/`v_sec_consensus`) + **0017 테이블+0018 뷰를 BOTH schema SSOT 미러**(현재 0017이 SSOT 부재 → check:schema 보강 필요) + Δ 배지 + cross-fund consensus 엔드포인트/UI + 비선택 디밍/연결선. 또한 **SEC 13F Parquet 콜드 아카이브(별개 트랙):** 스코프 = **2013~현재**(구조화 XML information-table 전 구간; pre-2013은 비구조화 텍스트/HTML이라 현 파서 미지원 → 30년 거부, 실질 상한 ~13년) · **전체 holdings(top-N truncation 안 함 — ml-engineer 결정)** · ~30MB → PRIVATE Storage 버킷 `sec13f/{cik}/{cik}_{YYYY}.parquet`(기존 OHLC Phase 1.5 `parquet_sink` 패턴 미러). **hot=thin, cold=full**: DB 서빙은 2년 유지, 아카이브만 2013~현재 전체. 용량은 binding 아님(1GB의 ~3%); 진짜 1990년대는 별도 레거시-파서 R&D(미스코프).

**Non-goals:** DB 서빙 백필은 2년만(장기 이력은 별도 Parquet 아카이브 L-next); 30년 백필(pre-2013 구조화 데이터 부재 → 2013~현재만); 아카이브 top-N truncation(전체 보관); 분석 뷰 v1 포함(L-next); 평면 테이블 유지(은퇴); period 드롭다운(컬럼 축이 대체); 연결선/디밍 v1.

---

### Error Telemetry — browser/SSR/Express → `error_log` — SHIPPED (`81e0962`)

> 모든 런타임의 에러를 단일 Supabase `error_log` 테이블로 수집. **Next SSR은 DB 권한 없음**(설계) →
> Express가 유일 소유자, server-to-server forward. 3개 소스: **browser**(React error boundary
> `error.tsx`/`global-error.tsx` → `reportError.util` → Next `/api/log-error` → Express
> `POST /v1/api/error-logs`), **ssr**(`reportSsrError.server` — dashboard/funds 액션 catch에서 호출),
> **express**(터미널 글로벌 핸들러 `middlewares/errorHandler.ts` — Express-5 async reject 자동 포착,
> insert await로 serverless-safe, DB 실패는 swallow, 500 JSON). 공용 pure shaper `helpers/errorLog.ts`
> (`normalizeErrorLog`/`buildExpressErrorRow`, 캡 2000/8000/1000/500). 마이그레이션 `0014`(테이블,
> no-`deleted_at` 운영로그 예외) + `0015`(batch_log) + `0016`(`resolved` 트리아지) **모두 적용됨**.
> 외부 모니터링(Sentry 등) 없음. pg_cron 7일 TTL cleanup은 commented-out(USER/Supabase-dashboard 게이트).
> Gates: server check-types 0 / build 0 / jest 174/174.

---

### Phase M — market_history 5-year retention tiering (hot/cold) — PLANNED (system-architect 설계 2026-06-30)

> **Intent:** `market_history`(Postgres)를 **롤링 5년 HOT 서빙 윈도우**로 한정하고, 전체 불변 히스토리는 이미 LIVE인 **Parquet COLD 아카이브**(Supabase Storage)에만 둔다. **연 1회(12/30 cron + workflow_dispatch)** prune이 cutoff(**달력연도 경계 `<(today.year−5)-01-01`** → 완전 연도 파티션만 떨굼)** 이전 봉을 **해당 (ticker,year) Parquet 파티션이 Storage에 존재함을 라벨별로 검증한 경우에만\*\* 삭제 → "삭제가 아니라 티어 이동". 연 1회라 DB는 5~6년 톱니(피크 ~90MB); 277,870행/105MB 기준 → bounded(+무한증가 중단). Atomic = `00.tasks.md` Phase M (M1–M8). 기술결정 + 소프트삭제 예외 정문 = Decisions Log #39.

> **Scope:** `market_history`(OHLC)만. (Phase L의 2년 backfill은 `sec_holdings`(13F) 대상 — 별개 테이블, 상보적.) `market_rankings`(append-only)/`sec_holdings`(tombstone 누적)/`batch_log` 2차 retention은 OUT OF SCOPE(후속 flag만).

> **USER gate(파괴적):** prod prune LIVE 전환은 **M7**(seed_archive를 `fetch_full`로 inception까지 심화) + **M6 dry-run** 확인 후에만. prune 자체는 아카이브 비활성/도달불가 시 **완전 no-op**.

---

### Phase N — 2차 retention: error_log / batch_log TTL + market_rankings 10년 아카이브 prune — ENG DONE (2026-06-30)

> **Intent:** Decision #39가 OUT-OF-SCOPE로 넘긴 후속 3개 테이블에 Phase M prune 머신을 재사용. 성격별 2갈래: **(로그) error_log·batch_log** = 오너 승인 no-tombstone 예외 → 아카이브 불필요, 순수 **일수 TTL 하드삭제**(daily). **(도메인) market_rankings** = 재현불가 point-in-time 스냅샷(shares 미저장) → OHLC와 동일 **아카이브-검증-삭제**, 단 window는 ML 자문에 따라 **10년**. pg_cron 대신 기존 패턴(Python 엔트리포인트 + GitHub Actions + `batch_log` self-observe)으로 통일 (활성 pg_cron 0개). Atomic = `00.tasks.md` Phase N (N1–N9). 기술결정 = Decisions Log #40.

> **보존값:** error_log **7일**(폐기된 `error_log_cleanup` pg_cron 의도 계승) · batch_log **90일**(`resolved=0` 미해결 실패는 나이 무관 보존) · market_rankings **10년**(재현불가 + 팩터 lookback 2사이클; 5년 아님 — 5년은 재현가능한 OHLC 기준).

> **Cadence/모드:** logs = 신규 `collector-prune-logs.yml` **매일 03:00 UTC LIVE**(무-아카이브·저-blast, dispatch만 dry-run). rankings = `collector-prune.yml` **연 12/30 dry-run**(belt-and-suspenders, 첫 LIVE는 수동 dispatch). rankings 아카이브는 `collector-weekly.yml`에 best-effort `archive_rankings` 스텝(주간 갱신) → `rankings/{YYYY}.parquet`. **현재 후보 0건**(주간 시리즈 최근 시작 → ~2036까지 삭제 대상 없음; 전향적 안전장치). **DDL 없음**(0018 불필요 — 기존 컬럼·인덱스로 충족).

---

## Free-Tier Reality Check

| Concern                 | Limit                                                                                     | Status                                                                                                                                                                            |
| ----------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vercel Hobby            | Personal use; private repo must be under an **individual** GitHub account (org needs Pro) | Repo must be transferred org → individual; then `git remote set-url`.                                                                                                             |
| Supabase Free           | 500 MB DB, 1 GB storage, 7-day auto-pause                                                 | Estimate DB size before Phase B. Auto-pause accepted; manual resume fallback. Phase I daily writes prevent auto-pause; est OHLC DB ~70–90MB.                                      |
| Supabase Storage        | 1 GB permanent free                                                                       | Blog images here from Phase B; `blog.prettylog` bucket created PUBLIC; optional Cloudflare CNAME. Phase 1.5 Parquet archive ~75MB (PRIVATE analytics bucket, separate from blog). |
| Clerk Free              | 10,000 MAU                                                                                | Safe for open self-signup launch (Phase D).                                                                                                                                       |
| Vercel Serverless Hobby | 100 GB-h/mo, 10s timeout                                                                  | Fine for blog reads. Ingestion cron = GitHub Actions (Phase I), NOT Vercel.                                                                                                       |

---

## Decisions Log (resolved questions)

> 32 resolved decisions (#1–#31 + the resolved parts of #6/#14/#23/#25 supersessions) archived as
> cold storage. Cross-references elsewhere ("see Decisions Log #N") resolve there. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/plan.decisions-log.md](./_docs/archive/plan.decisions-log.md)
>
> **Still OPEN (kept here):**
>
> - **#9 — KRX ticker suffix** (`.KS`/`.KQ`, e.g. NCSOFT `036570.KS`): confirm yahoo coverage; also the Phase I KRX data-quality risk.
> - **#32 — Phase I market-data provider:** default (a) yfinance + pinned `curl_cffi` impersonation; on datacenter-IP ban escalate (b) self-hosted runner / residential proxy → (c) paid provider. GH Actions schedule failure is silent → `if: failure()` alert required. Separate axis from #9.
>
> **Resolved this cycle (kept inline until the phase ships, then archived):**
>
> - **#33 — Phase 2-mobile mobile context UX (PM+architect LOCKED 2026-06-29):** (M1) list pages keep ONLY the top category/tag chip row — soft-unwire `userContextSheet` from both list layouts, KEEP `UserContextSheet.client.tsx` file (rm-rf rule); (M2) detail page keeps ONLY the promoted `MobileToc` on mobile — DROP the duplicate mobile-sheet `RightRailToc` (architect option 2a), desktop `>=lg` `RightRailToc` unchanged. Not escalated to the user (repo convention: PM+architect decide option/choice points).
> - **#34 — Phase 2-filter multi-category + multi-tag feed filter (PM+architect LOCKED 2026-06-29; SHIPPED 2026-06-29, commits `a05a299`..`4e8483b`; SUPERSEDES the prior Phase 2-tags / TAG.1–8 tag-only design):** combined two-facet filter (categories AND tags) on the author feed `/blog/[username]`, applied at ALL breakpoints. (F1) **URL scheme** — plural `?categories=a,b` alongside `?tags=a,b`; legacy `?category_title=` is an alias merged into `categories` at parse (canonical out `categories=`, no redirect; client pure core + an Express safety net). (F2) **Category multi + ancestor-OR** — `$5 text[]` matched via array OVERLAP `cpath.path && $5::text[]` (OR + ancestor-inclusive in one operator); empty/NULL → no filter. (F3) **Combined semantics** — `(cat1 OR cat2 …) AND (tag1 OR tag2 …)`, same-type OR, cross-type AND, two WHERE predicates (tags via correlated `EXISTS … = ANY($6)`). (F4) **OWNER OVERRIDE — highlight-in-place, NO reorder** — the architect's "selected chips sort to the front" is CANCELLED; chips stay in natural/source order, selected = HIGHLIGHT ONLY in place reusing the EXISTING active-chip theme style (no new inverted token) + leading ✓; `orderSelectedFirst` dropped from the pure core and chip rows. (F5) **CORRECTED — desktop RAILS are the filter UI; mobile rows; applied bar on both** — NOT a "FeedFilterBar". Desktop `≥lg`: `CategorySidebar`/`CategoryTree` (left) + `TagCloud` (right) take a `mode:'filter'` prop (default `'nav'` preserves reuse) so items toggle the filter + highlight in place. Mobile `<lg`: `FilterChipRow` category + tag rows at the top of the feed. An `AppliedFilters` summary bar (removable chips both facets + Clear-all) sits at the top of the feed on BOTH breakpoints. (F6) **CORRECTED — filter chips are LINKS, not buttons** — zero-JS `<Link href={buildToggleHref(...)}>` reading `useSearchParams()` (`FilterChip.client`), active via `aria-current` (NOT `aria-pressed`; not `<button>`); also solves the App-Router "layouts don't get searchParams" problem for the rail. nav chips stay `<Link>`. (F.11) **Cache (Option X, NEW)** — `Cache-Control` on the EXPRESS RESPONSE (old ineffective request-header removed): anon → `public, s-maxage=3600, stale-while-revalidate=86400`; authenticated (owner sees drafts via `OR p.user_id=$4` from `req.auth`) → `private, no-store`; invalidation = TTL/SWR only, `revalidateTag` N/A (feed is client `axios`→Express, not Next `fetch`); future instant-invalidation via Next `fetch`+tags + `/api/revalidate` webhook = SUPERSEDED by Decision #35 (Phase 2-fetch). Architect-verified; backend MUST use array OVERLAP (categories) + correlated `EXISTS` (tags), NOT INNER JOIN, to preserve `ROW_NUMBER() PARTITION BY slug` + the tags/category_path aggregates; NO migration / NO new table (`category_path.path` already `text[]`); respects soft-delete (`pt.deleted_at IS NULL`). Not escalated to the user (repo convention; the F4 override is the owner's explicit call).
> - **#35 — Phase 2-fetch feed data layer: axios→native fetch + tag revalidation (PM+architect LOCKED + SHIPPED 2026-06-29, commits `688bf8e`..`731250e`; gate GREEN — client check-types+lint, `dashboard_server` jest 166/166; builds on Phase 2-filter; DELIVERS the "future instant-invalidation upgrade" Decision #34 F.11 deferred):** migrate the blog FEED read off client `axios` (which bypasses Next's Data Cache) onto a server-only native `fetch()` thin wrapper so the feed can use Next 16 tag-based revalidation. (D1) **native fetch + thin SERVER wrapper** — NEW `apis/http.server.ts` (`import 'server-only'`) `serverRequest<T>({path,method,body,token,cache,tags,revalidate,signal,fallbackError})`, replaces axios for SERVER reads only; per-call auth header, JSON+FormData aware, `AbortSignal.timeout(5s)`, `!res.ok`→`ApiError` via new `toFetchApiError` (error shape preserved); chosen over ofetch/ky (zero dep) and over keeping axios (axios can't tag the Next Data Cache). (D2) **auth-aware cache split** — `apis/getPosts.fetch.server.ts` `getFeed`: anonymous → `cache:'force-cache'` + `next:{tags:['feed:global','feed:<username>'], revalidate:60}`; owner/token (drafts via `OR p.user_id=$4`) → `cache:'no-store'` (never cached); token via Clerk `auth().getToken()` resolved OUTSIDE any cached scope; NEVER pass `next.revalidate` with `no-store`. (D3) **publish-class writes revalidate** via Next Server Actions (`app/actions/postPublish.action.ts`) calling Next 16 `updateTag('feed:global')`+`updateTag('feed:<username>')` (Server-Actions-only, immediate read-your-writes); rewired `useDeletePost`→`deletePostAction`, `useSetAdminPostStatus`→`setAdminPostStatusAction`, editor publish/unpublish→`setPostStatusAction`; KEPT on axios (no revalidation needed): draft autosave/create/content-edit (`useUpdatePost`/`useCreatePost`/`useDraftAutosave`), `loadMorePosts` + load-more Route Handler, sitemap. (D4) **Express CDN backstop lowered** — anon feed `s-maxage` 3600→60 (keep `stale-while-revalidate=86400`; authed `private,no-store` unchanged) so the CDN can't out-stale Next's tag invalidation. (D5) **NOT done (deferred, YAGNI)** — Cache Components (`cacheComponents:true`) NOT enabled (legacy tagged-fetch model used); client axios retirement + `uploadImage` FormData migration = deferred Step 6 (= F-fetch.2). OPEN follow-ups: (F-fetch.1, Low) admin delete (`useDeleteAdminPost`→`deleteAdminPost.api`) not yet rewired → no instant revalidate (covered by 60s TTL), needs a `deleteAdminPostAction` mirroring `deletePostAction`; editor-delete edge: empty `username` yields a harmless `feed:` tag (`feed:global` always fires). NEW `apis/buildFeedQuery.ts` (pure) / `apis/http.server.ts` / `apis/getPosts.fetch.server.ts` / `app/actions/postPublish.action.ts`; SOFT-DELETED (`x_`) `apis/deletePost.api.ts` + `apis/setAdminPostStatus.api.ts`. Not escalated to the user (repo convention: PM+architect decide option/choice points).
> - **#36 — Phase J SEC 13F holdings monthly batch (PM+architect LOCKED 2026-06-30; system-architect design → 2nd adversarial validation re-verified live against SEC endpoints):** add SEC EDGAR 13F-HR institutional-holdings collection to `apps/stock_collector` (Yahoo-only today). (J-a) **Source path** — `GET data.sec.gov/submissions/CIK##########.json` (CIK **0-padded to 10**) → `filings.recent` parallel arrays; filter `form ∈ {13F-HR,13F-HR/A}`, pick latest by `filingDate`; `reportDate` IS the period-of-report (quarter-end) → use directly as `period_of_report`/as_of (no cover-page fetch). Info-table filename is **non-predictable** (e.g. `43977.xml`); `primaryDocument` = cover page only → discover infotable via the accession **folder `index.json`** at `www.sec.gov/Archives/edgar/data/<cik_INT_non-padded>/<accession_nodashes>/` (Archives uses NON-padded CIK; submissions uses padded — verified 200 vs 301). **3 GETs/fund** (submissions + index + infotable). (J-b) **Cadence** — monthly cron `0 7 1 * *` (1st, 07:00 UTC) + `workflow_dispatch`; 13F due ≤45d after quarter-end; monthly is idempotent + self-heals missed months + catches amendments. (J-c) **Registry** — `configs/funds.yaml` `{cik,label}` (~5–10 famous filers, Berkshire `0001067983`…), loaded via table-guarded `resolve_funds()` seam; `tracked_funds` DB table DEFERRED (YAGNI — nothing auto-discovers funds). (J-d) **Schema (migration 0017)** — TWO tables: `sec_filings` PK`(cik,accession)` (filer, form_type, period_of_report, filing_date, **value_units**, deleted_at) + `sec_holdings` PK **`(cik,accession,cusip,put_call,prn_type)`** (period_of_report denormalized, name_of_issuer, title_of_class, ticker NULL, shares, prn_type∈{SH,PRN}, **value_usd BIGINT**, put_call, discretion, deleted_at). Both carry `deleted_at` (NOT a batch/error_log no-tombstone exception). (J-e **CORRECTED PK** — must-fix) real Berkshire Q2-2025 infotable collides on ~20% of rows under `(cik,accession,cusip,put_call)`: multi-manager funds report the same security once **per `otherManager`** → **aggregate (sum shares+value) in the PURE transform across otherManager**, keep SH/PRN + put/call/none DISTINCT; `put_call` **NOT NULL sentinel** (`''`) because Postgres treats NULL as distinct → NULL breaks `ON CONFLICT`. (J-f **CORRECTED units** — must-fix, 1000× trap) 13F `value` units flipped at EDGAR 2023-01-03 (thousands→whole USD); key the decision on **`period_of_report >= 2022-12-31`** (NOT filing_date — a 2024 amendment of a 2022-Q3 period is still thousands); normalize to whole USD in `value_usd`, stamp raw unit in `sec_filings.value_units`. This is an explicit documented normalization (column says `_usd`), NOT a source-coercion (CIK/CUSIP stay source-native per Decision source-representation-first). (J-g **CORRECTED supersede** — must-fix) amendment = new accession → new rows; after upsert, soft-delete prior same-`(cik,period_of_report)` filings **only where `filing_date` strictly earlier** than the kept filing (never downgrade a newer stored filing; out-of-order/missed-month guard), `WHERE deleted_at IS NULL` for clean idempotent re-runs. (J-h **CORRECTED parser** — must-fix) infotable XML appears unprefixed AND namespaced (`ns1:`/`n1:`) → parser MUST be namespace-agnostic or it silently parses 0 rows. (J-i) **Rate/UA** — keep 300rpm (5 req/s, half the 10 req/s SEC ceiling); **required** `User-Agent: "Name email"` header (missing UA = #1 403 cause) via `SEC_USER_AGENT` secret. (J-j) **Module layout** — `config/funds_loader.py` (pure) + `sink/sec_fetch.py` (IO) + `transform/sec_parse.py` (PURE: parse_submissions/parse_infotable/normalize_value/aggregate_holdings) + `schema/rows.py` (FilingRow/HoldingRow) + `db_sink.py` (upsert_filings/upsert_holdings/supersede_prior_filings) + `entrypoints/run_monthly.py` (mirrors run_weekly: argparse, dry-run guard, try/finally, `insert_batch_log(job='collector-monthly')`, partial-failure tolerant); each ≤300 LOC, functional-core pure + IO isolated. (J-k) **Archive** — raw infotable.xml → Supabase Storage best-effort, default OFF (reuse `storage_sink`). (J-l) **Scope** — store+batch ONLY; NO dashboard endpoint/UI, `ticker` NULL (CUSIP→ticker needs licensed mapping = next phase). value_usd BIGINT confirmed (max 9.2e18 ≫ mega-fund ~1e11). USER gates: 0017 apply, `SEC_USER_AGENT`+DB secrets, first live run. Not escalated to the user (repo convention: PM+architect decide option/choice points).
> - **#37 — Phase K /funds 13F viewer (PM+architect LOCKED + ENG DONE 2026-06-30; system-architect design → developer ×2 parallel Express/Next → self-review):** read-only viewer of the Phase J store on a SEPARATE `/funds` page (owner's explicit call — NOT inside `/dashboard`; different domain/shape/cadence). (K-a) **Express** `controllers/funds/funds.ts` — `GET /api/funds` (store-derived fund list: only ciks WITH non-deleted filings, latest period + holdings_count) + `GET /api/funds/:cik` (holdings, default-latest filing, optional `?period=YYYY-MM-DD` whitelist `/^\d{4}-\d{2}-\d{2}$/`, cik digits-only → padStart(10), parameterized `$1/$2` never interpolated). (K-b **weight in SQL**) % of portfolio via `COALESCE(ROUND(value_usd*100/NULLIF(SUM(value_usd) OVER (),0),4),0)` — one pass, mapper stays a pure row→DTO transform (SRP); COALESCE→0 so the wire value is always a number (matches the non-nullable Next Zod `weightPct`). (K-c **table-absence guard, NEW Express pattern**) `to_regclass('public.sec_filings') IS NOT NULL AND ...sec_holdings...` probe at the top of each handler → returns empty (`[]`/`null`) NOT 500 when migration 0017 is unapplied; one probe only (real SQL errors still 500). This is the first `to_regclass` use on the Express side — the analog of the collector's Python table-guard; flagged as a new-but-consistent pattern. (K-d **cache**) Cache-Control s-maxage=86400 (24h) / SWR=604800 (7d) — 13F mutates ≤monthly; Next server actions `next:{revalidate:86400}` kept in phase. (K-e **Next**) `app/funds/page.tsx` + `app/funds/[cik]/page.tsx` (force-dynamic server components; **selection = route param `/funds/[cik]`, NOT a jotai atom** — static table, no client interactivity, shareable/bookmarkable, no client store/TanStack hook needed); `getFunds`/`getFundHoldings` server actions mirror dashboard (typed Result, NO mock fallback, reportSsrError on failure, `data:null` unknown-fund pass-through); Zod schemas + AssertEqual guards; `components/funds/{FundList,HoldingsTable,FundsEmptyState}`; `utils/formatUsd.ts` pure compact-USD ($1.23B) + vitest. (K-f **no-data UX**) prod shows an EMPTY state (honest, consistent with the removed dashboard mock fallback) + a fixture-backed `app/funds/preview/page.tsx` (`apis/getFunds.mock.api.ts`, Berkshire+BlackRock ~8 holdings each) that renders the REAL components so the owner previews the populated layout NOW, before data lands. (K-g **scope**) read-only; no write; `ticker` shown '—' (CUSIP→ticker = future phase); no /dashboard cross-link; no search/period-picker UI. USER gate: live data needs Phase J 0017 applied + first collector run (K1–K10 all ship against empty/preview). Gates GREEN: server check-types 0 + jest 26/171, web check-types 0 / lint 0 / vitest 9 files/47. Not escalated to the user (repo convention: PM+architect decide option/choice points).
> - **#38 — Phase L `/funds/[cik]` rank-flow 분기별 holdings 시각화 (PM+architect 설계 → 오너 4-결정 잠금 2026-06-30):** Phase J/K store 위 `/funds/[cik]` rank-flow 테이블(컬럼=분기, 행=비중랭킹, 종목별 cusip-해시 고정색, 클릭→전분기 inner-ring 시계열 추적). **오너 결정(escalated):** (1) rank-flow=`/funds/[cik]` 유일뷰 → Phase K 평면 단일분기 테이블 은퇴(`x_`), period 셀렉터 폐기(분기=컬럼 축); (2) 최근 8분기/2년 표시 + **백필도 2년만**(`run_backfill --since <2yr>`, 무료 500MB DB 보호, 추정 ~30–90MB); (3) 컬럼 행 = **8분기 중 ever-top-K(25) 진입 종목의 합집합**(union 멤버는 전 컬럼 행 유지=bump 연속성, 미보유=ghost, 나머지=컬럼별 "+M more (Σweight%)"); (4) Δ배지(▲▼/NEW/EXIT/Δweight)·비선택 디밍·연결선 = v1 미포함 → L-next. **v1 데이터** = 신규 순수 `all_13f/group_by_period/merge_submission_pages`(sec_parse) + 신규 fetch `submission_page_names/fetch_submission_page`(sec_fetch, `filings.files` 페이지네이션 안전망) + 신규 `run_backfill.py`(supersede는 `(cik,period)`+filing_date guard라 다분기 안전, workflow_dispatch 일회성). **v1 서빙** = 신규 `GET /api/funds/:cik/rankflow?quarters=8&k=25`(union rows + per-period cells/remaining, funds.ts 컨벤션 미러) — 분석 뷰 아님. **L-next(DEFERRED)** = 마이그레이션 0018 분석 뷰 4종 + 0017/0018 SSOT 미러(현재 0017 SSOT 부재 보강) + Δ배지 + cross-fund consensus(`/api/securities/:cusip/consensus`). 상세 = Phase L 블록 + `00.tasks.md` Phase L. escalated 4결정 외 옵션은 repo 컨벤션(PM+architect 결정).
> - **#39 — Phase M market_history 5년 retention 티어링 (system-architect 설계 2026-06-30; PM+architect 잠금):** `market_history`를 롤링 5년 HOT 서빙 티어로 한정; 전체 히스토리는 이미 LIVE인 Parquet COLD 아카이브(Storage)에만. (M-a) **Prune 컴포넌트** — 순수 `transform/retention.py`(`prune_cutoff(today,years)` 결정적·hidden clock 없음 + `partition_years_present`) + `db_sink.read_prune_candidates`/`delete_history_before`(라벨별 commit; psycopg는 sink만) + `storage_sink.object_exists`(아카이브 존재 프로브; 에러→False) + 신규 `entrypoints/prune_history.py`(≤300 LOC). (M-b) **안전 가드** — "upload step 실행"이 아니라 **라벨별·연도별 Parquet 객체 존재를 독립 검증한 경우에만** DELETE; `parquet_archive_enabled()` AND `supabase_storage()` 둘 다 없으면 **완전 no-op**; cutoff는 boundary에서 `today` 1회 읽어 순수코어로 주입; `--dry-run` 우선. 과거-연도 parquet는 immutable이라 "파일 존재 == 완전성"(행 단위 diff 안 함 — YAGNI). (M-c) **소프트삭제 예외(SANCTIONED)** — repo 규칙은 "절대 하드삭제 금지, `deleted_at` tombstone + read 시 mask". 본 prune은 명시적 예외다: **티어 MOVE이지 콘텐츠 삭제가 아님** — 봉은 파괴되지 않고 검증된 불변 Parquet 콜드 티어로 이동(`.claude/rules/docs.md` §7 "archiving = move not delete"의 시계열 판본; Parquet가 시계열의 `_docs/archive/`). 소프트삭제 규칙의 본래 우려(유일본 손실)는 검증된 2번째 불변 카피 존재로 충족. **tombstone은 거부** — prune의 목적인 바이트(~396B/행)를 그대로 보유하고 hot read 경로에 `WHERE deleted_at IS NULL` 부담을 추가해 size 이득을 손실로 뒤집기 때문. `market_history`는 재생성 가능한 기계 데이터(Yahoo `fetch_full` 재취득 가능)이지 유저 콘텐츠 아님(FK 자식·감사 의무 없음). **"move not delete"를 참으로 만드는 가드 = (1) 아카이브 비활성/Storage 도달불가 시 no-op, (2) 라벨별 전 연도 파티션 Storage 존재 확인 후에만 삭제, (3) 과거연도 parquet immutability.** (M-d) **신규 티커 흐름** — 증분 커서 `MAX(bar_time)+1d` → prune된 과거 봉 **재유입 불가**(MAX 미변동, prune은 MAX 미만만 제거); 신규 tracked는 seed_archive(전체→Parquet) + 5년 hot 백필 1회. (M-e **must-fix R3**) `seed_archive`가 `fetch_daily`라 inception 미도달 → prune이 Parquet를 유일 콜드 카피로 신뢰하기 전 `fetch_full`로 심화 필수(M7). (M-f) **옵션** — A(파일 존재 검증, **채택**) / B(Postgres↔Parquet 행수 reconciliation, 업그레이드 경로) / C(Postgres native range partitioning by year + `DROP PARTITION`, 대규모시 미래 방향). Bounded: 150티커×5년×~396B ≈ ~75MB(연 1회 cadence라 5~6년 톱니, 피크 ~90MB). (M-g **카덴스 — 오너 결정 2026-06-30**) prune = **연 1회 12/30 cron**(`0 7 30 12 *`) + `workflow_dispatch` + `if:failure()` 이슈. 주간 대신 연 1회 선택 = 파괴적 DELETE 횟수 최소(위험 표면↓) + idempotent라 놓쳐도 수동 복구; trade-off는 DB가 정확히 5년이 아니라 5~6년. cutoff를 **달력연도 floor**(`<(year−5)-01-01`)로 스냅 → prune이 항상 완전한 immutable 과거 연도 파티션만 떨궈 "파일 존재=완전성" 검증이 깔끔(boundary 연도 부분삭제 엣지 제거). **OUT OF SCOPE 후속 flag:** `market_rankings`(append-only)/`sec_holdings`(tombstone 누적)/`batch_log` 2차 retention(Phase L의 13F 윈도우와 같은 패턴이라 본 prune 머신을 재사용 가능). escalated 옵션 외는 repo 컨벤션(PM+architect 결정).
> - **#40 — Phase N 2차 retention: 로그 TTL + rankings 10년 아카이브 prune (ML+architect 자문 → PM+architect 잠금 2026-06-30; #39 OUT-OF-SCOPE 후속 실현):** Phase M prune 머신을 남은 3테이블에 성격별 2갈래로 재사용. (N-a **경로 결정**) pg_cron 대신 **Python 엔트리포인트 + GitHub Actions**로 통일 — 활성 pg_cron 0개(전 스케줄이 Actions), 오너 룰이 "Phase M 머신 재사용"(=retention.py+db_sink 삭제+entrypoints+batch_log+workflow), 각 prune 실행이 스스로 `batch_log` 행을 남겨 self-observe. 폐기된 `error_log_cleanup` pg_cron 주석은 문서화 fallback으로 그대로 둠(비활성). (N-b **로그=무-아카이브 TTL**) error_log·batch_log는 오너 승인 no-tombstone 예외(0014/0015) → 아카이브 없이 순수 일수 하드삭제. 순수 `age_cutoff(now,days)`(retention.py, now는 boundary 1회) + `db_sink.delete_error_log_before`/`delete_batch_log_before`(caller commit, rowcount) + count 프리뷰 헬퍼. **`batch_log`는 `keep_open_failures=True`가 기본** → `resolved=0`(미해결 실패) 행은 나이 무관 보존(triage 전 소멸 방지). 공유 엔트리포인트 `prune_logs.py`(`--target error_log|batch_log|all`, DRY: 두 테이블이 delete 함수 빼고 모든 속성 동일). error **7일**/batch **90일**. (N-c **rankings=도메인, 아카이브-검증-삭제**) market_rankings는 로그 예외 아님·재현불가(shares 미저장 → price×shares 재구성 불가) → OHLC와 동일 verify-후-delete. window=**10년**(ML 자문: 재현불가 원본 신호 + 팩터 lookback 2사이클; 5년은 재현가능 OHLC 기준이라 부적용). `prune_cutoff` 재사용 + `read_rankings_prune_years`/`delete_rankings_before` + `prune_rankings.py`(`--years 10 --verify-archive`, per-year `object_exists` all-or-skip, 절대 부분삭제 안 함). (N-d **아카이브 라이터**) 신규 순수 `rankings_parquet_sink.py`(`rankings/{YYYY}.parquet`, PK(as_of,symbol,scope) dedupe keep-last, sort as_of/scope/rank, read-merge-rewrite) + `db_sink.read_rankings_years`/`read_rankings_by_year` + 신규 `archive_rankings.py`(DB→per-year Parquet→best-effort Storage upload, always exit 0) → `collector-weekly.yml`에 주간 best-effort 스텝. (N-e **cadence/모드**) logs=신규 `collector-prune-logs.yml` **매일 03:00 LIVE**(무-아카이브·저-blast, dispatch만 dry-run); rankings=`collector-prune.yml` 3번째 잡 **연 12/30 dry-run**(첫 LIVE=수동 dispatch). (N-f **현 상태**) 주간 rankings 시리즈 최근 시작 → 10년 cutoff 기준 **삭제 후보 ~2036까지 0건**(전향적 안전장치; dry-run 실증 확인). (N-g **DDL 없음**) 0018 불필요 — error_log.created_at/batch_log.created_at+resolved/market_rankings.as_of 및 기존 인덱스로 모두 충족(앱-사이드 DELETE). 검증: pytest 208 green(신규 age_cutoff 4 + rankings sink 8), 3 엔트리포인트 live dry-run 통과. escalated 외 옵션은 repo 컨벤션(PM+architect 결정).
