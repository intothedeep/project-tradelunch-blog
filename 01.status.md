# Status — tradelunch / dashboard_client_web

**Created:** 2026-06-24
**Owner:** engineer agent (authoritative source of truth for done / in-progress / blocked)
**Active phase:** Phase 2-simple — Financial Dashboard MVP
**Branch:** feature/finance

> This file was missing until 2026-06-24 (a Phase 0 gap). It is now the single source of
> truth for execution state. `00.plan.md` owns product intent; `00.tasks.md` owns the atomic
> task list; this file owns *what is actually true in the tree right now*.

---

## Headline

**Variant C (TradingView-style chart dashboard) is adopted as the product direction** (user
decision, 2026-06-24). Cycle 2 (variant evaluation) is closed. The implementation has run well
ahead of the written task list — several "non-goal" features are already built and are now
in-scope. The next work front is **Cycle 3: wire the adopted dashboard to the real backend and
fold it into the canonical `/dashboard` route.**

---

## Done today (2026-06-24)

### Secrets cleanup
- Removed `SESSION_SECRET` from `apps/dashboard_server/src/config/env.schema.ts`. This was an unused required var — not needed with Supabase + Vercel deployment. Removing it unblocks Phase A typecheck.
- Removed `AWS_RDS_CA` from `apps/dashboard_server/src/config/env.schema.ts`. Dead var; only referenced in a comment. EC2/SSH-tunnel + Sequelize-CA teardown remains part of Phase A Group A2 (SSH tunnel is incompatible with serverless; Supabase uses direct TLS).

### Shared-package refactor ("Option A + B")
- Created `packages/@repo/types`: no build step; raw TS; `main`/`types` → `src/index.ts`. Holds API-boundary types: `ETreeNodeType`, `TCategoryTreeNode`, `TPostTreeNode`, `TCategoryTreeResponse`, `TTreeNode`, `TTreeNodeWithChildren`. These moved out of `@repo/markdown-parsing`.
- `@repo/markdown-parsing` now re-exports the moved types for back-compat. Import sites in both apps repointed to `@repo/types`. Added `transpilePackages: ['@repo/types']` to `apps/dashboard_client_web/next.config` (package ships a runtime enum).
- Fixed 4 server publish-scripts that imported from `@repo/markdown-parsing/src/types` raw path — now use the `/types` subpath.
- Removed dead `@repo/assets` dependency from client. Replaced `require.resolve("@repo/assets/...")` in `apps/dashboard_server/src/helpers/home.ts` with a direct `path.join`.

### User action required
- Run `pnpm install` from repo root — `@repo/types` is a new workspace package and `@repo/assets` was removed from the client. The lockfile must be regenerated before `check-types` will pass.
- (Optional) hard-delete the `x_*` quarantined files/dirs once you have confirmed the new dashboard.

---

## Done (verified in tree, 2026-06-24)

### Phase 1 — Blog MVP
- Shipped. See `00.tasks.md` Phase 1 (all boxes checked).

### Phase 2-simple — Cycle 2 prototypes
- `app/dashboard/preview/cards/page.tsx` — Variant A (reference-only)
- `app/dashboard/preview/table/page.tsx` — Variant B (reference-only)
- `app/dashboard/preview/chart/page.tsx` — Variant C (**adopted**)
- `types/dashboard.ts`, `types/history.ts` — snapshot + OHLC types
- `apis/getDashboardSnapshot.mock.api.ts`, `apis/getDashboardHistory.mock.api.ts` (250 daily candles, deterministic, SSR-stable)

### Variant C chart — built beyond the v3 spec
- `components/dashboard/ChartPanel.client.tsx` (218 LOC — see Carry-over #1)
- `components/dashboard/ChartHeader.client.tsx` (gear button lives here)
- `components/dashboard/ChartTimescale.client.tsx`, `ChartLegend.client.tsx`, `ChartConfigMenu.client.tsx`
- `components/dashboard/ChartDrawToolbar.client.tsx` — drawing tools (trendline, h-line, ray, ext-ray, rectangle, Bézier) + magnet snap + undo
- `components/dashboard/ChartVariantLayout.client.tsx` (70/30 split)
- `hooks/useTradingViewChart.hook.ts` (369 LOC — see Carry-over #2)
- `store/dashboard.atom.ts`, `store/drawings.atom.ts` (per `label::interval` namespace)
- Pure indicator core in `utils/`: `computeMA`, `computeEMA`, `computeRSI`, `computeMACD`,
  `computeIchimoku`, `computeMacdSignals`, `computeRsiSignals`, `generateIntervalCandles`,
  `chart-format`
- Real intraday interval switching: 1m / 5m / 15m / 30m / 1h / 4h / D / W / M
- `lib/chart-theme.ts` dual palette (TV_DARK / TV_LIGHT)

### Phase 4 — pnpm migration
- Effectively complete in practice: `pnpm-lock.yaml` present, `yarn.lock` removed
  (commit `150d460 chore: remove yarn.lock, finish pnpm migration`). The Phase 4 checkboxes
  in `00.tasks.md` are stale `[ ]` — reconcile or mark the phase closed.

### Infra fix (2026-06-24)
- Removed the stray `app.listen(4000)` from `apps/dashboard_server/src/server.ts`. The single
  listener (with SIGTERM/SIGINT graceful shutdown) now lives only in `src/index.ts`.

---

## In progress / Next (Cycle 3 — active front)

Prerequisite (variant selection) is now MET. Open work:

1. **Backend endpoints** — `dashboard_server` must expose `GET /api/dashboard/snapshot` and
   `GET /api/dashboard/history?label=…` matching the type contract. Not started.
2. **Server Actions + Zod** — `app/actions/getDashboardSnapshot.{schema,action}.ts` and the
   (now unconditional) `getDashboardHistory.action.ts`, flag-gated by `DASHBOARD_DATA_SOURCE=mock|backend`.
3. **Polling hook** — `hooks/useDashboardSnapshot.query.client.ts` with per-category intervals.
4. **Page conversion** — DONE (2026-06-24). `/dashboard/page.tsx` and `/dashboard/[username]/page.tsx`
   now render `ChartVariantLayout` with Cycle 2 mock data. **Dashboard nav link added** to the shared
   `LINKS` array in `components/navigation-desktop.tsx` (covers both desktop + mobile drawer). Data swap
   to backend happens in #2 above.
5. **Soft-delete old grid** — DONE (2026-06-24). `x_`-prefixed via `git mv`:
   `x_FinancialGridLayout.client.tsx`, `components/dashboard/x_widgets/` (all 8 widgets + GridCard + index),
   `apis/x_getFinancialData.mock.api.ts`, `hooks/x_useFinancialData.query.client.ts`,
   `hooks/x_useElementSize.hook.ts`. tsconfig already excludes `**/x_*`, so they don't typecheck.
   USER may delete the `x_*` files manually. **`react-grid-layout` REMOVED** from `package.json`
   dependencies (only the soft-deleted grid used it) — USER must run `pnpm install` to update the
   lockfile. **`recharts` STAYS** — `components/ui/chart.tsx` (shadcn) still imports it.

---

## Decisions recorded (2026-06-24 — second session)

### M2 — Supabase blog schema migration plan
- Architect derived full blog schema from server SQL queries. Draft migration files committed:
  `apps/dashboard_server/supabase/migrations/0001_blog_schema.sql` and `0002_rls_policies.sql`.
- Full step-by-step plan (C1–C9) recorded in `00.plan.md` M2 section and `00.tasks.md` Group A3.
- C1, C3, C5, C6, C7 require user action (AWS + Supabase access). C2, C4, C8, C9 are engineer tasks.
- C9 (`DATABASE_URL: z.string()` in `env.schema.ts`) is the prerequisite for M3 pg Pool wiring.

### Env var naming — kcdd2 convention
- Env variable naming follows the kcdd2 reference repo convention: SCREAMING_SNAKE_CASE, no prefix namespacing.
- Key vars: `DATABASE_URL` (Supabase pooler 6543), `NODE_ENV`, `SITE_DOMAIN`, `API_SITE_DOMAIN`, `CDN_ASSET_POSTS`.

### CDN_ASSET_POSTS → Supabase Storage (CDN Option A)
- `CDN_ASSET_POSTS` will point at Supabase Storage public URL for blog images (Phase B).
- Later optionally fronted by a Cloudflare CNAME (custom domain CNAME'd to Supabase Storage URL).
- Env var name and contract are unchanged regardless of whether Cloudflare CNAME is active.
- `apps/dashboard_server/.env.example` comment for `CDN_ASSET_POSTS` should read:
  `# Supabase Storage public URL for blog images (later: Cloudflare CNAME to Supabase Storage)`

### Vercel Hobby + GitHub Organization repos require Pro
- Vercel Hobby supports private repos under an individual GitHub account only.
- Private repos owned by a GitHub Organization require Vercel Pro.
- Action: transfer repo from `tradelunch` GitHub org to individual GitHub account; run
  `git remote set-url origin <new-individual-url>` after transfer.
- Recorded in `00.plan.md` Free-Tier Reality Check table and Open Questions #11.
- A user action task for this transfer is added in `00.tasks.md` Group A4.

---

## Blocked / Open questions

- **NCSOFT KRX ticker suffix** (`036570.KS`?) — must be confirmed before the backend history
  fetcher is implemented. Backend's responsibility, not the Next.js app's.
- **Backend readiness** — Cycle 3 real-data tasks stay pending until `dashboard_server` has the
  DB schema + scheduled ingestion job. Until then the actions return Cycle 2 mocks.
- **`article_server` workspace** — referenced in plan but no `apps/article_server/` exists;
  confirm moved/renamed/separate repo.
- **M2-C1 reconciliation** — draft migration DDL has [INFERRED] columns; AWS RDS schema dump
  (C1) is required before C4 reconciliation can proceed.

---

## Architecture / tech-debt decisions (resolved 2026-06-24)

### `@repo/types` package (RESOLVED)
Previously a "phantom" concern: API-boundary types lived inside `@repo/markdown-parsing`, coupling the LOGIC package to TYPE consumers that had nothing to do with markdown parsing. Now resolved via the shared-package refactor above. The `@repo/types` package is the single source of truth for tree/category API-boundary types. `@repo/markdown-parsing` stays for shared parsing logic only.

### S3 → Supabase Storage (DECIDED)
AWS S3 is confirmed as the current blog image store. Decision: migrate to Supabase Storage in Phase B (permanent free tier: 1 GB). No action needed before Phase B. The following are explicitly retired as part of Phase B (not now):
- `apps/dashboard_server/src/lib/awsS3.ts`
- `AWS_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_S3_BUCKET` env vars
- `aws-sdk` / `@aws-sdk/*` package from `dashboard_server` dependencies

Open question #8 in `00.plan.md` is now marked RESOLVED.

---

## Carry-over / tech debt (non-blocking)

1. **`ChartPanel.client.tsx` = 218 LOC** — over the ≤180 v3 budget and approaching the repo
   ≤300 ceiling. Either re-slim (extract interval/drawing wiring) or formally raise the budget
   for this now-richer orchestrator.
2. **`useTradingViewChart.hook.ts` = 369 LOC** — over the ≤300 ceiling; single ~280-line
   `useEffect` with a 14-entry dep array rebuilds the whole chart on any indicator toggle.
   Candidate split: `useCrosshairHover` + per-indicator series effects (already noted as
   "deferred" in `00.tasks.md`).
3. **Server has no service layer** — `controllers/posts/posts.ts` is 591 LOC with raw SQL inline
   and cursor-pagination duplicated 3×. Violates the declared `controllers → helpers → lib`
   layering. Phase C hardening candidate.
4. **Dead deps / files** — `recharts` still in `package.json` (kept — shadcn chart.tsx uses it);
   `apps/dashboard_server/src/utils/__database.ts` is an orphaned duplicate of `database.ts`
   (wrong soft-delete prefix `__` instead of `x_`).
5. **`env.schema.ts` (client)** `console.log`s env values at module load — drop before prod (Phase A blocker).
6. **`@repo/axios` vs local `apis/axios_instance.ts`** — two parallel axios setups; pick one (Phase C).

---

## Acceptance gates not yet met (Cycle 3 Group E)

- All 22 items wired to **real** data (currently mock)
- `/dashboard` renders the chart variant (DONE — now renders ChartVariantLayout with mock data)
