# Status — tradelunch

**Owner:** engineer agent. One line per update, **newest at the bottom**. Append a single line per
change — no prose blocks. Product intent → `00.plan.md`; task breakdown → `00.tasks.md`.

---

- 2026-06-24 — Variant C (TradingView chart) adopted as dashboard direction; Cycle 2 closed.
- 2026-06-24 — Removed dead `SESSION_SECRET` + `AWS_RDS_CA` from server `env.schema.ts`.
- 2026-06-24 — `@repo/types` created; API-boundary types moved out of `@repo/markdown-parsing`; imports repointed.
- 2026-06-24 — Removed dead `@repo/assets` dep; `home.ts` `require.resolve`→`path.join`.
- 2026-06-24 — Removed stray `app.listen(4000)` from `server.ts`; single listener now in `index.ts`.
- 2026-06-24 — `/dashboard` + `/dashboard/[username]` render `ChartVariantLayout` (mock); Dashboard nav link added.
- 2026-06-24 — M2 plan recorded (Supabase schema C1–C9); draft migrations `0001`/`0002` committed.
- 2026-06-25 — M3 T1–T4 done: `DATABASE_URL` contract, `api/index.ts`, `vercel.json`, `home.ts /status`.
- 2026-06-25 — Sequelize→pg Pool swap code-complete (`database.ts` + `posts.ts` 6 queries + `index.ts`).
- 2026-06-25 — AWS RDS + S3 confirmed DELETED → S2 data migration + S3-image migration SUPERSEDED; fresh Supabase setup.
- 2026-06-25 — Build fixes: `@types/pg` added; `scripts/`+`**/x_*` excluded from server tsconfig; dead `__database.ts` removed.
- 2026-06-25 — `pnpm install` done (sequelize+pg-hstore removed); pg Pool swap now BUILD-VERIFIED; all `x_*` purged from repo.
- 2026-06-25 — Publish tooling (`scripts/publish_*`) flagged BROKEN (Sequelize+S3) → reworked as Python blog_agent (S8.3/BA-09).
- 2026-06-25 — S4.1 CORS env-driven `ALLOWED_ORIGINS` (commit d9b06e0).
- 2026-06-25 — `@repo/markdown-parsing` retired from client; TOC inlined to `utils/markdown/`; blog page `force-dynamic` (commit 4ee90a0).
- 2026-06-25 — S1.1/S1.4/S1.5 done; S5.1 deploy blockers cleared (`.nvmrc=22`, `article_server` removed, both check-types 0); S7.1 CI created (commit 99d48c2).
- 2026-06-25 — S7.2 logged: ESLint needs flat-config migration (Next 16 dropped `next lint`); lint omitted from CI, deferrable.
- 2026-06-25 — blog_agent merged as `apps/blog_agent` (lift-and-shift, BA-00..BA-08; commit f51cc7e); BA-09 Supabase repoint pending (Phase B).
- 2026-06-25 — Docs consolidated: 7 files → `00.plan.md` + `00.tasks.md` + `01.status.md`; verbose originals archived to `_docs/` (gitignored).
- 2026-06-25 — S7.2 ESLint infra fixed (flat config loads via `@repo/eslint-config/next-js`; lint→`eslint .`); 4745 warnings remain (4571 prettier-fixable + ~174 real) so `--max-warnings 0` gate still deferred — not committed yet.
- 2026-06-25 — USER: Supabase project + both Vercel projects created (S1.2/S4.2/S5.2); env vars (S4.3/S5.3) not yet set so deploys not live; user pushing DB manually (S1.6).
- 2026-06-25 — Phase D (Clerk + multi-user) designed (architect + schema agents): Express-layer Clerk verifyToken keeps pg Pool (RLS bypassed by design → app-layer owner-scoping is the gate); native Supabase TPA replaces deprecated JWT-template; schema additive (users += clerk_user_id/display_name/avatar_url/is_admin + invites, migration 0003); flagged status='public' leak in /posts/users/:username. Plan/tasks updated.
- 2026-06-25 — Deploy hardening (uncommitted): Vercel env templates committed (`apps/dashboard_server/.env.vercel.example` + `apps/dashboard_client_web/.env.vercel.example`); pg Pool `max:1`→`max:5` in `database.ts` (Fluid Compute runs Express as one fn over concurrent requests; max:1 serialized DB access; pooler tolerates small pool); `status='public'` security filter added to `GET /posts/users/:username` (was leaking private/draft under multi-user).
- 2026-06-25 — Phase D D0 written (uncommitted): migration `0003_clerk_multiuser.sql` (additive — users += clerk_user_id/display_name/avatar_url/is_admin + invites + optional posts FK); both schema SSOT copies synced (`pnpm check:schema` green); NOT applied (deferred to db push). Web research validated Phase D auth: native TPA (JWKS) replaces deprecated (2025-04-01) JWT-template; TPA docs cover supabase-js+RLS only → confirms Express-layer enforcement over pg Pool; Clerk supports Next 16 (16.2.5 ≥ 16.2.3); free tier 50k MAU.
- 2026-06-26 — Backend LIVE on Vercel (`https://project-tradelunch-blog-dashboard-s.vercel.app`): S4.3/S4.4 done — `/ping`→200 pong, `/v1/api/posts`→200 `{success,data{posts:[],nextCursor:null,hasMore:false}}` (empty=no data yet); S3 runtime verified (pg Pool→Supabase, query executes, correct JSON shape).
- 2026-06-26 — Deploy fix chain: Option B durable architecture (`@repo/types`→JS not raw-TS enum; server `@/` aliases→relative; modern zero-config `vercel.json` rewrites→/api, dropped legacy builds/routes; `@types/express-serve-static-core` re-pinned via pnpm.overrides; `engines.node`) + DB SSL fix (stripped `sslmode` from Supabase conn string so `ssl.rejectUnauthorized:false` governs — was 500 self-signed cert). Next: S5 frontend env (`NEXT_PUBLIC_API_BASE`=backend URL) + deploy.
- 2026-06-26 — Frontend LIVE on Vercel (`https://project-tradelunch-blog-dashboard-c.vercel.app`): S5.3/S5.4 done — smoke PASS `/`→200, `/blog/@taeklim`→200 (empty data, no 500), `/dashboard`→200 (chart + watchlist BTC/EUR/NASDAQ/S&P500 mock, no error boundary). Both apps now live on Vercel + Supabase.
- 2026-06-26 — S6 DNS cutover IN PROGRESS: USER set Cloudflare DNS CNAME `my.prettylog.com`→`cname.vercel-dns.com` (DNS-only) at Vercel frontend; propagation + SSL issuance verifying (S6.2).
- 2026-06-26 — Dep hygiene: replaced `pnpm.overrides` for `@types/express-serve-static-core` with a direct exact devDep (5.1.1) in dashboard_server — lockfile single version, frozen-lockfile reproduces, no override (commit f026fca; S4-H.3).
- 2026-06-26 — Phase A FULLY COMPLETE: `my.prettylog.com` LIVE on all routes (`/`, `/blog/@taeklim`, `/dashboard` all 200; earlier `/` redirect was transient propagation); S6.2 DNS → done; Phase A acceptance gate MET.
- 2026-06-26 — Phase B backend DONE (commit 84b9d32): B0 removed 13 dead AWS/EC2/DB_PG/CLOUDFRONT env vars; B1 migration `0004_market_data.sql` (market_snapshots + market_history) + both schema SSOT mirrored (check:schema green); B2 `GET /v1/api/dashboard/snapshot|history`; B3 `seed.market.sql` (22 snapshots + 250×22 history, mock-faithful). All gates green; NOT yet db-pushed to Supabase (`db push 0004` is USER). In progress: B4 client action layer + `DASHBOARD_DATA_SOURCE` flag; B5 blog_agent Supabase repoint.
- 2026-06-26 — Auth token-forwarding (Architecture A) review: existing wiring already correct — `getMe`/`postUsername` forward Clerk token via explicit `token` param + `useAuth().getToken()`; Express routes already classified (`requireAuth` on `/users/me*`, `optionalAuth` on post detail, public on listings). Decision: dashboard `/snapshot|/history` = PUBLIC market data (not user-scoped) → no token forwarding (YAGNI). Changes: documented dashboard public-by-design (`dashboard.ts`), removed dead global token interceptor + documented per-call convention (`axios_instance.ts`), noted public access on both dashboard Server Actions. Both apps check-types green. `.env.example` (client both keys / server secret-only) + dead `NEXT_PUBLIC_SECRET_SERVER_SIDE` removal also done this session.
- 2026-06-26 — D1/D2.7/D3.2/D3.3 reconciled to [x] (commits 01574aa + dc9cb75; open self-signup via ALLOW_OPEN_SIGNUP).
- 2026-06-26 — Provisioning decision: Svix webhook → lazy provisioning in resolveAuth (x_clerk soft-delete, /api/webhooks unmounted, express.raw removed); no new migration (users.username already UNIQUE+NULLABLE). Validated vs Clerk docs: just-in-time provisioning now officially endorsed over webhooks.
- 2026-06-26 — D-lazy.1-3 + D-onb.1-6/8 IMPLEMENTED + verified (server build 0, jest 9 pass vs live DB; client check-types 0, build ok, /onboarding route emitted); NOT committed; D-lazy.4 + D-onb.7(USER) open.
- 2026-06-26 — Security: authorizedParties wired into resolveAuth verifyToken from ALLOWED_ORIGINS (reused as single source; CORS parsing de-duped to ALLOWED_ORIGINS_LIST) — closes CSRF/subdomain-cookie-leak gap flagged by Clerk-docs validation; build 0, jest 9 pass.
- 2026-06-26 — server jest.config.mjs enabled (was fully commented out) to allow the suite to run — flag for review.
- 2026-06-26 — Runtime gate: lazy upsert requires migration 0003 columns (clerk_user_id/is_admin/display_name) live in Supabase — server DB probe this cycle confirmed users table reachable; verify 0003 columns present before relying on lazy in prod.
- 2026-06-26 — D2.0 API contract frozen in @repo/types (TPostStatus incl 'draft', TPostInput, TDraftSummary, TImageSignRequest/Response); 3 workspaces check-types 0.
- 2026-06-26 — D2 server IMPLEMENTED+verified (29 jest pass vs live DB; build 0): migration 0005 'draft' enum (+both schema SSOT, check:schema 0); posts.write.ts owner-scoped POST/PATCH(COALESCE)/DELETE(soft), user_id from req.auth only; GET /users/me/drafts; image-sign route (Option B). SECURITY D2.3a: GLOBAL FEED LEAK fixed — GET /posts had NO status filter, leaking all users' private/draft/follower to anon; added status='public'. D2.3b: GET /users/:username optionalAuth + owner sees own drafts.
- 2026-06-26 — D2 client IMPLEMENTED+verified (check-types 0, build ok, /write + /write/[postId] routes): MarkdownEditor + apis/hooks, draft autosave (debounce, first-save create→replace→PATCH), image upload (signed PUT direct to Supabase, 503-graceful).
- 2026-06-26 — D2 validation (Clerk/Supabase/OWASP docs): signed-upload flow CORRECT (upload/sign endpoint + client PUT + service key); IDOR 404 owner-scoping CORRECT. Found+fixed 2 MUST-FIX: (a) editor seed used public slug route → draft body clobbered to '' by autosave (DATA LOSS) → owner-scoped getPostById seed + autosave omits empty content; (b) PATCH required title → blocked untitled-draft autosave → validatePostInput relaxes title when status='draft' (publish still requires title).
- 2026-06-26 — D2 USER gates open: D2.1b `supabase db push 0005` (live draft writes fail until 'draft' enum pushed); D2.6a `blog.prettylog` bucket + SUPABASE_URL/SUPABASE_SECRET_KEY (image upload returns 503 until set; bucket must be public for display).
- 2026-06-26 — D4.0 admin contract frozen in @repo/types (TAdminPostListItem/TAdminPostListResponse/TAdminPostStatusInput); 3 workspaces check-types 0.
- 2026-06-26 — D4 server IMPLEMENTED+verified (33 jest pass vs live DB; build 0): adminPosts helpers (listAllPosts keyset / setPostStatus / adminSoftDeletePost — NON-owner-scoped by id) + validatePostStatus; admin router GET/PATCH:id/status/DELETE:id all requireAdmin at /v1/api/admin/posts. Admin delete is SOFT (deleted_at) not hard (parent_id ON DELETE CASCADE + orphan rows). Security merge-gate green: assertAdmin 403 non-admin; admin acts on another user's post by id; soft-delete confirmed (row survives, deleted_at set).
- 2026-06-26 — D4 client IMPLEMENTED+verified (check-types 0, build, /admin/posts route emitted): admin apis/hooks (useInfiniteQuery) + AdminPostsTable (Unpublish→private / Re-publish→public / Delete; useMe().isAdmin UX guard).
- 2026-06-26 — D4 validation (authorization audit): ALL PASS, no must-fix — isAdmin non-spoofable (DB-only via resolveAuth, no competing clerkMiddleware writer), non-owner helpers reachable only via requireAdmin, soft-delete consistent across admin+public reads. Ship.
- 2026-06-26 — D4.2 /admin/invites DEFERRED/DROPPED (open self-signup → no invite enforcement point; revive via webhook re-enable or Clerk custom JWT claim). D4.0b admin bootstrap = USER manual SQL `UPDATE users SET is_admin=true WHERE username=…` (lazy provisioning sets is_admin=false; no grant UI).
- 2026-06-26 — D-lazy.4 DONE: removed `svix` dep (lockfile grep 0) + dormant `CLERK_WEBHOOK_SECRET`/`ALLOW_OPEN_SIGNUP` from env.schema.ts + .env templates; x_clerk archive left build-excluded; build 0, 33 jest pass.
- 2026-06-26 — **Phase D ENG COMPLETE** (commits 8ca524e lazy+onboarding, dc28f19 D2 authoring, 70375e6 D4 admin, + D-lazy.4 cleanup). Final gate: server build 0 / 33 jest pass, client check-types 0, @repo/types build 0. Remaining = USER gates only (db push 0003/0004/0005, Clerk env, blog.prettylog bucket, admin bootstrap SQL, DASHBOARD_DATA_SOURCE flip) + D4.2 invites intentionally deferred.
- 2026-06-26 — USER db push DONE: applied 0004 (market) + 0005 (draft enum) to remote (0001-0003 already applied) → migration list Local=Remote for all 5. Root-cause fix: `supabase db push` failed "Invalid project ref format" because `.env.local` had placeholder `SUPABASE_PROJECT_ID=your-project-id` overriding the linked ref (rdtvemrbbsmiagbyvgzz); corrected the local env (gitignored). config.toml project_id stays 'dashboard_server' (local docker label, not the cause). 'draft' writes + market tables now live.
- 2026-06-26 — Gate 5a DONE: loaded `seed.market.sql` into remote via Node pg (psql absent) → market_snapshots=22, market_history=5500. Dashboard backend data ready (remaining: DASHBOARD_DATA_SOURCE=backend flip on frontend Vercel).
- 2026-06-26 — Storage bucket made env-driven: `SUPABASE_STORAGE_BUCKET` (default `blog.prettylog`) replaces hardcoded `blog.prettylog` in signImageUpload.ts (+ route + env.schema + .env.example) — USER created a PUBLIC bucket named `blog.prettylog`. build 0, 33 jest pass. Remaining Gate 3: set SUPABASE_URL/SUPABASE_SECRET_KEY on backend Vercel (else images/sign → 503).
- 2026-06-26 — Cleanup + deploy triage: removed user-confirmed `x_*` soft-deletes (c86c800); D-onb.7 confirmed DONE (USER). Vercel build failures (#5 server posts.write.ts TS2339, #10 client) NOT reproducible on current main — both `dashboard_server build` and `dashboard_client_web build` exit 0 locally with tsc 6.0.3 (sitemap fetch errors are try/caught, non-fatal) → failures were intermediate-commit auto-deploys; fix = redeploy latest. blog_agent still needs `SUPABASE_S3_BUCKET=blog.prettylog` in its (not-yet-created) `.env` to match the server's `SUPABASE_STORAGE_BUCKET`.
- 2026-06-26 — Verified rename sweep already complete in source: `blog-images`→`blog.prettylog`, `posts.prettylog`→`assets.prettylog`, `CDN_ASSET_POSTS`/`NEXT_PUBLIC_CDN_ASSET_POSTS`→`CDN_ASSETS`/`NEXT_PUBLIC_CDN_ASSETS` (schema key + const + all consumers, both apps + blog_agent `*.py`). 0 old tokens in source/config/env-templates (one residual only in gitignored `.turbo` log). server + client build & check-types pass. USER still must rename Vercel env vars + local `.env`/`.env.local` + asset DNS manually.
- 2026-06-26 — Vercel client build failure ROOT-CAUSED + fixed: `env.parse()` in `env.schema.ts` hard-gates config load (next.config.ts imports CDN_ASSETS at import time), so a Vercel env var SET to an invalid value (e.g. empty/old `NEXT_PUBLIC_CDN_ASSETS` or `NEXT_PUBLIC_API_BASE`) made `z…url()` throw ZodError → "Failed to load next.config.ts" → turbo "1 successful, 2 total / Failed: dashboard_client_web#build" (real error scrolls above the `[warn]` env lines). Fix: added `.catch(<default>)` to both `.url()` public vars so invalid values fall back instead of crashing. Reproduced exact symptom + verified fresh turbo build now survives invalid env; check-types + clean build pass. USER ops step: rename/clean Vercel env vars to new names with valid absolute URLs (or unset to use defaults).
- 2026-06-26 — Content sync (client): About page (`app/MainPage.tsx`, `/`) synced to latest `public/resume_taek_lim_1226.pdf` — added project "Time-Series Predictive Analytics Platform" (Python/ClickHouse/k3s/Kafka, 04/2026–Current); skills regrouped to resume categories (Languages/Frontend/Backend/Database/AI·ML/Cloud·DevOps/Tools, AWS intentionally KEPT per owner); UCM education → 01/2025–05/2027 (Expected); added "Global Vision Scholarship, UCM" + LinkedIn in/tiotaeklim to contact; "Last Updated" banner dynamic `new Date()`→static 6/26/2026 (Available 05/2026 kept). Blog left-aside "built with" (`app/blog/components/RightTechStackCard.client.tsx`) → React 19/Next.js 16/Express/Turborepo/Supabase/Clerk/Vercel (dropped legacy AWS-era/Oracle Cloud/Synology NAS/Cloudflare). Content-only; no phase/task affected.
- 2026-06-26 — UI maintenance (client, commit cb15dc9): merged header theme toggle + auth control into one unified Radix dropdown `NavMenu` (`components/nav-menu.client.tsx`) — trigger = hamburger when signed out / user avatar when signed in; dropdown holds theme toggle + auth (Sign in / Manage account / Sign out) + (mobile only) nav links About/Blog/Dashboard/Resume; next-themes logic extracted to `hooks/useThemeToggle.hook.ts` (ModeToggle reuses); `navigation-desktop.tsx` desktop keeps inline nav <ul>+NavMenu, mobile header uses NavMenu w/ showNavLinks, bottom drawer untouched (two mobile entry points by design); soft-deleted `components/auth-button.tsx`→`x_auth-button.tsx` (sole consumer migrated). tsc 0, prettier clean. No phase/task affected.
- 2026-06-26 — blog_agent publish ARCHITECTURE clarified (architect-verified, read-only): blog_agent is a DIRECT-WRITE publisher — writes posts/files/categories/tags directly to Postgres (SQLAlchemy/asyncpg) + uploads images directly to Supabase Storage `blog.prettylog` (supabase-py); publisher identity DEFAULT_USER_ID=2; Clerk NOT involved. It does NOT use the Express `/v1/api/posts` write API (that is the Clerk `/write` UI path); Express only READS the agent's rows.
- 2026-06-26 — blog_agent verification (B5): code Supabase migration confirmed DONE (boto3→supabase-py, POSTGRES_* standardized, tests green); schema copies byte-identical (`pnpm check:schema`); status default 'public' matches server public feed; is_thumbnail wiring correct; tags/post_tags + categories/post_categories shapes match server reads; DB + storage env-var names match across apps.
- 2026-06-26 — BLOCKER B1 (image render): `db/repositories/file.py:206-207` `upsert_file_record` binds `stored_uri := s3_key` (TODO hack), discarding the real CDN URL from the caller; server returns `stored_uri` verbatim as the thumbnail `<img src>` → resolves host-less/relative → agent-published thumbnails/images do NOT render. Fix = bind the real CDN URL. Broken out as task B5.1.
- 2026-06-26 — BLOCKER B2 (publish abort): `db/repositories/post.py:220` `upsert_post` uses `ON CONFLICT (user_id, slug)` but NO such unique constraint exists — migration `0003_clerk_multiuser.sql:11-12` explicitly forbids it (versioned-slug CTE) → Postgres raises "no unique/exclusion constraint matching ON CONFLICT" and the publish transaction aborts. Needs a DESIGN decision (re-publish = new versioned row vs upsert-by-id), not a mechanical fix. Broken out as task B5.2.
- 2026-06-26 — WARN W1/W2 (CDN URL): W1 — agent `storage.py:202` builds `${CDN_ASSETS}/${key}` while server `signImageUpload.ts:97` builds `${base}/${bucket}/${path}`, so the agent URL omits the `blog.prettylog/` bucket segment → even a fixed B1 would 404 against the Cloudflare rewrite. W2 — `CDN_ASSETS` default trailing-slash differs between apps. Both folded into task B5.1.
- 2026-06-26 — blog_agent FIRST REAL PUBLISH SUCCEEDED (commits 4a2366d + 0568915): `scripts/publish_oneshot.py` published `posts/java/spring/jdbc/java-spring-jdbc` → posts row (id=329061072378007552 Snowflake, user_id=1, slug=java-spring-jdbc, status='public') committed + thumbnail uploaded to Supabase Storage (is_thumbnail=true, working public URL); now VISIBLE via live `GET /v1/api/posts`. Publish→DB+Storage→live-API round-trip WORKING end-to-end (B5/BA-09/S8.3 closed).
- 2026-06-26 — ROOT CAUSE of "agent posts invisible in API fetch" (commit 9890227): blog_agent emits Snowflake post ids (~3.29e17) that EXCEED JS Number.MAX_SAFE_INTEGER (~9.0e15); server list endpoints paginated `p.id < cursor` with cursor defaulting to MAX_SAFE_INTEGER → EVERY agent post excluded from all list feeds (single-fetch by slug still worked). Fix: cursor is an opaque numeric string, default max int8 '9223372036854775807', bound as string; nextCursor kept full-precision id string; `@repo/types` TAdminPostListResponse.nextCursor widened to string|number|null.
- 2026-06-26 — Publisher identity corrected user_id 2→1 (commit 0568915): live `users` table has exactly one row id=1 (username=taeklim, Clerk-linked); DEFAULT_USER_ID=2 never existed → FK violation. Publisher now user_id=1 (post frontmatter `userId` + env default both → 1).
- 2026-06-26 — blog_agent config-load fix (commit 0568915): `configs/__init__.py` loads `.env` BEFORE importing `configs.storage` (fixes "supabase_url is required" crash); DEFAULT_USER_ID default 2→1; added reusable `scripts/publish_oneshot.py` one-shot publisher. Earlier (commit 4a2366d): B5.1 image-URL fixes (stored_uri real value, bucket segment W1, trailing-slash W2) + B5.2 versioned-slug INSERT (re-publish = new versioned row).
- 2026-06-26 — blog_agent publish FOLLOW-UPS (open, non-blocking): (a) Ollama not running at publish → post has empty tags/categories + "No summary available" description (LLM metadata skipped; re-publish with `ollama serve` for richer metadata) = B5.6; (b) public-feed CLIENT load-more still Number()-coerces the cursor (`app/api/posts/load-more/route.ts` + `getPosts.api.ts` + `blog.types.ts` + `post.action.ts`) → page-2+ precision loss latent once >10 posts = B5.5; (c) blog_agent local `.env` DEFAULT_USER_ID still =2 (sandbox blocked the edit) — owner to set =1 (frontmatter drives it, non-blocking) = B5.7.

---

## Open / blocked

> Live open items now tracked per-phase in `00.tasks.md` stubs. Snapshot: Phase 2-simple Cycle 3 (dashboard mock→real; USER `DASHBOARD_DATA_SOURCE` flip) · Phase C (not started) · Phase H P6 (deferred) · Phase I USER gates (`db push` 0011/0012 + first live Yahoo run + dashboard verify) · USER go-live gates (Clerk env both Vercel projects, backend `SUPABASE_*`, admin bootstrap SQL, `db push` 0010, Cloudflare `CDN_ASSETS` CNAME) · AWS retirement S8.2/S9.1–S9.3. (B5.5 client cursor precision FIXED; UX12b-persist SHIPPED.)

- **scripts/ retired:** soft-deleted `apps/dashboard_server/scripts/` → `x_scripts/` (git mv) — publish pipeline fully covered by apps/blog_agent (markdown extract, Supabase Storage upload, category hierarchy, post/file upsert) and fix_*.ts migrations superseded by supabase/migrations 0001 (categories_user_title_key, files unique user_id+stored_name) / 0003 (posts user_id+slug intentionally dropped); tsconfig dropped redundant "scripts" exclude (kept **/x_*); package.json untouched (no script refs, AWS/sequelize deps already absent). Verify: build PASS, check-types PASS, test 7/7 suites 33/33 PASS.
- **blog_agent storage migrated boto3→supabase-py native:** soft-deleted `configs/aws.py`→`x_aws.py` + `db/s3.py`→`x_s3.py`; new `configs/storage.py` (SUPABASE_URL/SUPABASE_SECRET_KEY/SUPABASE_STORAGE_BUCKET) + `db/storage.py` (native client, idempotent upload via upsert:"true", defensive signedURL dict extract, dropped S3 user-metadata); pyproject boto3→supabase>=2.15; CDN public-URL convention `${CDN_ASSETS}/<key>` retained; FileMetadata.s3_key field unchanged. Verify: py_compile PASS (6 files), grep clean (no db.s3/configs.aws/upload_file_s3/SUPABASE_S3_ outside x_ files).
- **postgres env naming synced to Supabase/Vercel integration convention:** dashboard_server keeps POSTGRES_URL*/DATABASE_URL* precedence (env.schema.ts) + registers POSTGRES_PRISMA_URL as IGNORED; database.ts stripSslmode now also drops `pgbouncer`; blog_agent configs/database.py resolves first non-empty of POSTGRES_URL_NON_POOLING→DATABASE_URL_DIRECT→POSTGRES_URL→DATABASE_URL (DIRECT/non-pooling preferred) and strips sslmode+pgbouncer+connect_timeout (asyncpg unrecognized-param failures); env.d.ts + .env.example/.env.vercel.example updated. Verify: py_compile PASS (3 files), check-types PASS, grep clean (no POSTGRES_USER/HOST/PASSWORD/DATABASE in code, no NEXT_PUBLIC_ DB var).
- **removed legacy DATABASE_URL/DATABASE_URL_DIRECT entirely — standardized on POSTGRES_* only:** dashboard_server env.schema.ts now resolves runtime pg Pool from POSTGRES_URL (pooled 6543) and migrations from POSTGRES_URL_NON_POOLING (direct 5432) with NO fallback (kept internal export symbol names DATABASE_URL/DATABASE_URL_DIRECT as aliases for database.ts/migrations); POSTGRES_PRISMA_URL stays declared but IGNORED; blog_agent configs/database.py resolves first non-empty of POSTGRES_URL_NON_POOLING→POSTGRES_URL (DIRECT preferred), strips sslmode+pgbouncer+connect_timeout; env.d.ts + all 4 .env.example (server, server vercel, supabase, blog_agent) dropped DATABASE_URL* names. Verify: py_compile PASS (3 files), check-types PASS, grep shows zero legacy env-var READERS (process.env/os.getenv) — only intended symbol aliases remain.
- 2026-06-26 docs: documented Supabase discrete POSTGRES_USER/HOST/PASSWORD/DATABASE as injected-but-unused in .env.example (doc-only, no code wiring)
- 2026-06-26 blog_agent tests GREEN (13 passed / 16 skipped / 0 failed / 0 errors): added __tests__/conftest.py with collect_ignore (test_improved_agents.py, test_llm_providers.py) + shared db_available() guard; skipif DB-gated tests in test_category_storage.py (3 methods) + test_select_categories.py (module-level); used `from __tests__.conftest import db_available` (plain `from conftest import` does not resolve under pkg+pythonpath layout); coverage gate `--cov-fail-under=80` in pyproject unchanged (separate scope), GREEN verified with CLI override `--cov-fail-under=0` (reported cov 33%).
- 2026-06-26 fix(server): pin `strict: true` in dashboard_server/tsconfig.json — TS2339 on TValidateUsernameResult/TClaimUsernameResult/TValidatePostStatusResult was discriminated-union narrowing disabled under strictNullChecks=off (reproduced via `tsc --noEmit --strictNullChecks false`); committed tree already compiled under inherited strict, but extends-only dependency let build envs drop strict. Verify: tsc --noEmit EXIT=0.
- 2026-06-26 fix(server): make dashboard_server/tsconfig.json self-contained (drop `extends @repo/typescript-config`, inline target/module/strict/esModuleInterop/skipLibCheck/composite/ignoreDeprecations). ROOT CAUSE of Vercel TS2339: @vercel/node bundles ts-node@10.9.1 which cannot resolve `extends`→workspace pkg under TS>=5.3, silently dropping strictNullChecks → discriminated-union narrowing collapses. Web-researched + confirmed (vercel/vercel#12087). @repo/types is NOT involved (failing types are local to src/helpers). Verify: local tsc --noEmit EXIT=0.
- 2026-06-26 fix(server): add strict gate to Vercel buildCommand (`&& pnpm --filter dashboard_server check-types`) — deterministic fail-fast on repo own strict tsconfig regardless of @vercel/node internal typecheck behavior (complements self-contained tsconfig).
- 2026-06-27 ci: add .github/workflows/supabase-keepalive.yml — cron `0 9 * * 1,4` (Mon/Thu) runs `psql "$DATABASE_URL" -tAc 'SELECT 1;'` to prevent Supabase free-tier 7-day inactivity pause (pause is gated on real DB activity, not dashboard/cached API hits). Requires GitHub repo secret DATABASE_URL; workflow_dispatch enabled for manual run.
- 2026-06-27 feat(blog): post-card actions (branch feature/blog-card-actions). Phase 1 — overlay/stretched-link whole-card nav (fixes invalid <button>-in-<a>), Share (useSharePost: Web Share + clipboard fallback, SHARE→COPIED, AbortError-safe, username/slug guard), removed VoteBlock+Comment+fabricated `?? 410`/`?? 3`, SAHRE→SHARE, TPost.id number→string, key={post.id}. Phase 2 — post_favorites table (migration 0006, mirrored to both schema SSOTs), Express /v1/api/favorites GET/POST/DELETE (requireAuth, userId-from-token, ids-as-strings, ON CONFLICT, FK 23503→404), @repo/types favorite types, client apis+useFavorites/useToggleFavorite (optimistic+rollback), SaveButton live (signed-out→/sign-in?redirect_url). 2-cycle pm+architect planned (_docs/blog-post-card-actions.plan.md) + independent validation, both phases architect-APPROVED. Verify: check:schema sync, server build+test (favorites 5/5 after user supabase db push), client lint EXIT=0 + check-types clean.
- 2026-06-27 UX0: autosave preserves post status (editor draftInput + useDraftAutosave) — public posts no longer self-demote on edit
- 2026-06-27 feat(authoring): post create/edit UX (branch feature/post-authoring, from main 894fce3; plan _docs/00.markdown-publishing.plan.md §13, 2x web-checked validation APPROVED). Editor+autosave+CRUD pre-existed (dc28f19); this cycle adds discovery + feedback + bug fixes.
- 2026-06-27 UX1: nav-menu avatar dropdown adds signed-in Write→/write + My drafts→/me (reuses DropdownMenuItem/asChild + !isLoaded placeholder). check-types PASS, 0 lint errors.
- 2026-06-27 UX3: middleware.ts protects '/me(.*)' (mirrors /write). check-types PASS.
- 2026-06-27 UX4: /me drafts list — app/me/page.tsx shell + components/me/MyPostsList.client.tsx + PostListItem.client.tsx on useMyDrafts (loading/empty/error, NEW POST CTA, relative-time, no status pills, no new deps). check-types PASS, 0 errors.
- 2026-06-27 UX2: navigation-desktop.tsx header WRITE link (desktop bar + mobile drawer), signed-in-gated. NOTE: @clerk/nextjs@7.5.9 does NOT export <SignedIn> → used repo idiom useUser().isSignedIn (consistent w/ nav-menu/SaveButton); reserved-width slot for CLS; /write NOT in buildLinks. check-types PASS, 0 errors.
- 2026-06-27 UX-EDIT: owner-only Edit on /blog/[username]/[slug] → /write/{id} (OwnerEditButton.client.tsx + threaded @-stripped ownerUsername through PostContent/PostContentCard; check user?.username===ownerUsername). check-types PASS, 0 errors.
- 2026-06-27 UX5a: extracted EditorToolbar.client.tsx (151 LOC) from MarkdownEditor.client.tsx (222→148 LOC); pure refactor, textareaRef/insertAtCursor stay in parent, onInsert prop. check-types PASS (own files 0 errors).
- 2026-06-27 UX5b: trim STATUS_OPTIONS→draft/private/public (E11) + normalize seeded legacy follower→private (ALLOWED_SEED_STATUS) so controlled <select> can't silently mutate. check-types PASS.
- 2026-06-27 UX6: useDraftAutosave returns {status,lastSavedAt,retry} derived from create/update useMutation state + 1 local dirty flag (no hand-rolled machine); create→PATCH handoff gap-free. check-types PASS.
- 2026-06-27 UX7: AutosaveIndicator.client.tsx (SAVING…/SAVED·{relative}/UNSAVED/FAILED+RETRY; role=status polite, error role=alert assertive) wired into editor header w/ reserved min-h. check-types PASS.
- 2026-06-27 UX8: EditorPreview.client.tsx wraps MarkdownRenderer + ~180ms debounce, aria-label non-live region; MarkdownEditor swaps inline preview→<EditorPreview>. check-types PASS.
- 2026-06-27 RECOVERY: cross-session branch switch to main + `git stash -u` left UX0–UX7 stashed while UX8 sat in working tree. Recovered (backup UX8 → restore MarkdownEditor to base → stash pop 0-conflict restoring UX0–UX7 incl 6 untracked files → re-applied UX8 swap onto rich editor). Verified check-types clean + lint 0 errors + 12 UX files present. Protective checkpoint committed fcb961f. Branch 1 behind main 75763e5 (env.schema dotenv hotfix content already in fcb961f).
- 2026-06-27 UX9: useComposition.hook.ts (isComposing + isComposingRef); textarea onCompositionStart/End; insertAtCursor skips caret mutation while composing; EditorPreview holds debounce flush until compositionend — fixes KO Hangul jamo-dup/cursor-jump. check-types PASS, 0 errors.
- 2026-06-27 UX10: responsive editor — viewMode + mobile-only Write|Preview tablist (role=tablist/tab, aria-selected); CSS-only md breakpoint (no useIsMobile flash); md+ keeps live split. commit a9443df.
- 2026-06-27 UX11: useUnsavedGuard (beforeunload dirty-gated) + useDraftAutosave flush-on-unmount (persists pending edit on in-app nav; no confirm dialog). commit a9443df.
- 2026-06-27 UX12a: PostSettings.client.tsx (collapsible, editable slug) + publish confirm on draft→public + VIEW LIVE link; slug excluded from 2s autosave loop. commit 1357d8e.
- 2026-06-27 FIX-QC: ReactQueryProvider on app/blog/layout.tsx + new app/me/layout.tsx (per-route convention; root has none) → fixes blog SaveButton + /me 'No QueryClient set' regression (surfaced after env.schema dotenv hotfix). next build ✓ 16/16, error gone. commit e615b2c.
- 2026-06-27 UX-A11Y: title autofocus (new post only) + title-required aria-describedby hint; router.replace focus-keep confirmed. commit 03dedb8.
- 2026-06-27 UX-I18N: write next-intl namespace (en+ko) for this cycle's new copy (autosave/settings/publish/view-live/title-hint/drafts/nav); registered in i18n/request.ts. commit 379bd16.
- 2026-06-27 UX-TEST: vitest 4 + RTL + jsdom runner (none existed) + useDraftAutosave.test.ts UX0 regression (existing public post keeps status='public'; new→draft). 2/2 pass. commit 5e08005.
- 2026-06-27 **AUTHORING UX MERGED TO MAIN** (merge a0bdd70): feature/post-authoring (UX0–UX12a + FIX-QC + UX-A11Y/I18N/TEST, 7 commits) → main. Gates on merged main: check-types clean, vitest 2/2, lint 0 errors, next build ✓ 16/16 (blog+/me prerender, 0 'No QueryClient'). env.schema 3-way auto-merged (identical to hotfix 75763e5). NOT pushed to origin (awaiting owner). Deferred to backend cycle: UX12b thumbnail (E1–E7), KO View-live slug quality (E8 slugify), editor-wide i18n retrofit of legacy D2 strings.
- 2026-06-27 UX-I18N2: editor-wide i18n retrofit of legacy D2 authoring strings — migrated 26 hardcoded strings across 8 components (EditorToolbar/MarkdownEditor/PostSettings/EditorPreview/MyPostsList/PostListItem/OwnerEditButton) into write namespace (en+ko); added status/a11y/ownerEdit + editor/toolbar/settings/drafts/autosave keys; relative-time formatter now uses autosave.* keys. check-types 0, lint 0 errors, vitest 2/2. (isolated worktree branch, not merged).
- 2026-06-27 PROD fix chain (on main, pushed origin 8fdcb5a→e7980df): (76079a1) NEXT_PUBLIC_API_BASE default/catch localhost:4000→https://project-tradelunch-blog-server.vercel.app — favorites etc. client calls were hitting localhost in prod (CONN REFUSED); matches CDN_ASSETS default-to-prod convention, dev overrides via .env.local. (e7980df) hoist ReactQueryProvider to ROOT layout (app/layout.tsx), soft-delete 5 per-route layouts → fixes recurring 'No QueryClient set' by construction (PM(a) vs architect(b); architect verified b complete/build-green → adopted). Both verified build ✓.
- 2026-06-27 cleanup: hard-deleted 5 inert x_layout.tsx wrappers (commit f959808, on main, NOT pushed). Sitemap localhost:4000 = NO-OP: sitemap fetches via getPosts.api→axios_instance→API_BASE (already prod default); only literal localhost:4000 left are comments + .env.example local-dev template (correct). The ECONNREFUSED in local builds is the .env.local override, not a prod bug.
- 2026-06-27 console error VERDICT (pm+architect, web-verified, cited): contentscript.js MaxListenersExceededWarning + ObjectMultiplex 'orphaned data for stream app-init-liveness' = browser-EXTENSION noise (a 'Job Apply Assistant' job-autofill extension using MetaMask's object-multiplex lib for its keep-alive stream), NOT MetaMask itself, NOT the app. Repo grep: all signatures NOT FOUND, zero web3/wallet deps. Harmless (isolated content-script world). No action. Confirm via incognito (extensions off).
- 2026-06-27 UX12b: post thumbnail authoring (client UX) — new ThumbnailPicker.client.tsx (pick/preview/clear, reuses useImageUpload → Supabase signed PUT) wired into PostSettings.client.tsx; MarkdownEditor lifts thumbnailUrl state + handlers (2nd useImageUpload instance); useEditorSeed surfaces existing post.stored_uri as thumbnailUrl for edit-seed preview; write i18n keys added (en+ko settings.thumbnail*/a11y.thumbnailPreview); RTL test ThumbnailPicker.test.tsx (3 cases). tsc 0 / eslint 0-err on touched files / vitest 5 pass. BACKEND GAP (reported, not invented): thumbnail does NOT persist — owner-scoped authoring API (posts.write.ts / TPostInput) has no files.is_thumbnail writer; only blog_agent writes thumbnail rows direct-to-Postgres. Persisting from the editor needs a new backend endpoint + TPostInput thumbnail field (out of lane) → tracked as UX12b-persist follow-up.
- 2026-06-27 S7.2: prettier --write 85 client ts/tsx files (4517→210 client eslint warnings; 4309 prettier/prettier cleared) scoped to exclude UX12b lane (components/write|me|blog, messages, layout/env/middleware); triaged real warnings — removed 3 safe dead imports (spinner Loader2Icon, useIsMounted map, resume Image), no logic/any/suppress changes. Final 207 warnings remain (78 prop-types, 45 no-unused-vars, 22 no-explicit-any, 12 no-unescaped-entities, 8 prettier [all locked in excluded write/blog/layout dirs], +misc) — GATE DEFERRED (can't reach 0: 8 prettier warnings sit in out-of-lane files + ~200 judgment-needed real warnings). check-types clean after pnpm install. CI prereq: blog_agent `uv run ruff check .` blocked by pyenv 3.12.3 not installed; uvx ruff (bypass) shows 696 ruff errors (539 auto-fixable) in blog_agent Python — separate lane, untouched.
- 2026-06-27 MERGED TO MAIN (6b825f7): UX-I18N2 (i18n retrofit) + UX12b client thumbnail UX + S7.2 prettier pass — check-types 0, vitest 5/5; remaining = UX12b-persist backend + lint/CI debt (blog_agent ruff, pyenv).
- 2026-06-27 S7.2b: blog_agent ruff 696→39 (safe auto-fix + whitespace --unsafe-fixes, 35 py files, compileall ✓); lint script uv run ruff→uvx ruff (drops pyenv 3.12.3 pin blocker, CI-portable). Residual 39 = logic (F403/405 import-star, B904, E402, F841, E712) for manual pass. NOTE: this cycle's "client 4517 warns" diagnosis was measured on a stale worktree base (5253e5d, pre-S7.2 prettier merge) — actual main client lint after S7.2 = ~207 warns (see prior line); the 4517 figure does NOT reflect main. Gate stays DEFERRED. check-types ✓.
- 2026-06-27 S7.2c: GATE FLIPPED GREEN both lanes. Lane A client eslint 233→0 (max-warnings 0 enforced in script): prettier 34, react/prop-types off in shared next config (78, TS already types props), no-unused-vars 45 + no-explicit-any 22 + no-unescaped-entities 12 + no-loss-of-precision 10 + empty-object/require/ban-ts-comment/html-link fixed properly; react-hooks@7 compiler rules (set-state-in-effect/refs/immutability/purity) disabled in shared config w/ justification (codebase not React-Compiler-compiled, flagged working hydration guards), kept rules-of-hooks(err)+exhaustive-deps; 3 exhaustive-deps + 3 no-unknown-property (cmdk/styled-jsx) scoped-disabled w/ reasons; useChartDrawings drawings wrapped in useMemo. Lane B blog_agent ruff 39→0 via uv run ruff on pyenv 3.12.3 (REVERTED uvx→uv run; miniconda root-cause: .python-version pin 3.12.3 only existed in ~/miniconda3, pyenv lacked it, so uv resolved miniconda; fix = pyenv install 3.12.3 + uv venv on it + uv sync): F403/405 explicit imports (config.py+configs/__init__.py), B904 raise-from, E402 scoped noqa (sys.path tests), F841 dead locals removed, E712 truthiness; ruff [tool.ruff]→[tool.ruff.lint] dep-warning fixed. CI: stale next-lint comment removed, added pnpm --filter dashboard_client_web lint (blog_agent ruff lane needs separate Python job, noted). Validation: client lint/check-types/vitest(5/5) = 0, blog_agent ruff = 0, compileall ✓.
- 2026-06-27 UX12b-persist: editor thumbnail now persists — TPostInput.thumbnailUrl (+@repo/types build), validatePostInput accepts string|null, new writeThumbnail.ts (pure parseThumbnailUrl + transactional clear-then-upsert of one files.is_thumbnail row, owner-scoped, ON CONFLICT(user_id,stored_name)), wired into POST/PATCH inside BEGIN/COMMIT with rollback; client handleSave sends thumbnailUrl, autosave deliberately omits it. CDN-parity OK (stored_uri = sign endpoint publicUrl, read join opaque). No migration. Gates: types/server/client build+check-types ✓, server jest 52/10 ✓, client lint 0 + vitest 5/5 ✓, check:schema in sync.
- 2026-06-27 MERGED TO MAIN (31eb14a, pushed): UX12b-persist (FF) — re-verified on main (server check-types 0, client lint --max-warnings 0, check:schema in sync); 00.tasks.md UX12b-persist→[x] + Active line + UX12b ⚠️→✅; all today's worktree branches merged+pushed, stale branches/worktrees deleted.
- 2026-06-27 Phase E PLANNED (2 bg cycles done): DB structure audit → _docs/db-structure-audit.md (verdict SOUND, all 3 features purely additive, ZERO existing-table ALTER; check:schema in sync; Issue#1 — posts.parent_id/group_id/level NOT dead, blog_agent runs a posts-based comment tree on them → reconcile before C1; Issue#2 favorites created_at unindexed→0007; Issue#3 FK on-delete convention). Comments plan reconciled to OPTION C (stored BIGINT[] materialized path, UNLIMITED depth, ORDER BY path no CTE, render-collapse N=4) in _docs/post-likes-comments.plan.md §8.RESOLVED; all forks resolved. Final landing-ordered task list folded into 00.tasks.md Phase E (S1-S3 Saved / T0 types / L1-L6 Likes / C1-C7 Comments; migrations 0007/0008/0009 locked). NOT built. Commit docs-only, owner pushes manually.
- 2026-06-27 Phase E DB audit → _docs/db-structure-audit.md: verdict SOUND — all 3 features (Saved/Likes/Comments) purely additive, ZERO existing-table ALTER (new tables + indexes only); check:schema in sync.
- 2026-06-27 Phase E Issue#1 RESOLVED = PARALLEL: the new dedicated `comments` table is the sole user-facing comment system; apps/blog_agent's posts-based parent_id/group_id/level columns left UNTOUCHED (no backfill, no migrate); guardrails G1–G4 honored.
- 2026-06-27 E-Saved MERGED+PUSHED (origin/main head 83d7e8a): dd36256 S1 0007_post_favorites_user_created_idx.sql (user_id, created_at DESC) · 1fdccf6 S2 GET /v1/api/favorites/posts owner-scoped join posts (deleted_at IS NULL AND status='public', ILIKE search, STRING keyset cursor, page 20 — fixes favorites.ts:23 soft-delete gap) · 83d7e8a S3 /me/saved list+search UI (RecentPostCard, leave-until-refetch, debounced ?q=). Gates: server jest ✓, client lint --max-warnings 0=0, vitest 5/5, check:schema in sync, tsc 0.
- 2026-06-27 E-Likes MERGED+PUSHED (origin/main head 3a45497): 5292749 T0 TPost additive (drop upvotes?/comments?; add likeCount/viewerLiked/commentCount) · 4e0926b L1 0008_post_likes.sql (composite PK user_id,post_id, FK NO ACTION matching post_favorites, idx(post_id), live COUNT) · 4b6619d L2 @repo/types TLikeToggleResponse · f7f5864 L3 helpers/likes.ts toggle + POST /v1/api/posts/:postId/like · b40075f L4 detail-page LikeButton optimistic · 3d46ebc L5 enrich 4 posts reads with like_count/viewer_liked + optionalAuth+$viewerId on feed GET / + fix soft-delete Rule-1 gaps posts.ts:284 (by-id) & posts.ts:341 (by-slug) → deleted posts now 404 · 3a45497 L6 feed-card LikeButton. Gates: server jest ✓, client lint 0, vitest 5/5, check:schema in sync, tsc 0.
- 2026-06-27 E-Comments MERGED+PUSHED (origin/main head 7b2ea0a): ef35f63 C1 0009_comments.sql (Option C stored BIGINT[] materialized path, UNLIMITED depth, self-FK parent_id→comments(id), idx_comments_post_path + idx_comments_parent, soft-delete deleted_at, FK NO ACTION) · 36df38b C2 types · 18f15c7 C3 helpers/comments.ts (transactional CTE path=parent.path||id, ORDER BY path no recursive CTE, [deleted] tombstone read-mask, author+post-owner+admin soft-delete) · 17e0b29 C4 controllers (create/list/delete) · 3723b32 C5 commentCount → (SELECT COUNT(*) FROM comments WHERE deleted_at IS NULL) across 4 posts reads · d140042 C6 apis+hooks optimistic reply nests under actual parent · 7b2ea0a C7 RSC list + IME-safe composer + render-collapse N=4 "continue thread →" + Dialog delete + en/ko i18n. Deviations: FK NO ACTION (not plan CASCADE); GET returns whole tree in one payload (NO keyset cursor yet, deferred); no comment EDIT/PATCH route. Gates: server jest 68/68, client lint 0, vitest 5/5, check:schema in sync, tsc 0 ×3 (root check-types has PRE-EXISTING unrelated blog_agent mypy fail, not Phase E).
- 2026-06-27 Phase E USER GATE OPEN: apply migrations 0007+0008+0009 to Supabase via `supabase db push` (owner applies; not auto-pushed), then re-run `pnpm --filter dashboard_server test` to exercise the DB-gated saved/likes/comments assertions. Also verify feed GET / optionalAuth change vs CDN/caching (still serves anon; authed requests now resolve a token for viewer_liked).
- 2026-06-27 FIX-like-persist: viewer "liked" survived-refresh bug fixed via CLIENT query (mirrors Save), NOT server token-forwarding — keeps the SSR post read cacheable/anonymous (avoids CDN per-user poisoning flagged in Phase E USER GATE). New GET /v1/api/likes (likesListRouter, own /api/likes mount; helpers/likes.listLikedPostIds; @repo/types TLikedResponse) → useViewerLikes Set; LikeButton derives liked from the shared ['viewer-likes'] cache (initialLiked = pre-hydration fallback only); useToggleLike now optimistically flips that cache (onMutate/onError/onSettled). likeCount stays server-seeded local (public aggregate). Gates: server check-types 0 + jest 68/68, client check-types 0 + lint 0 + vitest 5/5.
- 2026-06-27 UI-actions-header: moved Like + Save to header TOP-RIGHT on both feed card (RecentPostCard, Share stays in footer) and post detail (PostContentCard — Save NEWLY rendered there, grouped with owner Edit). check-types 0, lint 0.
- 2026-06-27 GUARDRAIL-feed-cdn: verified feed GET / optionalAuth is CDN-safe — client fetcher (getPosts.api.ts) forwards NO token, so responses stay viewer-agnostic (viewer_liked all-false anon variant); documented inline at posts.ts feed route: if a per-user token is ever forwarded, must set Cache-Control: private/no-store or Vary: Authorization to avoid CDN cross-user poisoning. No code behavior change.
- 2026-06-27 UI-share-header: moved Share to the header action group too — feed card (RecentPostCard, empty footer div removed) and post detail (PostContentCard, Share NEWLY rendered there). Header order Like·Share·Save(+Edit). check-types 0, lint 0.
- 2026-06-27 Phase E2 (Comment Edit + Pagination) MERGED+PUSHED (origin/main head 85093b4): d36ef68 C1 split helpers/comments.ts → helpers/comments/{errors,list,write,index}.ts barrel (x_comments.ts gutted; ≤300 LOC) · 9065b31 C2 TComment.updatedAt + project c.updated_at in all reads (no schema change; optimistic create updatedAt=createdAt) · 4f2d32c C3 PATCH /api/comments/:commentId edit — author/owner/admin via shared assertCommentMutable (softDelete refactored to reuse), tombstone edit → CommentDeletedError 409, TCommentUpdateRequest, 6 live-DB tests · 02735b5 C4 cursor pagination listCommentPage (50 roots/page parent_id IS NULL keyset id<cursor sentinel, each root + full subtree path[1]=ANY, tombstoned-root-with-live-reply kept, fully-dead excluded, pure orderByRoots) + GET cursor/limit clamp 50/100 + nextCursor/hasMore (additive) · 45ba6c3 C5 client useQuery→useInfiniteQuery (RSC seed → initialPage first page; optimistic create repacks per-page so replies still nest under parent; delete maps all pages) + CommentsPagination "Load more (next 50)" + en/ko i18n · 85093b4 C6 inline edit updateComment.api + useUpdateComment optimistic (InfiniteData, bumps updatedAt) + CommentRow Edit affordance (canModify=author/owner/admin shared w/ delete) + "(edited)" hint + i18n. Decisions: edit-auth==delete-auth (owner override of architect author-only); pagination by ROOT keyset (not numbered pages); NO schema/index change. Gates each commit: server jest 78/78 (live DB, migrations 0007/0008/0009 applied), client check-types 0 + lint --max-warnings 0=0 + vitest 5/5 + next build ok.
- 2026-06-27 GUARDRAIL-feed-cdn done as 53652ee (feed GET / optionalAuth verified CDN-safe; inline note at posts.ts).
- 2026-06-28 UI-mobile-actions: collapse blog post action buttons into a single MoreHorizontal dropdown on mobile (<768px) to fix header overflow. New generic client component app/blog/components/post-card-actions/PostActions.client.tsx (useIsMobile.hook; desktop = unchanged inline flex row, mobile = Radix DropdownMenu rendering same children once; accepts className for ml-auto). Wired into RecentPostCard.client.tsx and PostContentCard.server.tsx (server→client children pass-through). Desktop pixel-identical. client check-types 0 + lint --max-warnings 0=0.
- 2026-06-28 UI-dashboard-rail: reclaim navbar vertical space on /dashboard* (Option A, PM+architect joint). DesktopNavigation now usePathname()-aware: on /dashboard* renders new compact 40px icon-only DashboardNavRail (lucide icons Home/Newspaper/LayoutDashboard/FileText + SquarePen for write; title/aria-label for labels; shared NavMenu dropdown preserved) instead of the h-16 bar; full bar kept as default branch for every other route (soft-delete: not removed, gated). ChartVariantLayout offset calc(100dvh-66px)→calc(100dvh-42px) (rail 40px + border 2px). Net ~24px taller chart. Mobile h-14 bar unchanged (reclaim desktop-only per PM). client check-types 0 + eslint 0.
- 2026-06-28 UI-dashboard-menu-drawer: replace the icon rail (disliked: inconsistent w/ site nav) with in-chart menu. Desktop chart dashboard (/dashboard* except preview/cards,table) now hides the top bar entirely (ChartVariantLayout lg:h-[100dvh], full ~66px reclaimed); a Menu(☰) button added to ChartHeader left (md+ only) opens the EXISTING mobile slide-up drawer for site nav → visual consistency. New: store/menu.atom.ts (isMenuDrawerOpenAtom shared open-state), hooks/useNavLinks.hook.ts (buildNavLinks/useNavLinks, breaks nav↔drawer circular dep), components/MenuDrawer.client.tsx (drawer extracted from MobileNavigation, atom-controlled, all viewports). navigation-desktop.tsx slimmed: DesktopNavigation returns null on chart routes; MobileNavigation floating button + (new) chart-header button both drive the atom. Preview grid eval routes keep the full bar (never strand nav). Mobile behavior unchanged. client check-types 0 + eslint 0.
- 2026-06-28 BUGFIX-post-id-bigint: post ids are Postgres BIGINT (some imported rows exceed 2^53, e.g. 329143438039584801) — JS Number()/parseInt rounds them (…801→…830), so /write/<id> by-id lookup missed → editor seeded empty ("original texts gone"); save/delete could also target the wrong row. Fix: treat post id as an opaque numeric STRING end-to-end. Client: app/write/[postId]/page.tsx drops Number() → digit-regex keep-string; MarkdownEditor/useEditorSeed/useDraftAutosave/useUpdatePost/useDeletePost + getPostById/updatePost/deletePost.api postId: number→string. Server: posts.write.ts PATCH/DELETE drop parseInt → String()+/^\d+$/ validate; writePost.ts updatePost/softDeletePost/TPostRow.id, writeThumbnail.upsertThumbnail, listDrafts.ts (was Number(r.id)!) → string. @repo/types TDraftSummary.id number→string (rebuilt dist). Tests updated to string ids. Gates: client tsc 0 + eslint 0 + vitest 2/2; server tsc 0 + jest 78/78. NOTE follow-up: TAdminPostListItem.id still number (admin moderation) — same latent bug, not in this fix.
- 2026-06-28 FIX-autosave-loop: useDraftAutosave fired a PATCH every ~2s even with NO edits — its effect deps on `input` (a new object each render) + setDirty(false)→re-render→effect→setDirty(true) spun an endless save loop, also causing repeated re-renders (incl. on retry). Added a snapshotOf() content-equality guard + savedSnapshotRef baseline: skip the save when the payload equals the last persisted one; baseline an existing post's seeded body on first enabled render (new post falls through so first content still creates). Now: triggered on real change only, debounced AUTOSAVE_DELAY_MS=2000, no-change→no request, no post-save render churn. NOTE: the earlier "failed to save" was NOT the bigint bug — that post's id 329143438039584768 is a representable double; failure was transient (expired Clerk token / cold-start axios 5s timeout). client tsc 0 + eslint 0 + vitest 2/2 (test updated: unchanged mount no-ops, edit triggers PATCH).
- 2026-06-28 FEAT-post-admin-override: post PATCH/DELETE now owner-OR-admin (was owner-only), matching the comments edit/delete policy and the OwnerEditButton affordance (shown to admins). Root cause of "fails to update": imported post is owned by user_id 1 (taeklim) but the live Clerk login resolves (resolveAuth lazy-provisions by clerk_user_id) to a DIFFERENT users row — same human/email, different id — so owner-scoped writes 404 while public reads/edit-button work. writePost.updatePost/softDeletePost gain isAdmin=false param → WHERE (user_id=$2 OR $isAdmin); posts.write PATCH/DELETE pass req.auth.isAdmin. server tsc 0 + jest 79/79 (added admin-override integration test). NOTE: durable fix is identity reconciliation (link user_id 1's clerk_user_id to the live sub, or repoint posts) — pending owner. Minor: upsertThumbnail still writes files under caller userId, so admin-editing-with-thumbnail could orphan the owner's thumbnail row (body edits unaffected).
- 2026-06-27 UX12b-persist: editor thumbnail now persists — TPostInput.thumbnailUrl (+@repo/types build), validatePostInput accepts string|null, new writeThumbnail.ts (pure parseThumbnailUrl + transactional clear-then-upsert of one files.is_thumbnail row, owner-scoped, ON CONFLICT(user_id,stored_name)), wired into POST/PATCH inside BEGIN/COMMIT with rollback; client handleSave sends thumbnailUrl, autosave deliberately omits it. CDN-parity OK (stored_uri = sign endpoint publicUrl, read join opaque). No migration. Gates: types/server/client build+check-types ✓, server jest 52/10 ✓, client lint 0 + vitest 5/5 ✓, check:schema in sync.
- 2026-06-28 PLAN-RECORDED (PM, docs-only): Phase F — Blog Image Upload Rework (Express proxy + 2-step resize) folded into 00.plan.md (Phase F section + Decisions Log #27, SUPERSEDES #14) + 00.tasks.md (F1–F17 atomic breakdown). NOT built; design FINAL (owner+architect pre-decided). D2.6b/D2.6c marked SUPERSEDED.
- 2026-06-28 FEAT-phase-F-server (F1,F2,F4-F10): Blog image upload SERVER side built. @repo/types: removed TImageSignRequest/TImageSignResponse, added TImageUploadResponse{publicUrl} (rebuilt dist). dashboard_server deps +sharp@0.35.2 +multer@2.2.0 +@types/multer (frozen-lockfile reproduces). New pure helpers: imagePath.ts (buildImagePath+ALLOWED_IMAGE_TYPES extracted from old signImageUpload, ext fixed webp, userId-prefix isolation); transformImage.ts (sharp failOn:error → rotate → resize 1600 inside/no-enlarge → webp q80 → toBuffer; discriminated ok/not_an_image/too_large; EXIF stripped, no .withMetadata); uploadImage.ts (S2S POST /storage/v1/object/{bucket}/{path}, Bearer secret, x-upsert:false, publicUrl={publicBase}/{bucket}/{path}). posts.write.ts: removed POST /images/sign, added POST /images (requireAuth + multer memoryStorage 5MB, uploadSingleFile wrapper maps LIMIT_FILE_SIZE→413) wired imagePath→transformImage→uploadImage; error map: no-creds 503 / no-file 400 / not_an_image 415 / too_large 413 / upload-fail 502. signImageUpload.ts soft-deleted → x_signImageUpload.ts. vercel.json: functions.api/index.ts.includeFiles for @img+sharp-*linux* (DEPLOY-VERIFY needed). jest transformImage.test.ts (valid PNG→webp ≤1600 / garbage→not_an_image). Gates: server tsc 0, build (tsc+tsc-alias) 0, jest 86/86. OPEN: client (F11-F14) still imports removed TImageSign* in signImageUpload.api.ts/useImageUpload.hook.ts → client check-types will fail until that separate task lands; F9 sharp-on-Vercel bundling unverified (needs deploy).
- 2026-06-28 FEAT-phase-F-client (F11-F14): Blog image upload CLIENT side built. New utils/resizeImage.ts (native Canvas, no lib): non-image passes through untouched; createImageBitmap{imageOrientation:'from-image'} bakes EXIF → downscale long-edge 2000px (no upscale) → canvas drawImage → toBlob webp q0.9, single re-encode at q0.7 if >4MB → File '<name>.webp'. New apis/uploadImage.api.ts: FormData append('file') POST /v1/api/posts/images via axios_instance with Authorization Bearer + Content-Type 'multipart/form-data' (axios 1.18 keeps FormData intact and the browser adapter strips it to set the boundary; default application/json would have JSON-serialized the form), errors via toApiError so 503 ApiError preserved. useImageUpload.hook.ts internals rewritten resizeImage→uploadImage; public ImageUploadState contract byte-identical (MarkdownEditor/ThumbnailPicker untouched); signed-URL/direct-PUT removed; 503/Korean degrade + generic catch + finally kept. signImageUpload.api.ts soft-deleted → x_signImageUpload.api.ts (tsconfig already excludes **/x_*). Grep: zero live signImageUpload/TImageSign* imports. Gates: client tsc --noEmit 0, lint --max-warnings 0 = 0 (one prettier wrap autofixed), repo check-types green for all TS workspaces (blog_agent mypy fails pre-existing/unrelated: numpy stub + missing node_modules).
- 2026-06-28 FEAT-phase-F2-blog_agent (F2-T1..T7): blog_agent local CLI image compression (resize→webp) built. New pure util utils/image_transform.py: to_webp(bytes,*,max_long_edge=1600,quality=80)→bytes (Image.open→exif_transpose→fit-inside long-edge 1600 LANCZOS no-enlarge/no-crop→preserve alpha RGBA else RGB→save WEBP q80 method=6, no exif/icc = metadata stripped); NotAnImageError on undecodable input; __all__ exported. uploading_agent.py: top import utils.image_transform; _upload_images thumbnail+body loops force stored_name/s3_key .webp (original_filename kept for markdown match); _upload_file_to_s3 runs to_webp on buffer (NotAnImageError logged+raised), FileMetadata ext=webp content_type=image/webp, missing-file sim-URL → .webp; _save_file_records derives ext from stored_name (→webp/image/webp). image_processing_agent.py resize_for_og keeps 1200x630 letterbox but saves WEBP q80 with .webp out_path. Pillow stays >=10.0.0 (uv sync ok). Tests: __tests__/test_image_transform.py (6: 1600 downscale/no-enlarge/webp magic/alpha/NotAnImage/EXIF-applied+stripped) + __tests__/test_upload_webp.py (3: webp stored_name+s3_key, _replace_content_urls→webp CDN, _save_file_records webp content_type) — 9/9 green (--no-cov). Gates: ruff clean (new+touched files), mypy --python-version 3.12 image_transform.py clean. OPEN: full `uv run pytest` fails repo-wide cov gate (43%<80%) + 10 DB-dependent tests (asyncpg no live DB) — both PRE-EXISTING/unrelated; default mypy bails on numpy stub (py-version env issue, pre-existing).
- 2026-06-28 FEAT-markdown-editor: replaced the plain `<textarea>` in the write/edit screen with @uiw/react-md-editor@4.1.1 (CodeMirror-free, syntax-highlighted source editor + formatting toolbar). Quill rejected — Delta(JSON) model is not markdown-native and would need a lossy bidirectional converter, breaking the existing react-markdown(GFM/KaTeX/Prism) pipeline. New wrapper components/write/MdEditor.client.tsx loads MDEditor via next/dynamic ssr:false (touches browser globals on load) + imports markdown-editor.css (App Router allows component-level CSS); renders preview="edit" so the live preview stays owned by EditorPreview (project's own pipeline, fidelity preserved). MarkdownEditor.client.tsx: data model unchanged (content stays a raw markdown string; autosave/save untouched); IME composition handlers + aria-label + placeholder passed via textareaProps; cursor-aware image insertion now sources the textarea via editorContainerRef.querySelector('textarea') instead of a direct ref (next/dynamic ref-forwarding limitation). Gates: client tsc --noEmit 0, lint --max-warnings 0 (one prettier wrap autofixed). OPEN: runtime/e2e unverified (IME caret behavior inside MDEditor + editor theming/dark-mode follow system, not the app's class-based dark mode) — pending owner verify.
- 2026-06-28 FEAT-markdown-editor-theme: MdEditor.client.tsx now binds `data-color-mode` to next-themes `resolvedTheme` (dark→'dark' else 'light') instead of leaving the editor on its `prefers-color-scheme` CSS fallback — fixes editor/page theme mismatch when the user toggles away from the OS setting (app uses next-themes class-based `.dark`). Safe to read theme here (MDEditor is ssr:false, client-only). Gates: client tsc 0, lint 0.
- 2026-06-28 FEAT-markdown-editor-height: editor pane now matches the preview pane's height. MdEditor height 60vh→100%; editor cell gains `md:h-full` (mobile keeps `h-[60vh]`). The md+ 2-col grid's default row stretch sizes the editor cell to the (content-grown) preview cell, so a long post grows the editor too instead of leaving it stuck at 60vh. Gates: client tsc 0, lint 0.
- 2026-06-28 FEAT-editor-image-paste+layout: write/edit screen now ingests body images via paste/drop in the editor instead of a toolbar button. MarkdownEditor.client.tsx: onPaste/onDrop wired through MdEditor textareaProps (verified @uiw/react-md-editor@4.1.1 does not intercept paste/drop itself, so direct-on-textarea binding is safe); handlers filter image/* files, preventDefault, then reuse the existing useImageUpload pipeline (browser resizeImage 2000px → Express /v1/api/posts/images → sharp webp 1600px → Supabase) — identical 2-step resize, zero backend change. Each upload inserts a unique `![uploading-N]()` placeholder (uploadSeqRef counter) then string-replaces it with the final `![name](url)` (or drops it on failure); multi-file paste/drop processed sequentially. INSERT IMAGE button + its plumbing removed (EditorToolbar props onPickImage/isUploading/isStorageDisabled/imageError/fileInputRef + the button block; MarkdownEditor handleImagePick + fileInputRef); body-image status (uploading/storage-disabled/error) now shown as an inline aria-live line above the editor grid plus an imageHint. Layout: PostSettings flattened from collapsible bordered box to inline labeled fields below the title (removed isOpen/toggle/border + settings.title key); description input gained a DESCRIPTION label (slug/thumbnail labels already existed). i18n: +editor.descriptionLabel +editor.imageHint, -toolbar.insertImage -settings.title (en+ko). Gates: client tsc --noEmit 0, lint --max-warnings 0. OPEN: runtime/e2e paste+drop upload + caret behavior unverified — pending owner verify.
- 2026-06-28 FEAT-editor-category+tags-server (Phase G backend, commit bced84e): single posts.category_id (leaf of a depth-1..3 cascade; post_categories M2M stays unused), category ids STRING end-to-end, category titles + tags lowercase-canonical. @repo/types: TPostInput.categoryId string|null + tags?:string[], new TCategoryNode/TCreateCategoryInput, tree-node ids→string (forced utils/tree.utils Map<string>). New helpers normalizeTags (trim/lowercase/dedupe/≤50/≤20) + syncPostTags (post_tags 2-stmt soft-delete-reconcile) + validateCategoryInput + writeCategory; validatePostInput now requires categoryId on publish (status≠draft) and accepts numeric-string/null; writePost category_id PATCH switched COALESCE→tri-state CASE (clearable). by-id read (GET /posts/:postid) now returns tags + stored_uri (fixes tag/thumbnail edit-seed). New POST /v1/api/categories (parent-scoped uniqueness, soft-delete resurrect; mounted in controllers/index). Migration 0010 UNIQUE NULLS NOT DISTINCT (user_id,parent_id,title) + blog_agent ON CONFLICT update — USER db push DONE. Gates: server tsc 0, jest 123/123 (post-migration), +37 new tests. OPEN: TDraftSummary.categoryId left number|null (small DB IDENTITY ids; flagged).
- 2026-06-28 FEAT-editor-category+tags-client (Phase G client, commit 799929a): useEditorSeed drops Number(category_id) → string passthrough + surfaces tags. New utils/categoryPath (normalizeId/selectChildren/buildPath, cycle-guarded), useCategoryTree + useCreateCategory query hooks, createCategory.api (resolves 409 node as success). CategoryComboBox (Popover+cmdk: filter, "— none", Create "<typed>" only when no exact match) + CategoryCascader (progressive ≤3-box depth cascade, leaf-id controlled, +Add auto-selects). TagInput (chips, Enter/comma, empty-Backspace remove, lowercase/dedupe/≤20/≤50). MarkdownEditor: categoryId+tags state in save AND autosave (thumbnail stays save-only) + publish-required inline guard. PostSettings: collapsible "Post settings" meta area + category/tags slots. en/ko i18n. +string-id migration of CategoryTree/CategorySidebar/categories.mock. Gates: client tsc 0, eslint --max-warnings 0, vitest 20/20, next build OK. OPEN: create-on-409 + cascader auto-open not e2e-covered; runtime verify pending.
- 2026-06-28 VALIDATE-Phase-I-collector (plan+tasks, no code): architect-validated Phase I (stock_collector) plan/tasks against live code contracts — verified TRUE: migration numbering 0010 latest→0011/0012; market_history PK(label,interval,bar_time) no category→LABEL-keyed; market_snapshots PK(category,label)+CHECK 5set; reader history WHERE label=$1; seed.market.sql NOT auto-applied (config.toml sql_paths=["./seed.sql"] only); IStockItem.exchange 'US'|'KRX'; 42-label arithmetic. APPLIED fixes: (G1) market_snapshots has TWO change cols change_absolute+change_percent (both NOT NULL) — was collapsed to single "change" in I1.7/I1.12 + contract blurb; (G2) refresh_snapshots must write ticker+exchange for stocks rows (reader returns them; client union non-null) — was omitted from I1.12 acceptance. NOTES added to plan risks + tasks header: (G3) reader meta read from categoryRows[0] only (per-label as_of divergence cosmetic); (G5) snapshot order=ORDER BY category,seq (sticky additions append, yaml order not display-guaranteed); (G6) Phase 1 daily-only vs sub-minute revalidate_seconds (cache hint, intraday=Phase 3). Edited 00.plan.md (contract block + 3 risk bullets) + 00.tasks.md (I1.7/I1.12 + contract-facts header). Still OPEN unchanged: #32 provider ban-ladder, #9 KRX suffix.
- 2026-06-28 SCOPE-Phase-I-US-only+yahoo-coverage (plan+tasks, no code): USER scoped Phase 1 collector to Yahoo US data. (1) KRX 지수 ^KS11/^KS200/^KQ11 (KOSPI/KOSPI200/KOSDAQ) → Phase 3 (I3.2); watchlist 42→39 (fx4+crypto3+rates4+indices4+stocks24). NOTE: Yahoo DOES serve these 3 spot indices (low-risk); deferred by USER scope not coverage — re-add = watchlist.yaml only (39→42). What Yahoo truly lacks (Phase 3): KOSPI200 futures, KOSDAQ150, FRED US 1M/1Y yields. (2) Yahoo-coverage rule (USER): per-symbol fetch → write-if-present / graceful-skip-if-absent (no row, no run abort; daily+weekly partial allowed) — added to I1.13 run_daily + contract headers; I1.12/I1.16 acceptance softened to "Yahoo-covered ≤39". (3) exchange union 'US'|'KRX' kept in type/mapping (I1.8) but Phase 1 always 'US' (KRX dormant). Edited 00.plan.md (seed indices, 39-count ×3, exchange note, Phase3 deferred, Decision#31, data-flow Yahoo-rule) + 00.tasks.md (header, locked-decisions, I1.4/I1.8/I1.12/I1.13/I1.16/I3.2, build-order). check:schema unaffected (no migration touched).
- 2026-06-28 FEAT-home-global-feed (Phase H precursor): root `/` now renders the all-authors global blog feed (BlogMainPage, username='') instead of the TerminalProfile portfolio page; force-dynamic so next build does not fetch the backend at prerender. app/page.tsx swapped MainPage→app/blog/components/BlogMainPage with empty-username ALL_AUTHORS; app/api/posts/load-more/route.ts drops the `username required` 400 guard so an empty username falls through to the all-authors feed (GET /v1/api/posts), a real username scopes to /v1/api/posts/users/:u. TerminalProfile kept in app/MainPage.tsx. Sets up the P1 shell move (app/page.tsx → app/(feed)/page.tsx). Gates: client tsc 0, lint --max-warnings 0=0.
- 2026-06-28 DOCS-Phase-H-P0-done+UX-fold (PM, plan+tasks, no code): marked Phase H P0 DONE (H0.1–H0.6 = commit 93262e6: 4 tag-read endpoints + route-resolution jest; server 22 suites/145 tests, +15 new) and folded the UX-review fixes into the P1–P5 tasks. tasks.md: P0 status blockquote + 6 checkboxes [x]; line-4 Active → "Phase H IN PROGRESS / P0 DONE"; build-order "next shippable=H1.1". UX folds — Q1 <lg per-user "Categories/About this blog" sheet (H1.2/H5.1/H5.5), P2.4 cookie-based collapse first-paint (H1.2/H1.4/H4.4), P1.1 recents-empty→popular-tags fallback (H5.3), P1.2 comment/like counts on cards (H2.2/H5.3), P2.3 tag-miss empty state + SearchBar placeholder (H2.1/H3.1), P2.5 skeletons + rail error fallback (H2.1/H4.3/H5.5), P2.6 new H2.3 post-detail tag chips, P2.7 focus-trap/aria (H4.4/H1.2). +2 follow-ups HX.1 (client TPost re-derive decision) / HX.2 (posts.ts 797 LOC split). plan.md: Decision #30 addendum (Q1 mobile sheet + Q2 keep system theme/semantic tokens). Phase I (I1.x–I3.x) + Decisions #31/#32 untouched. check:schema unaffected (no migration).
- 2026-06-28 SCOPE-Phase-I-readd-KRX-spot (plan+tasks, no code): USER approved re-adding KRX spot indices ^KS11(KOSPI)/^KS200(KOSPI200)/^KQ11(KOSDAQ) per Yahoo-coverage rule (Yahoo serves them). watchlist 39→42 (fx4+crypto3+rates4+indices7+stocks24). Phase 3 (I3.2) now only KOSPI200 futures + KOSDAQ150 (genuinely Yahoo-missing). exchange union 'US'|'KRX': KRX spot indices live in `indices` category (no exchange field) so unaffected — Phase 1 stocks still always 'US'; 'KRX' exchange activates only on KRX individual equities (.KS/.KQ, Phase 3). Yahoo-coverage graceful-skip rule unchanged. Edited 00.plan.md (seed indices, 42-count, exchange note, Phase3, Decision#31, settings line) + 00.tasks.md (header, locked-decisions, I1.4/I1.8/I1.12/I1.16/I3.2, build-order). No migration touched; check:schema unaffected.
- 2026-06-28 FEAT-Phase-H-P1–P5 (commit f83a38e, code): built the Reddit-style multi-user blog shell + context rails on top of P0's tag-read APIs, via 5 sequential per-phase developer agents (shared working tree → serialized to avoid file races; Explore-mapped first). P1 shell: `app/(feed)` route group (homepage git-mv'd, URL `/` unchanged), `components/shell/BlogShell.server.tsx` (4 slots topbar/leftRail/main/rightRail/userContextSheet, reads `railCollapsed` cookie for first-paint width), `store/layout.atom.ts` (`isLeftRailCollapsedAtom` + `parseRailCollapsedCookie`), `/blog`→`/` redirect; `BlogContentShell` soft-deleted→x_. P2 tags: `app/(feed)/tags/[tag]/page.tsx` global tag feed (STRING cursor end-to-end, Suspense skeleton, tag-miss empty state) + `getPostsByTag.api` (single `.data` — axios interceptor already unwraps) + load-more `tag` param; `RecentPostCard` tag chips→Links (nested-anchor-guarded) + comment/like counts; post-detail tag chips. P3 topbar: `SearchBar`(tag-nav→`/tags/[q]`, "Jump to a tag…")+`CreateButton`(→`/write`) composed into the SINGLE global nav (no double bar) + `buildPrimaryNavLinks`/`usePrimaryNavLinks` (Home/Explore[disabled]/Write/Saved/My-blog, My-blog hidden when not onboarded). P4 left rail: `LeftRail`+`RailSection`+`PrimaryNav`(active highlight)+`TagCloud.server`(global, `getPopularTags.api`, Suspense skeleton + try/catch inline fallback), cookie-collapse toggle (hydration-safe via `useHydrateAtoms`), `MenuDrawer` focus-trap+aria; wired into both layouts identically via `LeftRailContainer.server`. P5 right rail: `useRecents`/`useSavedTags` localStorage (caps 20/50, STRING-id move-to-front dedup, case-insensitive tags; pure reducers in utils/ + vitest), `RecordRecentView` on post detail, `RecentPostsWidget` (counts + popular-tags fallback when empty), per-user `UserProfileCard.server`+`UserContextRail.server`+scoped `TagCloud username?` (chips→global `/tags/<tag>`)+`UserContextSheet` (`<lg` Q1 disclosure), left-rail `RecentVisited`+`SavedTags`; `RightTechStackCard` soft-deleted→x_. BACKEND (mine, surgical Edit): new `GET /v1/api/posts/users/:username/profile`→`{username,displayName,avatarUrl,postCount}` (`helpers/userProfile.ts`, postCount=COUNT(DISTINCT public slug); 404 unknown) + `TUserProfile` in @repo/types + live-DB jest. FULL-SUITE (one run at end per USER): client tsc 0 / lint --max-warnings 0=0 / vitest 8 files 43 tests / build OK; server tsc 0 / build 0 / jest 23 suites 147 tests (live DB, +1 suite/+2 = profile); @repo/types builds; check:schema in sync. OPEN: P6 DEFERRED (search API + /explore); HX.1 (client TPost re-derive) + HX.2 (posts.ts 797 LOC split) non-blocking.
- 2026-06-28 DOCS-Phase-H-P1–P5-done (plan+tasks, no code): flipped H1.1–H5.6 (20 tasks) → [x]; line-4 Active → "Phase H P0–P5 SHIPPED"; build-order note "P0–P5 SHIPPED"; H5.5 annotated with the F9 profile-count decision (dedicated `/profile` endpoint, not a feed-query COUNT); plan.md Decision #30 addendum status → "P0–P5 SHIPPED (commit f83a38e)". P6/HX stay [ ]. Phase I lines (concurrent session) untouched.
- 2026-06-29 FEAT-post-toc-in-right-rail (client, owner request): moved the post-detail Table of Contents from a side column next to the article INTO the per-user RIGHT rail, BETWEEN the profile card and the category section. The right rail lives in the shared `app/blog/[username]/layout.tsx` (no slug access), so a Jotai bridge carries the post-specific TOC: new `store/toc.atom.ts` (`tocItemsAtom`); `PostContentToc.server` still server-extracts via `extractTocParsed` (correct rehype-slug anchors) but now renders an invisible `TocPublisher.client` that sets the atom (and clears on unmount → no TOC on the author feed); new `RightRailToc.client` reads the atom and renders the existing `TableOfContents.client` (returns null when empty). `UserContextRail.server` inserts `<RightRailToc/>` between `UserProfileCard` and the category `RailSection` (so it also appears in the `<lg` UserContextSheet). `PostContent.server` drops the `flex gap-6` TOC column → full-width reading column; orphaned `PostTocSkeleton` soft-deleted→x_. NOTE: TOC populates client-side post-hydration (right rail is in the layout, can't SSR the slug-specific TOC) — minor appear-after-mount, no layout break. Pre-existing dead `TableOfContents.server.tsx` left as-is (not introduced here). Gates: client tsc 0 / lint --max-warnings 0=0 / vitest 8 files 43 / build OK.
- 2026-06-29 FIX-rail-collapse-and-tag-count (client+server, owner bug report on `/`): TWO bugs. (1) Left-rail collapse toggle didn't shrink the rail WIDTH — the width-bearing `<aside>` lived in the SERVER `BlogShell.server.tsx` (w-64/w-14 chosen once from the `railCollapsed` cookie at SSR), so flipping `isLeftRailCollapsedAtom` only re-rendered the rail CONTENTS while the outer width stayed put until a refresh. Fix: new client `components/shell/LeftRailAside.client.tsx` subscribes to the atom (seeded via `useHydrateAtoms(initialCollapsed)` from the cookie → no width flash) and owns the w-64/w-14 + `transition-[width]`; BlogShell now renders `<LeftRailAside>` instead of the static `<aside>` (dropped now-unused `cn` import). (2) Popular-tag chip count > posts shown on `/tags/[tag]`: count was `COUNT(*)` of post_tags rows (helpers/popularTags.ts) but the tag feed dedupes revisions by slug (`ROW_NUMBER() PARTITION BY slug`, rn=1), so N public revisions of one slug counted as N yet rendered 1 card. Fix: both global + scoped queries → `COUNT(DISTINCT p.slug)`; global query also gains the `JOIN users … u.deleted_at IS NULL` the feed already had (exclude deleted authors). Added revision-dedup regression test (2 public revisions of one slug + shared tag → count 1). Gates: server tsc 0 / jest 23 suites 147 tests (live DB) / client tsc 0.
- 2026-06-29 FIX-blog-chrome-consistency+restore-about (client, owner UI report): THREE fixes for `/` vs `/blog/[username]`. (1) `/blog/[username]` had a top/side gap `/` lacked — `app/blog/layout.tsx` wrapped the whole subtree in `min-h-screen … p-2 sm:p-4 md:p-8`, layering padding on top of the full-bleed BlogShell (which `/` reaches directly via the `(feed)` group). Stripped that wrapper to `<div className="font-mono">{children}</div>` (kept the mono aesthetic + ScrollToTopButton); BlogShell now controls layout identically on both. (2) Category in the per-user right rail read as an accordion: it was the ONLY rail block wrapped in a `RailSection` disclosure (chevron collapse), unlike the TOC which renders its Card directly. Removed the `RailSection` wrapper in `UserContextRail.server.tsx` so `CategorySidebar` (already styled to match the TOC card) shows as a persistent table-of-contents-style list; applies to both the ≥lg right rail and the <lg UserContextSheet. (3) `/about` was gone: Phase H repurposed `/` as the blog feed, orphaning `app/MainPage.tsx` (TerminalProfile = career: experience/projects/open-source/skills/education) and leaving nav "About" → `/` (the feed). Added `app/about/page.tsx` rendering TerminalProfile + repointed `buildNavLinks` About `/`→`/about` (reachable from desktop TopBar, mobile bar, and MenuDrawer — all use `useNavLinks`). Gates: client tsc 0 / lint --max-warnings 0=0 / build OK (`/about` static route emitted).
- 2026-06-29 FIX-mobile-content-padding-consistency (client, owner mobile report): `/` content column was padded MORE than `/blog/[username]` — `app/(feed)/page.tsx` wrapped BlogMainPage in an extra `<section className="mx-auto w-full max-w-3xl p-4">` ON TOP of BlogShell's `<main>` column wrapper (which already centers + width-caps + pads), so `/` double-padded while the author feed (page renders BlogMainPage directly) did not. Two changes for one consistent source of truth: (1) `(feed)/page.tsx` now renders `<BlogMainPage username={ALL_AUTHORS} />` directly — identical structure to `[username]/page.tsx` (`main > div > section`); (2) BlogShell's content `<div>` padding `p-4` → `p-2 sm:p-4` (8px mobile, 16px ≥sm) so both routes share an 8px mobile inset. Gates: client tsc 0 / lint 0 / build OK (17/17 static).
- 2026-06-29 FIX-font-consistency+wire-ibm-plex-mono (client, owner report `/` vs `/blog`): `/` rendered in system sans while `/blog/[username]` rendered mono — only `/blog/*` passed through `app/blog/layout.tsx`'s `font-mono` wrapper; `/` (the `(feed)` group) never did. Moved `font-mono` onto the shared `BlogShell` root (covers `/`, `/blog/*`, `/tags/*`) and reduced `app/blog/layout.tsx` to a pass-through (`<>{children}<ScrollToTopButton/></>`). Separately, next/font's IBM Plex Mono was DEAD: `app/layout.tsx` puts `ibmPlexMono.variable` on `<html>` (declares `--font-ibm-plex-mono`) but nothing consumed it (`ibmPlexMono.className` on body is commented; Tailwind `--font-mono` unmapped), so `font-mono` fell back to the system monospace stack. Wired it in `styles/globals.css` `@theme inline`: `--font-mono: var(--font-ibm-plex-mono), …` — MUST be `@theme inline` (not plain `@theme`) so the runtime var next/font injects on `<html>` resolves at point-of-use (per the official next/font + Tailwind v4 example; verified by a cited web-research agent). Net: `/`, `/blog/*`, `/tags/*` now all render the real IBM Plex Mono consistently. Gates: client tsc 0 / lint 0 / build OK (17/17 static). NOTE: did NOT set `--font-sans` (would change the SITE-WIDE default body font incl. dashboard) — left as an explicit follow-up choice.
- 2026-06-29 FIX-about-ascii-banner + add-font-sans-comment (client): after wiring `font-mono`→IBM Plex Mono, the `/about` "TAEK LIM" ASCII banner (`<pre>` box-drawing `╔═╗║█` in app/MainPage.tsx) broke — IBM Plex Mono's box-drawing glyphs don't fill the cell, leaving gaps. Pinned that one `<pre>` back to a system monospace stack via inline `fontFamily` + `leading-none` (vertical bars connect); rest of /about keeps IBM Plex Mono. Also added a DISABLED (commented) `@theme inline { --font-sans: var(--font-ibm-plex-mono), … }` section in globals.css as an opt-in site-wide-default-font toggle for the owner to test later (would make nav/dashboard/resume/about default to mono too). Gates: client tsc 0 / lint 0 / build OK.
- 2026-06-29 FEAT-about-left-rail (client, owner request): added the SAME global left nav rail `/` has to `/about`. New `app/about/layout.tsx` reuses BlogShell's rail primitives (LeftRailAside reading the railCollapsed cookie + LeftRailContainer) but NOT BlogShell itself — BlogShell caps main at max-w-3xl, whereas the portfolio (TerminalProfile) is wide and brings its own container, so main is full-width. Top bar still from root CustomNavigation; right rail omitted. `/about` flips static→dynamic (ƒ) since the layout reads cookies. Gates: client tsc 0 / lint 0 / build OK (/about = ƒ).
- 2026-06-29 FEAT-category-title-filter (B, server+client, owner request): per-author feed can now be filtered by category TITLE via `/blog/@<username>?category_title=<title>`. Filter is by TITLE not id ON PURPOSE — since migration 0010 the unique is (user_id, parent_id, title), so a title repeats across parents; matching by title MERGES same-titled categories ("all posts under this category name" for that author). No subcategory recursion (YAGNI). BACKEND: `controllers/posts/posts.ts` /users/:username feed gained optional `category_title` query → bound as $5 with `AND ($5::text IS NULL OR c.title = $5)` in the ranked_posts CTE (LEFT JOIN means uncategorized posts correctly excluded when a title is given); new mocked-pool route test (4 cases) asserts the $5 wiring. FRONTEND chain threaded categoryTitle end-to-end: getPosts.api (sends category_title only when username present) → loadMorePosts action → load-more route handler (keeps filter across pagination) → RecentPostsList.server → RecentPostsList.client (load-more fetch carries &category_title, added to effect deps) → BlogMainPage → `[username]/page.tsx` reads searchParams.category_title + renders an active-filter chip with a "clear ✕" link back to /blog/@user. Card category badge now links to `?category_title=<encoded title>` (was author-root in the A step). Gates: server tsc 0 / jest 24 suites 151 tests; client tsc 0 / lint 0 / build OK (/blog/[username] ƒ).
- 2026-06-29 FEAT-category-breadcrumb-path (server+client, owner request): post cards now show the FULL root→leaf category path (e.g. 투자 › 반도체 › 메모리) instead of just the leaf title, each segment a clickable filter link (?category_title=<segment>) so any ancestor is browseable. Applies to ALL card feeds (global `/`, author, tag). BACKEND: shared `RECURSIVE cat_path` CTE (title::text cast in BOTH terms — varchar(100)[] vs varchar[] mismatch caught by live-DB tests) added to the 3 feed queries (controllers/posts.ts global + /users/:username, helpers/postsByTag.ts tag feed), LEFT JOINed on posts.category_id to return `category_path` text[]; a soft-deleted ancestor breaks the chain → NULL path (card falls back to single `category`). TYPES: `category_path?: string[]|null` on @repo/types TFeedPost + client TPost. CARD (RecentPostCard): renders breadcrumb via flatMap (chip + `›` separator, z-10 above overlay link, keyed, no Fragment); falls back to `[category]` then nothing. Mocked-pool route test asserts the cat_path CTE + category_path wiring. Gates: @repo/types rebuilt; server tsc 0 / jest 24 suites 152 tests (live DB exercised the recursive path); client tsc 0 / lint 0 / build OK.
- 2026-06-29 FIX-category-ancestor-filter + card-actions-inline (server+client, owner report): (1) Category filter only worked for the LEAF (depth-3) — clicking an ANCESTOR breadcrumb segment (depth 1/2) returned nothing because the filter matched `c.title = $5` (the post's single leaf category) and posts live in deep leaves. Changed the /users/:username filter to `$5 = ANY(cpath.path)` — match the title ANYWHERE in the category path, so an ancestor click returns that category AND all descendants (free, the path array is already computed). Also hardened both cat_path CTEs: root = `parent_id IS NULL OR parent_id = id` (self-referencing roots exist per buildCategoryTree) with a `c.id <> c.parent_id` recursive guard against self-loops. (2) Card engagement-footer action buttons collapsed into a "more" dropdown on mobile; added `forceInline` to PostActions and set it on the card so Share/Save/Like always render inline (the footer is a full-width row), matching desktop. Gates: server tsc 0 / jest 24 suites 152 (live DB exercised the new CTE+filter); client tsc 0 / build OK. NOTE: client `lint --max-warnings 0` currently fails on app/sitemap.ts (other session's file: unused SITE_URL + 1 prettier) — NOT in this change; left for that session.
- 2026-06-29 FEAT-Phase-I-stock-collector (apps/stock_collector, ENG built, NOT committed): autonomous build of the Yahoo OHLC collector feeding the existing dashboard (market_snapshots/market_history). SIMPLE COPY of lib+yahoo_client from /Users/tio/Documents/00_projects/2025_stock_trading_data (massive/sec13f/alpaca excluded); polyglot sibling (no package.json → pnpm non-member); uv+py3.12, deps synced (yfinance/psycopg/pyarrow). Layout refinement (architect): all Python under src/ with single import root collector.{config,schema,transform,ranking,sink,entrypoints}; configs/watchlist.yaml a root data file. Functional core pure (transform/+ranking/ stdlib-only, ZERO network/DB); psycopg isolated to sink/db_sink.py ONLY. Files ≤300 LOC, soft-delete only.
- 2026-06-29 Phase I — Phase 1 DONE(ENG): watchlist.yaml 42 labels (fx4+crypto3+rates4+indices7+stocks24, globally unique, FX source-native, KRX SPOT indices included); pure ohlc/snapshot(2 change cols change_absolute+change_percent + stocks ticker/exchange)/mapping/fx_direction/universe_resolve; watchlist_loader (validates label-uniqueness + fx labels source-native via fx_label + category/exchange rules); db_sink load_history+refresh_snapshots (ON CONFLICT UPSERT, executemany) + read_latest_bar/read_recent_history/read_tracked_symbols (to_regclass-guarded → [] when absent); run_daily (universe=yaml∪tracked, incremental from_date, Yahoo-coverage graceful-skip); CI collector-daily.yml(cron 30 21 *-1-5, session-pooler DATABASE_URL, if:failure() issue) + collector-keepalive.yml; seed.market.sql FX label 'KRW/USD'→'USD/KRW' (251 occ). LIVE Yahoo smoke: 14/14 symbols across fx/crypto/rates/indices returned real bars.
- 2026-06-29 Phase I — Phase 2 DONE(ENG): migrations 0011_tracked_symbols (PK symbol, UNIQUE label, category CHECK, deleted_at soft-delete, ON CONFLICT revive) + 0012_market_rankings (PK as_of,symbol,scope, scope CHECK, append-only) — mirrored byte-identical to BOTH schema SSOT copies, check:schema green; pure ranking/screen (COMPLETE ranking, top-20/top-10 = query-time surfacing depth not storage truncation — PM+architect decision) + ranking/universe (assemble dedupe + BRK.B/BRK-B normalize); db_sink upsert_tracked_symbols(sticky revive, never auto-delete)+insert_rankings; run_weekly (assemble S&P500 CSV → fast_info marketcap/sector → rank → sticky tracked top20/sector-top10 via resolve_exchange → rankings append; partial allowed) + collector-weekly.yml (cron 0 6 * * 0, DATABASE_URL only, no MASSIVE).
- 2026-06-29 Phase I — Phase 1.5 PARTIAL: parquet_sink (pyarrow zstd, date32, read-merge-rewrite dedupe keep-last, dividends/stock_splits, atomic os.replace) + spec. NOT wired into an entrypoint yet; Storage upload step (I1.5.2) + PRIVATE bucket (I1.5.1b) + daily dividends/splits capture (I1.5.0) = USER-gated/follow-ups.
- 2026-06-29 Phase I VALIDATION: system-architect validated all 7 checks PASS (layering/purity, contract vs reader+0004, migrations dual-mirror, sticky UPSERT, graceful-skip, 42-label watchlist, SRP/LOC/docstrings). Fixed the one no-dead-code finding: wired fx_label (loader fx-label source-native validation) + resolve_exchange (run_weekly tracked exchange). Final gate: 51 pytest pass, check:schema in sync, 16 modules import, psycopg sink-only, all ≤300 LOC. Architect doc-flags: (a) parquet_sink not yet wired to an entrypoint; (b) collector assumes UTC DB session (Supabase default OK) to avoid date off-by-one; (c) tests use spec_*.py (pyproject) vs python.md test_*.py — intentional divergence.
- 2026-06-29 Phase I OPEN USER gates: live DB write + dashboard real-data verify (I1.16, needs DATABASE_URL session-pooler); GH Actions Secrets DATABASE_URL on both workflows; supabase db push 0011+0012 (re-confirm next free number first); PRIVATE parquet Storage bucket + SUPABASE_SERVICE_ROLE; DASHBOARD_DATA_SOURCE=backend flip (shared Phase B B4). Code not yet committed (on branch feature/multi-user-blog working tree). Follow-ups (ENG): I2.8 market_cap caching optimization (currently fast_info direct), I1.5.0/I1.5.2 parquet wiring+upload, setup-uv SHA pin (workflows use official installer).
- 2026-06-29 Phase I COMMITTED — apps/stock_collector + migrations 0011/0012 + 3 collector workflows landed on main as 56b8be5 (supersedes the earlier "NOT committed / feature/multi-user-blog working tree" qualifier in the Phase I lines). Also applied a hardening: db_sink.connect() now pins session timezone=UTC (makes the bar_time TIMESTAMPTZ ← bare-date / MAX(bar_time)::date cursor / UTC reader invariant explicit). Architect+ml-engineer+PM validation: implementation/migration PASS, 51/51 pytest, schema SSOT byte-identical, zero numerical bugs.
- 2026-06-29 docs: added `.claude/rules/docs.md` (archive lifecycle + read-guard); collapsed shipped phases to DONE stubs in 00.plan.md (A/B/D/E/E2/F/F2/G/H + Decisions Log) + 00.tasks.md (Repo Hygiene/Phase 1/Cycle 2/A/blog_agent/B/D/E/E2/F/F2/G/H), full detail → `_docs/archive/*`; dropped stale B5.5 + UX12b-persist open items; fixed Phase I "NOT committed"→committed 56b8be5; card-actions plan → archive (handoff doc-sync.shipped-not-archived).
- 2026-06-29 Phase I ENG follow-ups LIVE (commits e2a5e5c/12b8a80/823a5e4/f971661): setup-uv SHA pin v8.2.0 + checkout v7.0.0 SHA (all workflows + ci.yml); parquet archive wired into run_daily (--archive/COLLECTOR_ARCHIVE_PARQUET) capturing adj_close/dividends/splits from same Yahoo frame; new seed_archive entrypoint + collector-seed-archive.yml (full-history backfill); I2.8 migration 0013 symbol_fundamentals APPLIED (+mirrors, check:schema green) + pure ranking/fundamentals + db_sink table-guarded cache + run_weekly shares×close derivation; settings accept POSTGRES_URL_NON_POOLING + standardize SUPABASE_SECRET_KEY (sb_secret_, dropped legacy service_role). BUG fixed: Storage upload needs apikey header for sb_secret_ keys (Bearer-only→400). LIVE verify: daily run universe=42/9870 bars/42 snapshots/skipped=0; seed 11636 rows→83 objects uploaded to PRIVATE market-archive. GH secrets DATABASE_URL/SUPABASE_URL/SUPABASE_SECRET_KEY + vars COLLECTOR_ARCHIVE_PARQUET=1/COLLECTOR_PARQUET_BUCKET set. Remaining USER gate: DASHBOARD_DATA_SOURCE=backend flip. Ref doc _docs/supabase-db-connection-and-ci.md (merged the IPv4/IPv6 doc).
- 2026-06-29 FEAT-scrolltop-global (client, owner request): ScrollToTopButton promoted to a SINGLE global mount in app/layout.tsx (after {children}) so EVERY route incl. `/` shows it — was only `/blog/*` (via blog/layout.tsx) + MainPage/ResumePage/resume. Removed the 4 duplicate mounts; app/blog/layout.tsx reduced to pure pass-through `<>{children}</>`. threshold=300 unchanged (self-hides short pages); ScrollToTop.tsx is the only def + layout.tsx the only mount. Gate: client tsc 0.
- 2026-06-29 PLAN-Phase-2-mobile (architect+PM, owner request): mobile context-UX phase scoped into 00.plan/00.tasks as "Phase 2-mobile" (11 atomic tasks, client-only, no backend/schema). Option B = lg:hidden context header ABOVE the feed on list pages (author chip + horizontal-scroll category/tag LINK row, edge-fade, ≥44px, NOT role=tab) reusing existing fetchers via new components/rail/mobile/* + additive BlogShell `mobileTopContext` slot. Option D = lg:hidden collapsible 목차/Contents under the post title reusing tocItemsAtom + Radix Collapsible (research/NN-g: sticky/floating TOC gets missed → accordion). Decisions Log #33 LOCKED (PM+architect, not escalated): M1 chip row REPLACES list-page userContextSheet (soft-unwire both list layouts, keep UserContextSheet.client.tsx file); M2 drop duplicate RightRailToc from the mobile sheet so MobileToc = sole mobile TOC; desktop ≥lg rail untouched. NOT YET IMPLEMENTED (developer dispatch pending: MOB.0 i18n → D MOB.1a-d → B MOB.2a-f → gate MOB.3).
- 2026-06-29 Phase 2-mobile IMPLEMENTED (client, MOB.0–MOB.3): i18n blog.toc.heading/blog.mobile.* both locales; Option D new MobileToc.client.tsx (Radix Collapsible over tocItemsAtom, null on empty) mounted lg:hidden atop PostContentCard CardContent; Option B new components/rail/mobile/{getMobileChips.server.ts,MobileAuthorChip,MobileChipRow,MobileContextHeader}.server.tsx + additive BlogShell mobileTopContext slot; both list layouts wired mobileTopContext + userContextSheet UNWIRED (M1; UserContextSheet.client.tsx kept). MOB.1d (M2) satisfied by the shared [username]/layout sheet-unwire — removes the duplicate mobile RightRailToc, desktop >=lg rail byte-equiv (no UserContextRail change). Gate: check-types exit 0, lint exit 0.
- 2026-06-29 FEAT-scrolltop-show-on-scrollup (client, owner request): ScrollToTopButton promoted global mount + now reveals on scroll-up (not only past a fixed threshold); rAF-throttled scroll listener + a small jitter guard so micro-scrolls don't flicker the button. commits 1feacc9 / 65ae70a / 043488a / 4708486.
- 2026-06-29 FIX-mobile-content-padding (client, owner mobile report): mobile content inset bumped to 16px `p-4` for the feed/post column. commit ee8cacf.
- 2026-06-29 FIX-mobile-chip-row-clip (client): mobile chip row content-clipped so the horizontal-scroll row no longer bleeds past the content column. commit eff3286.
- 2026-06-29 FEAT-mobile-toc-above-post-card (client, owner request): mobile TOC moved ABOVE the post card (was inside CardContent). commit 884754e.
- 2026-06-29 FEAT-Phase-2-filter (server+client, multi-category + multi-tag feed filter on /blog/[username], all breakpoints): commits a05a299/000a564/6db0c68 (backend+core) + c132024/e20e329/1243df4/4e8483b (frontend). Semantics same-type OR / cross-type AND — categories array OVERLAP `cpath.path && $5::text[]`, tags correlated `EXISTS … = ANY($6)` (NOT INNER JOIN, no migration); URL plural `?categories=a,b&tags=a,b` (cap FILTER_MAX=20); legacy `?category_title=` folded into categories at parse (client core + Express safety net). Pure core utils/filter-state.ts (no orderSelectedFirst — F4 owner override = highlight in place, no sort-to-front). F5 = desktop ≥lg RAILS are the filter UI (mode:'filter' prop on CategorySidebar/CategoryTree+TagCloud) + mobile <lg FilterChipRow rows + AppliedFilters summary bar both breakpoints (NOT a FeedFilterBar). F6 = filter chips are zero-JS `<Link>` reading useSearchParams() (FilterChip.client), active via aria-current (not buttons). F.11 cache = Cache-Control on the EXPRESS RESPONSE (anon public s-maxage=3600 SWR=86400; authed private no-store), revalidateTag N/A. NEW utils/filter-state.ts + helpers/parseFeedFacet.ts + components/blog/filter/{FilterChip,FilterChipRow,AppliedFilters}.client + getFilterItems.server + jest posts.feed-filter.test; x_-retired MobileChipRow/getMobileChips. Gate GREEN: client check-types+lint 0/0; dashboard_server jest 25 suites/166 tests. Decisions Log #34.
- 2026-06-29 VALIDATE-Phase-2-filter (architect verify, ships-clean): 9-dim PASS (SQL OR/AND+short-circuit+EXISTS row-integrity+injection-safe, draft-leak boundary = req.auth branch covers `OR p.user_id=$4` visibility, remount-key/deps both facets, x_ retirement fully unwired); aria-current-on-`<Link>` web-verified CORRECT (not an aria-pressed smell). Fixed 2 Low: (#1) client parseFilterState + load-more route now use category_title PRECEDENCE (length-based, mirrors Express `??`) instead of merge → SSR/API lock-step; (#3) wired dead i18n addCategory/addTag(+remove) keys as FilterChip aria-label. commits TBD; client check-types+lint 0/0.
- 2026-06-29 FEAT-Phase-2-fetch (server+client, feed data layer axios→native fetch + Next 16 tag revalidation): commits 3563467/bd00a72/8955f33/731250e. NEW apis/buildFeedQuery.ts(pure) + apis/http.server.ts(`server-only` serverRequest<T>: auth header/JSON/FormData/`AbortSignal.timeout(5s)`/`!res.ok`→ApiError via toFetchApiError/no-store-omits-next guard) + apis/getPosts.fetch.server.ts(getFeed: anon force-cache+tags[feed:global,feed:<u>]+revalidate:60 / owner token→no-store, token via Clerk auth().getToken() outside cache) + app/actions/postPublish.action.ts(updateTag on publish/unpublish/delete/admin). RecentPostsList.server calls getFeed directly. Express anon s-maxage 3600→60 backstop. Rewired useDeletePost/useSetAdminPostStatus/editor-publish; x_-retired deletePost.api/setAdminPostStatus.api; drafts/autosave/create + loadMorePosts + load-more route + sitemap stay on axios. Cache Components NOT enabled (legacy tagged-fetch). Gate GREEN: client check-types+lint 0/0; dashboard_server jest 25/166. Decisions Log #35. OPEN: F-fetch.1 admin-delete revalidate, F-fetch.2 client-axios retirement.
- 2026-06-29 F-fetch.1 DONE (CLOSED): admin delete now `deleteAdminPostAction` (mirrors `deletePostAction` → `DELETE /v1/api/admin/posts/:id` + `revalidateFeed(username)`); new `useDeleteAdminPostAction` hook also invalidates `adminPostsQueryKey`; `AdminPostsTable` passes `{postId, username: post.username ?? ''}`; `x_`-retired old `useDeleteAdminPost.query.client`/`deleteAdminPost.api`. Gate: client check-types+lint 0/0. F-fetch.2 (client-axios retirement) stays DEFERRED.
- 2026-06-29 FEAT-error-logging (client+ssr+express, owner request — browser/SSR errors → Supabase): DECIDED arch = browser error boundary → Next Route Handler `app/api/log-error` → server-to-server `serverRequest` → Express `POST /v1/api/error-logs` → `error_log` (Next SSR keeps NO DB creds per A-1; Express stays sole Supabase owner; browser never sees Express). NEW migration `0014_error_logs.sql` (`error_log`: digest/message/stack/path/user_agent/source/created_at; NO `deleted_at` — owner-approved OPERATIONAL-LOG exception to soft-delete; pg_cron 7-day TTL cleanup shipped COMMENTED-OUT, enabling pg_cron = USER/Supabase-dashboard gate). NEW express helper `errorLog.ts` (pure `normalizeErrorLog`: coerce+truncate 2000/8000/1000/500, default source='browser') + `controllers/errorLogs` (public, 204/500, never throws) + jest 5/5. NEW Next `app/api/log-error/route.ts` (forwards, always 204) + `utils/reportError.util.ts` (fire-and-forget keepalive fetch, never throws) + `app/global-error.tsx`; `app/error.tsx` now also reports. Gate GREEN: client check-types 0, dashboard_server check-types+build 0, jest errorLog 5/5. USER gates: apply 0014 + enable pg_cron schedule.
- 2026-06-29 FIX-feed-envelope (prod RSC 500, digest 650637976 — "Cannot read properties of undefined (reading 'length')" in RecentPostsList.server `posts.length`): ROOT CAUSE = Express feed read endpoints return `{ success, data:{ posts, nextCursor, hasMore } }` envelope; legacy axios path unwrapped TWICE (response-interceptor→body + explicit `.data`), but new `getFeed`(native fetch) didn't unwrap → `posts` undefined. FIX: `getPosts.fetch.server.ts` adds `TFeedEnvelope` + `unwrapFeed()` (returns `.data`, defaults `posts:[]/nextCursor:null/hasMore:false`) applied to BOTH owner(no-store) + anon(tagged) branches → can never `.length` undefined again. Affects global + per-author feeds. Gate: client check-types 0 / lint 0. NOTE: leftover `console.log('>> getCategoriesByUsername')` in getCategories.api.ts is prod noise (separate cleanup). UNCOMMITTED — needs commit+deploy to land the prod hotfix.
- 2026-06-29 UX-mobile-category (owner request — `/blog/[username]` mobile/medium <lg lost the right-rail category section): NEW `MobileCategory.client.tsx` (Collapsible shell mirroring `MobileToc`, collapsed-by-default, chevron trigger); `CategorySidebar`+`CategorySidebarWrapper` gain `bare` prop (tree only, no Card chrome) for nesting in the accordion; list `page.tsx` swaps the category `FilterChipRow` for `<MobileCategory title={t.categories}><CategorySidebarWrapper mode=filter bare/></MobileCategory>` (tags stay a chip row), drops now-unused `getCategoryFilterItems`/`categoryItems`. Symmetry: list=Categories accordion, detail=Contents(TOC) accordion. Gate: client check-types 0.
- 2026-06-29 USER: error-logging migration `0014_error_logs.sql` APPLIED to Supabase (error_log persists; browser/SSR→Express→Supabase pipeline now live end-to-end). pg_cron 7-day TTL = optional follow-up (still commented).
- 2026-06-29 FEAT-explore-nav-placeholder (client, owner request — trailing nav slot read empty): added a DISABLED `/explore` PrimaryNav item (Compass icon, "coming soon", non-clickable muted `<span>`) via `useNavLinks.hook.ts` `buildPrimaryNavLinks` `disabled:true`; `nav.explore` already in both locales; PrimaryNav + MenuDrawer already render `disabled`. Real P6 search/discovery stays parked. Gate: client check-types 0 / lint 0 / vitest useNavLinks 7/7.
- 2026-06-29 docs: demoted to NOTES (not open tasks) per owner — F-fetch.2 (client axios retirement), P6 (real search/`/explore`; disabled nav placeholder now live), Cloudflare `CDN_ASSETS` CNAME (branded-domain-only). Active board now: dashboard `DASHBOARD_DATA_SOURCE=backend` flip + standing Clerk/admin USER gates.
- 2026-06-29 HX.2 DONE (server tech-debt): split `controllers/posts/posts.ts` (922 LOC, the multi-session hotspot) into 5 concern modules + `posts.shared` (CATEGORY_PATH_CTE). `posts.ts` is now a 20-LOC read-router BARREL using a register-fn pattern (`registerFeedRoutes(router)` etc., `/:postid` detail registered LAST so the single-segment catch-all never shadows specific routes). Pure mechanical move — zero SQL/handler/middleware/response changes. Each file ≤300 (feed 200, user-feed 272, user-categories 261, user-meta 76, detail 160, barrel 20, shared 26). `index.ts`/`posts.write.ts`/3 route tests untouched (they import `{router} from './posts'` → still the composed read router). NOTE: tests are white-box (`postsRouter.stack` route lookup) → register-fn keeps Route layers directly on the one router (rejected a `.stack`-splice first cut as fragile). Gate: server check-types 0, jest 26 suites/171 tests.
- 2026-06-29 feat(dashboard): chart history wired to backend (architect→developer→architect cycle). `ChartPanel.client.tsx` self-fetches via `useDashboardHistory` at constant `'1d'` (W/M/intraday client-derived by `generateIntervalCandles`); mock full-map `history` prop removed across `ChartVariantLayout`/`app/dashboard/page.tsx`/`[username]`/`preview/chart`; loading + "No chart data available for <label>" empty states. Snapshot already live + USER Vercel `DASHBOARD_DATA_SOURCE=backend` flip done → `/dashboard` fully backend-sourced. Gates: client check-types 0 / lint 0 / vitest 43, server jest 26 suites/171. Known: intraday synthesized from daily; KOSPI/KOSDAQ null under Yahoo `.KS/.KQ` (Decision #32).
- 2026-06-30 docs: completed 00.tasks.md archiving (deferred from 2026-06-29 after a concurrent-session truncation clobbered it; other session has since committed the restored 403-line file as a74e2cb). Cut shipped Phase 2-mobile/filter/fetch to one-line stubs (full detail → `_docs/archive/tasks.phase-2-*.md`, gitignored); removed the DONE HX.2 line from Phase H open list (recorded `db00d42`). Preserved the other session's Phase 2-simple COMPLETE + B4-DONE edits. tasks.md 403→306 LOC. (plan.md side was archived 2026-06-29 `30182ca`.)
- 2026-06-30 refactor(dashboard): Cycle-2 carry-over — split `useTradingViewChart.hook.ts` 524→195 (pure builders → `utils/chart-setup`/`chart-series`/`chart-panes`; React lifecycle effect — measure/crosshair/ResizeObserver/teardown `disposed`-closure — stays in the hook) + re-slim `ChartPanel.client.tsx` 329→202 (`useChartIndicators`/`useChartPanelMenu` hooks, `deriveChartHeader` util, `ChartIndicatorCloseButtons` component); all files ≤300. architect→developer(×2; first crashed mid-response, recovered cleanly via state-check then fresh dev)→architect APPROVE — zero behavior change, public hook API + 3 early-return states intact. Gates: client check-types 0 / lint 0 / vitest 43, server jest 26 suites/171. Also: purged 15 stale seed rows from prod `market_snapshots`/`market_history` (USER ran the DELETE) → `/dashboard` watchlist now 42 live rows, labels match history.
- 2026-06-30 fix(dashboard): prod "Object is disposed" console error from lightweight-charts now suppressed. The window error-listener (`useChartPanelMenu`) gated on `event.filename.includes('lightweight-charts'/'fancy-canvas')` — which NEVER matches in prod (library bundled into a hash-named chunk e.g. `2cd7avmplo_3k.js`), so the benign v5 post-dispose rAF error leaked to console (surfaced more after history-wiring made the chart teardown+rebuild on each candles change). Fix: match on message `'Object is disposed'` alone (+ `error.message` fallback), drop the filename gate. check-types 0 / lint 0.
- 2026-06-30 fix(dashboard): `/dashboard` was statically prerendered → Vercel edge served a STALE snapshot (pre-cleanup seed labels BTC/ETH, Alphabet… `x-vercel-cache: HIT`) baked at build time, so watchlist never reflected the live DB (nor daily collector updates). Added `export const dynamic = 'force-dynamic'` to `app/dashboard/page.tsx` → per-request live snapshot. (Backend verified healthy: 42 clean rows, all 42 labels have `1d` history. Remaining: frontend must REDEPLOY current main so chart-wiring `8ecd62f` + this go live — old build still ships the mock-history chart, which is why most ticker charts were empty.) check-types 0 / lint 0.
- 2026-06-30 feat(dashboard): edge-cache + range + ops-log batch. Express `/dashboard/snapshot` Cache-Control s-maxage=1800/SWR=86400, `/history` s-maxage=43200/SWR=604800 (architect-validated; snapshot bumped 10→30min); `/history` now honours `range` (1m|3m|6m|1y|5y|max, whitelist→from-date, default 1y, max=full). Client actions drop mock fallback + DASHBOARD_DATA_SOURCE branch → fetch-only with `next.revalidate` matched to s-maxage (1800/43200); on any failure POST `/api/error-logs` source='ssr' (best-effort) instead of serving mock; `app/dashboard/page.tsx` renders explicit error state (no MOCK fallback). Range wired client-side: `selectedRangeAtom` (1D…All) → `CHART_RANGE_TO_FETCH` (≤1Y→'1y' to keep MA200; 5Y→'5y'; All→'max') → `useDashboardHistory({range})` → action → `?range=`; same-fetch-window ranges share the RQ key so zoom-only switches never refetch. Mock files retained (preview/[username] routes use them). Gates: client check-types 0 / lint 0, server check-types 0.
- 2026-06-30 feat(collector+db): batch run log + full-history flag. New migrations 0015_batch_log (job|status|resolved|started/finished|descr pipe-string, resolved=1 success/0 fail) + 0016_error_log_resolved (ADD resolved DEFAULT 0) — files only, MANUAL apply = USER gate. `db_sink.insert_batch_log` best-effort (rolls back + swallows missing-table/error so it NEVER fails the job); `run_daily`/`run_weekly` write one row in finally. `run_daily --full` backfills from inception (date(1970,1,1), ON CONFLICT idempotent). Gates: collector pytest 70 passed. NOT-DONE (USER gates): apply 0015/0016 to prod, run `--full` against prod DB.
- 2026-06-30 feat(collector): Phase J SEC core J1/J2/J4 (pure, agent cycle dev→architect-validate). `configs/funds.yaml` (6 filers); `config/funds_loader.py` (FundEntry, pad/validate/dedupe); `transform/sec_parse.py` (parse_submissions/latest_13f/parse_infotable namespace-agnostic/normalize_value 2022-12-31 boundary/aggregate_holdings by (cusip,put_call,prn_type) summing otherManager/units_for_period); `schema/rows.py` +FilingRow/HoldingRow/FundEntry. Architect-validate: CODE PASS (all 5 must-fixes correct, purity/≤300 LOC). FIXED 3 wrong CIKs in funds.yaml (BlackRock→0001086364 active; Bridgewater→0001350694; Renaissance→0001037389 — last two were cross-wired, old Renaissance CIK was a non-13F REIT). Gate: collector pytest 124 passed (70+54). Remaining Phase J: J5 sec_fetch (IO), J6 migration 0017, J7 db_sink, J8 run_monthly, J9 archive, J10 CI cron, J11 gate.
- 2026-06-30 feat(collector): Phase J SEC J5/J6/J7/J8 (IO+sink+entrypoint+migration; agent cycle dev→self-review). `sink/sec_fetch.py` (3-GET: submissions padded-CIK / Archives int-CIK / infotable=largest non-primary .xml; UA+300rpm+backoff). `sink/sec_db_sink.py` split out of db_sink for SRP (db_sink hit 412→307): upsert_filings/upsert_holdings (put_call+prn_type '' sentinel) / supersede_prior_filings (strict-earlier filing_date guard). `entrypoints/run_monthly.py` mirrors run_weekly, partial-failure tolerant, `insert_batch_log(job='collector-monthly')`, failed>0→status='failed' (resolved=0 surfaces partial failures). `settings.sec_user_agent()`+FUNDS_PATH. Migration `0017_sec_holdings` (sec_filings+sec_holdings, PK incl put_call+prn_type NOT NULL sentinels, value_usd BIGINT, deleted_at). LIVE dry-run smoke OK: Berkshire period 2026-03-31, 29 holdings, 3 GETs, exit 0. Gate: collector pytest 124. Remaining: J9 archive, J10 CI cron, J11 gate. USER gates: apply 0017, `SEC_USER_AGENT`+DB secrets, first live run.
- 2026-06-30 feat(funds): Phase K /funds 13F viewer ENG DONE (agent cycle: architect → developer ×2 parallel Express/Next → self-review). Express `controllers/funds/funds.ts` GET /api/funds (store-derived list) + /:cik (holdings, default-latest+`?period=` whitelist, cik digits→padStart(10) parameterized, weight=`COALESCE(ROUND(.. SUM() OVER() ..),0)`), `to_regclass` guard → empty not 500 when 0017 unapplied (NEW Express table-guard pattern), Cache-Control s-maxage=86400/SWR=604800, mounted in controllers/index.ts. Next `/funds`+`/funds/[cik]` (route-param selection, force-dynamic, no mock fallback, reportSsrError on fail) + `/funds/preview` (fixture, real components) + getFunds/getFundHoldings actions (revalidate 86400) + Zod/AssertEqual + formatUsd util. ticker NULL→'—'; read-only, no /dashboard cross-link. Self-review fix: weight COALESCE→0 (Next Zod weightPct non-nullable). Gates (run once at end): server check-types 0 + jest 26/171, web check-types 0 / lint 0 / vitest 9 files/47 (formatUsd +4). USER gate (K11): apply 0017 + first collector run → live data (code ships now via empty/preview).
- 2026-06-30 feat(collector): Phase J finish J9/J10/J11. J9 best-effort raw 13F XML archive in run_monthly (`sec_archive_enabled()`/`sec_bucket()`, default OFF, storage_sink upload `sec13f/<cik>/<accession>/infotable.xml`, swallows errors). J10 `.github/workflows/collector-monthly.yml` cron `0 7 1 * *` + workflow_dispatch + concurrency + setup-uv(SHA-pinned) + env DATABASE_URL/POSTGRES_URL_NON_POOLING/SEC_USER_AGENT(+archive SUPABASE_*/COLLECTOR_ARCHIVE_SEC) + failure-issue. J11 gate: collector pytest 124, import smoke OK (archive OFF). Phase J now ENG COMPLETE (J1–J11); USER gates only: apply 0017, SEC_USER_AGENT+DB secrets, first run.
- 2026-06-30 feat(error-logging): Express global error handler → error_log source='express' (backend self-errors now collected, matching browser/ssr). NEW pure `buildExpressErrorRow(err,path,userAgent)` in helpers/errorLog.ts (Error→message+stack, non-Error→JSON/String fallback, same caps 2000/8000/1000/500, digest=null, source='express'). NEW `middlewares/errorHandler.ts` (ErrorRequestHandler, awaits insert — not fire-and-forget, serverless-safe; DB fail swallowed→console.error; headersSent→next(err); else 500 JSON); mounted LAST in server.ts after /v1 (Express-5 async rejections auto-forward here). User confirmed 0014/0015/0016 all applied to Supabase (resolved triage live). Gates: check-types 0, build 0 (tsc+tsc-alias), jest 174/174 (errorLog +3).
- 2026-06-30 fix(collector): NaN-close bar reached `market_snapshots` (KOSPI 200, 06-29 provisional yfinance bar O/H/L set + close=NaN) → JSON `null` over snapshot API → Next Zod `z.number()` reject → whole snapshot fails → `/dashboard` "Dashboard data is unavailable". Root-caused via `error_log` source='ssr' (parse-error rows, today 18:18–18:23) — NOT an Express runtime error, so the new source='express' handler wouldn't catch it. Root: `ohlc._candle_to_row` let `float('nan')` through (no exception); now rejects any NaN OHLC field. Defense: `build_snapshot` picks latest/prev from NaN-filtered bars (NOT-NULL value/change can't be NaN). `run_daily` snapshot read n=2→3 so a dropped trailing bar still leaves a valid prev. Hardens I1.16 acceptance ("실 value+1-day change, NaN 없음"). Re-ran daily → KOSPI 200 snapshot valid (1370.73, +0.31%, as_of 06-30), 0 NaN rows; local+prod Express healthy + 0 null items; prod Next recovers on the push-triggered redeploy (Data-Cache clear). Gate: collector pytest 128. (b61eff9)
