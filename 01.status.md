# Status — tradelunch

**Owner:** engineer agent. One line per update, **newest at the bottom**. Append a single line per
change — no prose blocks. Product intent → `00.plan.md`; task breakdown → `00.tasks.md`.

---

> Older settled lines (2026-06-24 → 2026-06-28, blog + dashboard era) rolled off to
> `_docs/archive/status.2026-06-blog-dashboard.md` (cold storage). This log holds the active cycle.

- 2026-06-29 FEAT-Phase-I-stock-collector (apps/stock_collector, ENG built, NOT committed): autonomous build of the Yahoo OHLC collector feeding the existing dashboard (market_snapshots/market_history). SIMPLE COPY of lib+yahoo_client from /Users/tio/Documents/00_projects/2025_stock_trading_data (massive/sec13f/alpaca excluded); polyglot sibling (no package.json → pnpm non-member); uv+py3.12, deps synced (yfinance/psycopg/pyarrow). Layout refinement (architect): all Python under src/ with single import root collector.{config,schema,transform,ranking,sink,entrypoints}; configs/watchlist.yaml a root data file. Functional core pure (transform/+ranking/ stdlib-only, ZERO network/DB); psycopg isolated to sink/db_sink.py ONLY. Files ≤300 LOC, soft-delete only.
- 2026-06-29 Phase I — Phase 1 DONE(ENG): watchlist.yaml 42 labels (fx4+crypto3+rates4+indices7+stocks24, globally unique, FX source-native, KRX SPOT indices included); pure ohlc/snapshot(2 change cols change_absolute+change_percent + stocks ticker/exchange)/mapping/fx_direction/universe_resolve; watchlist_loader (validates label-uniqueness + fx labels source-native via fx_label + category/exchange rules); db_sink load_history+refresh_snapshots (ON CONFLICT UPSERT, executemany) + read_latest_bar/read_recent_history/read_tracked_symbols (to_regclass-guarded → [] when absent); run_daily (universe=yaml∪tracked, incremental from_date, Yahoo-coverage graceful-skip); CI collector-daily.yml(cron 30 21 \*-1-5, session-pooler DATABASE_URL, if:failure() issue) + collector-keepalive.yml; seed.market.sql FX label 'KRW/USD'→'USD/KRW' (251 occ). LIVE Yahoo smoke: 14/14 symbols across fx/crypto/rates/indices returned real bars.
- 2026-06-29 Phase I — Phase 2 DONE(ENG): migrations 0011_tracked_symbols (PK symbol, UNIQUE label, category CHECK, deleted_at soft-delete, ON CONFLICT revive) + 0012_market_rankings (PK as_of,symbol,scope, scope CHECK, append-only) — mirrored byte-identical to BOTH schema SSOT copies, check:schema green; pure ranking/screen (COMPLETE ranking, top-20/top-10 = query-time surfacing depth not storage truncation — PM+architect decision) + ranking/universe (assemble dedupe + BRK.B/BRK-B normalize); db_sink upsert_tracked_symbols(sticky revive, never auto-delete)+insert_rankings; run_weekly (assemble S&P500 CSV → fast_info marketcap/sector → rank → sticky tracked top20/sector-top10 via resolve_exchange → rankings append; partial allowed) + collector-weekly.yml (cron 0 6 \* \* 0, DATABASE_URL only, no MASSIVE).
- 2026-06-29 Phase I — Phase 1.5 PARTIAL: parquet_sink (pyarrow zstd, date32, read-merge-rewrite dedupe keep-last, dividends/stock_splits, atomic os.replace) + spec. NOT wired into an entrypoint yet; Storage upload step (I1.5.2) + PRIVATE bucket (I1.5.1b) + daily dividends/splits capture (I1.5.0) = USER-gated/follow-ups.
- 2026-06-29 Phase I VALIDATION: system-architect validated all 7 checks PASS (layering/purity, contract vs reader+0004, migrations dual-mirror, sticky UPSERT, graceful-skip, 42-label watchlist, SRP/LOC/docstrings). Fixed the one no-dead-code finding: wired fx*label (loader fx-label source-native validation) + resolve_exchange (run_weekly tracked exchange). Final gate: 51 pytest pass, check:schema in sync, 16 modules import, psycopg sink-only, all ≤300 LOC. Architect doc-flags: (a) parquet_sink not yet wired to an entrypoint; (b) collector assumes UTC DB session (Supabase default OK) to avoid date off-by-one; (c) tests use spec*\_.py (pyproject) vs python.md test\_\_.py — intentional divergence.
- 2026-06-29 Phase I OPEN USER gates: live DB write + dashboard real-data verify (I1.16, needs DATABASE_URL session-pooler); GH Actions Secrets DATABASE_URL on both workflows; supabase db push 0011+0012 (re-confirm next free number first); PRIVATE parquet Storage bucket + SUPABASE_SERVICE_ROLE; DASHBOARD_DATA_SOURCE=backend flip (shared Phase B B4). Code not yet committed (on branch feature/multi-user-blog working tree). Follow-ups (ENG): I2.8 market_cap caching optimization (currently fast_info direct), I1.5.0/I1.5.2 parquet wiring+upload, setup-uv SHA pin (workflows use official installer).
- 2026-06-29 Phase I COMMITTED — apps/stock_collector + migrations 0011/0012 + 3 collector workflows landed on main as 56b8be5 (supersedes the earlier "NOT committed / feature/multi-user-blog working tree" qualifier in the Phase I lines). Also applied a hardening: db_sink.connect() now pins session timezone=UTC (makes the bar_time TIMESTAMPTZ ← bare-date / MAX(bar_time)::date cursor / UTC reader invariant explicit). Architect+ml-engineer+PM validation: implementation/migration PASS, 51/51 pytest, schema SSOT byte-identical, zero numerical bugs.
- 2026-06-29 docs: added `.claude/rules/docs.md` (archive lifecycle + read-guard); collapsed shipped phases to DONE stubs in 00.plan.md (A/B/D/E/E2/F/F2/G/H + Decisions Log) + 00.tasks.md (Repo Hygiene/Phase 1/Cycle 2/A/blog_agent/B/D/E/E2/F/F2/G/H), full detail → `_docs/archive/*`; dropped stale B5.5 + UX12b-persist open items; fixed Phase I "NOT committed"→committed 56b8be5; card-actions plan → archive (handoff doc-sync.shipped-not-archived).
- 2026-06-29 Phase I ENG follow-ups LIVE (commits e2a5e5c/12b8a80/823a5e4/f971661): setup-uv SHA pin v8.2.0 + checkout v7.0.0 SHA (all workflows + ci.yml); parquet archive wired into run*daily (--archive/COLLECTOR_ARCHIVE_PARQUET) capturing adj_close/dividends/splits from same Yahoo frame; new seed_archive entrypoint + collector-seed-archive.yml (full-history backfill); I2.8 migration 0013 symbol_fundamentals APPLIED (+mirrors, check:schema green) + pure ranking/fundamentals + db_sink table-guarded cache + run_weekly shares×close derivation; settings accept POSTGRES_URL_NON_POOLING + standardize SUPABASE_SECRET_KEY (sb_secret*, dropped legacy service*role). BUG fixed: Storage upload needs apikey header for sb_secret* keys (Bearer-only→400). LIVE verify: daily run universe=42/9870 bars/42 snapshots/skipped=0; seed 11636 rows→83 objects uploaded to PRIVATE market-archive. GH secrets DATABASE_URL/SUPABASE_URL/SUPABASE_SECRET_KEY + vars COLLECTOR_ARCHIVE_PARQUET=1/COLLECTOR_PARQUET_BUCKET set. Remaining USER gate: DASHBOARD_DATA_SOURCE=backend flip. Ref doc \_docs/supabase-db-connection-and-ci.md (merged the IPv4/IPv6 doc).
- 2026-06-29 FEAT-scrolltop-global (client, owner request): ScrollToTopButton promoted to a SINGLE global mount in app/layout.tsx (after {children}) so EVERY route incl. `/` shows it — was only `/blog/*` (via blog/layout.tsx) + MainPage/ResumePage/resume. Removed the 4 duplicate mounts; app/blog/layout.tsx reduced to pure pass-through `<>{children}</>`. threshold=300 unchanged (self-hides short pages); ScrollToTop.tsx is the only def + layout.tsx the only mount. Gate: client tsc 0.
- 2026-06-29 PLAN-Phase-2-mobile (architect+PM, owner request): mobile context-UX phase scoped into 00.plan/00.tasks as "Phase 2-mobile" (11 atomic tasks, client-only, no backend/schema). Option B = lg:hidden context header ABOVE the feed on list pages (author chip + horizontal-scroll category/tag LINK row, edge-fade, ≥44px, NOT role=tab) reusing existing fetchers via new components/rail/mobile/\* + additive BlogShell `mobileTopContext` slot. Option D = lg:hidden collapsible 목차/Contents under the post title reusing tocItemsAtom + Radix Collapsible (research/NN-g: sticky/floating TOC gets missed → accordion). Decisions Log #33 LOCKED (PM+architect, not escalated): M1 chip row REPLACES list-page userContextSheet (soft-unwire both list layouts, keep UserContextSheet.client.tsx file); M2 drop duplicate RightRailToc from the mobile sheet so MobileToc = sole mobile TOC; desktop ≥lg rail untouched. NOT YET IMPLEMENTED (developer dispatch pending: MOB.0 i18n → D MOB.1a-d → B MOB.2a-f → gate MOB.3).
- 2026-06-29 Phase 2-mobile IMPLEMENTED (client, MOB.0–MOB.3): i18n blog.toc.heading/blog.mobile.\* both locales; Option D new MobileToc.client.tsx (Radix Collapsible over tocItemsAtom, null on empty) mounted lg:hidden atop PostContentCard CardContent; Option B new components/rail/mobile/{getMobileChips.server.ts,MobileAuthorChip,MobileChipRow,MobileContextHeader}.server.tsx + additive BlogShell mobileTopContext slot; both list layouts wired mobileTopContext + userContextSheet UNWIRED (M1; UserContextSheet.client.tsx kept). MOB.1d (M2) satisfied by the shared [username]/layout sheet-unwire — removes the duplicate mobile RightRailToc, desktop >=lg rail byte-equiv (no UserContextRail change). Gate: check-types exit 0, lint exit 0.
- 2026-06-29 FEAT-scrolltop-show-on-scrollup (client, owner request): ScrollToTopButton promoted global mount + now reveals on scroll-up (not only past a fixed threshold); rAF-throttled scroll listener + a small jitter guard so micro-scrolls don't flicker the button. commits 1feacc9 / 65ae70a / 043488a / 4708486.
- 2026-06-29 FIX-mobile-content-padding (client, owner mobile report): mobile content inset bumped to 16px `p-4` for the feed/post column. commit ee8cacf.
- 2026-06-29 FIX-mobile-chip-row-clip (client): mobile chip row content-clipped so the horizontal-scroll row no longer bleeds past the content column. commit eff3286.
- 2026-06-29 FEAT-mobile-toc-above-post-card (client, owner request): mobile TOC moved ABOVE the post card (was inside CardContent). commit 884754e.
- 2026-06-29 FEAT-Phase-2-filter (server+client, multi-category + multi-tag feed filter on /blog/[username], all breakpoints): commits a05a299/000a564/6db0c68 (backend+core) + c132024/e20e329/1243df4/4e8483b (frontend). Semantics same-type OR / cross-type AND — categories array OVERLAP `cpath.path && $5::text[]`, tags correlated `EXISTS … = ANY($6)` (NOT INNER JOIN, no migration); URL plural `?categories=a,b&tags=a,b` (cap FILTER*MAX=20); legacy `?category_title=` folded into categories at parse (client core + Express safety net). Pure core utils/filter-state.ts (no orderSelectedFirst — F4 owner override = highlight in place, no sort-to-front). F5 = desktop ≥lg RAILS are the filter UI (mode:'filter' prop on CategorySidebar/CategoryTree+TagCloud) + mobile <lg FilterChipRow rows + AppliedFilters summary bar both breakpoints (NOT a FeedFilterBar). F6 = filter chips are zero-JS `<Link>` reading useSearchParams() (FilterChip.client), active via aria-current (not buttons). F.11 cache = Cache-Control on the EXPRESS RESPONSE (anon public s-maxage=3600 SWR=86400; authed private no-store), revalidateTag N/A. NEW utils/filter-state.ts + helpers/parseFeedFacet.ts + components/blog/filter/{FilterChip,FilterChipRow,AppliedFilters}.client + getFilterItems.server + jest posts.feed-filter.test; x*-retired MobileChipRow/getMobileChips. Gate GREEN: client check-types+lint 0/0; dashboard_server jest 25 suites/166 tests. Decisions Log #34.
- 2026-06-29 VALIDATE-Phase-2-filter (architect verify, ships-clean): 9-dim PASS (SQL OR/AND+short-circuit+EXISTS row-integrity+injection-safe, draft-leak boundary = req.auth branch covers `OR p.user_id=$4` visibility, remount-key/deps both facets, x\_ retirement fully unwired); aria-current-on-`<Link>` web-verified CORRECT (not an aria-pressed smell). Fixed 2 Low: (#1) client parseFilterState + load-more route now use category_title PRECEDENCE (length-based, mirrors Express `??`) instead of merge → SSR/API lock-step; (#3) wired dead i18n addCategory/addTag(+remove) keys as FilterChip aria-label. commits TBD; client check-types+lint 0/0.
- 2026-06-29 FEAT-Phase-2-fetch (server+client, feed data layer axios→native fetch + Next 16 tag revalidation): commits 3563467/bd00a72/8955f33/731250e. NEW apis/buildFeedQuery.ts(pure) + apis/http.server.ts(`server-only` serverRequest<T>: auth header/JSON/FormData/`AbortSignal.timeout(5s)`/`!res.ok`→ApiError via toFetchApiError/no-store-omits-next guard) + apis/getPosts.fetch.server.ts(getFeed: anon force-cache+tags[feed:global,feed:<u>]+revalidate:60 / owner token→no-store, token via Clerk auth().getToken() outside cache) + app/actions/postPublish.action.ts(updateTag on publish/unpublish/delete/admin). RecentPostsList.server calls getFeed directly. Express anon s-maxage 3600→60 backstop. Rewired useDeletePost/useSetAdminPostStatus/editor-publish; x\_-retired deletePost.api/setAdminPostStatus.api; drafts/autosave/create + loadMorePosts + load-more route + sitemap stay on axios. Cache Components NOT enabled (legacy tagged-fetch). Gate GREEN: client check-types+lint 0/0; dashboard_server jest 25/166. Decisions Log #35. OPEN: F-fetch.1 admin-delete revalidate, F-fetch.2 client-axios retirement.
- 2026-06-29 F-fetch.1 DONE (CLOSED): admin delete now `deleteAdminPostAction` (mirrors `deletePostAction` → `DELETE /v1/api/admin/posts/:id` + `revalidateFeed(username)`); new `useDeleteAdminPostAction` hook also invalidates `adminPostsQueryKey`; `AdminPostsTable` passes `{postId, username: post.username ?? ''}`; `x_`-retired old `useDeleteAdminPost.query.client`/`deleteAdminPost.api`. Gate: client check-types+lint 0/0. F-fetch.2 (client-axios retirement) stays DEFERRED.
- 2026-06-29 FEAT-error-logging (client+ssr+express, owner request — browser/SSR errors → Supabase): DECIDED arch = browser error boundary → Next Route Handler `app/api/log-error` → server-to-server `serverRequest` → Express `POST /v1/api/error-logs` → `error_log` (Next SSR keeps NO DB creds per A-1; Express stays sole Supabase owner; browser never sees Express). NEW migration `0014_error_logs.sql` (`error_log`: digest/message/stack/path/user_agent/source/created_at; NO `deleted_at` — owner-approved OPERATIONAL-LOG exception to soft-delete; pg_cron 7-day TTL cleanup shipped COMMENTED-OUT, enabling pg_cron = USER/Supabase-dashboard gate). NEW express helper `errorLog.ts` (pure `normalizeErrorLog`: coerce+truncate 2000/8000/1000/500, default source='browser') + `controllers/errorLogs` (public, 204/500, never throws) + jest 5/5. NEW Next `app/api/log-error/route.ts` (forwards, always 204) + `utils/reportError.util.ts` (fire-and-forget keepalive fetch, never throws) + `app/global-error.tsx`; `app/error.tsx` now also reports. Gate GREEN: client check-types 0, dashboard_server check-types+build 0, jest errorLog 5/5. USER gates: apply 0014 + enable pg_cron schedule.
- 2026-06-29 FIX-feed-envelope (prod RSC 500, digest 650637976 — "Cannot read properties of undefined (reading 'length')" in RecentPostsList.server `posts.length`): ROOT CAUSE = Express feed read endpoints return `{ success, data:{ posts, nextCursor, hasMore } }` envelope; legacy axios path unwrapped TWICE (response-interceptor→body + explicit `.data`), but new `getFeed`(native fetch) didn't unwrap → `posts` undefined. FIX: `getPosts.fetch.server.ts` adds `TFeedEnvelope` + `unwrapFeed()` (returns `.data`, defaults `posts:[]/nextCursor:null/hasMore:false`) applied to BOTH owner(no-store) + anon(tagged) branches → can never `.length` undefined again. Affects global + per-author feeds. Gate: client check-types 0 / lint 0. NOTE: leftover `console.log('>> getCategoriesByUsername')` in getCategories.api.ts is prod noise (separate cleanup). UNCOMMITTED — needs commit+deploy to land the prod hotfix.
- 2026-06-29 UX-mobile-category (owner request — `/blog/[username]` mobile/medium <lg lost the right-rail category section): NEW `MobileCategory.client.tsx` (Collapsible shell mirroring `MobileToc`, collapsed-by-default, chevron trigger); `CategorySidebar`+`CategorySidebarWrapper` gain `bare` prop (tree only, no Card chrome) for nesting in the accordion; list `page.tsx` swaps the category `FilterChipRow` for `<MobileCategory title={t.categories}><CategorySidebarWrapper mode=filter bare/></MobileCategory>` (tags stay a chip row), drops now-unused `getCategoryFilterItems`/`categoryItems`. Symmetry: list=Categories accordion, detail=Contents(TOC) accordion. Gate: client check-types 0.
- 2026-06-29 USER: error-logging migration `0014_error_logs.sql` APPLIED to Supabase (error_log persists; browser/SSR→Express→Supabase pipeline now live end-to-end). pg_cron 7-day TTL = optional follow-up (still commented).
- 2026-06-29 FEAT-explore-nav-placeholder (client, owner request — trailing nav slot read empty): added a DISABLED `/explore` PrimaryNav item (Compass icon, "coming soon", non-clickable muted `<span>`) via `useNavLinks.hook.ts` `buildPrimaryNavLinks` `disabled:true`; `nav.explore` already in both locales; PrimaryNav + MenuDrawer already render `disabled`. Real P6 search/discovery stays parked. Gate: client check-types 0 / lint 0 / vitest useNavLinks 7/7.
- 2026-06-29 docs: demoted to NOTES (not open tasks) per owner — F-fetch.2 (client axios retirement), P6 (real search/`/explore`; disabled nav placeholder now live), Cloudflare `CDN_ASSETS` CNAME (branded-domain-only). Active board now: dashboard `DASHBOARD_DATA_SOURCE=backend` flip + standing Clerk/admin USER gates.
- 2026-06-29 HX.2 DONE (server tech-debt): split `controllers/posts/posts.ts` (922 LOC, the multi-session hotspot) into 5 concern modules + `posts.shared` (CATEGORY_PATH_CTE). `posts.ts` is now a 20-LOC read-router BARREL using a register-fn pattern (`registerFeedRoutes(router)` etc., `/:postid` detail registered LAST so the single-segment catch-all never shadows specific routes). Pure mechanical move — zero SQL/handler/middleware/response changes. Each file ≤300 (feed 200, user-feed 272, user-categories 261, user-meta 76, detail 160, barrel 20, shared 26). `index.ts`/`posts.write.ts`/3 route tests untouched (they import `{router} from './posts'` → still the composed read router). NOTE: tests are white-box (`postsRouter.stack` route lookup) → register-fn keeps Route layers directly on the one router (rejected a `.stack`-splice first cut as fragile). Gate: server check-types 0, jest 26 suites/171 tests.
- 2026-06-29 feat(dashboard): chart history wired to backend (architect→developer→architect cycle). `ChartPanel.client.tsx` self-fetches via `useDashboardHistory` at constant `'1d'` (W/M/intraday client-derived by `generateIntervalCandles`); mock full-map `history` prop removed across `ChartVariantLayout`/`app/dashboard/page.tsx`/`[username]`/`preview/chart`; loading + "No chart data available for <label>" empty states. Snapshot already live + USER Vercel `DASHBOARD_DATA_SOURCE=backend` flip done → `/dashboard` fully backend-sourced. Gates: client check-types 0 / lint 0 / vitest 43, server jest 26 suites/171. Known: intraday synthesized from daily; KOSPI/KOSDAQ null under Yahoo `.KS/.KQ` (Decision #32).
- 2026-06-30 docs: completed 00.tasks.md archiving (deferred from 2026-06-29 after a concurrent-session truncation clobbered it; other session has since committed the restored 403-line file as a74e2cb). Cut shipped Phase 2-mobile/filter/fetch to one-line stubs (full detail → `_docs/archive/tasks.phase-2-*.md`, gitignored); removed the DONE HX.2 line from Phase H open list (recorded `db00d42`). Preserved the other session's Phase 2-simple COMPLETE + B4-DONE edits. tasks.md 403→306 LOC. (plan.md side was archived 2026-06-29 `30182ca`.)
- 2026-06-30 refactor(dashboard): Cycle-2 carry-over — split `useTradingViewChart.hook.ts` 524→195 (pure builders → `utils/chart-setup`/`chart-series`/`chart-panes`; React lifecycle effect — measure/crosshair/ResizeObserver/teardown `disposed`-closure — stays in the hook) + re-slim `ChartPanel.client.tsx` 329→202 (`useChartIndicators`/`useChartPanelMenu` hooks, `deriveChartHeader` util, `ChartIndicatorCloseButtons` component); all files ≤300. architect→developer(×2; first crashed mid-response, recovered cleanly via state-check then fresh dev)→architect APPROVE — zero behavior change, public hook API + 3 early-return states intact. Gates: client check-types 0 / lint 0 / vitest 43, server jest 26 suites/171. Also: purged 15 stale seed rows from prod `market_snapshots`/`market_history` (USER ran the DELETE) → `/dashboard` watchlist now 42 live rows, labels match history.
- 2026-06-30 fix(dashboard): prod "Object is disposed" console error from lightweight-charts now suppressed. The window error-listener (`useChartPanelMenu`) gated on `event.filename.includes('lightweight-charts'/'fancy-canvas')` — which NEVER matches in prod (library bundled into a hash-named chunk e.g. `2cd7avmplo_3k.js`), so the benign v5 post-dispose rAF error leaked to console (surfaced more after history-wiring made the chart teardown+rebuild on each candles change). Fix: match on message `'Object is disposed'` alone (+ `error.message` fallback), drop the filename gate. check-types 0 / lint 0.
- 2026-06-30 fix(dashboard): `/dashboard` was statically prerendered → Vercel edge served a STALE snapshot (pre-cleanup seed labels BTC/ETH, Alphabet… `x-vercel-cache: HIT`) baked at build time, so watchlist never reflected the live DB (nor daily collector updates). Added `export const dynamic = 'force-dynamic'` to `app/dashboard/page.tsx` → per-request live snapshot. (Backend verified healthy: 42 clean rows, all 42 labels have `1d` history. Remaining: frontend must REDEPLOY current main so chart-wiring `8ecd62f` + this go live — old build still ships the mock-history chart, which is why most ticker charts were empty.) check-types 0 / lint 0.
- 2026-06-30 feat(dashboard): edge-cache + range + ops-log batch. Express `/dashboard/snapshot` Cache-Control s-maxage=1800/SWR=86400, `/history` s-maxage=43200/SWR=604800 (architect-validated; snapshot bumped 10→30min); `/history` now honours `range` (1m|3m|6m|1y|5y|max, whitelist→from-date, default 1y, max=full). Client actions drop mock fallback + DASHBOARD_DATA_SOURCE branch → fetch-only with `next.revalidate` matched to s-maxage (1800/43200); on any failure POST `/api/error-logs` source='ssr' (best-effort) instead of serving mock; `app/dashboard/page.tsx` renders explicit error state (no MOCK fallback). Range wired client-side: `selectedRangeAtom` (1D…All) → `CHART_RANGE_TO_FETCH` (≤1Y→'1y' to keep MA200; 5Y→'5y'; All→'max') → `useDashboardHistory({range})` → action → `?range=`; same-fetch-window ranges share the RQ key so zoom-only switches never refetch. Mock files retained (preview/[username] routes use them). Gates: client check-types 0 / lint 0, server check-types 0.
- 2026-06-30 feat(collector+db): batch run log + full-history flag. New migrations 0015_batch_log (job|status|resolved|started/finished|descr pipe-string, resolved=1 success/0 fail) + 0016_error_log_resolved (ADD resolved DEFAULT 0) — files only, MANUAL apply = USER gate. `db_sink.insert_batch_log` best-effort (rolls back + swallows missing-table/error so it NEVER fails the job); `run_daily`/`run_weekly` write one row in finally. `run_daily --full` backfills from inception (date(1970,1,1), ON CONFLICT idempotent). Gates: collector pytest 70 passed. NOT-DONE (USER gates): apply 0015/0016 to prod, run `--full` against prod DB.
- 2026-06-30 feat(collector): Phase J SEC core J1/J2/J4 (pure, agent cycle dev→architect-validate). `configs/funds.yaml` (6 filers); `config/funds_loader.py` (FundEntry, pad/validate/dedupe); `transform/sec_parse.py` (parse_submissions/latest_13f/parse_infotable namespace-agnostic/normalize_value 2022-12-31 boundary/aggregate_holdings by (cusip,put_call,prn_type) summing otherManager/units_for_period); `schema/rows.py` +FilingRow/HoldingRow/FundEntry. Architect-validate: CODE PASS (all 5 must-fixes correct, purity/≤300 LOC). FIXED 3 wrong CIKs in funds.yaml (BlackRock→0001086364 active; Bridgewater→0001350694; Renaissance→0001037389 — last two were cross-wired, old Renaissance CIK was a non-13F REIT). Gate: collector pytest 124 passed (70+54). Remaining Phase J: J5 sec_fetch (IO), J6 migration 0017, J7 db_sink, J8 run_monthly, J9 archive, J10 CI cron, J11 gate.
- 2026-06-30 feat(collector): Phase J SEC J5/J6/J7/J8 (IO+sink+entrypoint+migration; agent cycle dev→self-review). `sink/sec_fetch.py` (3-GET: submissions padded-CIK / Archives int-CIK / infotable=largest non-primary .xml; UA+300rpm+backoff). `sink/sec_db_sink.py` split out of db_sink for SRP (db_sink hit 412→307): upsert_filings/upsert_holdings (put_call+prn_type '' sentinel) / supersede_prior_filings (strict-earlier filing_date guard). `entrypoints/run_monthly.py` mirrors run_weekly, partial-failure tolerant, `insert_batch_log(job='collector-monthly')`, failed>0→status='failed' (resolved=0 surfaces partial failures). `settings.sec_user_agent()`+FUNDS_PATH. Migration `0017_sec_holdings` (sec_filings+sec_holdings, PK incl put_call+prn_type NOT NULL sentinels, value_usd BIGINT, deleted_at). LIVE dry-run smoke OK: Berkshire period 2026-03-31, 29 holdings, 3 GETs, exit 0. Gate: collector pytest 124. Remaining: J9 archive, J10 CI cron, J11 gate. USER gates: apply 0017, `SEC_USER_AGENT`+DB secrets, first live run.
- 2026-06-30 feat(funds): Phase K /funds 13F viewer ENG DONE (agent cycle: architect → developer ×2 parallel Express/Next → self-review). Express `controllers/funds/funds.ts` GET /api/funds (store-derived list) + /:cik (holdings, default-latest+`?period=` whitelist, cik digits→padStart(10) parameterized, weight=`COALESCE(ROUND(.. SUM() OVER() ..),0)`), `to_regclass` guard → empty not 500 when 0017 unapplied (NEW Express table-guard pattern), Cache-Control s-maxage=86400/SWR=604800, mounted in controllers/index.ts. Next `/funds`+`/funds/[cik]` (route-param selection, force-dynamic, no mock fallback, reportSsrError on fail) + `/funds/preview` (fixture, real components) + getFunds/getFundHoldings actions (revalidate 86400) + Zod/AssertEqual + formatUsd util. ticker NULL→'—'; read-only, no /dashboard cross-link. Self-review fix: weight COALESCE→0 (Next Zod weightPct non-nullable). Gates (run once at end): server check-types 0 + jest 26/171, web check-types 0 / lint 0 / vitest 9 files/47 (formatUsd +4). USER gate (K11): apply 0017 + first collector run → live data (code ships now via empty/preview).
- 2026-06-30 feat(collector): Phase J finish J9/J10/J11. J9 best-effort raw 13F XML archive in run*monthly (`sec_archive_enabled()`/`sec_bucket()`, default OFF, storage_sink upload `sec13f/<cik>/<accession>/infotable.xml`, swallows errors). J10 `.github/workflows/collector-monthly.yml` cron `0 7 1 * _` + workflow_dispatch + concurrency + setup-uv(SHA-pinned) + env DATABASE_URL/POSTGRES_URL_NON_POOLING/SEC_USER_AGENT(+archive SUPABASE_\*/COLLECTOR_ARCHIVE_SEC) + failure-issue. J11 gate: collector pytest 124, import smoke OK (archive OFF). Phase J now ENG COMPLETE (J1–J11); USER gates only: apply 0017, SEC_USER_AGENT+DB secrets, first run.
- 2026-06-30 feat(error-logging): Express global error handler → error_log source='express' (backend self-errors now collected, matching browser/ssr). NEW pure `buildExpressErrorRow(err,path,userAgent)` in helpers/errorLog.ts (Error→message+stack, non-Error→JSON/String fallback, same caps 2000/8000/1000/500, digest=null, source='express'). NEW `middlewares/errorHandler.ts` (ErrorRequestHandler, awaits insert — not fire-and-forget, serverless-safe; DB fail swallowed→console.error; headersSent→next(err); else 500 JSON); mounted LAST in server.ts after /v1 (Express-5 async rejections auto-forward here). User confirmed 0014/0015/0016 all applied to Supabase (resolved triage live). Gates: check-types 0, build 0 (tsc+tsc-alias), jest 174/174 (errorLog +3).
- 2026-06-30 fix(collector): NaN-close bar reached `market_snapshots` (KOSPI 200, 06-29 provisional yfinance bar O/H/L set + close=NaN) → JSON `null` over snapshot API → Next Zod `z.number()` reject → whole snapshot fails → `/dashboard` "Dashboard data is unavailable". Root-caused via `error_log` source='ssr' (parse-error rows, today 18:18–18:23) — NOT an Express runtime error, so the new source='express' handler wouldn't catch it. Root: `ohlc._candle_to_row` let `float('nan')` through (no exception); now rejects any NaN OHLC field. Defense: `build_snapshot` picks latest/prev from NaN-filtered bars (NOT-NULL value/change can't be NaN). `run_daily` snapshot read n=2→3 so a dropped trailing bar still leaves a valid prev. Hardens I1.16 acceptance ("실 value+1-day change, NaN 없음"). Re-ran daily → KOSPI 200 snapshot valid (1370.73, +0.31%, as_of 06-30), 0 NaN rows; local+prod Express healthy + 0 null items; prod Next recovers on the push-triggered redeploy (Data-Cache clear). Gate: collector pytest 128. (b61eff9)
- 2026-06-30 docs: synced Phase I gates to live DB (0011/0012 applied, I1.16 dashboard-READ verified) + archived Phase J/K to stubs (full detail → `_docs/archive/`, gitignored; Decisions #36/#37 preserved). DB cross-check: migrations 0011–0017 all applied; SEC monthly ran live (sec_filings=6/sec_holdings=16,543). (e147ce0)
- 2026-06-30 feat(collector): weekly rankings FIRST LIVE RUN done — `run_weekly` candidates=505 → market_rankings=1,007 (global+sector scopes, as_of 1) + tracked_symbols=110 active (all stocks), `collector-weekly` batch_log success 20:45 UTC. Closes Phase I's last functional gap (sticky universe established; e.g. GOOGL global #1 $4.36T, NVDA Tech #1). Remaining Phase I open: CI-cron secret/scheduled-fire verify + parquet wiring only.
- 2026-06-30 plan(Phase M): market_history 5yr retention 티어링 설계 (system-architect cycle). market_history=hot 5yr window; full history=Parquet cold (Storage, already LIVE — COLLECTOR_ARCHIVE_PARQUET=1 GH var, seed+daily upload confirmed). Weekly prune deletes pre-cutoff bars ONLY after per-label per-year Parquet object verified (no-op if archive off/unreachable). SANCTIONED soft-delete EXCEPTION: hard-DELETE = tier MOVE not content deletion (docs.md §7 analogue); tombstone rejected (would retain the bytes prune reclaims). Found must-fix R3: seed_archive uses fetch_daily not fetch_full → no inception (M7 before live prune). 277,870→~189k rows (~75MB bounded). Docs: 00.plan.md Phase M + Decisions #39; tasks M1–M8. NOT built; prune LIVE gated on M7 + M6 dry-run. (PLANNED)
- 2026-06-30 feat(collector): Phase M ENG M1–M7 built (agent cycle: architect 설계 → developer ×2 병렬 → architect 검증 PASS-WITH-NITS, must-fix 0). M1 pure `transform/retention.py`(`prune_cutoff`=달력연도 floor `date(y−5,1,1)` + `prunable_years`); M2 `storage_sink.object_exists`(2xx→True, 예외/non-2xx→False fail-safe); M3 `db_sink.read_prune_candidates`/`delete_history_before`(per-label commit); M4 `entrypoints/prune_history.py`(no-op 게이트 → label→symbol 매핑 via resolve_universe → 라벨별 전 연도 `object_exists` 검증 후에만 DELETE, dry-run 우선, batch_log job='collector-prune'); M7 `seed_archive`→`fetch_full`(inception); M6 `collector-prune.yml`(연 1회 `0 7 30 12 *` + dispatch `dry_run` 토글, 스케줄 항상 dry-run). 검증: verify-후-delete 불변식·no-op 게이트·label≠symbol 매핑 코드에서 참 확인. Gate: collector pytest 137 (retention +9); 로컬 dry-run no-op 게이트 동작 확인(exit 0). USER 게이트 남음: M8 신규티커 5년 hot 백필(운영) + 첫 LIVE prune = dispatch `dry_run` 해제. nit(defer): db_sink 361 LOC 분할.
- 2026-06-30 chore(collector-prune): nit-2 적용 — `collector-prune.yml`에 `workflow_dispatch` `dry_run` boolean(기본 true) + 조건부 run step. 스케줄은 항상 dry-run, 첫 LIVE prune = dispatch에서 `dry_run` 해제(1-click, YAML 편집 불필요). M8 진행: `run_daily --backfill-days 1825` 로컬 백그라운드 실행(110 신규 tracked 5년 hot 백필) + `collector-seed-archive` CI 트리거(M7 fetch_full로 inception Parquet seed, run 28478534278).
- 2026-06-30 docs(.github/CRON.md): 스케줄 잡 단일 레퍼런스 신설 — GH Actions 8 워크플로(cron/cadence/명령) + Supabase pg_cron 1잡(`0 3 * * *` error_log 7일 purge) + gh/psql 조회·실행 커맨드 + prune이 pg_cron 아닌 이유. (DB 조회로 pg_cron 잡 실측 확인.)
- 2026-06-30 chore(prune): Phase M gate를 mode B로 해제(오너 결정) — `collector-prune.yml` prune-ohlc 조건 뒤집어 **스케줄 12/30 = LIVE**(아카이브 검증된 cold 봉 삭제), 수동 dispatch는 기본 dry-run(미리보기). CRON.md + collector README 동기화(seed_archive `--backfill-days` 제거 stale 수정 포함, prune 행 추가, pg_cron 명시). **NOTE(멀티세션):** mode-B 플립은 디스크에만 — `collector-prune.yml`이 다른 세션 L18(prune-13f+prune_holdings.py, 미커밋)과 얽혀 단독 커밋 불가 → 워크플로 커밋은 L18 세션에 양보(오너 결정 옵션 A). 결합 working tree pytest 196 green. M8(양 계층) 완료: DB 5년 hot 백필 148라벨 + seed CI inception Parquet success.
- 2026-06-30 docs(plan/tasks): Phase L defined — `/funds/[cik]` 13F rank-flow viz (system-architect data/backfill + product-manager UX; Decisions #38). Owner locked 4: rank-flow=ONLY view (Phase K flat table retires), 8 quarters=2yr (backfill `--since` 2yr → ~30–90MB, under 500MB free-tier DB), rows=ever-top-K(25) union (bump continuity), Δ-badges/dim/lines→L-next. v1 = `run_backfill.py` (filings.files pagination + supersede period-safe + workflow_dispatch) → `GET /api/funds/:cik/rankflow` (union, NOT analytics views) → `RankFlowTable` (cusip-hash stable color, click→inner-ring cross-quarter track). Analytics views 0018 + cross-fund consensus + Δ-badges = L-next (deferred). L1–L14 in 00.tasks.md. NOT yet built (planning only).
- 2026-06-30 docs(tasks): Phase L Slice D extended — SEC 13F Parquet cold-archive track (L15–L17, DEFERRED). Scope locked: **2013→present** (structured-XML information-table era, ~13yr, ~30MB), **FULL holdings** (NO top-N truncation — ml-engineer verdict) → PRIVATE Storage `sec13f/{cik}/{cik}_{YYYY}.parquet` (mirrors OHLC Phase 1.5 `parquet_sink`). hot=thin (DB serving stays 2yr) / cold=full (archive 2013→present). 30yr REJECTED: pre-2013 13F = unstructured text/HTML, current XML parser can't read → realistic structured ceiling ~2013; size is not the constraint (1GB tier, ~3% used), parseability is.
- 2026-06-30 feat(Phase L): rank-flow ENG DONE (active scope) — parallel multi-session build. **A 백필** sec*parse(`all_13f`/`group_by_period`/`merge_submission_pages`)+sec_fetch(`filings.files` pagination)+`run_backfill.py`+`collector-13f-backfill.yml`(workflow_dispatch, dry-run default). **B** `GET /api/funds/:cik/rankflow?quarters=8&k=25`(ever-top-K union, funds.ts conventions). **C** `RankFlowTable`(cusip-hash stable color, click→inner-ring cross-quarter track, aria-pressed, h-scroll) + utils cusipColor/quarterLabel/rankFlowRows(+vitest) + `getFundRankFlow` action/Zod; flat `HoldingsTable`→`x*`retired. **L15/16**`sec_parquet_sink.py`(zstd, cross-year period-of-report partitioning, default-OFF archive step). **L18/19** `prune_holdings.py`(hard-delete, owner-signed exception, archive-`object_exists`precondition, keep 12q) reusing Phase M`retention.py`/`collector-prune.yml`(prune-13f job) + weekly cadence (`collector-monthly`→`0 7 \* \* 1`, period-advance 1-GET guard). Gates GREEN: collector pytest 196 · server jest 184/types 0 · client types 0/lint 0/vitest 27. L-next DEFERRED: L12–14 (0018 views/Δ-badges/consensus), L17 (2013 full backfill). USER gates: backfill dispatch ×1, PRIVATE 13F bucket.
- 2026-06-30 feat(Phase N): 2차 retention ENG DONE (#39 후속) — Phase M 머신 재사용, 성격별 2갈래. **로그**(no-tombstone 예외): `retention.age_cutoff` + `db_sink.delete_error_log_before`/`delete_batch_log_before`(keep_open_failures) + 공유 `prune_logs.py`(`--target`, error 7일/batch 90일) → 신규 `collector-prune-logs.yml` **매일 03:00 LIVE**. **rankings**(재현불가 도메인): `read_rankings_prune_years`/`delete_rankings_before` + `prune_rankings.py`(`--years 10 --verify-archive`, all-or-skip) + 아카이브 라이터 순수 `rankings_parquet_sink.py`(`rankings/{YYYY}.parquet`, PK dedupe keep-last)+`read_rankings_years`/`read_rankings_by_year`+`archive_rankings.py`(best-effort Storage, exit 0) → `collector-weekly.yml` 주간 스텝 + `collector-prune.yml` 3번째 잡(연 12/30 dry-run). **DDL 없음**(기존 컬럼/인덱스). ML 자문: rankings=10년(5년 아님 — 재현불가). live dry-run 실증: error/batch/rankings 후보 0건(rankings cutoff 2016-01-01, ~2036까지 무해 no-op), archive 2026 파일 1007행. Gates: collector pytest **208** green(신규 age_cutoff 4 + rankings sink 8). Decisions #40. USER 게이트: rankings 첫 LIVE prune 수동 dispatch(현재 무해).
- 2026-06-30 feat(Phase L): L17 + L20 ENG DONE. **L17** archive≠DB window split — `run_backfill --db-keep-quarters`(default 12=3yr) + pure `retention.db_keep_cutoff`; per period `>=cutoff`→DB upsert else skip, parquet always written (archive=`--since` full history, DB=recent 3yr only); dry-run prints `archive=N / db=M periods`; `collector-backfill.yml` +`db_keep_quarters` input; pytest 226. **L20** rank-flow bump-style redesign — `RankFlowTable` 2 modes: flow (per-column independent ranking, position=rank, cusip-stable color traces movement) + column-header click→aligned (sort rows by that quarter) + Reset ↺/Esc; pure `orderColumnByRank`/`sortRowsByReference`(+vitest); old row-aligned→`x_RankFlowTable`; check-types 0/lint 0/vitest 83. L-next: L12–14 (0018 views/Δ-badges/consensus), L21 sector enrichment (CUSIP→ticker via OpenFIGI; ticker→sector already in `symbol_fundamentals`; new `security_map`). USER: 2013 backfill dispatch (`since=2013 db_keep_quarters=12 archive=true`). Committed combined w/ Phase N (intertwined tree).
- 2026-06-30 docs: archived settled detail to `_docs/archive/` stubs — plan.md Phase I detail + Decisions #33–#37 (shipped) → stubs (556→360 LOC); tasks.md Phase I detail → stub (434→320); status.md rolled off blog/dashboard era 2026-06-24→28 (225→62). docs.md §5 amended to permit status roll-off (MOVE not delete). Archives gitignored.
- 2026-06-30 feat(rankings): market_rankings viewer ENG DONE — new `GET /v1/api/rankings?scope=global|sector&sector=&limit=` (raw SQL over market_rankings, MAX(as_of) latest week, distinct-sector filter list, table-absent guard→data:null, 24h cache) mounted at controllers/index.ts. Client: `getRankings` action/Zod schema + `types/rankings.ts` + `/rankings` page (force-dynamic, searchParams scope/sector) + `RankingsTable`(server, compact $T/B/M fmt) + `RankingsFilter.client`(global/sector toggle + sector dropdown, URL-driven nav). Nav: `buildNavLinks`→3 finance links; desktop TopBar flat links→**MARKETS dropdown**(Dashboard / SEC 13F Funds / Marketcap Rankings), mobile NavMenu lists 3 flat. Gates: server types 0 · client types 0/lint 0. NB: DB currently has 1 weekly as_of (~1007 rows); no migration needed (0012 already applied).
- 2026-06-30 feat(rankings): Phase 1 time-travel period picker ENG DONE (architect+PM design; funds `?period=` mirror). market_rankings can't be backfilled (snapshot-only, Decision #40) → phased: P1 now = week picker, P2 (gated ~8 weekly snapshots) = sampled rank-flow bump chart (reuse funds RankFlowTable, symbol-keyed). P1: `GET /v1/api/rankings` + optional `asOf` (nearest-≤ resolve, fallback latest) + `availableWeeks[]` in response; client action/Zod/type +asOf/availableWeeks (AssertEqual green); `RankingsFilter` +week `<select>` (latest stays UNPINNED→live, older week pins `?asOf=`; scope/sector/asOf co-preserved in URL); page reads searchParams.asOf + single-period note "unlocks as weekly snapshots accumulate". No migration/index (P2 adds idx_market_rankings_scope_asof 0019). Gates: server types 0 · client types 0/lint 0. DB has 1 week today→picker disabled+note.
- 2026-07-01 chore(collector): 13F archive env-var semantic rename + dedicated `sec-archive` bucket + parquet-upload bugfix + backfill UX. Toggles → `SHOULD_COLLECTOR_ARCHIVE_{MARKET,SEC}_PARQUET`; buckets → `COLLECTOR_{MARKET,SEC}_PARQUET_BUCKET`; 13F cold archive split to its own PRIVATE `sec-archive` bucket (was sharing `market-archive`). BUG fixed: bucket getters `os.getenv(k, default)` returned '' for a set-but-empty GH var (empty≠unset) → 13F Storage upload non-2xx to an empty bucket; now `os.getenv(k) or default`. `collector-backfill.yml`: +`cik` input, `python -u` (live logs), `dry_run` default true→false (LIVE — backfill is idempotent + DB-window-gated so safe). GH vars re-set to the 4 new names. Berkshire 2013 LIVE backfill OK: DB 13 periods / holdings 2060 / 0 failed (429 was transient); L17 db-window gating verified (Parquet full history, DB last 3yr). pytest 226. commits f53b8b4/4840c31/ebc6527. Phase L L17/L20 done; L21 sector-enrichment deferred.
- 2026-06-30 fix(rankings): prod "/rankings unavailable" incident — root cause = split-project deploy race: client shipped required `availableWeeks` Zod field before Express added it; Next Data Cache pinned old-shape body → parse reject (default URL failed, `?asOf=` fresh-key rendered fine → confirmed stale cache). Fix: `availableWeeks` → `.default([])` (tolerant additive parse) + redeploy clears Data Cache. Verified default /rankings recovered (NVDA + single-period note). NB error_log source='ssr' rows (reportSsrError) carry only message+path+source by design — digest/stack/user_agent NULL (those are browser/express sources).
- 2026-06-30 docs(plan/tasks): rankings 작업 Phase O로 접기 — plan.md Phase O 블록(P1 SHIPPED / P2 DEFERRED ~8주 게이트) + Decisions #41; tasks.md Phase O(O1–O5 done, O6–O9 deferred). market_rankings 백필 불가(#40)라 rank-flow는 히스토리 누적 후 단계.
- 2026-07-01 fix(collector): 13F amendment-type-aware ingest (commit 51ef82a). CONFIRMED bug — a `13F-HR/A` with `amendmentType=NEW HOLDINGS` (partial: confidential-treatment-expired positions, NOT a restatement) was superseding the full original because ingest kept only the latest filing_date per period. Berkshire 2025-03-31 served 5 holdings/$1.1B vs real ~110/$258.7B (amendment total to the dollar); also 2023-Q4/Q3, 2020-Q3, any fund w/ such amendments; hit backfill AND monthly. Fix: parse amendmentType from primary_doc.xml (new pure `parse_amendment_type` + `fetch_primary_doc` for 13F-HR/A; FilingRef +amendment_type/primary_document); pure `reconcile_period_filings` (base=max filing_date among original/RESTATEMENT; live=base ∪ NEW HOLDINGS filed ≥ base; supersede rest); shared `ingest_period()` for run_backfill + run_monthly (monthly drops single `latest_13f` → full period group); `supersede_period_others(cik,period,keep_accessions)` soft-deletes only non-live accessions; read sums live holdings per period (NEW HOLDINGS disjoint from original). New sec_reconcile.py + sec_period_ingest.py. Gate: pytest 241. USER: re-backfill affected funds/periods to revive tombstoned originals (`collector-backfill.yml -f cik=… -f since=2013 -f archive=true`).
- 2026-06-30 feat(rankings): 심볼 아래 회사명 (Yahoo longName) ENG DONE — market_rankings엔 이름 없음 + tracked_symbols.label=티커(run_weekly.py:132)라 소스 부재 확인. Yahoo `.info`(sector와 동일 호출)에서 longName 캡처: 신규 `fetch_sector_and_name` + `FundamentalsRow.long_name` + db_sink upsert/read(+`_has_long_name_column` 프로브, 0018 미적용 tolerant) + `plan_refresh`가 `long_name IS NULL`이면 `.info` 리페치 강제(1 주간런에 전 유니버스 backfill). 마이그레이션 0018(`symbol_fundamentals.long_name`, MANUAL). Express `GET /api/rankings` LEFT JOIN(정보스키마 컬럼 프로브로 조건부, deploy race 안전) → `name`; 클라 type/schema(`.default(null)`)/RankingsTable(심볼 밑 muted, name==symbol이면 생략). 게이트: collector pytest 242(신규 1)·ruff 0 · server types 0 · client types 0/lint 0. USER 게이트: 0018 수동 적용 + collector-weekly dispatch 1회(이름 채우기, ~1007 `.info` 콜).
- 2026-07-02 fix(category): 카테고리 필터 대소문자 불일치 수정 (?categories=rust → 0건 버그). 필터 값은 클라/서버 양쪽 소문자 강제인데 저장 title은 `"Rust"`(blog_agent Python 경로만 미정규화)라 Postgres 배열겹침 `&&`(대소문자 구분) 불일치. blog_agent category.py insert에 `strip().lower()` 추가(TS·태그 컨벤션 일치) + 마이그레이션 0021(기존 행 소문자 백필, 충돌 가드·멱등, MANUAL). 프로덕션 백필 실행(1행 `Rust`→`rust`, 충돌 0) → 피드 쿼리로 "Rust Config" 반환 검증.
- 2026-07-02 fix(collector): market_history 주말 가짜 캔들 제거 + 재발방지. 진단: TQQQ 차트 톱니 원인 = 비-24/7 종목(TQQQ·SOXL·S&P 500·KOSPI·KOSDAQ·EUR/USD)에 각 72개 가짜 주말 일봉(2025-08-30~2026-05-03, 2026-06-26 `run_daily --full` 단일 백필). 원인: yahoo_fetch→to_history_rows→load_history 경로에 거래일/자산군 필터 부재로 yfinance가 반환한 가짜 주말 바를 그대로 UPSERT(암호화폐 BTC/ETH/SOL 주말행은 24/7이라 정상). 평일값은 웹(stockanalysis)과 센트 단위 일치 확인. 정리: 프로덕션 하드 DELETE 432행(암호화폐 2782행 보존, market_history엔 deleted_at 없음). 코드: transform/ohlc.py에 순수 `allow_weekends` 필터(주말 bar 드롭, 기본 False) + run_daily 양 호출부에서 `e.category=='crypto'`만 True 전달. pytest 259 green·ruff 0.
- 2026-07-02 feat(Phase P): P10 momentum+lowVol 스코어항 DONE — deferred price-signal 2항 배선. 신규 순수 `helpers/priceSignals.ts`(`computeRawMomentum` 12-1M Jegadeesh-Titman skip-month[close t-21/t-252-1, ≥253봉] + `computeAnnualizedVol`[일간 로그수익률 stdev×√252, ≥60ret] + `percentileRank`[횡단면 정규화, ties=strictly-less, 단일=0.5]). `screenScore.ts` momentum(0.3)/lowVol(0.1) optional [0,1] 입력 수용, null이면 합산 제외(부분점수 계약 하위호환 유지, 스펙 고정 가중치라 미정규화). `controllers/securities/screen.ts` presence 프로브에 market_history 추가 → 후보 ticker들의 1d 종가 ANY() 1쿼리 조인→ticker별 그룹→raw momentum/-vol→후보군 percentile→computeScore. tracked universe 밖/이력<253봉은 null(graceful). filing_date lookahead 게이팅은 별도 deferred(라이브 가격 모멘텀은 룩어헤드 아님). 게이트: 타깃 jest 44/44(priceSignals 15 신규 + screenScore 확장 + screen price-path 케이스) · check-types 0. 전체 jest 4-fail은 postsByTag 통합(실 DB 필요, 무관). 브랜치 정리: 머지된 feature/phase-p-{nav-links,signals,symbol-price} 3개 삭제.
- 2026-07-02 feat(Phase P): /screener에 momentum+lowVol 노출 — 백엔드 스코어항(7355ad5) 프론트 배선. `types/screener.ts`+`getScreener.schema.ts` ScoreComponents momentum/lowVol `null`→`number|null`(z.number().nullable(), AssertEqual 유지). `app/screener/page.tsx` Momentum·Low-Vol 컬럼 추가(0–100%, tracked universe 밖은 "—") + 스코어 공식 노트 갱신(0.4c+0.3m+0.2t+0.1v). 게이트: client check-types 0 · lint 0(prettier fix). 커밋 82256e1. main ff-merge+push. 머지된 phase-p-{price-signals,screener-price-cols} 로컬 브랜치 삭제.
- 2026-07-02 feat(Phase P): /symbols 가격 차트 확장 — 기존 240×64 스파크라인을 전폭 area 차트로. 신규 `components/symbols/PriceChart.tsx`(의존성 없는 인라인 SVG, 서버 컴포넌트, 순수; 기존 `normalizeSpark` 지오메트리 재사용): area 그라데이션 채움 + min/max 기준선·우측 가격 라벨 + 마지막 종가 마커 + 상단 현재가/변동%(색+▲▼) + 하단 날짜축(first—last, Nd). `/symbols/[ticker]/page.tsx` PriceChart로 교체(섹션 max-w-xs→max-w-2xl). 구 `PriceSparkline.tsx`→`x_PriceSparkline.tsx` 소프트삭제(유일 사용처 교체됨). byTicker priceHistory(최대 260 1d봉) 그대로 사용, 미추적 심볼은 fallback 노트. 게이트: client check-types 0 · lint 0(prettier).
- 2026-07-02 fix(dashboard/fx): FX 캔들 "모두 초록" 수정. 원인=Yahoo FX 일봉이 2022~2025 구간 Open==Close(연도별 78~100%, H/L/C는 정상)로 도지→close>=open이라 전부 초록. 소스 결함이라 재수신 불가(신선 데이터도 동일 확인). 수정: 순수 util synthesizeFxOpen(시가=전일 종가, H/L 클램프; FX 연속시장 close-to-close 관례)를 ChartPanel에서 snapshot.fx.items로 FX 판별 후 daily에 적용(interval 집계 전). 라인차트 전환은 시리즈타입·지표·드로잉 대수술이라 배제. client check-types 클린(screener Link 에러는 타 세션 미커밋 건).
- 2026-07-02 feat(Phase P): screener 2티어 정렬 (PM+architect 공동판정 Option C). 문제=부분점수(가격이력 無, max 0.6)와 완전점수(max 1.0)가 한 리스트에 섞여 tracked universe 소속만으로 약한 종목이 강한 종목을 앞섬. 판정=점수 공식(순수 computeScore) 무변경, 정렬만 데이터가용성 2티어로 분리(품질서열 아님). 신규 순수 `helpers/screenSort.ts`(`hasPriceSignals`=momentum&&lowVol 둘 다 present[한쪽만이면 partial 티어로 귀속→티어1 완전비교가능 보장] / `compareScreenCandidates`=가격신호완비 우선→score DESC→holderCountActive DESC) + jest 신규 6. `screen.ts` 인라인 sort→`compareScreenCandidates`, candidate에 `hasPriceSignals` 방출. 클라 `types/screener.ts`+`getScreener.schema.ts` `hasPriceSignals:boolean`(AssertEqual 유지) + `/screener` 2섹션 렌더("Price-signal candidates" / "Consensus-only", 중립 데이터가용성 카피, `ScreenerTable.tsx` 추출로 마크업 DRY). 게이트: server 타깃 jest 52/52 · check-types 0 · 내 client 파일 types/lint 클린. 주의: 공유 main에 타 세션 미커밋(synthesizeFxOpen.ts untracked tsc err + ChartPanel.client.tsx prettier warn) 있음—내 스테이징에서 제외.
- 2026-07-02 fix(collector): 비거래일 가짜 바 가드 2차 — US 합의 기반 고립-날짜 탐지(로그-only) + market_history 전면 재구축. 진단: 2026-06-26 `--full` 백필의 일시적 yfinance 글리치가 주말+공휴일 가짜 바 주입(신선 재수신엔 없음 확인). 조치: (1) 비-암호화폐 148종목 `run_daily --full` 재적재(history=1.34M) 후 `updated_at<T0` 가짜 66행(11라벨) 삭제 — 주말 필터 후 남은 공휴일 아티팩트. (2) 신규 순수 `detect_isolated_bars`(US stocks ~110 합의, present<0.5·active면 suspect) + `db_sink.insert_error_log`(source=collector) + `run_daily --full`에서 로그-only 경보. KRX(2심볼)·FX(4)는 합의 불가라 DEFERRED(00.tasks DQ2, 거래캘린더 필요). pytest 264(+5)·ruff 0.
- 2026-07-02 fix(Phase P): /symbols 전종목 "not found" 회귀 수정 (by-ticker holders 쿼리 statement timeout). 프로덕션 진단: `GET /securities/:ticker/by-ticker`가 모든 티커에 data:null(catch가 에러 삼킴). 원인=2013 백필로 sec_holdings 208,614행 + Query B가 무거운 두 뷰(`v_sec_holdings_enriched` × `v_sec_position_delta`)를 LEFT JOIN→플래너가 delta 뷰를 전 홀딩에 계산→timeout. 회귀 트리거=0019/0020 적용 전엔 뷰 부재로 Query B skip(랭킹만으로 페이지 동작)했으나 적용 후 실행되며 터짐. 개별 측정: holders(no delta) 6행 OK · delta(cusip 필터) 70행 OK · 조인 결합만 timeout. 수정: byTicker.ts Query B의 delta LEFT JOIN 제거→holders(enriched 뷰, sector 유지)와 delta(`v_sec_position_delta` WHERE period+cusip IN security_map) 2개 빠른 쿼리로 분리, (cik,cusip) 키로 Node 병합. prod 검증: 새 delta 쿼리 5행 실데이터 반환. 게이트: byTicker jest 13/13 · check-types 0. 배포 후 /symbols/AAPL·GOOGL 실데이터 확인 필요.
- 2026-07-02 perf(Phase P): /screener 쿼리 속도 개선 (~6.3s→~1.7s, 프로덕션 측정). 2013 백필 후 느려짐. 병목 2개 prod 계측: (1) `SELECT MAX(period_of_report) FROM v_sec_consensus`가 208k 홀딩 전체 consensus 뷰를 계산해 2285ms → registry-filtered `sec_holdings` MAX(같은 put_call/prn 필터)로 714ms(candidate 쿼리는 여전히 v_sec_consensus를 period 필터). (2) 가격신호 쿼리가 141개 티커 전체 이력(386,223행) fetch, 3412ms → 티커별 최근 260봉 ROW_NUMBER 캡으로 480ms/10,140행(idx_market_history_label_interval 활용). 260봉 캡은 momentum(12-1M)·vol이 최근 253봉만 참조(오프셋이 끝에서 계산)라 **출력 완전 동일** — 순수 성능. byTicker Query C는 이미 LIMIT 260. 게이트: server jest 52/52 · check-types 0. NB: 엔드포인트 s-maxage=86400이라 캐시 히트는 즉시; 개선은 콜드 로드.
- 2026-07-02 perf(Phase P): /symbols by-ticker 속도 개선 (~1.6s→~0.8s warm, architect 설계). 병목=holders의 delta 쿼리 1188ms — `v_sec_position_delta` 뷰가 208k 홀딩 전체를 분기마다 GROUP BY(2회) 계산. architect 판정: 뷰 버리고 sec_holdings 집계로 대체(≤6 holder cik × 2분기, idx_sec_holdings_cik_period 스코프). architect의 단일 OR-both-periods 쿼리는 실측 1098ms(동적 prev가 인덱스 방해)라 **정적-latest 등식 + prev-per-fund-MAX ppjoin 2쿼리**(Promise.all)로 조정(측정 361ms→병렬 ~237ms). 신규 헬퍼 `helpers/holderWeights.ts`(getHolderWeights: weight=cusip값/펀드 전체 13F가치, delta=latest−prev, isNew=prev에 미보유; 주식클래스 cik당 fold) 추출로 byTicker.ts 238 LOC. **정확성 검증**: GOOGL 5개 holder 전부 기존 뷰와 weight/Δ/isNew 소수4자리 동일. probe에서 has_delta 제거. 게이트: byTicker jest 13/13 · check-types 0.
- 2026-07-02 style(funds): /funds 랭크플로우 선택 강조에 스포트라이트 추가. 요청=선택 블록 배경을 테마 primary로 반전(다크→밝은/라이트→어두운). 진단: 셀 텍스트색이 cusip 색(L48%)에 고정(대부분 white, 노랑계열 #111)이라 배경만 반전하면 한쪽 그룹 글씨 소실 + 단색화가 bump 색-추적 정체성 파괴 → 반전 배경 기각. 대안: 선택 셀은 cusip색 유지(legibility 무변), 비선택 셀 `opacity-35 saturate-50`로 페이드(HoldingCell isDimmed prop, RankFlowTable 양 컬럼서 activeCusip!==row.cusip일 때 true). transition-shadow→transition-all, 선택셀 z-10. 게이트: 터치 파일 check-types 0(기존 무관 에러 1건 getSymbolDetail).
- 2026-07-02 fix(Phase P): /symbols 특정 티커(AMD/KO/AVGO 등) "not found" — 프론트 Next Data Cache에 박힌 stale null. 원인: by-ticker가 백엔드 timeout 시절 반환한 data:null을 `getSymbolDetail`의 `next:{revalidate:86400}`이 24h 캐시. 백엔드 수정 후에도 프론트(별도 Vercel 프로젝트, 백엔드-only 커밋으로 재빌드 안 됨)가 stale null 서빙. 백엔드 API는 캐시버스터로 확인 시 전부 정상(x-vercel-cache HIT age:0). 수정: `getSymbolDetail` fetch를 `cache:'no-store'`로 — Express 자체 CDN 캐시(s-maxage=86400)에 의존해 속도 유지하되 이중 캐시의 stale-null 함정 제거(백엔드 일시 실패가 24h 박히는 문제 근절). 이 프론트 커밋이 재배포 트리거→기존 Data Cache 전부 purge. 게이트: client check-types 0 · lint 0. 유사 패턴(getScreener/getRankings/getFundRankFlow revalidate)은 리스트 반환이라 per-key null 위험 낮음—필요시 후속.
- 2026-07-02 fix(funds): /funds/[cik] 모바일 비호환 해소. 원인: 좌측 사이드바 `w-64 shrink-0`가 375px에서도 256px 고정 → 본문 87px, rank-flow 컬럼 `min-w-[130px]` 못 들어가 즉시 가로스크롤. 컨테이너 `flex gap-8` 반응형 분기 전무. 수정: aside를 `hidden md:block`, 컨테이너 `md:flex md:gap-8`(모바일 단일 컬럼), 신규 클라이언트 `FundNavDrawer.client.tsx`(좌측 Sheet, side="left", w-80 max-w-[85vw], 검색·스크롤 가능—펀드 수 증가 대비 드롭다운 대신 드로어 채택). FundList는 children 주입으로 서버렌더 유지, 링크 탭 시 onClick으로 드로어 자동 닫힘. empty-state 분기도 동일 처리. RankFlowTable 스크롤 래퍼 `snap-x snap-mandatory`+컬럼 `snap-start`로 분기 스와이프 스냅. 게이트: client check-types 0 · lint 0.
- 2026-07-02 plan(Phase Q): 미 의원 STOCK Act 거래공시(PTR) 수집→"traded by N politicians·90d" 투명성 신호 설계 확정(3-agent: research+architect+ml, PM fable 문서화). 소스=Finnhub premium(유일 상업재배포 라이선스; Stock Watcher 사망·Quiver/FMP/UW 표준티어 재표시불가·Senate efdsearch 상업금지 → 기각). 데이터=소유아닌 라이선스(source='finnhub'+purge-on-cancel, Parquet 아카이브 없음). 스키마=0022 politician_registry(bioguide_id, congress-legislators CC0 collector-seed)+politician_trades(EVENT only, value 11브래킷 min/max/est, disclosure_date 기준, filer_owner, is_amendment, soft-delete)+뷰 2종(security_map-by-ticker 재사용, OpenFIGI 없음). 신호=disclosure-date 기준·"traded by"(보유 아님)·count>dollars; MVP headline traded_by_count(90d)+buy/sell 인원+freshness; 점수 Q7 DEFERRED(13F 스코어와 미합산). 강제 disclaimer(30–90일 지연·브래킷·not advice). Phasing Q1 스키마→Q2 시드→Q3 인제스트→Q4 심볼집계(첫 노출)→Q5 스크리너+검증게이트=MVP; Q6 드릴다운/Q7 점수 후속. OPEN(USER-gated): Finnhub 라이선스 raw-재표시 여부·티어/과금·응답 필드 실측. 00.plan.md/00.tasks.md Phase Q 추가(순수 additive, 삭제 0). 코드 미착수.
- 2026-07-02 plan(Phase Q): 소스 결정 반전 — Finnhub 유료 폐기, MVP 소스 = kadoa-org/congress-trading-monitor(MIT, 무료, 매일, House+Senate+행정부 57k+건 라이브) Supabase 미러링(핫링크 금지, SLA 없어 staleness 가드+후속 House 벌크 자가파싱 fallback). 리서치 핵심: 상업이용 제한은 웹 약관이 아니라 연방법 5 U.S.C. §13107(c)("news 예외 외 상업목적 금지")로 데이터에 따라붙어 유료 구매로도 해소 불가 → 유저 결정=무료·뉴스성 유지(유료화 전에만 법률검토). 00.plan/00.tasks Phase Q 수정(source='kadoa', API 키/구독 불필요, Q3 finnhub→kadoa fetch, Q6 raw 표시 허용, OPEN=kadoa 필드실측/SLA완화/§13107). 코드 미착수.
- 2026-07-02 feat(Phase Q): 미 의원 STOCK Act 공시 → "traded by N politicians·90d" 신호 Q1–Q5 ENG 구현(개발자 디스패치 사이클). Q1 마이그레이션 0022(politician_registry filer_id PK + politician_trades external_id UNIQUE + 뷰 v_politician_trades_enriched/v_politician_activity, SSOT 양측 미러 in-sync). Q2/Q3 collector: kadoa `trades.json` 미러(순수 transform/politician_parse + kadoa_fetch 키불필요 + politician_db_sink ON CONFLICT idempotent + run_politician_trades --dry-run + collector-politician-trades.yml 매일). Q4/Q5 serving: byTicker/screen presence-guarded(to_regclass, 뷰 부재 시 regression 0) + symbolDetail/screener DTO 관대파싱 + PoliticianActivity 패널("Traded by" 배지·freshness·상시 STOCK Act disclaimer·중립 party). architect 검증=FIX-FIRST: H2(빈 filer_id FK abort) 파서 가드+spec, M3(ETF 면제 문구 모순) 수정, H1(filer_id≠bioguide over-count) 문서정합+SQL주석, M2/M4 정리 — 전부 반영. 게이트: collector pytest 312 · server jest 255/255 · check-types 0 · client tsc/lint 0 · check:schema in-sync. USER-gated 잔여: 0022 수동 apply + workflow 1회 dispatch + Q5.3 검증(스팟체크/분포) green 후 프로덕션 노출. Q6/Q7 후속.
- 2026-07-02 feat(Phase Q): LIVE. USER가 0022 apply → `collector-politician-trades` dispatch(dry-run 검증→live 성공): rows_ingested=5000 distinct_filers=125 unresolved_ticker_pct=42.1% feed_staleness_days=1. Q5.3 검증 green — 배포 API(/v1/api/securities/:t/by-ticker) 스팟체크 분포 sanity: MSFT 14·NVDA 12·AAPL 11·JPM 7·TSLA 3·KO 2·WMT 1·unknown=null (heavy right-skew), net_direction/cluster_flag 일관(≥3만 cluster). 심볼페이지/스크리너 "Traded by N politicians·90d" 라이브. 잔여: Q6 드릴다운·Q7 점수 후속.
- 2026-07-02 feat(funds/mobile-nav): 햄버거 드로어에 페이지 보조메뉴 chooser 도입 — pageMenuAtom + PageMenuRegistrar.client(페이지가 메뉴 등록/해제)로, 페이지 메뉴 존재 시 햄버거 클릭→"SITE MENU / <페이지메뉴>" 버튼 2개 선택 후 열림(없으면 기존처럼 사이트메뉴 직결). MenuDrawer에 root/site/page 뷰+뒤로가기. /funds/[cik]는 인라인 FundNavDrawer 트리거 제거→PageMenuRegistrar(label="Funds")로 FundList 등록, FundNavDrawer 소프트삭제(x\_). 게이트: client tsc 0 · eslint 0.
- 2026-07-02 feat(Phase Q ext): per-politician holder breakdown + 분기 타임라인 Q9–Q12 + Q6.2–6.4 ENG 구현(개발자 디스패치 사이클). Q9 마이그레이션 0023(registry ALTER 6컬럼 + 뷰 v_politician_ticker_holders/v_politician_filer_timeline, SSOT 양측 미러) + politicianHolderShares(holderWeights 2-쿼리 클론) + by-ticker politicianHolders[](sibling) + 심볼 페이지 holder 테이블. Q10 filers.json enrich(photo/집계, 기존 잡 폴드, 트레이드-경로 upsert가 집계 컬럼 미클로버). Q6.2/6.3/6.4 /politicians/[filerId] API+페이지 + 스크리너 expand-row. Q11 backfill_politician_trades.py(filer/{id}.json 순회, 기존 파서 재사용, per-filer 격리, 일회성 워크플로). Q12 PoliticianTimeline(<150 LOC, 행=티커·열=분기). 정직성: "% of disclosed transaction volume"·밴드만·상시 disclaimer·중립 party. **architect FIX-FIRST 반영: C1(치명 — COUNT(DISTINCT) OVER는 불법 Postgres → 0023+데이터 시 양 엔드포인트 data:null; COUNT(\*) OVER로 수정), M1(타임라인 direction buy_skew→buy 어휘 정합), L1(Fragment key), L2(라벨 정확화).** 게이트: collector pytest 326 · server jest 290/290 · check-types 0 · client tsc/lint 0 · check:schema in sync. USER-gated: 0023 수동 apply + backfill 1회 dispatch → 분기 타임라인 자동 점등. M2(screen.ts 301코드라인) 경미 노트.
- 2026-07-02 feat(Phase Q ext): LIVE. USER가 0023 apply → backfill dispatch(dry-run 434/57736 검증→live) 후 실런타임 버그 2건 fix-forward: (1) InvalidSqlStatementName "\_pg3_x does not exist" = Supabase 풀러+psycopg3 auto-prepare 충돌(백필이 434 filer 반복 upsert) → db_sink.connect에 prepare_threshold=None + per-filer except에 conn.rollback (aborted-tx cascade 방어); (2) filer/{id}.json trade 행에 filer_name 없어(부모 객체에만) basic upsert가 enriched 이름을 빈값으로 clobber → basic SQL COALESCE(NULLIF) 순서무관화. 재백필 434/57736/0-fail. 검증: /politicians/house_josh_gottheimer = "Josh Gottheimer"(D/house/NJ) 547 티커 랭킹 + 2421 타임라인(38분기 2017–2026, buy/sell/mixed). by-ticker holders(Pelosi/Trump/Whitehouse share%·rank) 라이브. Phase Q 확장 MVP 전체 프로덕션 가동.
- 2026-07-02 feat(Phase Q Q8): 정치인 최다거래 티커 top-75 유니버스 편입 LIVE. 신규 promote_politician_tickers(순수 select: distinct-filer breadth DESC + tie-break, CUSIP-유사 거부, watchlist/tracked exclude, top-75) + db_sink.read_top_politician_tickers + dispatch-only 워크플로. 승격 dispatch(dry-run→live): candidates=3053 already_tracked=77 invalid=0 promoted=75(PFE/MDT/CVS/BLK/HON/LMT/UBER/ANET/REGN…) → tracked_symbols sticky upsert. run_daily 트리거해 신규 티커 OHLC/시총 수집(미지원 티커 graceful skip). additive·sticky·soft-delete 가역. pytest 353/353. Q7(political_interest_score)만 후속으로 남음.
- 2026-07-02 feat(Phase Q Q7): political_interest_score 스크리너 렌즈 구현. 순수 helpers/politicalScore.ts = (0.55·breadth(min(1,count/5)) + 0.30·consensus(max(buy,sell)/count))/0.85 ∈[0,1], count 0/null→null(0 미방출). notionalTier(0.15)는 90d notional 집계 필요라 DEFERRED(ml-spec partial-data 계약, 렌더 렌즈엔 무영향). screen.ts가 v_politician_activity의 buy/sell_member_count 노출 + 후보별 스코어 계산(13F score와 별도 렌즈, 미합산, 기본정렬 불변, presence-guard NULL::bigint fallback). 스크리너 "Political Interest" 컬럼(0–100% + 바, 정직 tooltip). 게이트: server check-types 0 · jest 14신규+32기존 green · client tsc/lint 0. Phase Q 전 항목(MVP Q1–Q5 + 확장 Q6/Q9–Q12 + Q8 + Q7) 완료 — notionalTier만 후속.
- 2026-07-02 chore(docs): 감사로 stale 체크박스 정리 — Phase P(P1–P10 security_map·figi·screener·/symbols·consensus)·Cycle 3 대시보드(22항목)·L13/L14(Phase P로 흡수)는 이미 구현/라이브였음(코드 확인) → 체크. 진짜 미구현 = Phase O.2 rank-flow(O6–O9, ~8주 데이터 축적 대기)·L12 SSOT 미러(경미).
- 2026-07-02 feat(Phase Q): bioguide enrichment LIVE — congress-legislators(CC0, current+historical 12,767) 매칭(last+state+chamber, first 디스앰비그) → politician_registry.bioguide_id. enrich_bioguide dispatch(live): congress_filers=355 matched=345 unmatched=10 match_rate=97.2%(executive는 설계상 NULL). over-count 재키잉·위원회 enrich 토대. + notionalTier LIVE: political_interest_score 전체 공식 복원 0.55·breadth+0.30·consensus+0.15·notionalTier(90d notional inline, 3-tier <$250K/…/>$5M; tier-only 노출). 스크리너 점수 재계산 확인(GOOGL 0.85). 게이트: collector pytest 384·server jest 315·check-types 0·client tsc 0.
- 2026-07-03 fix(funds): RankFlowTable 스크롤 gutter px-1→px-2/py-2·-mx-2 — 선택셀 ring-4+offset-2(~6px) 가장자리 잘림 해소(8eba89d); CI lint(prettier max-warnings 0) 재실패 → COMMITTEE_BADGE_TOOLTIP 포맷 픽스(c6544e5) green.
- 2026-07-03 docs(plan): Phase R–W 로드맵 추가(PM+ML 리서치 → architect 검증). U→R→S→T→W→V; R=event-study signal_backtest(0026)·U=v_sec_derivatives_exposure(0027)·V=gex_daily(0028)+ChainRow provider seam(문자열-키 레지스트리, ABC 금지)+Parquet 콜드. 핵심 gap = 4시그널 forward-return 미검증. 전부 free-tier, futures drop(YAGNI).
- 2026-07-03 docs: 완료 phase 아카이빙(tasks 512→331·plan 447→416, 열린태스크 33 보존, \_docs/archive/ 콜드), Phase R–W 로드맵 커밋(1d7d3ed).
- 2026-07-03 feat(Phase U): 13F 옵션노출 복원 — 마이그레이션 0027 뷰 v_sec_derivatives_exposure(v_sec_positions 여집합, put_call IN PUT/CALL, call/put FILTER)+SSOT 미러. byTicker Axis 7 presence-guarded(helpers/secDerivatives.ts, CUSIP→ticker via security_map, netSkew)+프론트 스키마/타입/심볼페이지 "Options Exposure(13F)" 카드. prod 검증 CALL 358·PUT 126(top=BlackRock SPY $25.7B). 신규 수집 0. 게이트: server jest 320/320·check-types 0·client tsc 0·lint 0. USER: 0027 수동 apply 대기.
- 2026-07-03 feat(Phase R): event-study 검증 하네스 — 순수 `transform/event_study.py`(event_window_start=filing_date+1거래일·CAR t+1/5/21·directional_hit, 23 pytest)·마이그레이션 0028 observation-level `signal_backtest` + roll-up 뷰 `v_signal_backtest_summary`(mean_car·hit_rate·n·t_stat 쿼리타임)·`db_sink.read_returns_window/read_signal_events/upsert_signal_backtest`·entrypoint `run_signal_backtest`(--dry-run/--limit/--since). 신규수집 0. 검증(실DB dry-run): politician 축 189 관측치; 13F 조인 버그 수정(accession→cik+period, 뷰에 accession 없음) 후 여전히 delta-뷰 statement_timeout → 13F graceful-skip(R.6 materialized 후속). 게이트: collector pytest 418·ruff clean. USER: 0028 수동 apply 대기.
- 2026-07-03 fix(Phase R): backtest 라이브 run 견고화 — 행별 commit(6744행 장시간 연결→pooler "connection is lost") → 청크 executemany(500/commit·청크 rollback 격리); 13F 프로브 statement_timeout 15s fast-fail(tx abort 시 자동 복원, write 무영향). 재실행 성공 6744 obs upsert. 게이트: pytest 418→ruff clean.
- 2026-07-03 analysis(R.5): signal_backtest raw CAR 6744 obs → politician_buy 5d/21d t=5.80/5.58·hit 57.9%/55.2% **t>3 강유의**(Ziobrowski); politician_sell mean_car 양수·hit<50% 하방예측력 無(Belmont); 1d 노이즈. ⚠ raw=market beta 포함 → Phase S sector-neutral 재측정이 정직 alpha. Phase W 가중치 입력.
- 2026-07-03 feat(Phase S): 섹터-상대 초과수익 — 순수 `transform/sector_benchmark.py`(126 LOC, 시총가중 섹터인덱스 t0고정가중·결측멤버 재정규화)+`db_sink.read_sector_members`/`read_symbol_sector`(83줄). R `cumulative_abnormal_return(benchmark_series=)` positional 정렬로 삽입. `run_signal_backtest --sector-neutral`(기본 off, `_resolve_benchmark` 섹터미상 raw fallback never-crash). 신규테이블 없음(in-harness). 게이트: collector pytest 436(+18)·ruff clean·전 파일 ≤300.
- 2026-07-03 analysis(S/R.5 핵심): sector-neutral 재측정(5682 uniq obs) → **politician_buy 유의성 붕괴** 5d t 5.80→0.83·21d 5.58→0.71(알파≈0). raw t~5.8은 전부 market beta(20–25 강세장)였음, 종목알파 아님. politician_sell 21d는 +3.45→**−2.64**(음수·hit 56.6%=약한 하방스킬, |t|>3 미달). **결론: sector-neutral 기준 t>3 통과 시그널 全無** → 하드코딩 가중치(0.55/0.30/0.15·0.4/0.3/0.2/0.1) 무근거 실증(McLean-Pontiff). Phase W = 회의적 소가중치+매수 과대가중 금지. ⚠ 13F축 여전히 미측정(R.6). signal_backtest 현재 sector-neutral 값 보유.
- 2026-07-03 feat(R.6): 13F 축 활성화 — 마이그레이션 0029 materialized view `mv_sec_new_positions`(is_new+ticker+filing_date, WITH NO DATA+인덱스) → db_sink 13F 쿼리 인덱스스캔 교체 + `refresh_new_positions`(600s) + `run_signal_backtest --refresh-13f`. v_sec_position_delta 자기조인 timeout 우회. presence-guard. 적용됨(0029). 13F t-stat 실측정 진행중.
- 2026-07-03 feat(Phase T): lead-lag — 순수 `transform/lead_lag.py`(156 LOC, lagged_cross_correlation·optimal_lag·build_aligned_series) + `entrypoints/run_lead_lag.py`(분석전용, R 리더 재사용). 22 신규 pytest. 시그널 공시→가격 최적 진입시차 측정 도구.
- 2026-07-03 feat(Phase V-collect): 옵션 GEX 파이프라인 — 순수 `transform/gamma.py`(138 LOC, BS γ + net GEX Σγ·OI·100·spot²·0.01, 콜+/풋− dealer-centric)+`schema/chain_rows.py`(ChainRow/GexDailyRow)+`sink/chain_provider.py`(문자열키 provider seam, PROVIDER_YFINANCE, ABC없음)+`sink/gex_sink.py`+`entrypoints/run_gex.py`. 마이그레이션 0030 `gex_daily`(파생스칼라만, raw 미저장)+SSOT 미러. 35 신규 pytest. 게이트: collector pytest 493(+57)·ruff clean. 잔여: V.4 Parquet·V.5 yml·V.6 대시보드 표시.
- 2026-07-03 analysis(R.6 완성): 13F 축 실측정 — **13f_new_position 21d sector-neutral t=3.15**(mean +1.88%·hit 57.4%·n=244) → **유일하게 t>3 통과**. politician_buy(t=0.71)·sell(t=−2.64) 대비 기관 13F 신규포지션만 진짜 알파(Copycat/Best-Ideas 정합, 정치인은 Belmont). Phase W 결론 역전: 정치 시그널 감량·기관컨센서스 가중.
- 2026-07-03 fix(R.6 성능): mv_sec_new_positions REFRESH >600s timeout → 마이그레이션 0031 재정의(security_map mapped-CUSIP 필터 CTE로 down-push, 자기조인 축소) → ~5s(630행/220티커). WITH DATA. 적용됨(31). + db_sink/entrypoint `--signal-source {all,politician,13f}` 필터(sector-neutral per-event 쿼리가 5630건에 연결소진 → 13F만 630건 격리 측정). 게이트: ruff clean.
- 2026-07-03 feat(SEO): dashboard_client_web 메타데이터·JSON-LD 정비 — sitemap.ts에 /rankings·/funds·/screener + 전 /funds/[cik](getFunds 열거) 추가; 블로그글 generateMetadata(공통 title 중복 해소)+BlogPosting/Breadcrumb JSON-LD; /funds·/symbols·/politicians 상세 동적 title+self-canonical+notFound(soft-404 수정)+Dataset JSON-LD; 루트 title템플릿(%s | Taek Lim)+SITE_URL상대 OG(하드코딩 my.prettylog.com origin 제거)+WebSite/Person JSON-LD(sameAs 실 LinkedIn/GitHub); resume/about ProfilePage; screener 필터변형 robots noindex; public/ads.txt(pub-7476929778409534). 신규 lib/jsonld.ts(순수 6빌더)+components/seo/JsonLd.server.tsx(</script> XSS 이스케이프). 잔여: /symbols·/politicians sitemap 열거용 백엔드 list 엔드포인트·symbols/politicians 300LOC 분리. 게이트: client tsc 0·lint 0(prettier)·server jest 320/320. USER: GSC my.prettylog.com 속성+sitemap 제출·Vercel env(GOOGLE_SEARCH_ENGINE/ADSENSE).
- 2026-07-03 feat(Phase W): 스코어 캘리브레이션 — architect+ml-engineer 합동판정으로 **가중치 동결**(단일-이벤트 t=3.15는 합성 가중치 출처 불가; t는 존재허들·계수 아님). computeScore 0.4/0.3/0.2/0.1 byte-불변. 13F 신규포지션 breadth→`newPositionBreadth` 진단 컴포넌트(가중합 미포함)+동점 tiebreak(순서만); 정치 렌즈 advisory-only 강등(`advisoryOnly:true`, buy t=0.71/sell −2.64, 랭킹 미투입); `mv_sec_new_positions`(0031) presence-gated 조인(부재→null degrade). 실제 재가중 게이트=합성 랭킹 백테스트(W.2 후속). 게이트: tsc 0·jest 38 suite/342 통과(+22). SHIPPED main.
- 2026-07-03 feat(V.6): GEX 서빙 — `helpers/fetchGexDaily.ts`(72 LOC, presence-guard `to_regclass('gex_daily')`→부재 null, `deleted_at IS NULL` read-mask) + byTicker Axis 7 DTO `gexDaily`(부재→null·500 아님, 스코어 미편입) + 프론트 `getSymbolDetail.schema`/`types/symbolDetail`(AssertEqual). architect가 deleted_at 마스크 누락 지적→fix. 게이트: server tsc/jest 342·client tsc·lint green. SHIPPED main.
- 2026-07-03 feat(Phase O, O6–O9): rankings Flow 시계열 뷰 — 0032 인덱스(USER-apply 대기, perf-only)+SSOT; `controllers/rankings/rankflow.ts`(199 LOC, `GET /rankings/flow` DISTINCT ON date_trunc 샘플+ever-top-K union, granularity 화이트리스트 injection불가, data:null 가드); SSR `getRankingsFlow.action/schema`+`types/rankingsFlow`+`utils/rankFlowRows` symbol-키 제너릭(funds 함수 무변경); `RankingsFlowTable.client`(378 LOC, architect KEEP)+`RankingsViewToggle`+`/rankings` Snapshot|Flow 토글(MIN_WEEKS_FOR_FLOW=8 게이트). architect 두 기능 PASS. 게이트 전부 green. SHIPPED main.
- 2026-07-05 feat(V.5): `collector-options-daily.yml` — 평일 22:00 UTC run_gex, curated 유동성 세트(SPY/QQQ/IWM/DIA+메가캡 12) 기본 + workflow_dispatch tickers/dry_run 오버라이드. security-map 패턴(uv pinned SHA·DATABASE_URL·failure-issue). gex_daily 히스토리 누적 시작(후속 GEX 백테스트/스코어편입 전제; GEX 스코어 통합은 누적 후로 보류=오너 결정). USER: 첫 dispatch 확인.
- 2026-07-05 fix(blog): /blog/@user 포스트카드 날짜 React #418 하이드레이션 불일치 수정 — date-fns `format`(런타임 로컬 TZ)이 ISO-UTC post.date를 서버(UTC)·클라(KST) 다르게 렌더 → 자정 근처 yyyy-MM-dd 불일치. 순수 `utils/formatPostDate`(ISO 앞10자 슬라이스, TZ무관·결정적)로 교체(렌더경로 유일 date-fns 사용처). vitest 4/4·tsc 0. (16e2e18)
- 2026-07-05 feat(SEO-2): 커버리지 완성 — sitemap에 /symbols/[ticker](getRankings top-1000, 대문자=페이지 canonical 정합) + /politicians/[filerId](신규 Express `GET /v1/api/politicians` presence-guard 리스트 + getPoliticians action); /blog/@user generateMetadata(getUserProfile per-author title/desc, owner→/ canonical 보존); 동적 OG(next/og `lib/og.tsx` renderOgCard → blog/symbols/politicians/funds 4 라우트, blog 상세 og:image 중복 제거); 홈 metadata; hreflang SKIP(단일언어 확정). 게이트: 양앱 build·tsc 0·server jest 342/342·lint 0. (63eb56b)
- 2026-07-05 feat(blog): 소유자 private 글 가시성 + StatusBadge — private 글이 SSR 익명 fetch(토큰 미포워딩)라 소유자에게도 안 보이던 문제. 상세/TOC/카테고리사이드바 fetch에 Clerk `auth().getToken()` 포워딩(백엔드 detail·user-feed 라우트는 이미 `status='public' OR user_id=viewer` owner-aware; 카테고리 라우트 2곳에 optionalAuth+`OR user_id=$viewer` 추가, 익명 -1=출력불변); `/me` listDrafts `status IN ('draft','private')`; 🔒Private/Draft 배지(피드카드·상세·/me). 안전: `auth()`가 dynamic 렌더 강제→private 정적캐시 누수 없음, OG는 익명 유지. architect가 잡은 블로커(PostContentToc 토큰 누락→소유자 페이지 전체 에러) 수정. 게이트: server jest 342/342·양앱 build·client tsc·lint 0. (53196a8)
- 2026-07-05 plan(Phase X): 자산 투자 백테스트(`/backtest`) 3-agent 설계(PM+ML+architect) → 오너 2-결정 잠금(#42). (1) 데이터 선행 → 제대로 DRIP: market_history는 OHLCV만 저장(배당/분할 Parquet 콜드에만)이라 additive 컬럼+JEPQ/VOO/VOOG 편입+백필 선행(Slice 0); (2) 예측=성장(CAGR 복리곡선+몬테카를로 p10/p50/p90)+인컴(선택기간 실현 분배율 배당 현금흐름), 선형회귀 제외. 엔진=순수함수 client 재계산+시리즈 서버 fetch(ISR/Express batch, egress 캐시격리), 분수주 buy-and-hold, TQQQ 레버리지 경고. 코드 미착수 — 00.plan/00.tasks Phase X 기록만. ⚠착수전 DB 실측(이력깊이·ETF 적재).
- 2026-07-05 plan(Phase X X.0): DB 실측 완료 — market*history daily 깊이 2023-01-03~2026-07-02(~3.5년/877봉, 222 라벨). 대상 적재확인: QQQ·QQQM·QLD·TQQQ·SPY·SCHD + ^IXIC(NASDAQ Composite)·^NDX(NASDAQ 100) 존재; JEPQ·VOO·VOOG 미적재(X.1 편입 확정); dividends/stock_splits 컬럼 부재(X.2 마이그레이션 확정). 계획 가정 전부 확정, 백필 범위=신규 심볼 period=max(retention 5yr hot 내). 진단 스크립트 x_backtest_dbcheck.py(read-only, x* 소프트삭제).
- 2026-07-05 feat(Phase X Slice 0): 백테스트 데이터 기반 구현(X.1–X.3) — watchlist JEPQ/VOO/VOOG 편입(42→45); 마이그레이션 0034 market_history +dividends/+stock_splits(NUMERIC DEFAULT 0) additive + BOTH SSOT 미러; collector HistoryRow/ohlc(NaN→0)/db_sink INSERT 확장(이미 fetch 중인 배당/분할이 DB 도달); run_daily에 --symbols 스코프 필터(filter_universe 순수) 추가로 타깃 백필 가능(Phase M 크기 규율 준수). pytest 506 통과(신규 13). ⚠USER 게이트: (1) 0034 Supabase apply (2) 백필 `run_daily --full --symbols QQQ,QQQM,QLD,TQQQ,SPY,SCHD,JEPQ,VOO,VOOG`(--full=커서우회라야 기존 ETF 과거행 div/split upsert). 모듈경로=collector.entrypoints.run_daily. NB check:schema는 X.2 이전부터 blog_agent 인덱스 drift로 실패(별개 이슈). Slice 0 코드 완료, 프로덕션 미실행.
- 2026-07-05 feat(Phase X X.3b): USER가 0034 apply + 백필(--full --symbols 9개) 실행 완료(history=38574). 검증(x_backtest_verify): 9심볼 inception 이력 확보(QQQ 1999·SPY 1993·JEPQ 2022-05·TQQQ 2010), JEPQ 월배당 49행 월캐던스 정상($0.64/0.59/0.56…), TQQQ 분할 8·QLD 6 포착, div/split 컬럼 채워짐. 총수익/DRIP 실데이터 준비 완료 → Slice 1 착수. NB --full로 9심볼만 pre-2021 깊은 이력 보유(백테스트 Max 프리셋 확장 이득, 개별주는 2023+ 유지).
- 2026-07-05 feat(Phase X Slice 1 stage1): 백테스트 엔진+데이터경로 완료. X.4 Express seriesBatch.ts `GET /v1/api/dashboard/history/batch`(N종목 1왕복 label=ANY, ≤15, close/dividends/stock_splits, dashboard 캐시 미러). X.5 Next getPriceSeries api/mock/action(serverRequest ISR 24h, labels sort 캐시정규화, ok/error 유니온). X.6 순수 엔진 engine/metrics/projection/types(분수주·분할연속·DRIP 2경로·CAGR/MDD/변동성/Sharpe/누적배당·CAGR곡선+인라인 mulberry32 seeded 몬테카를로 p10/50/90+Yield인컴, Math.random 금지). 게이트: client check-types 0·vitest 115통과(신규16 필수6케이스)·Express build 통과. 다음 stage2=페이지/컴포넌트/nav(X.7-9)+X.10 실데이터 검증.
- 2026-07-05 feat(Phase X Slice 1 stage2+게이트): 백테스트 UI 완성 — 페이지 /backtest(+/preview) + BacktestClient 오케스트레이터(전 입력+seed URL-state 재현) + 컴포넌트 17종(BudgetInput·AssetPicker[universe.ts]·WeightSliders·DateRangePicker[^IXIC/^NDX 미니차트 클릭 선택]·DripToggle·ResultChart[recharts 실적선+CAGR점선+몬테카를로밴드]·MetricsPanel·DividendTable·IncomeProjection·LeverageWarning·Disclaimer) + nav(useNavLinks) + middleware/robots de-index. recharts 설치. X.10 검증 PASS: QQQ 2023-24 DRIP 엔진 95.78% ≈ Python재구현 95.782% ≈ yfinance AdjClose 95.788%(차 0.006pp), gate.test.ts 상시. 게이트: lint 0·check-types 0·vitest 117·Express/Next build 통과·전파일 ≤300LOC. Phase X MVP ENG DONE, uncommitted(커밋 지시 대기). x*backtest*\*.py 진단 3종 소프트삭제.
- 2026-07-05 plan(Phase X-ext): 오너 요청 후속 3종 잠금(미착수) — XE.3 지수 깊은 백필(^IXIC/^NDX --full로 DateRangePicker 2023 벽 제거·USER 명령·코드0), XE.1 DCA 적립식(월/년 정액 기여, 지속 현금흐름이라 CAGR 대신 XIRR money-weighted return 도입 필요·엔진+UI), XE.2 배당 라우팅(DRIP 토글을 same-asset/cash/다른자산 선택으로 확장, JEPQ 배당→VOO). 착수순서 XE.3→XE.1→XE.2.
- 2026-07-05 plan(Phase X-ext architect 검증): system-architect가 XE.1(DCA)/XE.2(라우팅) 설계를 shipped 엔진 대비 검증. 문서에 없던 함정 확정: (a) 변동성/Sharpe/몬테카를로 μσ에 Modified-Dietz flow-보정 필수(예치일 점프 오염), (b) projection yield 분모 budget→capitalBase(budget=0 폭주 버그), (c) XE.2 날짜별 2-phase 배당(ex-date 주식 선계산→반영, A↔B 순환 안전), (d) LOC 방어 contributions.ts/dividends.ts 분리+addMonths 공용 승격. XIRR=bisection(부호변화 가드·actual/365·전손 -1·degenerate null), flow=예치(음)+최종가치(양)만(현금배당 이중계상 금지). 위험순위 XE.1>XE.2, 순차 착수. XE.3 지수백필 검증완료(^IXIC 1971~·^NDX 1985~).
- 2026-07-05 plan(Phase X-ext XE.4/XE.5 PM+architect 기획): XE.4 월별 통계표 — 순수 monthlyStats.ts(일별 timeline+배당 스케줄→달력월 date.slice(0,7) 리샘플, Date객체 금지=타임존 회피), 기본6컬럼(월·월말액·월수익률·누적수익률·당월배당·누적배당)+DCA시 월기여/누적투입, StatsTable.tsx + Chart↔Table 토글, 엔진 0줄. XE.5 seed 제어 — architect 근본원인: useBacktestUrl이 URL seed 부재시 매 진입 Math.random 재생성(nav 쿼리없음→팬 매번 변함); 수정=고정 DEFAULT_SEED+자동생성 effect 삭제+setSeed, SeedControl(숫자입력+프리셋칩5+Randomize), AC 회귀=seed외 입력 10회에도 URL seed 불변. 시퀀스 XE.3(완료)→XE.1→XE.2→XE.5→XE.4(BacktestClient 공유 순차, XE.4/5는 엔진 0줄 순수후처리). XE.3 검증 완료(^IXIC 1971~/^NDX 1985~).
- 2026-07-05 feat(Phase X-ext XE.1): DCA 적립식+XIRR 완료 — 신규 dateAdd/invest/contributions.ts, metrics에 XIRR(bisection, 손계산 15.83% 검증)+Modified-Dietz flow-보정, engine flowsByDate/기여주입/totalContributed/moneyWeightedReturn, projection yield 분모 capitalBase, types additive, UI ContributionInput+useBacktestUrl(dca=amt:freq)+MetricsPanel(Total Invested·XIRR 스왑). lump-only byte-identical(X.10 게이트 유지). vitest 132(신규15)·check-types 0·lint 0. + XE.3-UI fix: DateRangePicker 레퍼런스 fetch from 2023→1971(하드코딩 탓 지수 백필했는데 UI 2023 벽 잔존하던 것 수정). 다음 XE.2.
- 2026-07-05 plan(PM advisor 재정렬): 시퀀스 XE.5(seed 버그·즉시)→XE.4(월표)→XE.2(라우팅)→XE.6(prune 면제·병렬). XE.1 착지로 "대형 diff 선착지" 근거 반감, 오너 불만 2건(차트/seed)이 XE.2 아닌 항목이라 XE.5/XE.4 우선. ⚠CRITICAL 발견=XE.6: Phase M 12/30 prune이 백테스트 딥 히스토리(QQQ 1999·^IXIC 1971) 삭제→2026-12-30 Max 프리셋 자멸, 해결=백테스트 유니버스 prune allowlist 면제(collector, 수십MB 무해). UX 갭: 진행형 공개·공통구간 클램프 안내·몬테카를로 기간체리피킹 라벨·세금 disclaimer. XE.5 developer 착수.
- 2026-07-05 feat(Phase X-ext XE.5): 몬테카를로 seed 제어 완료 — 근본버그(진입마다 Math.random 재롤) 수정: DEFAULT_SEED=42 + 자동생성 effect 삭제 + seedReady 제거 + setSeed. 신규 SeedControl.client.tsx(숫자입력+프리셋칩 5+🎲Randomize) + BacktestClient 게이트 수정 + ResultChart 몬테카를로 caveat 라벨. vitest 153(신규21, AC3 회귀=seed외 입력에도 URL seed 불변)·check-types 0·lint 0. 미커밋. XE.6(prune 면제) 병렬 디스패치(오너 승인).
- 2026-07-05 feat(Phase X-ext XE.6): CRITICAL prune 면제 완료 — retention.py BACKTEST_RETAIN_LABELS(11: 9ETF+^IXIC/^NDX 라벨) + db_sink.read_prune_candidates(exempt_labels=, 기본 frozenset back-compat) + prune_history 주입. 2026-12-30 연간 prune이 백테스트 딥 히스토리(QQQ 1999·^IXIC 1971) 삭제하던 자멸 방지. 실패모드=보존과다(안전). pytest 515(신규9). 미커밋.
- 2026-07-05 feat(Phase X-ext XE.2): 배당 라우팅 완료 — 신규 dividends.ts(159, 날짜별 2-phase: ex-date 주식 선계산→반영, A↔B 순환 검증)로 engine.ts 312→291(300 복귀). Holding.dividendRoute descriptor(same/cash/asset:T, 레거시 drip+URL :1/:0 back-compat), DividendEvent.routedTo, 타깃 미선택/close≤0 cash 폴백, 배당=소스귀속·주식=타깃가산. UI DividendRouteSelect(DripToggle→x\_ 소프트삭제)+WeightSliders/AssetPicker/DividendTable. X.10 게이트 유지. vitest 186(신규23)·check-types 0·lint 0. → Phase X-ext XE.1–XE.6 ALL DONE, 배치 커밋 대기.
- 2026-07-05 fix(Phase X 백테스트): 자산 시리즈 fetch가 사용자 범위(기본 DEFAULT_FROM 2023)에 묶여 QLD 등 딥 이력(2006)이 발견 안 되고 피커 바닥이 2023 고정되던 버그 수정 — BacktestClient 자산 fetch를 전체 이력(HISTORY_FLOOR 1971→today)으로 변경, 범위는 엔진이 클라 슬라이스(from/to 의존 제거, labelsKey에만 refetch). seriesFirstDate=실제 상장일→minAllowedFrom/Max 프리셋이 자산 inception까지 열림. XE.3-UI(레퍼런스 배경 1971)는 배경만 넓혔고 자산 fetch 누락이 실제 벽이었음. check-types/lint/vitest 그린.
- 2026-07-05 fix(Phase X 백테스트 CRITICAL): 액면분할 이중계산 수정 — market_history.close는 이미 split-adjusted(Yahoo가 소스에서 OHLC 분할조정; auto_adjust=False여도 배당만 미조정. QLD 2006 close $0.31=6회 2:1 역조정, DB 실측 확인). 그런데 engine.ts가 분할일마다 shares×stockSplits 재적용→정확히 분할배수만큼 부풀림. QLD DCA($5k+$500/mo 2008–26) 실측: BUGGY $218M(≈사용자보고 $215.51M) vs FIXED $4.13M. 수정=engine.ts 분할 재적용 블록 삭제(close 이미 조정, 배당만 명시적 유지)+PricePoint 주석 정정(close=split-adjusted, stockSplits=informational). engine.test 분할 테스트 조정가 기준 재작성. vitest 65/65·check-types 0. 미커밋.
- 2026-07-05 fix(Phase X 백테스트 배당 기준): 배당 split-adjust rebase 추가(9eab7d3 분할 이중계산 수정 후속) — yfinance auto_adjust=False가 close는 split-adjusted·dividends는 raw(as-paid)로 줘서 기준 불일치. raw배당×split-adjusted(64배 많은)주식수→초기배당 분할배수만큼 과다. engine.splitAdjustDividends()가 배당÷(이후 누적분할)로 역-1pass 재기준(close/stockSplits 불변, stockSplits 용도 부활). 영향=저배당 레버ETF엔 소폭(QLD DCA $5.08M→$4.91M ~3.5%), 고배당·잦은분할 종목엔 큼. yfinance 원본 대조 검증. 결론: 사이트 $4.87M이 정확(2독립계산 일치), 오너 외부추정 $1.8~2.2M=평활CAGR근사라 경로의존성(2008-09 -80% 저점매집 후 24%복리) 과소. vitest 67/67·check-types 0. 커밋 93d42aa 푸시.
- 2026-07-05 feat(Phase P): filing_date 룩어헤드 가드 구현 — 신규 마이그 0035_sec_consensus_asof.sql(뷰 체인: 펀드별 as-of[filing_date≤CURRENT_DATE] 최신 접수분 사용 → 룩어헤드 제외 + 45일창 엇갈린접수 언더카운트 해소) + screen.ts presence-guard 분기(0035 미적용시 레거시 v_sec_consensus 무중단) + $N 파라미터 경로별 재바인딩(period 필터 제거로 시프트) + as-of 뷰 SSOT 미러(dashboard_server schema). screenScore 무변경. jest 352 green·tsc build OK. USER: 0035 apply 완료. 선행 USER 게이트도 완료(0019/0020 apply·OpenFIGI·security-map dispatch → security_map 271/250 resolved). Phase P 유일 미구현 코드 해소. NB screen.ts 576 LOC(사전부채 531+45).
- 2026-07-06 feat(백테스트 표 확장): 월별 표에 자산별 월말 조정close 칼럼 + 연도별 누적 연환산 표 추가. buildMonthlyAssetPrices(클라 seriesByLabel 재사용, 범위 슬라이스, 월말 종가·분할조정)→StatsTable 후행 칼럼. buildYearlyStats=헤드라인 정합(일시불 시간가중 CAGR bars/252, DCA는 초기budget+연말까지 납입으로 XIRR-to-date)→YearlyTable(표뷰 상단). 순수 유틸+단위테스트. 가격 기준=분할조정가(오너 확정, QLD 2006 $1.13/2016 $4.7 vs 실거래 $72/$75 대조 후). vitest 194/194·check-types 0·lint 0. 커밋 cd725a9 푸시.
- 2026-07-06 fix(funds 모바일 크래시): /funds/[cik] 랭크플로 셀 재탭 시 iOS Safari "A problem repeatedly occurred"(WebKit 렌더 프로세스 OOM 크래시, JS 예외 아님) — 원인=HoldingCell 딤 처리 `saturate-50`(CSS filter)가 셀마다 GPU 합성 레이어 승격, 대형 그리드(분기×종목 수백 셀, 예: Berkshire 0001067983)에서 레이어 폭증. 수정=filter 제거(딤=opacity-35 단독)+transition-all→transition-opacity(filter 애니메이션 차단). 데스크톱 정상이던 이유=GPU/메모리 여유. check-types 0·lint 0. 모바일 실기 재테스트 필요.
- 2026-07-06 chore(next16): middleware.ts→proxy.ts 이름변경 — Next.js 16 "middleware file convention deprecated" 경고 해소. Clerk clerkMiddleware()가 proxy 컨벤션 완전 지원(default export+config 그대로, 함수명 변경 불필요=named export 아님). git mv로 내용 보존. build ✓(ƒ Proxy로 인식)·lint 0.
- 2026-07-06 plan(Phase X2 백테스트 v2): PM+architect+ML 3-agent 설계 → 오너 결정 잠금. 리밸런싱 부재(X는 매수후 drift만) → 포트폴리오 관리 컨트롤로 승격. 활성 빌드=리밸런싱+sell-lock(canSell)+sell priority+그룹핑+지향성 현금흐름(배당·DCA 자산별 라우팅, 강제매도 최소)+조건부 트리거(take-profit/buy-dip/weight-cap/floor); 몬테카를로 합성 back-history만 DEFERRED. 설계=effective target map 단일진실원+date-walk rebalanceIfDue 1스텝(dividends→DCA→rebalance→snapshot), 트리거 리스트 일반화(스케줄 drift-band+매-bar 조건부). take-profit '저점'=오너결정 bearTrough(고점대비 −20% 진입후 최저, 2배 회복시 발동). rebalance 부재⇒byte-identical(zero-regression 게이트). 00.plan.md Phase X2+#43 / 00.tasks.md Phase X2(Slice1–7, X2.1–X2.16) 기록. 코드 미착수.
- 2026-07-06 feat(Phase X2 백테스트 v2 리밸런싱 — Phase 1 ENG DONE, 미커밋): 6-wave 아키텍트-검증 dispatch로 X2.1–X2.19 구현. 리밸런싱(drift-band, effective-target-map 단일진실원)+sell-lock(canSell)+sell priority+그룹핑+지향성 현금흐름(DCA route 신설·배당 route XE.2 재사용)+조건부 트리거(take-profit/buy-dip/weight-cap/floor, bearTrough 상태머신=고점대비 −20% 진입후 최저 2배시 발동)+수동 현금흐름(manualFlows 임의일자)+자산별 월 비중% 칼럼. date-walk 순서 dividends→DCA+manualFlow→advanceRunState→rebalanceIfDue→snapshot. rebalance/manualFlows 부재⇒byte-identical(X.10 gate 유지). 신규 순수모듈 targets/rebalance-schedule/rebalance/rebalance-apply/triggers/initial-state/split-adjust/engine-output/url-codec(+rebalance)/monthlyStats.buildMonthlyAssetWeights + UI 6종(진행형 공개 고급 접이식). engine.ts 319→264, url-codec 407→188 split. 버그수정: sell-lock take-profit warning 경로 누락. 게이트 vitest 296/296·tsc 0·lint 0·전 소스 ≤300(BacktestClient 303 응집). Phase 2(몬테카를로 합성) DEFERRED. USER: 커밋+수동 e2e.
- 2026-07-06 plan(Phase X2 Phase-2 합성 back-history GREENLIT): architect 백그라운드 검증(GO) → PM 계획. #43 DEFERRED 해제 → PLANNED(#44). 통합=순수 클라 전처리 엔진 0줄(utils/backtest/synth/, BacktestClient prepend 스플라이스 → DateRangePicker 클램프 자동완화, 베이스 라벨 labelsKey 주입); 결정성=prng.ts 추출(mulberry32/standardNormal, projection byte-identical)+synthSeed; 방법 v1 유일=회귀프록시+비대칭 2-regime β+bootstrap 잔차+flat 실현분배율 배당; 베이스 4종(QQQ 기본). 5 가드레일 강제(SYNTHETIC 라벨·듀얼지표 real-first=엔진 더블패스·horizon cap 2×overlap·R²-floor 0.80·synth= URL). admin-only 토글 OFF 기본. synth OFF⇒byte-identical. 태스크 S1–S8(X2-P2.1–13). 오너 confirmable: 깊이 2×overlap·단일 method·JEPQ-only. 코드 미착수.
- 2026-07-06 plan(Phase X2 Phase-2 2-method 시퀀스 A, #45): ML(구조적 커버드콜 복제)+architect(통합) 백그라운드 검증 → PM 재계획. 합성 back-history를 2a(회귀 MVP 선행 ship, method-차원 스캐폴딩 포함)+2b(구조적 M2+^VXN/^VIX collector 편입[마이그레이션 0]+비교뷰 committed)로 분할. M2=β·base − within-cycle 콜 give-back + BS 프리미엄(실제 VXN/VIX vol), {β,moneyness,coverage,haircut} 4-param overlap 캘리브레이션 FREEZE→전이력 적용, 완전 결정적(RNG 0). vol=INPUT not holding(refSeries 패턴). 비교=3 엔진 패스(real-only dedup)+useSyntheticBacktest hook 추출+ComparisonPanel. 가드레일 5→7(ELN 잔차·pre-2001 k·VIX 프록시). 크래시윈도우 M1↔M2 발산=model-uncertainty. 태스크 2a(X2-P2.1–13)+2b(X2-P2b.1–14). 코드 미착수.
- 2026-07-06 feat(Phase X2 Phase-2a 합성 back-history 회귀 — ENG DONE, 미커밋): 4-wave dispatch(ml-engineer synth 코어 + developer 결선/UI + ml validation). admin-only 합성 pre-inception 히스토리. prng.ts 추출(mulberry32/standardNormal/hashLabel, projection byte-identical) + synth/{overlap,fit(비대칭 2-regime β),generate(block-bootstrap 잔차),dividends(실현분배율),index(method-key+horizon cap 2×overlap)} + url-codec-synth(synth=<short>:<base>:<method>, str/cmp 예약) + useSyntheticBacktest hook(splice prepend+엔진 더블패스 real-only headline) + UI(BacktestControls 합성토글/base셀렉터/method스텁·ResultChart 음영+SYNTHETIC 배너·MetricsPanel 듀얼지표 real-first+R²<0.80/cap 경고·StatsTable/YearlyTable SYNTH 태그) + validation.test(overlap back-test 복원 tolerance). synth OFF⇒byte-identical(gate.test.ts 확인). 게이트 vitest 369/369·tsc 0·lint 0. Phase 2b(구조적 M2+^VXN 수집+비교) COMMITTED 대기. USER: 수동 e2e.
- 2026-07-07 feat(Phase X2 Phase-2b 합성 back-history 구조적+비교 — ENG DONE, 미커밋): 4-wave 병렬 dispatch(ml-engineer 구조적 코어+검증 · developer collector+통합+UI). M2 구조적 커버드콜 복제 — synth/{bs(BS 콜),vol(VXN/VIX+pre-2001 k·VIX 프록시),calibrate(4-param 결정적 grid descent),overlay(cap-and-collect 월roll·BS 프리미엄=배당)} + index str 분기(완전 결정적 RNG 0). vol=INPUT(refSeries `^VXN`/`^VIX` fetch, 보유자산 아님). method 셀렉터 reg/str/cmp 활성 + synth-passes 3-pass dedup(real-only 1회+reg-full+str-full). ComparisonPanel real|M1|M2 3열+divergence(범위표시·평균금지)+ELN 모델리스크. 공유 SynthBanner 가드레일(bypass 불가)+proxy-vol 표기. LOC 정리: SynthControls/useBacktestStats/resultChartData 추출(BacktestControls 344→224·ResultChart 380→262·BacktestClient 296). collector `^VXN`/`^VIX` 편입(watchlist+retention 13, raw 라벨, 마이그레이션 0, pytest 518). validation-structural.test 복원 tolerance. synth OFF/reg⇒byte-identical. 게이트 vitest 421/421·tsc 0·lint 0. ⚠잔여 USER 게이트=X2-P2b.2 vol 백필(run_daily --full --symbols ^VXN,^VIX) — 실행 전 Structural/Compare 실데이터 없음.
- 2026-07-07 backtest: 당월배당 gross 표시 + 자산별 월매수 컬럼(배당재투자+납입) (StatsTable/monthlyStats)
- 2026-07-07 backtest: 표 리밸런싱 월 마커 + 자산 가격 지수 차트(자산 가격 탭) 추가
- 2026-07-07 backtest: 차트 리밸런싱 시점 세로 점선 마커 (ResultChart+AssetPriceChart)
- 2026-07-07 backtest: 리밸런싱 월 선택(freq=custom)+스케줄 조건 게이트(armNext/gated, weightCap 유지) (rebalance-schedule/rebalance/rebalance-gate/codec/UI)
- 2026-07-07 backtest: scheduleGate 2축화(checkAt 예약일/상시 × executeAt 즉시/다음예약일) — mode enum 대체
- 2026-07-07 backtest: scheduleGate 활성 시 '허용 오차'(band) UI 숨김+안내 (band는 drift 기본모드에서만 사용) (RebalancePolicyPanel)
- 2026-07-07 backtest: 월별표 헤더 sticky(스크롤 고정) (StatsTable)
- 2026-07-07 backtest: 당월 배당 2줄 표시 — 총$ + 자산별 주당배당(perShare) (monthlyStats/StatsTable); JEPQ 배당 과소=코드정상, 최근월 데이터 미반영 진단
- 2026-07-08 backtest: 3 per-source 가중치(original/DCA dcaPct/dividend divPct) — DCA byDcaWeight route + 배당 pool(coexist, drw flag) + PerSourceWeights UI (types/invest/dividend-pool/engine/url-codec/hook/UI)
- 2026-07-08 backtest: fix codec keyed-tail vs positional groupId 충돌(H1) + DCA% 입력 시 route 자동 byDcaWeight(M1)
- 2026-07-08 backtest: 가중치 UI 통합 — Original(입력+DRIP드롭다운)/DCA/Div 단일 그리드, WeightSliders 슬라이더 제거 (PerSourceWeights)
- 2026-07-08 F-fetch.2 DONE (UNCOMMITTED): axios fully retired from BOTH frontends. dcw(25 .api.ts + axios_instance)→native-fetch wrappers apis/http.{core,client,server}.ts; finance_web(getMe) same; axios_instance→x_, npm axios dropped both apps; ApiError+body?. Extra latent envelope-unwrap bugs fixed (createPost/updatePost/getMyDrafts/uploadImage raw-vs-envelope; getPosts/getPostsByTag/getPopularTags/getUserPopularTags double .data; getCategories dead .status). Bundled: backend {success,data} envelope convergence — sendOk/sendError helpers (dashboard_server+finance_api), 4 raw routes→envelope (users/me, me/username, categories; finance users/me) + 4 fetchers unwrap, finance_api global errorHandler added. TCategoryTreeResponse now dead (kept). NOT converted: /users/:username/category/:categoryId (no consumer). Also fixed finance_api byTicker duplicate-CIK (GROUP BY cik+SUM value_usd, share-class fold). Gate: 4 apps check-types 0 · 4 apps build green · dashboard_server jest 352/352 · zero axios in active code. architect-validated A/B/C PASS.
- 2026-07-08 finance-migration: B0.2 doc reconcile — stale POSTGRES_URL refs → POSTGRES_URL in tasks/RUNBOOK/README/finance-migrations (code reads POSTGRES_URL*)
- 2026-07-08 finance users: 0002_users.sql (finance-local Clerk mirror, is_admin DB col) + finance_api helpers/provisionUser.ts lazy upsert + /users/me now reads isAdmin from DB not Clerk metadata; typecheck green. Admin grant = SQL. Backtest advanced-filter gate now DB-driven
- 2026-07-08 finance-migration: B0.3 re-diff 6 domains DONE — zero real drift (all intentional finance_api divergences, no re-copy). B0.4 parity harness DONE — scripts/parity/parity.mjs (Node24 ESM, 0 deps, 2 base URLs as CLI args → reused by B2.1, normalizes ts/order, 11 endpoints, node --check green; live run gated on Oracle=B2.1)
- 2026-07-08 db-module: M0 DONE — apps/dashboard_server/supabase → packages/db (@repo/db); db:* CLI scripts relocated; blog-only cut (12 blog migs kept, 22 finance migs + seed.market.sql x_ soft-deleted) (a2cd584)
- 2026-07-08 blog-db-migration: Phase M PLAN set — relocate blog DB to NEW blog-only Supabase (clean-cut: apply 12 blog migs + pg_dump --data-only 11 tables + IDENTITY resync); storage→OCI (not Supabase); Vercel integration swap; error_log reclassified blog-owned; old shared project→finance-only rollback source
- 2026-07-09 feat(storage): Phase N storage strategy module SHIPPED (6cf71b4) — provider-swappable image upload (put/remove/exists bytes-only; publicUrl shared pure {CDN_ASSETS}/{key}); TS apps/dashboard_server/src/lib/storage/ (provider.type/publicUrl/supabaseProvider/ociS3Provider/factory + CONTRACT.md) + Python blog_agent/db/storage/ mirror (base/public_url/supabase_provider/oci_provider/factory/object_key; SRP split of 281-LOC storage.py → object_key/utils.load_file/db.file_meta); oci+s3 = one S3-compat class; swaps by STORAGE_PROVIDER env; old uploadImage.ts/storage.py x_-retired; unit tests storage.test.ts + test_storage.py
- 2026-07-09 refactor(storage): Option B — bucket dropped from public URL (adef23e) + STORAGE_BUCKET_IMAGE→STORAGE_BUCKET (fda0cf2) + bucketless thumbnail URL parse (64b47c3) + OCI bucket renamed blog-assets.prettylog.com; STORAGE_BUCKET now upload-only → bucket renames rewrite-free, no files.stored_uri rewrite; next/image blog-assets host allow (06b1834)
- 2026-07-09 fix(storage): OCI S3 checksum WHEN_REQUIRED both clients (boto3 + @aws-sdk/client-s3) — botocore≥1.36 default CRC breaks OCI "aws chunked encoding not supported"; + finalized blog-only migration set (hard-removed x_00* finance migs + x_seed.market.sql from packages/db) (8ba1794)
- 2026-07-09 refactor(blog): env-driven default blog author + consistent DEFAULT_BLOG_AUTHOR naming across blog_agent + dashboard_client_web + seed.sql (9568b47)
- 2026-07-09 docs: blog migration DONE — Phase M (blog DB relocation) + Phase N (storage strategy) folded to DONE stubs; full detail → _docs/archive/tasks.phase-M-blog-db.md + tasks.phase-N-storage.md (distinct slugs from the retention Phase M/N archives); plan.md blog-relocation decision flipped planned→DONE.
- 2026-07-09 chore(cleanup): finance leftovers excised (blog-only repo) — BOTH tradelunch.schema.sql SSOT copies stripped of dead finance DDL (15 tables + 17 views/MVs: market_*/sec_*/politician_*/gex_daily/signal_backtest/committee_* + v_sec_*/v_politician_*/mv_sec_new_positions), 1037/886→367 lines each, check:schema in-sync, 11 blog tables kept verbatim (error_log was migration-only, never in SSOT); dashboard_client_web (feed)/page.tsx SEO TITLE/DESC definance'd (13F/congress/marketcap→blog+portfolio copy), menu.atom.ts stale /funds comment genericized (pageMenuAtom kept — MenuDrawer still consumes). check-types 0. UNCOMMITTED (owner review)
- 2026-07-09 docs(plan/tasks): Phase Y — Log (Threads-style 개인 마이크로피드) PLANNED 기록 — PM 2-pass 아이디에이션(Threads=비주얼만, 소셜 배제) + architect 설계; M1(글+재귀답글) 확정, 렌더=포커스-노드 뷰(조상 평면+포커스+depth-1), 테이블=전용 self-ref `log`+materialized-path(comments 0009 재사용, posts/comments 재사용 기각=8 FROM posts+sitemap 제외조건 회피), M2/M3 전방호환(주변테이블+nullable ADD COLUMN), 네이밍=Log(feed 충돌회피). T1–T12 미착수(문서 기록만, 오너 결정). 코드 변경 0
