# Tasks — tradelunch

**Last updated:** 2026-06-28
**Active:** Phase A DONE (live on `my.prettylog.com`) · Phase B backend DONE (84b9d32) · **Phase B blog_agent publish pipeline WORKING end-to-end** (first real publish live via `GET /v1/api/posts`, commits 4a2366d/0568915/9890227) · Phase B dashboard client (B4) still on USER `DASHBOARD_DATA_SOURCE` flip · **Phase D ENG COMPLETE** — D1+lazy+onboarding (8ca524e), D2 Authoring (dc28f19), D4 Admin (70375e6), authoring UX cycle (a0bdd70) + i18n retrofit + thumbnail UX/persist (31eb14a) + lint gate GREEN (7d9b485) · **Phase E ENG COMPLETE — Saved + Likes + Comments shipped (Issue#1 RESOLVED=PARALLEL, Option C materialized-path comments), all merged+pushed to `origin/main` HEAD `7b2ea0a`** (server jest 68/68, client lint `--max-warnings 0`=0, vitest 5/5, check:schema in sync, tsc 0 ×3); remaining Phase E = **USER apply-migrations gate** (`supabase db push` `0007`+`0008`+`0009`, then re-run server jest for DB-gated assertions) · **Phase E2 DONE/SHIPPED — Comment Edit + Comment Pagination (C1–C6) all merged+pushed to `origin/main` HEAD `85093b4`** (additive: NO schema change / NO new index / NO new migration) · **Phase F PLANNED — Blog Image Upload Rework (Express proxy + 2-step resize, F1–F17), NOT built** (supersedes D2.6 signed-URL; Decision #27) · **Phase F2 PLANNED — blog_agent webp compression (Pillow, F2-T1..F2-T7), NOT built** (Decision #28; Option A thumbnails; supersedes the Phase F "blog_agent path untouched" non-goal) · remaining blog ENG tail = S7.2 blog_agent ruff CI job (needs Python/uv on runner); rest = USER go-live gates
**Branch:** feature/finance → main

> Atomic execution breakdown. Product intent → `00.plan.md`. Running state → `01.status.md`.
> Verbose source docs (full migration runbook, blog_agent merge ADR) archived in `_docs/`.

**Legend:** `[x]` done · `[~]` in progress · `[ ]` todo · **(USER)** needs Supabase/Vercel/DNS creds · **(ENG)** code only · **SUPERSEDED** no longer applicable.

---

## Go-live USER gates (consolidated snapshot — 2026-06-26)

**DONE:** `supabase db push` of `0004` (market) + `0005` (draft enum) applied to remote (all
`0001`–`0005` Local=Remote); `seed.market.sql` loaded (`market_snapshots`=22, `market_history`=5500);
Supabase Storage bucket `blog.prettylog` created PUBLIC; **blog_agent first real publish landed live**
(`java-spring-jdbc`, user_id=1, status='public', thumbnail public URL) and reads back via
`GET /v1/api/posts`.

**STILL OPEN (USER):**
- Clerk env on BOTH Vercel projects — frontend (`pk_live` + `CLERK_SECRET_KEY`), backend
  (`CLERK_SECRET_KEY` + `ALLOWED_ORIGINS`) — and Clerk dashboard public-sign-up ON. *(D1.1)*
- Backend Vercel `SUPABASE_URL` + `SUPABASE_SECRET_KEY` — else image upload → 503. *(D2.6a)*
- Admin bootstrap SQL `UPDATE users SET is_admin=true WHERE username='<owner>'` — owner =
  `taek.lim.us@gmail.com`, AFTER signup + onboarding; `email` is NULL for lazy-provisioned users →
  match by `username`/`clerk_user_id`, never email. *(D4.0b)*
- Flip `DASHBOARD_DATA_SOURCE=backend` on frontend Vercel + redeploy. *(Phase B B4)*
- Cloudflare CNAME for `CDN_ASSETS`; market-data provider + KRX suffix decision. *(Phase B)*
- Set blog_agent local `.env` `DEFAULT_USER_ID=2`→`1` (sandbox blocked the edit; non-blocking —
  post frontmatter `userId` drives publisher identity). *(B5.7)*

---

## Repo Hygiene — DONE (2026-06-24/25)

- [x] Remove dead `SESSION_SECRET`, `AWS_RDS_CA` from server `env.schema.ts`
- [x] Create `@repo/types`; move API-boundary types out of `@repo/markdown-parsing`; repoint imports; add `transpilePackages`
- [x] Remove dead `@repo/assets` dep from client; replace `require.resolve` in `home.ts` with `path.join`
- [x] Add `@types/pg`; exclude `scripts/` + `**/x_*` from server tsconfig; remove dead `__database.ts`
- [x] Retire `@repo/markdown-parsing` from client (inline TOC to `utils/markdown/`; `force-dynamic` blog page); soft-delete to `packages/x_markdown-parsing`
- [x] `pnpm install` (sequelize+pg-hstore removed, @types/pg added, lockfile regenerated); all `x_*` files removed from repo
- [x] Replace `pnpm.overrides` for `@types/express-serve-static-core` with a direct exact devDep (5.1.1) in dashboard_server — lockfile single version, frozen-lockfile reproduces, no override (commit f026fca)

---

## Phase 1 — Blog MVP — DONE

All shipped: SSR post list, infinite scroll, Markdown render (GFM/KaTeX/highlight), skeletons,
nav (desktop + mobile drawer), theme toggle, next-intl i18n, OG metadata, hydration fix, React `cache()`.

---

## Phase 2-simple — Financial Dashboard MVP

### Cycle 2 — Variant prototypes — DONE
Variant C (TradingView-style chart) adopted. A/B reference-only. Built: snapshot/history types +
deterministic mocks; `ChartPanel`/`ChartHeader`/`ChartTimescale`/`ChartLegend`/`ChartConfigMenu`/
`ChartDrawToolbar`/`ChartVariantLayout`; `useTradingViewChart.hook`; indicator core
(`computeMA/EMA/RSI/MACD/Ichimoku`, `generateIntervalCandles`); `chart-theme` dual palette;
intraday intervals 1m–M. `react-grid-layout` removed; old grid soft-deleted + purged.

### Cycle 2 — Carry-over (before Phase C)
- [ ] Re-slim `ChartPanel.client.tsx` to ≤180 LOC OR document raised budget in file header
- [ ] Split `useTradingViewChart.hook.ts` (369 LOC) OR document split plan in header

### Cycle 3 — Full Implementation (ACTIVE)
- [ ] Finalize `IDashboardSnapshot` (Cycle 3 shape; `fetchedAt` ISO; zero `any`)
- [ ] `app/actions/getDashboardSnapshot.schema.ts` — Zod schema covering all 22 items + inferred type
- [ ] `app/actions/getDashboardSnapshot.action.ts` — Server Action; backend when `DASHBOARD_DATA_SOURCE=backend`, else mock; typed errors
- [ ] `app/actions/getDashboardHistory.action.ts` — ticker+interval → `IItemOHLCHistory|null`; TanStack `staleTime` 5m; lazy on click
- [ ] `hooks/useDashboardSnapshot.query.client.ts` — per-category polling (FX 60s, Crypto 30s, Indices US 60s, KRX once/session, Rates 24h, Stocks 60s)
- [x] Replace `/dashboard/page.tsx` with `ChartVariantLayout` (mock wired); add Dashboard nav link
- [ ] Swap mock→backend fetcher once `DASHBOARD_DATA_SOURCE=backend`
- [x] Soft-delete + purge old implementation

### Cycle 3 — Group E acceptance gate
- [ ] All 22 items show value + 1-day change % with color (real data; no "undefined"/"NaN")
- [ ] `/dashboard` renders <2s on dev with mock
- [ ] Zero `any` in Cycle 3 paths (`check-types` exit 0; grep `: any` clean)
- [ ] No hydration errors on `/dashboard`

### Cycle 4 — Verification
- [ ] PM verifies Group E against live `/dashboard`; decides Cycle 5 scope

---

## Phase A — Deploy: Vercel + Supabase (Read-Only, Free)

> Backend in monorepo at `apps/dashboard_server`. `pg` Pool (max:5) → Supabase pooler 6543, raw
> SQL, no supabase-js. Supabase CLI + migrations under `apps/dashboard_server/supabase/`.
> Stages S1–S9 below are the runbook; full verbose version (risk register, rollback, key-file
> index, exact env-var tables) is in `_docs/00.migration.md`.

### S1 — Supabase project + schema
- [x] **S1.1 (ENG)** `supabase init`; commit `supabase/config.toml` — commit 99d48c2
- [x] **S1.2 (USER)** Create Supabase project (region `us-east-1`); record project ref + DB password — DONE (project created)
- [x] **S1.3 (USER)** `pnpm supabase link --project-ref <ref>` (enter DB password) — DONE (CLI linked; backend live querying Supabase confirms)
- [x] **S1.4 (ENG)** Reconcile `0001_blog_schema.sql`; zero `[INFERRED]`; keep `post_categories`/`tags`/`posts.parent_id` — commit 99d48c2
- [x] **S1.5 (ENG)** Add `supabase/seed.prod.sql` to root `.gitignore` — commit 99d48c2
- [x] **S1.6 (USER)** `pnpm supabase db push` — DONE (all `0001`–`0005` Local=Remote; 7+ tables visible, RLS enabled)
- [x] **S1.7 (USER)** Run recursive category CTE from `posts.ts` in SQL editor — DONE (executes; backend `/v1/api/posts` → 200)

### S2 — Data migration — SUPERSEDED
AWS RDS deleted. Supabase starts empty; fresh data authored directly. Skip entirely.

### S3 — Backend data layer: Sequelize → pg Pool — DONE (runtime verified on Vercel 2026-06-26)
- [x] **S3.1** `database.ts` → pg Pool (max:5, reads `DATABASE_URL`, `ssl rejectUnauthorized:false`)
- [x] **S3.2** 6 queries in `posts.ts` → `pool.query()`; `:param`→`$N`; `::int` casts on BIGINT; repeated `$1` in CTE
- [x] **S3.3** Rewrite `src/index.ts`; SSH tunnel + Sequelize imports removed; local dev `app.listen()` only
- [x] **S3.4** `DATABASE_URL: z.string()` required in `env.schema.ts`
- [x] **S3.5** Remove `sequelize`+`pg-hstore`; add `@types/pg`; `pnpm install` done
- [x] **Runtime verify (on Vercel 2026-06-26):** pg Pool connects to Supabase; query executes; `/v1/api/posts`→200 `{success,data{posts:[],nextCursor:null,hasMore:false}}` (empty = no data yet, correct shape). *DB SSL fix required: strip `sslmode` from Supabase connection string so `ssl.rejectUnauthorized:false` governs (was 500 "self-signed certificate in certificate chain").*

### S4 — Backend on Vercel — LIVE (2026-06-26)
> Live at `https://project-tradelunch-blog-dashboard-s.vercel.app`. Deploy needed the **Option B
> durable architecture fix** (`@repo/types` now compiles to JS — was raw-TS enum that crashed
> strip-only transpile; server `@/` aliases → relative imports; modern zero-config `vercel.json`:
> rewrites→`/api`, dropped legacy `builds`/`routes` that caused 404 + invalid-runtime; `@types/express-serve-static-core`
> re-pinned via `pnpm.overrides` for Vercel-only type drift; `engines.node` for Vercel Node) **plus the
> S3 DB SSL `sslmode` strip** (see S3 runtime verify).
> *Follow-up (2026-06-26):* the `pnpm.overrides` for `@types/express-serve-static-core` was replaced by a
> direct exact devDep (5.1.1) in dashboard_server — lockfile single version, frozen-lockfile reproduces, no override (commit f026fca).
- [x] **S4.1 (ENG)** CORS reads `ALLOWED_ORIGINS` env (comma-split whitelist) — commit d9b06e0
- [x] **S4.2 (USER)** Vercel project #2 (backend): Root `apps/dashboard_server`, preset Other, Install `cd ../.. && pnpm install --frozen-lockfile`, Build blank. *RISK resolved:* `@vercel/node` esbuild resolves `@repo/types` (now compiles to JS); modern zero-config `vercel.json` — DONE
- [x] **S4.3 (USER)** Set backend env vars (NODE_ENV, DATABASE_URL 6543, DATABASE_URL_DIRECT 5432, SUPABASE_*, ALLOWED_ORIGINS, APP_URL, DEFAULT_USER_ID). NOT `DB_PG_*`/`EC2_*`/`AWS_*` — DONE (deploy live)
- [x] **S4.4 (USER)** Smoke test: `/ping`→200 `{status:ok,msg:pong}`; `/v1/api/posts`→200 `{success,data{posts:[],nextCursor:null,hasMore:false}}` — PASS

### S4-H — Deploy hardening (Fluid Compute / Vercel)
- [x] **S4-H.1 (ENG)** Vercel env templates committed: `apps/dashboard_server/.env.vercel.example` + `apps/dashboard_client_web/.env.vercel.example`
- [ ] **S4-H.2 (ENG)** pg Pool `max:1`→`max:5` in `src/database.ts` — Vercel Fluid Compute runs Express as one function serving concurrent requests; `max:1` serialized DB access. Supabase transaction pooler tolerates a small pool. *(implemented, pending commit/verify; updates S3.1)*. Acceptance: under concurrent `/v1/api/posts` requests no serialized-pool stall; pooler connection count stays ≤5.
- [x] **S4-H.3 (ENG)** `@types/express-serve-static-core` `pnpm.overrides` → direct exact devDep (5.1.1) in dashboard_server; lockfile single version, frozen-lockfile reproduces, no override (commit f026fca).
- [x] **S4-H.4 (ENG)** Fix Vercel `TS2339` strict-narrowing deploy failure (server) — made `apps/dashboard_server/tsconfig.json` self-contained (inlined strict opts, dropped `extends @repo/typescript-config`) + `vercel.json` `buildCommand` = `pnpm --filter @repo/types build && pnpm --filter dashboard_server check-types` (deterministic `tsc` strict gate). Root cause: `@vercel/node`'s bundled `ts-node@10.9.1` drops `strictNullChecks` from a workspace-`extends` tsconfig (ref vercel/vercel#12087). `@repo/types` kept centralized; no app code changed. Acceptance MET: Vercel build green; `check-types` exit 0. *(see `00.plan.md` Decisions Log #22.)*

### S5 — Frontend on Vercel — LIVE (2026-06-26)
> Live at `https://project-tradelunch-blog-dashboard-c.vercel.app`.
- [x] **S5.1 (ENG)** Deploy blockers cleared: `.nvmrc=22`, `article_server` ref removed, both `check-types` exit 0, no `@repo/axios`/`proxy.ts` — commits 99d48c2 + 4ee90a0
- [x] **S5.2 (USER)** Vercel project #1 (frontend): Root `apps/dashboard_client_web`, preset Next.js, Output `.next`, install from repo root (already in `vercel.json`) — DONE
- [x] **S5.3 (USER)** Set frontend env vars (`NEXT_PUBLIC_API_BASE` = `https://project-tradelunch-blog-dashboard-s.vercel.app`, `NEXT_PUBLIC_CDN_ASSETS` [Phase B], `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_APP_NAME`, `NEXT_PUBLIC_ENABLE_ANALYTICS`). NO `SUPABASE_*`/`DATABASE_URL` — DONE (deploy live)
- [x] **S5.4 (USER)** Verify (smoke 2026-06-26 against `https://project-tradelunch-blog-dashboard-c.vercel.app`): `/`→200; `/blog/@taeklim`→200 (empty data, no 500); `/dashboard`→200 (chart + watchlist BTC/EUR/NASDAQ/S&P500 mock, no error boundary) — PASS

### S6 — DNS + GitHub
- [ ] **S6.1 (USER)** Confirm repo under individual account; `git remote set-url origin git@github.com:intothedeep/project-tradelunch-blog.git` if needed
- [x] **S6.2 (USER)** Add `my.prettylog.com` to Vercel project #1 Domains; set CNAME/A per Vercel (TTL 300) → HTTPS auto-issued — DONE (live; Cloudflare CNAME `my.prettylog.com`→`cname.vercel-dns.com` DNS-only; `/`, `/blog/@taeklim`, `/dashboard` all 200; earlier `/` redirect was transient propagation)
- [ ] **S6.3 (USER, optional)** Custom backend domain (e.g. `api.prettylog.com`); update `NEXT_PUBLIC_API_BASE` + `ALLOWED_ORIGINS`

### S7 — GitHub Actions CI
- [x] **S7.1 (ENG)** `.github/workflows/ci.yml`: install `--frozen-lockfile` + `check-types` both apps; lint omitted pending S7.2 — commit 99d48c2
- [ ] **S7.2 (ENG, defer-OK)** Make `pnpm lint` green at `--max-warnings 0`, then re-add lint step to `ci.yml`. Acceptance: `pnpm lint` exit 0 `--max-warnings 0`; CI green
  - *Infra FIXED (uncommitted, working tree):* client `eslint.config.js` now loads via shared `@repo/eslint-config/next-js` (`nextJsConfig`) instead of dropped `eslint-config-next/core-web-vitals`; client lint script `next lint --max-warnings 0`→`eslint .`; `npx eslint .` / `turbo run lint` load + run (exit 0 only because `eslint-plugin-only-warn` downgrades all rules to warnings).
  - *Bulk prettier pass DONE (2026-06-27, commit 7b7fed5):* `prettier --write` on 85 client ts/tsx files cleared the auto-fixable formatting bulk (client eslint warnings **4517→207**; 4309 `prettier/prettier` cleared) + removed 3 safe dead imports. **Gate STILL DEFERRED** — can't reach `--max-warnings 0`: ~207 remain (78 prop-types, 45 no-unused-vars, 22 no-explicit-any, 12 no-unescaped-entities, 8 residual prettier in out-of-lane write/blog/layout dirs, + misc) — all judgment-needed, no mass-suppress.
  - *Remaining to close gate:* (1) triage the ~207 real-rule warnings (no mass-suppress); (2) add `--max-warnings 0` gate + re-add lint to CI.
  - *CI prerequisites (separate-lane follow-ups, NOT done):* (a) `turbo run lint` invokes `apps/blog_agent` `uv run ruff check .`, which is currently **blocked** because pyenv Python `3.12.3` (pinned in `apps/blog_agent/.python-version`) is **not installed** on the runner → runner must install that Python or drop the pin; (b) `apps/blog_agent` has **~696 ruff errors (539 auto-fixable)** that must be cleared before `turbo run lint` is green end-to-end.

### S8 — Phase B: Supabase Storage for blog images
- [x] **S8.1 (USER)** Create `blog.prettylog` bucket — DONE (created PUBLIC, 2026-06-26)
- [x] **S8.3 (ENG)** Rework publish tooling — now the **Python `apps/blog_agent`** (BA-09 / B5 below). **DONE (2026-06-26):** first real publish ran end-to-end — `publish_oneshot.py` uploaded the thumbnail to `blog.prettylog` (working public URL) and the post row reads back via `GET /v1/api/posts`. Acceptance MET: test publish uploads to `blog.prettylog`, resolves public URL, and the row reads back via the live API.
- [ ] **S8.4 (USER)** Set `CDN_ASSETS` (backend) + `NEXT_PUBLIC_CDN_ASSETS` (frontend) to the Supabase Storage URL / Cloudflare CNAME (default `assets.prettylog.com`); redeploy both. *(CDN must rewrite `/<bucket>/<path>` → `/storage/v1/object/public/<bucket>/<path>`.)* *(Publish already resolves a working Supabase public URL; this CNAME is the branded-domain layer.)*

### S9 — Retire AWS infra (Phase B)
- [ ] **S9.1 (ENG)** Soft-delete `ssh.tunnel.ts`, `config/env.database.ts`, RDS CA pem → `x_*`; drop EC2_*/DB_PG_*/AWS_* from `env.schema.ts`
- [ ] **S9.2 (ENG)** Remove deps: `tunnel-ssh`, `ss2`, `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `@types/ssh2`, `ssh2`. Keep `pg`/`zod`/Express/`@repo/*`. `pnpm install`
- [ ] **S9.3 (USER)** Delete AWS env vars from Vercel project #2; redeploy; `/v1/api/posts` still returns

### Phase A acceptance gate — MET (2026-06-26)
- [x] Blog + dashboard live on `my.prettylog.com` (Vercel + Supabase, no SSH/AWS at runtime); dashboard mock 22 items; both `check-types` 0. *(CI-lint green deferred via S7.2; AWS dep/env removal moved into Phase B S8/S9 — not a Phase A blocker.)*

---

## blog_agent merge (apps/blog_agent)

> Phase A = lift-and-shift (DONE, commit f51cc7e). Phase B = Supabase/Cloudflare repoint
> (BA-09, separate session, USER prereqs). Full task DAG + ADR archived in `_docs/`.

### Phase A — DONE
- [x] **BA-00..BA-08** rsync source → `apps/blog_agent/` (excludes); fix `pyproject` `config`→`configs` typo (setuptools kept); soft-delete `src/`+`temp/`→`x_*`; gitignore `.venv`/`posts`/`*.egg-info`/`nfs_nas_sync`; thin `package.json` stub (`start`/`test`/`lint`/`check-types` → `uv run`); root `dev:agent`/`test:agent`; `turbo.json` test task (build/dev exclude agent); `.env.example` (DB_PG_* kept, Phase B vars commented); root `pnpm check:schema` (diff vs server schema SSOT) exit 0
  - Acceptance met: `pnpm --filter blog_agent test` calls pytest; `check:schema` exit 0; build/dev dry-run exclude agent; code logic unchanged (move + config + x_ rename only). `uv sync` runtime test deferred if network-restricted

### Phase B — BA-09 — DONE (2026-06-26)
- [x] **BA-09 (ENG, USER prereqs)** Repoint blog_agent: `DB_PG_*`→`POSTGRES_*` (asyncpg+Supabase pooler); boto3/AWS S3→Supabase Storage (supabase-py) + `blog.prettylog`; `CDN_ASSETS`→Cloudflare CNAME; clean RDS CA/SSH/boto3 remnants (`x_`); close S8.3. **DONE (2026-06-26):** code migration architect-verified, the B5.1/B5.2 publish→read blockers fixed (commits 4a2366d/0568915), and the first real publish landed live (`java-spring-jdbc`, user_id=1, status='public', thumbnail public URL, reads back via `GET /v1/api/posts`). **USER prereqs:** `blog.prettylog` bucket (DONE), Cloudflare CNAME (OPEN — branded-domain layer only, publish already resolves a working Supabase public URL), pooler `POSTGRES_*` confirmed (DONE)

---

## Phase B — Dashboard backend + Supabase Storage repoint

> Goal: replace dashboard mock with live data and finish the AWS→Supabase/Cloudflare migration.
> Backend (B0–B3) DONE; **blog_agent publish (B5) DONE — pipeline working end-to-end**; client (B4)
> remaining on the USER `DASHBOARD_DATA_SOURCE` flip. USER-gated items listed at the bottom.

### Phase B — backend DONE (commit 84b9d32, 2026-06-26)
- [x] **B0 (ENG)** Remove 13 dead AWS/EC2/DB_PG/CLOUDFRONT env vars from server `env.schema.ts` (+ `.env*` templates). Acceptance MET: no `EC2_*`/`DB_PG_*`/`AWS_*`/`CLOUDFRONT_*` in `env.schema.ts`; `check-types` 0. *(supersedes part of S9.1 env cleanup)*
- [x] **B1 (ENG)** Migration `0004_market_data.sql` — `market_snapshots` + `market_history` tables; mirror BOTH schema SSOT copies (`dashboard_server/schema` + `blog_agent/schema`). Acceptance MET: `pnpm check:schema` exit 0. *(APPLIED 2026-06-26 — `0004` Local=Remote; `seed.market.sql` loaded: `market_snapshots`=22, `market_history`=5500.)*
- [x] **B2 (ENG)** `GET /v1/api/dashboard/snapshot` + `GET /v1/api/dashboard/history` (pg Pool, raw SQL). Acceptance MET: endpoints implemented, typed, `check-types` 0; runtime verify unblocked now `0004` + seed applied.
- [x] **B3 (ENG)** `seed.market.sql` — 22 snapshots + 250×22 history rows, mock-faithful. Acceptance MET: seed authored matching the 22 dashboard items; loaded (22 / 5500 rows).

### Phase B — client + agent
- [ ] **B4 (ENG)** Client Cycle 3 action layer + `DASHBOARD_DATA_SOURCE` flag — wires `getDashboardSnapshot`/`getDashboardHistory` Server Actions to the B2 endpoints when `DASHBOARD_DATA_SOURCE=backend`, else mock. Acceptance: with flag `backend` + seed loaded, `/dashboard` shows the 22 live items; with flag unset, mock unchanged. *(closes Cycle 3 action-layer tasks above. Seed now loaded → final step is the USER flag flip below.)*
- [x] **B5 (ENG)** blog_agent Supabase repoint (= BA-09 / S8.3) — **DONE (2026-06-26): publish→read loop WORKING end-to-end.** Code migration architect-verified (boto3→supabase-py, `POSTGRES_*` env, byte-identical schema, `status='public'`, `is_thumbnail`, tags/categories join shapes match server reads); the B5.1/B5.2 blockers fixed; first real publish (`java-spring-jdbc`, user_id=1) is live via `GET /v1/api/posts` with a working thumbnail public URL. blog_agent is a **DIRECT-WRITE publisher** — writes posts/files/categories/tags directly to Postgres (SQLAlchemy/asyncpg) + uploads images directly to Supabase Storage `blog.prettylog` (supabase-py); publisher identity `user_id=1`; Clerk is NOT involved; Express only **READS** the agent's rows.
  - [x] **B5.1 (ENG)** Fix the image-URL render bugs (B1 + W1 + W2) — DONE (commit 4a2366d). `upsert_file_record` now binds the real CDN URL into `stored_uri` (was the `:= s3_key` TODO hack); the CDN URL includes the `blog.prettylog/` bucket segment (W1); `CDN_ASSETS` trailing-slash aligned across apps (W2). Acceptance MET: the published file's `stored_uri` is an absolute Supabase public URL including the bucket segment; `GET /v1/api/posts` returns it and the thumbnail loads.
  - [x] **B5.2 (ENG/DESIGN)** Resolve re-publish semantics (B2) — DONE (commit 4a2366d). Decision: **re-publish = new versioned row** (matches the `0003` versioned-slug CTE, which intentionally forbids `UNIQUE(user_id,slug)`); `upsert_post` no longer uses the invalid `ON CONFLICT (user_id, slug)` — it INSERTs a fresh Snowflake-id row. Acceptance MET: publish completes with no "no unique/exclusion constraint matching ON CONFLICT" error; decision recorded in `00.plan.md` Decisions Log.
  - [x] **B5.3 (ENG)** Local `apps/blog_agent/.env` loads config — DONE. `configs/__init__.py` now loads `.env` BEFORE importing `configs.storage` (fixes the "supabase_url is required" crash); `db_available()` returns true; publish ran against the live DB. *(Owner `.env` `DEFAULT_USER_ID` still =2 — non-blocking, see B5.7.)*
  - [x] **B5.4 (ENG)** First real publish run + verify — DONE (commits 0568915 + 9890227). Published `posts/java/spring/jdbc/java-spring-jdbc` (Snowflake id=329061072378007552, user_id=1, slug=java-spring-jdbc, status='public', thumbnail public URL); `GET /v1/api/posts` returns it. Acceptance MET: full publish→DB+Storage→live-API round-trip. *(Tags/categories empty + "No summary available" because Ollama was down at publish — re-publish with metadata tracked in B5.6.)*
  - [~] **B5.5 (ENG)** Client public-feed load-more Snowflake-id precision. **CODE DONE (verified 2026-06-27):** the cursor is already carried as a full-precision **string** end-to-end — `apis/blog.types.ts` `nextCursor: string | null`; `app/actions/post.action.ts` `cursor: string | undefined`; `apis/getPosts.api.ts` passes the cursor through unconverted (`Number()` is only on `limit`); `app/api/posts/load-more/route.ts` reads `searchParams.get('cursor')` as a raw string. No `Number()`/`parseInt` coercion remains on the cursor path (matches the server fix in commit 9890227). **Remaining = runtime verification only** (blocked on B5.6 — needs >10 published posts): confirm load-more page 2 returns the correct next page with no id precision loss / skipped / duplicated rows.
  - [ ] **B5.6 (ENG)** Re-publish with Ollama running for richer LLM metadata. Run `ollama serve`, then re-publish so tags/categories populate and a description is generated (current live post has empty tags/categories + "No summary available" because the LLM step was skipped). Acceptance: a re-published post shows non-empty tags + categories + a generated description via `GET /v1/api/posts`.
  - [ ] **B5.7 (USER)** Set blog_agent local `.env` `DEFAULT_USER_ID=2`→`1` (sandbox blocked the edit). Non-blocking — post frontmatter `userId` already drives publisher identity. Acceptance: local `.env` shows `DEFAULT_USER_ID=1`.

### Phase B — USER-gated
- [x] **(USER)** Create Supabase `blog.prettylog` bucket (public, image mimetypes) — DONE (PUBLIC, 2026-06-26; = S8.1)
- [ ] **(USER)** Cloudflare CNAME for `CDN_ASSETS` (Supabase Storage public URL; CDN rewrite `/<bucket>/<path>` → `/storage/v1/object/public/<bucket>/<path>`) — **branded-domain layer only**; publish already resolves a working Supabase public URL (= S8.4). **OPEN (non-blocking for publish round-trip).**
- [ ] **(USER)** Decide market-data provider + confirm NCSOFT KRX ticker suffix (`036570.KS`?) with provider before any live history fetcher. **OPEN.**
- [x] **(USER)** `supabase db push` migration `0004_market_data.sql` + load `seed.market.sql` — DONE (2026-06-26; `market_snapshots`=22, `market_history`=5500) — unblocks B4 runtime verify
- [ ] **(USER)** Flip `DASHBOARD_DATA_SOURCE=backend` on frontend Vercel + redeploy — final B4 step. **OPEN.**

### Phase B — S8 / S9 (AWS retirement) — see stages above
S8.2 (soft-delete `awsS3.ts`/`multer-s3.js`) and S9.1–S9.3 (drop SSH/RDS CA, remove AWS deps,
delete Vercel AWS env) remain open under Phase A stages S8/S9 and complete the migration; B0 already
removed the dead env-var declarations.

### Phase B acceptance gate
- [~] Two halves: **(blog-image/publish — SATISFIED)** blog_agent publishes to Supabase Storage and
  the row + thumbnail resolve via `GET /v1/api/posts` (no AWS in the publish path; only the optional
  Cloudflare branded-CNAME for `CDN_ASSETS` remains, non-blocking). **(dashboard live-data — OPEN)**
  `/dashboard` serves 22 live items from `market_snapshots` (no mock) once `DASHBOARD_DATA_SOURCE=backend`
  is flipped on Vercel; plus no AWS deps/env in server runtime (S8.2/S9.x); both `check-types` 0.

---

## Phase C — planned

**Phase C (after B):** env Zod build-gate; hook/component LOC splits; server service layer; dead-dep
reconcile; `@repo/axios` vs local axios; Phase 4 pnpm checkbox cleanup; `.agent` rule consolidation.

---

## Phase D — Multi-User Blog Platform (Clerk Auth)

> Prereq: after Phase C accepted (per `00.plan.md`). **EXCEPTION:** D0 schema `0003` + the
> `status='public'` security filter may land earlier — they are additive and unblock nothing else.
> Enforcement model is **Express-layer Clerk `verifyToken` over the existing `pg` Pool** (RLS
> bypassed by design → owner-scoped queries are the authorization gate). See `00.plan.md` Phase D.
> *Web research (2026-06-25) validated this approach:* Clerk↔Supabase JWT-template deprecated
> (2025-04-01) → native TPA (JWKS) is current; Supabase TPA docs cover supabase-js+RLS only, not
> direct `pg` Pool → confirms Express-layer enforcement; Clerk supports Next 16 (we're on 16.2.5 ≥
> recommended 16.2.3); Clerk free tier now 50k MAU.

### D0 — Schema + security (ENG) — DONE (committed + applied)
- [x] **D0.1** Write `supabase/migrations/0003_clerk_multiuser.sql` (additive only): `ALTER TABLE users ADD COLUMN IF NOT EXISTS clerk_user_id TEXT UNIQUE`, `display_name`, `avatar_url`, `is_admin BOOLEAN DEFAULT false`. Acceptance MET: `supabase db push` succeeds; re-running migration is a no-op (idempotent). *(committed with D1 8ca524e; APPLIED — `0003` Local=Remote.)*
- [x] **D0.2** Add `invites` table in `0003`: `code TEXT UNIQUE`, `created_by`, `used_by`, `used_at`, `expires_at`. Acceptance MET: table present after push; `code` unique constraint enforced. *(committed 8ca524e; APPLIED.)*
- [x] **D0.3** Optional: add missing `posts.user_id → users.id` FK in `0003`. Acceptance MET: FK present; existing rows validate. *(committed 8ca524e; APPLIED.)*
- [x] **D0.4** Mirror `0003` schema changes into BOTH SSOT copies (`dashboard_server/schema` + `blog_agent/schema`). Acceptance MET: `pnpm check:schema` exit 0. *(committed 8ca524e.)*
- [x] **D0.5 (SECURITY)** Add `status='public'` filter to the `GET /posts/users/:username` list query. Acceptance MET: anonymous request for a username returns only `status='public'` posts; jest asserts a `private`/`draft` post is absent. *(committed 8ca524e; was leaking private/draft posts under multi-user.)*

### D1 — Auth foundation (ENG + USER)
- [ ] **D1.1 (USER)** Create Clerk app; record `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` (`pk_live`) + `CLERK_SECRET_KEY`; set Clerk env on BOTH Vercel projects (frontend `pk_live`+`CLERK_SECRET_KEY`; backend `CLERK_SECRET_KEY`+`ALLOWED_ORIGINS`); enable Clerk dashboard public-sign-up. *(Open self-signup; invite-only model retired.)* Acceptance: `/sign-in` + `/sign-up` render against the Clerk app; live signup provisions a user. **OPEN.**
- [ ] **D1.2 (USER, optional)** Register Clerk as a Supabase Third-Party Auth provider (defense-in-depth for future supabase-js reads only). Acceptance: provider listed in Supabase Auth settings.
- [x] **D1.3 (ENG)** Install `@clerk/nextjs` (client) + `@clerk/express` (server); wrap app in `<ClerkProvider>`; add `clerkMiddleware` gating `/write`, `/settings`, `/admin`. Acceptance: unauthenticated visit to `/write` redirects to sign-in; public routes unaffected. — DONE (commit 01574aa; ClerkProvider + middleware + sign-in/up pages)
- [x] **D1.4 (ENG)** Server `requireAuth` middleware: `verifyToken` (Clerk JWKS via `CLERK_SECRET_KEY`) → resolve `users` row → attach `req.auth`. Acceptance: valid token → `req.auth.userId` set; no/invalid token → 401; verified Clerk user with no `users` row handled (now lazily provisioned, see D-lazy.1). — DONE (commit 01574aa; `requireAuth` via `resolveAuth`)
- [x] **D1.5 (ENG)** `requireAdmin` middleware (composes `requireAuth` + `users.is_admin`). Acceptance: non-admin → 403; admin → pass. — DONE (commit 01574aa)
- [x] **D1.6 (ENG)** `POST /v1/api/webhooks/clerk` for `user.created` (Svix-verified, idempotent INSERT). — DONE (commit 01574aa) **SUPERSEDED this cycle:** replaced by lazy provisioning in `resolveAuth`; webhook soft-deleted to `x_clerk.ts`/`x_index.ts`, `/api/webhooks` unmounted, `express.raw()` removed (see D-lazy.3). Re-enable only if reverting to invite-only.
- [x] **D1.7 (ENG)** Client → server token forwarding: Clerk `getToken()` attached as `Authorization: Bearer` on write/Server-Action paths. Acceptance MET: authenticated write requests carry the bearer token; `req.auth` populated server-side. — DONE (Bearer forwarding landed across onboarding `getMe`/`postUsername` (D-onb.4), authoring write apis (D2.4), and admin apis (D4.1d, 70375e6)).
- [x] **D1.8 (ENG)** Env: add `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY` (`ALLOW_OPEN_SIGNUP` later removed in D-lazy.4). Acceptance: keys wired; open self-signup provisions new users without an invite. — DONE (commits 01574aa + dc9cb75; flag removed 3a77847)

### D1-rev — Lazy provisioning + onboarding — SHIPPED (commit 8ca524e)
> All verified: server build exit 0; jest 9 tests pass vs live DB; client `check-types` 0 + build ok;
> live DB confirms `users` table + unique constraint; webhook route → 404; anon `/me` → 401;
> `/onboarding` route emitted. Provisioning architecture decision recorded in `00.plan.md` Phase D.

- [x] **D-lazy.1 (ENG)** `resolveAuth` lazy upsert — a valid Clerk token with no `users` row creates a minimal row (`clerk_user_id` only, `username` NULL) via race-safe `INSERT ... ON CONFLICT DO NOTHING` + UNION SELECT; `TAuthIdentity.username` → `string | null`. Acceptance MET: first authenticated hit provisions exactly one row; concurrent hits do not duplicate. — DONE (8ca524e)
- [x] **D-lazy.2 (ENG)** Propagate nullable `username` to `express.d.ts` (`req.auth.username: string | null`). Acceptance MET: `check-types` 0 with nullable username threaded. — DONE (8ca524e)
- [x] **D-lazy.3 (ENG)** Soft-delete webhook: `clerk.ts`→`x_clerk.ts`, `index`→`x_index.ts`; unmount `/api/webhooks`; drop `express.raw()` block from `server.ts`. Acceptance MET: webhook route → 404; server build 0. — DONE (8ca524e)
- [x] **D-lazy.4 (ENG)** Remove `svix` dep + dormant `CLERK_WEBHOOK_SECRET` / `ALLOW_OPEN_SIGNUP` env — DONE (commit 3a77847): `svix` gone from `package.json` + lockfile (grep 0); both env fields/exports removed from `env.schema.ts` + `.env.example`/`.env.vercel.example`; `x_clerk.ts` archive left build-excluded; build 0, 33 jest pass. *(invite-only revert now requires re-adding the webhook + svix, per the lazy-provisioning trade.)*
- [x] **D-onb.1 (ENG)** Pure `validateUsername` helper + jest (`^[a-z0-9_]{3,30}$` + reserved blocklist). Acceptance MET: jest covers valid + each invalid class (too short/long, bad chars, reserved). — DONE (8ca524e)
- [x] **D-onb.2 (ENG)** `GET /v1/api/users/me` (`requireAuth`) → identity + `needsOnboarding` (true when `username` NULL). Acceptance MET: anon → 401; authed new user → `needsOnboarding:true`. — DONE (8ca524e)
- [x] **D-onb.3 (ENG)** `POST /v1/api/users/me/username` owner-scoped claim, 400 (invalid) / 409 (taken); SQL extracted to `helpers/claimUsername.ts`. Acceptance MET: invalid → 400; duplicate → 409; valid → claims for caller only. — DONE (8ca524e)
- [x] **D-onb.4 (ENG)** Client apis `getMe`/`postUsername` + hooks `useMe`/`useClaimUsername` (Bearer via `getToken`). Acceptance MET: hooks send Clerk bearer; `useMe` returns identity; `useClaimUsername` posts username. — DONE (8ca524e)
- [x] **D-onb.5 (ENG)** `/onboarding` page (+ `onboarding/layout.tsx` ReactQueryProvider): form → claim → redirect `/blog/@username`. Acceptance MET: successful claim redirects to `/blog/@<username>`. — DONE (8ca524e)
- [x] **D-onb.6 (ENG)** Protect `/onboarding` in middleware + `<SignUp forceRedirectUrl="/onboarding">`. Acceptance MET: unauthenticated `/onboarding` → sign-in; post-signup lands on `/onboarding`. — DONE (8ca524e)
- [x] **D-onb.7 (USER)** Clerk dashboard: set after-signup redirect + disable the (now-unmounted) webhook endpoint. Acceptance: new signup routes to `/onboarding`; no webhook delivery failures logged in Clerk. — DONE (USER confirmed)
- [x] **D-onb.8 (ENG)** De-hardcode `taeklim` (folds in D3.2). Acceptance MET: blog renders for arbitrary username; no literal `taeklim` in the three blog files. — DONE (8ca524e)
- *Infra flag (review):* `apps/dashboard_server/jest.config.mjs` was previously fully commented out → enabled a minimal ts-jest config so the server suite can run. Flag for review.

### D2 — Authoring UI — SHIPPED (commit dc28f19)
> Cycle 2 atomic split (architect-validated, 2026-06-26). Two CRITICAL findings drove this cycle:
> (1) the global `GET /v1/api/posts` feed had NO status filter → leaked private/draft/follower posts
> (closed in D2.3a); (2) `post_status_enum` has NO `'draft'` value → migration `0005` + USER `db push`
> required before any draft write (DONE — `0005` applied, Local=Remote). Image upload = Option B
> (signed-URL). `posts.ts` (591 LOC) stayed read-only; writes live in a new `posts.write.ts` composed
> via `posts/index.ts`. See `00.plan.md` Phase D "D2 Authoring — findings + decisions".
> **Two MUST-FIX integration fixes shipped with dc28f19:** (1) the editor seed now loads via an
> owner-scoped `getPostById`, preventing the open-editor-then-first-autosave path from clobbering the
> existing draft; (2) `validatePostInput` relaxes the title requirement for `status='draft'` so drafts
> may save with an empty/partial title (full validation still enforced on publish).

- [x] **D2.0 (ENG)** Freeze API contract in `@repo/types` (`TPostStatus` incl `'draft'`, `TPostInput`, `TDraftSummary`, `TImageSignRequest`/`TImageSignResponse`). Acceptance MET: both apps `check-types` exit 0. — DONE (dc28f19) *(NOTE: `TImageSignRequest`/`TImageSignResponse` are REMOVED in Phase F / Decision #27, replaced by `TImageUploadResponse`.)*
- [x] **D2.1a (ENG)** Migration `0005_post_status_draft.sql` — `ALTER TYPE post_status_enum ADD VALUE IF NOT EXISTS 'draft'`; mirror BOTH schema SSOT copies. Acceptance MET: `pnpm check:schema` exit 0; re-running the migration is a no-op. — DONE (dc28f19)
- [x] **D2.1b (USER)** `supabase db push 0005`. Acceptance MET: `'draft'` enum value present in Supabase `post_status_enum` (`0005` Local=Remote). — DONE (2026-06-26)
- [x] **D2.1c (ENG)** Pure `helpers/slugify.ts` + `helpers/validatePostInput.ts` + jest. Acceptance MET: empty title, over-length title, and bad status each covered by a failing-input test. *(`validatePostInput` relaxes the title rule for drafts — see MUST-FIX note above.)* — DONE (dc28f19)
- [x] **D2.1d (ENG)** `controllers/posts/posts.write.ts` — POST / PATCH (COALESCE) / DELETE (soft via `deleted_at`), all `requireAuth`, `WHERE id=$1 AND user_id=req.auth.userId`; `user_id` NEVER from body; composed in `posts/index.ts`. Acceptance MET: `check-types` 0; routes mounted. — DONE (dc28f19)
- [x] **D2.1e (ENG — security test, MERGE GATE)** `__tests__/controllers/posts.write.test.ts` (live-DB pattern): user A PATCH/DELETE user B's post → 404 + row unchanged (`deleted_at` still NULL); POST ignores body `user_id` and records `req.auth.userId`. Acceptance MET: tests pass. — DONE (dc28f19)
- [x] **D2.2 (ENG)** `GET /v1/api/users/me/drafts` (users router, `requireAuth`) — `status='draft' AND user_id=$caller`. Acceptance MET: only caller's drafts returned; another user's drafts absent (test). — DONE (dc28f19)
- [x] **D2.3a (ENG — security, REQUIRED)** Add `AND p.status='public'` to the global `GET /v1/api/posts` feed. Acceptance MET: anon `GET /posts` returns no private/draft/follower rows (test). *(Closed the global-feed leak — D2.7 only fixed the single-fetch routes.)* — DONE (dc28f19)
- [x] **D2.3b (ENG)** `GET /v1/api/users/:username` → add `optionalAuth` + `(status='public' OR user_id=$viewer)`. Acceptance MET: anon sees public only; owner sees own drafts on own profile. — DONE (dc28f19)
- [x] **D2.3c (ENG, DEFER)** Category tree / category-list routes stay public-only (decision recorded; drafts surfaced via the dedicated drafts route). — DONE (decision recorded, dc28f19)
- [x] **D2.4 (ENG, client)** `/write` + `/write/[postId]` + `components/write/MarkdownEditor.client.tsx` + apis (create/update/delete/getMyDrafts) + hooks, Bearer token. Acceptance MET: create + edit submit to the owner-scoped routes. *(editor seed via owner-scoped `getPostById` — see MUST-FIX note.)* — DONE (dc28f19)
- [x] **D2.5 (ENG, client)** `hooks/useDraftAutosave.hook.ts` — debounce; first save creates → `router.replace('/write/[id]')` → subsequent PATCH. Acceptance MET: edits persist as draft without manual publish. — DONE (dc28f19)
- [ ] **D2.6a (USER)** Bucket `blog.prettylog` created PUBLIC (= S8.1) — **DONE**; but set backend Vercel `SUPABASE_URL` / `SUPABASE_SECRET_KEY` — **STILL OPEN** (else image upload → 503). Acceptance: bucket exists (met) AND env vars set on backend (open). *(precondition for D2.6b runtime — STILL the runtime precondition under the Phase F Express-proxy rework.)*
- [x] **D2.6b (ENG)** `helpers/signImageUpload.ts` + `POST /v1/api/posts/images/sign` (`requireAuth`, native `fetch`, Option B). Acceptance MET: `contentType` validated; returns `signedUrl` + `publicUrl`. — DONE (dc28f19) **SUPERSEDED by Phase F (Decision #27)** — replaced by `POST /v1/api/posts/images` (Express proxy + sharp resize); `signImageUpload.ts` soft-deleted to `x_signImageUpload.ts`.
- [x] **D2.6c (ENG, client)** Editor image upload: sign → direct PUT → insert `publicUrl` into body. Acceptance MET: an uploaded image yields a working public URL in the post body. — DONE (dc28f19) **SUPERSEDED by Phase F (Decision #27)** — replaced by `resizeImage → uploadImage` (FormData POST to the Express proxy); the public `ImageUploadState` contract is preserved so the editor is unchanged.
- [x] **D2.7 (SECURITY — latent leak)** Add `status='public' OR user_id=req.auth.userId` to the single-fetch read paths `GET /posts/slug/:slug` and `GET /posts/:postid` via `optionalAuth`. Acceptance MET: anon gets 404 for a non-public post by direct slug/id; owner can still fetch own draft. — DONE (commit 01574aa; `optionalAuth` on `GET /posts/:postid` + `/slug/:slug`)

### D2-UX — Authoring create/edit UX cycle (client) — SHIPPED (merge a0bdd70 + follow-ups)
> Discovery + feedback + bug-fix layer on the already-shipped D2 editor (dc28f19). Full atomic UX0–UX12
> breakdown archived in `_docs/00.markdown-publishing.plan.md` (Phase E). Net result merged to main; the
> per-step running log lives in `01.status.md`. Only the cross-cycle outcomes + the open backend gap are
> tracked here.
- [x] **UX0–UX12a + FIX-QC + UX-A11Y/I18N/TEST (ENG, client)** — autosave status-preserve, nav/Write/My-drafts discovery, `/me` drafts list, owner Edit, editor toolbar/preview/autosave-indicator split, IME composition, responsive Write|Preview, unsaved-guard, PostSettings (slug + publish confirm + View Live), title autofocus/a11y, write i18n namespace (en+ko), vitest+RTL runner. — DONE (merge a0bdd70). Gates: check-types clean, vitest pass, lint 0 errors, next build ✓.
- [x] **UX-I18N2 (ENG, client)** Editor-wide i18n retrofit of legacy D2 strings — 26 hardcoded strings across 8 components (EditorToolbar/MarkdownEditor/PostSettings/EditorPreview/MyPostsList/PostListItem/OwnerEditButton) migrated into the `write` next-intl namespace (en+ko); relative-time formatter now keyed off `autosave.*`. Acceptance MET: no hardcoded user-facing strings remain in the editor surface; check-types 0, lint 0 errors, vitest pass. — DONE (commit 6f03e04)
- [x] **UX12b (ENG, client) — thumbnail authoring UX** `ThumbnailPicker.client.tsx` (pick / preview / clear) wired into `PostSettings.client.tsx`; reuses existing `useImageUpload` → Supabase signed PUT (2nd instance); `useEditorSeed` surfaces existing `post.stored_uri` as the edit-seed preview; write i18n keys (en+ko `settings.thumbnail*` / `a11y.thumbnailPreview`); RTL test (3 cases). Acceptance MET (client UX only): author can pick/preview/clear a thumbnail in the editor; tsc 0, eslint 0-err on touched files, vitest pass. — DONE (commit 9c05a87). ✅ **PERSISTENCE NOW DONE** via UX12b-persist (commit 31eb14a) — the chosen thumbnail saves through POST/PATCH and reloads; see below. *(NOTE: the underlying `useImageUpload` upload mechanism is rewritten in Phase F to the Express proxy; the picker is unaffected because the `ImageUploadState` contract is preserved.)*
- [x] **UX12b-persist (ENG, backend lane)** — DONE (commit 31eb14a). Editor-chosen thumbnail now persists end-to-end. `TPostInput.thumbnailUrl?: string | null` added (`@repo/types`); new `apps/dashboard_server/src/helpers/writeThumbnail.ts` does a transactional **soft-delete-then-upsert** of exactly one `files.is_thumbnail=true` row (owner-scoped `post_id`+`user_id`; clears when null/empty), wired into POST+PATCH in `posts.write.ts` under one `BEGIN/COMMIT` on a shared `pool.connect()` client (reuses the existing `TDb = Pool|PoolClient` seam). Stores the full absolute `publicUrl` (= sign endpoint output) in `stored_uri` so the feed's `LEFT JOIN files … AND is_thumbnail=true` resolves it as an opaque URL. Client: `MarkdownEditor.client.tsx` `handleSave` sends `thumbnailUrl` — **explicit Save only, NOT autosave** (deliberate deviation from the original "thread through autosave" sketch). NO migration (existing `files` schema + `UNIQUE(user_id, stored_name)` + thumbnail index suffice). Tests: pure `writeThumbnail.test.ts` + DB-gated `upsertThumbnail.integration.test.ts`. **CDN-parity verified (no blocker):** Express + blog_agent share the `CDN_ASSETS` default (`assets.prettylog.com`) + `blog.prettylog` bucket, and the stored value is an opaque absolute URL. Gates: types build / server build+check-types / jest 52 / client check-types+lint(`--max-warnings 0`)+vitest 5 / check:schema in sync — all green. *(Runtime precondition for actual upload: backend `SUPABASE_URL`/`SUPABASE_SECRET_KEY` — D2.6a — else sign → 503.)*
- *Deferred (non-blocking):* KO View-live slug quality (E8 slugify romanization).

### D3 — Publish + profiles (ENG)
- [x] **D3.1** Publish/unpublish routes (toggle `status` between `public`/`private`), owner-scoped. Acceptance MET: only the owner can change `status` — satisfied by the owner-scoped `posts.write.ts` PATCH (status COALESCE, `WHERE id=$1 AND user_id=req.auth.userId`); non-owner mutate → 404 (asserted by D2.1e). — DONE (commit dc28f19)
- [x] **D3.2** De-hardcode `taeklim` → `params.username` (strip leading `@`) in `sidebar-categories.server.tsx`, `sidebar-header.server.tsx`, `navigation-desktop.tsx`. Acceptance MET: blog renders for an arbitrary username; no literal `taeklim` remains in those three files. — DONE (folds in D-onb.8; 8ca524e; `navigation-desktop.tsx` literal de-duplicated against `@/utils/blog-author`)
- [x] **D3.3** Author byline on posts (`display_name` / `avatar_url`). Acceptance MET: post page shows the owning author's byline. — DONE (commit 01574aa; author byline)
- [ ] **D3.4** `/settings/profile` page + `PATCH /v1/api/users/me` (`requireAuth`, updates own `display_name`/`avatar_url`). Acceptance: caller can update own profile only; jest asserts another user's row is untouched. **RESIDUAL ENG — deferred (non-blocking for launch).**

### D4 — Admin moderation — SHIPPED (commit 70375e6)
> Cycle 3 atomic split (architect-validated, 2026-06-26). Admin delete is **soft-delete
> (`deleted_at`), acting on ANY post by id** — NOT hard-delete: `posts.parent_id` is
> `ON DELETE CASCADE` (a true delete would cascade to child posts) and `post_tags`/`files`/
> `post_categories` lack FK cascade (true delete orphans rows). Admin moderation routes are
> NON-owner-scoped (by id), gated by `requireAdmin`. First admin is bootstrapped via manual USER SQL
> (no grant UI). See `00.plan.md` Phase D "D4 Admin — findings + decisions".

- [x] **D4.0 (ENG)** Freeze `@repo/types`: `TAdminPostListItem`, `TAdminPostListResponse`, `TAdminPostStatusInput`. Acceptance MET: 3 workspaces `check-types` exit 0. — DONE (70375e6). *(Follow-up commit 9890227 widened `TAdminPostListResponse.nextCursor` → `string|number|null` for Snowflake-id precision — see Snowflake pagination note below.)*
- [ ] **D4.0b (USER)** Admin bootstrap: AFTER the owner (`taek.lim.us@gmail.com`) signs up + completes onboarding, run Supabase SQL `UPDATE users SET is_admin=true WHERE username='<owner>'`. **`email` is NULL for lazy-provisioned users** → match by `username` (or `clerk_user_id`), NEVER email. Acceptance: `GET /v1/api/users/me` returns `isAdmin:true`. *(Precondition for D4.1f live verify.)* **OPEN.**
- [x] **D4.1a (ENG)** Pure SQL helpers `helpers/adminPosts.ts` (`listAllPosts` keyset cursor / `setPostStatus` / `adminSoftDeletePost` — all NON-owner-scoped, by id) + `helpers/validatePostStatus.ts`. Acceptance MET: `check-types` 0; soft-delete sets `deleted_at`. — DONE (70375e6). *(Keyset cursor made Snowflake/BIGINT-safe in commit 9890227 — opaque numeric-string cursor, default max int8 `'9223372036854775807'`, bound as string; see note below.)*
- [x] **D4.1b (ENG)** `controllers/admin/posts.admin.ts` (GET / cursor list, PATCH /:postid/status, DELETE /:postid — all `requireAdmin`) + `controllers/admin/index.ts` barrel + mount `router.use('/api/admin', admin)` in `controllers/index.ts`. Acceptance MET: routes mounted at `/v1/api/admin/posts`. — DONE (70375e6)
- [x] **D4.1c (ENG — security MERGE GATE)** `__tests__/controllers/admin.posts.test.ts`: `assertAdmin` non-admin → 403 / admin → next; admin can `setStatus` + soft-delete ANOTHER user's post by id; soft-delete sets `deleted_at` (not hard delete). Acceptance MET: tests pass. — DONE (70375e6)
- [x] **D4.1d (ENG, client)** apis `getAdminPosts`/`setAdminPostStatus`/`deleteAdminPost` + hooks `useAdminPosts` (`useInfiniteQuery`) / `useSetAdminPostStatus` / `useDeleteAdminPost` (Bearer via `getToken`, invalidate `['adminPosts']`). Acceptance MET: `check-types` 0. — DONE (70375e6)
- [x] **D4.1e (ENG, client)** `app/admin/layout.tsx` (ReactQueryProvider) + `app/admin/posts/page.tsx` + `components/admin/AdminPostsTable.client.tsx` (Unpublish → `'private'` / Re-publish → `'public'` / Delete; `useMe().isAdmin` guard). Acceptance MET: `check-types` 0, build, `/admin/posts` route emitted. — DONE (70375e6)
- [ ] **D4.1f (USER)** Live verify: admin token unpublish/delete another user's post; non-admin → 403. Acceptance: admin actions succeed, non-admin 403, list shows draft/private. *(Blocked on D4.0b admin bootstrap + Clerk env.)* **OPEN.**
- [ ] **D4.2 (DEFERRED/DROPPED)** `/admin/invites` — under open self-signup there is NO enforcement point (webhook soft-deleted, lazy provisioning ignores invites), so building it creates a false-authorization illusion. Reviving invite-gating requires EITHER re-enabling the Svix webhook (restore `x_clerk.ts`/`x_index.ts`, re-mount `/api/webhooks`, restore `express.raw`, re-register the Clerk endpoint + `CLERK_WEBHOOK_SECRET`) OR adding a Clerk custom JWT claim. Recorded as a known trade; do NOT build now.

---

**Phase 4 — pnpm migration:** DONE in practice (`pnpm-lock.yaml` present, `yarn.lock` removed,
commit 150d460). Stale checkboxes reconciled in Phase C.

---

## Phase E — Blog Engagement (Saved Posts · Likes · Comments) — ENG COMPLETE + pushed `7b2ea0a`

> **STATUS (2026-06-27): Phase E ENG COMPLETE — Saved + Likes + Comments all implemented, merged, pushed to `origin/main` (HEAD `7b2ea0a`).** All gates green at each merge (server jest 68/68, client lint `--max-warnings 0`=0, vitest 5/5, `check:schema` in sync, tsc 0 for all 3 TS workspaces). Root `check-types` has a PRE-EXISTING unrelated `apps/blog_agent` mypy failure — NOT from Phase E. Remaining work = **USER apply-migrations gate** (`supabase db push` of `0007`+`0008`+`0009`, then re-run server jest to exercise the DB-gated saved/likes/comments assertions).
>
> **Issue #1 RESOLVED = PARALLEL:** the new dedicated `comments` table is the sole user-facing comment system; `apps/blog_agent`'s posts-based `parent_id/group_id/level` columns are left UNTOUCHED (no backfill, no migrate). Guardrails G1–G4 honored. DB audit (`_docs/db-structure-audit.md`) verdict SOUND — all 3 features purely additive, ZERO existing-table ALTER.
>
> **Deviations from the plan body (followed task specs):** Comments FK = **NO ACTION** (not plan's CASCADE) for cross-table consistency with `post_favorites`/`post_likes`; app soft-deletes so no orphans. Comments GET returns the **whole tree in one payload — NO keyset cursor yet** (plan §5.7 lazy `path @>` deep-fetch deferred; render-collapse expands the already-loaded subtree in-place; additive later if needed). **No comment EDIT (PATCH)** route — create/list/delete only. DB-gated jest suites (savedPosts/likes/comments) skip-guard without a live DB — they assert for real only after the migrations are applied. *(The two deferrals above — comment EDIT + comment pagination — are now CLOSED in Phase E2 below.)*

> Design SSOT (gitignored `_docs/`): `saved-posts.plan.md`, `post-likes-comments.plan.md` (§8.RESOLVED
> = fork decisions §A + locked cross-cutting facts §B + sequenced task list §C), `db-structure-audit.md`.
> All three features are **purely additive** — DB audit confirms ZERO ALTER to existing tables (new
> tables + new indexes only). Owner pushes migrations/commits manually.
>
> **Migrations LOCKED:** `0007` = saved-posts index · `0008` = `post_likes` · `0009` = `comments`.
> **Resolved defaults (plan §8.RESOLVED §A):** comment body = plain-text v1 · LikeButton detail-page
> first, feed gated on EL3(L5) · delete = `Dialog` confirm · comments = load-more (top-level) + lazy
> subtree · sibling sort = ASC oldest-first · comment depth = UNLIMITED, **Option C stored `BIGINT[]`
> materialized path** (`path = parent.path || id`, read = `WHERE post_id=$1 ORDER BY path`, NO recursive
> CTE, `depth = cardinality(path)-1`) · render-collapse N=4 visible indents + "continue thread →"
> (mobile 2–3) · `TPost` additive in `apps/dashboard_client_web/apis/blog.types.ts` (drop `upvotes?`
> stub; add `likeCount`/`viewerLiked`/`commentCount`; NOT `@repo/types`) · component path
> `app/blog/components/comments/` · soft-delete `deleted_at`-only + read-mask (`'[deleted]'`).
>
> **⚠️ Cross-system reconciliation (DB audit Issue #1 — MUST resolve before C1):** `posts.parent_id/
> group_id/level` are NOT dead — `apps/blog_agent` (Python) already runs a **posts-based comment tree**
> on them (`db/repositories/post.py:239-303` `create_comment`/`get_comments`; self-FK
> `parent_id→posts(id) CASCADE` exercised). The new dedicated `comments` table is a *deliberate parallel/
> replacement* — decide replace-vs-parallel-vs-migrate so the two mechanisms don't silently diverge. Do
> NOT drop those columns.
>
> **3 soft-delete Rule-1 gaps to fix in-flight:** `posts.ts:284` (by-id), `posts.ts:341` (by-slug),
> `favorites.ts:23` (favorites join). **FK ON DELETE convention** (audit Issue #3): pick CASCADE-vs-soft
> for new `post_likes`/`comments` `post_id`/`user_id` FKs, consistently with `post_favorites`.

### E-Saved — Saved Posts list+search (landing first; independent stream)
- [x] **S1 (migration)** `0007_post_favorites_user_created_idx.sql` — add `(user_id, created_at DESC)` index (audit Issue #2: favorites `ORDER BY created_at DESC` currently unindexed); KEEP existing `(user_id)` index (additive-only). — DONE/pushed (`dd36256`).
- [x] **S2 (dashboard_server)** new owner-scoped `GET /v1/api/favorites/posts` — joins `posts` (full cards), `WHERE user_id=$caller AND p.deleted_at IS NULL AND status='public'` (fixes `favorites.ts:23` Rule-1 soft-delete gap), title+description `ILIKE` search, keyset STRING cursor (Snowflake precision, never `Number()`), page 20. — DONE/pushed (`1fdccf6`).
- [x] **S3 (dashboard_client_web)** `/me/saved` list+search UI (RecentPostCard, leave-until-refetch on unsave, debounced `?q=` search box). — DONE/pushed (`83d7e8a`, E-Saved stream head).

### E-Types — TPost additive (unblocks Likes+Comments client; parallel with S1/L1/C1)
- [x] **T0 (dashboard_client_web)** `TPost` additive — added `likeCount: number`, `viewerLiked: boolean`, `commentCount: number`; dropped the `upvotes?`/`comments?` stubs. — DONE/pushed (`5292749`).

### E-Likes — public like count (detail-page first; feed after EL3)
- [x] **L1 (migration)** `0008_post_likes.sql` — `post_likes` composite PK `(user_id, post_id)`, FK **NO ACTION** (matching `post_favorites`), `idx (post_id)` for COUNT; live `COUNT(*)` (no denormalized counter). — DONE/pushed (`4e0926b`).
- [x] **L2 (@repo/types)** `TLikeToggleResponse`. — DONE/pushed (`4b6619d`).
- [x] **L3 (dashboard_server)** `helpers/likes.ts` toggle (owner-scoped, live count) + `POST /v1/api/posts/:postId/like`. — DONE/pushed (`f7f5864`).
- [x] **L4 (dashboard_client_web)** detail-page `LikeButton` (optimistic, signed-out→`/sign-in`). — DONE/pushed (`b40075f`).
- [x] **L5 (dashboard_server, EL3)** enrich 4 posts reads with `like_count`/`viewer_liked` + add `optionalAuth`+`$viewerId` to feed `GET /` + fix soft-delete Rule-1 gaps `posts.ts:284` (by-id) & `posts.ts:341` (by-slug) → deleted posts now 404. — DONE/pushed (`3d46ebc`).
- [x] **L6 (dashboard_client_web)** feed-card `LikeButton` (live count + viewer state). — DONE/pushed (`3a45497`, E-Likes stream head).

### E-Comments — threaded comments, Option C materialized path (last; resolve Issue #1 first)
- [x] **C1 (migration)** `0009_comments.sql` — Option C stored `BIGINT[]` materialized path (UNLIMITED depth), self-FK `parent_id→comments(id)`, `post_id` FK, soft-delete `deleted_at`, `idx_comments_post_path (post_id, path)` + `idx_comments_parent`, FK **NO ACTION**. Issue #1 RESOLVED = PARALLEL (blog_agent columns untouched). — DONE/pushed (`ef35f63`).
- [x] **C2 (@repo/types)** `TComment` / `TCommentListResponse` / `TCommentCreateRequest`. — DONE/pushed (`36df38b`).
- [x] **C3 (dashboard_server)** `helpers/comments.ts` — transactional CTE `path = parent.path || id`, read `ORDER BY path` (NO recursive CTE), `[deleted]` tombstone read-mask, author+post-owner+admin soft-delete. — DONE/pushed (`18f15c7`).
- [x] **C4 (dashboard_server)** `controllers/comments` — create / list / soft-delete (post-owner+admin moderation, owner-scoped). — DONE/pushed (`17e0b29`).
- [x] **C5 (dashboard_server, EC3)** replaced `commentCount` 0-literal with `(SELECT COUNT(*)::int FROM comments WHERE post_id=p.id AND deleted_at IS NULL)` across all 4 posts reads. — DONE/pushed (`3723b32`).
- [x] **C6 (dashboard_client_web)** apis + hooks — optimistic reply nests under the ACTUAL clicked parent. — DONE/pushed (`d140042`).
- [x] **C7 (dashboard_client_web)** RSC comment list + IME-safe composer + render-collapse N=4 "continue thread →" + `Dialog` delete confirm + en/ko i18n. — DONE/pushed (`7b2ea0a`, E-Comments stream head + Phase E HEAD).

> **Parallelism (plan §8.RESOLVED §C):** migrations `S1 ∥ L1 ∥ C1` land first; types `T0 ∥ L2 ∥ C2`;
> then Saved / Likes / Comments run as 3 independent streams. `L5`/`C5` share ONE `GET /posts*`
> read-shape edit — sequence them together. Each lane: tsc 0 · lint `--max-warnings 0` · jest/vitest
> green · `check:schema` in sync before the owner pushes.

---

## Phase E2 — Comment Edit + Comment Pagination — DONE/SHIPPED (origin/main HEAD `85093b4`)

> Closes the two deliberate Phase E deferrals (comment EDIT/PATCH + comment-list keyset pagination)
> on the shipped Option-C materialized-path comment system. Owner + architect pre-decided; product
> intent + key decisions + guardrails in `00.plan.md` "Phase E2" + Decisions Log #26. **All additive:
> NO schema change · NO new index · NO new migration** (`updated_at` already shipped in `0009`).
> **C1–C6 all merged + pushed to `origin/main` (HEAD `85093b4`).** No migration gate remains for
> Phase E2 — migrations `0007`/`0008`/`0009` are already APPLIED (owner confirmed) and server jest is
> 78/78 against the live DB.
>
> **Policy:** SOFT-DELETE EVERYWHERE — a comment is never hard-deleted. PATCH on a tombstoned
> (`deleted_at`) comment → `409 CommentDeletedError`.
>
> **Sequencing (one commit each, dependency order):** `C1` (helper split, pure refactor) → `C2`
> (`updatedAt` plumbing) → `C3` (server edit) → `C4` (server pagination) → `C5` (client pagination)
> → `C6` (client edit). `C5` migrates the comment cache to `InfiniteData` shape, so `C6`'s optimistic
> edit MUST target that shape — keep `C5` before `C6`.
>
> **Per-commit gate (every C-item):** server `pnpm --filter dashboard_server test` (jest) ·
> client `pnpm --filter dashboard_client_web lint` `--max-warnings 0` = 0 · client vitest green ·
> per-workspace `tsc` exit 0 (`@repo/types` + dashboard_server + dashboard_client_web) ·
> `pnpm check:schema` in sync.

- [x] **C1 (ENG, express)** Split `apps/dashboard_server/src/helpers/comments.ts` (287 LOC) into a `helpers/comments/{errors,write,list,index}.ts` **barrel** — `errors.ts` (typed comment errors), `write.ts` (create + soft-delete + the shared mutability assertion), `list.ts` (tree read), `index.ts` (re-export barrel). **Pure refactor — NO behavior change.** Acceptance: each new file ≤300 LOC; all import sites resolve via the barrel (no deep imports); server jest 68/68 unchanged (no test added/removed); `tsc` 0; `check:schema` in sync. — DONE/pushed (`d36ef68`).
- [x] **C2 (ENG, types+express+front)** Add `TComment.updatedAt: string` (`@repo/types`); project `updated_at` in **all** comment reads (list query + create/soft-delete returning rows) and map it into the `TComment` shape; update the client optimistic builders (create/reply) to set `updatedAt` (= `createdAt` on insert). Acceptance: `TComment` exposes `updatedAt`; every comment payload (list + create response) includes `updatedAt`; an optimistic comment carries `updatedAt===createdAt`; 3-workspace `tsc` 0; server jest green; client lint `--max-warnings 0`=0 + vitest green. — DONE/pushed (`9065b31`).
- [x] **C3 (ENG, express)** Comment EDIT: add `CommentDeletedError` (→ `409`); add `assertCommentMutable` (author OR post-owner OR admin — the SAME authorization used by delete, refactored to be shared by both); add `updateComment` (trim + non-empty via existing `MAX_BODY_LENGTH=4000`; bumps `updated_at`; rejects a `deleted_at` row with `CommentDeletedError`); add `PATCH /v1/api/comments/:commentId` controller; add `TCommentUpdateRequest` (`@repo/types`). Acceptance + jest (live-DB pattern): author edits own comment → 200, body changed, `updated_at` bumped (> `created_at`); post-owner + admin can edit any comment on the post; an unrelated user → 403/404 (unchanged row); PATCH on a soft-deleted comment → 409; empty/whitespace or >4000-char body → 400. Gate: jest green, `tsc` 0, `check:schema` in sync. — DONE/pushed (`4f2d32c`).
- [x] **C4 (ENG, express)** Comment PAGINATION: `TCommentListResponse += nextCursor: string | null` + `hasMore: boolean` (additive, `@repo/types`); add `listCommentPage` — keyset over **ROOT** comments (`parent_id IS NULL`), newest-first, **50 roots/page**, then fetch each page's full descendant subtree (`path[1] IN (root ids)`) so replies never orphan; cursor = last root id as a full-precision **STRING**, first-page sentinel `9223372036854775807`. A soft-deleted root with live children still appears (masked). `GET /v1/api/posts/:postId/comments` reads `cursor` (string) + `limit` (clamp default 50, max 100). Acceptance + jest: page 1 returns ≤50 roots + each root's complete subtree; `nextCursor` = last root id string (no `Number()` coercion / no precision loss); `hasMore` true iff another root exists; page 2 (via `nextCursor`) returns the next distinct root window with no skipped/duplicated roots; a masked soft-deleted root with live children still appears; `limit` clamps to 100. Gate: jest green, `tsc` 0, `check:schema` in sync. — DONE/pushed (`02735b5`).
- [x] **C5 (ENG, front)** Client PAGINATION: `getComments.api.ts` accepts `cursor` + `limit` params; convert `useComments` → `useInfiniteQuery` (cursor from `nextCursor`, `hasMore` → `hasNextPage`); migrate the create + delete optimistic mutations to operate on the `InfiniteData` cache shape (insert/mask within the correct page, not a flat list); add a bottom **CommentsPagination "Load more (next 50)"** block (NOT numbered pages); en/ko i18n for the load-more label + remaining-count copy. Acceptance: initial render shows the first 50 roots + subtrees; clicking "Load more" appends the next page (no duplicate/missing roots); a new comment + a delete still update optimistically against the infinite cache; load-more hidden when `hasMore===false`; client lint `--max-warnings 0`=0, vitest green, `tsc` 0; no hardcoded user-facing strings. — DONE/pushed (`45ba6c3`).
- [x] **C6 (ENG, front)** Client EDIT: `updateComment.api.ts` (PATCH); `useUpdateComment.query.client.ts` optimistic mutation on the **infinite** cache shape (locate the comment by id within its page; revert on error); `CommentRow` inline edit reusing `CommentComposer` (enter edit → save → cancel); `canEdit` = `canDelete` (author/post-owner/admin); "(edited)" badge rendered when `updatedAt !== createdAt`; en/ko i18n for Edit/Save/Cancel/"(edited)". Acceptance: an authorized user toggles inline edit, saves, and the row updates optimistically + shows "(edited)"; an unauthorized user sees no Edit affordance; a server error reverts the optimistic edit; editing a tombstoned comment is not offered (no Edit on `[deleted]`); client lint `--max-warnings 0`=0, vitest green, `tsc` 0; no hardcoded user-facing strings. — DONE/pushed (`85093b4`).

> **Guardrails (Phase E2):** SOFT-DELETE everywhere (never hard delete) · edit auth == delete auth
> (one shared `assertCommentMutable`) · tombstone PATCH → 409 · NO schema/index/migration change ·
> cursor = full-precision STRING (Snowflake-safe, default int8 max `9223372036854775807`) · all new
> types in `@repo/types` (no per-app duplication) · helper files ≤300 LOC (drives C1) · blog_agent
> posts-based comment columns stay UNTOUCHED (Issue #1 = PARALLEL).
>
> **No migration gate (USER):** migrations `0007`/`0008`/`0009` are already APPLIED (owner confirmed);
> server jest is 78/78 against the live DB. Phase E2 has NO open USER item.

---

## Phase F — Blog Image Upload Rework (Express proxy + 2-step resize) — PLANNED (NOT built)

> Replaces the Phase D2 signed-URL-direct-to-Supabase image upload (D2.6b/D2.6c) with an
> **Express-proxied, server-resized** pipeline: browser → Express (multipart) → `sharp` resize →
> server-to-server upload to Supabase Storage. The browser never talks to Supabase directly; the Next
> SSR server is not involved. Owner + architect pre-decided; the design is FINAL — see `00.plan.md`
> "Phase F" + Decisions Log #27 (SUPERSEDES #14). All decisions below are recorded, NOT to be
> re-litigated by the engineer.
>
> **Endpoint:** `POST /v1/api/posts/images` (multipart `file`, `requireAuth`) → `200 { success:true,
> data:{ publicUrl } }`. multer `memoryStorage`, route-level only, `limits.fileSize` ~5MB.
>
> **Error mapping (deterministic):** no file → 400 · multer `LIMIT_FILE_SIZE` → 413 ·
> `not_an_image` → 415 · `too_large` → 413 · storage unconfigured → 503 · upload fail → 502.
>
> **Dependency versions (latest-stable, verified 2026-06-28):** server `dependencies` `sharp@^0.35.2`
> + `multer@^2.2.0`, server `devDependencies` `@types/multer`; client NONE (native Canvas). Vercel
> serverless request body limit = **4.5MB** (unchanged).
>
> **Per-task gate (every F-item):** server `pnpm --filter dashboard_server test` (jest) ·
> server `tsc` exit 0 (`check-types`) · `@repo/types` build + `tsc` 0 · client
> `pnpm --filter dashboard_client_web lint` `--max-warnings 0`=0 + vitest green + `tsc` 0 ·
> `pnpm check:schema` in sync (no schema change expected — this is a no-op guard).
>
> **Guardrails:** Step-1 client resize is **transport-only, NOT a security boundary** — the
> authoritative validation + normalization is `sharp` on the server (decode-failure = magic-byte
> reject; EXIF stripped; final-size cap). Soft-delete superseded files with `x_` prefix (never hard
> delete). `@repo/types` stays the single client↔server contract (no per-app type duplication).

### A — Prep
- [ ] **F1 (ENG, types)** `packages/types/src/index.ts` — remove `TImageSignRequest` + `TImageSignResponse`; add `TImageUploadResponse { publicUrl: string }`. Build `@repo/types`. Acceptance: `@repo/types` builds; `grep TImageSign` across the repo returns ZERO live references (only `x_*` archives may match); 3-workspace `tsc` 0 after the dependent edits land.
- [ ] **F2 (ENG, server deps)** Add `sharp@^0.35.2` + `multer@^2.2.0` to `apps/dashboard_server` `dependencies` and `@types/multer` to `devDependencies`; `pnpm install`. Acceptance: `package.json` shows the three deps at the pinned ranges; lockfile updated; `pnpm install --frozen-lockfile` reproduces; server `tsc` 0.
- [ ] **F3 (ENG, client deps)** NO client dependency (native Canvas). Acceptance: `apps/dashboard_client_web/package.json` is unchanged; `browser-image-compression` is absent from the repo (grep ZERO).

### B — Server
- [ ] **F4 (ENG, server)** New `apps/dashboard_server/src/helpers/imagePath.ts` — extract `buildImagePath` + `ALLOWED_IMAGE_TYPES` from the old `signImageUpload`; output extension fixed to `webp`; keep the `${userId}/` path-prefix isolation. Acceptance: pure function; given a userId returns a `${userId}/…​.webp` key; `tsc` 0; ≤300 LOC.
- [ ] **F5 (ENG, server)** New `apps/dashboard_server/src/helpers/transformImage.ts` — pure buffer→buffer `sharp` transform `sharp(input,{failOn:'error'}).rotate().resize(1600,1600,{fit:'inside',withoutEnlargement:true}).webp({quality:80}).toBuffer()`; returns a discriminated result `ok` / `not_an_image` / `too_large` (enforces the final-size cap); EXIF auto-stripped (no `.withMetadata()`). Acceptance: returns `ok` with a webp buffer for a valid raster input; returns `not_an_image` for a non-image buffer (sharp decode failure); returns `too_large` when the output exceeds the cap; `tsc` 0; ≤300 LOC.
- [ ] **F6 (ENG, server)** New `apps/dashboard_server/src/helpers/uploadImage.ts` — server-to-server Supabase Storage upload via `POST /storage/v1/object/{bucket}/{path}` using the secret key; returns the `publicUrl`; returns a typed "storage unconfigured" result when `SUPABASE_URL`/`SUPABASE_SECRET_KEY` are missing. Acceptance: on success returns an absolute `publicUrl` including the bucket segment (parity with the feed read join); on missing creds returns the unconfigured result (→ 503 mapping); `tsc` 0; ≤300 LOC.
- [ ] **F7 (ENG, server)** Edit `apps/dashboard_server/src/controllers/posts/posts.write.ts` — REMOVE the `/images/sign` route; ADD `POST /v1/api/posts/images` (`requireAuth`, multer `memoryStorage` route-level, `limits.fileSize` ~5MB) wired `imagePath → transformImage → uploadImage`, with the full error mapping (no file → 400 · multer `LIMIT_FILE_SIZE` → 413 · `not_an_image` → 415 · `too_large` → 413 · storage unconfigured → 503 · upload fail → 502); response `200 { success:true, data:{ publicUrl } }`. Acceptance: route mounted at `POST /v1/api/posts/images`; `/images/sign` no longer mounted (grep ZERO live ref); each error class maps to its status code; `tsc` 0.
- [ ] **F8 (ENG, server)** Soft-delete `apps/dashboard_server/src/helpers/signImageUpload.ts` → `x_signImageUpload.ts`. Acceptance: file renamed with `x_` prefix; no live import of `signImageUpload` remains (grep ZERO outside `x_*`); server build 0.
- [ ] **F9 (ENG, server, Vercel)** Add `vercel.json` `includeFiles` for the Express project so the `@img/sharp-libvips-linux-x64` libvips binary is bundled in the serverless function. Acceptance: `vercel.json` includes the sharp libvips binary path glob; (verify on deploy) the deployed function loads `sharp` without "Could not load the sharp module" at runtime.
- [ ] **F10 (ENG, server, test)** Jest for `transformImage` — a valid PNG buffer → `ok` webp output; a garbage (non-image) buffer → `not_an_image`. Acceptance: both assertions pass under `pnpm --filter dashboard_server test`.

### C — Client
- [ ] **F11 (ENG, client)** New `apps/dashboard_client_web/utils/resizeImage.ts` — `resizeImage(file: File): Promise<File>` via native Canvas (`createImageBitmap(file,{ imageOrientation:'from-image' })` → draw → `canvas.toBlob('image/webp', 0.9)`; downscale long edge ~2000px; re-encode at lower quality if the blob still exceeds ~4MB). NO library. Acceptance: returns a webp `File`; the result is under ~4MB for a typical large photo input; respects EXIF orientation; `tsc` 0; lint `--max-warnings 0`=0.
- [ ] **F12 (ENG, client)** New `apps/dashboard_client_web/apis/uploadImage.api.ts` — `FormData` POST to `/v1/api/posts/images`; do NOT set `Content-Type` manually (let the browser set the multipart boundary); preserve the 503 `ApiError` for graceful degrade. Acceptance: the request carries the file as multipart `file` with a browser-set boundary; a 503 surfaces as the existing `ApiError` type; `tsc` 0; lint `--max-warnings 0`=0.
- [ ] **F13 (ENG, client)** Rewrite `apps/dashboard_client_web/hooks/useImageUpload.hook.ts` internals to `resizeImage → uploadImage`; KEEP the public `ImageUploadState` interface unchanged so `MarkdownEditor.client.tsx` (and `ThumbnailPicker`) need NO change. Acceptance: the exported `ImageUploadState` shape is byte-identical to before; no edit required in `MarkdownEditor.client.tsx`/`ThumbnailPicker.client.tsx`; `tsc` 0; vitest green; lint `--max-warnings 0`=0.
- [ ] **F14 (ENG, client)** Soft-delete `apps/dashboard_client_web/apis/signImageUpload.api.ts` → `x_signImageUpload.api.ts`. Acceptance: file renamed with `x_` prefix; no live import of `signImageUpload.api` remains (grep ZERO outside `x_*`); client build 0.

### D — Verify
- [ ] **F15 (ENG)** `pnpm check-types` + `pnpm lint` across the repo. Acceptance: `check-types` exit 0 for all TS workspaces; client lint `--max-warnings 0`=0.
- [ ] **F16 (ENG)** `pnpm --filter dashboard_server test`. Acceptance: server jest green (including the new F10 `transformImage` cases).
- [ ] **F17 (ENG/USER, manual e2e)** Manual end-to-end against a running stack with backend `SUPABASE_URL`/`SUPABASE_SECRET_KEY` set: (a) a normal JPEG uploads → body gets a working `publicUrl`; (b) an EXIF-rotated photo renders in the correct orientation; (c) a >5MB photo still succeeds (step-1 client resize lands it under the 4.5MB Vercel limit); (d) a non-image disguised as `.jpg` → 415; (e) backend with storage creds UNSET → 503 (graceful degrade in the editor). Acceptance: all five cases behave as specified. *(Precondition: D2.6a backend `SUPABASE_*` creds — else upload → 503.)*

> **Open risks to verify during build (from Decision #27):**
> 1. sharp native-module bundling on Vercel (Express project) — the libvips linux binary must ship;
>    gated by F9 `vercel.json includeFiles`; confirm at deploy time (F17/runtime).
> 2. `axios_instance` default `Content-Type` may break the multipart boundary — F12 must override/omit
>    it per request so the browser sets the boundary.

---

## Phase F2 — blog_agent webp compression (local Python CLI) — PLANNED (NOT built)

> Applies Phase-F-style image compression to `apps/blog_agent`, the LOCAL Python LangGraph publishing
> CLI (it uploads thumbnails + body images to Supabase Storage with NO compression today). Single
> authoritative server-side transform mirroring Phase F: **resize long edge 1600px (fit-inside, no
> enlarge, no crop) → webp q80 → strip EXIF/metadata** — no browser step (it is a CLI). Owner +
> architect pre-decided; the design is FINAL — see `00.plan.md` "Phase F2" + Decisions Log #28. All
> decisions below are recorded, NOT to be re-litigated by the engineer. SUPERSEDES the Phase F non-goal
> "blog_agent image path untouched".
>
> **Library:** **Pillow** (already a dep — LANCZOS + `ImageOps.exif_transpose`); NO new pip dep, NO
> system lib. **pyvips rejected.**
>
> **Thumbnail/OG = Option A (owner-approved):** keep the 1200×630 letterbox OG framing; only switch the
> codec PNG→webp q80 + strip metadata. **Option B (1600 fit-inside on thumbnails) rejected.**
>
> **Path scheme UNCHANGED:** blog_agent keeps `{user_id}/{category…}/{slug}/{slug}.webp` + body
> `{slug}-{idx}.webp`; only the extension/codec changes. Do NOT flatten to the main app's scheme.
>
> **Per-task gate (every F2-item):** `apps/blog_agent` `uv run pytest` green at the repo
> `--cov-fail-under=80`; `ruff` clean; `mypy --strict` 0; `pnpm check:schema` in sync (no schema change
> — no-op guard).
>
> **Guardrails:** the pure transform is **intentionally duplicated** with Phase F's Node
> `transformImage.ts` (no cross-app import, no `@repo/*` package). Soft-delete any superseded file with
> `x_` prefix (never hard delete). KEEP `original_filename` for markdown matching.

- [ ] **F2-T1 (ENG, blog_agent)** New pure module `apps/blog_agent/utils/image_transform.py` — `to_webp(buffer: bytes, *, max_long_edge=1600, quality=80) -> bytes`; Pillow pipeline (LANCZOS resampling + `ImageOps.exif_transpose`), fit-inside long edge 1600 (no enlarge, no crop), webp q80, metadata stripped; raises `NotAnImageError` on undecodable input. Full type hints + module docstring. Acceptance: pure function (no I/O beyond in-memory bytes); given a >1600px raster returns a webp byte buffer; `mypy --strict` 0; `ruff` clean; file ≤300 LOC.
- [ ] **F2-T2 (ENG, blog_agent, test)** Unit tests `apps/blog_agent/__tests__/test_image_transform.py` — (a) input >1600px long edge → downscaled, aspect-ratio preserved, no crop; (b) input <1600px → NOT enlarged; (c) output is valid webp; (d) RGBA alpha preserved; (e) non-image bytes → `NotAnImageError`; (f) EXIF-orientation input → orientation applied AND metadata absent in output. Acceptance: all six cases pass under `uv run pytest`; coverage respects the repo `--cov-fail-under=80`.
- [ ] **F2-T3 (ENG, blog_agent)** Wire the body-image transform at the upload boundary in `apps/blog_agent/agents/uploading_agent.py` — force `ext="webp"` / `content_type="image/webp"` in `_upload_images` (body branch) and call `to_webp` in `_upload_file_to_s3` before building `FileMetadata`; KEEP `original_filename` for markdown matching. Acceptance: a body image is uploaded as `.webp` with `content_type="image/webp"`; the markdown reference still resolves via `original_filename`; tests green.
- [ ] **F2-T4 (ENG, blog_agent)** Thumbnail OG codec switch in `apps/blog_agent/agents/image_processing_agent.py` `resize_for_og` — output **webp q80** (no metadata), KEEP the 1200×630 letterbox framing, output `.webp` (Option A). Also force `ext="webp"` / `content_type="image/webp"` in the thumbnail branch of `_upload_images` (`uploading_agent.py`). Acceptance: the OG/thumbnail output is a 1200×630 webp with metadata stripped (framing unchanged); tests green.
- [ ] **F2-T5 (ENG, blog_agent)** Fix `_save_file_records` in `apps/blog_agent/agents/uploading_agent.py` — derive `ext` / `content_type` from `stored_name` / `s3_key` (now `.webp`), NOT from the original `local_path` (existing consistency bug — the DB would otherwise record `png`). Acceptance: the persisted `files` row records `ext="webp"` / `content_type="image/webp"` for an uploaded image; a regression test asserts the recorded values match the stored `.webp` name, not the source extension.
- [ ] **F2-T6 (ENG, blog_agent, test)** Regression test for the upload path (mock the Supabase boundary) — assert the stored_name / s3_key / content_type are `.webp` / `image/webp` AND the markdown body is rewritten to the `.webp` CDN URL. Acceptance: the test passes against a mocked Supabase upload (no real network/storage call); the markdown rewrite to the `.webp` CDN URL is verified.
- [ ] **F2-T7 (ENG, blog_agent, deps)** Confirm latest-stable Pillow still bundles libwebp on macOS arm64 (the dev/run host) — **web-verify twice** per the repo "latest-stable + repeat 2x" rule — then pin Pillow in `apps/blog_agent/pyproject.toml` + `uv lock`. Acceptance: Pillow pinned at the verified latest-stable range; `uv lock` updated; `ruff` clean + `mypy --strict` 0 + `uv run pytest` green end-to-end.

> **Open risks to verify during build (from Decision #28):**
> 1. Pillow wheel must ship libwebp on macOS arm64 (the dev/run host) — F2-T7 web-verifies twice + pins.
> 2. webp as `og:image` on older social scrapers — accepted (Option A keeps the OG framing; modern
>    scrapers support webp).
> 3. Intentional duplication of the pure transform with Phase F's Node `transformImage.ts` — no
>    cross-app import; no `@repo/*` package warranted.
