# Tasks — tradelunch / dashboard_client_web

**Last updated:** 2026-06-25
**Active phase:** Phase 2-simple Cycle 3 + Phase A (Deploy to Vercel + Supabase)
**Branch:** feature/finance → main (after Phase A deploy)

> **2026-06-24 sync (original):** Variant C adopted. Cycle 2 closed.
> **2026-06-24 roadmap update:** Deployment roadmap (Phases A–D) added. Cycle 3 tasks carry forward unchanged. Phase A tasks are new. Phases B–D task breakdowns will be added when preceding phase is accepted.
> **2026-06-24 decisions applied:** (1) backend stays in monorepo, (2) Supabase CLI under apps/dashboard_server/supabase/, (3) personal/non-profit — free tier sufficient, (4) Phase D invite-only signup, (5) ingestion cron deferred, (6) pg Pool + raw SQL (no supabase-js).
> **2026-06-24 hygiene done:** SESSION_SECRET and AWS_RDS_CA removed from env.schema.ts (dead vars). @repo/types package created; API-boundary types moved from @repo/markdown-parsing; @repo/assets dep removed from client; four server publish-scripts fixed. S3→Supabase Storage decision recorded; Phase B tasks updated.
> **2026-06-24 M2 plan added:** Full Supabase blog schema migration step-by-step (C1–C9) recorded. Env var naming convention (kcdd2) recorded. CDN_ASSET_POSTS → Supabase Storage decision recorded. Vercel + GitHub Org private repo constraint recorded (repo must be under individual account for Vercel Hobby).
> **2026-06-25 M3 progress:** T1 env.schema DATABASE_URL contract, T2 api/index.ts, T3 apps/dashboard_server/vercel.json, T4 home.ts /status fix — all DONE. Sequelize→pg Pool swap (database.ts + posts.ts 6 queries + index.ts) is CODE-COMPLETE; runtime verification pending Supabase data. AWS Postgres DELETED — Supabase is a fresh setup (no data migration). S3 DELETED — Supabase Storage is a fresh setup (not a migration). Schema reconciliation: post_categories/tags/posts.parent_id are unused columns — kept to match production schema, no action required.
> **2026-06-25 migration doc:** Full step-by-step execution plan written to `00.migration.md` at repo root. That file is the single source of truth for ordered stages S1–S9, owner per step, exact commands, acceptance criteria, and rollback points. S2 (AWS data migration) is marked SUPERSEDED in 00.migration.md because AWS RDS is deleted.
> **2026-06-25 build fixes + cleanup:** `pnpm install` done; `@types/pg` added to server devDeps; `scripts/` and `**/x_*` excluded from dashboard_server tsconfig; dead `src/utils/__database.ts` removed. `pnpm --filter dashboard_server build` exits 0; `pnpm --filter dashboard_client_web check-types` exits 0. All `x_*` soft-deleted files removed from repo. pg Pool swap is now BUILD-VERIFIED (runtime pending Supabase). Publish tooling (`scripts/`) is broken and must be rewritten for pg Pool + Supabase Storage — tracked as Phase B task.
> **2026-06-25 deploy blockers resolved + CI created (commits d9b06e0, 4ee90a0, 99d48c2):** S4.1 CORS DONE. S1.1/S1.4/S1.5 DONE. @repo/markdown-parsing retired from client (inlined to utils/markdown/; package soft-deleted; force-dynamic blog page; client build clean). S5.1 all deploy blockers resolved (.nvmrc=22, article_server refs removed, both check-types pass). S7.1 CI created (install + check-types; lint omitted pending S7.2). S7.2 ESLint flat-config migration added as TODO ENGINEER.

---

## Repo Hygiene (completed 2026-06-24)

- [x] Remove `SESSION_SECRET` from `apps/dashboard_server/src/config/env.schema.ts`
  - Was an unused required var; not needed with Supabase + Vercel deployment. Removed.
- [x] Remove `AWS_RDS_CA` from `apps/dashboard_server/src/config/env.schema.ts`
  - Dead var; only referenced in a commented-out block. Removed.
- [x] Create `@repo/types` package (no build step; raw TS; `main`/`types` → `src/index.ts`)
  - Holds `ETreeNodeType`, `TCategoryTreeNode`, `TPostTreeNode`, `TCategoryTreeResponse`, `TTreeNode`, `TTreeNodeWithChildren`
  - Added `transpilePackages: ['@repo/types']` to `apps/dashboard_client_web/next.config`
- [x] Move API-boundary types out of `@repo/markdown-parsing` into `@repo/types`; add re-export in `@repo/markdown-parsing` for back-compat
- [x] Repoint import sites in both apps from `@repo/markdown-parsing` types to `@repo/types`
- [x] Fix 4 server publish-scripts that imported from `@repo/markdown-parsing/src/types` raw path → use `/types` subpath
- [x] Remove dead `@repo/assets` dependency from `apps/dashboard_client_web/package.json`
- [x] Replace `require.resolve("@repo/assets/...")` in `apps/dashboard_server/src/helpers/home.ts` with `path.join`

### Build fixes (completed 2026-06-25)

- [x] Add `@types/pg` to `apps/dashboard_server` devDependencies
  - Acceptance: `pnpm --filter dashboard_server check-types` exits 0 with no pg type errors
- [x] Exclude `scripts/` directory from `apps/dashboard_server/tsconfig.json` (offline tooling; not part of Vercel function)
  - Acceptance: `apps/dashboard_server/tsconfig.json` excludes `scripts/`; `pnpm --filter dashboard_server build` exits 0
- [x] Exclude `**/x_*` from `apps/dashboard_server/tsconfig.json`
  - Acceptance: soft-deleted files do not appear in typecheck output
- [x] Remove `apps/dashboard_server/src/utils/__database.ts` (dead Sequelize duplicate of `database.ts`)
  - Acceptance: file absent from git; `pnpm --filter dashboard_server check-types` exits 0
- [x] Retire `@repo/markdown-parsing` from client: inline `extractTocParsed` + `TTocItem` into `apps/dashboard_client_web/utils/markdown/`; add `github-slugger`, `unist-util-visit`, `@types/mdast` to client; remove dep from both apps; drop turbo dev `dependsOn @repo/markdown-parsing#build`; soft-delete package to `packages/x_markdown-parsing`; set `app/blog/page.tsx` to `force-dynamic` (fixes build-time ECONNREFUSED prerender) — commit 4ee90a0
  - Acceptance: `pnpm --filter dashboard_client_web build` compiles clean and generates pages without error; `pnpm --filter dashboard_client_web check-types` exits 0
- [x] Add `check-types` npm script to `apps/dashboard_server/package.json` — commit 99d48c2
  - Acceptance: `pnpm --filter dashboard_server check-types` exits 0

### pnpm install

- [x] Run `pnpm install` from repo root — `@repo/types` new workspace package; `sequelize` + `pg-hstore` removed; `@types/pg` added; lockfile regenerated
  - Acceptance: `pnpm install` exits 0; `pnpm --filter dashboard_client_web check-types` exits 0; `pnpm --filter dashboard_server check-types` exits 0

### x_* cleanup

- [x] All `x_*` soft-deleted files removed from the repository (financial grid, mock api, hooks, types, __database)
  - Note: `packages/x_axios` node_modules cruft remains on disk only — not in git; safe to `rm -rf` manually.

---

## Phase 0 — Workflow Baseline

- [x] Create `00.plan.md` at repo root
- [x] Create `00.tasks.md` at repo root
- [x] Create `01.status.md` at repo root

---

## Phase 1 — Blog MVP

- [x] Blog post list server-side render (first 10 posts via server action)
- [x] Infinite scroll load-more via route handler `/api/posts/load-more`
- [x] Per-post Markdown rendering (GFM, KaTeX, syntax highlighting)
- [x] Loading skeleton for post list
- [x] Desktop sticky navigation bar
- [x] Mobile bottom drawer navigation
- [x] Dark/light theme toggle with system default
- [x] next-intl i18n setup and locale routing
- [x] Favicon set and OG/Twitter metadata
- [x] Fix hydration error from `new Date()` in server component
- [x] Apply React `cache()` to post fetching
- [x] Dashboard grid layout scaffold

---

## Phase 2-simple — Financial Dashboard MVP

### Cycle 2 — Display Variant Prototypes (DONE)

> All Cycle 2 tasks are complete. Variant C won. Variants A and B are reference-only.

- [x] Define `IDashboardSnapshot` type in `apps/dashboard_client_web/types/dashboard.ts`
- [x] Define history types in `apps/dashboard_client_web/types/history.ts`
- [x] Create mock snapshot file `apis/getDashboardSnapshot.mock.api.ts`
- [x] Create mock history file `apis/getDashboardHistory.mock.api.ts` (250 daily candles, deterministic)
- [x] Build Variant A preview page at `app/dashboard/preview/cards/page.tsx`
- [x] Build Variant B preview page at `app/dashboard/preview/table/page.tsx`
- [x] Build Variant C preview page at `app/dashboard/preview/chart/page.tsx`
- [x] Create Jotai atom file `store/dashboard.atom.ts`
- [x] Build `ChartPanel.client.tsx` (TV-style, lightweight-charts v5)
- [x] Build `ChartHeader.client.tsx` (gear button lives here)
- [x] Build `ChartTimescale.client.tsx`, `ChartLegend.client.tsx`, `ChartConfigMenu.client.tsx`
- [x] Build `ChartDrawToolbar.client.tsx` (drawing tools + magnet snap + undo)
- [x] Build `ChartVariantLayout.client.tsx` (70/30 split)
- [x] Extract `hooks/useTradingViewChart.hook.ts`
- [x] Extract `utils/chart-format.ts`, `utils/computeMA.ts`, `utils/computeRSI.ts`, `utils/computeMACD.ts`, `utils/computeIchimoku.ts`, `utils/generateIntervalCandles.ts`
- [x] Create `store/drawings.atom.ts`
- [x] Create `lib/chart-theme.ts` (TV_DARK / TV_LIGHT)
- [x] SRP refactor: MA constants hoisted, duplicate `formatPrice` removed, `IndicatorState` grouped, `menuRef` ref-based
- [x] Soft-delete old grid: `x_FinancialGridLayout.client.tsx`, `x_widgets/`, `x_getFinancialData.mock.api.ts`, `x_useFinancialData.query.client.ts`, `x_useElementSize.hook.ts`
- [x] Remove `react-grid-layout` from `dashboard_client_web` package.json
- [x] Remove all `x_*` soft-deleted files from repo (cleanup 2026-06-25)

### Cycle 2 — Carry-over (non-blocking, do before Phase C hardening)

- [ ] Re-slim `ChartPanel.client.tsx` to ≤180 LOC OR formally raise file budget to ≤250 and document reason in file header
  - Acceptance: file is either ≤180 LOC or has a top-of-file comment explaining why the budget is raised; `check-types` exits 0
- [ ] Split `useTradingViewChart.hook.ts` (369 LOC): extract `useCrosshairHover` + per-indicator series effects OR document split plan in file header
  - Acceptance: file is ≤300 LOC (repo ceiling) or a documented split plan exists; `check-types` exits 0

### Cycle 3 — Full Implementation (ACTIVE)

#### Group A — Type contract + validation

- [ ] Finalize `IDashboardSnapshot` type in `apps/dashboard_client_web/types/dashboard.ts` for Cycle 3 shape
  - Acceptance: type exported, zero `any`, includes `fetchedAt: string` (ISO 8601)
- [ ] Create `apps/dashboard_client_web/app/actions/getDashboardSnapshot.schema.ts` — Zod schema + inferred TS type
  - Acceptance: Zod schema covers all 22 items; inferred type exported alongside schema; invalid API response causes typed error; no `any`

#### Group B — Server fetcher

- [ ] Create `apps/dashboard_client_web/app/actions/getDashboardSnapshot.action.ts`
  - Acceptance: Next.js Server Action; reads from internal backend when `DASHBOARD_DATA_SOURCE=backend`, else returns Cycle 2 mock; returns `IDashboardSnapshot`; backend URL from `.env`; no external API keys; network errors return typed error; `fetchedAt` is ISO string from backend response; schema imported, not redefined inline
- [ ] Create `apps/dashboard_client_web/app/actions/getDashboardHistory.action.ts`
  - Acceptance: accepts ticker label + interval, returns `IItemOHLCHistory | null`; mock fallback when `DASHBOARD_DATA_SOURCE=mock`; TanStack Query `staleTime: 5 * 60 * 1000`; lazy fetch on click; error fallback shows "history unavailable" without crashing
- [ ] Implement per-category polling in `hooks/useDashboardSnapshot.query.client.ts`
  - Acceptance: FX 60s; Crypto 30s; Indices US 60s; Indices KRX once per session; Rates 24h; Stocks 60s; all intervals are named constants

#### Group C — Page conversion

- [x] Replace `/dashboard/page.tsx` with `ChartVariantLayout` (mock data wired)
- [x] Add "Dashboard" link to desktop + mobile navigation
- [ ] Swap mock fetcher for real backend fetcher once `DASHBOARD_DATA_SOURCE=backend` is set
  - Acceptance: `/dashboard` shows real values for all 22 items; no item shows "undefined" or "NaN"; clicking watchlist row loads real OHLC history

#### Group D — Soft-delete old implementation

- [x] Rename superseded files with `x_` prefix (completed 2026-06-24)
- [x] Remove all `x_*` files from repo (completed 2026-06-25)

#### Group E — MVP acceptance gate

- [ ] All 22 items display value + 1-day change % with color indicator (real data)
  - Acceptance: manual browser check on `/dashboard` with `DASHBOARD_DATA_SOURCE=backend` confirms all 22 labels visible; no "undefined" or "NaN"
- [ ] Page renders in <2s on dev server with mock data
  - Acceptance: Chrome DevTools Network tab shows `/dashboard` document response under 2000ms on localhost
- [ ] Zero `any` types in Cycle 3 code paths
  - Acceptance: `pnpm --filter dashboard_client_web check-types` exits 0; grep for `: any` in `types/`, `app/actions/`, `hooks/` returns no matches
- [ ] No hydration errors on `/dashboard`
  - Acceptance: browser console shows zero hydration warnings on hard reload in both dev and production build

### Cycle 4 — Verification

- [ ] PM verifies all Cycle 3 Group E acceptance criteria against live `/dashboard`
  - Acceptance: all Group E items checked off; PM records pass/fail
- [ ] PM decides Cycle 5 scope (display polish, Phase C hardening, or Phase A deploy sprint)
  - Acceptance: `00.tasks.md` updated with Cycle 5 task list before any Cycle 5 work begins

---

## Phase A — Deploy: Vercel + Supabase (Read-Only, Free)

> Backend stays in the Turborepo monorepo at `apps/dashboard_server`. No standalone backend dir.
> Data access: pg Pool (max:1) → Supabase transaction pooler port 6543. Raw SQL unchanged. No supabase-js.
> Supabase CLI config and migrations live under `apps/dashboard_server/supabase/`.
> **Execution detail:** See `00.migration.md` at repo root. That file is the single source of truth for how to execute this phase — ordered stages S1–S9, owner per step, exact commands, acceptance criteria, and rollback points.

### Group A1 — Deploy blockers: dashboard_client_web

> **All Group A1 items resolved — commits 99d48c2 + 4ee90a0.**

- [x] Change `.nvmrc` from Node 24 to Node 22
  - Acceptance: `.nvmrc` contains `22`; `pnpm --filter dashboard_client_web build` succeeds locally with Node 22
- [x] Fix `@repo/axios` default-export typecheck error
  - Acceptance: `pnpm --filter dashboard_client_web check-types` exits 0 with no `@repo/axios` errors
- [x] Remove `console.log` statements from `env.schema.ts`
  - Acceptance: `grep -r "console.log" apps/dashboard_client_web/env.schema.ts` returns no matches
- [x] Rename or remove `proxy.ts` middleware (currently misnamed)
  - Acceptance: no file named `proxy.ts` exists in `apps/dashboard_client_web/` unless it is genuinely a proxy; if renamed, all imports updated; `check-types` exits 0
- [x] Resolve `article_server` workspace reference in root `package.json`
  - Acceptance: root `package.json` references only workspaces that exist in the tree; `pnpm ls -r --depth 0` exits 0 with no "workspace not found" error

### Group A2 — Backend: Vercel serverless adapter (dashboard_server, monorepo)

> **M3 status (2026-06-25):** T1–T4 DONE. Sequelize→pg Pool swap is BUILD-VERIFIED (`pnpm --filter dashboard_server build` exits 0; `pnpm --filter dashboard_client_web check-types` exits 0). Runtime verification pending Supabase DB live with data. `@types/pg` added; `scripts/` and `**/x_*` excluded from tsconfig; dead `__database.ts` removed; all `x_*` files removed from repo; `pnpm install` done. Schema reconciliation: `post_categories`, `post_tags.tag`, and `posts.parent_id` kept in migration DDL; no active queries use them; no DDL changes needed.
>
> **@repo/markdown-parsing retired (commit 4ee90a0):** `extractTocParsed` + `TTocItem` inlined into `apps/dashboard_client_web/utils/markdown/`. `github-slugger`, `unist-util-visit`, `@types/mdast` added to client devDeps. Dep removed from both apps. Turbo dev `dependsOn @repo/markdown-parsing#build` dropped (turbo.json updated). Package soft-deleted to `packages/x_markdown-parsing`. `app/blog/page.tsx` set to `force-dynamic`. Client build now compiles + generates pages clean.

- [x] Add `DATABASE_URL: z.string()` to `apps/dashboard_server/src/config/env.schema.ts` and export the validated value (M3 T1)
  - Acceptance: `env.schema.ts` contains `DATABASE_URL: z.string()`; value exported for use by pg Pool; build fails with descriptive Zod error if `DATABASE_URL` is absent; `pnpm --filter dashboard_server check-types` exits 0
- [x] Add `@vercel/node` export wrapper — `apps/dashboard_server/api/index.ts` exports `export default app` (M3 T2)
  - Acceptance: `apps/dashboard_server/api/index.ts` exists and exports the Express app as default; `pnpm --filter dashboard_server check-types` exits 0
- [x] Add `apps/dashboard_server/vercel.json` with catch-all route `{ "src": "/(.*)", "dest": "/api/index.ts" }` (M3 T3)
  - Acceptance: `apps/dashboard_server/vercel.json` exists; catch-all route present; `GET /posts` resolves through the entry point in a local Vercel dev simulation
- [x] Fix `apps/dashboard_server/src/helpers/home.ts` — `/status` route returns correct response (M3 T4)
  - Acceptance: `GET /status` returns a 200 with expected JSON; `pnpm --filter dashboard_server check-types` exits 0
- [x] Remove Sequelize execution layer; wire blog read queries to `pg` Pool pointing at Supabase transaction pooler (port 6543) (M3 T5 — BUILD-VERIFIED; runtime verification pending Supabase data)
  - Changes: `apps/dashboard_server/src/lib/database.ts` (pg Pool, max:1, reads DATABASE_URL); `apps/dashboard_server/src/helpers/posts.ts` (6 raw-SQL queries migrated to `pool.query()`); `apps/dashboard_server/src/index.ts` (pg Pool import wired). `sequelize` + `pg-hstore` removed from `apps/dashboard_server/package.json`. `@types/pg` added. `scripts/` and `**/x_*` excluded from tsconfig. Dead `__database.ts` removed.
  - Acceptance: no `sequelize` import in any file under `apps/dashboard_server/src/`; no supabase-js import introduced; all blog read queries execute via `pg` Pool; raw SQL in posts.ts unchanged in semantics; `pnpm --filter dashboard_server build` exits 0; runtime: blog routes return expected data against live Supabase DB
- [ ] Remove SSH tunnel code from `dashboard_server`
  - Acceptance: no SSH tunnel import or invocation in any file under `apps/dashboard_server/src/`; server starts without requiring an SSH connection
- [ ] Configure Vercel project #2 build to install from repo root (monorepo pattern)
  - Acceptance: `vercel.json` or Vercel dashboard install command is `pnpm install` run from repo root; build command resolves `@repo/*` workspace packages without error; first deploy build log shows zero "module not found" errors for workspace deps

### Group A3 — M2: Supabase blog schema migration

> Design DONE (2026-06-24). Draft migration files at `apps/dashboard_server/supabase/migrations/0001_blog_schema.sql` and `0002_rls_policies.sql`. Steps below require coordinated user + engineer actions.
>
> **Schema reconciliation finding (2026-06-25):** `post_categories` table, `post_tags.tag` column, and `posts.parent_id` column are present in the production schema but not referenced by any active query in `posts.ts`. These are kept as-is in the migration DDL to match production exactly. No columns are dropped or renamed. C4 reconciliation must account for this.
>
> **AWS Postgres DELETED (2026-06-25):** The AWS RDS instance no longer exists. C1 (schema dump) and C6/C7 (data dump + load) are no longer possible. Supabase is a fresh setup — no historical data to migrate. C1, C6, C7 are marked superseded below.

- [x] C1 (SUPERSEDED — AWS RDS deleted): Schema dump against AWS RDS no longer possible. Migration DDL (`0001_blog_schema.sql`) proceeds from the existing draft + schema reconciliation finding above; any [INFERRED] columns must be resolved by inspection of the draft file alone.
- [x] C2 (engineer): Run `supabase init` in `apps/dashboard_server/`; commit generated `supabase/config.toml` — DONE commit 99d48c2
  - Acceptance: `apps/dashboard_server/supabase/config.toml` exists and is committed — MET; `supabase` CLI recognises the project dir — MET
- [ ] C3 (USER): Run `supabase link --project-ref <ref>` to link CLI to the Supabase project
  - Acceptance: `supabase status` shows the project ref; no auth error
- [x] C4 (engineer): Reconcile migration files against schema reconciliation finding (2026-06-25); resolve any [INFERRED] columns by inspection; ensure migration is idempotent (`IF NOT EXISTS`); retain `post_categories`, `post_tags.tag`, `posts.parent_id` — DONE commit 99d48c2
  - Acceptance: `0001_blog_schema.sql` has no [INFERRED] placeholders — MET; `0002_rls_policies.sql` enables RLS + public SELECT on all tables — MET
- [ ] C5 (USER): Run `supabase db push` (direct port 5432) to apply migration to Supabase
  - Acceptance: `supabase db push` exits 0; all tables visible in Supabase dashboard
- [x] C6 (SUPERSEDED — AWS RDS deleted): Data dump from AWS RDS no longer possible. Supabase starts empty; fresh data will be authored directly.
- [x] C7 (SUPERSEDED — AWS RDS deleted): No data to load. Supabase starts empty.
- [ ] C8 (engineer): Commit `apps/dashboard_server/supabase/seed.sql` (dev seed, synthetic posts, at least 3 rows); ensure `seed.prod.sql` is listed in `.gitignore` and never committed
  - Acceptance: `seed.sql` committed with synthetic data; `git ls-files apps/dashboard_server/supabase/seed.prod.sql` returns no output (file is ignored)
- [x] C9 (engineer): DONE as M3 T1 — `DATABASE_URL: z.string()` added to `env.schema.ts` and exported.

### Group A4 — Vercel: project setup

- [ ] Create Vercel project #1 for `dashboard_client_web`
  - Acceptance: Vercel project exists with Root Directory = `apps/dashboard_client_web`; framework = Next.js; all required env vars set in Vercel dashboard; first deploy succeeds (build log shows zero errors)
- [ ] Create Vercel project #2 for `dashboard_server` (monorepo root install)
  - Acceptance: Vercel project exists with Root Directory = `apps/dashboard_server`; install command runs from repo root so workspace deps resolve; first deploy succeeds; `GET /posts` endpoint returns blog posts from Supabase via pg Pool
- [ ] Point `my.prettylog.com` DNS at Vercel project #1
  - Acceptance: `https://my.prettylog.com/blog/@taeklim` loads blog posts; HTTPS certificate valid
- [ ] (USER) Transfer GitHub repo from `tradelunch` org to individual GitHub account; update git remote
  - Acceptance: repo is under the individual GitHub account (not an organization); `git remote -v` shows the new individual URL; Vercel Hobby connects to the repo without requiring Pro; `git push` succeeds to the new remote

### Group A5 — CI

- [x] Create `.github/workflows/ci.yml` — DONE (commit 99d48c2); installs with `pnpm --frozen-lockfile`, runs `check-types` for both apps; lint step omitted pending S7.2
  - Acceptance: workflow runs on PR and push to main — MET; check-types steps pass — MET; badge goes green after first run post-push
- [ ] S7.2 — Migrate ESLint setup to `@next/eslint-plugin-next` flat-config so `pnpm lint` runs again; re-add lint step to `ci.yml`
  - Priority: Phase A nice-to-have; can defer to Phase B
  - Acceptance: `pnpm lint` exits 0 with `--max-warnings 0`; `ci.yml` includes `- run: pnpm lint` and the step passes; CI badge remains green

### Group A6 — Phase A acceptance gate

- [ ] `https://my.prettylog.com/blog/@taeklim` loads posts from Supabase; infinite scroll works
  - Acceptance: manual check; first 10 posts load without SSH tunnel or AWS connection; load-more works; data served via pg Pool from Supabase
- [ ] `https://my.prettylog.com/dashboard` loads chart with mock data; all 22 watchlist items visible
  - Acceptance: manual check; no console errors; chart renders
- [ ] CI passes on main branch
  - Acceptance: `.github/workflows/ci.yml` badge is green on main
- [ ] No secrets in source
  - Acceptance: `git grep -i "SUPABASE_SERVICE_ROLE_KEY\|CLERK_SECRET\|TWELVE_DATA\|DATABASE_URL" -- '*.ts' '*.tsx' '*.js'` returns no matches

---

## Phase B — DB Migration + Real Dashboard Data (PLANNED)

> Task breakdown will be written after Phase A is accepted.

Prerequisite: Phase A acceptance gate passed.

> **Cron note (DEFERRED — post-refactor):** No market-data ingestion cron is implemented in Phase B. Preferred future implementation is a GitHub Actions scheduled workflow. Without an active cron, Supabase's 7-day auto-pause is an accepted known risk; fallback is manual resume via Supabase dashboard.

> **Storage decision (DECIDED 2026-06-24, updated 2026-06-25):** AWS S3 has been DELETED. Supabase Storage is a fresh setup (not a migration). Blog images will be authored directly into Supabase Storage `blog-images` bucket from Phase B onward. No historical image migration required.

> **CDN_ASSET_POSTS (DECIDED 2026-06-24):** Points at Supabase Storage public URL for blog images. Optionally fronted by a Cloudflare CNAME later. Env var name and contract unchanged.

> **Publish tooling (BROKEN as of 2026-06-25):** `scripts/publish_posts.ts`, `scripts/publish_post/*`, and `scripts/fix_*.ts` used Sequelize + AWS S3 and are now non-functional. They are excluded from the Vercel build (`tsconfig.json` excludes `scripts/`). The rework task below must be completed before blog posts can be published again.

Placeholder tasks (to be expanded when Phase A is accepted):
- [ ] Define `market_snapshots` and `market_history` Supabase schema + migration SQL under `apps/dashboard_server/supabase/migrations/`
- [ ] Confirm NCSOFT KRX ticker suffix before implementing history fetcher
- [ ] Implement `GET /api/dashboard/snapshot` and `GET /api/dashboard/history` in `dashboard_server` using pg Pool + raw SQL
- [ ] Remove remaining SSH tunnel code if any survives Phase A
- [ ] Flip `DASHBOARD_DATA_SOURCE=backend` in Vercel env; verify Cycle 3 Group E acceptance criteria
- [ ] Verify Supabase DB size is under 500 MB before going live
- [ ] Configure Supabase Storage `blog-images` bucket (public); update `CDN_ASSET_POSTS` value to Supabase Storage public URL
  - Acceptance: blog image upload and serve uses Supabase Storage URLs; no AWS S3 SDK import remains in any file under `apps/dashboard_server/src/`; AWS S3 env vars (`AWS_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_S3_BUCKET`) absent from `env.schema.ts` and Vercel dashboard env; `CDN_ASSET_POSTS` value resolves to the Supabase Storage public URL
- [ ] Soft-delete `apps/dashboard_server/src/lib/awsS3.ts` → `x_awsS3.ts`; remove `aws-sdk` / `@aws-sdk/*` from `dashboard_server` package.json
  - Acceptance: `grep -r "awsS3\|aws-sdk\|@aws-sdk" apps/dashboard_server/src/` returns no matches in non-`x_` files; `pnpm --filter dashboard_server check-types` exits 0
- [ ] Rework publish tooling: rewrite `scripts/publish_posts.ts`, `scripts/publish_post/*`, and `scripts/fix_*.ts` for pg Pool + Supabase Storage (replacing Sequelize + AWS S3)
  - Acceptance: `scripts/publish_posts.ts` publishes a test post to Supabase DB via pg Pool with no Sequelize import; images upload to Supabase Storage `blog-images` bucket with no `@aws-sdk` import; `pnpm --filter dashboard_server build` exits 0 (scripts remain excluded from the Vercel build via tsconfig); at least one end-to-end publish run completes without error

---

## Phase C — Production Hardening (PLANNED)

> Task breakdown will be written after Phase B is accepted. Items below carry forward from the existing plan.

Prerequisite: Phase B accepted.

- [ ] `env.schema.ts` — Zod build-time failure on missing required vars
- [ ] Split `useTradingViewChart.hook.ts` (if not resolved in Cycle 2 carry-over)
- [ ] Extract service layer in `dashboard_server` controller (posts controller is 591 LOC; raw SQL stays, only structural duplication resolved)
- [ ] Remove or reconcile dead deps: `recharts` (if chart.tsx removed), `__database.ts` orphan
- [ ] Resolve `@repo/axios` vs local `apis/axios_instance.ts` — pick one, delete other
- [ ] Phase 4 task list checkbox reconciliation (pnpm done in practice)
- [ ] `.agent/rules/nexjs.rule.md` — consolidate into `.claude/rules/nexjts.md`

---

## Phase D — Multi-User Blog Platform, Invite-Only (PLANNED)

> Task breakdown will be written after Phase C is accepted. Sub-milestones defined in `00.plan.md`.

Prerequisite: Phase C accepted.

> Signup is invite-only. Open public signup is out of scope. Admin issues invite codes stored in an `invites` table; sign-up flow validates the code before creating the user row.

Sub-milestones:
- [ ] D1 — Auth Foundation (Clerk + Supabase JWT bridge + invite-code enforcement)
- [ ] D2 — Authoring UI (Editor + Draft Save)
- [ ] D3 — Publish + Profile Pages
- [ ] D4 — Admin Moderation + Invite Management

---

## Phase 3 — Production Hardening (legacy label — merged into Phase C above)

- [ ] Audit `_scripts/` directory — determine purpose and move to correct location or delete
  - Acceptance: contents moved to named `packages/` subpackage with `package.json`, or directory removed and deletion committed
- [ ] Resolve `.agent/rules/nexjs.rule.md` duplication with `.claude/rules/nexjts.md`
  - Acceptance: exactly one canonical Next.js rule file exists; references updated in any agents that consume it
- [ ] Decide language policy for `docs/`
  - Acceptance: `docs/doc.nextjs.api.actions.md` is either translated to English and kept in `docs/`, or moved to `.claude/` if agent-facing only
- [ ] Verify `article_server` workspace status
  - Acceptance: either `apps/article_server/` exists with valid `package.json`, or workspace entry removed from root `package.json` and `turbo.json` updated
- [ ] Add `env.schema.ts` build-time validation gate
  - Acceptance: build fails with descriptive error if required env variable is missing; no secrets hardcoded in source

---

## Phase 4 — Package Manager Migration (pnpm 9.5.0) — DONE IN PRACTICE

> `pnpm-lock.yaml` present, `yarn.lock` removed (commit `150d460`). Checkbox items below are stale; reconcile in Phase C.

- [x] `pnpm-lock.yaml` exists and `yarn.lock` absent (confirmed in tree)
- [ ] Set `"packageManager": "pnpm@9.5.0"` in root `package.json` and remove `"workspaces"` field
- [ ] Fix broken `dev:web` and `dev:server` script targets in root `package.json`
- [ ] Create `pnpm-workspace.yaml` at repo root
- [ ] Create `.npmrc` at repo root enforcing strict isolation
- [ ] Remove `yarn` from build scripts in `apps/dashboard_client_web/package.json`, `packages/markdown-parsing/package.json`, `packages/axios/package.json`
- [ ] Update yarn references in documentation files (CLAUDE.md, READMEs, guide files)
- [ ] Replace yarn permission entries in `.claude/settings.local.json`
- [ ] Verify: `pnpm ls -r --depth 0` exits 0
- [ ] Verify: `turbo dev` starts without module-not-found errors
- [ ] Verify: `pnpm --filter dashboard_client_web run check-types` exits 0
- [ ] Verify: `pnpm --filter dashboard_client_web run build` exits 0
