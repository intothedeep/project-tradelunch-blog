# Tasks — tradelunch

**Last updated:** 2026-06-29
**Status** (per-phase detail in the stubs below; full history in `_docs/archive/`):
- **DONE/SHIPPED:** Phase 1 · Repo Hygiene · dashboard Cycle 2 · Phase A (live on `my.prettylog.com`) · blog_agent merge · Phase B backend + publish pipeline · Phase D (ENG) · Phase E + E2 · Phase F (`2a0457b`) · F2 (`3e5b04c`) · G (`bced84e`) · Phase H P0–P5 (`2c0f897`/`f83a38e`) · blog card actions (`894fce3`) · Phase 2-mobile (MOB.0–MOB.3) · **Phase 2-filter** (multi-category + multi-tag feed filter, F.1–F.11, commits `a05a299`..`4e8483b`, jest 25 suites/166 tests) · Phase I committed (`56b8be5`) + ENG follow-ups LIVE (`f971661`: parquet archive+seed 11636 rows/83 obj uploaded, I2.8 cache + migration 0013 applied, SECRET_KEY+apikey, checkout v7).
- **ACTIVE/OPEN:** Phase 2-mobile (mobile context UX — SHIPPED MOB.0–MOB.3; note: Phase 2-filter SUPERSEDED its mobile category/tag NAV chip row → `MobileChipRow`/`getMobileChips` `x_`-retired, only `MobileAuthorChip` + Option-D TOC remain) · Phase 2-fetch (feed axios→native-fetch + tag revalidation — SHIPPED; OPEN follow-ups F-fetch.1 admin-delete revalidate, F-fetch.2 client-axios retirement) · Phase 2-simple Cycle 3 (dashboard mock→real — **COMPLETE**: snapshot live + `DASHBOARD_DATA_SOURCE=backend` flip done + chart history wired to backend; `/dashboard` fully backend-sourced) · Phase C (not started) · Phase H P6 (DEFERRED) · Phase I (collector-write live + db push 0011/0012/0013 applied; remaining = KOSPI/KOSDAQ `.KS/.KQ` coverage [Decision #32] + I1.16 USER live-prod READ smoke).
- **USER go-live gates:** see the consolidated snapshot below (Clerk env, backend `SUPABASE_*`, admin bootstrap, `db push` 0010, Cloudflare CNAME).
**Branch:** feature/finance → main

> Atomic execution breakdown. Product intent → `00.plan.md`. Running state → `01.status.md`.
> Verbose source docs (full migration runbook, blog_agent merge ADR) archived in `_docs/`.

**Legend:** `[x]` done · `[~]` in progress · `[ ]` todo · **(USER)** needs Supabase/Vercel/DNS creds · **(ENG)** code only · **SUPERSEDED** no longer applicable.

---

## Go-live USER gates (consolidated snapshot — 2026-06-26)

**DONE:** `supabase db push` of `0004` (market) + `0005` (draft enum) applied to remote (all
`0001`–`0005` Local=Remote); `seed.market.sql` loaded (`market_snapshots`=22, `market_history`=5500);
Supabase Storage bucket `blog.prettylog` created PUBLIC; **blog_agent first real publish landed live**
(`java-spring-jdbc`, user_id=1, status='public', thumbnail public URL) and reads back via
`GET /v1/api/posts`.

**DONE (added 2026-06-29):** Clerk Production env on BOTH Vercel projects (`pk_live`/`sk_live`,
backend `ALLOWED_ORIGINS`) + public-sign-up ON *(D1.1)*; `0014_error_logs.sql` applied *(error log live)*.

**STILL OPEN (USER):**
- Backend Vercel `SUPABASE_URL` + `SUPABASE_SECRET_KEY` — else image upload → 503. *(D2.6a)*
- Admin bootstrap SQL `UPDATE users SET is_admin=true WHERE username='<owner>'` — owner =
  `taek.lim.us@gmail.com`, AFTER signup + onboarding; `email` is NULL for lazy-provisioned users →
  match by `username`/`clerk_user_id`, never email. *(D4.0b)*
- ~~Flip `DASHBOARD_DATA_SOURCE=backend` on frontend Vercel + redeploy.~~ **DONE (2026-06-29)** — flipped + redeployed; chart history also wired. *(Phase B B4)*
- market-data provider + KRX `.KS/.KQ` suffix decision (KOSPI/KOSDAQ snapshot null until resolved). *(Phase B / Decisions #32)*
- Set blog_agent local `.env` `DEFAULT_USER_ID=2`→`1` (sandbox blocked the edit; non-blocking —
  post frontmatter `userId` drives publisher identity). *(B5.7)*

> **NOTE (not an open task):** Cloudflare `CDN_ASSETS` CNAME is branded-domain-only polish — parked, not on the active board. Revisit only when the branded asset domain is actually wanted.

---

## Repo Hygiene — DONE (2026-06-24/25)
> `@repo/types` created; `@repo/markdown-parsing` retired from client (TOC inlined); sequelize/pg-hstore removed (`@types/pg` added); express-types override → direct devDep (f026fca). <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.repo-hygiene.md](./_docs/archive/tasks.repo-hygiene.md)

---

## Phase 1 — Blog MVP — DONE
> SSR post list, infinite scroll, Markdown render (GFM/KaTeX/highlight), nav, theme toggle, next-intl i18n, OG metadata. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-1-and-dashboard-cycle2.md](./_docs/archive/tasks.phase-1-and-dashboard-cycle2.md)

---

## Phase 2-simple — Financial Dashboard MVP

### Cycle 2 — Variant prototypes — DONE
> Variant C (TradingView-style chart) adopted; chart panel/header/legend/toolbar + `useTradingViewChart.hook` + indicator core + intraday intervals built; old grid purged. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-1-and-dashboard-cycle2.md](./_docs/archive/tasks.phase-1-and-dashboard-cycle2.md)

### Cycle 2 — Carry-over (before Phase C)
- [x] Re-slim `ChartPanel.client.tsx` (329→202 LOC; documented ≤210 budget banner — extraction floor, global ≤300). Extracted `useChartIndicators`/`useChartPanelMenu` hooks + `deriveChartHeader` util + `ChartIndicatorCloseButtons`.
- [x] Split `useTradingViewChart.hook.ts` (524→195 LOC) into pure builders `utils/chart-setup`+`chart-series`+`chart-panes` (each ≤300); React lifecycle (measure/crosshair/resize/teardown) stays in the hook. architect→developer→architect APPROVE; pure structural, zero behavior change.

### Cycle 3 — Full Implementation (ACTIVE)
- [ ] Finalize `IDashboardSnapshot` (Cycle 3 shape; `fetchedAt` ISO; zero `any`)
- [ ] `app/actions/getDashboardSnapshot.schema.ts` — Zod schema covering all 22 items + inferred type
- [x] `app/actions/getDashboardSnapshot.action.ts` — Server Action; backend when `DASHBOARD_DATA_SOURCE=backend`, else mock; typed errors (LIVE)
- [x] `app/actions/getDashboardHistory.action.ts` — label+interval → history; lazy per-selected-label; gated by `DASHBOARD_DATA_SOURCE`. `ChartPanel.client.tsx` fetches via `useDashboardHistory` at constant `'1d'`; W/M/intraday client-derived by `generateIntervalCandles`; loading + "No chart data" empty states.
- [ ] `hooks/useDashboardSnapshot.query.client.ts` — per-category polling (FX 60s, Crypto 30s, Indices US 60s, KRX once/session, Rates 24h, Stocks 60s)
- [x] Replace `/dashboard/page.tsx` with `ChartVariantLayout` (mock wired); add Dashboard nav link
- [x] Swap mock→backend fetcher once `DASHBOARD_DATA_SOURCE=backend` — DONE: snapshot live + flag flipped; CHART history wired to backend (mock full-map `history` prop removed across `ChartVariantLayout`/`page.tsx`/`[username]`/`preview/chart`). Remaining separate: KOSPI/KOSDAQ null under Yahoo `.KS/.KQ` (Decision #32); intraday 1m–4h synthesized from daily.
- [x] Soft-delete + purge old implementation

### Cycle 3 — Group E acceptance gate
- [ ] All 22 items show value + 1-day change % with color (real data; no "undefined"/"NaN")
- [ ] `/dashboard` renders <2s on dev with mock
- [ ] Zero `any` in Cycle 3 paths (`check-types` exit 0; grep `: any` clean)
- [ ] No hydration errors on `/dashboard`

### Cycle 4 — Verification
- [ ] PM verifies Group E against live `/dashboard`; decides Cycle 5 scope

---

## Phase 2-mobile — Mobile context UX (chip header + in-article TOC) — SHIPPED (MOB.0–MOB.3)
> Two additive client-side-only mobile (`<lg`) UX changes — (D) top-of-article collapsible TOC, (B) top-of-feed context header (author chip + chip row) — replacing the heavier `<lg` UserContextSheet; Phase 2-filter later SUPERSEDED the NAV chip row (`MobileChipRow`/`getMobileChips` `x_`-retired). Decision #33. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-2-mobile.md](./_docs/archive/tasks.phase-2-mobile.md)

---

## Phase 2-filter — Multi-category + multi-tag feed filter (desktop rails + mobile rows) — DONE (SHIPPED 2026-06-29)
> Reader narrows the author feed `/blog/[username]` by categories AND/OR tags at all breakpoints; URL state `?categories=a,b&tags=a,b`, same-type OR / cross-type AND, no migration; desktop rails (`mode:'filter'`) + mobile FilterChipRows + AppliedFilters bar. SUPERSEDES Phase 2-tags. Decision #34. Gate: jest 25 suites/166. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-2-filter.md](./_docs/archive/tasks.phase-2-filter.md)

---

## Phase 2-fetch — Feed data layer: axios→native fetch + tag revalidation — DONE (SHIPPED 2026-06-29)
> Blog feed read moved off client axios onto a server-only native-fetch wrapper (Next 16 tag revalidation → instant read-your-writes on publish-class mutations); Express CDN backstop lowered. F-fetch.1 (admin-delete revalidate) DONE; F-fetch.2 (axios retirement) parked. Decision #35. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-2-fetch.md](./_docs/archive/tasks.phase-2-fetch.md)

---

## Phase A — Deploy: Vercel + Supabase (Read-Only, Free) — acceptance gate MET (2026-06-26)
> Backend on Vercel via `pg` Pool → Supabase (S1–S5 done; SSL `sslmode` strip + self-contained tsconfig deploy fixes); blog + mock dashboard live on `my.prettylog.com`; CI typecheck. Full S1–S9 runbook archived. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-A.md](./_docs/archive/tasks.phase-A.md)
> **Open tail:** S4-H.2 (pg pool `max:5` commit/verify) · S6.1 (repo under individual acct) / S6.3 (optional backend domain) · S7.2 (lint `--max-warnings 0` CI gate; ~207 client warnings + blog_agent ruff/pyenv) · S8.4 (Cloudflare `CDN_ASSETS` CNAME) · S9.1–S9.3 (AWS dep/env retirement).

---

## blog_agent merge (apps/blog_agent) — DONE
> Phase A lift-and-shift (f51cc7e) + Phase B BA-09 Supabase/Cloudflare repoint (`DB_PG_*`→`POSTGRES_*`, boto3→supabase-py, first real publish live). <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.blog-agent-merge.md](./_docs/archive/tasks.blog-agent-merge.md)

---

## Phase B — Dashboard backend + Supabase Storage repoint — backend + publish DONE (84b9d32)
> Backend B0–B3 (migration `0004` market tables + dashboard read endpoints + seed) DONE; blog_agent publish (B5/BA-09) WORKING end-to-end (first real publish live). <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-B.md](./_docs/archive/tasks.phase-B.md)
> **Open:**
> - [x] **B4 (ENG)** Client Cycle 3 action layer reads B2 endpoints when `DASHBOARD_DATA_SOURCE=backend`, else mock — **DONE (2026-06-29)**: snapshot live + USER flag flipped + chart history wired (`useDashboardHistory` `'1d'` lazy per-label; mock `history` prop removed). `/dashboard` fully backend-sourced.
> - [ ] **B5.6 (ENG)** Re-publish with `ollama serve` up so tags/categories/description populate.
> - [ ] **B5.7 (USER)** Set blog_agent local `.env` `DEFAULT_USER_ID`→`1` (non-blocking).
> - [ ] **(USER)** market-data provider + KRX `.KS/.KQ` suffix decision (Phase I / Decisions #32). *(`DASHBOARD_DATA_SOURCE=backend` flip DONE 2026-06-29. Cloudflare `CDN_ASSETS` CNAME = S8.4 branded-domain-only — parked, see go-live NOTE.)*
> - [ ] **S8.2 / S9.1–S9.3** AWS retirement (soft-delete `awsS3.ts`/SSH/RDS CA, remove AWS deps, drop Vercel AWS env). *(B5.5 client cursor precision — CODE DONE; runtime verify only, awaits >10 posts.)*

---

## Phase C — planned

**Phase C (after B):** env Zod build-gate; hook/component LOC splits; server service layer; dead-dep
reconcile; `@repo/axios` vs local axios; Phase 4 pnpm checkbox cleanup; `.agent` rule consolidation.

---

## Phase D — Multi-User Blog Platform (Clerk Auth) — ENG COMPLETE
> Express-layer Clerk `verifyToken` over pg Pool (RLS bypassed → owner-scoping is the gate); schema `0003`; lazy provisioning + `/onboarding` (8ca524e); D2 authoring + autosave (dc28f19) + UX cycle + i18n + thumbnail persist (31eb14a); D4 admin soft-delete moderation (70375e6). Open-signup default; webhook retired; `/admin/invites` DROPPED. Phase 4 pnpm migration DONE (150d460). <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-D.md](./_docs/archive/tasks.phase-D.md)
> **Open:**
> - [x] **D1.1 (USER) — DONE (2026-06-29)** Clerk Production (`pk_live`/`sk_live`) env on BOTH Vercel projects + `ALLOWED_ORIGINS` on backend + dashboard public-sign-up ON.
> - [ ] **D1.2 (USER, optional)** Register Clerk as a Supabase TPA provider.
> - [ ] **D2.6a (USER)** Backend Vercel `SUPABASE_URL` + `SUPABASE_SECRET_KEY` (else image upload → 503).
> - [ ] **D3.4 (ENG)** `/settings/profile` + `PATCH /v1/api/users/me` (residual, non-blocking).
> - [ ] **D4.0b (USER)** Admin bootstrap SQL `UPDATE users SET is_admin=true WHERE username='<owner>'` (match by username/clerk_user_id, never email).
> - [ ] **D4.1f (USER)** Live admin verify (blocked on D4.0b + Clerk env).

---

## Phase E — Blog Engagement (Saved Posts · Likes · Comments) — ENG COMPLETE + pushed `7b2ea0a`
> Saved Posts (`/me/saved`, migration `0007`), Likes (`0008` `post_likes`, optimistic detail+feed buttons), Comments (`0009` Option-C `BIGINT[]` materialized path, unlimited depth, `[deleted]` tombstone). 3 soft-delete Rule-1 gaps fixed. Issue #1 = PARALLEL (blog_agent comment columns untouched). All purely additive. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-E.md](./_docs/archive/tasks.phase-E.md)
> **Open:** USER apply-migrations gate — `supabase db push` `0007`+`0008`+`0009`, then re-run server jest for the DB-gated saved/likes/comments assertions.

---

## Phase E2 — Comment Edit + Comment Pagination — DONE/SHIPPED (origin/main HEAD `85093b4`)
> Closed Phase E's two deferrals: comment EDIT (`PATCH /comments/:id`, auth==delete via shared `assertCommentMutable`, tombstone→409, "(edited)" badge) + keyset pagination (50 roots/page each with full subtree, STRING cursor, `useInfiniteQuery`). `comments.ts` split into a barrel (C1). Purely additive — NO schema/index/migration. No open USER item (`0007`/`0008`/`0009` APPLIED; jest 78/78). <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-E2.md](./_docs/archive/tasks.phase-E2.md)

---

## Phase F — Blog Image Upload Rework (Express proxy + 2-step resize) — DONE (`2a0457b`)
> `POST /v1/api/posts/images` (multer memoryStorage + sharp `rotate().resize(1600,inside).webp(q80)`, server-to-server upload, full error mapping); client step-1 native-Canvas resize (no library); `vercel.json includeFiles` ships sharp libvips; old signed-URL path soft-deleted `x_*`. Decision #27. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-F.md](./_docs/archive/tasks.phase-F.md)
> **Open:**
> - [ ] **F17 (ENG/USER, manual e2e)** With backend `SUPABASE_*` set: normal JPEG → working `publicUrl`; EXIF-rotated correct; >5MB succeeds; non-image `.jpg`→415; creds unset→503. (Precondition: D2.6a.) Plus runtime confirm sharp libvips loads on Vercel.

---

## Phase F2 — blog_agent webp compression (local Python CLI) — DONE (`3e5b04c`)
> `apps/blog_agent/utils/image_transform.py` `to_webp` (Pillow LANCZOS + `exif_transpose`, 1600 fit-inside, q80, metadata stripped, `NotAnImageError`); body+thumbnail upload boundary forces webp; thumbnails keep 1200×630 letterbox (Option A); `_save_file_records` ext-from-stored_name fix; path scheme unchanged. Decision #28. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-F2.md](./_docs/archive/tasks.phase-F2.md)

---

## Phase G — Editor Category Selector + Tags (write/edit screen) — DONE (`bced84e`)
> Cascading 3-box category picker + flat tag-chip editor on `/write`; migration `0010` → category uniqueness `(user_id, parent_id, title)`; tri-state `category_id` PATCH (CASE); by-id read now returns `tags` + `stored_uri`; new `POST /v1/api/categories` (parent-scoped dedupe, soft-delete revive); AC1–AC10 met. Decision #29. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-G.md](./_docs/archive/tasks.phase-G.md)
> **Open (USER):** `supabase db push` migration `0010_category_unique_parent.sql`.

---

## Phase H — Multi-user Reddit-style Blog (global feed + context rails) — DONE P0–P5 (`2c0f897` / `f83a38e`)
> 3-col `BlogShell` route group (`app/(feed)`, cookie-collapse first-paint); `/tags/[tag]` global tag feed; global TopBar (SearchBar tag-nav + CreateButton); identical global LEFT rail (PrimaryNav + global TagCloud + mobile focus-trap); per-user context RIGHT rail (category tree + scoped tags + `UserProfileCard` from `GET /posts/users/:username/profile`) with `<lg` sheet; localStorage recents(20)/saved-tags(50); 4 viewer-agnostic tag-read APIs (slug-dedup-before-keyset, STRING cursors). Decision #30. <!-- ARCHIVE: history-only -->
> Detail: [_docs/archive/tasks.phase-H.md](./_docs/archive/tasks.phase-H.md)
> **Open:**
> **NOTE (not an open task):** P6 real search/discovery (`GET /v1/api/posts/search` title+tag ILIKE + keyset; `/explore` surface; H6.1/H6.2/H6.3) stays parked. A **disabled `/explore` PrimaryNav placeholder** ("coming soon", non-clickable, Compass icon) is now live (`useNavLinks.hook.ts` `disabled:true` + `nav.explore` both locales) so the trailing nav slot reads intentional. Flip `disabled` off + add the route when P6 is actually picked up.
> - [ ] **HX.1 (architect)** decide client `TPost` re-derive from `@repo/types` `TFeedPost` vs parallel.
> *(HX.2 split `posts.ts`→6 modules: DONE `db00d42`.)*

---

## Phase I — Stock Collector 편입 + Yahoo OHLC 적재 + 시총 랭킹 — ENG BUILT (2026-06-29)

> **STATUS (2026-06-29, COMMITTED to main as `56b8be5`; architect+ml-engineer+PM re-validated PASS, 51 pytest green, check:schema in sync; db_sink.connect() now pins session timezone=UTC):**
> Built at `apps/stock_collector/` (SIMPLE COPY of lib+yahoo_client; polyglot sibling, uv+py3.12). Layout
> refinement (architect): all Python under `src/` → import root `collector.{config,schema,transform,ranking,sink,
> entrypoints}`; `configs/watchlist.yaml` a root data file. Functional core pure (psycopg ONLY in sink/db_sink.py).
> - **Phase 1 — DONE(ENG):** I1.1 copy-in · I1.2 gitignore · I1.3 packaging · I1.4 watchlist.yaml(42) + settings ·
>   I1.5 schema/rows · I1.6 ohlc · I1.7 snapshot(2 change cols) · I1.8 mapping · I1.9 fx_direction · I1.10
>   watchlist_loader(+fx_label validate) · I1.11 load_history · I1.12 refresh_snapshots(+ticker/exchange) · I1.13
>   run_daily(graceful-skip) · I1.14 collector-daily.yml · I1.15 seed FX fix · I1.17 collector-keepalive.yml.
>   **I1.16 = USER** (live DB write + dashboard verify; needs DATABASE_URL). LIVE Yahoo smoke 14/14 OK.
> - **Phase 2 — DONE(ENG):** I2.1 migration 0011_tracked_symbols · I2.2 migration 0012_market_rankings (both
>   dual-mirrored, check:schema green) · I2.3 ranking/universe · I2.4 ranking/screen · I2.5 sticky UPSERT · I2.6
>   rankings append · I2.7 run_weekly · I2.9 collector-weekly.yml. **I2.8** (market_cap caching opt) = ENG follow-up
>   (uses fast_info直接 now). **USER:** supabase db push 0011+0012 (re-confirm next free number first).
> - **Phase 1.5 — PARTIAL:** I1.5.1 parquet_sink DONE(+spec). **NOT wired to an entrypoint yet**; I1.5.0
>   (daily dividends/splits) + I1.5.1b (PRIVATE bucket, USER) + I1.5.2 (Storage upload step, USER) = follow-ups.
> - **Architect doc-flags:** (a) parquet_sink not yet invoked by any entrypoint; (b) collector assumes a **UTC DB
>   session** (Supabase default OK) to avoid a bar_time/as_of date off-by-one; (c) tests use `spec_*.py` (pyproject)
>   — intentional divergence from python.md `test_*.py`; (d) workflows use the official uv installer; pin
>   `astral-sh/setup-uv@<sha>` as hardening.
> - **OPEN USER gates:** GH Actions Secrets `DATABASE_URL` (session pooler) on both workflows · `supabase db push`
>   0011/0012 · PRIVATE parquet bucket + `SUPABASE_SERVICE_ROLE`. *(`DASHBOARD_DATA_SOURCE=backend` flip = Phase B B4 — DONE 2026-06-29.)*

> 외부 Python 시장-데이터 수집기를 `apps/stock_collector/`로 **SIMPLE COPY** 편입하여 **기존**
> 금융 대시보드(`market_snapshots`/`market_history`, reader `controllers/dashboard/dashboard.ts`)에
> 실데이터를 공급한다. Phase 1 = Yahoo(yfinance) OHLC 일간 적재 + 스냅샷 합성(mock→real 전환);
> Phase 1.5 = Parquet archive(Storage); Phase 2 = 시총 글로벌/섹터 랭킹으로 universe 자동 확장
> (sticky); Phase 3 = FRED/KRX/추가 소스(DEFERRED). 제품 의도 + locked 결정 + 아키텍처 + 리스크는
> `00.plan.md` "Phase I" + Decisions Log #31. system-architect가 기술 설계를 OWN — 아래 태스크는
> 제품-레벨(무엇 + done-condition)이다.
>
> **Locked 결정 (do NOT re-litigate):** SIMPLE COPY(subtree 아님; pnpm 무시 — package.json 없음;
> 원본 수동 제거) · 소스 = Yahoo ONLY(SEC 13F 제외) · 스케줄 = GitHub Actions cron(Supabase 아님) ·
> CSV는 ephemeral, **절대 커밋/업로드 안 함** · Supabase Postgres=serving(UPSERT `ON CONFLICT`),
> Storage+Parquet=archive(Phase 1.5) · 카테고리 = fx/crypto/indices/rates/stocks ·
> **Sticky universe (USER RULE):** 한 번 랭킹 진입 심볼은 영구 수집, 자동 제거/히스테리시스 없음,
> soft-delete(`deleted_at`)는 수동 admin 제거에만 · Sector depth = top-10 + global top-20 ·
> **FX 라벨 RESOLVED = source-native** · **rates RESOLVED = Phase 1 Yahoo treasury YIELD 지수** ·
> **DAILY universe = `watchlist.yaml` ∪ `tracked_symbols`(active)**(순수 `universe_resolve`, yaml-wins) ·
> **42-label seed**(전역 유일; KRX 스폿 지수 `^KS11`/`^KS200`/`^KQ11` 포함 — Yahoo-available, USER 승인
> 2026-06-28; Yahoo 미제공 KOSPI200 선물/KOSDAQ150만 Phase 3) · **Yahoo-coverage 규칙(USER 2026-06-28):**
> 심볼별 Yahoo fetch → 있으면 적재, 없으면 graceful skip(행 미기록, 런 실패 아님; daily/weekly partial 허용) ·
> **Massive·sec13f 제외**(universe=Wikipedia+IWB) · **provider OPEN(#32)**.
>
> **검증된 계약 사실 (코드 대조 완료 2026-06-28):** `market_history` PK=`(label,interval,bar_time)`,
> **category 없음** → db_sink는 LABEL로 기록(라벨 전역 유일) · `market_snapshots` PK=`(category,label)`,
> CHECK 5set, **change 컬럼은 2개** `change_absolute`+`change_percent`(둘 다 NOT NULL — 단일 change 아님),
> `value`/`change_absolute`/`change_percent`/`as_of`/`revalidate_seconds`/`fetched_at` NOT NULL +
> `ticker`/`exchange` NULLABLE(**stocks 행은 둘 다 채워야 함** — 리더가 stocks에서 반환) → meta(`as_of`=
> 최신 bar 날짜, `fetched_at`=런 시각, `revalidate_seconds` 카테고리별 fx=60/crypto=30/indices=60/
> rates=86400/stocks=60)는 collector가 **합성** · client `IStockItem.exchange`는 `'US'|'KRX'` non-null
> union. ⚠️ **리더 nuance(기존 계약, 재설계 안 함):** 카테고리 meta는 `categoryRows[0]`(seq 최소 행)에서만
> 읽음 → 같은 카테고리 내 라벨별 `as_of`가 달라도(US 휴장 vs KRX) 첫 행 값만 반영(표시상 nuance, non-blocking);
> 스냅샷 표시 순서 = `ORDER BY category, seq`(최초 insert 순) → sticky 신규 편입 심볼은 맨 뒤 append(yaml 순서
> 미보장); Phase 1은 일간 OHLC뿐이라 sub-minute revalidate(crypto=30/fx=60/stocks=60)는 준실시간 힌트일 뿐
> 데이터는 전일 종가(인트라데이 신선도는 Phase 3).
>
> **레이어/SRP 가드레일:** `apps/stock_collector/` 파일 ≤300 LOC, SRP, functional core(순수 transform/
> ranking, psycopg는 `sink/`에만 격리), 레이어 방향 entrypoints→(config|transform|ranking)→sink ·
> 테스트 `__tests__/spec_*.py`(pytest) · 마이그레이션은 BOTH schema SSOT copy에 mirror(`pnpm
> check:schema` green) · soft-delete 전용(어떤 행도 hard-DELETE 금지) · psycopg3 `cursor.executemany()`
> + `ON CONFLICT … DO UPDATE`(psycopg2 `execute_values` 금지).
>
> **마이그레이션 번호 주의:** **확정: `0010`(Phase G) 적용됨 → Phase I = `0011`(tracked_symbols)/
> `0012`(market_rankings); 적용 직전 `ls apps/dashboard_server/supabase/migrations/`로 재확인(multi-session).**

### Phase 1 — 편입 + Yahoo OHLC 일간 적재 + 대시보드 실데이터 전환
- [x] **I1.1 (ENG, copy-in)** 외부 collector 소스를 `apps/stock_collector/`로 SIMPLE COPY: 재사용 `src/lib/{job_queue,rate_limit,constants,read_all_tickers}` + `src/yahoo_client/{download_publisher,download_consumer}` — **`src/massive_client/download_tickers`는 copy하지 않는다**(Massive drop: import-time side-effect로 lib import 불가; universe는 Wikipedia+IWB로 조립) — **sec13f 다운로더 제외**. **Acceptance:** 위 lib/yahoo_client 모듈이 존재; `massive_client` 부재; sec13f **DOWNLOADER**(`download_13f`/`edgar_api`) 부재(grep ZERO); 재사용 lib에 남는 `PROVIDER_SEC13F` 상수는 허용(선택 scrub); 앱은 polyglot sibling으로 공존(`package.json` 없음 → pnpm workspace 비멤버).
- [x] **I1.2 (ENG, gitignore)** 루트 `.gitignore`에 `apps/stock_collector/{data,.venv,queue,*.csv,htmlcov}` 추가. **Acceptance:** `git status`에 collector의 raw CSV/`data`/`.venv`/`queue`/`htmlcov`가 나타나지 않음(원시 CSV 절대 커밋 안 됨).
- [x] **I1.3 (ENG, packaging)** `apps/stock_collector/`에 `pyproject.toml`(name=`stock-collector`) + `uv.toml` + `.python-version`(`3.12`) + `.env.example`(`DATABASE_URL` 등) 추가. **Acceptance:** `uv sync`가 격리된 `.venv`를 구성; `.python-version`=3.12; `.env.example`에 시크릿 placeholder만(실제 값 없음).
- [x] **I1.4 (ENG, config)** `configs/watchlist.yaml`(category-keyed; fx/crypto/rates-yields/indices/stocks **ALL 채움** — seed 목록은 `00.plan.md` Phase I) + `configs/settings.py`(env→상수 로딩). **Acceptance:** YAML이 5 카테고리 모두 비어있지 않게 로드; FX는 source-native 라벨(`EURUSD=X`→"EUR/USD", `KRW=X`→"USD/KRW", `JPY=X`→"USD/JPY", `THB=X`→"USD/THB"); rates는 `^IRX`/`^FVX`/`^TNX`/`^TYX` 포함; indices는 `^GSPC`/`^IXIC`/`^NDX`/`NQ=F` + **KRX 스폿 `^KS11`/`^KS200`/`^KQ11`(Yahoo-available 포함; KOSPI200 선물/KOSDAQ150만 Phase 3)**; **42개 전 심볼의 label이 전역 유일**(stocks label=ticker; indices/fx/crypto/rates 서술형 — 교차 충돌 0); watchlist_loader 중복-label 검증(I1.10) 통과.
- [x] **I1.5 (ENG, schema)** `schema/rows.py` — history/snapshot/tracked/ranking row의 타입드 dataclass/TypedDict. **Acceptance:** 각 row 타입이 DB 계약(컬럼/NOT NULL)을 반영; mypy(있다면) 0; 순수 데이터 정의(I/O 없음).
- [x] **I1.6 (ENG, transform — 순수)** `transform/ohlc.py`(CSV/yfinance row → history row, label resolve) + `__tests__/spec_ohlc.py`. **Acceptance:** 순수 함수(deterministic, 부작용 없음); 유효 OHLC → `(label,interval,bar_time)` 키 history row; spec 통과.
- [x] **I1.7 (ENG, transform — 순수)** `transform/snapshot.py`(최신 2 bar → snapshot `value` + **change 컬럼 2개**(`change_absolute`=latest−prev, `change_percent`=(latest−prev)/prev×100; 스냅샷 테이블은 둘 다 `NOT NULL`) + **합성** `as_of`/`fetched_at`/`revalidate_seconds`[카테고리별]) + `__tests__/spec_snapshot.py`. **Acceptance:** `change_absolute`=latest−prev **및** `change_percent`=(latest−prev)/prev×100 **둘 다** 산출(단일 change 아님 — 리더가 `change:{absolute,percent}`로 매핑); `value`/`change_absolute`/`change_percent`/`as_of`/`revalidate_seconds`/`fetched_at` NOT NULL 충족; revalidate가 카테고리 맵(fx=60/crypto=30/indices=60/rates=86400/stocks=60)을 따름; spec 통과.
- [x] **I1.8 (ENG, transform — 순수)** `transform/mapping.py`(symbol↔label↔category↔exchange 매핑; exchange는 `'US'|'KRX'` union; label 전역 유일 보장) + `__tests__/spec_mapping.py`. **Acceptance:** KRX `.KS/.KQ` 개별주 심볼 → `'KRX'`, 그 외 → `'US'`(`exchange`는 `stocks` 카테고리에만; **Phase 1 stocks는 전부 US 티커 → 항상 `'US'`**; KRX 스폿 지수 `^KS11`/`^KS200`/`^KQ11`은 `indices` 카테고리라 exchange 무관); 라벨 충돌 시 명확한 에러; spec 통과.
- [x] **I1.9 (ENG, transform — 순수)** `transform/fx_direction.py`(source-native FX 방향/라벨 결정) + `__tests__/spec_fx_direction.py`. **Acceptance:** `EURUSD=X`→"EUR/USD"; `KRW=X`→"USD/KRW"; `JPY=X`→"USD/JPY"; `THB=X`→"USD/THB"; spec 통과.
- [x] **I1.10 (ENG, config-loader + 검증)** `config/watchlist_loader.py` — YAML 로드 + 검증(라벨 전역 유일, exchange union, category 5set 멤버십). **Acceptance:** 중복 라벨/잘못된 exchange/잘못된 category 입력 시 각각 명확히 거부; 정상 YAML은 통과; spec 통과.
- [x] **I1.11 (ENG, sink — IO 경계)** `sink/db_sink.py` `load_history` — `transform.ohlc`로 만든 history row를 `market_history`에 UPSERT `ON CONFLICT(label,interval,bar_time) DO UPDATE`; psycopg는 이 레이어에만. (psycopg3 `cursor.executemany()` + `ON CONFLICT … DO UPDATE`; psycopg2 `execute_values` 금지.) **Acceptance:** 동일 `(label,interval,bar_time)` 재적재가 idempotent(중복 행 없음); 라벨로 기록(category 컬럼 의존 없음).
- [x] **I1.12 (ENG, sink — IO 경계)** `sink/db_sink.py` `refresh_snapshots` — `transform.snapshot`(합성 meta 포함)을 `market_snapshots`에 UPSERT `ON CONFLICT(category,label) DO UPDATE`. **stocks 행은 `ticker`+`exchange`(`'US'|'KRX'`)도 함께 기록**(리더가 `category='stocks'`에서 `ticker`/`exchange`를 반환하고 클라이언트 `IStockItem.exchange`는 **non-null** union이므로 누락 시 계약 위반). (psycopg3 `cursor.executemany()` + `ON CONFLICT … DO UPDATE`; psycopg2 `execute_values` 금지.) **Acceptance:** Yahoo가 커버한 watchlist 항목(≤42) 각각에 대해 `value`/`change_absolute`/`change_percent`/`as_of`/`revalidate_seconds`/`fetched_at`가 NOT NULL로 기록(Yahoo 미커버 심볼은 행 미기록 = graceful skip); **stocks 항목은 `exchange`가 `'US'|'KRX'`로 non-null이고 `ticker` 채워짐**; 재실행 idempotent.
- [x] **I1.13 (ENG, entrypoint + universe_resolve)** `entrypoints/run_daily.py` — **collection target = `watchlist.yaml` ∪ `tracked_symbols`(active)**: 순수 `transform.universe_resolve.resolve_universe`가 yaml 엔트리와 `sink.db_sink.read_tracked_symbols()`(table-guarded `to_regclass` → 테이블 부재 시 `[]`)를 symbol-dedupe(yaml이 label/category/exchange 우선)로 UNION → label별 incremental `from_date`(`max(bar_time)+1d`) → yahoo publisher/consumer → CSV(ephemeral) → `load_history` → `refresh_snapshots`. **Yahoo-coverage 규칙(USER):** Yahoo가 빈 응답/미커버하는 심볼은 그 심볼만 **graceful skip**(행 미기록, 런 abort 금지) — 전체 partial 허용. + `__tests__/spec_universe_resolve.py`. **Acceptance:** universe = yaml ∪ tracked; symbol-dedupe yaml-wins on label/category/exchange; tracked_symbols absent(Phase 1) → read returns `[]`, run_daily unbroken; `resolve_universe` pure; 빈 DB에서 풀 적재; **일부 심볼 Yahoo 미커버 시 그 심볼만 skip되고 런 완료(나머지 적재)**; 재실행 시 incremental(최신 bar 이후만 fetch); CSV는 런 종료 후 커밋/업로드되지 않음; spec passes.
- [x] **I1.14 (ENG, CI cron)** `.github/workflows/collector-daily.yml` — cron `"30 21 * * 1-5"`(월–금 종가 직접 캡처; 토요일 휴장 drop; incremental `from_date`가 catch-up), `astral-sh/setup-uv`(commit SHA pin `# v8.x`, `enable-cache: true`, `working-directory: apps/stock_collector`), `uv sync`, `run_daily`, `concurrency:` group(중복 런 취소); 시크릿 `DATABASE_URL` = Supabase Supavisor **session pooler**(`aws-0-<region>.pooler.supabase.com:5432` — direct `db.<ref>.supabase.co:5432`는 IPv6-only라 IPv4 GH runner에서 실패하므로 금지). **Acceptance:** 워크플로가 GH Actions Secrets의 session-pooler `DATABASE_URL`을 사용; CSV 업로드 step 없음; 스케줄 트리거 + 수동 dispatch 가능; **워크플로 실패 시 알림(`if: failure()` step → issue/webhook); silent 스케줄 실패 방지.**
- [x] **I1.15 (ENG, seed fix — *(선택)* ref-consistency)** *(선택)* mock 참조파일 `seed.market.sql`의 FX 라벨을 source-native로 갱신(`'KRW/USD'`→`'USD/KRW'` 등) — **prod 영향 없음**(`seed.market.sql`은 `config.toml` `sql_paths`에 없어 auto-apply 안 됨; prod `market_snapshots`는 collector 적재 전까지 EMPTY). **Acceptance:** `seed.market.sql`에 `'KRW/USD'` 부재; FX 라벨이 collector source-native 라벨과 일치; `pnpm check:schema` in sync.
- [~] **I1.16 (ENG/USER, verify)** ✅ collector-WRITE verified LIVE (run 28389772672: universe=42, 9870 bars + 42 snapshots, skipped=0); ⏳ dashboard-READ: `DASHBOARD_DATA_SOURCE=backend` flip DONE + chart history wired → 남은 건 USER의 prod live-READ 확인(대시보드 `GET /v1/api/dashboard/snapshot` + `/history`가 실데이터 반환, mock 아님). **Acceptance:** **Yahoo 커버 watchlist 항목(≤42)**이 실 value + 1-day change 반환(`undefined`/`NaN` 없음; Yahoo 미커버 심볼은 행 자체가 없음 — graceful skip); `/history` by **label**(`?label=…&interval=…`; reader `WHERE label=$1`)이 실 OHLC 시리즈 반환. *(USER: `DATABASE_URL` 시크릿 세팅 + `DASHBOARD_DATA_SOURCE=backend` flip은 Phase B B4와 공유.)*
- [x] **I1.17 (ENG, CI keepalive)** `.github/workflows/collector-keepalive.yml` — 주기적(`cron "0 12 1,15 * *"`) GH API touch/dummy-commit으로 repo 활동 유지 → 60일 무커밋 cron auto-disable 방지. **Acceptance:** keepalive가 ≤60일 간격 트리거; daily/weekly cron이 60일 후에도 enabled. (Supabase 7일 pause와 별개 — GH 측 제약.)

### Phase 1.5 — Parquet archive (Supabase Storage)
- [x] **I1.5.0 (ENG, dividends/splits)** ✅ (e2a5e5c — captured from the same Yahoo frame, no extra fetch) parquet에 필요한 `dividends`/`stock_splits` 확보 — (a) copy된 `download_consumer.py` `YAHOO_FIELDS`+`writerow` 확장 또는 (b) `parquet_sink`에서 yfinance `Ticker.actions` 재-fetch(serving CSV 불필요하므로 (b) 선호). **Acceptance:** 현재-연도 parquet에 dividends/stock_splits(float64 nullable) 채워짐. (copy-in 시 line-ref 재확인.)
- [x] **I1.5.1 (ENG, sink — IO 경계)** *(NOTE: sink BUILT + spec'd, but NOT yet invoked by any entrypoint — orphaned until I1.5.2 wiring.)* `sink/parquet_sink.py` — pyarrow, **zstd** 압축, 파티션 `market/{source}/{ticker}/{ticker}_{YYYY}.parquet`; 컬럼 `symbol`(dict string)/`date`(**date32**)/`open/high/low/close`(float64)/`adj_close`(float64)/`volume`(int64)/`dividends`(float64 nullable)/`stock_splits`(float64 nullable); 현재-연도 파일 = **read-merge-rewrite**(read→concat→date dedupe keep=last→sort→고정 스키마 cast→tmp write→atomic `os.replace`); 과거-연도 파일 immutable + `__tests__/spec_parquet_sink.py`. **Acceptance:** 같은 date 재적재가 dedupe(중복 없음); 스키마 고정(date32, dividends/stock_splits 포함); atomic replace; spec 통과.
- [x] **I1.5.1b (ENG/USER, storage bucket)** ✅ (market-archive PRIVATE created) parquet 전용 **PRIVATE** Supabase Storage 버킷 생성(공개 blog 버킷과 분리). **Acceptance:** 버킷 PRIVATE; analytics 전용. (object-upsert는 CDN-stale eventual consistency — archive엔 허용.)
- [x] **I1.5.2 (ENG, CI cron)** ✅ (f971661 — storage_sink+upload_archive+collector-seed-archive.yml; validated 83/83 upload; uses `SUPABASE_SECRET_KEY`(sb_secret_, 2024+) + `apikey` header, NOT legacy service_role) daily 워크플로에 Storage archive step 추가 + 시크릿 `SUPABASE_URL`+`SUPABASE_SECRET_KEY`(Storage object upsert). **Acceptance:** archive step이 현재-연도 Parquet를 Supabase Storage(PRIVATE 버킷)에 object-upsert; **원시 CSV는 업로드 안 함**; 시크릿은 GH Actions Secrets.

### Phase 2 — 시총 글로벌/섹터 랭킹 (weekly, sticky universe)
- [x] **I2.1 (ENG, migration)** `tracked_symbols` 마이그레이션 — `symbol`(PK), `category`(CHECK 5set), `label`(UNIQUE — 전역 history 라벨 보호), `sector` NULL, `source` DEFAULT `'yahoo'`, `exchange` NULL, `first_ranked_at`, `last_ranked_at`, `created_at`, `updated_at`, `deleted_at`(soft-delete 전용); `category` index `WHERE deleted_at IS NULL`. BOTH schema SSOT copy에 mirror. **번호 = `0011`(확정; `0010` Phase G 점유).** **Acceptance:** 마이그레이션 idempotent-guarded + USER apply 노트; `label` UNIQUE 강제; `pnpm check:schema` in sync; **적용 직전 `ls apps/dashboard_server/supabase/migrations/`로 다음 빈 번호 재확인(multi-session; 현재 0010 점유 → 기본 0011/0012).**
- [x] **I2.2 (ENG, migration)** `market_rankings` 마이그레이션 — append-only 주간 시계열: `as_of` DATE, `symbol`, `scope`('global'|'sector' CHECK), `sector` NULL, `rank` INT, `market_cap` NUMERIC NULL, `created_at`; PK`(as_of,symbol,scope)`; **모든** tracked 심볼을 주간 기록; idempotent `ON CONFLICT(as_of,symbol,scope) DO UPDATE`. BOTH schema SSOT copy에 mirror. **번호 = `0012`(I2.1 다음).** **Acceptance:** 같은 주(as_of) 재실행 idempotent; top-N뿐 아니라 전체 tracked 기록; `pnpm check:schema` in sync; **적용 직전 `ls apps/dashboard_server/supabase/migrations/`로 다음 빈 번호 재확인(multi-session; 현재 0010 점유 → 기본 0011/0012).**
- [x] **I2.3 (ENG, ranking — 순수)** `ranking/universe.py` `assemble` — large-cap 후보 풀(~600–1000: **Wikipedia S&P500 + Wikipedia Nasdaq100 + iShares IWB CSV(Russell 1000)**; S&P500 leg는 GitHub `datasets/s-and-p-500-companies` CSV를 parser-stable primary/fallback로; **Massive 의존 없음**) 조립; 심볼 정규화(Wikipedia `BRK.B` vs yfinance `BRK-B` 양형 저장) + `__tests__/spec_universe.py`. **Acceptance:** 순수 함수; 중복 제거된 심볼 풀 반환; `BRK.B`/`BRK-B` 양형 보존; spec 통과.
- [x] **I2.4 (ENG, ranking — 순수)** `ranking/screen.py` `rank` — 시총 기준 global/sector 랭킹 산출(순수) + `__tests__/spec_screen.py`. **(PM+architect 결정: `market_rankings`는 COMPLETE 랭킹을 저장하고 `screen.rank`는 관측된 전 심볼을 global+sector 두 scope로 emit한다 — global top-20 / per-sector top-10은 STORAGE truncation이 아니라 query-time surfacing DEPTH다. 어느 심볼을 tracked로 sticky 편입할지(`select_tracked_symbols`)에만 top-N을 적용.)** **Acceptance:** 순수 함수; 모든 관측 심볼을 global+sector로 rank; 결정적 정렬키(known-cap 우선, cap DESC, symbol ASC tie-break); spec 통과.
- [x] **I2.5 (ENG, sink — sticky UNION)** `sink/db_sink.py` `tracked_symbols` UPSERT — `ON CONFLICT(symbol) DO UPDATE`로 UNION(sticky): 신규 진입 INSERT, 기존 심볼 `last_ranked_at` 갱신, **`deleted_at=NULL`로 revive**(자동 제거 절대 없음). (psycopg3 `cursor.executemany()` + `ON CONFLICT … DO UPDATE`; psycopg2 `execute_values` 금지.) **Acceptance:** 한 번 진입 심볼은 이후 랭킹에서 빠져도 tracked에 잔존(자동 soft-delete 없음); 수동 admin soft-delete 후 재진입 시 revive; **심볼이 tracked_symbols에만 존재(yaml 미포함)해도 다음 run_daily에서 OHLC+snapshot 수집됨(universe UNION 확인);** spec 통과.
- [x] **I2.6 (ENG, sink — append)** `sink/db_sink.py` `market_rankings` INSERT — `ON CONFLICT(as_of,symbol,scope) DO UPDATE`로 주간 append; 전체 tracked 기록. (psycopg3 `cursor.executemany()` + `ON CONFLICT … DO UPDATE`; psycopg2 `execute_values` 금지.) **Acceptance:** 주간 스냅샷이 누적(과거 주 보존); 같은 주 재실행 idempotent.
- [x] **I2.7 (ENG, entrypoint)** `entrypoints/run_weekly.py` — `universe.assemble` → marketCap+sector fetch → `screen.rank` → `tracked_symbols` UNION(sticky) + `market_rankings` INSERT; rate-limit(yahoo 30rpm token bucket) + exp backoff(2/4/8s, 3 retries) + **partial 허용** + 실패 시 직전 주 `market_cap` **carry-forward**(전체 런 abort 금지). **Acceptance:** 일부 심볼 fetch 실패해도 런 완료(carry-forward 적용); rate-limit 준수; sticky universe 규칙 보존.
- [x] **I2.8 (ENG, screening 최적화)** ✅ (e2a5e5c — migration 0013 symbol_fundamentals APPLIED; ranking/fundamentals + db_sink table-guarded cache; run_weekly derives shares×close, fast_info fallback) `market_cap = 캐시된 shares_outstanding × local close`(OHLC archive에서) 유도; shares 월간 `fast_info` refresh; sector `.info` 1회 후 분기 캐시. **Acceptance:** 주간 시총 산출이 심볼당 .info 호출을 회피(캐시된 shares×close 사용); shares/sector 캐시 갱신 주기가 월간/분기로 동작; API 호출 수가 naive 대비 감소.
- [x] **I2.9 (ENG, CI cron)** `.github/workflows/collector-weekly.yml` — cron `"0 6 * * 0"`, `run_weekly`, `astral-sh/setup-uv`(commit SHA pin `# v8.x`, `enable-cache: true`, `working-directory: apps/stock_collector`) + `concurrency:` group(중복 런 취소); 시크릿 **`DATABASE_URL` only**(MASSIVE_API_KEY 제거 — universe는 Wikipedia+IWB). **Acceptance:** 주간 스케줄 트리거 + 수동 dispatch; 시크릿은 GH Actions Secrets의 session-pooler `DATABASE_URL`; **MASSIVE_API_KEY 없음**; CSV 업로드 step 없음.

### Phase 3 — DEFERRED (추가 소스)
- [ ] **I3.1 (ENG) — DEFERRED** FRED 1M/1Y treasury yields → `rates`(Yahoo에 없는 만기). **Acceptance:** `rates`에 1M/1Y 추가. *(DEFERRED.)*
- [ ] **I3.2 (ENG) — DEFERRED** KRX KOSPI200 **선물** / **KOSDAQ150**(Yahoo 미제공 — 대체 소스 필요). **Acceptance:** KRX 파생/서브 지수가 `indices`에 반영. *(DEFERRED — Decisions Log #9 KRX suffix 리스크 연관. KRX 스폿 지수 `^KS11`/`^KS200`/`^KQ11`은 Yahoo-available이라 **Phase 1에 이미 포함** — 여기서 제외.)*
- [ ] **I3.3 (ENG) — DEFERRED** 추가 fx/crypto 소스. **Acceptance:** 추가 심볼/소스 적재. *(DEFERRED.)*

> **Build order:** Phase 1 (I1.1→I1.17) → Phase 1.5 (I1.5.0→I1.5.2) → Phase 2 (I2.1→I2.9); Phase 3
> DEFERRED. 각 Phase는 독립적으로 shippable.
> **First shippable task:** **I1.1** (collector SIMPLE COPY, massive·sec13f 제외), 이어서 **I1.4**
> (`configs/watchlist.yaml` ALL 카테고리, 42 라벨 — KRX 스폿 지수 포함) — 이후 모든 transform/sink/entrypoint가 의존한다.
> **Per-task gate (every I-item):** `apps/stock_collector` `uv run pytest`(해당 spec) green ·
> 마이그레이션 항목은 `pnpm check:schema` in sync · 순수 transform/ranking은 부작용 없음(psycopg는
> `sink/`에만) · 파일 ≤300 LOC.

## Phase J — SEC 13F Holdings (monthly batch) — PLANNED (PM+architect LOCKED 2026-06-30)

> Adds SEC EDGAR 13F-HR institutional holdings to `apps/stock_collector` (Yahoo-only today). Design
> FINAL (architect → 2nd adversarial validation, live-verified). Full decision summary → plan.md
> Decisions Log **#36**. Scope = store + monthly batch ONLY (no dashboard endpoint/UI; `ticker` NULL).

- [ ] **J1 (ENG) — `configs/funds.yaml`** seed CIK list `{cik,label}` (~5–10 famous filers, e.g. Berkshire `0001067983`), CIK zero-padded to 10 chars. **Acceptance:** file parses; every CIK 10-char string.
- [ ] **J2 (ENG) — `config/funds_loader.py`** (PURE) load+validate: pad CIK, dedupe, raise on bad/short; `resolve_funds()` seam = yaml ∪ table-guarded read (returns `[]` when `tracked_funds` absent, mirrors `read_tracked_symbols`). **Acceptance:** unit tests on good/dup/short-CIK; table-guard returns `[]` when table missing.
- [ ] **J3 (ENG) — `schema/rows.py`** add frozen `FilingRow`, `HoldingRow` dataclasses next to existing rows. **Acceptance:** import + pytest collection green.
- [ ] **J4 (ENG, PURE core) — `transform/sec_parse.py`** `parse_submissions(json)` (filter 13F-HR*, latest by filing_date, `reportDate`→period); `parse_infotable(xml)` **namespace-agnostic** (`ns1:`/`n1:`/unprefixed — else 0 rows); `normalize_value(raw, period)` (period `>= 2022-12-31` → whole USD else ×1000, return units); `aggregate_holdings(rows)` (sum shares+value across `otherManager`, keep `(cusip,put_call,prn_type)` distinct, `put_call` sentinel `''`). **Acceptance:** unit tests vs committed sample submissions JSON + infotable XML fixtures, incl. pre-/post-2023 value + namespaced XML + multi-manager dedup cases.
- [ ] **J5 (ENG, IO) — `sink/sec_fetch.py`** `fetch_submissions(cik)` (padded), `fetch_accession_index(cik_int, accession)` (NON-padded Archives CIK, `index.json`), `fetch_infotable(url)`; all via existing backoff+`for_provider(PROVIDER_SEC13F)` limiter (300rpm) + **`User-Agent: sec_user_agent()`** header. **Acceptance:** `--dry-run` live fetch smoke against one CIK prints holding count, no DB; 3-GET/fund confirmed.
- [ ] **J6 (ENG, migration) — `0017_sec_holdings.sql`** `sec_filings` PK`(cik,accession)` + `sec_holdings` PK`(cik,accession,cusip,put_call,prn_type)`, `value_usd BIGINT`, `put_call NOT NULL DEFAULT ''`, `deleted_at` soft-delete, partial indexes `WHERE deleted_at IS NULL`; header block, `IF NOT EXISTS`, idempotent. Re-confirm next free number. **Acceptance:** SQL lints; `pnpm check:schema` in sync. **[USER gate: manual apply to Supabase.]**
- [ ] **J7 (ENG, sink) — `db_sink.py` helpers** `upsert_filings`/`upsert_holdings` (executemany ON CONFLICT DO UPDATE); `supersede_prior_filings(conn, cik, period, keep_accession, keep_filing_date)` soft-deletes prior filings **only where `filing_date` strictly earlier** + `WHERE deleted_at IS NULL`; table-guarded reads → `[]` if absent. **Acceptance:** idempotency test (double-run = same active row count; older-filing re-run does NOT downgrade newer stored; amendment supersedes).
- [ ] **J8 (ENG, entrypoint) — `entrypoints/run_monthly.py`** argparse `main` (`--dry-run`/`--limit`); dry-run guard on `database_url()`; `connect(options="-c timezone=UTC")`; flow `resolve_funds → per CIK: fetch→parse→normalize→aggregate→upsert_filings+upsert_holdings+supersede`; partial-failure tolerant (one fund error logged+skipped, run continues); try/finally **`insert_batch_log(job='collector-monthly')`**. **Acceptance:** `--dry-run --limit 1` prints fetched-holding counts, no DB write; failure of one fund doesn't abort.
- [ ] **J9 (ENG, archive) — raw-XML archive** best-effort `storage_sink` upload `sec13f/<cik>/<accession>/infotable.xml` behind env toggle, default OFF. **Acceptance:** toggle OFF → no upload attempt; object-key builder unit-tested.
- [ ] **J10 (ENG, CI cron) — `.github/workflows/collector-monthly.yml`** cron `"0 7 1 * *"` + `workflow_dispatch` + `concurrency`; `setup-uv` (SHA-pinned, `working-directory: apps/stock_collector`); env `DATABASE_URL`/`POSTGRES_URL_NON_POOLING` + **`SEC_USER_AGENT`**; `if: failure()` issue (labels `collector`,`ci-failure`) like daily/weekly. **Acceptance:** monthly schedule + manual dispatch; secrets present; no CSV upload step. **[USER gate: add `SEC_USER_AGENT`+DB secrets; first live run.]**
- [ ] **J11 (ENG, gate)** extend lint/typecheck/pytest to cover new modules; run once at end. **Acceptance:** collector pytest green incl. new specs; ≤300 LOC/file; pure transform side-effect-free.

> **Build order:** J1→J2→J3→J4(pure core, tested first) → J5(IO) → J6(migration) → J7(sink) → J8(entrypoint) → J9(archive) → J10(CI) → J11(gate). Each shippable incrementally.
> **USER gates:** J6 (apply 0017), J10 (`SEC_USER_AGENT`+DB secrets, first live run).
