# Tasks — tradelunch

**Last updated:** 2026-06-30
**Status** (per-phase detail in the stubs below; full history in `_docs/archive/`):

- **DONE/SHIPPED:** Phase 1 · Repo Hygiene · dashboard Cycle 2 · Phase A (live on `my.prettylog.com`) · blog_agent merge · Phase B backend + publish pipeline · Phase D (ENG) · Phase E + E2 · Phase F (`2a0457b`) · F2 (`3e5b04c`) · G (`bced84e`) · Phase H P0–P5 (`2c0f897`/`f83a38e`) · blog card actions (`894fce3`) · Phase 2-mobile (MOB.0–MOB.3) · **Phase 2-filter** (multi-category + multi-tag feed filter, F.1–F.11, commits `a05a299`..`4e8483b`, jest 25 suites/166 tests) · Phase I committed (`56b8be5`) + ENG follow-ups LIVE (`f971661`: parquet archive+seed 11636 rows/83 obj uploaded, I2.8 cache + migration 0013 applied, SECRET_KEY+apikey, checkout v7).
- **ACTIVE/OPEN:** Phase 2-mobile (mobile context UX — SHIPPED MOB.0–MOB.3; note: Phase 2-filter SUPERSEDED its mobile category/tag NAV chip row → `MobileChipRow`/`getMobileChips` `x_`-retired, only `MobileAuthorChip` + Option-D TOC remain) · Phase 2-fetch (feed axios→native-fetch + tag revalidation — SHIPPED; OPEN follow-ups F-fetch.1 admin-delete revalidate, F-fetch.2 client-axios retirement) · Phase 2-simple Cycle 3 (dashboard mock→real — **COMPLETE**: snapshot live + `DASHBOARD_DATA_SOURCE=backend` flip done + chart history wired to backend; `/dashboard` fully backend-sourced) · Phase C (not started) · Phase H P6 (DEFERRED) · Phase I (collector-write live + db push 0011/0012/0013 applied; remaining = KOSPI/KOSDAQ `.KS/.KQ` coverage [Decision #32] + I1.16 USER live-prod READ smoke) · **Phase L (PLANNED — `/funds/[cik]` 13F rank-flow viz, backfill 2년; L1–L14, Decisions #38; not started)**.
- **USER go-live gates:** see the consolidated snapshot below (Clerk env, backend `SUPABASE_*`, admin bootstrap, `db push` 0010, Cloudflare CNAME).
  **Branch:** feature/finance → main

> Atomic execution breakdown. Product intent → `00.plan.md`. Running state → `01.status.md`.
> Verbose source docs (full migration runbook, blog_agent merge ADR) archived in `_docs/`.

**Legend:** `[x]` done · `[~]` in progress · `[ ]` todo · **(USER)** needs Supabase/Vercel/DNS creds · **(ENG)** code only · **SUPERSEDED** no longer applicable.

---

## Go-live USER gates (consolidated snapshot — 2026-06-26)

**DONE:** `supabase db push` of `0004` (market) + `0005` (draft enum) applied to remote (all
`0001`–`0005` Local=Remote); `seed.market.sql` loaded (`market_snapshots`=22, `market_history`=5500);
Supabase Storage bucket `blog.prettylog` created PUBLIC; **blog_agent first real publish landed live**
(`java-spring-jdbc`, user_id=1, status='public', thumbnail public URL) and reads back via
`GET /v1/api/posts`.

**DONE (added 2026-06-29):** Clerk Production env on BOTH Vercel projects (`pk_live`/`sk_live`,
backend `ALLOWED_ORIGINS`) + public-sign-up ON _(D1.1)_; `error_log` telemetry `0014`/`0015`/`0016`
all applied — browser+SSR+**Express global handler** (`source='express'`, `81e0962`) live, `resolved` triage on.

**STILL OPEN (USER):**

- Backend Vercel `SUPABASE_URL` + `SUPABASE_SECRET_KEY` — else image upload → 503. _(D2.6a)_
- Admin bootstrap SQL `UPDATE users SET is_admin=true WHERE username='<owner>'` — owner =
  `taek.lim.us@gmail.com`, AFTER signup + onboarding; `email` is NULL for lazy-provisioned users →
  match by `username`/`clerk_user_id`, never email. _(D4.0b)_
- ~~Flip `DASHBOARD_DATA_SOURCE=backend` on frontend Vercel + redeploy.~~ **DONE (2026-06-29)** — flipped + redeployed; chart history also wired. _(Phase B B4)_
- market-data provider + KRX `.KS/.KQ` suffix decision (KOSPI/KOSDAQ snapshot null until resolved). _(Phase B / Decisions #32)_
- Set blog_agent local `.env` `DEFAULT_USER_ID=2`→`1` (sandbox blocked the edit; non-blocking —
  post frontmatter `userId` drives publisher identity). _(B5.7)_

> **NOTE (not an open task):** Cloudflare `CDN_ASSETS` CNAME is branded-domain-only polish — parked, not on the active board. Revisit only when the branded asset domain is actually wanted.

---

## Repo Hygiene — DONE (2026-06-24/25)

> `@repo/types` created; `@repo/markdown-parsing` retired from client (TOC inlined); sequelize/pg-hstore removed (`@types/pg` added); express-types override → direct devDep (f026fca). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.repo-hygiene.md](./_docs/archive/tasks.repo-hygiene.md)

---

## Phase 1 — Blog MVP — DONE

> SSR post list, infinite scroll, Markdown render (GFM/KaTeX/highlight), nav, theme toggle, next-intl i18n, OG metadata. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-1-and-dashboard-cycle2.md](./_docs/archive/tasks.phase-1-and-dashboard-cycle2.md)

---

## Phase 2-simple — Financial Dashboard MVP

### Cycle 2 — Variant prototypes — DONE

> Variant C (TradingView-style chart) adopted; chart panel/header/legend/toolbar + `useTradingViewChart.hook` + indicator core + intraday intervals built; old grid purged. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-1-and-dashboard-cycle2.md](./_docs/archive/tasks.phase-1-and-dashboard-cycle2.md)

### Cycle 2 — Carry-over (before Phase C)

- [x] Re-slim `ChartPanel.client.tsx` (329→202 LOC; documented ≤210 budget banner — extraction floor, global ≤300). Extracted `useChartIndicators`/`useChartPanelMenu` hooks + `deriveChartHeader` util + `ChartIndicatorCloseButtons`.
- [x] Split `useTradingViewChart.hook.ts` (524→195 LOC) into pure builders `utils/chart-setup`+`chart-series`+`chart-panes` (each ≤300); React lifecycle (measure/crosshair/resize/teardown) stays in the hook. architect→developer→architect APPROVE; pure structural, zero behavior change.

### Cycle 3 — Full Implementation (ACTIVE)

- [ ] Finalize `IDashboardSnapshot` (Cycle 3 shape; `fetchedAt` ISO; zero `any`)
- [ ] `app/actions/getDashboardSnapshot.schema.ts` — Zod schema covering all 22 items + inferred type
- [x] `app/actions/getDashboardSnapshot.action.ts` — Server Action; backend when `DASHBOARD_DATA_SOURCE=backend`, else mock; typed errors (LIVE)
- [x] `app/actions/getDashboardHistory.action.ts` — label+interval → history; lazy per-selected-label; gated by `DASHBOARD_DATA_SOURCE`. `ChartPanel.client.tsx` fetches via `useDashboardHistory` at constant `'1d'`; W/M/intraday client-derived by `generateIntervalCandles`; loading + "No chart data" empty states.
- [ ] `hooks/useDashboardSnapshot.query.client.ts` — per-category polling (FX 60s, Crypto 30s, Indices US 60s, KRX once/session, Rates 24h, Stocks 60s)
- [x] Replace `/dashboard/page.tsx` with `ChartVariantLayout` (mock wired); add Dashboard nav link
- [x] Swap mock→backend fetcher once `DASHBOARD_DATA_SOURCE=backend` — DONE: snapshot live + flag flipped; CHART history wired to backend (mock full-map `history` prop removed across `ChartVariantLayout`/`page.tsx`/`[username]`/`preview/chart`). Remaining separate: KOSPI/KOSDAQ null under Yahoo `.KS/.KQ` (Decision #32); intraday 1m–4h synthesized from daily.
- [x] Soft-delete + purge old implementation

### Cycle 3 — Group E acceptance gate

- [ ] All 22 items show value + 1-day change % with color (real data; no "undefined"/"NaN")
- [ ] `/dashboard` renders <2s on dev with mock
- [ ] Zero `any` in Cycle 3 paths (`check-types` exit 0; grep `: any` clean)
- [ ] No hydration errors on `/dashboard`

### Cycle 4 — Verification

- [ ] PM verifies Group E against live `/dashboard`; decides Cycle 5 scope

---

## Phase 2-mobile — Mobile context UX (chip header + in-article TOC) — SHIPPED (MOB.0–MOB.3)

> Two additive client-side-only mobile (`<lg`) UX changes — (D) top-of-article collapsible TOC, (B) top-of-feed context header (author chip + chip row) — replacing the heavier `<lg` UserContextSheet; Phase 2-filter later SUPERSEDED the NAV chip row (`MobileChipRow`/`getMobileChips` `x_`-retired). Decision #33. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-2-mobile.md](./_docs/archive/tasks.phase-2-mobile.md)

---

## Phase 2-filter — Multi-category + multi-tag feed filter (desktop rails + mobile rows) — DONE (SHIPPED 2026-06-29)

> Reader narrows the author feed `/blog/[username]` by categories AND/OR tags at all breakpoints; URL state `?categories=a,b&tags=a,b`, same-type OR / cross-type AND, no migration; desktop rails (`mode:'filter'`) + mobile FilterChipRows + AppliedFilters bar. SUPERSEDES Phase 2-tags. Decision #34. Gate: jest 25 suites/166. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-2-filter.md](./_docs/archive/tasks.phase-2-filter.md)

---

## Phase 2-fetch — Feed data layer: axios→native fetch + tag revalidation — DONE (SHIPPED 2026-06-29)

> Blog feed read moved off client axios onto a server-only native-fetch wrapper (Next 16 tag revalidation → instant read-your-writes on publish-class mutations); Express CDN backstop lowered. F-fetch.1 (admin-delete revalidate) DONE; F-fetch.2 (axios retirement) parked. Decision #35. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-2-fetch.md](./_docs/archive/tasks.phase-2-fetch.md)

---

## Phase A — Deploy: Vercel + Supabase (Read-Only, Free) — acceptance gate MET (2026-06-26)

> Backend on Vercel via `pg` Pool → Supabase (S1–S5 done; SSL `sslmode` strip + self-contained tsconfig deploy fixes); blog + mock dashboard live on `my.prettylog.com`; CI typecheck. Full S1–S9 runbook archived. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-A.md](./_docs/archive/tasks.phase-A.md)
> **Open tail:** S4-H.2 (pg pool `max:5` commit/verify) · S6.1 (repo under individual acct) / S6.3 (optional backend domain) · S7.2 (lint `--max-warnings 0` CI gate; ~207 client warnings + blog_agent ruff/pyenv) · S8.4 (Cloudflare `CDN_ASSETS` CNAME) · S9.1–S9.3 (AWS dep/env retirement).

---

## blog_agent merge (apps/blog_agent) — DONE

> Phase A lift-and-shift (f51cc7e) + Phase B BA-09 Supabase/Cloudflare repoint (`DB_PG_*`→`POSTGRES_*`, boto3→supabase-py, first real publish live). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.blog-agent-merge.md](./_docs/archive/tasks.blog-agent-merge.md)

---

## Phase B — Dashboard backend + Supabase Storage repoint — backend + publish DONE (84b9d32)

> Backend B0–B3 (migration `0004` market tables + dashboard read endpoints + seed) DONE; blog_agent publish (B5/BA-09) WORKING end-to-end (first real publish live). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-B.md](./_docs/archive/tasks.phase-B.md)
> **Open:**
>
> - [x] **B4 (ENG)** Client Cycle 3 action layer reads B2 endpoints when `DASHBOARD_DATA_SOURCE=backend`, else mock — **DONE (2026-06-29)**: snapshot live + USER flag flipped + chart history wired (`useDashboardHistory` `'1d'` lazy per-label; mock `history` prop removed). `/dashboard` fully backend-sourced.
> - [ ] **B5.6 (ENG)** Re-publish with `ollama serve` up so tags/categories/description populate.
> - [ ] **B5.7 (USER)** Set blog_agent local `.env` `DEFAULT_USER_ID`→`1` (non-blocking).
> - [ ] **(USER)** market-data provider + KRX `.KS/.KQ` suffix decision (Phase I / Decisions #32). _(`DASHBOARD_DATA_SOURCE=backend` flip DONE 2026-06-29. Cloudflare `CDN_ASSETS` CNAME = S8.4 branded-domain-only — parked, see go-live NOTE.)_
> - [ ] **S8.2 / S9.1–S9.3** AWS retirement (soft-delete `awsS3.ts`/SSH/RDS CA, remove AWS deps, drop Vercel AWS env). _(B5.5 client cursor precision — CODE DONE; runtime verify only, awaits >10 posts.)_

---

## Phase C — planned

**Phase C (after B):** env Zod build-gate; hook/component LOC splits; server service layer; dead-dep
reconcile; `@repo/axios` vs local axios; Phase 4 pnpm checkbox cleanup; `.agent` rule consolidation.

---

## Phase D — Multi-User Blog Platform (Clerk Auth) — ENG COMPLETE

> Express-layer Clerk `verifyToken` over pg Pool (RLS bypassed → owner-scoping is the gate); schema `0003`; lazy provisioning + `/onboarding` (8ca524e); D2 authoring + autosave (dc28f19) + UX cycle + i18n + thumbnail persist (31eb14a); D4 admin soft-delete moderation (70375e6). Open-signup default; webhook retired; `/admin/invites` DROPPED. Phase 4 pnpm migration DONE (150d460). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-D.md](./_docs/archive/tasks.phase-D.md)
> **Open:**
>
> - [x] **D1.1 (USER) — DONE (2026-06-29)** Clerk Production (`pk_live`/`sk_live`) env on BOTH Vercel projects + `ALLOWED_ORIGINS` on backend + dashboard public-sign-up ON.
> - [ ] **D1.2 (USER, optional)** Register Clerk as a Supabase TPA provider.
> - [ ] **D2.6a (USER)** Backend Vercel `SUPABASE_URL` + `SUPABASE_SECRET_KEY` (else image upload → 503).
> - [ ] **D3.4 (ENG)** `/settings/profile` + `PATCH /v1/api/users/me` (residual, non-blocking).
> - [ ] **D4.0b (USER)** Admin bootstrap SQL `UPDATE users SET is_admin=true WHERE username='<owner>'` (match by username/clerk_user_id, never email).
> - [ ] **D4.1f (USER)** Live admin verify (blocked on D4.0b + Clerk env).

---

## Phase E — Blog Engagement (Saved Posts · Likes · Comments) — ENG COMPLETE + pushed `7b2ea0a`

> Saved Posts (`/me/saved`, migration `0007`), Likes (`0008` `post_likes`, optimistic detail+feed buttons), Comments (`0009` Option-C `BIGINT[]` materialized path, unlimited depth, `[deleted]` tombstone). 3 soft-delete Rule-1 gaps fixed. Issue #1 = PARALLEL (blog_agent comment columns untouched). All purely additive. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-E.md](./_docs/archive/tasks.phase-E.md)
> **Open:** USER apply-migrations gate — `supabase db push` `0007`+`0008`+`0009`, then re-run server jest for the DB-gated saved/likes/comments assertions.

---

## Phase E2 — Comment Edit + Comment Pagination — DONE/SHIPPED (origin/main HEAD `85093b4`)

> Closed Phase E's two deferrals: comment EDIT (`PATCH /comments/:id`, auth==delete via shared `assertCommentMutable`, tombstone→409, "(edited)" badge) + keyset pagination (50 roots/page each with full subtree, STRING cursor, `useInfiniteQuery`). `comments.ts` split into a barrel (C1). Purely additive — NO schema/index/migration. No open USER item (`0007`/`0008`/`0009` APPLIED; jest 78/78). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-E2.md](./_docs/archive/tasks.phase-E2.md)

---

## Phase F — Blog Image Upload Rework (Express proxy + 2-step resize) — DONE (`2a0457b`)

> `POST /v1/api/posts/images` (multer memoryStorage + sharp `rotate().resize(1600,inside).webp(q80)`, server-to-server upload, full error mapping); client step-1 native-Canvas resize (no library); `vercel.json includeFiles` ships sharp libvips; old signed-URL path soft-deleted `x_*`. Decision #27. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-F.md](./_docs/archive/tasks.phase-F.md)
> **Open:**
>
> - [ ] **F17 (ENG/USER, manual e2e)** With backend `SUPABASE_*` set: normal JPEG → working `publicUrl`; EXIF-rotated correct; >5MB succeeds; non-image `.jpg`→415; creds unset→503. (Precondition: D2.6a.) Plus runtime confirm sharp libvips loads on Vercel.

---

## Phase F2 — blog_agent webp compression (local Python CLI) — DONE (`3e5b04c`)

> `apps/blog_agent/utils/image_transform.py` `to_webp` (Pillow LANCZOS + `exif_transpose`, 1600 fit-inside, q80, metadata stripped, `NotAnImageError`); body+thumbnail upload boundary forces webp; thumbnails keep 1200×630 letterbox (Option A); `_save_file_records` ext-from-stored_name fix; path scheme unchanged. Decision #28. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-F2.md](./_docs/archive/tasks.phase-F2.md)

---

## Phase G — Editor Category Selector + Tags (write/edit screen) — DONE (`bced84e`)

> Cascading 3-box category picker + flat tag-chip editor on `/write`; migration `0010` → category uniqueness `(user_id, parent_id, title)`; tri-state `category_id` PATCH (CASE); by-id read now returns `tags` + `stored_uri`; new `POST /v1/api/categories` (parent-scoped dedupe, soft-delete revive); AC1–AC10 met. Decision #29. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-G.md](./_docs/archive/tasks.phase-G.md)
> **Open (USER):** `supabase db push` migration `0010_category_unique_parent.sql`.

---

## Phase H — Multi-user Reddit-style Blog (global feed + context rails) — DONE P0–P5 (`2c0f897` / `f83a38e`)

> 3-col `BlogShell` route group (`app/(feed)`, cookie-collapse first-paint); `/tags/[tag]` global tag feed; global TopBar (SearchBar tag-nav + CreateButton); identical global LEFT rail (PrimaryNav + global TagCloud + mobile focus-trap); per-user context RIGHT rail (category tree + scoped tags + `UserProfileCard` from `GET /posts/users/:username/profile`) with `<lg` sheet; localStorage recents(20)/saved-tags(50); 4 viewer-agnostic tag-read APIs (slug-dedup-before-keyset, STRING cursors). Decision #30. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-H.md](./_docs/archive/tasks.phase-H.md)
> **Open:**
> **NOTE (not an open task):** P6 real search/discovery (`GET /v1/api/posts/search` title+tag ILIKE + keyset; `/explore` surface; H6.1/H6.2/H6.3) stays parked. A **disabled `/explore` PrimaryNav placeholder** ("coming soon", non-clickable, Compass icon) is now live (`useNavLinks.hook.ts` `disabled:true` + `nav.explore` both locales) so the trailing nav slot reads intentional. Flip `disabled` off + add the route when P6 is actually picked up.
>
> - [ ] **HX.1 (architect)** decide client `TPost` re-derive from `@repo/types` `TFeedPost` vs parallel.
>       _(HX.2 split `posts.ts`→6 modules: DONE `db00d42`.)_

---

## Phase I — Stock Collector 편입 + Yahoo OHLC 적재 + 시총 랭킹 — DONE / LIVE (2026-06-29)

> Daily OHLC + snapshots · weekly rankings + sticky universe · Parquet archive; migrations 0011/0012/0013 applied; weekly ranking first live run done (505 candidates → 1,007 rankings / 110 tracked). Open USER: CI-cron scheduled-fire verify + KOSPI/KOSDAQ `.KS/.KQ` coverage (Decision #32). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-I.md](./_docs/archive/tasks.phase-I.md)

---

## Phase J — SEC 13F Holdings (monthly batch) — DONE (ENG J1–J11) (2026-06-30)

> SEC EDGAR 13F-HR monthly batch added to `apps/stock_collector` (registry→fetch→parse→normalize→aggregate→upsert+supersede; migration 0017 `sec_filings`+`sec_holdings`; raw-XML archive default OFF; `collector-monthly` cron `0 7 1 * *`). 0017 APPLIED + first live run DONE (sec_filings=6, sec_holdings=16,543). Decision record: plan.md Decisions Log #36. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-J.md](./_docs/archive/tasks.phase-J.md)
> RESIDUAL (open): verify GH Actions `SEC_USER_AGENT`/DB secrets + first SCHEDULED monthly cron fire (1st-of-month) — local live run proven; CI-trigger unverified.

## Phase K — `/funds` 13F Holdings Viewer (read-only) — DONE (2026-06-30)

> Read-only `/funds` viewer over the Phase J store (Express `GET /api/funds`[`/:cik`] with `to_regclass` guard + SQL `SUM() OVER()` weight; Next `/funds`+`/funds/[cik]` force-dynamic + `/funds/preview` fixture). K11 SATISFIED: 0017 applied + first collector run → `/funds` shows live data (sec_holdings=16,543). Decision record: plan.md Decisions Log #37. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-K.md](./_docs/archive/tasks.phase-K.md)

## Phase L — `/funds/[cik]` 13F Rank-Flow (분기별 시각화, backfill 2년) — ENG DONE (active scope) / L-next DEFERRED (2026-06-30)

> rank-flow 테이블(컬럼=분기 / 행=비중랭킹 / 종목별 cusip-해시 고정색 / 클릭 시계열 추적) on `/funds/[cik]`.
> **오너 결정 잠금 (2026-06-30):** rank-flow=`/funds/[cik]` 유일뷰(Phase K 평면테이블 은퇴) · 최근 8분기=2년(백필도 `--since` 2년) · 행=ever-top-K(25) 합집합(bump 연속성) · Δ배지/디밍/연결선=L-next. 제품의도+결정 = `00.plan.md` Phase L + Decisions Log #38.
> **Build order:** Slice A 백필(데이터 전제) → B rankflow 엔드포인트 → C rank-flow UI(shippable MVP) → D L-next(분석뷰/Δ배지/consensus, DEFERRED).
> **Per-task gate:** collector = `apps/stock_collector` `uv run pytest`(해당 spec) · UI util = vitest · 마이그레이션 = `pnpm check:schema` in sync · TS = `check-types` 0 + `lint --max-warnings 0` · 파일 ≤300 LOC · 순수 transform은 부작용 없음(psycopg는 `sink/`에만) · soft-delete 전용.

> **STATUS (2026-06-30): ENG DONE — active scope shipped, gates GREEN.** Slice A 백필(L1–L5) + B 엔드포인트(L6) + C rank-flow UI(L7–L11) + L15/16 13F parquet 아카이브 + L18/19 prune·weekly-cadence 모두 구현·검증. 게이트: stock_collector pytest **196** · dashboard_server jest **184**/check-types 0 · dashboard_client_web check-types 0/lint 0/vitest 27. L18/19는 Phase M 인프라(`retention.py`/`prune_history.py`/`collector-prune.yml`) **재사용**(OHLC 경로 무변경 additive). **USER 게이트:** `collector-13f-backfill` workflow_dispatch 1회 트리거(2년) · PRIVATE 13F parquet 버킷(또는 `market-archive` 재사용) + `COLLECTOR_SEC_PARQUET_BUCKET`/archive 시크릿 · prune 첫 LIVE는 archive 채워진 뒤(현재 무해 no-op). **추가 DONE:** L17(archive/DB 윈도우 분리, `--db-keep-quarters`) · L20(bump-style sort). **L-next DEFERRED:** L12–14(분석뷰 0018/Δ배지/cross-fund consensus) · L21(sector enrichment: CUSIP→ticker→sector, `security_map`).

### Slice A — 백필 (데이터 전제, 최근 2년만)

- [x] **L1 (ENG)** 순수 `all_13f(refs, *, since=None)` + `group_by_period(refs)` (`transform/sec_parse.py`). **Acceptance:** mixed-period refs → period 오름차순 정렬; `since`가 `period_of_report` floor; period당 amendment 그룹(keep=max filing_date) 표기; I/O 없음; pytest green.
- [x] **L2 (ENG)** 순수 `merge_submission_pages(recent, older)` (`sec_parse.py`) — 병렬 컬럼 배열을 `recent` 셰이프 dict로 합침. **Acceptance:** `parse_submissions({"filings":{"recent":merged}})`가 전 페이지 13F-HR 합집합 반환; 2-page pytest.
- [x] **L3 (ENG)** `submission_page_names(subs)` + `fetch_submission_page(name)` (`sink/sec_fetch.py`; `data.sec.gov/submissions/{name}`, 공유 limiter+session+backoff). **Acceptance:** `filings.files[].name` 읽음; 부재 시 `[]`; `for_provider(PROVIDER_SEC13F)` 사용.
- [x] **L4 (ENG)** `entrypoints/run_backfill.py` (argparse `--cik/--since/--limit/--dry-run`; **기본 `--since`≈2년 전**; run_monthly 미러; per-(fund,period) iterate; fetch/parse/aggregate/upsert 재사용; period당 `supersede_prior_filings` 1회; partial-tolerant; try/finally `insert_batch_log(job='collector-backfill')`). **Acceptance:** `--dry-run`이 펀드별·period별 holdings 수 출력(DB write 0); 라이브 idempotent(재실행 신규 tombstone 0); ≤300 LOC; pytest green.
- [x] **L5 (USER/ENG)** ✅ ENG: `.github/workflows/collector-backfill.yml`(`collector-13f-backfill`, workflow_dispatch, 기본 dry-run) 생성. **USER 잔여: 1회 수동 dispatch(2년)**. GitHub Actions `workflow_dispatch` 잡 — `run_backfill` 호출(`SEC_USER_AGENT`+`DATABASE_URL` 시크릿; monthly 워크플로 미러; **기본 2년 윈도우**). **Acceptance:** 수동 dispatch 완주; `batch_log` `job='collector-backfill'` 행; 펀드당 `sec_filings`에 다분기(최근 ≤8) 존재.

### Slice B — rank-flow 엔드포인트

- [x] **L6 (ENG)** `GET /api/funds/:cik/rankflow?quarters=8&k=25` (funds/ 신규 sibling 파일; funds.ts 컨벤션: `to_regclass` guard → 빈/null(500 아님), `^\d+$`→`padStart(10)`, parameterized, cache `s-maxage=86400`, 순수 DTO mapper). 응답 = `{periods:[{periodOfReport,totalValueUsd,remainingCount,remainingWeightPct}], rows:[{cusip,label,cells:{[period]:{rank,weightPct,valueUsd}|null}}]}`; rows=8분기 중 ever-top-K(25) cusip 합집합; weight=`value_usd/SUM() OVER(PARTITION BY period_of_report)`; `deleted_at IS NULL`·`put_call=''`·`prn_type<>'PRN'`. **Acceptance:** ≥2분기 cik → union rows + per-period cells/remaining; 미보유 분기 cell=null; unknown cik → 빈/`null`; `check-types`+`lint` clean.

### Slice C — rank-flow UI (shippable MVP)

- [x] **L7 (ENG)** 순수 util + vitest: `utils/cusipColor.ts`(cusip→고정 hue, rank/period 무관, light/dark 대비, 회귀잠금 테스트) · `utils/quarterLabel.ts`(03-31→Q1…12-31→Q4, `YYYY Q#`) · `utils/rankFlowRows.ts`(union-of-ever-top-K + tie-break weight DESC→valueUsd DESC→cusip ASC). **Acceptance:** 각 util 순수+vitest green; `cusipColor` 동일입력 byte-identical(교차컬럼 동일색); quarter 매핑 4분기 정확.
- [x] **L8 (ENG)** `app/actions/getFundRankFlow.action.ts` + `getFundRankFlow.schema.ts`(Zod + AssertEqual; revalidate=86400; reportSsrError). **Acceptance:** `{ok,data}` envelope가 `getFundHoldings` 패턴 미러; schema↔type drift는 `check-types` 실패.
- [x] **L9 (ENG)** `components/funds/RankFlowTable.tsx` — 컬럼=분기(newest-left, `YYYY Q#` 헤더+원본일자), 셀=고정색+라벨+rank+weight%, "+M more (Σ%)" 집계행; 클릭→active cusip→전 컬럼 같은 cusip 셀 **inner-border ring**(ring-inset, layout shift 0); toggle/switch 단일선택(0/1 active); 키보드 focus+Enter/Space + `aria-pressed`; 가로 스크롤(세로 wrap 없음); 스크롤 중 선택 유지. **Acceptance:** N컬럼 존재 cusip 클릭→정확히 그 셀들만 ring; 재클릭 해제/타종목 전환; `Tab`/`Enter`/`Space` 동작 + focus ring; 가로 스크롤 후도 ring 유지(no flicker).
- [x] **L10 (ENG)** `/funds/[cik]/page.tsx`를 RankFlowTable로 교체(평면 `HoldingsTable` 은퇴 `x_`); `/funds/preview` fixture를 ≥2 분기 fabricated로 populated 렌더; 375px 반응형 tap 토글. **Acceptance:** rank-flow가 `/funds/[cik]` 유일 뷰; preview가 다분기 rank-flow 렌더; 모바일 가로 swipe + tap; 0종목→Phase K empty state, 정확히 1분기→단일 컬럼 정상(에러/`NaN` 없음).
- [x] **L11 (ENG)** 수용 게이트: 신규 경로 zero `any`(`check-types` 0) · web `lint --max-warnings 0` · 3 util vitest green · `/funds/[cik]` 하이드레이션 에러 0 · 렌더 ≤2s(dev mock). **Acceptance:** 위 전부 충족.

### Slice D — L-next (follow-up, DEFERRED)

- [ ] **L12 (ENG) — DEFERRED** 마이그레이션 `0018_sec_analytics.sql` 분석 뷰 4종(`v_sec_position_delta`/`v_sec_exits`/`v_sec_fund_period_summary`/`v_sec_consensus`; `deleted_at IS NULL`·`put_call=''`·window SUM weight) + **0017 테이블 + 0018 뷰를 BOTH `tradelunch.schema.sql` SSOT 미러**(현재 0017이 SSOT 부재 → 보강). **Acceptance:** 0017 DB에 idempotent 적용; backfill된 펀드에 행 반환; `pnpm check:schema` in sync.
- [ ] **L13 (ENG) — DEFERRED** Δ 배지(인접 분기 대비 ▲▼ rank / NEW / EXIT / Δweight) per 셀 + 비선택 디밍 옵션 + (선택)연결선. **Acceptance:** 첫 백필 분기는 전부 NEW; 배지가 `v_sec_position_delta`/`v_sec_exits` 기반; 색은 여전히 비-load-bearing.
- [ ] **L14 (ENG) — DEFERRED** cross-fund consensus: `GET /api/securities/:cusip/consensus`(`^[A-Za-z0-9]{6,9}$`, `v_sec_consensus`+`sec_filings.filer`) + consensus 배지/오버레이 UI. **Acceptance:** holderCount + 펀드별 분해; unknown cusip→`null`; `controllers/index.ts` 마운트.

#### SEC 13F Parquet 콜드 아카이브 — L15/16/17 DONE (2013 라이브 런 = USER)

> 스코프/결정 = `00.plan.md` Phase L L-next + Decisions #38. **hot=thin(DB 2년) / cold=full(아카이브 2013~현재 전체).** pre-2013 비구조화라 30년 거부. 기존 OHLC Phase 1.5 `parquet_sink` 패턴 미러.

- [x] **L15 (ENG)** `sink/sec_parquet_sink.py` — pyarrow/zstd, 파티션 `sec13f/{cik}/{cik}_{YYYY}.parquet`; 컬럼 = `sec_holdings` 미러(cik/accession/period_of_report[**date32**]/cusip/name_of_issuer/title_of_class/ticker/shares[int64]/prn_type/value_usd[int64]/put_call/discretion); **전체 holdings(top-N truncation 안 함 — ml-engineer 결정)**; 현재-연도 파일 read-merge-rewrite(같은 `(cik,accession,cusip,put_call,prn_type)` dedupe keep=last → sort → 고정 스키마 cast → tmp → atomic `os.replace`), 과거-연도 immutable + `__tests__/spec_sec_parquet_sink.py`. **Acceptance:** 동일 키 재적재 dedupe(중복 0); 스키마 고정(date32); atomic replace; `value_usd`는 normalize_value 통과분(2023 units flip 일관); pytest green.
- [x] **L16 (ENG/USER)** PRIVATE Storage 버킷(기존 `market-archive` 재사용 또는 `sec-archive` 신설) + `run_backfill`/`run_monthly`에 best-effort archive step(기본 OFF 토글, Phase J raw-XML archive 패턴; `SUPABASE_URL`+`SUPABASE_SECRET_KEY`). **Acceptance:** archive step이 현재-연도 13F parquet를 PRIVATE 버킷에 object-upsert; 원시 XML/CSV 미업로드; 시크릿은 GH Actions Secrets.
- [x] **L17 (ENG)** 아카이브 윈도우 ≠ DB 윈도우 분리 — `run_backfill`에 `--db-keep-quarters`(기본 12=3년) + 순수 `retention.db_keep_cutoff(today, keep_quarters)`; 각 분기 `period>=cutoff`면 DB upsert(filings/holdings/supersede), 아니면 skip; **parquet은 게이트 밖에서 무조건**(archive=`--since` 전체 이력, DB=최근 3년만). dry-run이 `archive=N periods / db=M periods (cutoff=…)` 분리 출력; `collector-backfill.yml`에 `db_keep_quarters` 입력. **Acceptance:** `--since 2013 --archive`가 parquet 2013~present + DB 최근 3년만; OHLC/prune retention 경로 무변경; pytest 226. ✅ **(2013 라이브 = USER workflow_dispatch: `since=2013 db_keep_quarters=12 archive=true`)**

#### 운영 트랙 — 13F DB prune + weekly cadence (DONE, Phase M 인프라 재사용)

- [x] **L18 (ENG)** 13F DB prune — `entrypoints/prune_holdings.py`(`prune_history.py` 미러; argparse `--cik/--dry-run/--keep-quarters` 기본 12분기=3년); 순수 `retention.holdings_prune_periods`(OHLC 경로 무변경 additive); `sec_db_sink` `read_all_periods`+`prune_period`(**hard-delete — 오너 승인 예외, 파생·아카이브된 13F 한정, 헤더 주석 명시**); **아카이브-선행조건** `storage_sink.object_exists`로 `sec13f/{cik}/{cik}_{YYYY}.parquet` 존재 확인 후에만 삭제(없으면 skip+로그); `collector-prune.yml`에 `prune-13f` job 추가(12/30 연간 공유); `batch_log(job='collector-prune-sec')`. **Acceptance:** dry-run이 후보+archive-status 로그(삭제 0); 미아카이브 분기 절대 미삭제; OHLC prune 경로 무변경; pytest green. ✅ pytest 196.
- [x] **L19 (ENG)** weekly-poll cadence + period-advance guard — `sec_db_sink.read_latest_period`(table-guarded); `run_monthly`에서 펀드별 submissions 1 GET → `latest_13f` → stored max와 비교, 신규 분기 없으면 infotable fetch skip(cheap no-op); `collector-monthly.yml` cron `0 7 1 * *`→`0 7 * * 1`(weekly, name `collector-13f`). 실작업은 4개 마감 윈도우(Feb/May/Aug/Nov)에만. **Acceptance:** 미진전 펀드는 1-GET no-op; 신규 분기만 full ingest; idempotent; pytest green. ✅

#### rank-flow UI 폴리시 + L-next enrichment
- [x] **L20 (ENG)** rank-flow **bump-style 재설계** — `RankFlowTable` 2모드: **flow**(기본; 컬럼별 독립 랭킹, 위치=순위, cusip 고정색으로 분기 이동 추적) + 컬럼 헤더 클릭→**aligned**(그 분기 순위 기준 전체 행 정렬) + **Reset ↺/Esc** 복귀; 순수 `orderColumnByRank`/`sortRowsByReference`(+vitest); 기존 row-aligned→`x_RankFlowTable` soft-delete; preview 3분기 확장(랭킹 이동 가시). **Acceptance:** 모드 전환·정렬·스크롤에도 cusip 고정색·클릭추적(inner-ring) 안정; check-types 0/lint 0/vitest 83. ✅ (오너 결정: bump > row-aligned)
- [ ] **L21 (ENG) — → Phase P 0-b(P1–P6)로 승격 (2026-07-01)** SEC 13F **sector enrichment** (rank-flow sector 컬럼 + 섹터별 색 그라데이션 + 상단 sector legend의 전제). 갭 = **CUSIP→ticker** (13F는 cusip만·`ticker` NULL); **ticker→sector는 이미 있음**(Yahoo `.info`, `symbol_fundamentals` 분기 캐시). 신설 `security_map`(cusip PK, ticker, sector, name, source, refreshed_at, deleted_at) + enrichment 잡(distinct cusip → OpenFIGI 무료 CUSIP→ticker → Yahoo/`symbol_fundamentals` ticker→sector → upsert). **Acceptance:** 채워지면 sector 컬럼·색 그라데이션·legend 3종 활성.
- [ ] **L22 (ENG) — DEFERRED (NOTE-ONLY, do not build yet)** 다중-CIK 펀드 정체성 매핑 — 일부 필러는 13F를 시간에 따라 **다른 CIK로** 제출(예: BlackRock = `0001086364`[~2016] / `0001364742`[2023Q4–2024Q2] / `0002012383`[2025Q2~], 2024Q3–2025Q1 공백). 연속 이력을 원하면: `funds.yaml`에 `{slug,label,ciks:[…]}` + `cik→slug` 매핑 + `/funds/[slug]` 라우트 + rankflow 엔드포인트가 slug의 CIK들을 **period별 union**(홀딩은 실제 CIK로 저장 — source-native 유지, 위조 안 함). 매핑해도 공백(어느 CIK도 미제출 분기)은 못 메움. 현재는 단일 최신-CIK로 운영(YAGNI; 오너 결정 2026-07-01). **Acceptance:** N/A — 실제 과거 이력 필요 시 착수. 오너 노트 요청으로 기록만.

## Phase M — market_history 5년 retention 티어링 (hot/cold prune) — ENG M1–M7 DONE / M8 ops + LIVE flip USER-gated (2026-06-30)

> `market_history`를 5년 HOT 윈도우로 한정; 전체는 Parquet COLD(Storage, LIVE)에만. **연 1회(12/30 cron)** prune이 cutoff(**달력연도 경계 = `<(today.year−5)-01-01`**) 이전 봉을 **라벨별 Parquet 파티션 존재 검증 후에만** 삭제 → 항상 완전한 과거 연도 파티션만 떨굼(boundary 부분삭제 없음). 연 1회라 DB는 5~6년 사이 톱니(피크 ~90MB) — 무료 티어 무시 가능. 설계 + 소프트삭제 예외 정문 = `00.plan.md` Decisions #39. **파괴적 → prune LIVE는 M7 + M6 dry-run 후 USER 게이트.**

- [x] **M1 (ENG, 순수)** `transform/retention.py` — `prune_cutoff(today,years)`(결정적, **달력연도 floor = `date(today.year−years, 1, 1)`** → 완전 연도만 prune, Feb-29 무관, hidden clock 없음) + `partition_years_present(cutoff,today)`. ≤300 LOC. **Acceptance:** `__tests__/spec_retention.py` 달력경계 cutoff/연도범위; pytest green.
- [x] **M2 (ENG, IO)** `storage_sink.object_exists(url,key,bucket,path)` — Parquet 객체 존재 프로브(2xx만 True; 에러→False, network-graceful). **Acceptance:** mock 200→True / 404→False / 예외→False; pytest green.
- [x] **M3 (ENG, sink)** `db_sink` `read_prune_candidates(conn,cutoff,interval)`(라벨별 `MIN(bar_time)`,count) + `delete_history_before(conn,label,cutoff,interval)`(라벨별 DELETE, rowcount, per-label commit). psycopg는 sink만. **Acceptance:** SQL 리뷰; 통합 가능 시 rowcount 검증; pytest green.
- [x] **M4 (ENG, entrypoint, dry-run 우선)** `entrypoints/prune_history.py` — enable 게이트(`parquet_archive_enabled()` AND `supabase_storage()` 없으면 no-op) → candidates → 라벨별 전 연도 `object_exists` 검증 → DELETE(또는 `--dry-run` 로그) → `insert_batch_log(job='collector-prune')`. `today` 1회 boundary 주입. ≤300 LOC. **Acceptance:** `--dry-run`은 삭제 0 + 카운트 로그; archive/storage 미설정 시 완전 no-op 증명; pytest green.
- [x] **M5 (DOCS)** Decisions #39(소프트삭제 예외 정문 포함) 기록 — 이 커밋. **Acceptance:** #39 + 3 가드 plan.md에 존재.
- [x] **M6 (ENG, CI dry-run→live)** 전용 `collector-prune.yml` — cron **`0 7 30 12 *`**(연 1회 12/30) + **`workflow_dispatch`**(수동 fallback, idempotent라 놓쳐도 복구) + `if: failure()` 이슈 알림; env `DATABASE_URL`/`POSTGRES_URL_NON_POOLING`/`SUPABASE_URL`/`SUPABASE_SECRET_KEY`/`COLLECTOR_PARQUET_BUCKET`/`COLLECTOR_ARCHIVE_PARQUET`. 기본 dry-run; 스케줄은 **항상 dry-run**. **첫 LIVE prune = workflow_dispatch에서 `dry_run` 체크 해제(1-click, USER 게이트)**; 신뢰 후 run step 편집으로 스케줄 gate 해제. **Acceptance:** dry-run dispatch가 삭제 0으로 candidates 로그; `dry_run=false` dispatch만 실삭제; 연 1회 스케줄 + 수동 dispatch 동작.
- [x] **M7 (ENG, must-fix R3)** `seed_archive`를 `fetch_full`(period='max')로 심화(또는 `--full` 플래그) → Parquet가 inception 보유; 현 universe(신규 110 포함) seed 재실행. **Acceptance:** 장수 티커 최초 Parquet 연도 < cutoff; seed workflow green; pytest green. **(prune LIVE 전 필수.)**
- [ ] **M8 (ENG/USER, 운영)** 신규 tracked 티커 5년 hot 백필 1회(`run_daily --backfill-days 1825` 또는 `backfill_tracked` 헬퍼), ranking 추가와 연동. **Acceptance:** 샘플 신규 티커 ~5년 봉 존재; 다음 daily 재유입 0(커서 MAX 재개).

> **Ordering:** M1→M2→M3→M4(코어) · M5(doc, done) · M7(LIVE 전 필수) · M6(dry-run→live) · M8(ranking 연동 운영). **prune LIVE는 M7(Parquet inception) + M6 dry-run 성공 후에만.**
> **OUT OF SCOPE(후속 flag):** `market_rankings`(append-only)/`sec_holdings`(tombstone 누적)/`batch_log` 2차 retention.

## Phase N — 2차 retention: 로그 TTL + rankings 10년 아카이브 prune — ENG N1–N9 DONE / 첫 LIVE rankings prune USER-gated (2026-06-30)

> #39 OUT-OF-SCOPE 후속 실현. 성격별 2갈래로 Phase M 머신 재사용: **로그(error_log/batch_log)** = no-tombstone 예외 → 무-아카이브 일수 TTL 하드삭제(매일 LIVE); **도메인(market_rankings)** = 재현불가 → 아카이브-검증-삭제, window **10년**(ML 자문). pg_cron 아님(활성 0개) — 기존 엔트리포인트+Actions+`batch_log` self-observe. 설계 = `00.plan.md` Decisions #40. **DDL 없음**(기존 컬럼/인덱스로 충족).

- [x] **N1 (ENG, 순수)** `transform/retention.py` — `age_cutoff(now,days)`(now-days, tz 보존, boundary 1회 주입, hidden clock 없음) 추가 + `__all__`. **Acceptance:** `spec_retention.py` age_cutoff 4 케이스(감산/tzinfo/timedelta 동치/0일=now); pytest green.
- [x] **N2 (ENG, sink)** `db_sink` — `delete_error_log_before(conn,cutoff)` + `delete_batch_log_before(conn,cutoff,keep_open_failures=True)`(resolved=0 나이 무관 보존) + `read_rankings_prune_years`/`delete_rankings_before` + count 프리뷰 헬퍼. caller commit·rowcount 계약(=delete_history_before). **Acceptance:** SQL 리뷰; live dry-run count 정확.
- [x] **N3 (ENG, entrypoint)** 공유 `entrypoints/prune_logs.py` — `--target error_log|batch_log|all`, `--error-days 7 --batch-days 90 --[no-]keep-open-failures`; 게이트 `database_url()`; now 1회; per-target cutoff→(count|delete+commit); `insert_batch_log(job='collector-prune-logs')` finally. ≤300 LOC. **Acceptance:** live `--dry-run` = 삭제 0 + count 로그 + batch_log 행 1개.
- [x] **N4 (ENG, entrypoint)** `entrypoints/prune_rankings.py` — `--years 10 --[no-]verify-archive`; 게이트 `database_url()`(+verify 시 `supabase_storage()`); `prune_cutoff` 재사용; per-year `object_exists` all-or-skip(절대 부분삭제 안 함) → delete+commit; `insert_batch_log(job='collector-prune-rankings')`. ≤300 LOC. **Acceptance:** live `--dry-run` = cutoff 2016-01-01·후보 0건 실증.
- [x] **N5 (ENG, sink, 아카이브)** 순수 `sink/rankings_parquet_sink.py` — `rankings/{YYYY}.parquet`, PK(as_of,symbol,scope) dedupe keep-last, sort(as_of,scope,rank), read-merge-rewrite atomic. **Acceptance:** `spec_rankings_parquet_sink.py` 8 케이스(그룹/생성/빈=None/dedupe/scope/누적/정렬/nullable); pytest green.
- [x] **N6 (ENG, sink+entrypoint, 아카이브)** `db_sink.read_rankings_years`/`read_rankings_by_year` + `entrypoints/archive_rankings.py`(DB→per-year Parquet→best-effort Storage upload, always exit 0, `--year 0=all`). **Acceptance:** live 실행 2026 파일 1007행 기록; storage 미설정 시 로컬만+exit 0.
- [x] **N7 (ENG, CI logs)** 신규 `collector-prune-logs.yml` — cron **`0 3 * * *`**(매일) + `workflow_dispatch{dry_run}`; **스케줄 LIVE**(무-아카이브·저-blast, dispatch만 dry-run); env `DATABASE_URL`/`POSTGRES_URL_NON_POOLING`; `if:failure()` 이슈. **Acceptance:** dispatch dry-run=count만; 스케줄=실삭제.
- [x] **N8 (ENG, CI rankings)** `collector-prune.yml` 3번째 잡 `prune-rankings`(연 12/30, **스케줄 dry-run**, live=dispatch dry_run 해제, `--verify-archive` ON 양분기, Storage env) + `collector-weekly.yml` best-effort `archive_rankings` 주간 스텝. **Acceptance:** dry-run dispatch 후보 로그; 첫 LIVE=수동.
- [x] **N9 (DOCS)** Decisions #40 + Phase N plan/tasks 스텁 기록 — 이 커밋. **Acceptance:** #40 + N 섹션 존재.

> **Ordering:** N1→N2→N3/N4(prune 코어) · N5→N6(아카이브) · N7/N8(CI) · N9(doc). **rankings 첫 LIVE prune은 archive 채워진 뒤 + dry-run 확인 후 수동**(현재 후보 0건이라 무해 no-op). 로그 prune은 매일 스케줄 LIVE(저위험).
> **현 상태:** 주간 rankings 최근 시작 → 10년 기준 삭제 후보 ~2036까지 0건(전향적 안전장치). pytest 208 green.

## Phase O — `/rankings` Marketcap Rankings 뷰어 + MARKETS nav + 주차 피커 — Phase 1 SHIPPED / Phase 2 DEFERRED (2026-06-30)

> 주간 market_rankings를 `/rankings`로 노출(funds 패턴) + MARKETS nav 드롭다운. market_rankings 백필 불가(#40)라 rank-flow는 단계 분리. 설계 = `00.plan.md` Phase O + Decisions #41. 마이그레이션 없음(0012 기적용; P2가 0019 추가).

### Phase 1 — 뷰어 + 주차 타임트래블 (SHIPPED)

- [x] **O1 (ENG)** Express `controllers/rankings/`(`GET /v1/api/rankings?scope=&sector=&asOf=&limit=`) — raw SQL, `to_regclass` 가드→data:null, MAX(as_of) nearest-≤ asOf 해석+latest fallback, `availableWeeks[]`, s-maxage 86400; `controllers/index.ts` 마운트. (734dc3e/15472fa)
- [x] **O2 (ENG)** 클라 `types/rankings` + `getRankings.action`/`.schema`(Zod AssertEqual, `availableWeeks` `.default([])` 관대) + `asOf` 파라미터.
- [x] **O3 (ENG)** `/rankings` page(force-dynamic, searchParams scope/sector/asOf, single-period 안내) + `RankingsTable`(server, $T/B/M) + `RankingsFilter.client`(GLOBAL/SECTOR + 섹터·주차 드롭다운; 최신주 UNPINNED live·과거주 `?asOf=` pin; 3축 URL co-preserve; 빈목록 `[asOf]` fallback).
- [x] **O4 (ENG)** nav — `buildNavLinks` 3 finance 링크; 데스크톱 TopBar MARKETS 드롭다운, 모바일 NavMenu flat. `/funds`도 nav-도달 가능.
- [x] **O5 (FIX)** 배포 race — `.default([])` + 피커 fallback + 재배포 Data Cache clear; `reportSsrError` stack. **Acceptance:** 기본 `/rankings` 정상(NVDA + single-period 안내), 캐시 무관 현재주 항상 표시. 게이트: server types 0 · client types 0/lint 0. (0dd643d/9c5c64a)

### Phase 2 — 샘플링 rank-flow bump (DEFERRED, 게이트 ~8 주간 스냅샷)

- [ ] **O6** 마이그레이션 0019 `idx_market_rankings_scope_asof (scope, sector, as_of DESC, symbol, rank)`.
- [ ] **O7** `controllers/rankings/rankflow.ts` `GET /rankings/flow?…&granularity=week|month|quarter|year&periods=&k=` — `DISTINCT ON date_trunc` 기간 샘플링 + ever-top-K union(저장 rank·remaining-bucket 없음·granularity 화이트리스트), data:null 가드.
- [ ] **O8** `getRankingsFlow.action`/`.schema`(funds 패턴, cell=`{rank, marketCap}`) + `utils/rankFlowRows` symbol-키 일반화 + `periodLabel(asOf, granularity)`.
- [ ] **O9** `RankingsFlowTable.client`(funds `RankFlowTable` symbol-키 변형); `/rankings` `Snapshot|Flow` 토글, Flow는 `MIN_WEEKS_FOR_FLOW`(≈8) 게이트. **Acceptance:** ≥8주 누적 시 활성; 미만이면 disabled+"N/M weeks" 안내.

### Phase 1b — 심볼 아래 회사명 (Yahoo longName) — SHIPPED (2026-06-30)

- [x] **O10 (ENG)** collector: `fetch_sector_and_name`(sector와 1개 `.info`) + `FundamentalsRow.long_name` + db_sink upsert/read 컬럼-프로브(0018 tolerant) + `plan_refresh` name-missing 트리거(1런 backfill); 마이그레이션 0018 `symbol_fundamentals.long_name`(MANUAL). Express `GET /api/rankings` LEFT JOIN(정보스키마 프로브 조건부) → `name`; 클라 type/schema(`.default(null)`)/RankingsTable(심볼 밑, name==symbol 생략). **Acceptance:** 0018 적용+weekly 1회 후 대형주에 회사명 표시; 미적용/미백필 시 심볼만(graceful). 게이트 pytest 242·types0/lint0. **USER:** 0018 수동 적용 + collector-weekly dispatch.

## Phase P — Investment Signals: 3축 통합 + 투자 후보 선정 — PLANNED (3-agent 설계 2026-07-01)

> 랭킹·13F·가격 3축을 조인해 "투자 후보 선정" 의사결정 제품으로. 블로커 = CUSIP→ticker(`security_map`). 제품의도+설계 = `00.plan.md` Phase P. ml-engineer: ML 시기상조 → 규칙기반 Phase 0부터.
> **Build order:** STEP 0(0-a re-backfill ‖ 0-b security_map) → STEP 1(신호) ‖ STEP 2(종목뷰) → STEP 3(스크리너) → STEP 4(검증/ML). **0-b 없이는 STEP 1/2/3 불가.**
> **Per-task gate:** collector = `uv run pytest`(해당 spec) · 순수 transform 부작용 0(psycopg는 `sink/`만) · 마이그레이션 `pnpm check:schema` in sync + BOTH SSOT 미러 · TS check-types 0 + lint 0 · 파일 ≤300 LOC · soft-delete 전용.

> **STATUS (2026-07-02):** STEP 0 DONE — 백필 LIVE 완료(2013→ ), **STEP 0-b P1–P6 구현**(마이그 0019 + cusip_resolve/figi_fetch/security_map_sink/run_security_map + collector-security-map.yml + README; pytest **257** green, 전 파일 ≤300 LOC). architect 검증 통과(MEDIUM 2 + LOW 수정 반영). **STEP 1 백엔드/로직 DONE** — P7-1(마이그 **0020** fund_registry + 뷰 5종: v_sec_positions/fund_periods/position_delta/exits/consensus), P7-2(순수 `utils/rankFlowBadges.ts` + vitest 6), P8-1(`controllers/securities/consensus.ts` + 마운트 + jest 6), P8-2(`types/consensus.ts` + schema+AssertEqual + `getSecurityConsensus.action.ts`; client types 0). **STEP 1 UI DONE** — P7-3(`HoldingCell.tsx` 추출 + NEW/▲▼ 배지; RankFlowTable 412→365 LOC), P8-3(`ConsensusPanel.client.tsx` 선택-cusip lazy fetch, `/funds/[cik]`에 배선). 게이트: client tsc 0 · eslint 0 · vitest green. EXIT 배지는 ghost/flow 불일치 회피로 이번 제외(util은 계산, consensus/screener가 활용). **STEP 2 P9 DONE** — `GET /api/securities/:ticker/by-ticker` + `/symbols/[ticker]`(랭킹추이 + 보유기관; jest 8). **STEP 3 P10 DONE** — 순수 `helpers/screenScore.ts`(+jest) + `GET /api/securities/screen` + `/screener`(4항 결정적 스코어, 행→/symbols 링크). **momentum·lowVol 스코어항 DONE (2026-07-02)** — 순수 `helpers/priceSignals.ts`(12-1M Jegadeesh-Titman skip-month momentum + 연율 변동성 + 후보군 percentile 정규화; jest 15) + `screen.ts` `market_history` 1d 종가 1쿼리 조인→정규화→`computeScore(momentum 0.3/lowVol 0.1)`; tracked universe 밖/이력<253봉은 null(부분점수). 게이트: 타깃 jest 44/44 · check-types 0. **가격 차트 on /symbols DONE (2026-07-02, 07f0a86)** — 순수 서버 `components/symbols/PriceChart.tsx`(의존성 없는 SVG area 차트: 그라데이션 채움+min/max 라벨+날짜축+현재가/변동%; `normalizeSpark` 재사용), 구 `PriceSparkline`→`x_` 소프트삭제. **혼합정렬 정규화 DONE (2026-07-02, PM+architect 공동판정 Option C)** — 점수 공식(순수 computeScore) 무변경, 정렬만 2티어 분리: 순수 `helpers/screenSort.ts`(`hasPriceSignals`=momentum&&lowVol 둘 다 present / `compareScreenCandidates`=가격신호완비 우선→score DESC→holders DESC), `screen.ts`가 candidate에 `hasPriceSignals` 방출, `/screener`가 "Price-signal candidates" + "Consensus-only" 2섹션 렌더(품질서열 아님·데이터가용성 라벨; `ScreenerTable` 추출). 게이트: server jest 52/52 target · 제 client 파일 types/lint 클린. **REMAINING(코드 없음, 데이터/후속):** filing_date lookahead 게이팅(13F period vs filing_date 시점 정합; 라이브 가격 모멘텀은 룩어헤드 아님). **USER 게이트:** 0019·0020 수동 apply, OpenFIGI 키+secret, `collector-security-map` dispatch → 그때 holders/스크리너에 실데이터 유입. **STEP 0~3 코드 전부 완료** (feature/phase-p-signals). **참조 결정:** "액티브 펀드"=Option B `fund_registry` 테이블(PM+architect 공동채택 2026-07-02, ml-engineer 액티브 3사). O6이 참조하던 "0019 인덱스"는 재넘버링 필요(0019=security_map 선점).

### STEP 0 — 데이터 기반 (선행 필수)

- [ ] **P0a (USER)** 13F 5펀드 re-backfill — `gh workflow run collector-13f-backfill -f since=2013 -f archive=true`(cik 비움=6펀드 전체, idempotent). **Acceptance:** `batch_log job='collector-backfill'` 성공 행; 펀드당 다분기 sec_filings; amendment-정정 이후 원본 revive 확인.
- [ ] **P1 (ENG)** 마이그레이션 **`0019_security_map.sql`**(apply 직전 next free 번호 재확인) — `security_map`(cusip PK, ticker, name, sector, source, confidence, resolved_at, attempt_count, last_attempt_at, deleted_at) + `idx_security_map_ticker`(partial) + 뷰 `v_sec_holdings_enriched`(sec_holdings ⋈ security_map ⋈ symbol_fundamentals, `COALESCE(f.sector,m.sector)`); BOTH `tradelunch.schema.sql` SSOT 미러. **Acceptance:** idempotent 적용; 미해소 CUSIP은 ticker NULL로 뷰에서 자연 제외; check:schema in sync.
- [ ] **P2 (ENG)** 순수 `transform/cusip_resolve.py`(+spec) — ever-top-N 후보 필터, OpenFIGI 응답→row 파싱, ticker 정규화(`BRK/B→BRK-B`). **Acceptance:** I/O 없음; 정규화·파싱 pytest green.
- [ ] **P3 (ENG)** `lib/constants.py` `PROVIDER_OPENFIGI` + `sink/figi_fetch.py`(`/v3/mapping` batch, rate_limit + `OPENFIGI_API_KEY` env, `config/settings.py` getter). **Acceptance:** rate 페이싱; 키 미설정 시 graceful; 무키/유키 한도는 착수 시 공식문서 2회 검증(latest-stable 규칙).
- [ ] **P4 (ENG)** `sink/security_map_sink.py` — 후보 CUSIP SELECT(8분기 top-N·미해소·`prn_type='SH'`·`put_call=''`) + `ON CONFLICT(cusip)` upsert + attempt 카운터(영구 미매핑 재시도 폭주 차단). raw SQL psycopg3. **Acceptance:** 후보 정확; 재실행 idempotent.
- [ ] **P5 (ENG)** `entrypoints/run_security_map.py`(`--dry-run`/`--limit`; sector 복사 조인; `batch_log job='collector-secmap'`; run_monthly 골격). **Acceptance:** dry-run 후보 수 로그(write 0); ≤300 LOC; pytest green.
- [ ] **P6 (ENG/USER)** `.github/workflows/collector-security-map.yml`(월 08:00 UTC cron + `DATABASE_URL`/`OPENFIGI_API_KEY` 시크릿) + `apps/stock_collector/README.md` 갱신. **Acceptance:** dispatch 완주; 서빙 CUSIP ≥90% resolve. **USER:** OpenFIGI 키 발급 + 시크릿 등록 + 1회 dispatch.

### STEP 1 — 신호 레이어 (구 L13/L14 승격)

- [ ] **P7 (ENG)** Δ배지 — 마이그레이션 분석뷰(`v_sec_position_delta`/`v_sec_exits`; 구 L12 0018→재넘버링) + rank-flow 셀 NEW/EXIT/▲▼/Δweight 배지. **Acceptance:** 첫 백필 분기 전부 NEW; 색은 비-load-bearing 유지.
- [ ] **P8 (ENG)** cross-fund consensus — `v_sec_consensus` + `GET /api/securities/:cusip/consensus`(holderCount + 펀드별 분해) + UI "N개 펀드 보유". ⚠️ **액티브 3사만**(패시브 제외 or 분리). **Acceptance:** unknown cusip→null; `controllers/index.ts` 마운트.

### STEP 2 — 종목 중심 통합 화면

- [ ] **P9 (ENG)** `/symbols/[ticker]` — 가격 차트 + 시총 랭킹 추이 + 보유 기관 리스트 3블록(`v_sec_holdings_enriched` 경유). 기존 컴포넌트 재사용. **Acceptance:** /rankings·/funds 종목 클릭→3블록 실데이터 렌더; 하이드레이션 에러 0.

### STEP 3 — 후보 스크리너

- [ ] **P10 (ENG)** 스크리너 — 필터(`시총 rank≤N ∧ 보유 액티브펀드≥M ∧ Δ 양수 ∧ 모멘텀 상위 50%`) + Phase 0 스코어(`0.4·consensus+0.3·momentum(12-1M)+0.2·cap_tier+0.1·low_vol`). ⚠️ **filing_date 기준 신호 유효화**(룩어헤드 방지). **Acceptance:** 동일 입력→결정적 후보; 각 행 P9 링크.

### STEP 4 — 검증 → ML (DEFERRED until data)

- [ ] **P11 (ML) — DEFERRED** `rule.consensus_momentum.v001` paper-trading(SPY 대비 forward return 기록) → 6~12개월 축적 후 walk-forward + gradient boosting. **Acceptance:** N/A until 데이터 충분(현재 13F 6펀드×최신1분기·가격 ~1.1년). 리스크: 룩어헤드·패시브 오염·생존편향·13F 롱온리/45일지연/PRN NULL.

### 데이터 품질 — 비거래일 가짜 바 가드 (collector)

- [x] **DQ1 (ENG)** 주말 필터(`transform/ohlc.py` `allow_weekends`, crypto만 예외) + US 합의 기반 고립-날짜 탐지(`transform/detect_isolated_bars.py`, `run_daily --full`에서 `error_log`로 로그-only) SHIPPED. US(~110 stocks)엔 신뢰 높음.
- [ ] **DQ2 (ENG) — DEFERRED (KRX)** KRX(KOSPI·KOSDAQ, 2심볼)·FX(4) 공휴일 가짜 바는 합의로 불가 → 실제 거래 캘린더 필요. 옵션: `exchange_calendars`(XKRX 음력 포함) 경량 도입 or KR 휴일 테이블. **Acceptance:** KR 공휴일 바 원천 차단. **Note:** 정상 운영선 yfinance가 거래일만 반환하므로 잔여 리스크 낮음(주말 필터로 최빈 벡터는 차단됨). 우선순위 낮음.

## Phase Q — US Politician Stock Disclosures (STOCK Act PTR) — MVP LIVE (Q1–Q5, Q5.3 검증 green) / Q6·Q7 후속 (2026-07-02)

> **kadoa-org/congress-trading-monitor(MIT) 미러링** → `politician_registry`/`politician_trades`(EVENT only) → "traded by N politicians · 90d" 투명성 신호(심볼 페이지 + 스크리너). 제품의도 + settled 결정 + OPEN ITEMS(kadoa 필드/SLA/§13107) = `00.plan.md` Phase Q. 전 행 `source='kadoa'` provenance(미러=비권위본); 공공기록이라 아카이브 허용. **§13107(상업이용 제한): 무료·뉴스성 유지 = news 예외 포지션; 유료화 전에만 법률검토.** **MVP = Q1–Q5; ship은 Q5.3 검증 게이트 green에 gated.**
> **Build order:** Q1 → Q2 ‖ Q3(코드) → Q3.5 라이브 → Q4 → Q5(+게이트) → Q6/Q7. **OPEN ITEM (1) kadoa 필드 실측 전 파서 확정 금지.**
> **Per-task gate:** collector = `uv run pytest`(해당 spec) · 순수 transform 부작용 0(psycopg는 `sink/`만) · 마이그레이션 `pnpm check:schema` in sync + BOTH SSOT 미러 · TS check-types 0 + lint 0 · 파일 ≤300 LOC · 업서트 = `ON CONFLICT(external_id)` idempotent(kadoa id 유니크).

### Q1 — 스키마 (마이그레이션 0022 + 뷰)

- [x] **Q1.1 (ENG)** 마이그레이션 **`0022_politician_trades.sql`**(apply 직전 next free 번호 재확인 — multi-session) — kadoa-native(실측): `politician_registry`(**filer_id TEXT PK**, filer_name, party, chamber house|senate, branch congress|executive, state, office, agency, bioguide_id nullable(후속 enrichment), source='kadoa', deleted_at) + `politician_trades`(**external_id TEXT UNIQUE** = kadoa `id`, filer_id FK, disclosure_date **NOT NULL**(=kadoa filing_date), transaction_date nullable, transaction_type buy|sell|exchange, transaction_type_raw, filer_owner self|spouse|dependent|joint, owner_raw, asset_type equity|bond|option|other default 'other', asset_type_raw(kadoa 코드 원본 — source-representation-first), ticker nullable, asset_name, value_min/max BIGINT(kadoa amount_range_low/high)/value_estimate BIGINT(기하평균)/value_label, doc_url, source_id(house_clerk|senate_efd|oge), filing_type(PTR|278-T), days_to_file INT, is_late BOOL, source='kadoa', deleted_at) + partial 인덱스 `(ticker, disclosure_date DESC)`/`(disclosure_date DESC)` WHERE deleted_at IS NULL; BOTH `tradelunch.schema.sql` SSOT 미러. **Acceptance:** idempotent 적용; `pnpm check:schema` in sync. **(USER: 수동 apply.)**
- [x] **Q1.2 (ENG)** 뷰 `v_politician_trades_enriched`(trades ⋈ registry ⋈ **security_map-by-TICKER** ⋈ symbol_fundamentals — 이 경로 OpenFIGI 없음) + `v_politician_activity`(ticker × 90d disclosure-window: traded_by_count=DISTINCT filer_id(bioguide 미보유 시까지; slug 편차로 소폭 over-count 가능—후속 정합), buy/sell_member_count(의원별 net 방향), net_direction, latest_disclosure_date, cluster_flag ≥3 동방향; exchange는 방향 제외/count 포함; ticker NULL 제외; deleted_at IS NULL 전 경로). 0022 동봉. **Acceptance:** 빈 테이블에서 뷰 0행(에러 없음); 미해소 ticker=NULL 제외; kadoa id 유니크라 중복 없음.
- [x] **Q1.3 (ENG)** provenance/§13107 명문화 — 0022 헤더 주석에 5 U.S.C. §13107(c) 상업이용 제한(news 예외, 유료화 전 법률검토) + provenance(`source='kadoa'`, 미러=비권위본, 후속 House-bulk 교체 가능) 명시. 공공기록이라 아카이브 허용. **Acceptance:** 주석 존재.

### Q2 — Registry (feed-derived, 별도 시드 불필요)

- [x] **Q2.1 (ENG)** `schema/rows.py`에 `PoliticianRow` frozen dataclass(filer_id/filer_name/party/chamber/branch/state/office/agency/bioguide_id=None). kadoa 각 trade 행에 필러 메타가 인라인이므로 파싱 시 distinct filer로 집약. **Acceptance:** I/O 없음; pytest green.
- [x] **Q2.2 (ENG)** `sink/politician_db_sink.py` registry upsert(`ON CONFLICT(filer_id) DO UPDATE ... deleted_at=NULL`; 퇴임/현직 무관 전 filer 유지 — 과거 trade 귀속). ingest(Q3.4)가 feed의 distinct filer를 함께 upsert(별도 entrypoint 불필요). **Acceptance:** 재실행 idempotent; ≤300 LOC; pytest green. **Note:** congress-legislators로 bioguide_id 보강은 후속(committee 등 enrichment 필요 시).

### Q3 — Ingest (kadoa PTR mirror)

- [x] **Q3.1 (DONE 2026-07-02)** kadoa `trades.json` 실측 완료 — 필드/타입/카디널리티 확정, 픽스처 `__tests__/kadoa_trades_sample.json`(40건, 다양성) 커밋. 매핑: id→external_id, filing_date→disclosure_date, transaction_date, owner→filer_owner(Self/SP/Spouse→spouse, JT/Joint→joint, Child/DC→dependent, Self→self), transaction_type(Purchase→buy, Sale (Full)/(Partial)→sell, Exchange→exchange), asset_type 코드→enum(CS/PS/ST/Stock→equity, GS/Corporate Bond/Municipal Security→bond, OP/OL→option, 기타→other; raw 보존), amount_range_low/high→value_min/max, ticker(42% null).
- [x] **Q3.2 (ENG)** 순수 `transform/politician_parse.py` + `PoliticianTradeRow` frozen dataclass(`schema/rows.py`) — kadoa row→(TradeRow, PoliticianRow): 위 Q3.1 매핑 적용, value_estimate=sqrt(low*high) 기하평균, Yahoo ticker 정규화(BRK.B→BRK-B), 미해소 ticker=NULL 보존. **Acceptance:** I/O 없음; 픽스처 40건 전부 파싱 + type/owner/asset 매핑 edge pytest green.
- [x] **Q3.3 (ENG)** `sink/kadoa_fetch.py`(raw.githubusercontent.com `public/data/trades.json` GET, **키 불필요**, ETag/If-None-Match + backoff) + `politician_db_sink.py` trades upsert(`ON CONFLICT(external_id) DO UPDATE ... deleted_at=NULL`; kadoa `id` 유니크라 복합키/amendment 로직 불필요) + registry upsert(Q2.2) 동시. **Acceptance:** 네트워크 실패 graceful; 재실행 신규 write 0(idempotent).
- [x] **Q3.4 (ENG)** `entrypoints/run_politician_trades.py`(`--dry-run/--since`; 가드레일 로그: ptr_legs_ingested/distinct_members/unresolved_asset_pct/amendment_supersede_count/4a4b_collapse_count/max_traded_by_count/feed staleness; try/finally `insert_batch_log(job='collector-politician-trades')`). **Acceptance:** dry-run = write 0 + 가드레일 카운트 출력; ≤300 LOC; pytest green.
- [x] **Q3.5 (ENG)** `.github/workflows/collector-politician-trades.yml`(`collector-security-map.yml` 복사; cron **매일 ~09:30 UTC**, 기존 cron offset; `DATABASE_URL` 시크릿만 — **API 키 없음**; dispatch 기본 dry-run; `if:failure()` 이슈). **USER:** 1회 dispatch(구독/키 불필요). **Acceptance:** dispatch 완주; batch_log 행; bioguide 미해소 행 NULL로 보존.

- [ ] **Q3.6 (ENG) — 선택/DEFERRED (저우선)** 월 1회 best-effort Parquet DR 스냅샷 — kadoa SLA 부재 대비. 신규 `archive_politician_trades.py`(전체 테이블 read → `politician_trades/{YYYY-MM}.parquet` 덤프, 롤링 3–6개월 보관, best-effort always exit 0; Phase N `archive_rankings` 패턴 재사용) + `collector-politician-trades.yml`에 월간 스텝(또는 별 워크플로). **보존 = 전체 hot·prune 없음**(데이터 작음). **Acceptance:** 스냅샷 실패해도 수집/서빙 무영향; 데이터 재취득 가능성상 필수 아님(MVP 후).

### Q4 — 심볼 페이지 집계 (첫 user-visible ship)

- [x] **Q4.1 (ENG)** `controllers/securities/byTicker.ts` — presence-guarded `politicianActivity` 블록(`to_regclass('v_politician_activity')` 없으면 null/생략; ≤300 LOC 초과 시 `helpers/politicianActivity.ts` 추출): traded_by_count/buy_member_count/sell_member_count/net_direction/latest_disclosure_date/cluster_flag + 밴드 라벨(점 달러 금지). **Acceptance:** 뷰 부재 시 기존 응답 무변경(regression 0); jest green.
- [x] **Q4.2 (ENG)** 클라 — `types/symbolDetail.ts` + `getSymbolDetail.schema.ts`에 nullable/optional `politicianActivity`(**관대 파싱** `.optional()`/`.default` — Phase O 배포-race 교훈) + 신규 패널(ConsensusPanel 각색, holdings 시맨틱 제거): "Traded by N politicians · 90d" 배지 + freshness(≤14d Fresh/15–45d Recent/46–90d Aging) + **상시 disclaimer**(30–90+일 지연=현재 포지션 아님·브래킷 범위·not advice·no outperformance) + coverage 각주(House+Senate+행정부 278-T; 펀드/ETF/국채/부동산 면제) + 중립 party 배지. **금지:** held/own/portfolio/weight%. **Acceptance:** activity null → 패널 미렌더(레이아웃 정상); disclaimer 상시 가시(툴팁 숨김 금지); check-types 0/lint 0.

### Q5 — 스크리너 컬럼 + 검증 게이트 (MVP ship)

- [x] **Q5.1 (ENG)** `controllers/securities/screen.ts` — presence-guarded dynamic join(`v_politician_activity`)으로 traded_by_count(90d) + net_direction 추가; 정렬 = **raw traded_by_count DESC**(스코어 아님 — Q7 전). **Acceptance:** 뷰 부재 시 스크리너 무변경; jest green.
- [x] **Q5.2 (ENG)** `/screener` — traded_by_count 컬럼 + net_direction 칩(buy_skew|sell_skew|mixed) + Q4.2와 동일 disclaimer/coverage 각주. **Acceptance:** activity 없는 행 graceful(빈 셀); check-types 0/lint 0.
- [x] **Q5.3 (ENG/ML — MVP ship 게이트) DONE 2026-07-02** 검증 green — 배포 API 스팟체크 분포 sanity(MSFT 14·NVDA 12·AAPL 11·JPM 7·TSLA 3·KO 2·WMT 1·unknown=null, heavy right-skew) + net_direction/cluster 일관 + unresolved_ticker 42.1%. LIVE 노출.

### Q6 — 의원 드릴다운 (enrich, OPEN ITEM (1) 게이트)

- [ ] **Q6.1 (ENG)** `/politicians/[filer_id]` — **aggregate-first**(의원별 traded 자산·방향·밴드 버킷·freshness) + raw per-trade 테이블(공공기록·§13107 news 프레이밍 하 저위험). **비주식 포함 필수**: 거래의 53%가 채권(GS/muni/corp)·옵션·기타 — ticker 없이 `asset_name` + `asset_type` 라벨로 표시(가격 파이프라인 불필요; 이미 politician_trades에 저장됨). **Acceptance:** aggregate 요약 + raw 표(주식+비주식 전부) 렌더; 상시 disclaimer/coverage 각주 포함.

### Q8 — 정치인 티커 유니버스 top-N 확장 — LIVE (top-75 승격, 2026-07-02)

> LIVE: `promote_politician_tickers` dispatch → candidates=3053, already_tracked=77, promoted=**75**(distinct-filer breadth, CUSIP/이미추적 제외; PFE/MDT/CVS/BLK/LMT/UBER…) → tracked_symbols sticky upsert. run_daily 트리거로 OHLC/시총 수집. dispatch-only 워크플로(재큐레이션은 필요 시 재실행). pytest 353/353.

- [x] **Q8.1 (ENG)** 정치인 최다거래 **주식** 티커 top-N(예: 50–100)을 OHLC/시총 수집에 편입 → 심볼 페이지 가격/랭킹 + 스크리너 노출. 신규 830 티커 중 12개만 기존 유니버스(watchlist 24 ∪ tracked_symbols≈110)와 겹침; 전부(818×5yr≈450MB)는 무료 500MB 초과 → **top-N만**. 방식: `v_politician_activity` traded_by_count 상위 티커를 tracked_symbols에 sticky upsert(run_weekly 승격 패턴 재사용) 또는 신규 politician-tracked 소스. 대형주 다수는 이미 랭킹 tracked라 실제 신규 편입은 더 적음. **Acceptance:** top-N이 run_daily 유니버스에 포함; DB 증가 bounded(수십MB); 스크리너에서 해당 티커 discoverable.

> **NOTE (backfill):** MVP는 kadoa `trades.json`=최근 ~5000건(~6.5개월)만 적재(90d 신호 충분). 전체 역사(54k, 2012~)는 kadoa per-filer 파일(`public/data/filer/*`)에 — **Q11에서 실현**(다년 분기 타임라인 전제). 일일 크론이 전향 누적(5000-윈도우 이탈 행 미삭제).

> **Phase Q 확장 — LIVE (Q9–Q12 + Q6.2–6.4; 0023 applied + backfill 57,736행/434 filer 적재 + 프로덕션 검증 josh_gottheimer 547 티커·2421 타임라인/38분기). 2026-07-02.** Build order: Q9 ‖ Q10 → Q6.2–6.4 → Q11 → Q12; **MVP 3종(Q9/Q10/Q6확장)은 Q11 백필 무관, Q12만 Q11 의존.** 전 태스크 공통: presence-guard(to_regclass) + HONESTY 라벨(disclosed transaction volume/disclosed trading — held/own/position/portfolio-weight 금지) + 금액=밴드 `<$15K/$15K–50K/$50K–250K/$250K–1M/>$1M`(점 달러 금지) + PoliticianDisclaimer 상시.

### Q9 — per-ticker politician holders (확장 MVP #1, 백필 무관)

- [x] **Q9.1 (ENG)** 마이그레이션 **`0023_politician_holders.sql`**(apply 직전 next free 재확인 — multi-session; additive) — `ALTER politician_registry ADD` photo_url TEXT/trade_count INT/purchases INT/sales INT/late_filings INT/est_volume BIGINT + 뷰 `v_politician_ticker_holders`(politician_trades GROUP BY (ticker, filer_id): SUM(value_estimate)·trade_count·net direction(FILTER idiom)·latest/first disclosure; ticker NOT NULL·deleted_at IS NULL) + `v_politician_filer_timeline`(GROUP BY (filer_id, ticker, date_trunc('quarter', transaction_date)): net disclosed value+direction); BOTH `tradelunch.schema.sql` SSOT 미러. **Acceptance:** idempotent 적용; 빈/recent-only 데이터에서 뷰 에러 없음; `pnpm check:schema` in sync. **(USER: 수동 apply.)**
- [x] **Q9.2 (ENG)** `helpers/politicianHolderShares.ts` — holderWeights.ts 2-scoped-query 모양 클론(전테이블 GROUP BY 뷰 아님): filer별 disclosed volume 총합 분모 → sharePctOfFilerVolume + rankInFilerVolume; 순수 계산부 분리. **Acceptance:** jest(share 합=합리적·rank 결정적·동률 처리) green; ≤300 LOC.
- [x] **Q9.3 (ENG)** by-ticker DTO 확장 — `politicianHolders[]`(filerId/filerName/party/chamber/disclosedValueBand/sharePctOfFilerVolume/rankInFilerVolume/tradeCount/netDirection/latestDisclosure), 기존 `politicianActivity`와 **sibling 유지(둘 다)**; 신규 `fetchPoliticianHolders` helper, `to_regclass` 부재 시 `[]`. **Acceptance:** 뷰 부재 시 기존 응답 regression 0; 점 달러 미노출(밴드만); jest green.
- [x] **Q9.4 (ENG)** 심볼 페이지 — 13F holders 테이블 미러한 politician holder 테이블: filer name+중립 party 칩; weightPct 자리 = sharePctOfFilerVolume(라벨 "% of disclosed transaction volume"); rank 배지("among disclosed tickers"); 금액=밴드. 클라 스키마 관대 파싱(`.optional()`/`.default`); PoliticianDisclaimer 재사용. **Acceptance:** holders `[]` → 테이블 미렌더(레이아웃 정상); disclaimer 상시; held/own/position/portfolio-weight 문자열 0; check-types 0/lint 0.

### Q10 — registry enrichment (filers.json 미러, 확장 MVP #2)

- [x] **Q10.1 (ENG) — OPEN ITEM (3) 게이트** filer_id 3-소스 동일성 실측 — trades.json vs filers.json vs `filer/{id}.json` 슬러그 일치 확인 + filers.json 픽스처 커밋; 불일치 시 매핑 확정 전 Q10.2/Q11 진행 금지. **Acceptance:** 실측 결과 01.status.md 1줄 기록 + join 키 확정.
- [x] **Q10.2 (ENG)** filers.json fetch(434행, 키 불필요) → registry enrichment upsert(photo_url/trade_count/purchases/sales/late_filings/est_volume)를 **기존 `run_politician_trades.py`에 폴드**(별도 잡/워크플로 없음; best-effort — filers 실패 시 trades 무영향). 소스 집계 = "as reported by source"(전 역사 기준, 백필 전 불일치 정상). **Acceptance:** 재실행 idempotent; filers 실패해도 trades upsert 완주+exit 정상; ≤300 LOC; pytest green.

### Q6 확장 — 프로필 API+페이지+스크리너 expand-row (MVP #3)

- [x] **Q6.2 (ENG)** 신규 `GET /v1/api/politicians/:filerId`(filerId regex-validated; 미존재 → `data:null`) — { filer 프로필+소스 집계(est_volume 등 **밴드로**, "as reported by source"), tickers[] ranked(Q9.2 helper 재사용), timeline[] 분기(volume-guard <2분기 → 숨김; **백필 전 `[]`**) }. presence-guard. **Acceptance:** 미존재 filerId → data:null(500 아님); 백필 전 timeline `[]` graceful; jest green.
- [x] **Q6.3 (ENG)** `/politicians/[filerId]` 페이지 — 헤더(photo_url+중립 party/chamber/state 칩+소스 토탈 밴드) + ranked-tickers 테이블(Q9.4 컴포넌트 재사용) + 분기 타임라인 슬롯(데이터 없으면 섹션 미렌더). aggregate-first·비주식 asset_name+asset_type 포함. **Acceptance:** timeline `[]` → 섹션 숨김·레이아웃 정상; disclaimer/coverage 각주 상시; check-types 0/lint 0.
- [x] **Q6.4 (ENG)** 스크리너 — 후보별 `politicianTopFilers[]`(top ~3 filer_id+name만; per-row 비대화 금지, presence-guarded) → expand-row 아코디언 "Traded by: {names}"; 이름 → `/politicians/[filerId]` 링크. **신규 viz 없음.** **Acceptance:** topFilers 없는 행 graceful; 뷰 부재 시 스크리너 무변경; check-types 0/lint 0.

### Q11 — 전체 역사 one-shot 백필 (~54k; Q12 전제조건)

- [x] **Q11.1 (ENG)** `entrypoints/backfill_politician_trades.py` — filers.json 순회 → `filer/{id}.json` fetch → **기존** parse_trades+upsert_trades 재사용(신규 파서 금지), `ON CONFLICT(external_id)` dedup; `--dry-run/--limit`; 가드레일 로그(filers_visited/rows_fetched/rows_upserted/dup_skipped/filer_failures) + batch_log; workflow_dispatch 일회성(cron 없음). ~54k ≈ ~50MB — 전체 hot·prune 없음. **Acceptance:** dry-run write 0; 재실행 신규 write 0(idempotent); filer 부분 실패 시 나머지 계속; ≤300 LOC; pytest green. **(USER: 1회 dispatch.)**

### Q12 — 분기 타임라인 (Q11 의존 — 백필 후 자동 점등)

- [x] **Q12.1 (ENG)** `PoliticianTimeline.client.tsx`(<150 LOC 신규 소형 — **Phase L RankFlowTable 포크 금지**): 행=티커, 열=분기, 셀=net buy/sell **밴드**(green/red/neutral); 분기별 그룹 리스트 fallback 허용. Q6.2 timeline[] 소비; volume-guard(<2분기) 시 숨김. 카피 = "disclosed trading activity per quarter"(분기말 holdings 아님). **Acceptance:** timeline `[]`(백필 전) → 미렌더 regression 0; 점 달러 미노출; disclaimer 상시; check-types 0/lint 0.

> **OPEN ITEMS (확장):** (1) 밴드 경계 = `<$15K/$15K–50K/$50K–250K/$250K–1M/>$1M`(PTR 법정 브래킷 정렬) — 확정; (2) registry 소스 집계(전 역사) vs recent-only 불일치 → "as reported by source"(Q10.2), 백필(Q11) 후 수렴; (3) filer_id 3-소스 동일성 실측(Q10.1) 전 FK/join 의존 금지; (4) filer_id≠bioguide over-count 캐비앳 유지.

### Q7 — political_interest_score (DEFERRED, 검증 후)

- [x] **Q7.1 (ENG) — DONE 2026-07-02** 순수 `helpers/politicalScore.ts`(`0.55·breadth(min(1,count/5)) + 0.30·consensus(max(buy,sell)/count) + 0.15·notionalTier(3단)`, [0,1]; jest) + 스크리너 정렬 편입. **13F 스코어와 절대 합산 금지(2렌즈 분리).** **Acceptance:** 결정적(동일 입력→동일 점수); Q5.3 green + 분포 안정 후에만 raw-count 정렬 대체.
