# Tasks — tradelunch

**Last updated:** 2026-06-30
**Status** (per-phase detail in the stubs below; full history in `_docs/archive/`):

- **DONE/SHIPPED:** Phase 1 · Repo Hygiene · dashboard Cycle 2 · Phase A (live on `my.prettylog.com`) · blog_agent merge · Phase B backend + publish pipeline · Phase D (ENG) · Phase E + E2 · Phase F (`2a0457b`) · F2 (`3e5b04c`) · G (`bced84e`) · Phase H P0–P5 (`2c0f897`/`f83a38e`) · blog card actions (`894fce3`) · Phase 2-mobile (MOB.0–MOB.3) · **Phase 2-filter** (multi-category + multi-tag feed filter, F.1–F.11, commits `a05a299`..`4e8483b`, jest 25 suites/166 tests) · Phase I committed (`56b8be5`) + ENG follow-ups LIVE (`f971661`: parquet archive+seed 11636 rows/83 obj uploaded, I2.8 cache + migration 0013 applied, SECRET_KEY+apikey, checkout v7).
- **ACTIVE/OPEN:** Phase 2-mobile (mobile context UX — SHIPPED MOB.0–MOB.3; note: Phase 2-filter SUPERSEDED its mobile category/tag NAV chip row → `MobileChipRow`/`getMobileChips` `x_`-retired, only `MobileAuthorChip` + Option-D TOC remain) · Phase 2-fetch (feed axios→native-fetch + tag revalidation — SHIPPED; F-fetch.1 admin-delete revalidate DONE; **F-fetch.2 client-axios retirement PLANNED 2026-07-07, GATED on backtest commit** — see Phase 2-fetch F-fetch.2) · Phase 2-simple Cycle 3 (dashboard mock→real — **COMPLETE**: snapshot live + `DASHBOARD_DATA_SOURCE=backend` flip done + chart history wired to backend; `/dashboard` fully backend-sourced) · Phase C (not started) · Phase H P6 (DEFERRED) · Phase I (collector-write live + db push 0011/0012/0013 applied; remaining = KOSPI/KOSDAQ `.KS/.KQ` coverage [Decision #32] + I1.16 USER live-prod READ smoke) · **Phase L (PLANNED — `/funds/[cik]` 13F rank-flow viz, backfill 2년; L1–L14, Decisions #38; not started)**.
- **USER go-live gates:** see the consolidated snapshot below (Clerk env, backend `SUPABASE_*`, admin bootstrap, `db push` 0010, Cloudflare CNAME).
  **Branch:** feature/finance → main

> Atomic execution breakdown. Product intent → `00.plan.md`. Running state → `01.status.md`.
> Verbose source docs (full migration runbook, blog_agent merge ADR) archived in `_docs/`.

**Legend:** `[x]` done · `[~]` in progress · `[ ]` todo · **(USER)** needs Supabase/Vercel/DNS creds · **(ENG)** code only · **SUPERSEDED** no longer applicable.

---

## Go-live USER gates (consolidated snapshot — 2026-06-26)

**DONE:** `supabase db push` of `0004` (market) + `0005` (draft enum) applied to remote (all
`0001`–`0005` Local=Remote); `seed.market.sql` loaded (`market_snapshots`=22, `market_history`=5500);
Supabase Storage bucket `blog.prettylog` created PUBLIC; **blog_agent first real publish landed live**
(`java-spring-jdbc`, user_id=1, status='public', thumbnail public URL) and reads back via
`GET /v1/api/posts`.

**DONE (added 2026-06-29):** Clerk Production env on BOTH Vercel projects (`pk_live`/`sk_live`,
backend `ALLOWED_ORIGINS`) + public-sign-up ON _(D1.1)_; `error_log` telemetry `0014`/`0015`/`0016`
all applied — browser+SSR+**Express global handler** (`source='express'`, `81e0962`) live, `resolved` triage on.

**STILL OPEN (USER):**

- Backend Vercel `SUPABASE_URL` + `SUPABASE_SECRET_KEY` — else image upload → 503. _(D2.6a)_
- Admin bootstrap SQL `UPDATE users SET is_admin=true WHERE username='<owner>'` — owner =
  `taek.lim.us@gmail.com`, AFTER signup + onboarding; `email` is NULL for lazy-provisioned users →
  match by `username`/`clerk_user_id`, never email. _(D4.0b)_
- ~~Flip `DASHBOARD_DATA_SOURCE=backend` on frontend Vercel + redeploy.~~ **DONE (2026-06-29)** — flipped + redeployed; chart history also wired. _(Phase B B4)_
- market-data provider + KRX `.KS/.KQ` suffix decision (KOSPI/KOSDAQ snapshot null until resolved). _(Phase B / Decisions #32)_
- Set blog*agent local `.env` `DEFAULT_USER_ID=2`→`1` (sandbox blocked the edit; non-blocking —
  post frontmatter `userId` drives publisher identity). *(B5.7)\_

> **NOTE (not an open task):** Cloudflare `CDN_ASSETS` CNAME is branded-domain-only polish — parked, not on the active board. Revisit only when the branded asset domain is actually wanted.

---

## Repo Hygiene — DONE (2026-06-24/25)

> `@repo/types` created; `@repo/markdown-parsing` retired from client (TOC inlined); sequelize/pg-hstore removed (`@types/pg` added); express-types override → direct devDep (f026fca). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.repo-hygiene.md](./_docs/archive/tasks.repo-hygiene.md)

---

## Phase 1 — Blog MVP — DONE

> SSR post list, infinite scroll, Markdown render (GFM/KaTeX/highlight), nav, theme toggle, next-intl i18n, OG metadata. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-1-and-dashboard-cycle2.md](./_docs/archive/tasks.phase-1-and-dashboard-cycle2.md)

---

## Phase 2-simple — Financial Dashboard MVP

### Cycle 2 — Variant prototypes — DONE

> Variant C (TradingView-style chart) adopted; chart panel/header/legend/toolbar + `useTradingViewChart.hook` + indicator core + intraday intervals built; old grid purged. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-1-and-dashboard-cycle2.md](./_docs/archive/tasks.phase-1-and-dashboard-cycle2.md)

### Cycle 2/3 + Group E — DONE (dashboard mock→real COMPLETE)

> Chart re-slim + Cycle 3 snapshot/history actions + per-category polling + backend flip + Group E gate all done; `/dashboard` fully backend-sourced. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.dashboard-cycle3.md](./_docs/archive/tasks.dashboard-cycle3.md)

### Cycle 4 — Verification

- [ ] PM verifies Group E against live `/dashboard`; decides Cycle 5 scope

---

## Phase 2-mobile — Mobile context UX (chip header + in-article TOC) — SHIPPED (MOB.0–MOB.3)

> Two additive client-side-only mobile (`<lg`) UX changes — (D) top-of-article collapsible TOC, (B) top-of-feed context header (author chip + chip row) — replacing the heavier `<lg` UserContextSheet; Phase 2-filter later SUPERSEDED the NAV chip row (`MobileChipRow`/`getMobileChips` `x_`-retired). Decision #33. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-2-mobile.md](./_docs/archive/tasks.phase-2-mobile.md)

---

## Phase 2-filter — Multi-category + multi-tag feed filter (desktop rails + mobile rows) — DONE (SHIPPED 2026-06-29)

> Reader narrows the author feed `/blog/[username]` by categories AND/OR tags at all breakpoints; URL state `?categories=a,b&tags=a,b`, same-type OR / cross-type AND, no migration; desktop rails (`mode:'filter'`) + mobile FilterChipRows + AppliedFilters bar. SUPERSEDES Phase 2-tags. Decision #34. Gate: jest 25 suites/166. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-2-filter.md](./_docs/archive/tasks.phase-2-filter.md)

---

## Phase 2-fetch — Feed data layer: axios→native fetch + tag revalidation — DONE (SHIPPED 2026-06-29)

> Blog feed read moved off client axios onto a server-only native-fetch wrapper (Next 16 tag revalidation → instant read-your-writes on publish-class mutations); Express CDN backstop lowered. F-fetch.1 (admin-delete revalidate) DONE; F-fetch.2 (axios retirement) parked. Decision #35. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-2-fetch.md](./_docs/archive/tasks.phase-2-fetch.md)

### F-fetch.2 — client axios retirement (`dashboard_client_web`) — PLANNED (architect design 2026-07-07)

> Migrate all 26 remaining `.api.ts` axios fetchers + local `apis/axios_instance.ts` to native fetch; drop npm `axios` from `dashboard_client_web` ONLY (`finance_web` axios OUT OF SCOPE; `dashboard_server` has none). Design + ground-truth findings (2 isomorphic / 6 server-only / 18 client-only; **`@repo/axios` does not exist**; dominant risk = envelope double-unwrap) = `00.plan.md` Phase 2-fetch follow-up.
>
> **⛔ GATE:** do NOT start until the in-progress backtest working-tree changes (8 files) are committed. Each stage below is independently shippable + typecheck-green.

- [ ] **FX.0 (ENG)** Core scaffolding — NEW `apis/http.core.ts` (pure/isomorphic: buildHeaders, composeSignal(5s), encodeBody(FormData passthrough), buildQuery, parseResponse→toFetchApiError) + NEW `apis/http.client.ts` (`clientRequest<T>`, no cache opts, token per-call) + refactor `apis/http.server.ts` to delegate to core (`serverRequest` signature unchanged; getPriceSeries/getFeed untouched; Next tags/revalidate kept) + `ApiError` +`body?: unknown`. **Acceptance:** no behavior change; check-types 0; existing serverRequest consumers unchanged.
- [ ] **FX.1 (ENG)** 10 simple client leaf fetchers (favorites/likes/comments/drafts/adminPosts) → `clientRequest`; drop per-file try/catch+toApiError boilerplate. **Acceptance:** each unwraps its envelope exactly once vs real Express shape; check-types 0.
- [ ] **FX.2 (ENG)** 4–6 special client fetchers — uploadImage (FormData), getMe (raw body), createPost/updatePost/getPostById/getSavedPosts (envelope). **Acceptance:** FormData passthrough (no manual Content-Type); each unwraps exactly once; check-types 0.
- [ ] **FX.3 (ENG)** 2 error-class fetchers — postUsername, createCategory: `axios.isAxiosError` → `e instanceof ApiError`; createCategory 409 branch reads `ApiError.body`. **Acceptance:** 409 conflict behavior identical; check-types 0.
- [ ] **FX.4 (ENG)** 6 server-only fetchers (getPost, getPosts, getUserProfile, getUserPopularTags, getPostsByTag, getPopularTags) → `serverRequest`; fix getPosts double `.data` unwrap. **Acceptance:** each unwraps exactly once; feed/detail render unchanged; check-types 0.
- [ ] **FX.5 (ENG)** 2 isomorphic fetchers (getCategories, getComments) → `clientRequest`; fix getCategories latent broken `response.status !== 200` check. **Acceptance:** both server + client call sites green; check-types 0.
- [ ] **FX.6 (ENG)** Dependency removal — soft-delete `apis/axios_instance.ts` → `x_axios_instance.ts`; remove dead `toApiError` + axios import from `utils/apiError.util.ts`; drop `axios` from `dashboard_client_web` package.json + lockfile update. **Acceptance:** zero `axios` imports remain in `dashboard_client_web` (`finance_web` untouched); lint 0 · check-types 0 · build green.

---

## Phase A — Deploy: Vercel + Supabase (Read-Only, Free) — acceptance gate MET (2026-06-26)

> Backend on Vercel via `pg` Pool → Supabase (S1–S5 done; SSL `sslmode` strip + self-contained tsconfig deploy fixes); blog + mock dashboard live on `my.prettylog.com`; CI typecheck. Full S1–S9 runbook archived. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-A.md](./_docs/archive/tasks.phase-A.md)
> **Open tail:** S4-H.2 (pg pool `max:5` commit/verify) · S6.1 (repo under individual acct) / S6.3 (optional backend domain) · S7.2 (lint `--max-warnings 0` CI gate; ~207 client warnings + blog_agent ruff/pyenv) · S8.4 (Cloudflare `CDN_ASSETS` CNAME) · S9.1–S9.3 (AWS dep/env retirement).

---

## blog_agent merge (apps/blog_agent) — DONE

> Phase A lift-and-shift (f51cc7e) + Phase B BA-09 Supabase/Cloudflare repoint (`DB_PG_*`→`POSTGRES_*`, boto3→supabase-py, first real publish live). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.blog-agent-merge.md](./_docs/archive/tasks.blog-agent-merge.md)

---

## Phase B — Dashboard backend + Supabase Storage repoint — backend + publish DONE (84b9d32)

> Backend B0–B3 (migration `0004` market tables + dashboard read endpoints + seed) DONE; blog_agent publish (B5/BA-09) WORKING end-to-end (first real publish live). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-B.md](./_docs/archive/tasks.phase-B.md)
> **Open:**
>
> - [x] **B4 (ENG)** Client Cycle 3 action layer reads B2 endpoints when `DASHBOARD_DATA_SOURCE=backend`, else mock — **DONE (2026-06-29)**: snapshot live + USER flag flipped + chart history wired (`useDashboardHistory` `'1d'` lazy per-label; mock `history` prop removed). `/dashboard` fully backend-sourced.
> - [ ] **B5.6 (ENG)** Re-publish with `ollama serve` up so tags/categories/description populate.
> - [ ] **B5.7 (USER)** Set blog_agent local `.env` `DEFAULT_USER_ID`→`1` (non-blocking).
> - [ ] **(USER)** market-data provider + KRX `.KS/.KQ` suffix decision (Phase I / Decisions #32). _(`DASHBOARD_DATA_SOURCE=backend` flip DONE 2026-06-29. Cloudflare `CDN_ASSETS` CNAME = S8.4 branded-domain-only — parked, see go-live NOTE.)_
> - [ ] **S8.2 / S9.1–S9.3** AWS retirement (soft-delete `awsS3.ts`/SSH/RDS CA, remove AWS deps, drop Vercel AWS env). _(B5.5 client cursor precision — CODE DONE; runtime verify only, awaits >10 posts.)_

---

## Phase C — planned

**Phase C (after B):** env Zod build-gate; hook/component LOC splits; server service layer; dead-dep
reconcile; ~~`@repo/axios` vs local axios~~ (RESOLVED 2026-07-07: `@repo/axios` does not exist — axios retirement promoted to Phase 2-fetch **F-fetch.2**); Phase 4 pnpm checkbox cleanup; `.agent` rule consolidation.

---

## Phase D — Multi-User Blog Platform (Clerk Auth) — ENG COMPLETE

> Express-layer Clerk `verifyToken` over pg Pool (RLS bypassed → owner-scoping is the gate); schema `0003`; lazy provisioning + `/onboarding` (8ca524e); D2 authoring + autosave (dc28f19) + UX cycle + i18n + thumbnail persist (31eb14a); D4 admin soft-delete moderation (70375e6). Open-signup default; webhook retired; `/admin/invites` DROPPED. Phase 4 pnpm migration DONE (150d460). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-D.md](./_docs/archive/tasks.phase-D.md)
> **Open:**
>
> - [x] **D1.1 (USER) — DONE (2026-06-29)** Clerk Production (`pk_live`/`sk_live`) env on BOTH Vercel projects + `ALLOWED_ORIGINS` on backend + dashboard public-sign-up ON.
> - [ ] **D1.2 (USER, optional)** Register Clerk as a Supabase TPA provider.
> - [ ] **D2.6a (USER)** Backend Vercel `SUPABASE_URL` + `SUPABASE_SECRET_KEY` (else image upload → 503).
> - [ ] **D3.4 (ENG)** `/settings/profile` + `PATCH /v1/api/users/me` (residual, non-blocking).
> - [ ] **D4.0b (USER)** Admin bootstrap SQL `UPDATE users SET is_admin=true WHERE username='<owner>'` (match by username/clerk_user_id, never email).
> - [ ] **D4.1f (USER)** Live admin verify (blocked on D4.0b + Clerk env).

---

## Phase E — Blog Engagement (Saved Posts · Likes · Comments) — ENG COMPLETE + pushed `7b2ea0a`

> Saved Posts (`/me/saved`, migration `0007`), Likes (`0008` `post_likes`, optimistic detail+feed buttons), Comments (`0009` Option-C `BIGINT[]` materialized path, unlimited depth, `[deleted]` tombstone). 3 soft-delete Rule-1 gaps fixed. Issue #1 = PARALLEL (blog_agent comment columns untouched). All purely additive. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-E.md](./_docs/archive/tasks.phase-E.md)
> **Open:** USER apply-migrations gate — `supabase db push` `0007`+`0008`+`0009`, then re-run server jest for the DB-gated saved/likes/comments assertions.

---

## Phase E2 — Comment Edit + Comment Pagination — DONE/SHIPPED (origin/main HEAD `85093b4`)

> Closed Phase E's two deferrals: comment EDIT (`PATCH /comments/:id`, auth==delete via shared `assertCommentMutable`, tombstone→409, "(edited)" badge) + keyset pagination (50 roots/page each with full subtree, STRING cursor, `useInfiniteQuery`). `comments.ts` split into a barrel (C1). Purely additive — NO schema/index/migration. No open USER item (`0007`/`0008`/`0009` APPLIED; jest 78/78). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-E2.md](./_docs/archive/tasks.phase-E2.md)

---

## Phase F — Blog Image Upload Rework (Express proxy + 2-step resize) — DONE (`2a0457b`)

> `POST /v1/api/posts/images` (multer memoryStorage + sharp `rotate().resize(1600,inside).webp(q80)`, server-to-server upload, full error mapping); client step-1 native-Canvas resize (no library); `vercel.json includeFiles` ships sharp libvips; old signed-URL path soft-deleted `x_*`. Decision #27. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-F.md](./_docs/archive/tasks.phase-F.md)
> **Open:**
>
> - [ ] **F17 (ENG/USER, manual e2e)** With backend `SUPABASE_*` set: normal JPEG → working `publicUrl`; EXIF-rotated correct; >5MB succeeds; non-image `.jpg`→415; creds unset→503. (Precondition: D2.6a.) Plus runtime confirm sharp libvips loads on Vercel.

---

## Phase F2 — blog_agent webp compression (local Python CLI) — DONE (`3e5b04c`)

> `apps/blog_agent/utils/image_transform.py` `to_webp` (Pillow LANCZOS + `exif_transpose`, 1600 fit-inside, q80, metadata stripped, `NotAnImageError`); body+thumbnail upload boundary forces webp; thumbnails keep 1200×630 letterbox (Option A); `_save_file_records` ext-from-stored_name fix; path scheme unchanged. Decision #28. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-F2.md](./_docs/archive/tasks.phase-F2.md)

---

## Phase G — Editor Category Selector + Tags (write/edit screen) — DONE (`bced84e`)

> Cascading 3-box category picker + flat tag-chip editor on `/write`; migration `0010` → category uniqueness `(user_id, parent_id, title)`; tri-state `category_id` PATCH (CASE); by-id read now returns `tags` + `stored_uri`; new `POST /v1/api/categories` (parent-scoped dedupe, soft-delete revive); AC1–AC10 met. Decision #29. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-G.md](./_docs/archive/tasks.phase-G.md)
> **Open (USER):** `supabase db push` migration `0010_category_unique_parent.sql`.

---

## Phase H — Multi-user Reddit-style Blog (global feed + context rails) — DONE P0–P5 (`2c0f897` / `f83a38e`)

> 3-col `BlogShell` route group (`app/(feed)`, cookie-collapse first-paint); `/tags/[tag]` global tag feed; global TopBar (SearchBar tag-nav + CreateButton); identical global LEFT rail (PrimaryNav + global TagCloud + mobile focus-trap); per-user context RIGHT rail (category tree + scoped tags + `UserProfileCard` from `GET /posts/users/:username/profile`) with `<lg` sheet; localStorage recents(20)/saved-tags(50); 4 viewer-agnostic tag-read APIs (slug-dedup-before-keyset, STRING cursors). Decision #30. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-H.md](./_docs/archive/tasks.phase-H.md)
> **Open:**
> **NOTE (not an open task):** P6 real search/discovery (`GET /v1/api/posts/search` title+tag ILIKE + keyset; `/explore` surface; H6.1/H6.2/H6.3) stays parked. A **disabled `/explore` PrimaryNav placeholder** ("coming soon", non-clickable, Compass icon) is now live (`useNavLinks.hook.ts` `disabled:true` + `nav.explore` both locales) so the trailing nav slot reads intentional. Flip `disabled` off + add the route when P6 is actually picked up.
>
> - [ ] **HX.1 (architect)** decide client `TPost` re-derive from `@repo/types` `TFeedPost` vs parallel.
>       _(HX.2 split `posts.ts`→6 modules: DONE `db00d42`.)_

---

## Phase I — Stock Collector 편입 + Yahoo OHLC 적재 + 시총 랭킹 — DONE / LIVE (2026-06-29)

> Daily OHLC + snapshots · weekly rankings + sticky universe · Parquet archive; migrations 0011/0012/0013 applied; weekly ranking first live run done (505 candidates → 1,007 rankings / 110 tracked). Open USER: CI-cron scheduled-fire verify + KOSPI/KOSDAQ `.KS/.KQ` coverage (Decision #32). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-I.md](./_docs/archive/tasks.phase-I.md)

---

## Phase J — SEC 13F Holdings (monthly batch) — DONE (ENG J1–J11) (2026-06-30)

> SEC EDGAR 13F-HR monthly batch added to `apps/stock_collector` (registry→fetch→parse→normalize→aggregate→upsert+supersede; migration 0017 `sec_filings`+`sec_holdings`; raw-XML archive default OFF; `collector-monthly` cron `0 7 1 * *`). 0017 APPLIED + first live run DONE (sec_filings=6, sec_holdings=16,543). Decision record: plan.md Decisions Log #36. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-J.md](./_docs/archive/tasks.phase-J.md)
> RESIDUAL (open): verify GH Actions `SEC_USER_AGENT`/DB secrets + first SCHEDULED monthly cron fire (1st-of-month) — local live run proven; CI-trigger unverified.

## Phase K — `/funds` 13F Holdings Viewer (read-only) — DONE (2026-06-30)

> Read-only `/funds` viewer over the Phase J store (Express `GET /api/funds`[`/:cik`] with `to_regclass` guard + SQL `SUM() OVER()` weight; Next `/funds`+`/funds/[cik]` force-dynamic + `/funds/preview` fixture). K11 SATISFIED: 0017 applied + first collector run → `/funds` shows live data (sec_holdings=16,543). Decision record: plan.md Decisions Log #37. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-K.md](./_docs/archive/tasks.phase-K.md)

## Phase L — `/funds/[cik]` 13F Rank-Flow (분기별 시각화, backfill 2년) — ENG DONE (active scope) / L-next DEFERRED (2026-06-30)

> rank-flow(컬럼=분기 / 행=비중랭킹 / cusip 고정색 / 클릭 시계열 추적) on `/funds/[cik]`. ENG active scope(L1–L11, L15–L20) DONE + gates GREEN. 제품의도 = `00.plan.md` Phase L + Decisions #38. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-L.md](./_docs/archive/tasks.phase-L.md)

### Slice D — L-next (남은 항목)

- [ ] **L12 (ENG) — DEFERRED** 마이그레이션 `0018_sec_analytics.sql` 분석 뷰 4종(`v_sec_position_delta`/`v_sec_exits`/`v_sec_fund_period_summary`/`v_sec_consensus`; `deleted_at IS NULL`·`put_call=''`·window SUM weight) + **0017 테이블 + 0018 뷰를 BOTH `tradelunch.schema.sql` SSOT 미러**(현재 0017이 SSOT 부재 → 보강). **Acceptance:** 0017 DB에 idempotent 적용; backfill된 펀드에 행 반환; `pnpm check:schema` in sync.
- [ ] **L21 (ENG) — → Phase P 0-b(P1–P6)로 승격 (2026-07-01)** SEC 13F **sector enrichment** (rank-flow sector 컬럼 + 섹터별 색 그라데이션 + 상단 sector legend의 전제). 갭 = **CUSIP→ticker** (13F는 cusip만·`ticker` NULL); **ticker→sector는 이미 있음**(Yahoo `.info`, `symbol_fundamentals` 분기 캐시). 신설 `security_map`(cusip PK, ticker, sector, name, source, refreshed_at, deleted_at) + enrichment 잡(distinct cusip → OpenFIGI 무료 CUSIP→ticker → Yahoo/`symbol_fundamentals` ticker→sector → upsert). **Acceptance:** 채워지면 sector 컬럼·색 그라데이션·legend 3종 활성.
- [ ] **L22 (ENG) — DEFERRED (NOTE-ONLY, do not build yet)** 다중-CIK 펀드 정체성 매핑 — 일부 필러는 13F를 시간에 따라 **다른 CIK로** 제출(예: BlackRock = `0001086364`[~2016] / `0001364742`[2023Q4–2024Q2] / `0002012383`[2025Q2~], 2024Q3–2025Q1 공백). 연속 이력을 원하면: `funds.yaml`에 `{slug,label,ciks:[…]}` + `cik→slug` 매핑 + `/funds/[slug]` 라우트 + rankflow 엔드포인트가 slug의 CIK들을 **period별 union**(홀딩은 실제 CIK로 저장 — source-native 유지, 위조 안 함). 매핑해도 공백(어느 CIK도 미제출 분기)은 못 메움. 현재는 단일 최신-CIK로 운영(YAGNI; 오너 결정 2026-07-01). **Acceptance:** N/A — 실제 과거 이력 필요 시 착수. 오너 노트 요청으로 기록만.

## Phase M — market_history 5년 retention 티어링 (hot/cold prune) — ENG M1–M7 DONE / M8 ops + LIVE flip USER-gated (2026-06-30)

> `market_history`를 5년 HOT 윈도우로 한정; 전체는 Parquet COLD에만. 연 1회(12/30 cron) prune이 달력연도 경계 이전 봉을 파티션 존재 검증 후에만 삭제. 설계+소프트삭제 예외 정문 = `00.plan.md` Decisions #39. M1–M7(retention 코어+CI+seed inception) DONE + gates GREEN. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-M.md](./_docs/archive/tasks.phase-M.md)

- [ ] **M8 (ENG/USER, 운영)** 신규 tracked 티커 5년 hot 백필 1회(`run_daily --backfill-days 1825` 또는 `backfill_tracked` 헬퍼), ranking 추가와 연동. **Acceptance:** 샘플 신규 티커 ~5년 봉 존재; 다음 daily 재유입 0(커서 MAX 재개).

> **OUT OF SCOPE(후속 flag):** `market_rankings`(append-only)/`sec_holdings`(tombstone 누적)/`batch_log` 2차 retention. **prune LIVE는 M7(Parquet inception) + M6 dry-run 성공 후에만.**

## Phase N — 2차 retention: 로그 TTL + rankings 10년 아카이브 prune — ENG N1–N9 DONE / 첫 LIVE rankings prune USER-gated (2026-06-30)

> #39 OUT-OF-SCOPE 후속 실현. Phase M 머신 재사용: 로그(error_log/batch_log)=무-아카이브 일수 TTL 하드삭제(매일 LIVE); market_rankings=아카이브-검증-삭제 window 10년. 설계 = `00.plan.md` Decisions #40. N1–N9 전부 DONE + pytest 208 green(첫 LIVE rankings prune은 후보 0건이라 무해 no-op, ~2036까지 안전장치). <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-N.md](./_docs/archive/tasks.phase-N.md)

## Phase O — `/rankings` Marketcap Rankings 뷰어 + MARKETS nav + 주차 피커 — Phase 1 SHIPPED / Phase 2 DEFERRED (2026-06-30)

> 주간 market_rankings를 `/rankings`로 노출(funds 패턴) + MARKETS nav 드롭다운. 설계 = `00.plan.md` Phase O + Decisions #41. Phase 1(뷰어 O1–O5 + 주차 타임트래블) + Phase 1b(O10 회사명) SHIPPED. Phase 2(샘플링 rank-flow bump O6–O9)는 ~8 주간 스냅샷 누적까지 DEFERRED. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-O.md](./_docs/archive/tasks.phase-O.md)

### Phase 2 — 샘플링 rank-flow bump (DEFERRED, 게이트 ~8 주간 스냅샷)

- [x] **O6** 마이그레이션 **0032**(0019→충돌회피) `idx_market_rankings_scope_asof (scope, sector, as_of DESC, symbol, rank)` + SSOT 미러. USER-apply 대기(perf-only; 미적용이어도 flow 쿼리 동작). SHIPPED main.
- [x] **O7** `controllers/rankings/rankflow.ts`(199 LOC) `GET /rankings/flow?…&granularity=week|month|quarter|year&periods=&k=` — `DISTINCT ON date_trunc` 샘플링 + ever-top-K union. granularity 화이트리스트(injection 불가)·periods/k `$1/$2` 바인딩·data:null 가드. index.ts 라우트 등록(/flow가 / 뒤). SHIPPED main.
- [x] **O8** `getRankingsFlow.action`/`.schema` + `types/rankingsFlow.ts`(cell=`{rank, marketCap}`) + `utils/rankFlowRows` symbol-키 제너릭 헬퍼 추가(funds 함수 무변경) + `periodLabel`. SHIPPED main.
- [x] **O9** `RankingsFlowTable.client`(378 LOC, architect KEEP=단일책임) + `RankingsViewToggle.client`; `/rankings` `Snapshot|Flow` 토글, `MIN_WEEKS_FOR_FLOW`(8) 게이트, view=flow&&canFlow일 때만 SSR fetch. ≥8주 활성/미만 disabled+"N/M". SHIPPED main.

## Phase P — Investment Signals: 3축 통합 + 투자 후보 선정 — PLANNED (3-agent 설계 2026-07-01)

> 랭킹·13F·가격 3축을 조인해 "투자 후보 선정" 의사결정 제품으로. 블로커 = CUSIP→ticker(`security_map`). 제품의도+설계 = `00.plan.md` Phase P. ml-engineer: ML 시기상조 → 규칙기반 Phase 0부터.
> **Build order:** STEP 0(0-a re-backfill ‖ 0-b security_map) → STEP 1(신호) ‖ STEP 2(종목뷰) → STEP 3(스크리너) → STEP 4(검증/ML). **0-b 없이는 STEP 1/2/3 불가.**
> **Per-task gate:** collector = `uv run pytest`(해당 spec) · 순수 transform 부작용 0(psycopg는 `sink/`만) · 마이그레이션 `pnpm check:schema` in sync + BOTH SSOT 미러 · TS check-types 0 + lint 0 · 파일 ≤300 LOC · soft-delete 전용.

> **STATUS:** STEP 0-b security_map(P1–P6) + STEP 1 신호(P7 Δ배지/P8 consensus) + STEP 2 종목뷰(P9) + STEP 3 스크리너(P10 + momentum·lowVol·2티어정렬 Option C) 코드 전부 DONE (feature/phase-p-signals; 마이그 0019/0020 + fund_registry + 뷰 5종; server jest 52/52 target · client types/lint 0). USER 게이트: 0019·0020 apply + OpenFIGI 키 + `collector-security-map` dispatch → 실데이터 유입. 참조: "액티브 펀드"=Option B `fund_registry`. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-P.md](./_docs/archive/tasks.phase-P.md)
>
> **UPDATE 2026-07-05 (USER 게이트 실행 + 룩어헤드 가드):** USER가 마이그 0019/0020(+0031) apply + OpenFIGI 키 등록 + `collector-security-map` dispatch 완료 → **`security_map` 271행/250 ticker resolved(~92%)**, CUSIP→ticker 조인키 LIVE. `collector-13f-backfill`(since=2013) 병행 dispatch(성공 확인은 `gh run list`). **filing_date 룩어헤드 가드 구현(P 유일 미구현 코드 해소)** — 신규 마이그 **`0035_sec_consensus_asof.sql`**(뷰 체인 `v_sec_fund_period_public`→`v_sec_fund_latest_asof`→`v_sec_consensus_asof`: 펀드별 as-of 최신 접수분 = 룩어헤드 제외 + 엇갈린접수 언더카운트 해소) + `screen.ts` presence-guard 분기(0035 미적용시 레거시 무중단) + $N 파라미터 경로별 재바인딩 + as-of 뷰 SSOT 미러. jest **352 green**. **⚠남은 USER 게이트: 0035 apply** (후 수동 검증쿼리로 미래-filing 제외·staggered 카운트 확인). NB screen.ts 576 LOC(사전 부채 531에 +45; 전체 리팩터 별건).

### STEP 0 — 데이터 기반 (선행 필수)

- [ ] **P0a (USER)** 13F 5펀드 re-backfill — `gh workflow run collector-13f-backfill -f since=2013 -f archive=true`(cik 비움=6펀드 전체, idempotent). **Acceptance:** `batch_log job='collector-backfill'` 성공 행; 펀드당 다분기 sec_filings; amendment-정정 이후 원본 revive 확인.

### STEP 4 — 검증 → ML (DEFERRED until data)

- [ ] **P11 (ML) — DEFERRED** `rule.consensus_momentum.v001` paper-trading(SPY 대비 forward return 기록) → 6~12개월 축적 후 walk-forward + gradient boosting. **Acceptance:** N/A until 데이터 충분(현재 13F 6펀드×최신1분기·가격 ~1.1년). 리스크: 룩어헤드·패시브 오염·생존편향·13F 롱온리/45일지연/PRN NULL.

### 데이터 품질 — 비거래일 가짜 바 가드 (collector)

- [ ] **DQ2 (ENG) — DEFERRED (KRX)** KRX(KOSPI·KOSDAQ, 2심볼)·FX(4) 공휴일 가짜 바는 합의로 불가 → 실제 거래 캘린더 필요. 옵션: `exchange_calendars`(XKRX 음력 포함) 경량 도입 or KR 휴일 테이블. **Acceptance:** KR 공휴일 바 원천 차단. **Note:** 정상 운영선 yfinance가 거래일만 반환하므로 잔여 리스크 낮음(주말 필터로 최빈 벡터는 차단됨). 우선순위 낮음.

## Phase Q — US Politician Stock Disclosures (STOCK Act PTR) — MVP LIVE (Q1–Q5, Q5.3 검증 green) / Q6·Q7 후속 (2026-07-02)

> **kadoa-org/congress-trading-monitor(MIT) 미러링** → `politician_registry`/`politician_trades` → "traded by N politicians · 90d" 투명성 신호. 제품의도 + settled 결정 + OPEN ITEMS = `00.plan.md` Phase Q. MVP(Q1–Q5, 0022) + 확장(Q9–Q12/Q6.2–6.4 holder breakdown·프로필·타임라인, 0023 + backfill 57,736행) 전부 LIVE·검증 green. §13107: 무료·뉴스성 = news 예외. <!-- ARCHIVE: history-only -->
> Detail: [\_docs/archive/tasks.phase-Q.md](./_docs/archive/tasks.phase-Q.md)

### Q3 — Ingest (kadoa PTR mirror) — Q3.1–Q3.5 DONE (mirror LIVE)

- [ ] **Q3.6 (ENG) — 선택/DEFERRED (저우선)** 월 1회 best-effort Parquet DR 스냅샷 — kadoa SLA 부재 대비. 신규 `archive_politician_trades.py`(전체 테이블 read → `politician_trades/{YYYY-MM}.parquet` 덤프, 롤링 3–6개월 보관, best-effort always exit 0; Phase N `archive_rankings` 패턴 재사용) + `collector-politician-trades.yml`에 월간 스텝(또는 별 워크플로). **보존 = 전체 hot·prune 없음**(데이터 작음). **Acceptance:** 스냅샷 실패해도 수집/서빙 무영향; 데이터 재취득 가능성상 필수 아님(MVP 후).

### Q6 — 의원 드릴다운 (enrich, OPEN ITEM (1) 게이트)

- [ ] **Q6.1 (ENG)** `/politicians/[filer_id]` — **aggregate-first**(의원별 traded 자산·방향·밴드 버킷·freshness) + raw per-trade 테이블(공공기록·§13107 news 프레이밍 하 저위험). **비주식 포함 필수**: 거래의 53%가 채권(GS/muni/corp)·옵션·기타 — ticker 없이 `asset_name` + `asset_type` 라벨로 표시(가격 파이프라인 불필요; 이미 politician_trades에 저장됨). **Acceptance:** aggregate 요약 + raw 표(주식+비주식 전부) 렌더; 상시 disclaimer/coverage 각주 포함.

---

## Phase R–W — 시그널 검증 하네스 + 파생(옵션/GEX) 통합

> **Build order (system-architect 검증 2026-07-03): U → R → S → T → W → V.** 전부 free-tier + provider 교체가능. 마이그레이션 0026(R)/0027(U)/0028(V) — apply 직전 next-free 재확인(multi-session), 항상 `tradelunch.schema.sql` SSOT 미러. 순수 transform은 clock/DB 미접근(값 주입); I/O는 sink 경계만.

### Phase U — 13F 옵션노출 복원 (제로코스트 quick-win, 신규 수집 0 — 최우선)

- [x] **U.1 (ENG) — DONE 2026-07-03** 마이그레이션 **`0027_sec_derivatives_exposure.sql`**(view-only) — 뷰 `v_sec_derivatives_exposure` = `v_sec_positions`의 여집합(`put_call IN ('PUT','CALL')` AND `deleted_at IS NULL`, accession 합산 per `(cik, period_of_report, cusip)`, call/put 레그 분리 FILTER) + `tradelunch.schema.sql` SSOT 미러. prod 검증: CALL 358·PUT 126행 존재, top=BlackRock SPY 옵션 $25.7B CALL. **(USER: 수동 apply 0027.)**
- [x] **U.2 (ENG) — DONE 2026-07-03** byTicker "Axis 7" presence-guarded 편입 — 신규 `helpers/secDerivatives.ts`(CUSIP→ticker via security_map, 최신 period call/put 집계 + netSkew) + `to_regclass('public.v_sec_derivatives_exposure')` probe + DTO `secDerivatives`(부재→null, 500 아님) + 프론트 `symbolSecDerivativesSchema`(`.nullable().optional()`) + `types/symbolDetail.ts`(AssertEqual 통과) + 심볼 페이지 "Options Exposure (13F)" 카드(gamma 아님 명시). 게이트: server check-types 0·jest 320/320(신규 5)·client check-types 0·lint 0. byTicker 300 LOC 유지.

### Phase R — 이벤트 스터디/전방수익률 검증 하네스 (★나머지 전부의 게이트)

- [x] **R.1 (ENG)** 순수 `transform/event_study.py` — `event_window_start(filing_date)` + `cumulative_abnormal_return`(t+1/5/21, 섹터중립 옵션 benchmark) + `directional_hit`; **look-ahead 게이트 = 순수함수 `window_start = filing_date + 1 거래일`**(clock/DB 미접근). 23 pytest(정렬·CAR·동률·결측). 170 LOC.
- [x] **R.2 (ENG)** 마이그레이션 **`0028_signal_backtest.sql`**(0026→0028; 0027이 U로 이미 apply됨 → strictly-after) — observation-level 테이블 `signal_backtest(signal_type, ticker, as_of, horizon_days` PK, `car, is_hit, created_at/updated_at/deleted_at)` + roll-up **뷰** `v_signal_backtest_summary`(mean_car·hit_rate·n·t_stat 쿼리타임 집계 — 원자료 auditable 유지). upsert-idempotent(`ON CONFLICT DO UPDATE`) + SSOT 미러. **USER 수동 apply 대기.**
- [x] **R.3 (ENG)** forward-return 읽기 = 신규 `db_sink.read_returns_window(...)`(sink 경계, psycopg만) — `event_study.py`가 절대 `market_history` 직접 read 안 함(값 주입).
- [x] **R.4 (ENG)** `entrypoints/run_signal_backtest.py` — politician disclosure + 13F new-position 이벤트 → CAR 집계 → `signal_backtest` upsert; batch_log; `--dry-run/--limit/--since`. **신규 수집 0.** Acceptance: dry-run write 0·재실행 idempotent·≤300 LOC 충족. 검증(실 DB dry-run): politician 축 189 관측치 계산; 13F 조인 버그(accession→cik+period, 뷰에 accession 없음) 수정. 게이트: pytest 418·ruff clean.
- [x] **R.5 (분석)** t-stat/hit-rate 리뷰 (sector-neutral). **결론: 13f_new_position 21d t=3.15**(mean +1.88%·hit 57.4%·n=244)만 t>3 통과 = 유일 검증 시그널(Copycat/Best-Ideas). politician_buy raw t=5.80이 sector-neutral 0.71로 붕괴(=market beta, Ziobrowski는 pre-STOCK-Act). politician_sell 21d t=−2.64(약한 하방, 미달). ⚠ sector-neutral이 필수(McLean-Pontiff). Phase W 입력.
- [x] **R.6 (ENG)** 13F 축 활성화 — 마이그레이션 **`0029_sec_new_positions_mv.sql`**(materialized view `mv_sec_new_positions`: is_new+ticker+filing_date 사전계산, WITH NO DATA + unique/filing 인덱스) → db_sink 13F 쿼리를 빠른 인덱스 스캔으로 교체 + `refresh_new_positions`(600s timeout) + `run_signal_backtest --refresh-13f`. presence-guard(부재→politician만). **적용됨(db push up to 30).** 실측정 진행중(MV refresh 후 13F t-stat → 블로그/status 반영).

### Phase S — 섹터-상대 초과수익 정규화 (R의 return metric)

- [x] **S.1 (ENG)** 순수 `transform/sector_benchmark.py`(126 LOC, 시총가중 섹터 인덱스: t0 고정가중 wᵢ=sharesᵢ×closeᵢ,t0/Σ, 날짜별 결측멤버 재정규화) + `db_sink.read_sector_members`/`read_symbol_sector`. R의 `cumulative_abnormal_return(benchmark_series=)`에 positional 정렬로 꽂힘. 신규테이블 없음(in-harness). 18 신규 pytest. 게이트: pytest 436·ruff clean.
- [x] **S.2 (ENG)** `run_signal_backtest --sector-neutral` 플래그(기본 off, 기존 raw-CAR 유지) + `_resolve_benchmark`(섹터 미상/인덱스 불가 시 raw fallback, never crash).

### Phase T — 정치인 공시→가격 lead-lag (R 위)

- [x] **T.1 (ENG)** 순수 `transform/lead_lag.py`(156 LOC: `lagged_cross_correlation`·`optimal_lag`·`build_aligned_series`, 상수/short/empty→0.0) + `entrypoints/run_lead_lag.py`(256 LOC, `--signal-type/--max-lag`, read_signal_events/read_returns_window 재사용, 분석전용 no-write). 22 신규 pytest. R 하네스 재사용. 게이트: pytest·ruff clean.

### Phase W — 스코어 가중치 캘리브레이션 (R·S·T 전제)

- [x] **W.1 (ENG)** 스코어 캘리브레이션 — **architect+ml-engineer 합동판정: 가중치 동결(발명 금지)**. 단일-이벤트 t값(13f*new_position 21d t=3.15)은 *합성 스코어\_ 가중치 출처가 될 수 없음(t는 존재 허들이지 계수 아님; n=244·단일 강세장·sector-neutral≠full-FF·McLean-Pontiff 감쇠). → computeScore 4항 가중치(0.4/0.3/0.2/0.1) **byte-불변**. 대신: (1) 13F 신규포지션 breadth를 `newPositionBreadth` 진단 컴포넌트로 노출(가중합 미포함) + 동점 tiebreak(순서 방향만, t=3.15 정당화); (2) 정치 렌즈 **advisory-only 강등**(buy t=0.71·sell t=−2.64, `advisoryOnly:true`, 랭킹 미투입); (3) `mv_sec_new_positions`(0031) presence-gated 조인(부재→null degrade). 게이트: tsc 0·jest 342 green(+22). SHIPPED main.
- [ ] **W.2 (ENG, 후속)** 합성 랭킹 백테스트 harness — computeScore의 cross-sectional IC / decile long-short spread를 held-out 기간에 측정, new-position 항 with/without 사전등록 실험. 통과 시에만(정규화·shrink 패널 fit) W_NEWPOS 도입. 그 전까지 가중치 동결.

### Phase V — 옵션체인 수집 + GEX "wag the dog" (display-only 우선, R gate 안 함)

- [x] **V.0 (ENG)** provider seam = `sink/chain_provider.py::fetch_chain(provider, ticker)→list[ChainRow]`, 문자열-키 dispatch(`PROVIDER_YFINANCE`), ABC/DI 없음(KISS). 네트워크 격리. (source/ 디렉토리 부재 → 기존 sink/ 관례 따름.)
- [x] **V.1 (ENG)** 정규화 `ChainRow` + `GexDailyRow` dataclass = `schema/chain_rows.py`(55 LOC). 정규화 계약 테스트 포함.
- [x] **V.2 (ENG)** 마이그레이션 **`0030_gex_daily.sql`**(0028→0030; 0028/0029 선점) — 파생 스칼라 테이블 `gex_daily(as_of, ticker` PK, `net_gex, call_gex, put_gex, spot, source, …)` + SSOT 미러. raw 체인 미저장. **적용됨(db push up to 30).**
- [x] **V.3 (ENG)** 순수 `transform/gamma.py`(138 LOC: Black-Scholes γ=φ(d1)/(S·σ·√T), net GEX=Σγ·OI·100·spot²·0.01, 콜+/풋− dealer-centric, T→0/iv→0 가드; `list[ChainRow]`만 소비) + `sink/gex_sink.py`(132 LOC, chunked executemany, `prepare_threshold=None`). 35 신규 pytest.
- [ ] **V.4 (ENG)** raw 체인 → Parquet 콜드 라이터. (미착수 — 파생 스칼라만 hot 저장으로 충분, YAGNI 재평가.)
- [x] **V.5 (ENG)** `entrypoints/run_gex.py`(183 LOC) + **`collector-options-daily.yml`** 워크플로 작성 완료 — 평일 22:00 UTC(US 마감 후), curated 유동성 세트(SPY/QQQ/IWM/DIA + 메가캡 12, GEX는 지수/메가캡 딜러감마만 유의미) 기본 + `workflow_dispatch` tickers/dry_run 오버라이드, security-map 패턴 미러(uv pinned SHA·failure notify). gex_daily 히스토리 누적 시작 → 후속 GEX 백테스트 전제. **USER: workflow_dispatch로 첫 실행/스케줄 확인.** SHIPPED main.
- [x] **V.6 (ENG)** 서빙 — 신규 `helpers/fetchGexDaily.ts`(72 LOC, fetchPoliticianHolders 선례) + `to_regclass('public.gex_daily')` probe + presence-guarded **Axis 7** DTO `gexDaily`(부재→null, 500 아님). `deleted_at IS NULL` read-mask(architect fix). 프론트 `.nullable().optional()` + `types/symbolDetail.ts` AssertEqual. byTicker 302 LOC(방침 내). **스코어 미편입**(히스토리 누적 후 후속 W 재실행에서만). 게이트: server tsc/jest 342·client tsc·lint 전부 green. SHIPPED main.
- [ ] **V.7 (DEFERRED — YAGNI)** `futures_curve(root, expiry, as_of → settle/open_interest/volume)` 베이시스/텀구조. V-next(옵션 검증 후 별도 phase가 명시 수집할 때).

## Phase SEO-2 — SEO coverage completion — SHIPPED (2026-07-05, `63eb56b`)

> Detail + acceptance criteria: [\_docs/archive/plan.seo-2.md](./_docs/archive/plan.seo-2.md) <!-- ARCHIVE: history-only -->

- [x] **SEO2.1** sitemap `/symbols/[ticker]` via `getRankings` top-1000 (UPPERCASE = page canonical 정합, casing-drift fix).
- [x] **SEO2.2** sitemap `/politicians/[filerId]` via NEW Express `GET /v1/api/politicians` (presence-guard) + `getPoliticians` action.
- [x] **SEO2.3** `/blog/[username]` per-author title/desc (`getUserProfile`, owner→/ canonical 보존).
- [x] **SEO2.4** dynamic OG (`next/og` `lib/og.tsx` renderOgCard) — blog/symbols/politicians/funds; blog 상세 og:image 중복 제거.
- [x] **SEO2.5** `/` home metadata.
- [x] **SEO2.6 (DROPPED)** hreflang — content single-language (next-intl UI-only); no alternates ship.

## Blog owner-private visibility + StatusBadge — SHIPPED (2026-07-05, `53196a8`)

- [x] **PV.1** detail: `getPostBySlug` forwards Clerk token; threaded through `PostContentCard` + `PostContentToc` (both — the TOC sibling would else 404→blank the page). `auth()` forces dynamic render → no private cache leak.
- [x] **PV.2** category sidebar `/users/:username/categories` (×2 queries): `optionalAuth` + `(status='public' OR user_id=$viewer)`; anon viewer=-1 keeps identical output.
- [x] **PV.3** `/me`: `listDrafts` widened `status IN ('draft','private')`.
- [x] **PV.4** 🔒 `StatusBadge` (Private/Draft) on feed card + detail header + `/me` list.
- [x] **PV.0 (fix)** React #418 date hydration on post cards → `utils/formatPostDate` (deterministic ISO slice, `16e2e18`).

## Phase X — Asset Investment Backtest (`/backtest`) — ENG DONE / MVP 완성·검증 (2026-07-05, uncommitted)

> 오너 2-결정 잠금(#42): (1) 데이터 선행 → 제대로 DRIP, (2) 예측 = 성장(CAGR+몬테카를로) + 인컴(Yield). 상세 = `00.plan.md` Phase X.

### Slice 0 — 데이터 기반 (선행 필수, USER apply 게이트)

- [x] **X.0 (ENG)** DB 실측 완료(2026-07-05) — daily 깊이 2023-01-03~2026-07-02(~3.5년/877봉/222라벨). QQQ·QQQM·QLD·TQQQ·SPY·SCHD + ^IXIC·^NDX 적재; **JEPQ·VOO·VOOG 미적재**; dividends/stock_splits 컬럼 부재. → X.1/X.2 범위 확정, 신규 심볼 백필=period=max(retention 5yr hot 내).
- [x] **X.1 (ENG)** watchlist에 **JEPQ / VOO / VOOG** 심볼 편입 완료(watchlist.yaml stocks; loader 카운트 42→45). `is_leveraged`는 DB 아닌 Slice-1 프론트 상수로 처리(TQQQ/QLD/SOXL) — DB 컬럼 불필요.
- [x] **X.2 (ENG)** 마이그레이션 **`0034_market_history_dividends_splits.sql`**(0033이 last→0034 next-free) — `dividends`/`stock_splits NUMERIC DEFAULT 0` additive + BOTH SSOT 미러(dashboard_server/blog_agent schema). ⚠ **apply는 USER 게이트**. NB: `check:schema`는 X.2 이전부터 blog_agent 미러 인덱스 drift로 이미 실패 상태(우리 변경은 양쪽 동일 적용, 별개 이슈).
- [x] **X.3 (ENG)** collector 트랜스폼 확장 완료 — `HistoryRow`(+2필드)/`ohlc.py`(NaN→0 가드)/`db_sink` INSERT+ON CONFLICT 확장. **추가**: `run_daily.py`에 `--symbols` 스코프 필터(순수 `filter_universe`) — 전체 유니버스 재적재 없이 타깃 백필(Phase M 크기 규율). pytest **506 통과**(신규 13).
- [x] **X.3b (USER 완료 2026-07-05)** 0034 apply + 백필 실행(universe=9|history=38574). 검증(x_backtest_verify.py): 9심볼 inception 이력(QQQ 1999·SPY 1993·JEPQ 2022-05), JEPQ 월배당 49행 캐던스 정상, TQQQ 분할 8건·QLD 6건 포착. 총수익/DRIP 실데이터 준비 완료. 백필 명령: `cd apps/stock_collector && uv run python -m collector.entrypoints.run_daily --full --symbols QQQ,QQQM,QLD,TQQQ,SPY,SCHD,JEPQ,VOO,VOOG`. `--full`(커서 우회, period=max)이라야 기존 6 ETF 과거 행에 div/split upsert됨(`--backfill-days`는 증분 커서라 과거 미갱신 → DRIP 무의미). 9심볼이 일시적 pre-2021 봉 보유(~수십MB, 12/30 prune 대상; 백테스트 히스토리 늘어 오히려 이득 → 그대로 둠 권장).

### Slice 1 — 페이지 + 엔진 (Slice 0 후)

- [x] **X.4 (ENG)** Express `GET /v1/api/dashboard/history/batch?labels=&from=&to=` 완료 — `seriesBatch.ts`(96 LOC), N종목 1왕복(`label = ANY($1::text[])`, ≤15 상한·ISO 검증), close/dividends/stock_splits 반환, 순수 `toSeriesMap`, dashboard.ts Cache-Control(s-maxage 12h/swr 7d) 미러. Express build 통과.
- [x] **X.5 (ENG)** Next 데이터경로 완료 — `apis/getPriceSeries.api.ts`(serverRequest, `tags:['backtest-series']` revalidate 24h, labels sort로 엣지캐시 키 정규화) + `.mock.api.ts`(LCG 결정적, JEPQ 월배당+분할 preview용) + `app/actions/getPriceSeries.action.ts`(ok/error 유니온, mock 폴백 없음).
- [x] **X.6 (ENG)** **순수 엔진** 완료 — `engine.ts`(214)·`metrics.ts`(97)·`projection.ts`(138)·`types/backtest.ts`(74). 분수주·분할 연속성·배당 2경로(cash/DRIP 자산별); CAGR·MDD·연율변동성·Sharpe·누적배당; CAGR 곡선 + **인라인 mulberry32 seeded PRNG + Box–Muller 몬테카를로 p10/p50/p90**(Math.random 금지·numpy 아님·시드 재현) + Yield 인컴. vitest **115 통과(신규 16, 필수 6케이스 전부)**, check-types 0.
- [x] **X.7 (ENG)** 페이지 완료 — `app/backtest/page.tsx`(서버, revalidate=86400, de-index) + `preview/page.tsx`(mock) + `BacktestClient.client.tsx`(오케스트레이터) + `useBacktest.hook.ts`(`useMemo(runBacktest)`) + `useBacktestUrl.hook.ts`(전 입력+seed URL-state 인코딩, seed 미존재시 1회 생성·persist→리로드 시 동일 팬).
- [x] **X.8 (ENG)** 컴포넌트 17종 완료(각 ≤300 LOC) — BudgetInput·AssetPicker(매수가능 universe.ts 상수·가용시작일)·WeightSliders(합계100·균등배분)·**DateRangePicker(1Y/3Y/5Y/Max + ^IXIC/^NDX 미니차트 클릭으로 시점 선택)**·DripToggle(자산별)·ResultChart(recharts 실적선+CAGR점선+몬테카를로 밴드)·MetricsPanel·DividendTable·IncomeProjection(Yield)·**LeverageWarning(TQQQ/QLD/SOXL)**+Disclaimer(en/ko 인라인). recharts 신규 설치.
- [x] **X.9 (ENG)** nav+de-index 완료 — `useNavLinks`에 `{title:'backtest',href:'/backtest'}`(MARKETS 드롭다운+모바일 자동반영) + `middleware.ts` isFinanceRoute에 `/backtest(.*)` + `robots.ts` FINANCE_DISALLOW에 `/backtest`.
- [x] **X.10 (검증 게이트 — PASS)** 실데이터 교차검증(외부 계산기 대신 독립 소스): QQQ 2023-01~2024-12 DRIP 총수익 = 엔진 **95.78%** vs Python DRIP 재구현 95.782% vs **yfinance Adj Close 95.788%** (차 0.006pp). `utils/backtest/gate.test.ts`(vitest 상시 게이트) + fixture(x_backtest_gate.py 생성). 최종: lint 0·check-types 0·vitest **117 통과**·Express/Next build 통과·전 파일 ≤300 LOC.

> ✅ **SHIPPED main `3d94675`** (2026-07-05). 진단 스크립트 x*backtest*{dbcheck,verify,gate}.py는 x\_ 소프트삭제/gitignore(커밋 제외).
> **MVP OUT-OF-SCOPE 후속(YAGNI):** 포트폴리오 저장·주기 리밸런싱·벤치마크 오버레이·whole-share·수수료/세금·미래 경로별 MDD·차트 peak 마커.
> **PM AC 대비 관찰된 편차(경미):** AssetPicker가 자산 추가/제거 시 자동 균등분배(AC3.3 "명시적 재조정"과 상이하나 수동 조정 가능·UX상 개선). 필요시 후속 조정.

### Phase X-ext — 오너 요청 후속 (XE.1–XE.6 ALL ENG DONE 2026-07-05, uncommitted batch)

- [x] **XE.3 (USER 완료 2026-07-05)** ^IXIC/^NDX `--full` 백필 → DateRangePicker 2023 바닥 제거. 검증: NASDAQ Composite 1971-02-05~(13,968봉)·NASDAQ 100 1985-10-01~. 명령: `run_daily --full --symbols "NASDAQ Composite,NASDAQ 100"`. (개별주 등 2023+ 자산과 섞으면 공통구간=짧은 쪽 — 정상.)
- [x] **XE.1 (DCA 적립식 — ENG DONE 2026-07-05)** 완료 — 신규 `dateAdd.ts`(addMonths 공용)·`invest.ts`(investCash 순수)·`contributions.ts`(스케줄+비거래일 스냅); `metrics.ts` +XIRR(bisection, 손계산 15.83% 검증)+Modified-Dietz flow-보정 수익률; `engine.ts` flowsByDate/기여주입/totalContributed/moneyWeightedReturn; `projection.ts` yield 분모 budget→capitalBase; types +ContributionPlan/totalContributed/moneyWeightedReturn(additive); UI ContributionInput + BacktestClient/useBacktestUrl(`dca=amt:freq`)/MetricsPanel(Total Invested·XIRR 스왑). back-compat: lump-only byte-identical, **X.10 게이트 유지**. vitest 132(신규 15)·check-types 0·lint 0.
- [x] **XE.3-UI (fix)** DateRangePicker 레퍼런스 fetch `from:'2023-01-01'`→`'1971-01-01'` — XE.3가 지수를 1971까지 백필했으나 이 하드코딩 때문에 UI 벽 잔존하던 것 수정(격리 1줄). 이제 피커 배경이 ^IXIC 1971~ 전 구간, minAllowedFrom이 자산별 클램프.
- [x] **XE.2 (배당 라우팅 — ENG DONE 2026-07-05)** 완료 — 신규 `dividends.ts`(159, **날짜별 2-phase**: ex-date 주식으로 divCash 선계산→반영, A↔B 순환 검증) → `engine.ts` 312→**291 LOC**(300 복귀). 타깃 미선택/무바/close≤0 → cash 폴백; 배당=소스귀속·주식=타깃가산·`DividendEvent.routedTo`. 타입 `Holding.dividendRoute` descriptor(레거시 `drip` 폴백 + URL `:1`/`:0` 디코드 back-compat). URL codec `LABEL:weight:same|cash|<label>`. UI `DividendRouteSelect.client.tsx`(DripToggle→x\_ 소프트삭제) + WeightSliders/AssetPicker(기본 cash)/DividendTable(routedTo 컬럼). **X.10 게이트 유지**(drip:true→same). vitest **186(신규 23)**·check-types 0·lint 0.
- [x] **XE.5 (몬테카를로 seed 제어 — ENG DONE 2026-07-05)** 완료 — 근본원인(진입마다 `Math.random()` 재롤) 수정: `DEFAULT_SEED=42` + **자동생성 effect 삭제** + `seedReady` 제거 + `setSeed` 액션. 신규 `SeedControl.client.tsx`(92, 숫자입력+프리셋칩 [1·42·2024·7·777]+🎲Randomize=유일 재생성) + BacktestClient 게이트 `seedReady&&budgetValid`→`budgetValid`. ResultChart에 몬테카를로 caveat 라벨(선택구간 μ/σ 체리피킹·레버리지 감쇠 미반영). vitest **153(신규 21, AC3 회귀 포함)**·check-types 0·lint 0. (미커밋)
- [x] **XE.4 (월별 통계표 — ENG DONE 2026-07-05)** 완료 — 신규 순수 `monthlyStats.ts`(120, `date.slice(0,7)` 문자열만·타임존 회피·단일패스 drawdown/누적) + `StatsTable.tsx`(125, 순수, 7컬럼[낙폭 포함]+DCA시 월기여/누적투입, 무배당월 `—`) + BacktestClient Chart↔Table 세그먼트 토글. `BacktestResult.flowsByDate?` additive 노출(engine 8줄, lump-sum시 undefined→byte-identical). AC 불변식(마지막 행=metrics 일치) 테스트 통과. vitest **163(신규 10)**·check-types 0·lint 0.

- [x] **XE.6 (⚠ CRITICAL prune 면제 — ENG DONE 2026-07-05)** 완료 — `retention.py`에 `BACKTEST_RETAIN_LABELS`(11라벨: 9 ETF + "NASDAQ Composite"/"NASDAQ 100") + `db_sink.read_prune_candidates(exempt_labels=)` 필터(fetchall 후 Python 제외, 기본 frozenset()로 back-compat) + `prune_history.py`가 그 상수 주입(로그 `exempt=11`). 실패모드=보존 과다(안전). 2026-12-30 딥 히스토리 자멸 방지. pytest **515(신규 9)**. NB: Python 필터(SQL NOT IN 아님)=mock cursor 테스트 결정성 위함, 연1회·소량이라 성능 무해.

> **착수 시퀀스 (PM advisor 재정렬 2026-07-05): XE.3✅·XE.1✅ → XE.5(seed 버그·즉시) → XE.4(월표) → XE.2(라우팅) → XE.6(prune 면제, 병렬 가능).** 근거: XE.1 착지로 "대형 엔진 diff 선착지" 근거 절반 소멸; XE.5는 활성 버그(재현성 훼손)+엔진 0줄이라 최우선, XE.2 WIP=0이니 오너 불만 2건(차트→XE.4·seed→XE.5) 먼저 해소. XE.4/XE.5는 엔진 0줄 순수후처리라 XE.2와 논리충돌 없음. XE.6은 collector-side라 프론트 XE와 완전 독립(병렬 가능).
>
> **PM advisor 지적 UX 갭(각 태스크 AC에 반영):** (a) 진행형 공개 — 입력이 예산·자산·가중치·기간·DRIP·DCA·라우팅·seed로 과밀 → 기본(예산+자산+기간)+고급 접이식(DCA·라우팅·seed); (b) 혼합자산 공통구간 클램프 안내(결과 헤더 "유효 구간 YYYY-MM-DD~, JEPQ 상장으로 단축"); (c) 몬테카를로 팬에 "선택구간 실적 기반·레버리지는 감쇠 미반영" 라벨; (d) 세금/수수료 미반영 disclaimer 확인. **선제 대비(후속 후보):** 벤치마크 오버레이(vs SPY, fetch 경로 존재)·월표 CSV·개별주 추가(XE.6 정책과 묶기)·URL 복사 버튼.

**OUT-OF-SCOPE (YAGNI, 후속):** 포트폴리오 저장(Clerk 연계)·DCA 월적립·~~주기 리밸런싱~~(→ Phase X2로 승격)·벤치마크 오버레이·whole-share+현금잔여·수수료/세금·5년 초과(Parquet cold).

---

## Phase X2 — Backtest v2: 리밸런싱 · 조건부 트리거 · 그룹 · 현금흐름 라우팅 — ENG DONE (Phase 1, 2026-07-06, 미커밋)

> 오너 결정 잠금(#43): 2-phase 분할 · 지향성 현금흐름 · sell-lock · sell priority · 그룹핑 · 조건부 트리거(bearTrough). 제품의도+설계 = `00.plan.md` Phase X2. **수용 불변식(전 슬라이스 공통): `rebalance` 부재 ⇒ Phase X 결과 byte-identical(X.10 게이트 유지).** 전 파일 ≤300 LOC(엔진 split 포함). 하네스=기존 vitest(`gate.test.ts` 선례).

> ✅ **ENG DONE (Phase 1 전량, 6-wave 아키텍트-검증 dispatch, 미커밋):** X2.1–X2.19 구현 완료. 게이트 GREEN: **vitest 296/296**(gate.test.ts byte-identical 유지)·tsc 0·lint 0·전 소스 ≤300 LOC(BacktestClient 303=응집 오케스트레이터). **아티팩트:** 엔진= types(188)+`targets.ts`(82)+`rebalance-schedule.ts`(60)+`rebalance.ts`(208)+`rebalance-apply.ts`(270)+`triggers.ts`(262, bearTrough 상태머신)+`initial-state.ts`(82)+`split-adjust.ts`(31)+`engine-output.ts`(217)+`engine.ts`(319→264)+`invest.ts`(DCA route)+`perHoldingValues` emit; codec= `url-codec.ts`(188)+`url-codec-rebalance.ts`(227)+`useBacktestUrl.hook.ts`(193, `rb=`/`mf=`/`assets=` trailing back-compat); UI= `BacktestControls`(194)+`RebalancePolicyPanel`(270)+`RebalanceTriggerEditor`(288, weight-ratio 1급/take-profit 고급)+`ManualFlowsEditor`(152)+`HoldingAdvancedControls`(107)+`MetricsPanel`(리밸런싱 요약·경고)+`StatsTable`(자산별 월 비중% 칼럼)+`monthlyStats.buildMonthlyAssetWeights`. 진행형 공개(고급 접이식 기본 닫힘)로 미조작 시 기존 UI/결과 동일. **버그 수정:** sell-lock take-profit warning 경로 누락(evaluateTriggers canSell 체크 위치) 수정. **USER 후속:** 커밋(현재 미커밋 배치)·수동 e2e(리밸런싱/트리거/manualFlow 시나리오). 하위 태스크 체크박스는 이 배너가 authoritative(전량 완료; X2-P2 합성 히스토리 = PLANNED #44, 아래 Phase 2 S1–S8).

### Slice 1 — 타입 + effective-target map + 엔진 배선

- [x] **X2.1 (ENG) — DONE (Wave0, 미커밋)** `types/backtest.ts`(→188 LOC) — `Holding` +`canSell?`/`sellPriority?`/`groupId?`/`groupWeightPct?`(전부 optional), `RebalanceTrigger` 판별유니온(driftBand/takeProfit/buyDip/weightCap/weightFloor), `ExtremaReset`, `AssetGroup{id,targetPct,rebalanceWithin?}`, `RebalancePolicy{freq,band,groups,triggers?}`, `AssetRunState`/`RebalanceState`, `BacktestInput.rebalance?`, `BacktestResult.rebalance?{events?,warnings?}`. 로직 0. **Acceptance:** check-types 0; 전 필드 optional→기존 호출부 무수정 컴파일.
- [ ] **X2.2 (ENG)** 순수 `utils/backtest/targets.ts` — `buildEffectiveTargets(holdings,policy)`→per-label 목표맵(그룹 targetPct×그룹내 sub-weight 전개, 합=1 정규화). **Acceptance:** vitest 그룹없음/전부그룹/혼합 합=1(±1e-9); orphan groupId·그룹내 합≠100→명시적 에러; 결정적.
- [x] **X2.3 (ENG) — DONE (Wave0, 미커밋)** engine.ts 319→**288 LOC**(initial-state.ts 82 + split-adjust.ts 31 추출) + rebalance.ts no-op 스텁(46) + `RebalanceState` threaded; vitest 194/194(gate 포함 byte-identical)·tsc 0·lint 0. `rebalanceIfDue(date,shares,cash,state,policy,holdings,dateIndexes)→cash`. ~~`engine.ts` **현재 319 LOC = 이미 cap 초과**~~(XE.1 flow/XIRR 코드가 291→319으로 되돌림; 플랜의 "291" 표기 stale) → **`initial-state.ts` 추출은 예방 아닌 필수**(initial-alloc 블록 라인 136–172: shares/initialAlloc init+lump-sum+contributionDates+xirrFlows seed). 이후 date-walk에 `rebalanceIfDue` **no-op 훅**(순서 dividends→DCA→rebalanceIfDue→snapshot) + `RebalanceState` walk 전 1회 할당·threaded. **Acceptance:** `engine.ts` ≤300 LOC; `rebalance` 부재⇒타임라인 byte-identical(X.10+vitest green). **이게 green 되기 전 어떤 slice도 fan-out 금지**(모든 엔진-터치 태스크의 전제).

### Slice 2 — drift-band 리밸런스 (sell-lock · priority · group)

- [ ] **X2.4 (ENG)** 순수 `utils/backtest/rebalance-schedule.ts` — `isRebalanceDue(freq,prevDate,barDate)`. **Acceptance:** vitest never/bar/월·분기·연 경계/첫bar 결정적.
- [ ] **X2.5 (ENG)** 순수 `utils/backtest/rebalance.ts` 액션부 — `applyTrim`/`applyBuy`/재분배: band 초과자산 대상, 가용현금 우선(강제매도 최소화); `canSell:false`=매도0+초과중량 freeze→잔여 renormalize→best-effort 부분 리밸런스; 트림시 `sellPriority` 오름차순만; 분수주·현금계정 일관(invest.ts 스타일 in-place). **Acceptance:** vitest (a)locked 매도0 전시나리오, (b)priority 위반0, (c)band 이내 무거래, (d)locked-초과 renormalize 결정적, (e)거래 전후 NAV 보존(수수료0).
- [ ] **X2.6 (ENG)** 엔진 결선 — trades 적용 + `BacktestResult.rebalance.events?[]`(date,trades,turnover) additive. **Acceptance:** `freq:'never'`⇒이벤트0·byte-identical; 필드 undefined여도 기존소비자 무영향.
- [ ] **X2.17a (ENG) — Q1 엔진부(아키텍트 재스코프)** 엔진이 **holding별 월말 가치 스냅샷**을 timeline에 emit(예 `timeline[i].holdings?:Record<label,value>` 또는 병렬 `perHoldingTimeline?`) — `flowsByDate`(engine 현 additive 패턴) 방식으로 optional·부재⇒byte-identical. **이유:** 현 엔진은 최종 `shares`+집계 `timeline`만 보유 → 자산별 비중%는 `shares×close` 월말값이 필요(price 비율≠비중, 리밸런싱/DCA시 특히). **Acceptance:** 필드 부재⇒byte-identical(zero-regression); 존재시 각 월말 Σholding가치=timeline 총가치(±반올림).

### Slice 3 — 지향성 현금흐름 (DCA 라우팅 신설 + 배당 라우팅 감사)

- [ ] **X2.7 (ENG)** `invest.ts`가 effective-target map 수신(기본 by-weight=현행, 현 `invest.ts:23` `amount*(h.weightPct/100)` 대체) + `ContributionPlan`에 route(`byWeight|{kind:'asset',target}`) — 오너 결정 #43(2). ⚠ **asset-route는 "전액 1라벨" 콜 경로**(가중맵 교체가 아니라 별도 분기 — 아키텍트 지적). **Acceptance:** byWeight시 기존 DCA identical; asset-target시 전액 해당자산 매수(vitest); 순수성 유지.
- [ ] **X2.8 (ENG, 감사)** 배당 크로스자산 라우팅 = XE.2로 엔진(`dividends.ts`)+UI(`DividendRouteSelect`)+URL(`:same|cash|<label>`) **기존 존재** → 신규 그룹/타깃맵/리밸런스와 bar순서 상호작용 검증만(재구현 금지). **Acceptance:** "JEPQ 배당→QLD" vitest 1건이 신규 rebalance와 공존 green; UI 수동확인 1회.

### Slice 4 — 조건부 트리거 (take-profit · buy-dip · weight-cap/floor · bearTrough)

- [ ] **X2.9 (ENG)** 순수 `utils/backtest/triggers.ts` — `advanceRunState()`(자산별 trailing 고점/저점 + **bearTrough 상태머신**: 고점 대비 −`bearThresholdPct`(기본20) 진입, 진입 후 min, 새고점 종료, 발동시 상향) + `evaluateTriggers()`→`FiredAction[]` 고정우선순위(weightCap→takeProfit→driftBand→buyDip→weightFloor, mutation 후 재평가). **순서 계약(아키텍트 must-fix):** `advanceRunState`는 **매 bar, evaluate 전** 실행; **결측바(해당 자산 바 없음)=상태 HOLD(리셋·전진 금지)** 로 결정성 보존; 트리거는 **post-DCA 스냅샷 NAV 기준** 평가(스케줄 drift-band과 동일 기준); 조건부 트리거는 `freq` 무관 매 bar. **Acceptance:** vitest 각 kind 경계 발동/미발동; bearTrough(고점→−20%진입→저점→2배회복 발동) 결정적; 첫바 미발동; 결측바 HOLD(리셋 안 함) 검증; whipsaw(발동 후 재무장) 검증.
- [ ] **X2.10 (ENG)** `rebalance.ts`가 `FiredAction[]` 소비 — take-profit=목표비중 트림, weight-cap=상한까지 트림, buy-dip/floor=목표/하한까지 매수; `canSell:false` 매도트리거→no-op+`warnings`; 대금 재분배=저비중 비례. **Acceptance:** vitest sell-lock take-profit no-op+warning; cap→정확히 상한착지(재발동0); 매도대금 저비중 매수로 흐름.

### Slice 5 — URL codec back-compat

- [ ] **X2.11 (ENG)** codec — `assets=LABEL:weight:route`에 trailing optional positional(canSell·sellPriority·groupId·groupWeightPct) + `rb=<freq>:<band>[;<trigger>…]`(trigger=`tp/bd/wc/wf:label:pct[:reset]`, driftBand는 head 암시); `useBacktestUrl.hook.ts` cap 초과시 신규 `utils/backtest/url-codec.ts` 추출. **Acceptance:** **기존 공유 URL 전량 무변경 디코드** — 레거시 `:1|:0`+XE.2 `:same|cash|<label>` 픽스처 회귀 vitest; encode→decode round-trip 동일; 신규필드 생략시 URL 문자열 기존과 동일(팽창금지); 미지 토큰 skip·미지 label drop·throw 금지.

### Slice 6 — UI 컨트롤

- [ ] **X2.12 (ENG)** AssetPicker per-holding — canSell 토글·sellPriority·그룹배정·DCA-route 셀렉터(배당route 기존유지) — 진행형 공개(기본 UI 유지, 고급 접이식). **Acceptance:** 미조작시 `BacktestInput` 기존과 deep-equal; 각 컨트롤 대응필드만; 신규컴포넌트 ≤300 LOC.
- [ ] **X2.13 (ENG)** 신규 `RebalancePolicyPanel.client.tsx` — freq+band+트리거 편집기+그룹 편집기(targetPct+sub-weight 인라인 검증). **Q3 1급 기본:** 리밸런싱 ON시 **비중비율(driftBand/weightCap, 예 "5:5→6:4 매도")이 기본 노출**, take-profit(gain%·bearThreshold%)·buy-dip은 "고급" 접이식 opt-in. **Acceptance:** 기본 `freq:'never'`+트리거 없음(=OFF=zero-regression); ON시 driftBand row 우선; 그룹 합≠100 실행차단+인라인에러; URL round-trip 후 패널 복원. **NB:** BacktestClient(현 293 LOC) 배선시 cap 압박 → 컨트롤 섹션 sub-extract 가능.
- [ ] **X2.14 (ENG)** 결과 노출 — MetricsPanel에 rebalance 횟수·turnover·warnings, StatsTable에 rebalance/trigger 이벤트 행(additive). **Acceptance:** `freq:'never'`+트리거0시 UI 기존과 시각 동일; 이벤트시 date·trades·발동트리거 렌더.
- [ ] **X2.17b (ENG) — Q1 UI부(X2.17a 후행)** 결과 테이블에 **자산별 비중%(월별)** 칼럼 — X2.17a가 emit한 per-holding 가치 스냅샷을 `monthlyStats.ts`/`StatsTable`에서 소비(월말 holding가치÷총가치). ⚠ **순수 후처리 아님**(현 timeline은 집계값만 → X2.17a 엔진 emit 전제). **Acceptance:** 자산별 weight% 합=100%(±반올림)·행별 렌더; 단일자산/DCA/리밸런싱 시나리오 정합; 기존 칼럼 무변경(additive).

### Slice 7 — 테스트/게이트

- [ ] **X2.15 (ENG) — ★zero-regression 게이트** `rebalance` 부재 입력에 Phase X 스냅샷 대비 byte-identical 회귀(X.10 QQQ DRIP + lump/DCA/배당라우팅 전량 재실행). **Acceptance:** 기존 vitest 186 + X.10 전부 green, diff 0.
- [ ] **X2.16 (ENG)** 최종 게이트 — vitest 신규 포함 전량 green·check-types 0·lint 0·전 파일 ≤300 LOC(engine/useBacktestUrl split 확인). **Acceptance:** 전 게이트 green이 커밋 조건.

### Slice 8 — 수동 현금흐름 (Q2, 오너 2026-07-06) — 특정 시점 임의 추가납입/인출

- [ ] **X2.18 (ENG)** `BacktestInput.manualFlows?:{date,amount}[]`(amount 음수=인출) additive + engine date-walk 주입(기존 contribution 스케줄 주입 메커니즘 일반화, DCA와 독립·공존). 비거래일→다음 거래일 스냅(contributions.ts 패턴). XIRR/Modified-Dietz flow에 포함. **순서 계약(아키텍트 must-fix):** manualFlow는 **DCA 단계에서 주입**(같은 지점, scheduled DCA와 함께) → **rebalanceIfDue 전** — 당일 수동 입금이 같은 bar 리밸런스/트리거에 반영되도록. **Acceptance:** `manualFlows` 부재⇒byte-identical(zero-regression); 지정일 정확 주입(vitest, 예 "2008-03 +$10k"); 인출>보유가치 가드(부분 인출·음수 방지); DCA↔manualFlow 순서 결정적; 순수성 유지.
- [ ] **X2.19 (ENG)** UI — DCA 옆 "수동 현금흐름" **날짜·금액 입력 리스트**(고급 접이식) + URL codec(`mf=date:amt,…`, back-compat 부재⇒무변경). MetricsPanel/StatsTable에 flow 반영. **Acceptance:** 리스트 추가/삭제→결과 즉시 재계산; URL round-trip; 미설정시 UI/URL 기존과 동일. **NB:** 테이블 인라인(행 클릭) 편집 UX = **후속 DEFERRED**(결과표 입력겸용 복잡도).

### Phase 2a — 합성 back-history: 회귀 프록시 (regression MVP) — ENG DONE (2026-07-06, 미커밋 배치, #44·#45)

> ✅ **ENG DONE (X2-P2.1–13, 4-wave dispatch):** 게이트 GREEN **vitest 369/369**(gate.test.ts byte-identical=zero-regression 확인)·tsc 0·lint 0·전 synth 파일 ≤300(오케스트레이터 BacktestControls 323·BacktestClient 301은 <400 응집 정당화). **아티팩트:** `prng.ts`(42, mulberry32/standardNormal/hashLabel 추출·projection byte-identical) + synth 코어 `synth/{types,overlap,fit(2-regime β),generate(block-bootstrap),dividends,index(method-key+horizon cap)}` + `url-codec-synth.ts`(45, `synth=<short>:<base>:<method>`, str/cmp 예약) + `useSyntheticBacktest.hook.ts`(194, splice+더블패스) + `seriesMapper.ts` + UI(BacktestControls 합성토글/base셀렉터/method스텁 admin-only·ResultChart 음영+SYNTHETIC 배너·MetricsPanel 듀얼지표 real-first+R²/cap 경고·StatsTable/YearlyTable SYNTH 태그) + `validation.test.ts`(overlap back-test 복원 tolerance 밴드). synth OFF⇒byte-identical. **USER 후속:** 커밋·수동 e2e(JEPQ 보유+합성토글). Phase 2b(구조적+비교) COMMITTED 대기.

> admin-only 합성 pre-inception 히스토리, **M1(회귀) 선행 ship**. **엔진 무변경 0줄**(순수 클라 전처리 `utils/backtest/synth/`) — 유일 예외 = 듀얼지표의 **엔진 더블 패스**(X2-P2.7). **수용 불변식(전 슬라이스): synth OFF(토글/URL 부재) ⇒ Phase X/X2 결과+URL byte-identical.** 전 파일 ≤300 LOC·순수/결정적(Date.now/Math.random 금지)·vitest. v1=JEPQ만·베이스 기본 QQQ·**method=`reg` 단일 실행이되 method 차원은 지금 깐다**(URL `:method`·셀렉터 스텁·method-key 반환 인터페이스) → Phase 2b(구조적+비교, committed)가 재작업 없이 슬롯인. 오너 confirmable 기본값 채택(깊이 2×overlap·JEPQ-only) — 비차단.

#### S1 — PRNG 추출 (선행 필수)

- [ ] **X2-P2.1 (ENG)** `projection.ts:15-32`의 `mulberry32`/`standardNormal` → 신규 공용 `utils/backtest/prng.ts` 추출 + `hashLabel(label)→uint32` 추가; projection.ts는 import로 전환. **Acceptance:** projection 출력 byte-identical(기존 몬테카를로 vitest+X.10 게이트 green, diff 0); prng.ts 순수·vitest(동일 seed⇒동일 시퀀스).

#### S2 — synth 코어 (`utils/backtest/synth/`, 전부 순수·결정적)

- [ ] **X2-P2.2 (ENG)** `overlap.ts` — short×base 실제 overlap 정렬(공통 거래일 교집합, TOTAL return 시리즈화: close+dividends) + `realInception(short)` 도출. **Acceptance:** vitest 결측바/불일치 캘린더/빈 overlap→명시 에러; 결정적.
- [ ] **X2-P2.3 (ENG)** `fit.ts` — 비대칭 2-regime β 회귀(base up-day/down-day 분리 β↑/β↓+α) + in-sample **R²** 산출 + 경험적 잔차 벡터 추출. **Acceptance:** vitest 합성 픽스처에서 β↑≠β↓ 복원(±tol)·R²∈[0,1]; 잔차 평균~0; 결정적.
- [ ] **X2-P2.4 (ENG)** `generate.ts` — pre-inception 구간 base return에 fit 적용 + `synthSeed=(seed^hashLabel(short))>>>0`로 잔차 bootstrap 재표집 → 역방향 가격 체인 `PricePoint[]`(합성 구간은 splits=0) 생성. **Acceptance:** 동일 (seed,base,short)⇒byte-identical 출력(vitest); 마지막 합성 bar→실제 첫 bar 가격 연속(점프 0); prng.ts만 사용.
- [ ] **X2-P2.5 (ENG)** `dividends.ts` — 실제 overlap 연율화 실현 분배율 flat → 합성 구간 `dividends` 합성(월 캐던스, JEPQ 실측 미러). **Acceptance:** vitest 합성 구간 연 분배율≈실현율(±tol); 배당≥0; 결정적.
- [ ] **X2-P2.6 (ENG)** `index.ts` 오케스트레이터 — `buildSyntheticHistory({short, base, seed, capYears, method})→{points, realInception, r2, cappedAt?}` + **horizon cap 적용**(기본 2×real-overlap, 초과분 생성 거부·`cappedAt` 반환). **2b 스캐폴딩:** 반환은 **method-key 디스패치 인터페이스** 뒤(`method:'reg'`만 구현; `'str'`은 미구현 명시 에러) — 2b가 동일 시그니처로 구조적 generator 추가. **Acceptance:** cap 경계 vitest(2×overlap 정확·초과 요청시 cap 클램프); 반환 메타(r2/realInception)가 S7 경고 구동 가능; `method:'str'`→명시 에러(silent fallback 금지); 전 함수 순수.

#### S3 — 베이스 선택 + 라벨 주입 + 스플라이스

- [ ] **X2-P2.7a (ENG)** `BacktestClient.client.tsx` 스플라이스 — synth 활성 시 (a) **베이스 라벨을 fetched `labelsKey` set에 주입**(보유자산 아니어도 fetch), (b) `toSeriesByLabel`(~L42-55) 후·`setSeriesData`(~L129) 전 합성 `PricePoint[]` **prepend**. **Acceptance:** prepend만으로 `seriesFirstDate`/`minAllowedFrom` 완화(DateRangePicker가 합성 시작일까지 허용) 확인; synth OFF⇒fetch 라벨·seriesData byte-identical; 엔진 파일 diff 0.

#### S4 — 듀얼 지표 (엔진 더블 패스)

- [ ] **X2-P2.7 (ENG) — ★더블 패스** synth 활성 시 `runBacktest` **2회 실행** — pass-1 `range.from=realInception`(REAL-only, headline) + pass-2 합성 시작(full-span) — 결과 2세트 보관·표시. **Acceptance:** headline Sharpe/CAGR/maxDD = pass-1(real-only)와 정확 일치·**먼저** 표기; full-span은 "합성 포함 (modeled)" sub-label로 두 번째; 합성이 headline에 blend되는 경로 0(vitest: pass-1 결과 == synth OFF+from=realInception 실행과 동일); synth OFF⇒단일 패스 유지.

#### S5 — URL codec

- [ ] **X2-P2.8 (ENG)** `url-codec.ts`(현 188 LOC, 초과 시 synth 전용 파일 분리) `synth=<shortLabel>:<base>:<method>` codec(never-throw: 미지 토큰 skip·미지 라벨 drop) + `useBacktestUrl` 상태. **method 토큰 = 2a에선 `reg`만 유효; `str`/`cmp`는 예약**(디코드 시 synth OFF 취급 — 2b 활성 전 무해). **Acceptance:** encode→decode round-trip 동일; synth 미설정⇒URL 문자열 기존과 byte-identical; 기존 공유 URL 픽스처 전량 무변경 디코드; `str|cmp` 토큰⇒synth OFF(throw 0); param presence가 SYNTHETIC 배너 구동(S7 연동).

#### S6 — admin UI

- [ ] **X2-P2.9 (ENG)** `BacktestControls.client.tsx` — **admin-only**(기존 useMe 게이트) Advanced 접이식 토글 "합성 과거"(기본 OFF) + 베이스 `<select>`(QQQ 기본/SPY/NASDAQ Composite/NASDAQ 100 + QLD·TQQQ는 "(validation only — 2× leverage, opposite profile)" 라벨) + **method 셀렉터 스텁**(단일 옵션 "Regression"·비활성 — 2b에서 str/cmp 활성). **Acceptance:** 비admin에 토글 미렌더; OFF시 UI/URL/결과 기존과 동일(deep-equal); 스텁이 URL `:reg` 인코딩과 정합; 신규 컴포넌트 ≤300 LOC.

#### S7 — SYNTHETIC 라벨링 + 경고 (가드레일 1·3·4)

- [ ] **X2-P2.10 (ENG)** ResultChart — `date<realInception` 구간 음영 밴드 + 구분 선 스타일 + 상시 "SYNTHETIC — modeled, not real" 배너(dismiss 불가). **Acceptance:** 합성 span 전체에 밴드; 경계=realInception 정확; synth OFF⇒렌더 diff 0.
- [ ] **X2-P2.11 (ENG)** StatsTable/YearlyTable per-row SYNTHETIC 태그(합성 span 걸친 행) + **R²-floor 경고**(r2<0.80 → "low-fit — unreliable" 강조 배너) + **horizon-cap 경계 경고**(cappedAt 존재 시 "2×overlap 상한 도달, 그 이전 거부"). **Acceptance:** 합성 포함 행 전부 태그; QLD 베이스 선택 시 R² 경고 트립(설계 sniff-test); cap 초과 요청 시 경고+클램프 표기; dismiss 불가.

#### S8 — 테스트/게이트

- [ ] **X2-P2.12 (ENG) — 검증** overlap back-test — 실제 JEPQ 이력 후반부를 held-out으로 감추고 전반부 fit으로 합성 → 실제와 비교(연율 vol·CAGR·상관 ±허용밴드 vitest 픽스처). **Acceptance:** 결정적 픽스처 green; 허용밴드 명시(예 vol ±20% rel); 밴드 이탈 시 fit 회귀로 간주.
- [ ] **X2-P2.13 (ENG) — ★zero-regression + 최종 게이트** synth OFF(토글 OFF/URL 부재) 전 시나리오 Phase X/X2 스냅샷 byte-identical(X.10+X2.15 재실행, prng 추출 포함) + vitest 전량 green·check-types 0·lint 0·전 파일 ≤300 LOC. **Acceptance:** 전 게이트 green = 커밋 조건; diff 0.

### Phase 2b — 구조적 복제 + 비교 (structural covered-call + comparison) — ENG DONE (2026-07-07, 미커밋; USER 백필 게이트 X2-P2b.2 잔여)

> ✅ **ENG DONE (X2-P2b.1·3–14, 4-wave dispatch):** 게이트 GREEN **vitest 421/421**(gate.test.ts byte-identical)·tsc 0·lint 0·비-테스트 소스 전부 ≤300. **아티팩트:** 클라 synth `synth/{bs(76),vol(72,k·VIX 프록시),calibrate(218,4-param 결정적 grid descent),overlay(204,cap-and-collect 월roll 프리미엄=배당)}` + `index.ts` str 분기(vol 필수) + `synth-passes.ts`(3-pass dedup: real-only 1회+reg-full+str-full) + `useSyntheticBacktest` 훅 + UI(`SynthControls` 추출로 BacktestControls 344→224·method 셀렉터 reg/str/cmp 활성·`ComparisonPanel` real|M1|M2 3열+divergence 행·ResultChart cmp 2-라인 오버레이·공유 `SynthBanner` bypass-불가 가드레일+proxy-vol 표기+ELN 모델리스크 문구) + `resultChartData.ts`/`useBacktestStats` 추출 + `validation-structural.test.ts`(파라미터 복원 tolerance) + collector `^VXN`/`^VIX`(watchlist+retention frozenset 13, raw 라벨, pytest 518). synth OFF/reg⇒byte-identical. **⚠ 잔여 USER 게이트 = X2-P2b.2:** `^VXN`/`^VIX` 일회성 백필(`run_daily --full --symbols ^VXN,^VIX`) — **이 실행 전 Structural/Compare는 실데이터 없음**(vol 미도달 시 명시 에러).

> M2 = `β·base_return − within-cycle give-back + BS 프리미엄(실제 historical vol)`; 4-param overlap 캘리브레이션 FREEZE; **완전 결정적(RNG 0 기본)**. **수용 불변식: synth OFF 또는 method=reg ⇒ 2a 결과 byte-identical.** 크래시윈도우(2000·2008) M1↔M2 발산 = model-uncertainty 신호 — 범위 표시·평균 금지. **⚠ 2b-S1 = Phase-2 최초 collector 터치(cross-stack, USER 백필 게이트, 마이그레이션 0).** 전 파일 ≤300 LOC·순수·vitest.

#### 2b-S1 — collector `^VXN`/`^VIX` 편입 [CROSS-STACK · 마이그레이션 0]

- [ ] **X2-P2b.1 (ENG)** `apps/stock_collector/configs/watchlist.yaml` `indices:`에 `^VXN`+`^VIX` 추가 + `retention.py` `BACKTEST_RETAIN_LABELS` frozenset에 양자 추가(prune이 deep vol 봉 silent 삭제 방지). 코드 외 변경 0(seriesBatch/엔드포인트 label-agnostic). **Acceptance:** loader 카운트 +2; prune dry-run 로그 exempt 카운트 +2; 신규 마이그레이션 0(Volume=0이 NOT NULL 충족 확인).
- [ ] **X2-P2b.2 (USER) — ⚠ 게이트** 일회성 수동 백필 `cd apps/stock_collector && uv run python -m collector.entrypoints.run_daily --full --symbols ^VXN,^VIX`. **Acceptance:** `market_history`에 ^VXN ~2001-02~ / ^VIX ~1990~ 봉 존재(O/H/L/C 실값·Volume=0); 다음 daily 재유입 0. **2b-S2 `vol.ts` 실데이터·S7 유의미 프리미엄의 전제 — 이 게이트 전 M2 실행 불가.**

#### 2b-S2 — 구조적 synth 코어 (전부 순수·결정적·RNG 0)

- [ ] **X2-P2b.3 (ENG)** `synth/bs.ts` — Black–Scholes 콜 프라이서(~30 LOC). **Acceptance:** 알려진 BS 픽스처 값(±1e-6); T→0/σ→0/S≤0 가드; vitest.
- [ ] **X2-P2b.4 (ENG)** `synth/vol.ts` — VXN/VIX 시리즈 로드·정렬 + **pre-2001 `k·VIX` 프록시**(k=overlap VXN/VIX 비율) + 프록시 구간 `isProxy` 플래그. **Acceptance:** 2001+⇒VXN 원본; pre-2001⇒k·VIX+플래그; 양 시리즈 결측⇒명시 에러; 결정적.
- [ ] **X2-P2b.5 (ENG)** `synth/calibrate.ts` — `{β, moneyness, coverage, capture-haircut}` 4-param **결정적 grid/coordinate descent**(2022+ overlap, TE+yield-match 최소화) → FREEZE + R²/TE 리포트(2a R²-floor 가드레일 공급). **Acceptance:** 합성 픽스처 파라미터 복원(±tol); 동일 입력⇒bit-동일(RNG 0); R²∈[0,1]·TE≥0.
- [ ] **X2-P2b.6 (ENG)** `synth/overlay.ts` — cap-and-collect: β·base_return − within-cycle short-call give-back + BS 프리미엄(**월 roll**, 프리미엄=월 `dividends`) → `PricePoint[]`, X2-P2.6 method-key 인터페이스에 `method:'str'`로 슬롯인. **Acceptance:** 결정적(RNG 0); 합성末→실제首 가격 연속(점프 0); 프리미엄≥0·월 캐던스.

#### 2b-S3 — vol = 참조 입력 (보유자산 아님)

- [ ] **X2-P2b.7 (ENG)** vol 라벨을 기존 **`refSeries` 2차 요청 패턴**(^IXIC/^NDX 선례)으로 fetch — `labelsKey`/weight-validation/`backtestInput`/포트폴리오 **절대 미주입**. **Acceptance:** method=str|cmp에서만 fetch; labelsKey·자산 셀렉터 byte-identical(≤15 cap 무영향); vol 미도달 시 명시 에러(silent 0-vol 금지).

#### 2b-S4 — method 셀렉터 + URL 활성화

- [ ] **X2-P2b.8 (ENG)** 2a 스텁 활성화 — 셀렉터 `Regression | Structural | Compare` + URL `method∈{reg,str,cmp}`(cmp={reg,str} 고정 쌍); **seed/depth 두 method 공유**(공정 비교). **Acceptance:** round-trip; 레거시 `:reg` URL 무변경 디코드; cmp⇒쌍 고정; OFF⇒byte-identical.

#### 2b-S5 — 비교 모드 (3-pass + hook 추출)

- [ ] **X2-P2b.9 (ENG)** **`useSyntheticBacktest.hook.ts` 추출** — synth-compute+멀티패스 오케스트레이션을 hook으로, `BacktestClient.client.tsx` **≤300 LOC 복귀**. **Acceptance:** reg-only/synth-OFF 결과 byte-identical; BacktestClient·hook 각 ≤300 LOC; tsc/lint 0.
- [ ] **X2-P2b.10 (ENG)** 비교 모드 = **정확히 3 엔진 패스**(공유 real-only 1회 dedup + M1-full + M2-full) + memo key에 `method`. **Acceptance:** cmp에서 `runBacktest` 호출 수==3(vitest 카운트); method 전환 시 memo 히트; {reg,str} 외 불가.

#### 2b-S6 — 비교 UI + 가드레일 확장

- [ ] **X2-P2b.11 (ENG)** 신규 `ComparisonPanel.tsx` — real-only | M1 | M2 병렬 지표표 + **크래시윈도우(2000·2008) M1↔M2 divergence 행**("model uncertainty — 범위 표시, 평균 금지"; 구조적=하방 near-full로 more defensible). **Acceptance:** 3컬럼+divergence 행; 평균/블렌드 수치 부재; ≤300 LOC.
- [ ] **X2-P2b.12 (ENG)** ResultChart 비교모드 2번째 합성 라인 + **SYNTHETIC 가드레일 공유 렌더 경로**(BOTH 시리즈+BOTH 컬럼 단일 경로 태그=누락 방지) + pre-2001 `isProxy` "proxied vol" 표기 + M2 "modeled — 실제 JEPQ는 액티브 ELN, 기계적 BS 아님" 카피. **Acceptance:** 태그 누락 0; proxy 구간 별도 표기; dismiss 불가.

#### 2b-S7 — 테스트/게이트

- [ ] **X2-P2b.13 (ENG) — 검증** calibration-recovers-real — 실제 JEPQ overlap held-out 분할로 M2 fit→합성 vs 실제 비교(TE/연율 yield/상관 허용밴드 vitest). **Acceptance:** 결정적 픽스처 green; 밴드 명시; 이탈=fit 회귀 간주.
- [ ] **X2-P2b.14 (ENG) — ★zero-regression + 최종 게이트** synth OFF + method=reg 전 시나리오 2a 스냅샷 byte-identical + vitest 전량 green·check-types 0·lint 0·전 파일 ≤300 LOC(BacktestClient/hook split 확인). **Acceptance:** 전 게이트 green = 커밋 조건; diff 0.
