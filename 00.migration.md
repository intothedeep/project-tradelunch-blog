# Vercel + Supabase Migration — Full Roadmap

**Last updated:** 2026-06-25
**Owner:** Taek Lim
**Branch:** feature/finance → main (after Phase A deploy)

---

## Status Legend

- `DONE` — already shipped; do not re-execute
- `BUILD-VERIFIED` — code complete and build passes clean; runtime verification still pending
- `SUPERSEDED` — planned step that is no longer applicable due to changed conditions
- `TODO (ENGINEER)` — pure code change; no credentials required
- `TODO (USER)` — requires AWS / Supabase / Vercel / DNS credentials
- `RISK` — flag for special care before executing

---

## What Is Already DONE

Do not redo any of the following:

| Item | File / Evidence |
|---|---|
| `api/index.ts` exports `app` as default | `apps/dashboard_server/api/index.ts` line 5 |
| `vercel.json` catch-all route | `apps/dashboard_server/vercel.json` |
| `env.schema.ts` full server contract (SUPABASE_*, CLERK_SECRET_KEY, ALLOWED_ORIGINS, etc.) | `apps/dashboard_server/src/config/env.schema.ts` |
| `DATABASE_URL: z.string()` required in env.schema.ts | confirmed (M3 T1) |
| `home.ts` /status route fixed | `apps/dashboard_server/src/controllers/home/home.ts` |
| `tsconfig.json` includes `api/**`; excludes `scripts/` and `**/x_*` | `apps/dashboard_server/tsconfig.json` |
| Supabase migrations `0001_blog_schema.sql` + `0002_rls_policies.sql` | `apps/dashboard_server/supabase/migrations/` |
| Synthetic `seed.sql` committed | `apps/dashboard_server/supabase/seed.sql` |
| `@repo/types` package created; API-boundary types moved from `@repo/markdown-parsing` | `packages/types/src/index.ts` |
| `@repo/axios` removed from client; client uses `apis/axios_instance.ts` | `apps/dashboard_client_web/apis/axios_instance.ts` |
| Supabase CLI devDep + package.json scripts (db:start/stop/reset/push/diff/status/link) | `apps/dashboard_server/package.json` |
| Client `vercel.json` (Next.js, turbo build from root) | `apps/dashboard_client_web/vercel.json` |
| `env.schema.ts` console.log removed from client | confirmed clean |
| Repo transferred to individual GitHub account (`intothedeep`) | Vercel Hobby compatible |
| S3.1 — `database.ts` replaced with pg Pool (max:1, reads DATABASE_URL) | `apps/dashboard_server/src/database.ts` — BUILD-VERIFIED; runtime pending Supabase data |
| S3.2 — 6 queries in `posts.ts` migrated from Sequelize to `pool.query()` | `apps/dashboard_server/src/controllers/posts/posts.ts` — BUILD-VERIFIED |
| S3.3 — `src/index.ts` rewritten; SSH tunnel + Sequelize imports removed | `apps/dashboard_server/src/index.ts` — BUILD-VERIFIED |
| S3.4 — `DATABASE_URL` upgraded to `z.string()` required | `apps/dashboard_server/src/config/env.schema.ts` — DONE (M3 T1) |
| S3.5 — `sequelize` + `pg-hstore` removed from package.json; `@types/pg` added | `apps/dashboard_server/package.json` — DONE; `pnpm install` done; build verified |
| Dead `src/utils/__database.ts` removed (was Sequelize duplicate of database.ts) | not in git |
| All `x_*` soft-deleted files removed from repo | git clean |

**Changed conditions since original architect plan:**

| Condition | Impact |
|---|---|
| AWS RDS DELETED | S2 (data migration) is SUPERSEDED. Supabase starts empty; no `pg_dump` or `psql` load needed. Fresh data will be authored directly. |
| AWS S3 DELETED | S8.2 (S3-to-Supabase image migration script) is SUPERSEDED. Blog images will be uploaded directly to Supabase Storage `blog-images` bucket from Phase B onward. `files.stored_uri` UPDATE is not needed. |
| Sequelize + S3 removed from server | `scripts/publish_posts.ts`, `scripts/publish_post/*`, `scripts/fix_*.ts` are BROKEN. They are excluded from the Vercel build (`tsconfig.json` excludes `scripts/`). Must be rewritten for pg Pool + Supabase Storage before blog posts can be published again. See Phase B task in `00.tasks.md`. |

**Known gaps still remaining:**

- `apps/dashboard_server/supabase/config.toml` does not exist (`supabase init` not run) — S1.1
- `seed.prod.sql` not yet listed in root `.gitignore` — S1.5
- Supabase project not yet created or linked — S1.2, S1.3
- S3 runtime verification pending Supabase DB being live with data
- Two Vercel projects not yet created — S4.2, S5.2
- DNS not yet pointed at Vercel — S6.2
- GitHub Actions CI not yet created — S7
- SSH tunnel code soft-delete pending — S9.1
- AWS env var cleanup pending — S9.3
- Publish tooling rework pending (Phase B) — scripts excluded from build; not functional

---

## Critical Path

Minimum ordered steps to get blog + mock dashboard live on `my.prettylog.com`:

> S1.1 (supabase init) → S1.2 (create Supabase project) → S1.3 (link CLI) → S1.4 (reconcile schema) → S1.5 (gitignore) → S1.6 (db push) → S1.7 (verify CTE) → [S2 SUPERSEDED] → S3 runtime verify (run dev server against Supabase) → S4.1 (CORS fix) → S4.2 (create Vercel backend project) → S4.3 (set backend env vars) → S4.4 (verify backend live) → S5.1 (fix deploy blockers) → S5.2 (create Vercel frontend project) → S5.3 (set frontend env vars) → S6.1 (confirm GitHub transfer) → S6.2 (DNS) → S7.1 (CI)

Phase B (S8 simplified + publish tooling rework) executes only after Phase A acceptance gate passes.

---

## User Action Checklist

Steps that require Supabase / Vercel / DNS credentials — no engineer can do these:

- [ ] S1.2 — Create Supabase project in dashboard
- [ ] S1.3 — `supabase link --project-ref <ref>`
- [ ] S1.6 — `supabase db push`
- [ ] S1.7 — Run recursive CTE smoke test in Supabase SQL editor
- [ ] S4.2 — Create Vercel project #2 (backend) in Vercel dashboard
- [ ] S4.3 — Set all backend env vars in Vercel project #2
- [ ] S4.4 — Smoke test 3 curl commands against live backend URL
- [ ] S5.2 — Create Vercel project #1 (frontend) in Vercel dashboard
- [ ] S5.3 — Set all frontend env vars in Vercel project #1
- [ ] S5.4 — Smoke test frontend on Vercel preview URL
- [ ] S6.1 — Confirm GitHub repo is under individual account; update git remote if not
- [ ] S6.2 — Add `my.prettylog.com` to Vercel project #1 Domains tab; update DNS records
- [ ] S6.3 — (Optional) Add custom backend domain to Vercel project #2
- [ ] S8.1 — Create `blog-images` bucket in Supabase Storage
- [ ] S8.4 — Update `CDN_ASSET_POSTS` in Vercel dashboard for both projects
- [ ] S9.3 — Delete AWS env vars from Vercel project #2; trigger redeploy

---

## Stage S1 — Supabase Project + Schema

**Goal:** Supabase project exists, schema is pushed, CLI is linked.
**Depends on:** nothing (first stage)
**Phase:** A

### S1.1 — Run `supabase init`
**Owner:** TODO (ENGINEER)
**File:** `apps/dashboard_server/supabase/` (directory exists; `config.toml` does not)

```bash
cd apps/dashboard_server
pnpm supabase init
```

Commit the generated `supabase/config.toml`.

**Acceptance:**
- `apps/dashboard_server/supabase/config.toml` exists and is committed
- `pnpm --filter dashboard_server db:status` recognises the project directory without error

---

### S1.2 — Create Supabase Project
**Owner:** TODO (USER — Supabase credentials)

Go to `https://supabase.com/dashboard` → New project.
- Region: `us-east-1` (matches pooler URL in `.env.example`: `aws-1-us-east-1.pooler.supabase.com`)
- Set a strong DB password; record the project ref

**Acceptance:**
- Project visible in Supabase dashboard
- Project ref string obtained

---

### S1.3 — Link CLI to Project
**Owner:** TODO (USER — Supabase credentials)
**Depends on:** S1.1, S1.2

```bash
cd apps/dashboard_server
pnpm supabase link --project-ref <ref>
# enter DB password when prompted
```

**Acceptance:**
- `pnpm --filter dashboard_server db:status` shows the linked project ref
- No auth error

---

### S1.4 — Reconcile Migration DDL
**Owner:** TODO (ENGINEER)
**Depends on:** S1.3

Inspect `apps/dashboard_server/supabase/migrations/0001_blog_schema.sql` against the schema reconciliation finding:

- `post_categories` table — keep as-is (unused by active queries)
- `posts.parent_id` — keep as-is (unused by active queries)
- `tags` table — keep as-is
- `post_tags.tag_id` — keep as-is as part of PK
- Any column marked `[INFERRED]` in the draft DDL must be resolved by inspection of the draft file alone (AWS RDS is DELETED; no schema dump is available)

Note: AWS RDS is deleted so C1 (pg_dump --schema-only) from the original plan is SUPERSEDED. Reconciliation proceeds from the existing draft only.

**Acceptance:**
- `0001_blog_schema.sql` has zero `[INFERRED]` placeholders
- `0002_rls_policies.sql` unchanged

---

### S1.5 — Add `seed.prod.sql` to gitignore
**Owner:** TODO (ENGINEER)
**File:** `/Users/tio/Documents/00_projects/project_tradelunch/.gitignore`

Add this line to the root `.gitignore`:
```
apps/dashboard_server/supabase/seed.prod.sql
```

**Acceptance:**
- `git ls-files apps/dashboard_server/supabase/seed.prod.sql` returns empty after adding

---

### S1.6 — Push Schema to Supabase
**Owner:** TODO (USER — Supabase credentials)
**Depends on:** S1.3, S1.4, S1.5

```bash
cd apps/dashboard_server
pnpm supabase db push
```

Uses `DATABASE_URL_DIRECT` (port 5432). If the CLI does not pick up the env var automatically:
```bash
pnpm supabase db push --db-url "postgresql://postgres.<ref>:<pw>@aws-1-us-east-1.pooler.supabase.com:5432/postgres"
```

**Acceptance:**
- `supabase db push` exits 0
- Supabase dashboard Table Editor shows all 7 tables: `users`, `posts`, `categories`, `files`, `tags`, `post_tags`, `post_categories`
- RLS is enabled on all tables

---

### S1.7 — Verify Recursive CTE Runs
**Owner:** TODO (USER — Supabase credentials)
**Depends on:** S1.6

In Supabase dashboard SQL editor, run the category tree CTE from `posts.ts` with a known username from `seed.sql` (e.g. `darkowlrising`). With only seed data, the CTE returns zero rows but must not error.

**Acceptance:**
- CTE executes without SQL error

---

## Stage S2 — Data Migration: AWS RDS to Supabase

**Status: SUPERSEDED**

AWS RDS has been DELETED. There is no data to migrate. Supabase starts empty. Fresh data will be authored directly into Supabase from Phase B onward. Skip this entire stage.

Original steps (C1/C6/C7 in `00.tasks.md`) have been marked superseded in the tasks file.

---

## Stage S3 — Backend Data Layer: Sequelize to pg Pool

**Status: BUILD-VERIFIED (runtime verification pending)**

All code changes in S3.1–S3.5 are complete and the server build passes clean:
- `pnpm --filter dashboard_server build` exits 0 (0 errors)
- `pnpm --filter dashboard_client_web check-types` exits 0

Runtime verification requires Supabase DB to be live with a schema (S1.6) so the pg Pool can connect and queries can execute.

### S3.1 — Replace `database.ts` — BUILD-VERIFIED
**File:** `apps/dashboard_server/src/database.ts`
Replaced with pg Pool (`max:1`, reads `DATABASE_URL`, `ssl: { rejectUnauthorized: false }`). Exports `pool`.

### S3.2 — Migrate 6 Queries in `posts.ts` — BUILD-VERIFIED (runtime pending)
**File:** `apps/dashboard_server/src/controllers/posts/posts.ts`
All 6 query sites migrated from `sequalizeP.query()` to `pool.query()`. Named `:param` replaced with positional `$N`. `::int` casts added for BIGINT columns. Repeated `$1` used in recursive CTE where `username` appears twice.

**Runtime acceptance (verify after S1.6):**
- Each of the 6 queries executes against Supabase without SQL error
- Pagination cursor works: second page returns different rows than first page
- `id` columns are numbers (not strings) in response JSON

### S3.3 — Rewrite `src/index.ts` — BUILD-VERIFIED
**File:** `apps/dashboard_server/src/index.ts`
SSH tunnel + Sequelize imports removed. Local dev entry now calls `app.listen()` only.

**Verify after S1.6:**
- `pnpm --filter dashboard_server dev` starts without error
- `curl http://localhost:4000/ping` returns `{"status":"ok","msg":"pong"}`

### S3.4 — `DATABASE_URL` Required — DONE
**File:** `apps/dashboard_server/src/config/env.schema.ts`
`DATABASE_URL: z.string()` is confirmed required (M3 T1).

### S3.5 — Remove Dead Deps + Add Types — DONE
`sequelize` and `pg-hstore` removed from `apps/dashboard_server/package.json`. `@types/pg` added to devDependencies. `scripts/` and `**/x_*` excluded from `tsconfig.json`. Dead `src/utils/__database.ts` removed from repo. `pnpm install` done; lockfile updated.

---

## Stage S4 — Backend on Vercel

**Goal:** Express API is live on a Vercel URL, serving blog posts from Supabase.
**Depends on:** S1.6, S3 runtime verified
**Phase:** A

### S4.1 — Fix CORS Origins
**Owner:** TODO (ENGINEER)
**File:** `apps/dashboard_server/src/server.ts`

The current hardcoded CORS whitelist does not include `http://localhost:3001` (actual dev port) or the Vercel preview URL. Update to read from `ALLOWED_ORIGINS` env var (already in `env.schema.ts`):

```typescript
const whitelist = env.ALLOWED_ORIGINS
  ? env.ALLOWED_ORIGINS.split(',').map(o => o.trim())
  : ['https://my.prettylog.com', 'http://localhost:3001'];
```

**Acceptance:**
- `GET /ping` from `http://localhost:3001` returns 200 with CORS headers
- `GET /ping` from an unlisted origin returns CORS error

---

### S4.2 — Create Vercel Project #2 (Backend)
**Owner:** TODO (USER — Vercel credentials)
**Depends on:** S4.1 committed and pushed

In Vercel dashboard → New Project → Import GitHub repo (`intothedeep/project-tradelunch-blog`):
- Project name: `tradelunch-api` (or similar)
- Root Directory: `apps/dashboard_server`
- Framework Preset: Other
- Install Command: `cd ../.. && pnpm install --frozen-lockfile`
- Build Command: leave blank (`@vercel/node` transpiles `api/index.ts` with esbuild)
- Output Directory: leave blank

`RISK:` The `@vercel/node` builder bundles `api/index.ts` and its imports using esbuild. Workspace package imports (`@repo/types`) must be resolvable at build time. The install command running from repo root (where `pnpm-workspace.yaml` lives) ensures `node_modules/@repo/types` is available. If the build cannot find `@repo/*`, move the install command into `vercel.json` instead of the dashboard setting.

**Acceptance:**
- First deploy succeeds
- Build log shows no "module not found" errors for `@repo/*`
- `GET https://<backend-url>/ping` returns `{"status":"ok","msg":"pong"}`

---

### S4.3 — Set Vercel Backend Env Vars
**Owner:** TODO (USER — Vercel + Supabase credentials)
**Depends on:** S4.2

In Vercel project #2 dashboard → Settings → Environment Variables:

| Key | Value | Environment |
|---|---|---|
| `NODE_ENV` | `production` | Production |
| `DATABASE_URL` | `postgresql://postgres.<ref>:<pw>@aws-1-us-east-1.pooler.supabase.com:6543/postgres` | Production + Preview |
| `DATABASE_URL_DIRECT` | `postgresql://postgres.<ref>:<pw>@aws-1-us-east-1.pooler.supabase.com:5432/postgres` | Production + Preview |
| `SUPABASE_PROJECT_ID` | `<project-ref>` | Production |
| `SUPABASE_URL` | `https://<project-ref>.supabase.co` | Production |
| `SUPABASE_SECRET_KEY` | `sb_secret_...` | Production |
| `ALLOWED_ORIGINS` | `https://my.prettylog.com,https://tradelunch-web.vercel.app` | Production |
| `APP_URL` | `https://my.prettylog.com` | Production |
| `CDN_ASSET_POSTS` | (set in Phase B when Supabase Storage is ready) | Production |
| `DEFAULT_USER_ID` | `2` | Production |

Do NOT set `DB_PG_*`, `EC2_*`, `AWS_*` — those are removed in S9.

**Acceptance:**
- Deploy succeeds after env vars are set
- `GET https://<backend-url>/v1/api/posts` returns blog posts from Supabase

---

### S4.4 — Verify Backend Live
**Owner:** TODO (USER)
**Depends on:** S4.3

```bash
curl https://<backend-vercel-url>/ping
# expect: {"status":"ok","msg":"pong"}

curl https://<backend-vercel-url>/v1/api/posts
# expect: {"success":true,"data":{"posts":[...],"nextCursor":...,"hasMore":...}}

curl "https://<backend-vercel-url>/v1/api/posts/users/taeklim"
# expect: posts by taeklim from Supabase (empty array is OK if no data yet; no 500 error)
```

**Acceptance:**
- All 3 curl commands return expected JSON shape
- No 500 errors
- Response time under 3 seconds (cold start acceptable)

---

## Stage S5 — Frontend on Vercel

**Goal:** Next.js client is live on a Vercel URL, loading blog posts from the backend.
**Depends on:** S4 complete
**Phase:** A

### S5.1 — Fix Remaining Deploy Blockers
**Owner:** TODO (ENGINEER)
**Depends on:** S3 runtime verified

Items still open in Group A1 of `00.tasks.md`:

a) `.nvmrc` — change from `24` to `22`.
File: `/Users/tio/Documents/00_projects/project_tradelunch/.nvmrc`

b) `article_server` workspace reference — root `package.json` references a workspace that does not exist. Remove the stale entry.

c) `@repo/axios` typecheck error — `packages/axios` was deleted. Confirm `pnpm --filter dashboard_client_web check-types` passes.

d) `proxy.ts` in client — if `apps/dashboard_client_web/proxy.ts` exists, rename to `x_proxy.ts` per soft-delete rule.

**Acceptance:**
- `.nvmrc` contains `22`
- `pnpm --filter dashboard_client_web check-types` exits 0
- `pnpm --filter dashboard_server check-types` exits 0
- `pnpm ls -r --depth 0` exits 0 with no "workspace not found" error

---

### S5.2 — Create Vercel Project #1 (Frontend)
**Owner:** TODO (USER — Vercel credentials)
**Depends on:** S5.1 committed and pushed

In Vercel dashboard → New Project → Import same GitHub repo:
- Project name: `tradelunch-web` (or similar)
- Root Directory: `apps/dashboard_client_web`
- Framework Preset: Next.js
- Build Command: leave default — `apps/dashboard_client_web/vercel.json` already sets the turbo build from root
- Install Command: `cd ../.. && pnpm install --frozen-lockfile` (already in `apps/dashboard_client_web/vercel.json`)
- Output Directory: `.next`

**Acceptance:**
- First deploy succeeds
- `/blog/@taeklim` renders on the Vercel preview URL (empty posts list is OK; no 500)

---

### S5.3 — Set Vercel Frontend Env Vars
**Owner:** TODO (USER — Vercel credentials)
**Depends on:** S5.2

In Vercel project #1 dashboard → Settings → Environment Variables:

| Key | Value | Environment |
|---|---|---|
| `NEXT_PUBLIC_API_BASE` | `https://<backend-vercel-url>` | Production + Preview |
| `NEXT_PUBLIC_CDN_ASSET_POSTS` | (set in Phase B when Supabase Storage is ready) | Production |
| `NEXT_PUBLIC_SITE_URL` | `https://my.prettylog.com` | Production |
| `NEXT_PUBLIC_APP_NAME` | `TradeLunch` | Production |
| `NEXT_PUBLIC_ENABLE_ANALYTICS` | `false` | Production |

Do NOT add `SUPABASE_*` or `DATABASE_URL` to the frontend project.

**Acceptance:**
- `/blog/@taeklim` on the Vercel preview URL loads without 500 error
- `/dashboard` loads chart with mock data; all 22 watchlist items visible

---

### S5.4 — Verify Frontend Live
**Owner:** TODO (USER)

```
https://<frontend-vercel-url>/blog/@taeklim   → blog post list route loads (empty is OK; no 500)
https://<frontend-vercel-url>/dashboard        → chart renders; 22 watchlist items visible
```

**Acceptance:**
- Zero 500 errors in browser console
- Network tab shows API calls going to `<backend-vercel-url>`

---

## Stage S6 — DNS and GitHub

**Goal:** `my.prettylog.com` points at the Vercel frontend; GitHub repo is confirmed under the individual account.
**Depends on:** S5 complete
**Phase:** A

### S6.1 — Confirm GitHub Repo Transfer
**Owner:** TODO (USER)

```bash
git remote -v
# must show: git@github.com:intothedeep/project-tradelunch-blog.git
```

If the remote still points at the org:
```bash
git remote set-url origin git@github.com:intothedeep/project-tradelunch-blog.git
```

**Acceptance:**
- `git push` succeeds to the individual account repo
- Vercel Hobby connects without requiring Pro

---

### S6.2 — Point DNS at Vercel Frontend
**Owner:** TODO (USER — DNS credentials)
**Depends on:** S5.2

In Vercel project #1 → Domains tab → Add `my.prettylog.com`.
Vercel shows the required record (CNAME `cname.vercel-dns.com` or Vercel IPs).
At DNS provider: add the CNAME / A record as instructed. TTL: 300 for fast propagation.

**Acceptance:**
- `https://my.prettylog.com/blog/@taeklim` loads within 5 minutes of DNS propagation
- HTTPS certificate is issued automatically by Vercel

---

### S6.3 — Custom Backend Domain (Optional)
**Owner:** TODO (USER — DNS credentials)

If a custom domain for the API is desired (e.g., `api.prettylog.com`), add it to Vercel project #2. Then update `NEXT_PUBLIC_API_BASE` and `ALLOWED_ORIGINS` in both Vercel projects. This step is optional for Phase A.

---

## Stage S7 — GitHub Actions CI

**Goal:** Lint + typecheck run automatically on every push and PR.
**Depends on:** S5.1 (deploy blockers fixed; check-types passes)
**Phase:** A

### S7.1 — Create CI Workflow
**Owner:** TODO (ENGINEER)
**File:** `/Users/tio/Documents/00_projects/project_tradelunch/.github/workflows/ci.yml`

```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.5.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm --filter dashboard_client_web check-types
      - run: pnpm --filter dashboard_server check-types
```

`RISK:` If `turbo.json` has no `check-types` pipeline task, the per-workspace commands above are the safe fallback (they are already written that way above).

**Acceptance:**
- Workflow runs green on the main branch
- Vercel auto-deploys on merge to main

---

## Phase A Acceptance Gate

All of the following must be true before beginning Phase B:

- [ ] `https://my.prettylog.com/blog/@taeklim` loads posts from Supabase; infinite scroll works; no SSH tunnel or AWS connection required
- [ ] `https://my.prettylog.com/dashboard` loads TradingView chart with mock data; all 22 watchlist items visible
- [ ] CI passes on main branch (`.github/workflows/ci.yml` badge green)
- [ ] No secrets hardcoded in source: `git grep -i "SUPABASE_SERVICE_ROLE_KEY\|CLERK_SECRET\|DATABASE_URL" -- '*.ts' '*.tsx' '*.js'` returns no matches
- [ ] `pnpm --filter dashboard_client_web check-types` exits 0
- [ ] `pnpm --filter dashboard_server check-types` exits 0
- [ ] No `sequelize` import in any file under `apps/dashboard_server/src/`
- [ ] No `supabase-js` import in any file under `apps/dashboard_server/`

---

## Stage S8 — Phase B: Supabase Storage for Blog Images

**Goal:** Blog images are served from Supabase Storage. AWS S3 is DELETED so this is a fresh setup, not a migration.
**Depends on:** Phase A acceptance gate passed
**Phase:** B

> **Note on publish tooling (2026-06-25):** `scripts/publish_posts.ts`, `scripts/publish_post/*`, and `scripts/fix_*.ts` are currently BROKEN — they relied on Sequelize + AWS S3, both of which have been removed. These scripts are excluded from the Vercel build via `tsconfig.json` (`scripts/` excluded). Before any blog posts can be published, the publish tooling must be rewritten for pg Pool + Supabase Storage. This rework is a Phase B task tracked in `00.tasks.md`. It should be done in conjunction with or immediately after S8.1 (bucket creation).

### S8.1 — Create Supabase Storage Bucket
**Owner:** TODO (USER — Supabase credentials)

In Supabase dashboard → Storage → New bucket:
- Name: `blog-images`
- Public: true
- File size limit: 50 MB
- Allowed MIME types: `image/jpeg,image/png,image/webp,image/gif`

**Acceptance:**
- Bucket visible in Supabase Storage
- Public URL pattern: `https://<project-ref>.supabase.co/storage/v1/object/public/blog-images/<path>`

---

### S8.2 — Soft-Delete AWS Storage Client
**Owner:** TODO (ENGINEER)
**Depends on:** S8.1 confirmed

Rename (per CLAUDE.md "rm -rf" rule — never hard delete):
- `apps/dashboard_server/src/lib/awsS3.ts` → `x_awsS3.ts`
- `apps/dashboard_server/src/middlewares/multer-s3.js` → `x_multer-s3.js` (already commented out)

No new storage client is needed for blog reads. If upload routes become active in Phase D, implement Supabase Storage HTTP API directly with `SUPABASE_SECRET_KEY`.

**Acceptance:**
- `grep -r "awsS3\|S3Client" apps/dashboard_server/src/` returns no matches in non-`x_` files
- `pnpm --filter dashboard_server check-types` exits 0

---

### S8.3 — Rework Publish Tooling (Sequelize + S3 → pg Pool + Supabase Storage)
**Owner:** TODO (ENGINEER)
**Depends on:** S8.1

Rewrite `scripts/publish_posts.ts`, `scripts/publish_post/*`, and `scripts/fix_*.ts` for the new stack:
- Replace all Sequelize calls with `pg` Pool + raw SQL (same pattern as `posts.ts`)
- Replace all AWS S3 SDK calls with Supabase Storage HTTP API using `SUPABASE_SECRET_KEY`
- Keep `scripts/` excluded from the Vercel build (`tsconfig.json`); these are offline tooling only

**Acceptance:**
- `scripts/publish_posts.ts` has no `sequelize` import; no `@aws-sdk` import
- A test publish run completes without error against a local Supabase DB
- Images upload to the `blog-images` bucket and resolve to a public URL
- `pnpm --filter dashboard_server build` exits 0 (scripts remain outside the Vercel build)

---

### S8.4 — Update `CDN_ASSET_POSTS`
**Owner:** TODO (USER — Vercel credentials)
**Depends on:** S8.1

Update in Vercel dashboard for both projects:
- Backend (`tradelunch-api`): `CDN_ASSET_POSTS` = `https://<project-ref>.supabase.co/storage/v1/object/public/blog-images/`
- Frontend (`tradelunch-web`): `NEXT_PUBLIC_CDN_ASSET_POSTS` = same URL

Optional Cloudflare CNAME: create `posts.prettylog.com` → `<project-ref>.supabase.co`; update both env vars to `https://posts.prettylog.com/blog-images/`. The env var names and contract are unchanged either way.

Trigger a redeploy for both projects after updating.

**Acceptance:**
- `CDN_ASSET_POSTS` in backend resolves to Supabase Storage URL
- `NEXT_PUBLIC_CDN_ASSET_POSTS` in frontend resolves to the same URL

---

## Stage S9 — Retire AWS Infrastructure

**Goal:** Zero AWS dependencies remain in the codebase and Vercel config.
**Depends on:** S8 acceptance; AWS RDS and S3 are already deleted
**Phase:** B

### S9.1 — Remove SSH Tunnel Code
**Owner:** TODO (ENGINEER)

Soft-delete (rename with `x_` prefix):
- `apps/dashboard_server/src/utils/ssh.tunnel.ts` → `x_ssh.tunnel.ts`
- `apps/dashboard_server/src/config/env.database.ts` → `x_env.database.ts`
- `apps/dashboard_server/src/certs/rds-combined-ca-bundle.pem` → `x_rds-combined-ca-bundle.pem` (if it exists)

Remove from `env.schema.ts` the now-dead exports:
- `EC2_HOST`, `EC2_PORT`, `EC2_USERNAME`
- `DB_PG_HOST`, `DB_PG_DATABASE`, `DB_PG_USER`, `DB_PG_PASSWORD`, `DB_PG_PORT`
- `AWS_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_S3_BUCKET`

**Acceptance:**
- `grep -r "ssh\|tunnel\|SSHTunnel\|sshConfig" apps/dashboard_server/src/ --include="*.ts"` returns no matches in non-`x_` files
- `pnpm --filter dashboard_server check-types` exits 0

---

### S9.2 — Remove Dead npm Packages
**Owner:** TODO (ENGINEER)
**Depends on:** S9.1
**File:** `apps/dashboard_server/package.json`

Remove from deps:
- `tunnel-ssh`
- `ss2`
- `@aws-sdk/client-s3`
- `@aws-sdk/s3-request-presigner`

Remove from devDeps:
- `@types/ssh2`
- `ssh2`

Keep: `pg`, `zod`, all Express/CORS/middleware deps, `@repo/*` workspace deps.

Run `pnpm install` from repo root.

**Acceptance:**
- `pnpm --filter dashboard_server build` exits 0
- `grep -r "@aws-sdk" apps/dashboard_server/src/` returns no matches in non-`x_` files

---

### S9.3 — Remove AWS Env Vars from Vercel
**Owner:** TODO (USER — Vercel credentials)
**Depends on:** S9.1, S9.2

In Vercel project #2 dashboard, delete:
- `AWS_REGION`
- `AWS_ACCESS_KEY_ID`
- `AWS_SECRET_ACCESS_KEY`
- `AWS_S3_BUCKET`

Trigger a redeploy. Verify the build still succeeds.

**Acceptance:**
- Redeploy succeeds
- `GET https://<backend-url>/v1/api/posts` still returns blog posts

---

## Risk Register

| Risk | Severity | Stage | Mitigation |
|---|---|---|---|
| `supabase init` not yet run — `config.toml` absent | Medium | S1.1 | S1.1 must precede S1.3; `config.toml` must exist before `supabase link` |
| `seed.prod.sql` not in gitignore | Medium | S1.5 | Fix in S1.5 before creating the file |
| BIGINT returned as string from pg | High | S3.2 | `::int` casts added in all 6 queries — BUILD-VERIFIED; verify at runtime |
| `$1` used twice in recursive CTE | High | S3.2 | pg supports repeated `$N`; BUILD-VERIFIED; verify at runtime |
| Dynamic WHERE clauses change param index alignment | High | S3.2 | Param-array builder used in implementation; verify both cursor=0 and cursor>0 paths |
| Publish tooling broken after Sequelize + S3 removal | High | S8.3 | Scripts excluded from Vercel build; rework tracked as Phase B task S8.3 |
| Vercel build: `@repo/types` not found | Medium | S4.2 | Install command must run from repo root; verify in first deploy log |
| CORS: localhost:3001 missing from whitelist | Low | S4.1 | S4.1 fixes via `ALLOWED_ORIGINS` env var |
| Supabase 7-day auto-pause | Medium | ongoing | Accepted known risk; manual resume via dashboard; keepalive ping to be added post-Phase B |
| Vercel Hobby + org repo | High | S4.2 | Confirm repo is under individual account (S6.1) before creating Vercel projects |

---

## Rollback Points

| Stage | Rollback Method |
|---|---|
| S3.1 (`database.ts`) | `git revert` single file |
| S3.2 (`posts.ts`) | `git revert` single file |
| S3.3 (`index.ts`) | `git revert` single file |
| S4.2 (Vercel project) | Delete Vercel project; no code change |
| S8.2 (soft-delete awsS3.ts) | Rename `x_awsS3.ts` back; no other changes |

Note: AWS RDS and S3 are already DELETED. There is no rollback to AWS for data storage.

---

## Key File Index

| File | Stage | Status |
|---|---|---|
| `apps/dashboard_server/src/database.ts` | S3.1 | BUILD-VERIFIED (runtime pending) |
| `apps/dashboard_server/src/controllers/posts/posts.ts` | S3.2 | BUILD-VERIFIED (runtime pending) |
| `apps/dashboard_server/src/index.ts` | S3.3 | BUILD-VERIFIED |
| `apps/dashboard_server/src/config/env.schema.ts` | S3.4, S9.1 | S3.4 DONE; S9.1 pending |
| `apps/dashboard_server/src/server.ts` | S4.1 | TODO |
| `apps/dashboard_server/vercel.json` | — | DONE; no change |
| `apps/dashboard_server/api/index.ts` | — | DONE; no change |
| `apps/dashboard_server/supabase/migrations/0001_blog_schema.sql` | S1.4 | Reconcile pending |
| `apps/dashboard_server/supabase/migrations/0002_rls_policies.sql` | — | DONE; no change |
| `apps/dashboard_server/supabase/seed.sql` | — | DONE; no change |
| `apps/dashboard_server/package.json` | S3.5, S9.2 | S3.5 DONE; S9.2 pending |
| `apps/dashboard_client_web/vercel.json` | — | DONE; no change |
| `apps/dashboard_client_web/apis/axios_instance.ts` | — | DONE; no change |
| `apps/dashboard_server/src/lib/awsS3.ts` | S8.2 | Soft-delete pending |
| `apps/dashboard_server/scripts/` | S8.3 | BROKEN — rework pending (Phase B) |
| `apps/dashboard_server/src/utils/ssh.tunnel.ts` | S9.1 | Soft-delete pending |
| `apps/dashboard_server/src/config/env.database.ts` | S9.1 | Soft-delete pending |
| `.gitignore` (root) | S1.5 | TODO — add `seed.prod.sql` line |
| `.nvmrc` | S5.1 | TODO — change to `22` |
| `.github/workflows/ci.yml` | S7.1 | TODO — create |
