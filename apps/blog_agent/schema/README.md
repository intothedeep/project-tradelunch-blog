# schema

The blog DB schema snapshot now lives in the shared DB package:
`packages/db/schema/tradelunch.schema.sql` (`@repo/db`).

Applied truth = `packages/db/supabase/migrations/`. This dir no longer holds a copy.
