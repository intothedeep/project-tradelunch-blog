# Supabase CLI local env — holds SUPABASE_SERVICE_ROLE_KEY + db password.
# NEVER commit. Only .env.example (placeholders) is tracked.
.env
.env.*
!.env.example

# Supabase CLI local artifacts
.branches
.temp

# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local
