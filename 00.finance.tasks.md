# 00.finance.tasks.md — Financial Surface Split (atomic tasks)

> Task breakdown for [`00.finance.plan.md`](./00.finance.plan.md). Rollback-safe,
> incremental. Each phase produces working output; verify before the next. `[ ]` open,
> `[x]` done, `[~]` in progress.

## Phase 0-emergency — Stop the bleed + un-pause (do FIRST)

> DIAGNOSIS DONE (2026-07-06): both providers exploded from ONE body of traffic — a
> **crawler campaign on 07-03..05** hitting DB-backed finance routes. Vercel Fast Origin
> Transfer 49.86GB ≈ Supabase egress 32GB ≈ same flood. The **migration does NOT un-pause
> you** (it fixes Supabase egress fully but Vercel Edge/Fluid CPU are front-side,
> crawler-driven). These emergency tasks are the real un-pause path — and 0.B is a
> PERMANENT fix, kept after migration.

- [ ] **0.A Attribution from access logs.** Vercel/Express logs for 07-03..05: top
      path + UA + IP/ASN. Confirms the scraper and gives the firewall target.
- [~] **0.B [HIGHEST LEVERAGE] Finance auth-gate at the proxy.** DONE (partial): the
      front-edge gate is `apps/dashboard_client_web/proxy.ts` (Next 16 renamed
      `middleware.ts`→`proxy.ts` — architect's "no gate exists" was a stale-name miss; a
      UA `BOT_UA_RE` gate already existed but is spoofable). Added `isFinanceRoute` to
      `auth.protect()` → anonymous scrapers bounced to sign-in before any SSR/Express/DB.
      Kept the bot 403 as a cheap short-circuit. **Relieves all 4 meters** (same traffic).
      FOLLOW-UP for TRUE owner-only (block authed non-admins, e.g. blog commenters):
      `is_admin` lives only in the DB (`resolveAuth.ts:75`), NOT in Clerk claims → cannot
      be read at the edge. Sync `is_admin`→Clerk `publicMetadata` then check
      `sessionClaims.metadata.isAdmin` in proxy. Needs a one-time Clerk metadata set on
      taek.lim.us@gmail.com. Auth-required already stops the scraper egress; owner-only is
      an access-control nicety, not the bleed-stopper.
- [x] **0.C Cap the two unbounded queries + fix cache leaks.** DONE. `funds.ts` holdings
      → `LIMIT 5000` runaway guard (weight_pct via `SUM() OVER ()` stays correct — computed
      pre-LIMIT); `dashboard.ts` snapshot → defensive `LIMIT 500`; both 5xx catch paths now
      set `Cache-Control: public, s-maxage=10` (dampens hot error loops). Server+client
      typecheck clean.
- [~] **0.D De-`force-dynamic` — SKIPPED (obsolete after 0.B).** Once finance is
      auth-gated, anonymous crawlers never reach SSR, so the force-dynamic public-cache
      concern is moot. Removing it is also RISKY: `app/dashboard/page.tsx:11-15` documents
      that `force-dynamic` prevents serving a stale build-time snapshot. Leaving as-is
      (correct for per-user authed pages). Reopen only if finance goes public again.
- [ ] **0.E Un-pause** with a short Vercel Pro buffer — ONLY after 0.B/0.C confirm the
      bleed stopped (else it just moves the bleed to a paid meter). Downgrade later.

## Phase 0 — Provisioning (structural fix; proceed deliberately, NOT under billing pressure)

- [ ] **0.4 Provision Oracle VM + PG17.** TLS on, `5432` open, stable public IPv4,
      `pg_hba.conf`/security-lists locked to TLS-only. Confirm `psql -c 'select 1'` reaches
      it from **both** a Vercel region **and** GitHub runners. Confirm the VM is a
      paid/reserved shape (not idle-reclaimable Always Free).
- [ ] **0.5 Install PgBouncer/pgcat** (transaction pooling) on the VM. Required for
      serverless burst safety even though `finance_api` is co-located.
- [ ] **0.6 Backups.** Configure `pgBackRest`/WAL archiving → off-box/object storage +
      a **tested restore** (untested backup ≠ backup). Financial history is expensive to re-collect.
- [ ] **0.7 Generate financial DDL.** `pg_dump --schema-only --no-owner --no-privileges
    -t <financial tables>` → `packages/finance-migrations/0001_finance_init.sql`. Strip
      Supabase-isms (`auth.`/`storage.`/RLS/`supabase_*`). Review by hand.
- [ ] **0.8 Apply DDL to Oracle.** `psql -v ON_ERROR_STOP=1 -f 0001_finance_init.sql`;
      `REFRESH MATERIALIZED VIEW mv_sec_new_positions;`. Verify with `\dt` + smoke selects.
      _Rollback: none — Oracle is greenfield._

## Phase 1 — Carve `finance_api` (parallel, NO cutover)

- [ ] **1.1 Create `apps/finance_api/`** (Express workspace). **Copy** (not move) the
      financial controllers from `dashboard_server`: `dashboard/*`, `funds/*`,
      `securities/*`, `rankings/*`, `politicians/*`, `errorLogs/*` + their helpers/models +
      `blockCrawlers` middleware + a pg wiring pointed at `FINANCE_POSTGRES_URL`.
      Keep `dashboard_server` financial routes LIVE (still Supabase) in parallel.
- [ ] **1.2 Load a data snapshot into Oracle** (see 2.x data move) for parity testing.
- [ ] **1.3 Parity test** each endpoint `finance_api` (Oracle) vs `dashboard_server`
      (Supabase): diff `/api/dashboard`, `/api/funds/:cik`, `/api/rankings`,
      `/api/securities/:cusip/consensus`, `/api/politicians/:id`, `/api/securities` screen.
      _Rollback: delete `finance_api`; nothing in prod changed._

## Phase 2 — Collector (writer) cutover

- [ ] **2.1 Data move.** `pg_dump --data-only --no-owner --disable-triggers` scoped to
      financial tables (`-Fc` + `pg_restore -j` for large `market_history`,
      `politician_trades`, `signal_backtest`) → Oracle → `REFRESH MATERIALIZED VIEW` →
      row-count reconciliation before/after.
- [ ] **2.2 Repoint `stock_collector`** `DATABASE_URL` → Oracle session DSN (GitHub
      Environment `oracle-prod`). Manual-dispatch `collector-daily` + `collector-weekly`;
      verify rows land in Oracle. Optional dual-write matrix (Supabase + Oracle) to keep
      Supabase warm for rollback.
      _Rollback: revert the secret → collector writes Supabase again._
- [ ] **2.3 Repoint remaining collector workflows** (§6 list) secrets to Oracle.

## Phase 3 — Front read cutover

- [ ] **3.1 Deploy `finance_api` on the Oracle VM** behind Caddy/nginx TLS (Option A),
      systemd/PM2. Final delta re-sync of financial data (brief collector write-freeze).
- [ ] **3.2 Set `FINANCIAL_API_BASE_URL`** in Vercel `dashboard_client_web`; repoint
      financial `apis/*.api.ts` fetchers; redeploy. Blog fetchers untouched.
- [ ] **3.3 Verify in prod**: `/dashboard`, `/funds`, `/rankings`, `/screener`,
      `/symbols`, `/politicians`, `/backtest`. Watch Supabase financial egress drop to ~0.
      _Rollback: unset `FINANCIAL_API_BASE_URL` (fetchers fall back to Supabase-backed
      `dashboard_server`); redeploy. Keep Supabase financial tables ≥1 week._

## Phase 4 — Decommission financial on Supabase (after ≥1 week stable)

- [ ] **4.1** Soft-delete (`x_` prefix) the moved controllers in `dashboard_server`;
      remove financial routes from its router; redeploy blog API.
- [ ] **4.2** Retire `supabase-keepalive.yml` (`x_`); add `oracle-keepalive`/health check.
- [ ] **4.3** Drop financial tables from Supabase (dump-then-drop; keep the dump).
      _Rollback window closes here — only after monitoring confirms parity._

## Phase 5 — Storage/Parquet archive (LATER, out of scope)

Parquet `market-archive`/`sec-archive` → Oracle Object Storage. Do not couple to this split.

## Cross-cutting / open items

- [ ] Q2 `error_log`/`batch_log` ownership → confirm move to Oracle (blog errors then log there).
- [ ] Q3 verify no blog/preview controller reads `market_snapshots` before moving it.
- [ ] Q4 classify migration 0032–0034 objects (blog vs financial).
- [ ] `ci.yml` — add `finance_api` to build/lint/typecheck graph (Turbo auto-discovers once it's a workspace).
