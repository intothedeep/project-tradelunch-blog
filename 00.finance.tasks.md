# 00.finance.tasks.md — Financial Surface Split (atomic tasks)

> Task breakdown for [`00.finance.plan.md`](./00.finance.plan.md). Rollback-safe,
> incremental. Each phase produces working output; verify before the next. `[ ]` open,
> `[x]` done, `[~]` in progress.

## Phase 0-emergency — Stop the bleed + un-pause (do FIRST)

> DIAGNOSIS DONE (2026-07-06): both providers exploded from ONE body of traffic — a
> **crawler campaign on 07-03..05** hitting DB-backed finance routes. Vercel Fast Origin
> Transfer 49.86GB ≈ Supabase egress 32GB ≈ same flood. The **migration does NOT un-pause
> you** (it fixes Supabase egress fully but Vercel Edge/Fluid CPU are front-side,
> crawler-driven). These emergency tasks are the real un-pause path — and 0.B is a
> PERMANENT fix, kept after migration.

- [ ] **0.A Attribution from access logs.** Vercel/Express logs for 07-03..05: top
      path + UA + IP/ASN. Confirms the scraper and gives the firewall target.
- [~] **0.B [HIGHEST LEVERAGE] Finance auth-gate at the proxy.** DONE (partial): the
      front-edge gate is `apps/dashboard_client_web/proxy.ts` (Next 16 renamed
      `middleware.ts`→`proxy.ts` — architect's "no gate exists" was a stale-name miss; a
      UA `BOT_UA_RE` gate already existed but is spoofable). Added `isFinanceRoute` to
      `auth.protect()` → anonymous scrapers bounced to sign-in before any SSR/Express/DB.
      Kept the bot 403 as a cheap short-circuit. **Relieves all 4 meters** (same traffic).
      FOLLOW-UP for TRUE owner-only (block authed non-admins, e.g. blog commenters):
      `is_admin` lives only in the DB (`resolveAuth.ts:75`), NOT in Clerk claims → cannot
      be read at the edge. Sync `is_admin`→Clerk `publicMetadata` then check
      `sessionClaims.metadata.isAdmin` in proxy. Needs a one-time Clerk metadata set on
      taek.lim.us@gmail.com. Auth-required already stops the scraper egress; owner-only is
      an access-control nicety, not the bleed-stopper.
- [x] **0.C Cap the two unbounded queries + fix cache leaks.** DONE. `funds.ts` holdings
      → `LIMIT 5000` runaway guard (weight_pct via `SUM() OVER ()` stays correct — computed
      pre-LIMIT); `dashboard.ts` snapshot → defensive `LIMIT 500`; both 5xx catch paths now
      set `Cache-Control: public, s-maxage=10` (dampens hot error loops). Server+client
      typecheck clean.
- [~] **0.D De-`force-dynamic` — SKIPPED (obsolete after 0.B).** Once finance is
      auth-gated, anonymous crawlers never reach SSR, so the force-dynamic public-cache
      concern is moot. Removing it is also RISKY: `app/dashboard/page.tsx:11-15` documents
      that `force-dynamic` prevents serving a stale build-time snapshot. Leaving as-is
      (correct for per-user authed pages). Reopen only if finance goes public again.
- [ ] **0.E Un-pause** with a short Vercel Pro buffer — ONLY after 0.B/0.C confirm the
      bleed stopped (else it just moves the bleed to a paid meter). Downgrade later.

## Phase 0 — Provisioning (structural fix; proceed deliberately, NOT under billing pressure)

- [ ] **0.4 Provision Oracle VM + PG17.** TLS on, `5432` open, stable public IPv4,
      `pg_hba.conf`/security-lists locked to TLS-only. Confirm `psql -c 'select 1'` reaches
      it from **both** a Vercel region **and** GitHub runners. Confirm the VM is a
      paid/reserved shape (not idle-reclaimable Always Free).
- [ ] **0.5 Install PgBouncer/pgcat** (transaction pooling) on the VM. Required for
      serverless burst safety even though `finance_api` is co-located.
- [ ] **0.6 Backups.** Configure `pgBackRest`/WAL archiving → off-box/object storage +
      a **tested restore** (untested backup ≠ backup). Financial history is expensive to re-collect.
- [ ] **0.7 Generate financial DDL.** `pg_dump --schema-only --no-owner --no-privileges
    -t <financial tables>` → `packages/finance-migrations/0001_finance_init.sql`. Strip
      Supabase-isms (`auth.`/`storage.`/RLS/`supabase_*`). Review by hand.
- [ ] **0.8 Apply DDL to Oracle.** `psql -v ON_ERROR_STOP=1 -f 0001_finance_init.sql`;
      `REFRESH MATERIALIZED VIEW mv_sec_new_positions;`. Verify with `\dt` + smoke selects.
      _Rollback: none — Oracle is greenfield._

## Phase 1 — Carve `finance_api` (parallel, NO cutover)

- [ ] **1.1 Create `apps/finance_api/`** (Express workspace). **Copy** (not move) the
      financial controllers from `dashboard_server`: `dashboard/*`, `funds/*`,
      `securities/*`, `rankings/*`, `politicians/*`, `errorLogs/*` + their helpers/models +
      `blockCrawlers` middleware + a pg wiring pointed at `FINANCE_POSTGRES_URL`.
      Keep `dashboard_server` financial routes LIVE (still Supabase) in parallel.
- [ ] **1.2 Load a data snapshot into Oracle** (see 2.x data move) for parity testing.
- [ ] **1.3 Parity test** each endpoint `finance_api` (Oracle) vs `dashboard_server`
      (Supabase): diff `/api/dashboard`, `/api/funds/:cik`, `/api/rankings`,
      `/api/securities/:cusip/consensus`, `/api/politicians/:id`, `/api/securities` screen.
      _Rollback: delete `finance_api`; nothing in prod changed._

## Phase 2 — Collector (writer) cutover

- [ ] **2.1 Data move.** `pg_dump --data-only --no-owner --disable-triggers` scoped to
      financial tables (`-Fc` + `pg_restore -j` for large `market_history`,
      `politician_trades`, `signal_backtest`) → Oracle → `REFRESH MATERIALIZED VIEW` →
      row-count reconciliation before/after.
- [ ] **2.2 Repoint `stock_collector`** `DATABASE_URL` → Oracle session DSN (GitHub
      Environment `oracle-prod`). Manual-dispatch `collector-daily` + `collector-weekly`;
      verify rows land in Oracle. Optional dual-write matrix (Supabase + Oracle) to keep
      Supabase warm for rollback.
      _Rollback: revert the secret → collector writes Supabase again._
- [ ] **2.3 Repoint remaining collector workflows** (§6 list) secrets to Oracle.

## Phase 3 — Front read cutover

- [ ] **3.1 Deploy `finance_api` on the Oracle VM** behind Caddy/nginx TLS (Option A),
      systemd/PM2. Final delta re-sync of financial data (brief collector write-freeze).
- [ ] **3.2 Set `FINANCIAL_API_BASE_URL`** in Vercel `dashboard_client_web`; repoint
      financial `apis/*.api.ts` fetchers; redeploy. Blog fetchers untouched.
- [ ] **3.3 Verify in prod**: `/dashboard`, `/funds`, `/rankings`, `/screener`,
      `/symbols`, `/politicians`, `/backtest`. Watch Supabase financial egress drop to ~0.
      _Rollback: unset `FINANCIAL_API_BASE_URL` (fetchers fall back to Supabase-backed
      `dashboard_server`); redeploy. Keep Supabase financial tables ≥1 week._

## Phase 4 — Decommission financial on Supabase (after ≥1 week stable)

- [ ] **4.1** Soft-delete (`x_` prefix) the moved controllers in `dashboard_server`;
      remove financial routes from its router; redeploy blog API.
- [ ] **4.2** Retire `supabase-keepalive.yml` (`x_`); add `oracle-keepalive`/health check.
- [ ] **4.3** Drop financial tables from Supabase (dump-then-drop; keep the dump).
      _Rollback window closes here — only after monitoring confirms parity._

## Finance standalone app — carve phases (REVISED 2026-07-07)

> Owner decision: finance = standalone app (finance_web + finance_api + finance-migrations),
> existing UI MOVED not rebuilt. These interleave with the Oracle phases above.
> **CAN START NOW (no infra)** = P1–P5. **NEEDS infra** = P6–P11.

- [x] **P5 (F.1) Commit the backtest work.** DONE — landed by the other session
      (`2466f28` Phase 2b structural synth + method comparison; `c11a97e` dropped client
      tsconfig baseUrl). Synth engine now fully tracked → P3 copy is fork-safe.
- [x] **P1 Scaffold workspaces.** DONE (`2a2794c`). finance_web (Next16 shell + proxy
      auth/admin gate + next-intl i18n/request.ts + messages/{en,ko}/common.json — BUILDS
      clean), finance_api (Express + pg Pool + /health), packages/finance-migrations.
      Additive; dashboard_client_web/dashboard_server untouched + still typecheck.
- [x] **P2 DDL draft.** DONE (`2a2794c`). `packages/finance-migrations/0001_finance_init.sql`
      — reviewed DRAFT from migrations (17 tables + 16 views + mv_sec_new_positions,
      Supabase-isms stripped). `pg_dump --schema-only` authoritative at cutover.
- [x] **P3 COPY finance UI → finance_web.** DONE (`9ee5397`). 274 files: 12 routes +
      components (7 domains + ui atoms + seo/providers) + 25 actions + 15 hooks + whole
      `utils/backtest/` + finance types + lib (cn/chart-theme/drawings/og/jsonld) + jotai
      store + apis infra. API base → `NEXT_PUBLIC_API_BASE` (fallback = current prod
      Express URL → runs standalone pre-cutover). Nav inlined in layout. **check-types +
      build GREEN (all routes). dashboard_client_web UNTOUCHED + still typechecks.**
      NOTE: backtest `.test.ts` copied but excluded from tsc (no vitest in finance_web yet).
- [x] **P4 COPY finance controllers → finance_api.** DONE (`e94d311`). Controllers
      dashboard(+seriesBatch)/funds(+rankflow)/securities(screen,byTicker,consensus)/
      rankings(+rankflow)/politicians/errorLogs + 12 helpers + blockCrawlers; index.ts
      mounts /api/{dashboard,funds,securities,rankings,politicians,error-logs}. Imports
      resolve to finance_api's own database.ts (FINANCE_DATABASE_URL). check-types+build
      GREEN. dashboard_server UNTOUCHED + typechecks. **Parity test vs dashboard_server
      deferred to P7 (needs a DB with data).**
- [ ] **P6 Provision Oracle** (= Phase 0.4–0.8: VM/PG17/PgBouncer/backups+tested-restore/TLS/DDL apply).
- [ ] **P7 Data move + collector cutover** (= Phase 2.1–2.3) → Oracle.
- [ ] **P8 Deploy finance_api** on the VM behind Caddy/nginx TLS.
- [ ] **P9 Deploy finance_web** as a NEW Vercel project (own domain e.g. finance.prettylog.com),
      `API_BASE_URL`→Oracle, Clerk shared instance (Option A), admin claim configured.
- [ ] **P10 Verify prod** finance_web ↔ finance_api ↔ Oracle e2e; Supabase finance egress → 0.
- [x] **P11-front FRONTEND cutover DONE (2026-07-08, commits 00508dc + e0c5a65).**
      (a) Re-synced the drifted `backtest` domain dcw → finance_web (only drift since the P3
      copy; all other finance domains already in sync); dropped finance_web's stale bare
      `WeightSliders`. (b) REMOVED finance from `dashboard_client_web`: `git mv` the whole
      finance surface (routes/components/hooks/utils/backtest/finance-utils/types/lib-chart+
      drawings/store-atoms/apis/actions) → `x_finance/` quarantine (238 renames, restorable;
      tsconfig already excludes `**/x_*`, added `**/x_*/**` to eslint + vitest). Cross-cutting
      EDITS: `proxy.ts` (dropped isFinanceRoute/bot-gate/admin-claim), `robots.ts` (dropped
      FINANCE_DISALLOW + /dashboard/preview), `useNavLinks` (dropped MARKETS set, kept blog
      primary nav), `TopBar` (dropped MARKETS dropdown), `navigation-desktop` (dropped
      isChartDashboard). dcw check-types 0, build GREEN (blog-only routes). No blog→finance
      import coupling existed.
- [ ] **P11-back BACKEND decommission** — soft-delete finance from `dashboard_server` = **B5**
      below. GATED: must come AFTER finance_api is deployed on Oracle (B3) AND finance_web is
      repointed to it (B4) AND a ≥1wk soak — else it 500s finance_web (which still reads
      dashboard_server). Then Phase 4 (drop finance tables on Supabase = B7).

## Backend migration: dashboard_server → finance_api (B0–B7)

> Design by system-architect (2026-07-08), code-verified. **Finding: NO controller drift** —
> finance_api's 6 domains are byte-identical to dashboard_server (P4 copy still current), so no
> big re-sync (unlike the frontend). Only 2 P4-copy OMISSIONS + infra gates remain.
> **Only B0 is runnable now (no infra); B1–B7 are Oracle-gated (P6).** HOLD implementation
> until Oracle is provisioned (owner decision 2026-07-08: docs-only for now).

**B0 — Drift/gap sync into finance_api (CAN START NOW, no infra):**
- [ ] **B0.1** Copy `dashboard_server/src/middlewares/errorHandler.ts` → finance_api; mount LAST
      in `finance_api/src/index.ts` (finance_api currently has NO terminal error handler →
      unhandled finance errors leak stack traces + skip `error_log`).
- [ ] **B0.2** Doc reconcile: finance_api reads `POSTGRES_URL` / `POSTGRES_URL_NON_POOLING`
      (its `env.schema.ts`), NOT `FINANCE_POSTGRES_URL` / `FINANCE_DATABASE_URL` (plan §5.4 +
      P4 are STALE). Setting the nonexistent `FINANCE_*` var at deploy = pool points at nothing.
- [ ] **B0.3** Re-diff the 6 domains dashboard_server vs finance_api at deploy time; re-copy if
      drift appeared (currently none).
- [ ] **B0.4** Parity harness (script, no prod change): diff JSON finance_api vs dashboard_server
      for `/v1/api/{dashboard, dashboard/history/batch, funds, funds/:cik, funds/:cik/rankflow,
      rankings, rankings/flow, securities/screen, securities/:cusip/consensus,
      securities/:ticker/by-ticker, politicians/:filerId}`.

**B1 — Data move + collector cutover (needs Oracle gate P6):**
- [ ] **B1.1** `pg_dump --data-only --disable-triggers -Fc` 18 finance tables + MV → `pg_restore -j`
      → `REFRESH MATERIALIZED VIEW mv_sec_new_positions` → row-count reconciliation.
- [ ] **B1.2** Repoint `stock_collector` `DATABASE_URL` → Oracle DSN (env `oracle-prod`); dispatch
      daily+weekly; verify rows in Oracle. Keep Supabase finance tables DUAL-WARM through the soak
      (else B4 rollback serves stale data).

**B2 — Parity:** [ ] **B2.1** run B0.4 harness finance_api(Oracle) vs dashboard_server(Supabase); zero diff modulo ts/order.

**B3 — Deploy finance_api on VM behind TLS (P8):** [ ] **B3.1** PM2/systemd + Caddy/nginx; env `POSTGRES_URL` (Oracle localhost/PgBouncer), `CLERK_*`, `ALLOWED_ORIGINS`=finance_web; `/health` green from a Vercel region.

**B4 — finance_web repoint (THE GATE, P9/P10):**
- [ ] **B4.1** Set finance_web `NEXT_PUBLIC_API_BASE` → finance_api TLS URL; redeploy. Set Clerk
      `publicMetadata.isAdmin` on owner (finance_api `users/me` reads Clerk, not the users table).
- [ ] **B4.2** Verify e2e (dashboard/funds/rankings/screener/symbols/politicians/backtest); watch
      Supabase finance egress → ~0. **Rollback: unset `NEXT_PUBLIC_API_BASE` → dashboard_server.**

**B5 — Decommission finance in dashboard_server (after ≥1wk soak) = P11-back:**
- [ ] **B5.1** `x_` soft-delete controllers `dashboard/ funds/ securities/ rankings/ politicians/`.
      **KEEP `errorLogs/` — the BLOG writes SSR errors there** (see risk #2 / Q2).
- [ ] **B5.2** `controllers/index.ts`: remove the finance mounts + imports (dashboard/funds/
      securities/rankings/politicians + `blockCrawlers` import). KEEP errorLogs + all blog mounts.
- [ ] **B5.3** `x_` finance-only helpers (`screenScore`, holder/weight helpers, …) — **KEEP
      `helpers/errorLog.ts`** (blog errorLogs + errorHandler use it).
- [ ] **B5.4** `x_` `middlewares/blockCrawlers.ts` (finance-only). **B5.5** dashboard_server
      check-types 0 + jest green; redeploy; verify blog posts/comments/likes/error-logs work.

**B6/B7 — Collector + Supabase teardown (Phase 4.2/4.3):**
- [ ] **B6.1** Repoint remaining collector workflow secrets → Oracle (§6 list); retire
      `supabase-keepalive.yml` (`x_`); add oracle health check.
- [ ] **B7.1** `pg_dump` then DROP Supabase finance tables (keep dump). **Do NOT drop
      `error_log`/`batch_log` on Supabase** (blog keeps its own `error_log`). Rollback window closes.

**Risks:** (1) B5 before B4+soak breaks finance_web prod (order is the #1 gate). (2) `error_log`
is SHARED (blog + finance) → keep dashboard_server errorLogs controller + table on Supabase.
(3) finance_api missing terminal errorHandler (B0.1). (4) `blockCrawlers` finance-only in
dashboard_server → drop its import after removing mounts. (5) env is `POSTGRES_URL` not `FINANCE_*`.
(6) no shared DB pool (separate processes). (7) collector dual-warm needed for real rollback.
(8) owner needs Clerk `publicMetadata.isAdmin` set for finance_api admin gating.

## Phase 5 — Storage/Parquet archive (LATER, out of scope)

Parquet `market-archive`/`sec-archive` → Oracle Object Storage. Do not couple to this split.

## Cross-cutting / open items

- [x] Q2 RESOLVED (2026-07-08): `error_log` is **SHARED, not moved** — the BLOG writes SSR/client
      errors to dashboard_server `/api/error-logs` → Supabase `error_log` (`dashboard_client_web`
      instrumentation.ts / reportSsrError / app/api/log-error). So blog KEEPS its `error_log` +
      `errorLogs` controller on Supabase, AND finance gets its **OWN** `error_log` on Oracle
      (finance_api's copied `errorLog.ts` + errorHandler write there). Same table NAME, two DBs —
      duplicate per-DB, NOT a move. → `packages/finance-migrations/0001_finance_init.sql` MUST
      include `error_log`. `batch_log` = finance-only (collector) → Oracle. Owner-confirmed.
- [ ] Q3 verify no blog/preview controller reads `market_snapshots` before moving it.
- [ ] Q4 classify migration 0032–0034 objects (blog vs financial).
- [ ] `ci.yml` — add `finance_api` to build/lint/typecheck graph (Turbo auto-discovers once it's a workspace).
